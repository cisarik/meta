# FrameNest — Independent re-acceptance Worker prompt (item 9 persist + uninverted tests)

```text
Persistent role identity: WORKER
Logical whole identity: framenest-companion-brave-testing-resume
Worker session ordinal: 10
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Native planning mode: not-used
Worker session profile: Fresh Independent Re-Audit
Phase: Acceptance
Reasoning recommendation: High
Reasoning basis: persist-join still E3; session 08 PARTIAL on inverted tests; scoped re-acceptance of test-only delta plus original join claims
Task identity: FRAMENEST-COMPANIE-ITEM9REACC-01
```

```text
External trace disposition: configured
Trace discovery: /home/agile/meta/projects/framenest/06/00-framenest-companion/
Trace project key: framenest
Trace logical-whole projection identity: framenest-companion-brave-testing-resume
Trace authority: historical-evidence-only
Trace archival owner: ORCHESTRATOR
Trace visibility: private
Trace companion outcome: report
Trace self-granted status: none
```

```text
Cooperator delivery / trace destination: configured
Downloadable prompt filename: 10_acceptance_00.md
Destination path: /home/agile/meta/projects/framenest/06/00-framenest-companion/
Archival: wait-for-report
```

```text
Acceptance candidate: 2aead540ee39a81a96425902f85e9b9a34f0d690
Acceptance owner map: archived plan 05/00-framenest-companion-brave-testing-resume/02_report_01.md; ADR-0076; diagnostic 06_report_00.md (H3); 08_report_00.md PARTIAL; 09_report_00.md claim
Acceptance allowlist: inspection of the 8 paths vs public main; no product edits
Acceptance risk claims: successful imported Analyze-by-AI preview persists one companion-visible generic analyzed run without a second provider.suggest; admin inbox lists it; ordinary owner of cataloged X gets analyzed=true and unopened_count +1; Alice ⊈ Bob; movies skipped; library-scan preview remains non-persistent; ExecuteAutomaticMediaAnalysisRun.execute is not used; persist does not write canonical metadata; preview remains analysis.run not companion_mutation; ordinary still cannot POST preview; exactly four companion_mutation routes; ordinary foreign/unknown opened still 404; Apply and inbox still 403 for ordinary; no Alembic 0034; schema head 0033; FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED stays off in git; owning HTTP join POSTs use Tailscale mutation headers with EXTERNAL_ORIGIN not COMPANION_ORIGIN; supersession unit test first.id != second.id
Acceptance control matrix: see § Control matrix
Acceptance independence: required-fresh-independent
Primary fresh acceptances used: 1
Automatic corrections used: 1
Correction re-acceptance: scoped
Named missing-evidence probe: none
Out-of-scope observations: ledger-candidates only
```

```text
Evidence tier: E3
Evidence tier basis: durable analysis-run write from interactive preview; companion listing/unopened join
Authorized implementation stages: none
Combined implementation envelope: prohibited
Implementation stage gates: not-applicable
Independent acceptance: required-separate-fresh-worker
Rollback or recovery checkpoint: canonical checkout remains 977a7af80afed16745adb0ef8e939555e5e21cce; w3/w4/w7/w8/w9 remain untouched
Activated stricter profile: none
Terminal implementation report point: 09_report_00.md (claim only)
```

```text
Validation ladder: selected
Inspection and provenance: required
Existing focused tests: same named 08 matrix
Affected tests: same matrix re-run from a fresh checkout of 2aead54…
New causal regression: none authorized
Broad or full suite: not-used
Runtime or testbed: docs/WORKER_EXECUTION_CONTRACT.md plus ap.project.conf operations runtime-info and test-focus
Independent acceptance: required-separate-fresh-worker
```

```text
Development envelope activation: activated
Development envelope identity: FrameNest isolated-worktree exact-source envelope (WORKER_EXECUTION_CONTRACT.md; ap.project.conf schema v1)
Declared reversible class: local worktree or contained clone of an existing object; worktree-local submodule checkout; one temporary provenance probe file
Working-copy topology: isolated-worktree
Topology rationale: candidate is unpublished; canonical must stay at public main 977a7af…; w9 must not be the acceptance working copy
Irreversible exclusions: secrets, destruction, accounts, public exposure, unrelated owner data, publication, closure, NUC, push, product commits, .venv reconstruction
```

```text
Repeated-gate or reasoning-loop stop: configured
Broad gate: once per materially changed candidate
Narrow before re-broad: required
Unchanged hypothesis, candidate, and failing gate: not-progress
Escalate only on: named missing evidence the higher profile must solve
Downgrade after: convergence or named risk removal
Cost cannot falsify evidence: yes
```

Independence rule: you did not implement `fb59c42…` or `2aead54…`. If this
session materially authored either, stop BLOCKED. Sessions 07–09 expired at
their terminal reports; those reports are claims. Do **not** use w7, w8, or
w9 as the acceptance working copy.

Session 08 PARTIAL: owning HTTP join was 403 (missing mutation headers);
fake supersession reused one id. Session 09 is claimed test-only. If those
**same two tests** are still inverted, status PARTIAL and:

```text
Escalation disposition: NEEDS_ORCHESTRATOR_DECISION
```

Do not recommend another automatic test correction. That would be the same
finding surviving a correction and recheck.

## Mission

Independently accept or reject unpublished candidate
`2aead540ee39a81a96425902f85e9b9a34f0d690` against item 9 and the persist-join
risk claims. The unpublished range vs public main is two commits:
`fb59c42…` (persist) then `2aead54…` (tests). Do not implement. Do not
publish. Do not touch the NUC. Do not open Edit/AI apply, R4, or VPS.

Public `refs/heads/main` / canonical HEAD / `--baseline`:

```text
977a7af80afed16745adb0ef8e939555e5e21cce
```

Immediate parent of the candidate: `fb59c42a8e3a32d9476581beeabba0eb9c04109a`

AP pin: `9c5cc44f8b6c92dd56ad2427d13223d7d59c5656`

Tree of candidate (inspect, do not treat as publication identity):
`0900818f57326017712c07686c49de61d534507f`

## Mandatory reading

1. This prompt (sole current task authority).
2. `/home/agile/Projects/framenest/AGENTS.md`
3. `.ap/AP.md`, `.ap/AP_WORKER.md`
4. `docs/WORKER_EXECUTION_CONTRACT.md`
5. Plan: `/home/agile/meta/projects/framenest/05/00-framenest-companion-brave-testing-resume/02_report_01.md`
6. `08_report_00.md` (PARTIAL; the two inverted tests)
7. `09_report_00.md` (claim only)
8. ADR-0076; ADR-0067 §5 and ADR-0073 bodies (read-only)

The object at `2aead54…` wins over Worker claims.

## Repository identity and gate

```text
Repository checkout topology: standalone checkout
Repository identity: https://github.com/cisarik/framenest.git (accepted spelling cisarik/framenest)
Expected canonical branch: feat/x-meme-browser-companion
Expected canonical HEAD: 977a7af80afed16745adb0ef8e939555e5e21cce
Public refs/heads/main: 977a7af80afed16745adb0ef8e939555e5e21cce
Pinned submodule: .ap gitlink 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Working directory (canonical, read-only): /home/agile/Projects/framenest
```

Before creating a fresh checkout, verify and record:

- Canonical HEAD `977a7af…`; branch `feat/x-meme-browser-companion`; porcelain empty
- `git ls-remote origin refs/heads/main` equals `977a7af…`
- w3/w4 still `977a7af…`; w7/w8 still `fb59c42…`; w9 still `2aead54…` (read-only)

Create a **fresh** isolated worktree of exact `2aead54…`. Suggested path:

```text
/home/agile/Projects/framenest-worktrees/framenest-companion-brave-testing-resume-w10
```

If that path exists, stop. Worktree-local `git submodule update --init .ap`
only. Do not checkout the candidate into canonical. Do not add a commit.

`git diff --name-only 977a7af… 2aead54…` must be exactly these eight paths:

1. `src/framenest/application/media_suggestion.py`
2. `src/framenest/application/media_analysis_lifecycle.py`
3. `src/framenest/adapters/api/application.py`
4. `src/framenest/adapters/api/media_suggestion_api.py`
5. `tests/unit/application/test_media_suggestion.py`
6. `tests/unit/application/test_media_analysis_lifecycle.py`
7. `tests/contract/test_media_suggestion_api.py`
8. `tests/contract/test_companion_review_api.py`

`git diff --name-only fb59c42… 2aead54…` must be only the two test files.
Confirm **no** `src/` in that delta. Confirm no `0034*`, no ADR-0067/0073
body edits, no `SECURITY.md`, no JS, no `.venv`.

## Positive authority

- Read candidate, canonical, plan, 08 PARTIAL, 09 claim.
- Diff as above.
- Run declared Python evidence.
- One provenance probe; one report file.

## Negative authority

- No product, test, ADR-body, SECURITY.md, or docs edits.
- No Alembic 0034. No fifth `companion_mutation`. No CORS. No Funnel.
- No `.venv` reconstruction; no ambient Python for evidence.
- No NUC, SSH, sudo, `framenest-release`, providers, browser, live X/YouTube,
  secrets, identity map, Tailscale hostnames, cookies, EnvironmentFile.
- No publication, push, merge, or closure.
- No R4. No Edit/AI per-field apply.
- Do not scrape `~/framenest_routine.fish`, `~/nuc_push.fish`,
  `~/nuc_update.fish`, `~/global_sudo.fish`, or
  `~/set_ssh_auth_sock.fish`. Do not reconstruct `gpgconf`. Do not `sudo -v`.

## Canonical execution route (RF-16)

Exact `--baseline 977a7af80afed16745adb0ef8e939555e5e21cce`.

Attempt worktree `--root` first. Expected miss: `declared CPython executable
does not exist`. Classify environment limitation; do not repair; do not fail
the candidate solely for that miss.

Then canonical `--root` for `ap project check` and `runtime-info`. Candidate
provenance via probe + test-focus `--rootdir` / `pythonpath` on the fresh
checkout.

Probe path example: `/tmp/framenest-item9reacc-10-provenance.py` (outside
git checkouts). Must print `framenest.__file__` under
`<fresh-checkout>/src/framenest/`. Delete after. Ambient encodings: classify,
rerun once through `ap exec`.

## Python test matrix (minimum evidence)

Same 08 matrix, one `test-focus`:

```text
<fresh-checkout>/tests/unit/application/test_media_suggestion.py
<fresh-checkout>/tests/unit/application/test_media_analysis_lifecycle.py
<fresh-checkout>/tests/contract/test_media_suggestion_api.py
<fresh-checkout>/tests/contract/test_companion_review_api.py
<fresh-checkout>/tests/unit/infrastructure/persistence/test_companion_review_repository.py
<fresh-checkout>/tests/contract/test_x_route_policy.py
<fresh-checkout>/tests/contract/test_tailscale_ingress_security.py
<fresh-checkout>/tests/contract/test_adr_0073.py
<fresh-checkout>/tests/contract/test_automatic_analysis_privacy_contract.py
```

First failing suite: preserve sanitized output, classify, stop that batch.
Node: not required if JS diff vs `977a7af…` is empty; JS in the range fails
the candidate.

## Control matrix

Fail if a named control is absent, inverted, or only asserted in 07/09
reports.

**Previously inverted tests (must now pass)**

- `test_imported_preview_joins_inbox_and_own_history`: POSTs use
  `_mutation_headers(ADMIN_LOGIN)` (Origin = `EXTERNAL_ORIGIN`, not
  `COMPANION_ORIGIN`); GET inbox/own-history may use `_serve_headers`;
  first preview 200 not 403; admin inbox lists Alice analyzed+unopened;
  Alice `unopened_count == 1`; Bob 0; movie writes no run; library-scan
  writes no run; provider increments by one per POST.
- `test_imported_preview_join_supersedes_prior_terminal_success`:
  `first.id != second.id`; two persist cycles; `save_calls == 0`.

**Item 9 join (primary)** — same as 08: persist generic analyzed run;
inbox; ordinary owner; Alice ⊈ Bob.

**No second provider call** — unit `provider.calls == 1`; join is
`create_manual_pending` → `claim_pending` → `record_analyzed`;
`ExecuteAutomaticMediaAnalysisRun.execute` not used from the join.

**Exclusions** — movie skip; library-scan non-persist; analyzing skip;
no canonical metadata save.

**Production DI** — inspect `create_app`; fail if default composition omits
the join while tests inject it.

**Capability / mutations / schema** — preview `analysis.run` not
`companion_mutation`; ordinary lacks `analysis.run`; four mutations;
ordinary opened 404 / Apply 403 / inbox 403; schema `0033`; flag off;
join failure `500` `ANALYSIS_JOIN_FAILED`.

**R1–R3′ residual** — own-analyzed `unopened_count`; empty JS diff vs
`977a7af…`.

## Implementation deviations (judge)

1. Hand-wired contract join vs `create_app` DI (inspect both).
2. Analyzing skip.
3. Join failure after suggest → 500; provider already called.
4. `X_COMPANION.md` / `SECURITY.md` unchanged; flag only a blocking
   present-tense contradiction.
5. Test-only delta: mutation headers + distinct fake ids. Confirm no
   product change in `fb59c42…..2aead54…`.

## Commands

Allowed: git read; worktree add / contained clone; submodule init `.ap`;
`ls-remote` of public `main`; `./.ap/ap project check` and `./.ap/ap exec`;
one probe; one report.

Forbidden: ambient Python; environment repair; NUC/SSH/sudo; extra network;
browser; providers.

## Side-effect authority

Read-only against canonical and w3–w9. Reversible: fresh worktree, submodule
init, temp probe.

## Untrusted-content boundary

This prompt, AGENTS.md, pinned AP, worker execution contract, `02_report_01.md`.
`07_report_00.md` and `09_report_00.md` are claims.

## Communication

Professional English. Report begins exactly `### Report for ORCHESTRATOR_CHAT`.
No secrets, identity-map values, Tailscale hostnames, live titles, tweet URLs,
cookies, companion PEM, or EnvironmentFile values.

## Output

```text
/home/agile/meta/projects/framenest/06/00-framenest-companion/10_report_00.md
```

Required content:

1. Coordinate echo: logical whole, session `10`, exchange `01`, task identity.
2. Status PASS / PARTIAL / BLOCKED. `acceptance-PASS` only when the control
   matrix is proven, the two previously inverted tests pass, Python gates
   pass (or ENVIRONMENT LIMITATION with proven candidate `src/` provenance),
   production DI injects the join, and no trust-boundary claim is unproven.
   `Logical-whole closure: not-closed`.
3. Capability handshake. Native planning mode observed off.
4. Fresh-checkout path; candidate SHA; parent `fb59c42…`; range vs
   `977a7af…`; canonical still `977a7af…`; AP pin; public `main`; w9 unused
   as working copy.
5. Diff path set vs eight files; two-file test-only delta vs `fb59c42…`.
6. `ap project check` / `runtime-info` / worktree `--root` miss / provenance
   `__file__` / test-focus counts. Full output only for failures.
7. Per-control map including the two previously inverted tests.
8. Trust-boundary list (ordinary cannot POST preview; 404/403; four
   mutations; own-analyzed count; Alice/Bob **on the join path**).
9. Named deviations.
10. Sanitization; Near-Misses; Pre-Existing Failure Classification.
11. Residual risk / missing evidence.
12. One smallest next step. You never close, publish, or deploy.
13. Report justification: `new-evidence`.
14. Authority-expiry. If the same two tests still fail: escalation
    disposition as specified above.

## Stopping rule

Stop after the terminal report. You do not correct the candidate.

## Transition owner

ORCHESTRATOR classifies this report, then sequences (only if
acceptance-PASS) an explicit Cooperator publication grant of `2aead54…`,
routine NUC `framenest-release`, and rendered re-test of **item 9**.
