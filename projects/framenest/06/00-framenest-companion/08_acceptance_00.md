# FrameNest — Independent acceptance Worker prompt (item 9 persistence-join)

```text
Persistent role identity: WORKER
Logical whole identity: framenest-companion-brave-testing-resume
Worker session ordinal: 08
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Native planning mode: not-used
Worker session profile: Fresh Independent Audit
Phase: Acceptance
Reasoning recommendation: High
Reasoning basis: durable analysis-run write from previously non-persistent Analyze-by-AI preview; companion inbox/own-history join; E3
Task identity: FRAMENEST-COMPANIE-ITEM9ACC-01
```

```text
External trace disposition: configured
Trace discovery: /home/agile/meta/projects/framenest/06/00-framenest-companion/
Trace project key: framenest
Trace logical-whole projection identity: framenest-companion-brave-testing-resume
Trace authority: historical-evidence-only
Trace archival owner: ORCHESTRATOR
Trace visibility: private
Trace companion outcome: report
Trace self-granted status: none
```

```text
Cooperator delivery / trace destination: configured
Downloadable prompt filename: 08_acceptance_00.md
Destination path: /home/agile/meta/projects/framenest/06/00-framenest-companion/
Archival: wait-for-report
```

```text
Acceptance candidate: fb59c42a8e3a32d9476581beeabba0eb9c04109a
Acceptance owner map: archived plan 05/00-framenest-companion-brave-testing-resume/02_report_01.md; ADR-0076; diagnostic 06_report_00.md (H3); correction claim 07_report_00.md
Acceptance allowlist: inspection of the 8 committed paths only; no product edits
Acceptance risk claims: successful imported Analyze-by-AI preview persists one companion-visible generic analyzed run without a second provider.suggest; admin inbox lists it; ordinary owner of cataloged X gets analyzed=true and unopened_count +1; Alice ⊈ Bob; movies skipped; library-scan preview remains non-persistent; ExecuteAutomaticMediaAnalysisRun.execute is not used; persist does not write canonical metadata; preview remains analysis.run not companion_mutation; ordinary still cannot POST preview; exactly four companion_mutation routes; ordinary foreign/unknown opened still 404; Apply and inbox still 403 for ordinary; no Alembic 0034; schema head 0033; FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED stays off in git
Acceptance control matrix: see § Control matrix
Acceptance independence: required-fresh-independent
Primary fresh acceptances used: 1
Automatic corrections used: 1
Correction re-acceptance: full-fresh
Named missing-evidence probe: none
Out-of-scope observations: ledger-candidates only
```

```text
Evidence tier: E3
Evidence tier basis: durable analysis-run write from interactive preview; companion listing/unopened join
Authorized implementation stages: none
Combined implementation envelope: prohibited
Implementation stage gates: not-applicable
Independent acceptance: required-separate-fresh-worker
Rollback or recovery checkpoint: canonical checkout remains 977a7af80afed16745adb0ef8e939555e5e21cce; w3/w4/w7 remain untouched
Activated stricter profile: none
Terminal implementation report point: 07_report_00.md (claim only)
```

```text
Validation ladder: selected
Inspection and provenance: required
Existing focused tests: join and isolation files named in § Python test matrix
Affected tests: same matrix re-run from a fresh checkout of fb59c42…
New causal regression: none authorized
Broad or full suite: not-used
Runtime or testbed: docs/WORKER_EXECUTION_CONTRACT.md plus ap.project.conf operations runtime-info and test-focus
Independent acceptance: required-separate-fresh-worker
```

```text
Development envelope activation: activated
Development envelope identity: FrameNest isolated-worktree exact-source envelope (WORKER_EXECUTION_CONTRACT.md; ap.project.conf schema v1)
Declared reversible class: local worktree or contained clone of an existing object; worktree-local submodule checkout; one temporary provenance probe file
Working-copy topology: isolated-worktree
Topology rationale: candidate is unpublished; canonical must stay at public main 977a7af…; implementation worktree w7 must not be the acceptance working copy
Irreversible exclusions: secrets, destruction, accounts, public exposure, unrelated owner data, publication, closure, NUC, push, product commits, .venv reconstruction
```

```text
Repeated-gate or reasoning-loop stop: configured
Broad gate: once per materially changed candidate
Narrow before re-broad: required
Unchanged hypothesis, candidate, and failing gate: not-progress
Escalate only on: named missing evidence the higher profile must solve
Downgrade after: convergence or named risk removal
Cost cannot falsify evidence: yes
```

Independence rule: you did not implement `fb59c42…`. If this session
materially authored that commit, stop and report BLOCKED (independence
conflict). Session 07 authority expired at `07_report_00.md`. That report is
a claim, not proof. Do not use
`/home/agile/Projects/framenest-worktrees/framenest-companion-brave-testing-resume-w7`
as the acceptance working copy.

## Mission

Independently accept or reject unpublished candidate
`fb59c42a8e3a32d9476581beeabba0eb9c04109a` against item 9 (ordinary
own-history unopened after Analyze by AI of an owned cataloged X item) and
the R1–R3′ risk claims this persist could affect. Do not implement product
code. Do not publish. Do not touch the NUC. Do not open Edit/AI per-field
apply, R4, or VPS.

Parent / public `refs/heads/main` / canonical HEAD / `--baseline`:

```text
977a7af80afed16745adb0ef8e939555e5e21cce
```

AP pin (superproject gitlink and `.ap` HEAD):

```text
9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
```

## Mandatory reading

1. This prompt (sole current task authority).
2. `/home/agile/Projects/framenest/AGENTS.md`
3. `.ap/AP.md`, `.ap/AP_WORKER.md` (pin above)
4. `docs/WORKER_EXECUTION_CONTRACT.md`
5. Plan: `/home/agile/meta/projects/framenest/05/00-framenest-companion-brave-testing-resume/02_report_01.md` (R3′ join)
6. Diagnostic claim: `…/06/00-framenest-companion/06_report_00.md` (H3)
7. Implementation claim to verify, not believe: `…/06/00-framenest-companion/07_report_00.md`
8. Candidate ADR-0076; ADR-0067 §5 and ADR-0073 bodies (read-only; do not edit)

Where this prompt and the repository object conflict, the object wins. Fail
the candidate if the join is missing, a second `provider.suggest` occurs per
preview, a fifth `companion_mutation` appears, schema 0034 appears, or
ordinary callers can write analysis runs through this path.

## Repository identity and gate

```text
Repository checkout topology: standalone checkout
Repository identity: https://github.com/cisarik/framenest.git (accepted spelling cisarik/framenest)
Expected canonical branch: feat/x-meme-browser-companion
Expected canonical HEAD: 977a7af80afed16745adb0ef8e939555e5e21cce
Public refs/heads/main: 977a7af80afed16745adb0ef8e939555e5e21cce
Pinned submodule: .ap gitlink 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Working directory (canonical, read-only): /home/agile/Projects/framenest
```

Before creating a fresh checkout, verify and record:

- Canonical `git rev-parse HEAD` equals `977a7af80afed16745adb0ef8e939555e5e21cce`
- Branch `feat/x-meme-browser-companion`; porcelain empty
- Credential-free `git ls-remote origin refs/heads/main` equals that SHA
- w3 and w4 still `977a7af…`; w7 still `fb59c42…` (read-only evidence; do not mutate)

Create a **fresh** isolated worktree of exact `fb59c42…`. Suggested path:

```text
/home/agile/Projects/framenest-worktrees/framenest-companion-brave-testing-resume-w8
```

If that path exists, stop. `git submodule update --init .ap` worktree-local
only. Do not checkout the candidate into canonical. Do not add a second
commit.

Claimed path set (must match `git diff --name-only 977a7af… fb59c42…`):

1. `src/framenest/application/media_suggestion.py`
2. `src/framenest/application/media_analysis_lifecycle.py`
3. `src/framenest/adapters/api/application.py`
4. `src/framenest/adapters/api/media_suggestion_api.py`
5. `tests/unit/application/test_media_suggestion.py`
6. `tests/unit/application/test_media_analysis_lifecycle.py`
7. `tests/contract/test_media_suggestion_api.py`
8. `tests/contract/test_companion_review_api.py`

Confirm **no** `alembic_environment/versions/0034*`, no ADR-0067/0073 body
edits, no `SECURITY.md`, no JS/extension, no Edit/Details chrome, no
`.venv`. Parent of `fb59c42…` must be `977a7af…`.

## Positive authority

- Read the candidate tree, canonical tree, plan, diagnostic, and
  implementation claim.
- Diff `fb59c42…` against `977a7af…`. Confirm the eight paths.
- Run the declared Python evidence below.
- Write exactly one report file (path in Output).
- Create and delete one temporary provenance probe file as specified.

## Negative authority

- No product, test, ADR-body, SECURITY.md, or docs edits.
- No Alembic 0034. No fifth `companion_mutation`. No CORS. No Funnel.
- No `.venv` create/copy/symlink/reconstruct; no ambient
  `.venv/bin/python` / `python` / `python3` / `poetry run` for Python
  evidence.
- No NUC, SSH, sudo, `framenest-release`, provider calls, browser, live
  X/YouTube, secrets, identity map, Tailscale hostnames, cookies, or
  EnvironmentFile values.
- No publication, push, merge to main, or logical-whole closure.
- No R4. Do not enable `FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED`.
- No Edit/AI per-field apply UX.
- Do not scrape `~/framenest_routine.fish`, `~/nuc_push.fish`,
  `~/nuc_update.fish`, `~/global_sudo.fish`, or
  `~/set_ssh_auth_sock.fish`. Do not reconstruct `gpgconf`. Do not `sudo -v`.

## Canonical execution route (RF-16)

All Python evidence goes through declared `ap.project.conf` operations with
exact `--baseline 977a7af80afed16745adb0ef8e939555e5e21cce`.

Declared route to attempt first (expected isolated-worktree limitation):

```text
./.ap/ap project check --root <fresh-checkout> \
  --baseline 977a7af80afed16745adb0ef8e939555e5e21cce
./.ap/ap exec --root <fresh-checkout> \
  --baseline 977a7af80afed16745adb0ef8e939555e5e21cce --operation runtime-info
./.ap/ap exec --root <fresh-checkout> \
  --baseline 977a7af80afed16745adb0ef8e939555e5e21cce \
  --operation test-focus -- <selection> -q -p no:cacheprovider
```

If those fail with `declared CPython executable does not exist`, classify
**environment limitation**. Do not repair. Do not fail the candidate solely
for that miss.

### Explicit bounded deviation (only after that classified miss)

```text
./.ap/ap project check --root /home/agile/Projects/framenest \
  --baseline 977a7af80afed16745adb0ef8e939555e5e21cce
./.ap/ap exec --root /home/agile/Projects/framenest \
  --baseline 977a7af80afed16745adb0ef8e939555e5e21cce --operation runtime-info
```

Canonical `runtime-info` proves the envelope, not the candidate. Candidate
provenance is the probe below, then test-focus with the same `pythonpath` /
`--rootdir`.

Stopping condition: if candidate `framenest.__file__` cannot be proven under
`<fresh-checkout>/src/`, record ENVIRONMENT LIMITATION for Python provenance;
do not fail the candidate solely for the known launch-path miss; do fail it
if tests fail or trust-boundary claims are unproven for a candidate-defect
reason.

Ambient-route encodings signature: classify, rerun once through the same
`ap exec` operation, never rebuild environments.

### Provenance probe (temporary)

Write one file **outside** both git checkouts, for example:

```text
/tmp/framenest-item9acc-08-provenance.py
```

Contents must print `framenest.__file__` and assert it resolves under
`<fresh-checkout>/src/framenest/`. Then:

```text
./.ap/ap exec --root /home/agile/Projects/framenest \
  --baseline 977a7af80afed16745adb0ef8e939555e5e21cce \
  --operation test-focus -- \
  --rootdir <fresh-checkout> \
  -o pythonpath=<fresh-checkout>/src \
  /tmp/framenest-item9acc-08-provenance.py \
  -q -p no:cacheprovider
```

Delete that file. Report the printed path and cleanup. Do not leave the probe
in any FrameNest worktree.

## Python test matrix (minimum evidence)

One `test-focus` (canonical `--root` plus classified `pythonpath` /
`--rootdir` if required):

```text
<fresh-checkout>/tests/unit/application/test_media_suggestion.py
<fresh-checkout>/tests/unit/application/test_media_analysis_lifecycle.py
<fresh-checkout>/tests/contract/test_media_suggestion_api.py
<fresh-checkout>/tests/contract/test_companion_review_api.py
<fresh-checkout>/tests/unit/infrastructure/persistence/test_companion_review_repository.py
<fresh-checkout>/tests/contract/test_x_route_policy.py
<fresh-checkout>/tests/contract/test_tailscale_ingress_security.py
<fresh-checkout>/tests/contract/test_adr_0073.py
<fresh-checkout>/tests/contract/test_automatic_analysis_privacy_contract.py
```

First failing suite: preserve sanitized output, classify
(candidate / harness / ambient-route / environment / acceptance-limitation),
stop that batch. Do not continue into a green narrative over a red gate.

Node: **not required** if `git diff --name-only` contains no JS. If JS is
present, fail the candidate for allowlist violation unless you can prove it
is not in the commit.

## Control matrix (must map to owning tests or inspection)

Fail if a named control is absent, inverted, or only asserted in
`07_report_00.md`.

**Item 9 join (primary)**

- Successful imported `POST …/ai-suggestion-preview` persists
  `automatic_post_catalog` / `generic_media` / `analyzed`.
- Admin inbox lists that media; `analyzed=true`, `unopened=true`.
- Ordinary **owner** of cataloged X: own-history `analyzed=true`,
  `unopened=true`, `unopened_count == 1`.
- Bob does not see Alice’s media; Bob `unopened_count == 0`.
- Owning test claimed: `test_imported_preview_joins_inbox_and_own_history`.

**No second provider call**

- One `provider.suggest` per imported preview (`provider.calls == 1` on the
  unit path; contract increments by one per POST).
- `PersistImportedPreviewAnalysis` transactions are
  `create_manual_pending` → `claim_pending` → `record_analyzed`.
- `ExecuteAutomaticMediaAnalysisRun.execute` is not invoked from the join
  (inspect call graph; unit test
  `test_imported_preview_join_records_generic_analyzed_run_without_executor`).

**Exclusions**

- Movie `content_category`: preview JSON still returns; no new run; absent
  from inbox.
- Library-scan `PreviewMediaSuggestion` does not write `media_analysis_runs`.
- Active `analyzing` run: join skips persist (does not steal the row);
  preview JSON still returns
  (`test_imported_preview_join_skips_when_latest_active_run_is_analyzing`).
- Persist does not `save` canonical metadata (`metadata.save_calls == 0`).

**Production DI (inspection, not only the hand-wired contract test)**

- `create_app` injects `PersistImportedPreviewAnalysis` into
  `PreviewImportedMediaSuggestion` when the provider and run/metadata
  repositories exist.
- The contract join test may construct the use case by hand; that does **not**
  replace inspecting `application.py`. If default composition omits the join
  while tests inject it, **fail** the candidate.

**Capability / mutations / schema**

- Preview route remains `capability=analysis.run`, not `companion_mutation`.
- Ordinary role still lacks `analysis.run` (cannot trigger this persist).
- Exactly four `companion_mutation` routes. GET own-history is not a
  mutation.
- Ordinary foreign/unknown opened 404; Apply 403; inbox list/detail 403.
- Schema head `0033`; no `0034*`. Automatic-analysis flag stays off in git.
- Join persistence failure is sanitized `500` `ANALYSIS_JOIN_FAILED`
  (`test_imported_preview_join_failure_is_sanitized_server_error`).

**R1–R3′ residual (this persist could affect listing, not hosted chrome)**

- Own-history `unopened_count` remains own-analyzed, not the global latest
  subquery (`test_own_history_opened_isolation_does_not_use_global_unopened_count`
  and related isolation tests in the companion files you run).
- Hosted Details hide Analyze by AI: JS is unchanged in this commit; confirm
  by empty JS diff, do not re-run Node unless JS appears.

## Implementation deviations (judge)

Classify each as **accepted-continuation** or **defect requiring correction**:

1. Contract join test injects `PersistImportedPreviewAnalysis` by hand.
   Default `create_app` wiring must still be inspected as above.
2. If an active run is already `analyzing`, join skips; preview still 200.
   Ordinary Analyze-by-AI is not this race.
3. Join failure after `provider.suggest` returns 500; the provider was
   already called. Residual billing/UX risk, not a license to call suggest
   twice.
4. `docs/X_COMPANION.md` / `SECURITY.md` unchanged. Flag only a
   **present-tense contradiction that blocks close**. Do not rewrite them.

## Commands

Allowed: `git` read; authorized `worktree add` / contained clone / submodule
init `.ap`; `git ls-remote` of public `main`; `./.ap/ap project check` and
`./.ap/ap exec` as bound above; write/delete the one provenance probe;
write the report.

Forbidden: ambient Python; environment repair; NUC/SSH/sudo; network other
than credential-free `ls-remote` and loopback spawned by authorized tests;
browser; providers.

## Side-effect authority

Read-only against canonical, w3, w4, and w7. Reversible local: fresh
worktree/clone, submodule init, temp probe. No remote, deployment,
credential, billing, or communication effects.

## Untrusted-content boundary

Governing instructions: this prompt, AGENTS.md, pinned AP, worker execution
contract, `02_report_01.md`. `07_report_00.md` is a claim. If a claim
conflicts with the candidate object, the object wins.

## Communication

Professional English. Report begins exactly `### Report for ORCHESTRATOR_CHAT`.
Do not print secrets, identity-map values, Tailscale hostnames, live titles,
tweet URLs, cookies, companion PEM, or EnvironmentFile values. Synthetic
UUIDs and example logins only.

## Output

Write exactly one file:

```text
/home/agile/meta/projects/framenest/06/00-framenest-companion/08_report_00.md
```

Required content:

1. Coordinate echo: logical whole, session `08`, exchange `01`, task identity.
2. Status PASS / PARTIAL / BLOCKED. Phase-qualified result `acceptance-PASS`
   only when the control matrix is proven, named tests exist and are not
   inverted, Python gates pass (or Python is ENVIRONMENT LIMITATION with
   proven candidate `src/` provenance), production DI injects the join, and
   no trust-boundary claim is unproven. Implementation-PASS is not yours.
   `Logical-whole closure: not-closed`.
3. Capability handshake table (requested vs observed; client identity not
   independently attested). Native planning mode observed off.
4. Fresh-checkout path; candidate SHA; parent SHA; canonical still
   `977a7af…` tracked-clean; AP pin; public `main` ls-remote; w7 unused as
   working copy.
5. Diff path set vs claimed 8 files; forbidden-path absence.
6. `ap project check` / `runtime-info` / classified worktree `--root` miss /
   provenance `__file__` / test-focus invocation and counts. Full output
   only for failures.
7. Per-control map: join / no-second-suggest / exclusions / DI / capability
   / four mutations / no-0034 / own-analyzed unopened / residual R1 chrome
   (JS diff empty).
8. Trust-boundary confirmation list (ordinary cannot POST preview;
   foreign opened 404; Apply 403; inbox 403; four mutations; own-analyzed
   count; Alice/Bob).
9. Classification of the named deviations.
10. Sanitization compliance.
11. Resolved Execution Issues / Near-Misses; Pre-Existing Failure
    Classification.
12. Deviations, residual risk, missing evidence.
13. One smallest next step (ORCHESTRATOR: accept / one bounded correction /
    escalate). You never close, publish, or deploy.
14. Report justification: `new-evidence`.
15. Authority-expiry statement.

## Stopping rule

Stop after the terminal report. Stop earlier as BLOCKED on independence
conflict, failed repository gate, unauthorized surface, unclassifiable
failure, or a trust-boundary claim you cannot prove.

You do not correct the candidate.

## Transition owner

ORCHESTRATOR classifies this report, then sequences (only if
acceptance-PASS) an explicit Cooperator publication grant of `fb59c42…`,
routine NUC `framenest-release`, and rendered re-test of **item 9**. Those
surfaces are inactive here.
