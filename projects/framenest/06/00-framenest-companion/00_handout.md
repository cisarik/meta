# Fresh Agent Orchestrator Restoration

## FrameNest 06/00 — companion history R1–R3′ closeout

You are a **fresh** terminal-capable Agent Orchestrator for FrameNest under
Analytic Programming. Load **this file completely** before any Worker prompt.
It is not a Worker prompt, not a repository plan, not a mutation receipt, and
not a substitute for the pinned AP documents. Restoration transfers
information, **never authority**.

The predecessor Orchestrator of Meta era
`05/00-framenest-companion-brave-testing-resume` **crashed after issuing and
receiving an implementation-PASS report**, before independent verification,
publication, NUC refresh, or Cooperator rendered re-test. The logical whole is
**not-closed**. The stall is operational, not a missing product idea.

Your mission: **close that whole**. Do not replan R1–R3′ from zero. Do not
open R4, VPS, or a new companion chrome rewrite inside this restore. Verify
the unpublished candidate, run fresh independent acceptance, then sequence
Cooperator publication, routine NUC refresh, and rendered admin/ordinary
re-test until Michal’s UX PASS (or an explicit waiver of remainder).

If a later explicit Michal message contradicts this file, the later message
wins. Record the conflict. Do not paper over it.

```text
STOP: Do not execute 03/03–03/10 00_handout*.md, 04/00 00_handout.md, or
      05/00 00_handout.md as live product authority. They are historical
      / parked evidence. Do not resume 03/10 Worker ordinals 20+ or
      05 Worker sessions 01–03.
STOP: Do not implement FrameNest product code yourself. Issue Workers.
STOP: Do not treat 03_report_01.md as proof. It is a Worker claim.
STOP: Do not treat 01_report_00.md mixed-inbox / ordinary-403-hides-history
      invariants as living product truth. R2 and R3′ superseded them.
      02_report_01.md is the normative R1–R3′ plan.
STOP: Do not start a Planner from a blank page. The decision-ready plan
      already exists. The missing work is verification → independent
      acceptance → publication → NUC → rendered re-test.
STOP: Do not push, merge to public main, or NUC-refresh unpublished
      977a7af… without an explicit Cooperator grant for that exact SHA.
STOP: Do not deploy a SHA that is not public refs/heads/main. AGENTS.md:
      rendered companion testing is against public main AND the NUC.
STOP: Do not checkout the candidate into /home/agile/Projects/framenest.
      Canonical stays at public main until a publication grant says how
      to integrate. Use the isolated worktree or a fresh clone of 977a7af.
STOP: Do not symlink, copy, or reconstruct .venv to make ap exec --root
      <worktree> pass. Classify the miss; keep the declared AP envelope.
STOP: Do not add Alembic 0034. Schema head stays 0033. Per-actor opened
      already lives in 0031 PK (actor_login_key, media_id).
STOP: Do not edit ADR-0073 or ADR-0067 bodies. Successor is ADR-0076
      (unpublished, in the candidate).
STOP: Do not give ordinary identities analysis.run, metadata.canonical.write,
      media.content.publish, media.workflow.read, inbox list/detail, or Apply.
STOP: Do not add a fifth companion_mutation route. Opened stays the same
      fourth route. GET /api/companion/own-history is not a mutation.
STOP: Do not enable FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED in git or
      from a Worker. NUC env is Cooperator ops. R4 (Settings checkbox) is
      the NEXT whole after this one closes, unless Michal folds it in.
STOP: Do not scrape ~/framenest_routine.fish, ~/nuc_update.fish,
      ~/global_sudo.fish, or ~/set_ssh_auth_sock.fish for host, user,
      identity, sockets, or secrets. Do not print those values.
STOP: Do not reconstruct gpgconf. Do not sudo -v. Do not handle a password.
STOP: Do not Funnel /run/framenest/framenest.sock. Do not router-forward.
      VPS/public-net deployment remains FROZEN.
STOP: Do not use Max unless Michal selects it. High for independent
      acceptance (trust-boundary). Extra High is not the default.
STOP: Communicate with Michal in Slovak. Feminine self-reference.
      Masculine address for him. Repository artifacts professional English.
```

Paste seed for a new Agent chat (pointer only, not durable authority):

```text
Resume this AP-integrated FrameNest project as a fresh Orchestrator.
Read /home/agile/meta/projects/framenest/06/00-framenest-companion/00_handout.md
completely before any Worker prompt.
Begin read-only. Restore canonical state and the declared AP upgrade ledger.
Do not mutate. Do not implement product code.
The previous Orchestrator crashed after companion-history implementation-PASS
at unpublished 977a7af80afed16745adb0ef8e939555e5e21cce. Public main remains
91410fe063d9907304cff4550f61d403880a2eeb. Close logical whole
framenest-companion-brave-testing-resume: verify the candidate, independent
acceptance, then publication + NUC refresh + rendered R1–R3′ re-test.
Do not replan from zero. Do not open R4 or VPS inside this whole.
Communicate with Michal in Slovak. High. No Max.
```

Continuation Bootstrap (AP pointer, not this whole’s authority):

```text
Resume this AP-integrated project.
Read the root AGENTS.md and the pinned AP documents it names.
Begin read-only. Restore canonical state and any declared AP upgrade ledger.
With the COOPERATOR, select exactly one bounded next logical whole before any
mutation authority is issued.
```

For **this** restore, Stage 2 is already satisfied: Michal selected companion
Brave-testing resume, then R1–R3′, then “close this logical whole.” Do not
ask him to pick a different object unless he changes the objective.

---

## 0. Handoff identity

```text
Role: ORCHESTRATOR
Orchestrator session target: fresh-agent-orchestrator-session
Orchestrator profile: terminal-capable repository and operations coordinator
Orchestrator handoff artifact:
  06/00-framenest-companion/00_handout.md
Predecessor Orchestrator artifacts (historical evidence, not live authority):
  05/00-framenest-companion-brave-testing-resume/
    especially 00_handout.md, 01_addendum_2026-08-26.md,
    08_orchestrator_notes.md, 02_report_01.md, 03_implementation_00.md,
    03_report_01.md
  05/00-framenest-nuc-push-workflow-and-companion-testing/ (CLOSED)
  04/00-framenest-public-published-surface-and-tailscale-workspace/ (PARKED)
  03/10-framenest-companion-review-inbox-ux-history-mvp/ (not-closed parent;
    Brave backlog unparked into 05; do not resume 03/10 ordinals)
Logical whole identity (continue; do not mint a new kebab):
  framenest-companion-brave-testing-resume
Next Worker session ordinal: 04
Next Worker exchange ordinal: 01
Worker session target for first prompt: fresh-worker-session
  (independent acceptance; implementer of session 03 must not accept)
Current phase: Restoration → independent acceptance of unpublished candidate
Native planning mode: not-used (plan already exists)
Reasoning recommendation: High (opened capability is a trust-boundary edit)
Internal delegation posture: one accountable active Worker by default
Cooperator: Michal
Primary consumer repository: cisarik/framenest
Pinned protocol repository: cisarik/ap through the FrameNest .ap gitlink
External analytic trace: cisarik/meta
Parent logical wholes (not closed; do not close from here):
  framenest-x-meme-browser-companion-mvp
  framenest-x-companion-save-alias-mvp
  framenest-x-companion-save-category-mvp
  framenest-x-save-overlay-edit-subset-mvp
  framenest-companion-ai-review-inbox-mvp
  framenest-companion-review-inbox-ux-history-mvp
```

Trace folder for **this Orchestrator era** (create Worker files here):

```text
/home/agile/meta/projects/framenest/06/00-framenest-companion/
```

Keep 05/00 as the evidence archive of sessions 01–03. Do not rewrite those
files. Do not continue their prompt ordinals. Session 04+ lives here.

---

## 1. Restoration classification

**PARTIAL**

Useful continuity exists: public main, AP pin, schema head, decision-ready
plan, unpublished candidate commit, expired Worker authorities, and a
clear closeout sequence. Material uncertainty remains: session-03 report
is unverified; NUC active SHA was last attested earlier on 2026-08-26 as
equal to public main and must be re-read; `SECURITY.md` was not in the
implementation allowlist and still lacks ADR-0076 / own-history sentences.

Not BLOCKED: no in-flight mutation on the canonical checkout; no live
Worker authority; candidate worktree is clean.

This classification is a synthesis for the incoming Orchestrator. Re-run
the §2 gates yourself. Do not inherit this file as proof.

---

## 2. Canonical state at rotation (verify read-only at start)

Observed 2026-08-26 by the restoration writer. **Re-verify.** Treat every
row as stale until you observe it.

| Fact | Last observed value | How to re-verify |
|---|---|---|
| Canonical checkout | `/home/agile/Projects/framenest` | `git -C … rev-parse --show-toplevel` |
| Canonical branch | `feat/x-meme-browser-companion` | `git rev-parse --abbrev-ref HEAD` |
| Canonical HEAD | `91410fe063d9907304cff4550f61d403880a2eeb` | `git rev-parse HEAD` |
| Canonical index | tracked-clean | `git status --porcelain=v1` |
| Public `refs/heads/main` | **same 40-hex** `91410fe…` | credential-free `git ls-remote https://github.com/cisarik/framenest.git refs/heads/main` |
| Tracking curiosity | canonical branch **ahead 26** of `origin/feat/x-meme-browser-companion` | informational; **publication target is public main**, not that stale tracking ref. Do not “fix” it as a side quest. |
| AP gitlink + `.ap` HEAD | `9c5cc44f8b6c92dd56ad2427d13223d7d59c5656` | `git rev-parse :.ap` and `git -C .ap rev-parse HEAD` |
| Public `cisarik/ap` `main` | **same pin** `9c5cc44…` | `git ls-remote https://github.com/cisarik/ap.git refs/heads/main` |
| Schema head | `0033` (`media_analysis_proposals`) | versions dir + `test_current_schema_head_is_0033` |
| Candidate worktree | `/home/agile/Projects/framenest-worktrees/framenest-companion-brave-testing-resume-w3` | `git worktree list` |
| Candidate branch | `feat/companion-history-r1-r3prime` (no upstream) | `git status -sb` |
| Candidate HEAD | `977a7af80afed16745adb0ef8e939555e5e21cce` | `git rev-parse HEAD` |
| Candidate parent | `91410fe…` | `git rev-parse HEAD^` |
| Candidate vs public | **not on main**; descendant of `91410fe…` | `git merge-base --is-ancestor` |
| Candidate subject | `feat: hosted companion history with analyzed inbox and ordinary own-history` | `git log -1` |
| Live NUC SHA | last attested equal to `91410fe…` / schema `0033` | Cooperator `framenest-release status` only; do not invent deploy commands |

Do not treat `977a7af…` as live NUC or public `main`.

---

## 3. Stall diagnosis (why the previous Orchestrator died here)

Era 05 unparked companion Brave testing against a NUC already on public
`91410fe…`. Deterministic acceptance of **that SHA** passed (sessions 01/01
and 01/02). Rendered testing then produced product defects and Cooperator
amendments R1, R2, R3→R3′. A Planner produced a decision-ready plan
(`02_report_01.md`). Implementation Worker 03 produced one local commit in
an isolated worktree and expired.

The next AP transitions were never executed:

1. Orchestrator verification of `03_report_01.md` against repository objects.
2. Fresh **independent** acceptance (E3: opened ingress widened from
   `media.workflow.read` to `x.request` plus ownership 404).
3. Cooperator **publication** grant for exact SHA `977a7af…`.
4. Routine NUC `framenest-release` to that **public** SHA.
5. Cooperator rendered re-test of R1 / R2 / R3′.
6. Closure of `framenest-companion-brave-testing-resume`.

That is the zasek. Context death, not an empty backlog.

---

## 4. What era 05 actually completed (context, not authority)

Classify each row. Worker reports are claims.

| Item | Classification | Evidence |
|---|---|---|
| Public main = `91410fe…` including ADR-0075 NUC-as-dev-test | verified at restoration write | `git ls-remote` |
| Deterministic companion net at `91410fe…` (409 tests across 01/01+01/02) | Worker-claimed; predecessor Orchestrator classified acceptance-PASS for **pre-R1–R3′** contract | `01_report_00.md`, `01_report_01.md` |
| NUC allowlist gate (privileged) PASS; membership yes | Cooperator-observed, predecessor-classified | `08_orchestrator_notes.md` |
| Rendered Connect/Save/badge plumbing works on live NUC | Cooperator-observed | notes |
| Item 4 `X_EXTRACTOR_FAILED` then a later Save succeeded | Cooperator-observed; **not INFOSEC**; parked | notes |
| R1–R3 then R3′ product amendments | Cooperator decisions (chat) | notes + `02_report_01.md` |
| Plan: no 0034; reuse 0031 PK; opened stays 4th mutation; ingress `x.request` | Planner-claimed; PK independently readable in `0031_companion_review_inbox.py` lines 65–68 | repository |
| Implementation commit `977a7af…` exists, 21 files, parent `91410fe…` | **object exists**; behavioral PASS is a claim | worktree `git log -1`, `git show --stat` |
| Node 107 / Python 151 on candidate | Worker-claimed in `03_report_01.md` | must be re-run by independent acceptor |
| Publication / NUC of `977a7af…` | **not done** | public main still `91410fe…` |
| ADR-0076 on public main | **absent** | file exists only in candidate |
| Canonical `docs/X_COMPANION.md` | still has “fade by position” and ordinary-403-hides | public tree; candidate rewrote the history section |

Session 01 invariant “mixed inbox lists pending + analyzed” and “ordinary
403 hides history” described **91410fe**, not the candidate. Using them as
acceptance oracles for `977a7af…` would fail a correct R2/R3′ tree.

---

## 5. The unpublished candidate (treat as claim)

Path:

```text
/home/agile/Projects/framenest-worktrees/framenest-companion-brave-testing-resume-w3
```

Commit `977a7af80afed16745adb0ef8e939555e5e21cce` (21 files, +1128 / −177).
Normative spec for that commit: archived plan

```text
/home/agile/meta/projects/framenest/05/00-framenest-companion-brave-testing-resume/02_report_01.md
```

Implementation prompt (historical, expired): `03_implementation_00.md`.
Implementation report (claim): `03_report_01.md`.

Claimed product behavior (must be proven, not believed):

- **R1.** Every history row → hosted `open_details` into surviving `#frame`
  (`v: "framenest.companion.web.v1"`, `storedOrigin`, never `*`). Analyzed
  click also POSTs opened **without gating** the iframe (hosted first).
  Pending never POSTs opened. `ui/review.html` stays in tree, unused for
  this path. Hosted Details hide **Analyze by AI** and **Load AI suggestion**.
  Standalone Details/Edit and Gallery 🧠 stay. Edit remains
  `metadata.canonical.write`.
- **R2.** Admin `GET /api/companion/review-inbox` is **analyzed-only**, still
  the **global** analyzed pool, still `media.workflow.read`. Per-actor
  unopened accent/badge. Compact = newest 5 analyzed; All = remainder
  analyzed. Pending admin Saves are **absent** from this list (they may
  still appear on admin **own-history**).
- **R3′.** Ordinary `GET /api/companion/own-history` (`x.request`, not a
  mutation). Own cataloged X Saves, all analysis states, movies excluded.
  Pending: `unopened=false`, no badge. Analyzed own rows: per-actor
  unopened accent/badge. `unopened_count` is **own-analyzed only** (must
  not reuse the global latest subquery). Compact = newest 5 own saves of
  any state. Alice ⊈ Bob. Inbox list/detail/apply remain 403.
- **Opened.** Same fourth `companion_mutation`. Ingress capability
  `x.request`. API: admin (`media.workflow.read`) any eligible non-movie
  run; ordinary owner-only else uniform **404** `MEDIA_NOT_FOUND`. Apply
  never publishes.
- **No 0034.** Head `0033`.

Claimed implementation deviations (acceptance must judge, not silently
widen):

1. `application.py` and `ports/companion_review_repository.py` and
   `extension/shared/messages.js` were **not allowlisted**. Worker used
   `getattr(list_own_history)`, passes `require_owner` to the repository
   **only when true**, and hardcodes `/api/companion/own-history` in the
   service worker via `fetchJson` path override.
2. Isolated-worktree `ap exec --root <worktree>` fails (`declared CPython
   executable does not exist`). Worker ran `ap exec --root` canonical plus
   pytest `-o pythonpath=<worktree>/src` and `--rootdir=<worktree>`. Do
   **not** reconstruct `.venv`. Independent acceptance should reproduce
   exact-source provenance (`framenest.__file__` under the candidate `src/`)
   through the declared AP envelope without copying that workaround as a
   second standing route unless you record it as a classified deviation.
3. Two repository tests keep historical names (`test_mixed_inbox_…`) while
   asserting analyzed-only inbox + own-history pending.
4. `SECURITY.md` was forbidden. Living security prose on public main does
   not yet name own-history or opened-for-owners. Flag for the publication
   allowlist or a one-line successor sentence pointing at ADR-0076 — **do
   not silently expand acceptance into a SECURITY.md rewrite unless the
   acceptor finds a present-tense contradiction that blocks close.**

Preserve the worktree until after public equality of `977a7af…` (or a
replacement SHA if correction produces one). Do not delete it to tidy.

---

## 6. Authoritative product contract for closeout

Source precedence for **this** object:

1. Latest explicit Michal message (wins).
2. Public / canonical repository at the SHA under test.
3. Accepted ADRs on that SHA (0076 exists only on the candidate).
4. Decision-ready plan `02_report_01.md` (R3′; **no 0034**).
5. `02_report_00.md` is **superseded** where it says ordinary
   `unopened` always false / badge never / ordinary 403 on opened.
6. Session 01 reports are evidence for **91410fe pre-correction** only.
7. `03_report_01.md` is a claim.

Cooperator amendments that remain binding unless he changes them:

- R1: every own-row click = hosted iframe Details, not the small native
  overlay. Analyze by AI stays out of the companion popup.
- R2: admin history = analyzed only; newest analyzed top + accent + badge;
  click opens and marks opened for **that** admin actor.
- R3′: ordinary sees **all own saves**; accent/badge **only after analysis**.
- R4: Settings Administration auto-analysis checkbox = **next whole**.
- Apply remains metadata-only; administrator PUT is sole publication
  writer including unpublish (ADR-0074; untouched by this slice).
- Auto-analysis flag stays off in git.

---

## 7. Closure gates for THIS whole

Do **not** emit logical-whole closure until all of:

1. Independent acceptance-PASS of the candidate (or of one bounded
   correction SHA) against `02_report_01.md`, by a Worker who did **not**
   implement session 03. Trust-boundary checks must include: ordinary
   opened on foreign/unknown id → 404; Apply still 403; inbox list/detail
   still 403; exactly four `companion_mutation`; empty-allowlist mutations
   fail-closed; GET own-history readable; own-history `unopened_count`
   is not global; Alice/Bob/admin isolation; no 0034; movie 409.
2. Living docs on the **published** SHA are honest in present tense for
   R1–R3′ (`X_COMPANION.md`, SPEC/PRODUCT/README, ADR-0076 + index).
   ADR-0073/0067 bodies remain unedited with successor notes.
3. Cooperator **publication** grant executed; public `refs/heads/main`
   equals the accepted SHA (credential-free `ls-remote`).
4. Routine NUC refresh through **only** `deploy/ubuntu/framenest-release`
   (`status`, `check --release <40-hex>`, then a **separate** deploy
   grant). Same-schema (`0033` → `0033`). No improvised commands. No `uv`.
5. Michal’s numbered rendered re-test (§10) is PASS or he explicitly
   waives remainder. Reload unpacked Brave **from the SHA the NUC serves**.

Implementation-PASS alone never closes. Publication without NUC refresh
never satisfies AGENTS.md companion-testing rule. NUC on `91410fe…` will
keep showing the **old** overlay/mixed-inbox behavior.

---

## 8. Recommended sequence (do not skip)

```text
read-only restore (§2)
  → independent acceptance Worker 04 (fresh; not the implementer)
      → if FAIL: one bounded correction (new session), then scoped
        re-acceptance of the correction + original risk claim
      → if same assumption survives: PARTIAL / NEEDS_ORCHESTRATOR_DECISION
  → Cooperator publication grant for the accepted 40-hex
  → public ls-remote equality
  → Cooperator routine NUC refresh to that public SHA
  → Cooperator numbered re-test (§10)
  → close this whole
  → write the next restoration prompt (§16)
```

Do not combine acceptance with push. Do not combine push with NUC. Do not
combine NUC with “while we’re there, R4.”

Accelerated-trust mode (predecessor notes): ceremony compressed, **hard
gates unchanged**. Publication and NUC remain explicit owner approvals.

---

## 9. First Worker: independent acceptance (session 04 / exchange 01)

Issue a complete prompt. Do not code.

```text
Role: WORKER
Logical whole identity: framenest-companion-brave-testing-resume
Worker session ordinal: 04
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Native planning mode: not-used
Phase: acceptance
Worker session profile: Fresh independent acceptance; did not implement 977a7af
Reasoning recommendation: High
Exact candidate: 977a7af80afed16745adb0ef8e939555e5e21cce
Exact baseline of that commit's parent: 91410fe063d9907304cff4550f61d403880a2eeb
Normative plan: 05/00-framenest-companion-brave-testing-resume/02_report_01.md
Claim to verify: 05/00-framenest-companion-brave-testing-resume/03_report_01.md
```

Acceptance must:

- Work from a **fresh** checkout of `977a7af…` (new worktree or contained
  clone). Prefer not to mutate the session-03 worktree. Canonical remains
  `91410fe…`.
- Diff the candidate against parent `91410fe…` and confirm the path set is
  the reported 21 files (or explain extras). Confirm **no**
  `alembic_environment/versions/0034*`, no ADR-0073/0067 body edits, no
  `SECURITY.md` change unless you separately grant it.
- Re-run the plan test matrix through `./.ap/ap project check` /
  `./.ap/ap exec` with exact `--baseline 91410fe…` (baseline-bound
  envelope) and Node `node --test` from the candidate root. Prove
  `framenest.__file__` resolves under **candidate** `src/`.
- Map R1 / R2 / R3′ listing / R3′ opened isolation / ingress / no-0034 to
  owning tests. Fail the report if a named plan test is absent or inverted.
- Inspect the getattr / hardcoded-path / pythonpath deviations. Classify:
  accepted-continuation inside the original allowlist, or a defect requiring
  correction (for example Protocol/port honesty, or `pathFor` after an
  allowlist expansion).
- Git: none unless you later issue a **separate** correction prompt.
- Push / NUC / browser / providers: none.

Plan Mode off. One accountable Worker. After `04_report_01.md`: you
accept, authorize one smallest correction, or escalate. You never close.

If the acceptor cannot get exact-source Python through `ap exec` without
`.venv` reconstruction, that is an **environment limitation**, not a
candidate defect. Record it. Do not repair `.venv`. Do not fail the
candidate solely for the known isolated-worktree launch-path miss if
provenance under candidate `src/` is otherwise proven.

---

## 10. After acceptance-PASS: publication, NUC, rendered checklist

### Publication (separate grant)

Exact SHA only. Normal non-force. Public equality via `git ls-remote`.
Do not fast-forward a SHA that failed acceptance. If correction produced
`HEAD'` , publish `HEAD'` not `977a7af…`.

Suggested allowlist additions **only if** acceptance found present-tense
SECURITY/X_COMPANION contradictions on the candidate: surgical
`SECURITY.md` companion paragraph naming ADR-0076, own-history GET, and
opened-for-owners-with-404. Not a security-policy rewrite.

### NUC (separate grant; Cooperator-run unless a later prompt says otherwise)

Sole entry point:

```text
deploy/ubuntu/framenest-release
```

Always `status` then `check --release <40-hex>` before deploy. Deploy
never follows automatically from check. Poetry/CPython paths in AGENTS.md.
No `uv`. No `.git` under `/opt/framenest/current`. Tailnet testing is
normal (ADR-0075). Do not treat the NUC as guarded production.

Same-schema refresh (`0033`). No migration-required continuation expected
for this SHA.

Reload unpacked Brave from the tree that matches that SHA.

### Numbered Cooperator re-test (prepare after NUC is on the published SHA)

Walk sequentially. Each item: `PASS` / `FAIL` / `NOT TESTED` plus optional
`+` commentary. Classify FAIL as defect vs new product idea.

**Administrator (mapped admin identity)**

1. Compact history shows **analyzed only**; a pending own Save does **not**
   appear in admin compact/All.
2. Newest analyzed row is top-most with unopened accent; toolbar badge
   reflects `unopened_count` (not a title).
3. Click opens **hosted Details** in the surviving iframe, not
   `ui/review.html` / small native overlay. `#frame` stays mounted.
4. Hosted Details **hide** Analyze by AI and Load AI suggestion. **Edit**
   remains available. Gallery card 🧠 on the website still exists.
5. After a successful analyzed click, that row’s accent clears and badge
   decrements; the row **remains**. A failed opened POST still opened the
   iframe.
6. Apply is **not** in this click path. Publication still requires
   administrator PUT (spot-check: unpublished item absent from ordinary
   Gallery).

**Ordinary (reuse existing mapped ordinary profile, or NOT TESTED)**

7. History shows **own** cataloged X Saves of any analysis state; a fresh
   Save can sit in the compact five.
8. Pending own rows are plain; they do not increment the badge.
9. After analysis of an **own** item, that row gets unopened accent and
   badge +1 for **this** user.
10. Click is hosted Details; Analyze by AI hidden; Edit hidden (no
    `metadata.canonical.write`).
11. This user’s opened of that own analyzed item clears **their** accent
    only. Admin’s global unopened for the same media (if present) is
    unchanged until admin opens. If a second ordinary user exists: Alice
    ⊈ Bob. Else mark NOT TESTED.
12. Direct inbox list/detail/apply remain forbidden (403) if you probe;
    do not ask Michal to attack the API unless he wants to. Companion
    chrome must not show admin inbox.

**Shared / hygiene**

13. Movies stay out of companion history.
14. Disconnect still clears stored origin / alarm / badge (if not already
    accepted on `91410fe…`, re-spot-check).
15. One transient `composer_unbound` on cold Attach remains **parked**
    (item 7 of the old rendered pass). Do not fail this whole on it
    unless Michal upgrades it to a defect for *this* slice.
16. `X_EXTRACTOR_FAILED` / missing history row after a failed Save is
    **expected** (no media row yet). Cockpit retry is parked, not R1–R3′.

Old 03/10 items “pending visible to admin” and “ordinary 403 hides
history” are **superseded**. Do not score them as FAIL against R2/R3′.

---

## 11. Parked-objects ledger you inherit

Do not work these inside this whole unless Michal explicitly folds one in
(that is a changed objective → usually a **new** kebab).

1. **R4** — extension Settings “Administration” auto-analysis checkbox +
   runtime-writable server setting. New admin-gated mutation surface.
   **Recommended next whole after close.**
2. **Failed-save tombstones** in companion history; X extractor failure
   (`X_EXTRACTOR_FAILED`, ~1.7 s, `can_retry=true`). Cockpit is the current
   retry surface.
3. **Auto-rebind polish** — one transient `composer_unbound` then Attach
   PASS. Do not redesign Attach here.
4. **Delete `ui/review.html`** — parked; leave unused in tree.
5. **Workspace-media-in-companion** — rejected for this slice (wrong
   payload/pagination).
6. **Companion public-origin reconnect** (ADR-0074 rollout 7) — parked
   until a real public origin exists. VPS/TLS remains FROZEN.
7. **04/00 Part A/B** Cooperator execution of
   `docs/ACCEPTANCE_DUAL_AUDIENCE.md` — still parked; not a closure gate
   for companion history.
8. **INFOSEC R3**; **cisarik/ap `FUTURE.md` auditor idea** — parked /
   non-authorizing.
9. **Analysis-proposal dismiss/complete routes** — statuses exist, no
   routes.
10. **Hygiene from 01/01** — six-per-hour default not asserted by executed
    test; `automatic_media_analysis_enabled is False` on `load_settings`
    not asserted (wording-level coverage only).
11. **Canonical `docs/X_COMPANION.md` “fade by position”** — stale on
    `91410fe…`; candidate rewrites the history section. Closes with
    publication of the candidate, not a separate docs whole.
12. **Isolated-worktree AP `.venv` launch path** — known topology
    limitation (`ap.project.conf` relative `.venv/bin/python`). Corroborates
    upgrade-ledger theme (declared route vs ambient). Non-authorizing.
    Do not turn this restore into an AP upgrade task.

---

## 12. Standing rules (unchanged)

- Loopback-first services; no router port-forwarding; Tailscale-only
  remote access until a superseding accepted decision; Funnel to the
  workspace socket forbidden.
- `/srv/media` read-only to the service; ordinary clients never receive
  provider secrets; premium gallery invariant intact.
- Communication with Michal: Slovak; masculine forms for him; feminine
  self-reference for the Orchestrator; repository artifacts professional
  English; Worker reports begin exactly `### Report for ORCHESTRATOR_CHAT`;
  human command blocks `# [MacBook / fish]` / `# [NUC / bash]` ending with
  `#------------------------------------------------------`.
- Workers: Cursor/AppImage ambient Python forbidden; `./.ap/ap project
  check` / `./.ap/ap exec` with exact `--baseline`; NUC SSH via
  `scripts/operator/network/framenest_nuc_worker_gate.fish`; sudo lifecycle
  outside Workers (`sudo -v` is Cooperator; Worker terminal `sudo -K`).
- Git writes only with exact per-task grants; push/publication/deployment
  never implied by implementation or acceptance authority.
- Rendered UI/UX and Brave companion testing: code already on public
  `main` **and** refreshed onto the NUC (ADR-0075 / AGENTS.md).

---

## 13. Security invariants (do not bargain)

- Exactly four `companion_mutation` routes: submit, retry, **same** opened,
  apply.
- Allowlist + `X-FrameNest-Request: 1` + Origin; no CORS; empty allowlist
  fail-closed for those mutations.
- GET inbox and GET own-history are **not** mutations (empty allowlist may
  still 200 given identity/capability).
- Ordinary never gains `media.workflow.read`, `analysis.run`,
  `metadata.canonical.write`, or `media.content.publish`.
- Ordinary opened: own cataloged X only; else uniform 404 (no existence
  leak).
- Apply never publishes; administrator PUT sole writer including unpublish.
- Movie exclusion retained (ADR-0070).
- Schema head `0033`; no 0034.
- Auto-analysis default-off in git (ADR-0066).

Widening opened callers is the sharp trust-boundary of this slice. If
acceptance cannot prove ordinary-cannot-open-others and still-cannot-Apply,
do not publish.

---

## 14. Cursor / AP execution notes the next Worker will hit

- Canonical Python: `/home/agile/Projects/framenest/.venv` ; CPython 3.13
  via `ap.project.conf`.
- Isolated worktrees have **no** `.venv`. `ap exec --root <worktree>`
  fails. Do not repair. Exact-source is `PYTHONPATH`/`sourceRoot` under
  candidate `src/`, proven by `runtime-info` or equivalent provenance
  print under the same envelope.
- Node tests: `node --test` from the candidate root; no npm test toolchain.
- Do not run `~/nuc_update.fish` / `~/framenest_routine.fish` from Cursor.

Upgrade ledger (declared, non-authorizing):

```text
Upgrade ledger: upgrade https://github.com/cisarik/ap.git
Ledger storage version: 1
Ledger path: docs/AP_UPGRADE_OBSERVATIONS.md
```

Active entry: `consumer-declared-execution-and-capability-route-binding`
(`untriaged`). Last stored revalidation SHA is older than current pin.
Revalidate against current repository + public AP `main` (today equal to
the pin). Do not implement AP from this whole.

---

## 15. Negative space (do not do)

- Replan R1–R3′ as if `02_report_01.md` did not exist
- Resume Worker 03 or treat Plan UI / chat Continue as implementation
  authority
- Mix candidate files into canonical `91410fe…` without a publication grant
- Force-push; rewrite shared history
- Enable automatic analysis; ship R4 Settings Administration
- Fifth companion mutation; CORS; router forward; Funnel
- Ordinary inbox/Apply/Edit/Analyze-by-AI
- Personal libraries, Stripe, public registration, Cover Studio, Tauri,
  kiosk, Grafana, YouTube page +, movie companion
- Close 03/09, 03/10, or 04/00 from this restore
- Close **this** whole on implementation-PASS or on NUC-not-refreshed
- Print identity map, Tailscale hostnames, tweet URLs, live titles,
  cookies, companion PEM, or EnvironmentFile values

---

## 16. Terminal duty — next restoration prompt

When **this** whole actually ends (UX PASS or explicit waiver + published
SHA on NUC), generate a new professional restoration prompt for the next
fresh Orchestrator. Inventory at least:

- Public main SHA vs NUC `framenest-release status` (no secrets)
- Whether ADR-0076 is on that SHA
- Companion park: closed or remainder waived; R1–R3′ live or not
- Recommended next whole: **R4 Settings Administration auto-analysis**
  unless Michal selects otherwise
- Still parked: X extractor / tombstones, auto-rebind, public-origin
  companion, 04/00 Part A/B, VPS freeze, INFOSEC R3
- Upgrade ledger still non-authorizing
- Isolated-worktree AP `.venv` observation if still untriaged

Do not generate that next prompt until this whole actually ends.

Suggested next-era folder: `06/01-…` or `07/00-…` following Meta grammar.
Do not reuse 03/10 or 05/00 as live authority.

---

## 17. Restoration readiness review

| Review | Result |
|---|---|
| Contradiction | Session 01 mixed-inbox / 403-hides vs R2/R3′ — **named**; 02/01 wins |
| Omission | Independent acceptance, publication, NUC, rendered re-test never ran — **named as the remaining sequence** |
| Stale-state | Canonical `X_COMPANION.md` still pre-R1–R3′; NUC last attested on `91410fe…` — **named** |
| Authority | All 05 Worker authorities expired at their terminal reports; this file grants none |
| Active mutation | Canonical clean at public main; candidate worktree clean at `977a7af…` |
| Active Worker | None live |
| Security boundary | Opened `x.request` + owner 404 is E3; independent acceptance required |
| Strategic direction | Local-first companion on Tailscale NUC; VPS frozen; premium gallery intact |
| Next-step executability | First Worker prompt is fully specified in §9 |

---

## 18. Predecessor note (non-authorizing)

This restore was written after the era-05 Orchestrator instance died with
an unpublished companion-history candidate and a complete plan. The writer
verified Git objects listed in §2, read the 05 trace end-to-end, and did
**not** re-run the Python/Node matrix or `framenest-release status`. Those
remain your first independent observations.

You are the Orchestrator now. Verify. Issue Worker 04. Close R1–R3′
properly. Then hand Michal a clean next whole.

Required reading before your first Worker prompt, in order: this file;
`/home/agile/Projects/framenest/AGENTS.md`; `.ap/AP.md`;
`.ap/AP_ORCHESTRATOR.md`; `.ap/PROMPT_CONTRACTS.md`;
`docs/WORKER_EXECUTION_CONTRACT.md`; candidate `docs/adr/0076-*.md` (in
the worktree); `docs/adr/0073-*.md` and `0067-*.md` (bodies read-only);
`SECURITY.md` companion paragraphs; `docs/X_COMPANION.md` on **both**
`91410fe…` and `977a7af…`; `02_report_01.md`; `03_report_01.md`;
`08_orchestrator_notes.md`.
