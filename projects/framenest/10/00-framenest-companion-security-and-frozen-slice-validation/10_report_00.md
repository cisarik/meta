### Report for ORCHESTRATOR_CHAT

**Coordinates:** framenest-companion-security-and-frozen-slice-validation; Worker session 10; exchange 01.
**Status:** implementation-PASS (non-independent; logical whole not closed).

**Phase-qualified result:** implementation-PASS.

**Commits:** start `460b37b20ed803c55d125b30c1d51816d201f62a` (branch `feat/x-meme-browser-companion`, porcelain empty, AP pin `7ef45da7…` verified) → end commit `ba54cfa7eb107efaf62c94ba76e895842ee35065` (single commit, no push, no force; porcelain clean after commit).

**Changed files and purpose (all inside the exact allowlist):**

1. `src/framenest/application/youtube_acquisition.py` — added `YouTubeAcquisitionInvalidCursorError(YouTubeAcquisitionInvalidRequestError)` next to the exception family; `_decode_owned_cursor` now raises it with the same static message. No other raiser or consumer touched.
2. `src/framenest/adapters/api/youtube_request_api.py` — typed-cursor fix: `_map_service_error` catches the new subclass BEFORE the generic invalid-request branch and returns 400 `YOUTUBE_REQUEST_INVALID_REQUEST` with static "Invalid YouTube request cursor."; the "cursor" string matching on `str(exc)` is removed; generic branch returns static "Invalid public YouTube video URL." (all 3 remaining invalid-request raisers verified static with exactly that message → byte-identical). Limit site `str(exc)` replaced with a static code→message map covering all 5 verified raiser pairs (`YOUTUBE_REQUEST_ACTIVE_LIMIT/GLOBAL_QUEUE_FULL/RATE_LIMIT/FAILED_24H_LIMIT/PRIVATE_QUOTA`) plus a static unreachable default.
3. `src/framenest/adapters/api/x_request_api.py` — sites replaced with static literals: limit 429 → static code→message map (4 verified raiser pairs + static unreachable default); insufficient-storage 507 → "X staging has insufficient free space." (sole raiser `x_acquisition.py:508`); submit-infra 503 → "X alias overlay is unavailable." (only reachable raisers `x_acquisition.py:532,555`, identical literals); list/get/retry infra 503 → "X acquisition is unavailable." (no raiser reachable from those service paths — verified zero `XAcquisitionInfrastructureError` raise sites outside `x_acquisition.py`; coordinator raisers 945/995/1060/1066/1106 and admin raiser 257 are not on any adapter route). **Two named sites intentionally left unchanged:** `x_request_api.py:239` (list-route invalid-request 422) and `x_request_api.py:284` (retry-route state-conflict 409). For each, all feeding raisers are provably static, but two DIFFERENT static literals feed one catch site with no discriminator attribute (239: "Invalid X request cursor." / "Invalid requester identity."; 284: "X claim is not retryable in its current state." / "X claim has no retryable assets."). A single literal would change observable messages (violating byte-identity); a typed discriminator would require editing `x_acquisition.py`, outside the allowlist — so per the stopping rule I changed the others and report these two exactly.
4. `src/framenest/adapters/api/x_admin_api.py` — infra 503 → static "X review is unavailable." (no raiser reachable from admin `get()`; behavior trivially preserved).
5. `src/framenest/adapters/api/library_api.py` — 409 → static `LIBRARY_UNAVAILABLE_MESSAGE = "Library scan preview is not available."`; 500 → static `SCAN_FAILED_MESSAGE = "Library scan failed."` (sole raisers are the `library_scanner.py:23-24` singletons built from the identical application-layer constants; in-file constant pattern followed).
6. `src/framenest/adapters/api/analysis_proposal_api.py` — 429 → static "Too many analysis proposals this hour." (sole raiser `analysis_proposal.py:86`, constant identical; exact message asserted by existing test remains byte-identical).
7. `src/framenest/infrastructure/persistence/companion_review_repository.py` — annotation-only: `analysis_run_id: MediaId` → `MediaAnalysisRunId` at lines 346 and 421 (post-edit numbering; import added to the existing `media_analysis_runs` import). Runtime untouched; no other `analysis_run_id: MediaId` remains in the file (grep-verified).
8. `tests/contract/test_youtube_request_api.py` — exactly the two authorized tests: route-level (malformed cursor via owned-list route → 400 `YOUTUBE_REQUEST_INVALID_REQUEST`, exact static message, raiser text absent) and seam-level (`_decode_owned_cursor("!!!not-a-cursor")` raises the new subclass, still an `YouTubeAcquisitionInvalidRequestError`, message unchanged). The unit-level test lives in this allowlisted contract file because no unit test path is allowlisted.

Byte-identity: every externally observable response (status, code, message) is unchanged for all reachable raisers; the two unchanged `str(exc)` sites preserve behavior by construction. `tests/contract/test_x_admin_api.py` does not exist (verified); the admin site is covered through existing suites (`tests/contract/test_x_route_policy.py`, 8 passed, plus app wiring).

**Tests and validation (exact commands/outcomes, all via the declared route):**
- `./.ap/ap project check --baseline 460b37b…` → PASS.
- `./.ap/ap exec … --operation test-focus -- tests/contract/test_x_request_api.py tests/contract/test_x_companion_api.py tests/contract/test_youtube_request_api.py tests/contract/test_library_api.py tests/contract/test_analysis_proposal.py tests/contract/test_companion_review_api.py -q -p no:cacheprovider` → **1 failed, 64 passed**; the single failure is `test_library_api.py::test_malformed_library_id_uses_fastapi_validation` (pre-existing, classified below, not caused by this candidate).
- `./.ap/ap exec … test-focus -- tests/contract/test_library_api.py::test_malformed_library_id_uses_fastapi_validation -q -p no:cacheprovider` → same failure in isolation (stability confirmed).
- `./.ap/ap exec … test-focus -- tests/contract/test_x_route_policy.py -q -p no:cacheprovider` → 8 passed.
- Committed tree is byte-identical to the tested tree (porcelain contained only the 8 allowlisted files before staging; staged diff == working diff), so the green evidence binds to `ba54cfa`.

**Pre-Existing Failure Classification (complete record):** baseline `460b37b` (= start of this logical whole); test identity `tests/contract/test_library_api.py::test_malformed_library_id_uses_fastapi_validation` — file byte-identical at baseline and working copy, unchanged since `3e60088`; signature: 422 response body is `{"error": {"code": "VALIDATION_FAILED", "message": "Request validation failed."}}` while the test asserts `"error" not in body`; topical relation: same file as one changed path, but the failing path is the global `RequestValidationError` handler in `src/framenest/adapters/api/application.py:1262-1282` — code introduced by the baseline commit itself ("fix: enforce uniform sanitized error contract for malformed requests") whose companion test was not updated; none of the candidate's changed code executes in that request path, so the candidate did not introduce the regression; debt blocks nothing in this slice and is parked for Orchestrator disposition.

**Commit result:** exactly one commit `ba54cfa…`, conventional subject `refactor: static adapter error messages, typed YouTube cursor error, run-id annotations` — chosen over `fix:` because no externally observable behavior changed anywhere (byte-identical contract; annotation-only edit); justified per task criterion. No push, no force, no tags.

**Deviations/risks/missing evidence:** (1) the two un-replaced `str(exc)` sites above (B3 partially open there; smallest fix is typed discriminators in `x_acquisition.py` — needs a new task/allowlist); (2) out-of-scope observation, unchanged: `src/framenest/application/companion_review.py:620` annotates `_parse_analysis_run_id` returning `MediaId` while constructing run identities — same class of wrong-identity annotation in a non-allowlisted file (ledger candidate); (3) message-map defaults are static unreachable fallbacks (no raiser produces an unmapped code — verified); (4) the two new tests use a hostile cursor fixture string in assertions only. No secrets, no network, no provider calls, no browser, no NUC/SSH.

**Report justification:** new-mutation.

**Authority expiry:** this report terminates my authority; retained context grants nothing. Smallest next step: Orchestrator decision on the two remaining `str(exc)` sites (one bounded task adding discriminators in `src/framenest/application/x_acquisition.py`) and disposition of the pre-existing library 422 test debt.

Resolved Execution Issues / Near-Misses: none.
