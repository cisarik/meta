### Report for ORCHESTRATOR_CHAT

**Coordinates:** logical whole `framenest-companion-security-and-frozen-slice-validation`; Worker session ordinal: `07`; Worker exchange ordinal: `01`.

**Status:** PASS
**Phase-qualified result:** acceptance-PASS
**Start commit:** 53e6448a573a7ac5a2e94ea83f94f68a83ef3074 (read-only audit)
**End commit:** 53e6448a573a7ac5a2e94ea83f94f68a83ef3074 (unchanged; porcelain empty before and after)
**Changed files:** none
**Commit and push result:** none authorized, none performed.

**Tests and validation:**
- Repository gate verified before action: branch `feat/x-meme-browser-companion`, HEAD `53e6448…`, porcelain empty, `.ap` gitlink `7ef45da756ed3cc14808e89bf25d0a9f9aba5d26`. No mutation, commits, file writes, network, NUC, or browser use. No raw python/poetry.
- P2 (run twice, canonical envelope, exact command from the prompt): test-focus over tests/unit/test_server_runtime.py, tests/contract/test_uvicorn_runtime.py, tests/contract/test_tailscale_ingress_security.py → Run 1: **116 passed in 48.19s**. Run 2: **116 passed in 48.05s**. Both green.
- `./.ap/ap project check --baseline 53e6448…` → **PASS**.

**Per-risk-claim verdicts:**

- **R1 — verified-closed.** `create_server` (src/framenest/server.py:107–115) selects `UdsProvenanceVerifyingServer` exactly for the two UDS modes (`INGRESS_MODE_TAILSCALE_UDS`, `INGRESS_MODE_PUBLIC_PUBLISHED_UDS`); TCP path returns plain `uvicorn.Server`, with a second guard `config.uds is None → return` (server.py:84–86). The override awaits `super().startup()` (bind) then tightens to `0o600` (server.py:88–89) before uvicorn's `main_loop()` can begin (`_serve`: startup → main_loop, uvicorn/server.py:94–96). Unit tests assert `type(server) is uvicorn.Server` for the default mode and `isinstance(server, UdsProvenanceVerifyingServer)` for both UDS modes.
- **R2 — verified-closed.** Tighten→assert order: `os.chmod(path, 0o600)` then `_verify_uds_socket_provenance` asserts `S_ISSOCK`, `st_mode & 0o077 == 0`, and `st_uid == os.geteuid()` (server.py:41–48), with reason tokens `not_a_socket` / `permission_bits_not_owner_only` / `foreign_owner` / `chmod_failed` / `stat_failed`. Each failure path closes listeners (`_close_listening_servers`) **before** the raise; `_fail_closed_uds_socket` emits one CRITICAL record (`uds_socket_provenance_failure` / `UDS_SOCKET_PROVENANCE_FAILURE`, `context={"reason": token}`) and raises with `from None` (server.py:51–64). Sanitization verified through the full logging pipeline, not just the call site: `_sanitize_exception` serializes only `{"type": type(value).__name__}` (structured_logging.py:258–259), so a real `OSError` message (which could embed the path) never reaches output; context passes through `_sanitize_value`. "NO request read or processed" on the failure path is independently supported (see P4 conclusion 3).
- **R3 — verified-closed.** Regression test `test_uds_startup_tightens_bound_socket_to_owner_only_and_serves` performs a real fresh bind under `_restored_umask(0o077)` (the documented UMask posture), asserts `S_ISSOCK` + mode `0600` post-startup, completes a genuine HTTP GET over the UDS socket (`/health` → `200`, `{"status": "ok"}`), and asserts zero failure emissions. `RuntimeDirectory=` itself is the systemd directory contract, correctly out of scope per R4's residual.
- **R4 — verified-closed.** SECURITY.md diff is insertions-only (25 lines, 0 deletions). The new section matches the implementation exactly (event/error-code names byte-identical; three provenance assertions enumerated; sanitized-record claim accurate per the logging pipeline evidence). The residuals paragraph states the one-iteration window, transport-level accept possibility, and that directory protection remains the `RuntimeDirectory`/`UMask` contract — all consistent with my independent P4 analysis. No contradiction with existing statements: five `companion_mutation` routes (line ~140), extension-origin allowlist fail-closed (`403 MUTATION_ORIGIN_FORBIDDEN`), S1 residuals block (line ~165), and the dual-audience boundary (`Tailscale-*` headers MUST NOT be trusted outside `tailscale_uds`) are untouched and unrebutted.
- **R5 — verified-closed.** Enumerated every touched pre-existing assertion: in test_server_runtime.py, `server_mock.assert_not_called()` (MagicMock) is replaced by a raising stub plus `assert server_init_attempts == []` — equivalent-to-stronger; nothing else pre-existing was removed or weakened. Strengthenings: `type(server) is uvicorn.Server` (TCP exactly base class) and `isinstance(server, UdsProvenanceVerifyingServer)` ×2 (UDS modes are the subclass). test_uvicorn_runtime.py has zero removed assertions (only a docstring change and an import relocation into the first test). The guard test still binds because `_tighten_uds_socket_permissions` is a named module-level production seam called by the startup override — a real patch point, not test-only.

**P4 — my independent conclusion (from installed uvicorn 0.49.0 and the runtime CPython 3.13.9 sources, not the implementer's account):**
1. uvicorn's `Server.startup` awaits lifespan startup **before** the UDS bind; after `loop.create_unix_server(...)` returns, the remainder is fully synchronous (`os.chmod(config.uds, 0o666)` at server.py:158,164 → assert → `self.servers` → log → `started=True`) — no awaits after the bind. The FrameNest tighten therefore runs in the same event-loop step as uvicorn's post-bind chmod and `startup()` return.
2. CPython 3.13 `create_unix_server` (unix_events.py:357–361) arms the accept reader (`server._start_serving()` → `add_reader`) and then **explicitly yields once** (`await tasks.sleep(0)`) before returning. A real one-iteration post-bind, pre-tighten window exists in which a connection can be accepted and a protocol instantiated before either chmod.
3. Nevertheless no request data is read or processed before tighten+verify complete, including on the failure path: `_accept_connection`'s accepted connection registers its reader during `_process_events` (base_events.py:2012–2013); the suspended task resumes in the ready-queue drain (base_events.py:2033–2035), and the entire remainder (uvicorn chmod → tighten → provenance checks) is synchronous, so the earliest possible `data_received` poll is strictly after tighten+verify; on failure the listeners are closed and the exception propagates before any such poll.
4. Fresh bind confirms the hardcoded number: `uds_perms = 0o666` (uvicorn/server.py:158) applies because the path does not exist before the bind; the preserve-existing-mode branch (lines 159–160) is not taken on fresh bind. If the tighten call were deleted, a fresh bind would end at **0o666** and the regression test's `0o600` assertion would fail (and `/health` would never be served due to fail-closed exit) — the regression test binds. Conversely, with the tighten active, a pre-created 0755 socket is removed and re-bound by `create_unix_server` (unix_events.py:311–312), uvicorn re-applies 0755, the tighten ends it at 0600, verification passes, and the server serves — so the guard fails only when the seam is genuinely neutralized (or absent).

**Deviations / risks / missing evidence:** none. One out-of-scope observation, ledger-candidate disposition: the new SECURITY.md main paragraph says tightening happens "before the server accepts or serves any request," while its own residuals paragraph correctly notes transport-level accept may occur during the window; read together the document is accurate, but the main sentence alone is slightly over-broad — candidate for future wording tightening only.

**Smallest next step:** Orchestrator reconciliation of the ledger candidate above and deterministic continuation per the accepted plan.

**Report justification:** final-acceptance
**Authority expiry:** this terminal report expires all authority granted by this prompt; no further action is authorized without a new complete authoritative Orchestrator prompt.
