### Report for ORCHESTRATOR_CHAT

**Coordinates:** framenest-companion-security-and-frozen-slice-validation; Worker session ordinal `06`; Worker exchange ordinal `01`.
**Status:** PASS — implementation-PASS (non-independent; does not claim acceptance, publication, or closure of the logical whole).
**Commits:** start `6cdbe6f14f31fabc011a316a52026b3800f4e180` (branch `feat/x-meme-browser-companion`, porcelain empty, AP pin verified before mutation) → end commit `53e6448a573a7ac5a2e94ea83f94f68a83ef3074` (parent = exact baseline). One commit, no push, no force.

**Changed files (exactly the allowlist):**
- `src/framenest/server.py` — new `UdsProvenanceVerifyingServer(uvicorn.Server)`: after `await super().startup()` (UDS modes only, discriminator `config.uds is not None`), tightens the bound socket to `0o600` via module-level seam `_tighten_uds_socket_permissions`, then `_verify_uds_socket_provenance` asserts `S_ISSOCK`, `mode & 0o077 == 0`, `st_uid == os.geteuid()`. Any OSError or provenance failure → sanitized CRITICAL record (`event=uds_socket_provenance_failure`, `error_code=UDS_SOCKET_PROVENANCE_FAILURE`, machine reason token: `chmod_failed`/`not_a_socket`/`permission_bits_not_owner_only`/`foreign_owner`/`stat_failed`, no paths, no env values) via `framenest.structured_logging` conventions, synchronous close of listening servers, then `raise UdsSocketProvenanceError(reason) from None` before `main_loop()` (`from None` suppresses the OSError cause that would embed the socket path; traceback carries reason tokens only). `create_server` returns the subclass in `tailscale_uds`/`public_published_uds`, plain `uvicorn.Server` in TCP modes.
- `SECURITY.md` — new subsection "UDS socket provenance tightening and fail-closed startup verification" after the dual-audience section: both new facts plus honest residuals (microsecond post-bind pre-chmod window; `RuntimeDirectory`/`UMask` directory contract out-of-scope). All existing statements intact.
- `tests/unit/test_server_runtime.py` — import-side-effects test now uses a raising stub class (with trailing `sys.modules.pop` so the stub-based copy cannot poison later tests); TCP type assertion strengthened (`type(server) is uvicorn.Server`); both UDS wiring tests assert the subclass; five new seam-function unit tests (tighten→0600; accept owner-only socket; reject non-socket; reject group/other bits; reject foreign owner via `os.geteuid` monkeypatch).
- `tests/contract/test_uvicorn_runtime.py` — dynamic documented-posture pass case (fresh bind under `umask 0o077` → mode exactly `0600`, `/health` served over the socket, zero emissions); guard case (pre-created 0755 socket file + neutralized tightening → fail-closed, reason `permission_bits_not_owner_only`, sighting recorded mode 0755); chmod-failure case (`os.chmod` patched to raise at mode `0o600` only — path-based patching would have hit uvicorn's own 0666 chmod first); foreign-owner case (`os.geteuid` → euid+1 → fail-closed after successful tightening, sighting mode 0600). All failure cases assert listeners closed, exactly one sanitized CRITICAL emission with no `tmp_path` leakage, and exact cleanup.

**Verification of the no-yield ordering guarantee (installed uvicorn 0.49.0):** `uvicorn/server.py:104-195` — the UDS branch's only `await` is `loop.create_unix_server(...)` (line 161); afterwards `os.chmod(config.uds, 0o666)` (164), `_log_started_message` (synchronous), `self.started = True` (195) contain no further await, so the subclass resumes after `super().startup()` with no event-loop cycle since the bind, and its chmod/stat/raise are synchronous in that same step — no request can be read or processed before the checks complete, and a failure closes listeners and raises first. Refined residual discovered and documented: CPython 3.13.9 `asyncio/unix_events.py:283-363` shows `create_unix_server` removes a stale socket file, binds, registers the accept reader, and yields exactly one loop iteration (`await tasks.sleep(0)`, line 361) before returning — that iteration is the accept-possible window (connection accepted at transport level; no request data read); `cleanup_socket=True` also deletes the socket file at loop close (observed: file absent after fail-closed exit). Probe evidence: bind-to-existing socket file succeeds; fresh bind under umask 0077 yields mode 0700 (uvicorn's hardcoded 0666 is what defeats the umask); `asyncio.Server.close()` synchronously closes the listening fd.

**Validation (exact commands/outcomes):**
- `./.ap/ap project check --root /home/agile/Projects/framenest --baseline 6cdbe6f…` → PASS (run three times).
- `./.ap/ap exec … --operation test-focus -- tests/unit/test_server_runtime.py tests/contract/test_uvicorn_runtime.py tests/contract/test_tailscale_ingress_security.py -q -p no:cacheprovider` → **116 passed**, stable across three consecutive runs, final run against the committed HEAD; porcelain clean afterward.

**Resolved Execution Issues / Near-Misses:** (1) Contract tests originally bound `framenest.server` at collection time while the unit import-side-effects test legitimately re-imports the module — patches then targeted a different module copy than the executed one. Fixed by deferred call-time imports in all contract tests so import, patch, and execution bind one copy; classification: test-authoring defect, fully resolved. (2) Post-failure `stat` assertions were invalid once `cleanup_socket` deletion was discovered; replaced with a verification-sighting recorder that snapshots mode/uid before delegating to the real assertion. (3) A 5s join timed out once on a cold first run; raised to 15s; no recurrence in three runs.

**Pre-Existing Failure Classification:** none — all pre-existing tests passed in every run.

**Deviations/risks/missing evidence:** chmod-failure test uses mode-based (0600) `os.chmod` discrimination, not path-based, because uvicorn's own same-path 0666 chmod must pass through; full-startup wrong-kind case is infeasible (a live bind always yields a socket) and is covered at seam level; CRITICAL-record assertions use a call-time recorder on the LOGGER facade (dictConfig during `config.load()` makes caplog unreliable) — the record's sanitized stderr JSON was also directly observed with no paths and reason tokens only. No secrets, no network, no provider calls, no NUC/SSH, no browser.

**One smallest next step:** route the committed candidate `53e6448…` to the required separate fresh Worker for independent acceptance.

**Report justification:** new-mutation.
**Authority expiry:** this terminal report expires all authority granted by this prompt; I claim no acceptance, publication, deployment, or closure of the logical whole.
