# Addendum to 00_handout.md — state delta as of 2026-08-26

Issued by the era-05 Agent Orchestrator
(`05/00-framenest-nuc-push-workflow-and-companion-testing/`, now CLOSED by
Cooperator decision) at Cooperator direction. Read together with
`00_handout.md`; where they conflict, this addendum wins.

## What changed since the base handout was written

1. **Publication happened.** The previously unpublished stack plus docs landed
   on public `main`: fast-forwards `0fe2b32… → 06af60a → 0706818 → 91410fe`.
   Current canonical HEAD == public main == `91410fe063d9907304cff4550f61d403880a2eeb`
   on branch `feat/x-meme-browser-companion`; tracked worktree clean.
2. **NUC is current, not stale.** Base-handout claims (`aec2f009…`, schema
   `0028`, or even interim `a5487149…`/`0032`) are superseded: the NUC now
   serves the tested public-main SHA with schema `0033` after two successful
   routine immutable release updates through `deploy/ubuntu/framenest-release`
   (first run: full exit-13 schema-jump continuation `0032 → 0033`, all nine
   planner evidence points PASS; second run: same-schema, Cooperator-attested
   success). The authoritative mutable readback remains
   `framenest-release status`.
3. **ADR-0075 accepted** (2026-08-26): NUC is FrameNest's development-and-
   testing machine; routine exact-main refreshes ("push to NUC"), including
   schema jumps via the documented exit-13 continuation, are normal operation
   through the sole entry point; non-routine host work still needs explicit
   bounded tasks. Runbook section 5 annex now documents the safe continuation;
   contract suites `tests/contract/test_nuc_release_docs.py` and
   `test_nuc_operator_runbook.py` enforce parity.
4. **AGENTS.md standing rule added:** rendered UI/UX acceptance and Brave
   companion testing always happen against code already public on `main` AND
   refreshed onto the NUC; tailnet testing is normal operation; guarded-
   production framing for the NUC is retired.
5. **Companion unpark authority:** the Cooperator explicitly reopened the
   03/10 Brave backlog on 2026-08-26. Defaults agreed: Apply acceptance
   deterministic-only (no rendered Apply entry exists); one owner-selected
   disposable public X item allowed later with acquisition authorized only at
   scenario time; reuse existing analyzed rows / ordinary profile else NOT
   RUN; allowlist-gate failure routes to a separate EnvironmentFile/restart
   task; no synthetic live-data mutation.
6. **A complete decision-ready testing procedure already exists** — do not
   replan from zero. See `05/00-framenest-nuc-push-workflow-and-companion-testing/01_report.md`
   (session 01 Planner synthesis): route matrix, 13-step companion procedure,
   evidence ownership table, INFOSEC finding route, preserved invariants,
   proposed follow-on coordinates (deterministic acceptance = next session).

## Transfer note

The closed whole delivered: push-to-NUC workflow proven, documentation
coherence published, companion testing planned but NOT executed. The
deterministic acceptance Worker and Michal's rendered Brave pass belong to
THIS successor whole. Session/exchange coordinates restart at `01` here under
its own logical-whole identity.

## Still true from the base handout

Frozen VPS/public-net direction; parked-objects items 3–8; standing rules
(§6); upgrade ledger (§7); required reading (§8); AP pin `9c5cc44…`;
schema head `0033`. Re-verify §1 refs before acting — most are now satisfied
by the deltas above.
