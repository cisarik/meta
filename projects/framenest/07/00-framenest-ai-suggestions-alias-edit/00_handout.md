# Fresh Agent Orchestrator Restoration

## FrameNest 07/00 — AI suggestions (plural) and ordinary alias Edit

You are a **fresh** terminal-capable Agent Orchestrator for FrameNest under
Analytic Programming. Load **this file completely** before any Worker prompt.
It is not a Worker prompt, not a repository plan, not a mutation receipt, and
not a substitute for the pinned AP documents. Restoration transfers
information, **never authority**.

The predecessor Orchestrator closed logical whole
`framenest-companion-brave-testing-resume`. Companion history R1–R3′ is on
public `main` and on the NUC. Item 9 (ordinary unopened / badge after Analyze
by AI) is **Cooperator PASS**. The Cooperator then changed the objective:
FrameNest cannot reach a credible MVP while **AI suggestion** remains a
singular bulk-load chrome and while an ordinary user who **is notified** that
their download has AI analysis **cannot Edit** (cannot write their
caller-private alias) in Gallery or hosted Details.

Your mission: **restore read-only, then issue one Planner** for the bounded
whole below. Do not replan companion history R1–R3′. Do not reopen persist-join
unless a new defect is proven against `2aead54…`. Do not open R4 or VPS inside
this whole.

If a later explicit Michal message contradicts this file, the later message
wins. Record the conflict. Do not paper over it.

```text
STOP: Do not execute 03/03–03/10, 04/00, 05/00, or 06/00 00_handout*.md as
      live product authority. They are historical evidence. Do not resume
      era-06 Worker ordinals 04–11.
STOP: Do not implement FrameNest product code yourself. Issue Workers.
STOP: Do not treat 06/00 Worker reports as proof of this new UX. They
      proved persist-join and publication of 2aead54….
STOP: Do not start implementation from a blank page. First Worker is a
      Planner. Architecture is unresolved (alias vs canonical Save;
      multi-draft source of truth; hosted vs standalone chrome).
STOP: Do not give ordinary identities analysis.run, metadata.canonical.write,
      media.content.publish, media.workflow.read, inbox list/detail, or Apply.
STOP: Do not add a fifth companion_mutation. Alias PUT is not a companion
      mutation (ADR-0062). Keep it that way.
STOP: Do not invent a second suggestion store. ADR-0067: website Analyze
      successes join the same generic-run pool. Dropdown options should be
      those runs (plus in-session preview), not a new table, unless the
      Planner proves 0033 cannot represent the UX (then stop and escalate).
STOP: Do not enable FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED in git.
      R4 (Settings auto-analysis checkbox) is the NEXT whole after this
      one, unless Michal explicitly folds it in.
STOP: Do not scrape ~/nuc_push.fish, ~/framenest_routine.fish,
      ~/nuc_update.fish, ~/global_sudo.fish, or ~/set_ssh_auth_sock.fish.
      Routine NUC refresh is Cooperator-run via that wrapper. Bare
      ./deploy/ubuntu/framenest-release on the Cursor host without wrapper
      transport sanitizes to "command failed". Do not paste MacBook blocks
      inside an NUC SSH login. Do not issue a Worker for routine NUC.
STOP: Do not reconstruct gpgconf. Do not sudo -v. Do not handle a password.
STOP: Do not Funnel the service socket. Do not router-forward. VPS remains
      FROZEN.
STOP: Do not hide new product scope inside “small Edit CSS”. Ordinary Edit
      is a capability + Save-semantics change. Per-field AI apply is an
      ADR-0023 implementation gap, not a label rename.
STOP: Do not let Gallery 🧠 (admin Analyze-and-save canonical) become the
      ordinary apply path. Flag it in the plan.
STOP: Do not use Max unless Michal selects it. High for planning (UX +
      capability + ADR conflict). Extra High is not the default.
STOP: Communicate with Michal in Slovak. Feminine self-reference.
      Masculine address for him. Repository artifacts professional English.
STOP: Rendered UX acceptance belongs to Michal. Code under test must already
      be public main AND on the NUC via framenest-release.
```

Paste seed for a new Agent chat (pointer only, not durable authority):

```text
Resume this AP-integrated FrameNest project as a fresh Orchestrator.
Read /home/agile/meta/projects/framenest/07/00-framenest-ai-suggestions-alias-edit/00_handout.md
completely before any Worker prompt.
Begin read-only. Restore canonical state and the declared AP upgrade ledger.
Do not mutate. Do not implement product code.
Predecessor closed framenest-companion-brave-testing-resume at public
2aead540ee39a81a96425902f85e9b9a34f0d690 (item 9 PASS). Next whole is
framenest-ai-suggestions-alias-edit-mvp: Planner first, then bounded
implementation. Priority is AI suggestions (plural, per-field apply) and
ordinary Edit = alias. Do not reopen R1–R3′. Do not open R4 or VPS.
Communicate with Michal in Slovak. High. No Max.
```

Continuation Bootstrap (AP pointer):

```text
Resume this AP-integrated project.
Read the root AGENTS.md and the pinned AP documents it names.
Begin read-only. Restore canonical state and any declared AP upgrade ledger.
With the COOPERATOR, select exactly one bounded next logical whole before any
mutation authority is issued.
```

For **this** restore, Stage 2 is already satisfied: Michal selected this kebab
in the rotation message. Do not ask him to pick a different object unless he
changes the objective.

---

## 0. Handoff identity

```text
Role: ORCHESTRATOR
Orchestrator session target: fresh-agent-orchestrator-session
Orchestrator profile: terminal-capable repository and operations coordinator
Orchestrator handoff artifact:
  07/00-framenest-ai-suggestions-alias-edit/00_handout.md
Predecessor Orchestrator artifacts (historical evidence, not live authority):
  06/00-framenest-companion/
    especially 00_handout.md, 08_orchestrator_notes.md,
    06_report_00.md (H3 persist-join), 07_report_00.md, 08_report_00.md,
    09_report_00.md, 10_report_00.md, 11_report_00.md
  05/00-framenest-companion-brave-testing-resume/02_report_01.md (R1–R3′ plan)
  03/07-framenest-x-save-overlay-edit-subset-mvp/00_handout.md (alias/Edit parent)
Logical whole identity (this era; do not continue the closed kebab):
  framenest-ai-suggestions-alias-edit-mvp
Next Worker session ordinal: 01
Next Worker exchange ordinal: 01
Worker session target for first prompt: fresh-worker-session
Worker session profile for first prompt: Planner (implementation-planning)
Current phase: Restoration → one planning cycle → implementation
Native planning mode: required for Worker 01 (architecture unresolved)
Reasoning recommendation: High
Internal delegation posture: one accountable active Worker by default
Cooperator: Michal
Primary consumer repository: cisarik/framenest
Pinned protocol repository: cisarik/ap through the FrameNest .ap gitlink
External analytic trace: cisarik/meta
Closed predecessor whole:
  framenest-companion-brave-testing-resume
Parent logical wholes (not closed from here):
  framenest-x-meme-browser-companion-mvp
  framenest-x-companion-save-alias-mvp
  framenest-x-companion-save-category-mvp
  framenest-x-save-overlay-edit-subset-mvp
  framenest-companion-ai-review-inbox-mvp
  framenest-companion-review-inbox-ux-history-mvp
```

Trace folder for **this Orchestrator era** (create Worker files here):

```text
/home/agile/meta/projects/framenest/07/00-framenest-ai-suggestions-alias-edit/
```

Keep 06/00 as the closed-whole archive. Do not rewrite those files. Do not
continue their prompt ordinals. Session 01+ lives here.

---

## 1. Restoration classification

**PARTIAL**

Useful continuity exists: public `main` equals the accepted persist-join SHA,
AP pin, schema `0033`, NUC same-schema cutover Cooperator-observed,
companion R1–R3′ + item 9 PASS, and a Cooperator-selected next whole with
concrete UX. Material uncertainty remains for the Planner, not for “what
kebab”: how ordinary Edit Save maps to `PUT …/alias` vs canonical; whether
Gallery cards stay canonical while Edit writes overlay (ADR-0062 freeze);
how the suggestions dropdown is sourced from existing `media_analysis_runs`
without a second store; first-attempt “AI provider is not available” vs
stale confirmation copy.

Not BLOCKED: no in-flight Worker authority; canonical is public main;
no unpublished candidate.

Re-run §2 yourself. Do not inherit this file as proof.

---

## 2. Canonical state at rotation (verify read-only at start)

Observed 2026-08-26 by the restoration writer. **Re-verify.**

| Fact | Last observed value | How to re-verify |
|---|---|---|
| Canonical checkout | `/home/agile/Projects/framenest` | `git rev-parse --show-toplevel` |
| Canonical branch | `feat/x-meme-browser-companion` | `git rev-parse --abbrev-ref HEAD` |
| Canonical HEAD | `2aead540ee39a81a96425902f85e9b9a34f0d690` | `git rev-parse HEAD` |
| Canonical index | tracked-clean | `git status --porcelain=v1` |
| Public `refs/heads/main` | **same 40-hex** | credential-free `git ls-remote https://github.com/cisarik/framenest.git refs/heads/main` |
| Tree | `0900818f57326017712c07686c49de61d534507f` | `git rev-parse HEAD^{tree}` |
| Range on main vs previous | two commits: `fb59c42` persist-join, `2aead54` test uninvert | `git log -2` |
| Tracking curiosity | `origin/feat/x-meme-browser-companion` stale ahead-count | informational; **do not “fix”** |
| AP gitlink + `.ap` HEAD | `9c5cc44f8b6c92dd56ad2427d13223d7d59c5656` | `git rev-parse :.ap` |
| Public `cisarik/ap` `main` | same pin | `ls-remote` |
| Schema head | `0033` (`media_analysis_proposals`); no Alembic `0034*` | versions dir |
| Live NUC | Cooperator wrapper: `active_release` `2aead54…`, schema `0033`, service active, backup ready, same-schema cutover | Cooperator `framenest-release status` / wrapper only |
| Isolated worktrees w3–w10 | leftover evidence; do not delete unless Michal asks | `git worktree list` |

Upgrade ledger `docs/AP_UPGRADE_OBSERVATIONS.md`: one active entry
`consumer-declared-execution-and-capability-route-binding`, **untriaged**,
**non-authorizing**. Isolated-worktree `ap exec --root <worktree>` still
fails (`declared CPython executable does not exist`). Classify; do not
reconstruct `.venv`. Canonical `--root` plus pytest `--rootdir` /
`pythonpath` remains the task-specific deviation used in era 06.

Cursor Workers: no ambient `python` / `.venv/bin/python` / `poetry run`.
Python evidence through `./.ap/ap project check` and `./.ap/ap exec` with
exact `--baseline`.

---

## 3. What just closed (do not reopen)

Logical whole `framenest-companion-brave-testing-resume` —
**closed-by-ORCHESTRATOR**.

Shipped and on NUC at `2aead54…`:

- R1 hosted click → iframe Details (`open_details`, never `ui/review.html`).
- Hosted Details hide **Analyze by AI** and **Load AI suggestion**.
- R2 admin inbox analyzed-only.
- R3′ ordinary `GET /api/companion/own-history`; own-analyzed `unopened_count`.
- Opened is still the fourth `companion_mutation`; ordinary foreign/unknown
  → 404; Apply and inbox remain 403 for ordinary.
- Item 9 join: successful imported Analyze by AI persists a generic analyzed
  run (`automatic_post_catalog` / `generic_media`) **without a second
  `provider.suggest`**. Ordinary owner then sees unopened + badge.

Parked remainders of that whole (not this kebab unless Michal upgrades them):

- Items 11–12 Alice/Bob isolation / inbox 403 probe — **NOT TESTED**.
- `SECURITY.md` still does not name own-history (publication-flag, not a
  close blocker then).
- First Analyze attempt copy “New AI analysis is available after
  confirmation” + “AI provider is not available”, then retry succeeded —
  **new adjacent UX/reliability remainder**, in scope to *classify* here
  (stale copy vs real provider miss vs confirm_cloud_upload). Not a license
  to rip persist-join.

---

## 4. Cooperator intent for this whole (priority)

Treat the following as **Cooperator product direction**, not yet an ADR.
The Planner must map it onto ADR-0023 / 0062 / 0065 / 0067 / 0076 and name
every conflict.

### 4.1 Ordinary Edit = alias (the “nonsense” after item 9 PASS)

Observed on live NUC after `2aead54…`:

- Ordinary user **does** get companion notification that their download has
  AI analysis (item 9 PASS).
- Ordinary user **cannot Edit**. Details Edit is hidden unless
  `metadata.canonical.write` (`app.js` `applyIdentityCapabilities`).
- Gallery cards paint bottom-left Edit only when
  `identityHasCapability("metadata.canonical.write")` (`app.js` ~6029).
  Ordinary therefore has **no** Gallery Edit.
- Ordinary already has `metadata.alias.write` (`identity_access.py`
  `_ORDINARY_CAPABILITIES`). `GET/PUT /api/media/{id}/alias` already exist
  (ADR-0062). The UI never offers the control.

Cooperator: ordinary **must** be able to Edit = **create/update their
caller-private alias** for that meme (title, description, tags). This is
MVP, not a later nice-to-have. They must not receive canonical write,
Analyze by AI, Load from durable admin chrome, inbox Apply, or
`analysis.run`.

Hosted companion Details currently hide Analyze/Load (keep that). They
must **not** hide ordinary Edit-as-alias. R1 hid admin analysis chrome, not
the user’s overlay.

### 4.2 AI suggestions (plural) — per-field apply, not bulk overwrite

Observed admin Manage-media Edit (screenshot, live NUC):

- Heading **“AI suggestion”** (singular).
- **View details** + “Generated automatically after upload. New AI analysis
  is available after confirmation.”
- First Analyze: also **“AI provider is not available.”**
- Retry: “AI suggestion loaded into draft” + suggested filename; **Load AI
  suggestion** / **Analyze by AI** still present; Save grey until dirty.
- That chrome is the pre-ADR-0023 bulk path. After persist-join, Analyze
  also writes a companion-visible run. Bulk Load still dumps a whole
  suggestion into Current (era-06 Cooperator: Save after Analyze wrote
  every field). That is exactly what Michal rejected.

Frozen UX intent (Cooperator brainstorming, now the planning target):

1. Rename the section to **AI suggestions** (plural).
2. **Model dropdown** above **Title** (not buried at the bottom). Default
   the first / latest compatible suggestion. Changing the dropdown must
   **not** call the provider by itself (ADR-0023 picker rule).
3. **Load** next to that dropdown, still **above Title**. Load fetches or
   reveals the selected suggestion as **proposal strips**. Load **must not**
   replace the Title/Description/Tags inputs.
4. Under **each** Current field (Title, Description, and Tags as a list
   under TAGS): a **non-clickable** green-bordered strip in the same visual
   language as companion **compact history** (dark, green accent, dense,
   not a second form).
5. A **✅** on that strip applies **that field only** into Current.
   Tags: one ✅ **per suggested tag** (append/map into the tag editor, still
   Current, not catalog until Save).
6. Remove **View details** and the “Generated automatically…” /
   confirmation-essay copy. Status may be a short live region (Analyzing…,
   Loaded, Provider unavailable) without a second essay panel.
7. Analyze by AI remains **administrator** (`analysis.run`), standalone
   Manage media / Gallery 🧠 — **not** hosted ordinary Details.

ADR-0023 already accepted: `Current` is never silently overwritten; every
AI invocation is a draft; promotion is explicit (`Use this draft` then;
now **per-field ✅**). This whole **implements** that direction in the
existing Edit dialog. It does **not** authorize persistent multi-model
comparison chrome, Cover Studio, or a new workspace app.

### 4.3 Multiple suggestions

“Which of course they can have more than one.” After persist-join, each
successful Analyze is a generic `media_analysis_runs` row. The dropdown
should list **those companion-visible generic successes** for that media
(newest first), plus any in-session preview that is not yet a run if the
Planner keeps that distinction. Do not add Alembic `0034` for a parallel
draft table unless the Planner proves the run row cannot carry title /
description / tags (`result_json` already does). Movie-identification
runs stay excluded.

### 4.4 Gallery 🧠 (admin) is a landmine

`handleAnalyzeCatalogCard` POSTs the same preview then **canonical PUT**.
That is analyze-and-save, not per-field apply. Ordinary must never see
that control. The plan must say whether admin Gallery 🧠 stays bulk (flag
as known debt) or is narrowed in this whole. Default recommendation:
**out of this kebab** except hide/keep-admin-only; per-field apply lives
in Edit. Do not “fix” 🧠 by giving ordinary canonical write.

---

## 5. Durable decisions the Planner must reconcile (conflicts)

| Source | What it says | Tension with §4 |
|---|---|---|
| ADR-0023 | Current vs AI draft; drafts do not overwrite Current; explicit promotion; inline model picker must not invoke provider | **Aligns.** Implement per-field ✅ as promotion. |
| ADR-0020 | On-demand suggestion; not automatic | Keep Analyze explicit. |
| ADR-0062 | Gallery and Details **remain canonical**; ordinary lacks `canonical.write` and `analysis.run`; alias HTTP exists; Gallery UX **does not read overlay** | **Conflict on Edit affordance**, not necessarily on Gallery *display*. Cooperator wants Edit to **write** alias. Whether Gallery *shows* alias is a **Planner decision** — default: Gallery/Details **read** stay canonical this whole; Edit **writes** overlay for ordinary. If Michal later wants Gallery to show alias, that is a follow-on (read-path), not required to ship Edit. |
| ADR-0065 | Later Save updates caller-private alias; first catalog seeds canonical from pending alias | Ordinary Edit Save = alias PUT. Admin Edit Save = existing canonical PUT. |
| ADR-0067 / 0076 | Analyze successes join the same inbox/run pool; no second suggestion store | Dropdown over runs. |
| ADR-0076 / `X_COMPANION.md` | Hosted Details hide Analyze and Load; Edit stays capability-gated | Keep hide Analyze/Load. **Change** capability gate from canonical-write-only to: admin Edit = canonical; ordinary Edit = alias. |
| PRODUCT.md §17 | Website AI review “session-only, not catalog truth” | Stale vs persist-join. Planner: surgical present-tense in PRODUCT/SPEC/`X_COMPANION` only where this whole makes them false. Do not rewrite ADR-0067/0073 bodies. |
| Premium Gallery invariant | Frozen visual unless a concrete defect | Ordinary missing Edit **is** a defect relative to §4. Do not restyle the gallery. Add the overlay control. |

Do not edit ADR-0062/0067/0073 **bodies**. If the plan needs a successor
ADR (ordinary Edit-as-alias + AI suggestions chrome), one new ADR in the
implementation allowlist is proportionate. Index it.

---

## 6. Evidence classification (so you do not mix layers)

| Item | Class |
|---|---|
| Public main = `2aead54…` | verified at restoration write (`ls-remote`) |
| Canonical HEAD same SHA, tracked-clean | verified |
| NUC `active_release` `2aead54…` / schema `0033` | Cooperator-observed wrapper |
| Items 1–8, 10, 13–16 PASS on prior SHA; item 9 PASS on this SHA | Cooperator-observed rendered scores |
| Persist-join design (no second suggest; movie skip) | repository + independent acceptance 10 |
| Ordinary Edit hidden; Gallery Edit gated on canonical.write | repository (`app.js`) |
| Alias API + ordinary `metadata.alias.write` | repository |
| Singular “AI suggestion” + View details copy | repository `index.html` + Cooperator screenshot |
| First Analyze “provider not available” then retry OK | Cooperator-observed; cause **unproven** |
| Desired per-field ✅ / dropdown / Load above Title | Cooperator intent (brainstorming → this whole) |
| Gallery should display alias instead of canonical | **not decided**; do not assume |

---

## 7. What the Planner must produce

One decision-ready plan (`01_planning_00.md` prompt → `01_report_00.md`).
No implementation in session 01.

The plan must include:

1. **Surface matrix** (rows = actor × chrome):

   | Surface | Ordinary | Admin |
   |---|---|---|
   | Gallery card bottom-left Edit | alias Edit | canonical Edit (existing) |
   | Details Edit button | alias Edit | canonical Edit |
   | Hosted companion Details | alias Edit **shown**; Analyze/Load **hidden** | Analyze/Load hidden (R1); Edit canonical if they have it |
   | Standalone Manage media | no Analyze | Analyze + suggestions chrome |
   | Gallery 🧠 | hidden | keep or park; not ordinary apply |

2. **Save semantics:** ordinary Save → `PUT /api/media/{id}/alias`; admin
   Save → existing canonical metadata PUT. Dirty/validation rules per
   surface. Empty alias still means no overlay row (ADR-0062).

3. **Suggestions data:** source list from existing generic analyzed runs
   for that `media_id` (and in-session preview if needed). One provider
   call per Analyze, never per dropdown change, never per ✅. ✅ copies
   into Current only.

4. **Chrome:** Load + model dropdown **above Title**; strips under fields;
   per-tag ✅; delete View details + generated-automatically copy; plural
   heading.

5. **First-attempt provider miss:** classify (capability snapshot, confirm
   dialog, transient 503, stale copy). Smallest fix in this whole if it is
   copy/control; else named remainder.

6. **Tests:** contract for alias PUT from Edit; ordinary cannot canonical
   PUT; hosted Analyze still hidden; dropdown does not call provider;
   per-field apply does not persist until Save; four `companion_mutation`
   unchanged; schema still `0033` unless a proven migration is required
   (default: **no 0034**).

7. **Docs:** surgical present tense only. Successor ADR if 0062 freeze
   would stay false.

8. **Out of scope (name them):** R4 Settings checkbox; VPS; Cover Studio;
   persistent AI Drafts comparison board; Funnel; CORS; ordinary
   `analysis.run`; admin Apply; items 11–12 isolation probe; SECURITY.md
   own-history rewrite unless a blocking contradiction appears.

If the Planner cannot resolve alias-display vs alias-write without Michal,
stop with **one** question, not a menu of five.

---

## 8. Recommended sequence (do not skip)

```text
read-only restore (§2)
  → Planner Worker 01 (this whole)
  → Cooperator accepts the plan (or one clarification)
  → Implementation Worker 02 (isolated worktree, one or few commits)
  → Independent acceptance (E3: ordinary alias write + hosted hide Analyze)
  → Cooperator publication of that SHA
  → Cooperator NUC via ~/nuc_push.fish (not a Worker)
  → Numbered rendered re-test (§10)
  → close this whole
  → R4 is the likely next kebab
```

Do not combine planning with implementation. Do not combine push with NUC.
Do not fold R4 “while we’re there.”

---

## 9. First Worker: Planner (session 01 / exchange 01)

Issue a complete planning prompt. Do not code.

```text
Role: WORKER
Logical whole identity: framenest-ai-suggestions-alias-edit-mvp
Worker session ordinal: 01
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Native planning mode: used (single implementation-planning cycle)
Phase: Discovery / planning (no implementation authority)
Worker session profile: Planner
Reasoning recommendation: High
Exact baseline: 2aead540ee39a81a96425902f85e9b9a34f0d690
```

Mandatory reading for that Planner: this file; `AGENTS.md`; `.ap/AP.md`;
`.ap/AP_WORKER.md`; ADR-0023, 0020, 0062, 0065, 0067 §5, 0076; `PRODUCT.md`
§17; `docs/X_COMPANION.md`; `identity_access.py`; `app.js`
`updateMetadataControls`, `applyIdentityCapabilities`, catalog-card Edit
gate; `media_alias_api.py`; `PreviewImportedMediaSuggestion` +
`PersistImportedPreviewAnalysis` (do not redesign persist-join).

---

## 10. Numbered Cooperator re-test (after NUC serves the future SHA)

Draft for the plan to freeze; Michal scores PASS / FAIL / NOT TESTED.

1. Ordinary Gallery: bottom-left Edit visible on a cataloged own X meme.
2. Ordinary Edit Save writes **alias only**; canonical title in admin
   Manage media unchanged.
3. Ordinary hosted Details: Edit visible; Analyze by AI and Load hidden.
4. Admin standalone Edit: heading **AI suggestions**; dropdown + Load
   **above Title**.
5. Load does not overwrite Title/Description/Tags inputs.
6. ✅ on title strip copies only title into Current; other fields untouched.
7. Suggested tags appear under TAGS with per-tag ✅.
8. Changing dropdown does not call the provider.
9. Second Analyze still joins companion history (item 9 still true).
10. Ordinary still 403 on inbox Apply; still cannot Analyze.
11. View details / “Generated automatically…” gone from Edit.
12. First Analyze no longer stuck on “provider not available” when the
    provider is actually up (or FAIL with classified remainder).

---

## 11. Authority boundaries

```text
Account: none
Browser: none until a later explicit grant
Filesystem: Meta trace writes in 07/00 only; no FrameNest product edits
  until an implementation prompt
Git: none until an implementation prompt names branch/paths/commit
Host / NUC / sudo / SSH: none for Workers; Cooperator wrapper only
Secrets: none
Providers: none for Workers (tests use fakes)
Publication / deploy: later Cooperator grants, separate sessions
Production / VPS: frozen
```

Restoration grants **no** mutation authority.

---

## 12. Active Worker and mutation state

```text
Active Worker authority: none (all era-06 sessions expired at their reports)
Canonical mutation: none in flight
Unpublished product candidate: none
Public main: 2aead54… (living)
```

---

## 13. Unresolved questions and risks

1. Does Gallery **display** alias this whole, or only Edit-write? Default:
   write only.
2. First Analyze provider-unavailable: copy vs transport vs capability
   race.
3. Admin Gallery 🧠 bulk canonical save vs per-field Edit — park 🧠.
4. Suggested filename strip: Cooperator named title/description/tags;
   filename may be admin-only or omitted for ordinary alias.
5. Content category / acquisition selects in Edit: ordinary alias probably
   must not change canonical category. Planner must say.
6. Ledger entry still untriaged — not this kebab.

---

## 14. Restoration readiness review

| Review | Result |
|---|---|
| Contradiction | ADR-0062 Gallery freeze vs ordinary Edit — **named**, default write-only |
| Omission | R4, 11–12, SECURITY.md, ledger called out as not this kebab |
| Stale-state | PRODUCT “session-only AI” vs persist-join — Planner docs |
| Authority | no live Worker; NUC is Cooperator wrapper |
| Active mutation | none |
| Security | ordinary still no analysis.run / canonical.write / Apply |
| Strategy | MVP Edit + suggestions; not Cover Studio, not VPS |
| Next-step executability | Planner prompt can be issued after you re-verify §2 |

Overall: **PARTIAL** (enough to plan; not enough to implement without the
plan). Incoming Orchestrator: verify §2, then issue Worker 01.

---

## 15. Communication and files

- Slovak with Michal; feminine self-reference; masculine address.
- Worker prompts/reports: professional English.
- Human MacBook blocks: `# [MacBook / fish]` … `#------------------------------------------------------`
  Prefer telling him to use `~/nuc_push.fish` for NUC rather than a bare
  `framenest-release` block on this host.
- Do not print Tailscale hosts, IPs, SSH fingerprints, identity paths.

Orchestrator notes for this era: create
`/home/agile/meta/projects/framenest/07/00-framenest-ai-suggestions-alias-edit/08_orchestrator_notes.md`
append-only, sanitized.

---

## 16. No-mutation-authority statement

This restoration is synthesis. It does not authorize implementation,
publication, deployment, provider contact, NUC mutation, Git writes in
FrameNest, or closure of parent companion/X wholes. The only lawful next
mutation-adjacent act is a **Planner** prompt whose authority is planning
text in the Meta trace, not FrameNest source.
