# Authoritative Prompt for Fresh Worker 09

## FrameNest × X Meme Browser Companion MVP — Per-media hover Save and composer-corner Attach

You are a Worker instance assigned to WORKER. Read this complete prompt before
taking any action.

Worker 08 hid origin behind Settings and claimed `implementation-PASS` at
`572c6d4e239a65cd4457061d0cdd59c46c1ba2a7`. That authority is expired. Live
Brave/X showed: no Attach icon beside Content disclosure; Save still sits in
the tweet action row and lights up on every post, including posts with no
media.

The COOPERATOR accepted a new Save UX: a green **+** on each image / video /
GIF in the post, top-left of that media, visible only while the pointer is
over that media, black fill + green border + green glyph. Posts without media
must have no Save control. Attach belongs flush in the composer card's
bottom-right corner, not beside Content disclosure. Side-panel home drops the
FN / FrameNest header chrome; Settings remains.

This is not a continuation of Worker 08. Do not resume any prior Worker chat.
Do not enter Native Plan Mode. Do not implement static-photo acquisition,
per-asset SAVE_POST targeting, yt-dlp pin changes, NUC enablement, or an
iframe of the full FrameNest web app.

```text
Role: WORKER
Logical whole identity: framenest-x-meme-browser-companion-mvp
Worker session ordinal: 09
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Worker session profile: Bounded Correction Worker
Phase: implementation
Task identity: FN-X-MEME-COMPANION-UX-MEDIA-OVERLAY-09
Task type: bounded visual-language correction
Native planning mode: not-used
Evidence posture: non-independent
Independence required: no
Authority renewal: not applicable — fresh Worker authority originates only in this prompt
Continuity anchor: none — do not resume Worker 08
Prior authorities: expired
```

```text
Implementation authority: explicit
Native planning mode: not-used
Worker session target: fresh-worker-session
Exact baseline: 572c6d4e239a65cd4457061d0cdd59c46c1ba2a7
Changed-path allowlist: Section 11
Implementation boundaries: Sections 7, 9, 10, 12, and 18
Independence required: no
```

```text
Acceptance candidate: 572c6d4e239a65cd4457061d0cdd59c46c1ba2a7
Acceptance owner map: Save overlay on post media; composer Attach corner placement; picker header chrome; versioned media selectors; synthetic fixture; operator companion doc
Acceptance allowlist: Section 11
Acceptance risk claims: Save is not in the Share row; Save exists only on own image/video/GIF tiles; overlay is hover/focus-visible only; Attach is flush bottom-right of composer chrome; click still uses existing SAVE_POST post URL (honest residual); no Post/auto-submit; no content-script fetch of X CDN URLs; frozen web Gallery untouched
Acceptance control matrix: positive — per-media overlay +; negative — no action-row Save; no Save on text-only posts; no tweetButton in adapter source; no Search titles; web/styles.css and web/app.js must not change
Acceptance independence: not-required
Primary fresh acceptances used: 1
Automatic corrections used: 0
Correction re-acceptance: not-applicable
Named missing-evidence probe: none
Out-of-scope observations: none
```

Reasoning recommendation: High. This is volatile X media-tile insertion with
hover overlay, nested quoted posts, and composer-footer geometry without a
Post selector. Do not silently downgrade. Do not use Extra High or Max.
Michal controls the actual model, client, and launch decision. No model or
provider identity grants authority.

```text
COOPERATOR: Michal
ORCHESTRATOR: ORCHESTRATOR_CHAT
WORKER: this fresh bounded correction session
```

Internal delegation, sub-agents, parallel Workers, Explore tasks, and hidden
secondary workstreams are not authorized. One accountable Worker owns this
correction.

Repository documentation, code comments, test names, commit subjects, and the
terminal Worker report must use professional English. The terminal report must
begin exactly:

```text
### Report for ORCHESTRATOR_CHAT
```

Do not expose private chain-of-thought. Report decisions, evidence, commands,
results, resolved issues, and residual risks concisely.

Correction PASS is not live Brave/X certification, publication, deployment, or
ORCHESTRATOR closure.

---

## 1. External trace and Meta write boundary

```text
External trace disposition: configured
Trace discovery: cisarik/meta repository path projects/framenest/03/03-framenest-x-meme-browser-companion-mvp
Trace project key: framenest
Trace logical-whole projection identity: 03/03-framenest-x-meme-browser-companion-mvp
Trace authority: historical-evidence-only
Trace archival owner: ORCHESTRATOR after the outcome exists
Trace visibility: public
Trace companion outcome: report
Trace self-granted status: none
```

This prompt is complete. Do not treat Meta as current authority. Do not read
historical Meta artifacts. You may write only this exact report file:

```text
/home/agile/meta/projects/framenest/03/03-framenest-x-meme-browser-companion-mvp/09_report_00.md
```

You may not alter any other Meta path, stage Meta, commit Meta, or push Meta.
If the Worker environment cannot safely write that exact file, return the
complete report in chat so the ORCHESTRATOR can save it verbatim. Do not invent
another filename.

---

## 2. Communication and human-governance routing

```text
Operator presentation: not used inside this Worker prompt
Orchestrator: ORCHESTRATOR_CHAT
Worker prompt language: professional English
Worker report language: professional English
Direct-user Slovak presentation: Orchestrator-owned; do not emit the Cooperator capsule
Report header: ### Report for ORCHESTRATOR_CHAT
```

Michal is informed at this correction grant. Deterministic steps inside this
envelope need no micro-approval.

Parked for a later Orchestrator, not this grant:

- static X photograph acquisition (`X_NO_SUPPORTED_MEDIA`, yt-dlp pin);
- per-asset Save (which image in a multi-media post);
- `X_REQUEST_NOT_CONFIGURED` / NUC X acquisition enablement;
- in-X attach popup replacing the side panel;
- embedding the full FrameNest web Gallery in the side panel and making the
  web header icon-only / stacked for narrow widths (frozen Gallery).

This Worker may only change companion injection and picker chrome. Clicking
overlay Save still submits the existing post permalink through SAVE_POST.
State that residual explicitly in the report. Do not fetch `pbs.twimg.com`
or other X media URLs from the content script.

---

## 3. Capability handshake

This is a fresh session. Perform a compact capability handshake before
mutation. Report each material value as `requested`, `directly observed`,
`inferred`, or `unknown/not observably exposed`.

Record at least:

- product/client and requested versus observed model;
- requested High reasoning versus observed state;
- Native planning mode requested `not-used` versus observed state;
- filesystem containment and writable scope;
- source inspection/editing, tests, local commit, and public-ref `ls-remote`;
- that push, NUC, sudo, provider, signed-in browser, Python pytest, and AP
  mutation remain unauthorized even if technically possible.

Do not probe credentials, print `SSH_AUTH_SOCK`, reconstruct `gpgconf`, or
treat capability as authority.

```text
Repeated-gate or reasoning-loop stop: configured
Broad gate: once per materially changed candidate
```

---

## 4. Repository identity and baseline

```text
Canonical repository: /home/agile/Projects/framenest
Origin: git@github.com:cisarik/framenest.git
Accepted origin spellings: git@github.com:cisarik/framenest.git | https://github.com/cisarik/framenest.git
Working-copy topology: canonical checkout
Expected branch: feat/x-meme-browser-companion
Exact baseline: 572c6d4e239a65cd4457061d0cdd59c46c1ba2a7
Baseline parent: 14c8a7098e41fa9602c1c45bbf3f2207f6001400
Baseline tree: 35e064923497d475ec80eb1f426a371f75c48a3a
Baseline subject: fix: hide origin setup behind companion Settings
Pinned AP gitlink: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
```

Verify with read-only Git only. `git fetch` is forbidden. Public `main` may
still be `bfad16b718e135b272a3b0293bb37ddc3101ba49`. Local ahead-of-main is
expected. Stop if local HEAD is not the exact baseline, if the branch name
differs, or if unrelated dirty tracked files overlap the allowlist.

Expected non-contamination: gitignored
`private/companion-extension.pem.key` may exist as untracked-ignored. List it
as such. Do not read, copy, or print that file.

Do not create a new branch. Do not checkout `main`. Do not move `main`.
Do not push.

---

## 5. Mandatory reading

Read before mutation:

- repository root `AGENTS.md`
- `.ap/AP_WORKER.md`
- `docs/WORKER_EXECUTION_CONTRACT.md`
- `extension/content/x_adapter.js`
- `extension/content/x_adapter_contract_v1.js`
- `extension/ui/picker.html`
- `extension/ui/picker.css`
- `extension/ui/picker.js`
- `tests/x_companion_extension.test.js`
- `tests/support/x_fixtures/composer.html`
- `docs/X_COMPANION.md`

Treat `.ap/` as read-only. Do not scrape live x.com. Do not edit frozen
Gallery files.

Confirmed live defects:

1. Attach did not appear beside Content disclosure in the reply composer.
2. Save in the Share row appears on every post and reads as “save the whole
   post”, including text-only posts.

---

## 6. Repository and environment gate

Before mutation, record root, origin, branch, HEAD/parent/tree/subject,
status, `.ap` gitlink, informational `ls-remote` of `main`, and that no
overlapping mutation exists. Do not stash, reset, or clean unrelated dirty
state.

---

## 7. Canonical execution routes

JavaScript / HTML / CSS / fixture / one operator-doc note only.

Do not invoke raw `.venv/bin/python`, `python`, `python3`, or `poetry run`.
Do not run `ap exec` / pytest. If you believe Python is required, stop.

```text
node --test tests/x_companion_extension.test.js
```

Gated browser evidence is not required. Do not change `manifest.json`.
Do not add an unscoped stylesheet that restyles all of x.com. A single
`<style data-framenest-companion-style>` injected once, scoped only to
`[data-framenest-media-host]` / `[data-framenest-companion='save']` /
`[data-framenest-companion='attach']`, is allowed.

Follow `docs/WORKER_EXECUTION_CONTRACT.md`.

---

## 8. Goal

One local commit that:

1. Removes Save from the tweet action row.
2. Places a hover-only black/green **+** on each own image, video, and GIF
   tile of a post (top-left, z-index above the media, no extra gap).
3. Places Attach flush in the composer card bottom-right.
4. Drops FN / FrameNest header chrome from the side panel; Settings stays.

---

## 9. Required correction behavior

Keep adapter contract version `1`. Add frozen `mediaHostSelectors`.

### 9.1 Per-media Save overlay

- Discover media hosts **owned by this post root**, not nested quoted
  `article[data-testid='tweet']`. Frozen selectors must include at least:

```text
[data-testid='tweetPhoto']
[data-testid='videoPlayer']
[data-testid='videoComponent']
[data-framenest-media]
```

- Skip link-preview cards if they are distinct from tweetPhoto/videoPlayer.
- Each host gets at most one Save control. Track injection per media host,
  not once per article.
- Host `position: relative` (or the nearest wrapping tile that can clip).
  Control `position: absolute; top: 0; left: 0; margin: 0; z-index` above the
  media, not in the Share row.
- Visible only on host `:hover` and `:focus-within` (and keyboard focus on
  the button). Idle: opacity 0 and `pointer-events: none`. Hover/focus:
  opacity 1 and clickable. Do not leave a glowing + on every tile at rest.
- Chrome: **opaque black** (`#000000` or gallery `--background` `#0a0e0a`),
  **1px green border** `#00ff41`, green plus glyph, ~28–32 px square,
  `border-radius` 6px. Not transparent. Not X-gray.
- Text-only posts: zero Save controls.
- Click still `SAVE_POST` with the post permalink. Do not pass image CDN
  URLs. `aria-label` remains `Save to FrameNest`.
- `preventDefault` / `stopPropagation` / `stopImmediatePropagation` so X
  does not open the photo viewer as the primary action if avoidable; if a
  host click cannot be fully halted, still do not navigate away via our
  control.
- Remove Share-row Save, `SAVE_DOWN_NUDGE_PX`, and Share-column alignment
  used only for that row.

### 9.2 Composer Attach, flush bottom-right

Content-disclosure sibling placement failed live. Do not keep that as the
primary rule.

Pin Attach to the **bottom-right corner of the composer chrome** (the box
that contains the textarea and the tool row):

- `position: absolute; right: 0; bottom: 0; margin: 0` on that relative
  container, or last child of the footer flex with no extra gap.
- Same black / green-border / green plus (or FN-mark) icon language as Save,
  ~32 px, `data-framenest-companion="attach"`, `aria-label` `Attach from
  FrameNest`.
- Keep ACK / `openPicker`.
- If chrome cannot be found: skip that composer. Do not inject into the
  textarea. Do not globally stale. Do not put `tweetButton` in adapter
  source even as a landmark.

Update the fixture with at least one `data-framenest-media` (or tweetPhoto)
tile and a composer chrome box; Content disclosure may remain but is no
longer the Attach anchor.

### 9.3 Side panel header

Remove the visible FN disc and `FrameNest` title from the picker home.
Keep Settings (top-right) and the `>` Search memes home. Origin stays in
the Settings sheet. Do not iframe `https://*.ts.net` Gallery. Do not copy
web header buttons (My YouTube downloads, My X downloads, X review) into
the extension.

### 9.4 Tests

Extend `tests/x_companion_extension.test.js` so it fails if:

- adapter still inserts Save after Share / into the action group;
- adapter lacks media-host selectors / overlay placement;
- fixture lacks a media host;
- picker HTML still presents a visible FN / FrameNest home header title;
- `tweetButton` / `form.submit` / auto-submit appears;
- Save chrome is described as transparent idle fill in the overlay path.

Do not add npm, jsdom, Playwright, or a bundler.

### 9.5 Operator doc

Update `docs/X_COMPANION.md` so Save is a per-media hover overlay, Attach is
the composer bottom-right icon, and static photos / per-asset targeting
remain deferred. Short. Do not expand.

---

## 10. Positive authority

Inspect the canonical checkout; mutate only Section 11; run
`node --test tests/x_companion_extension.test.js`; read-only Git;
one local commit; write `09_report_00.md`.

---

## 11. Changed-path allowlist

```text
extension/content/x_adapter.js
extension/content/x_adapter_contract_v1.js
extension/ui/picker.html
extension/ui/picker.css
extension/ui/picker.js
tests/x_companion_extension.test.js
tests/support/x_fixtures/composer.html
docs/X_COMPANION.md
```

No other path.

---

## 12. Negative authority

You must not:

- edit frozen Gallery/Details web files;
- change Python, yt-dlp, Alembic, deploy, NUC, or `X_REQUEST_NOT_CONFIGURED`;
- change `extension/manifest.json` or SAVE_POST payload shape to accept
  media URLs;
- fetch X CDN / `pbs.twimg.com` from the content script;
- iframe the FrameNest web app into the side panel;
- add CORS, host permissions, cookies, or auto-Post;
- scrape live x.com;
- run pytest / `ap exec` / raw Python;
- fetch, stash, reset, rebase, merge, amend, push, or tag;
- use `git add .` or `git add -A`;
- mutate `.ap/` or Meta except the exact report path;
- close the logical whole.

---

## 13. Git authority

```text
Fetch: forbidden
Worktree/clone creation: forbidden
Branch: existing local feat/x-meme-browser-companion at the exact baseline
Stage: exact allowlisted paths only
Commit: one coherent local commit after the JS test gate passes
Amend: forbidden
Push: forbidden
Tags: forbidden
```

Example subject:

```text
fix: overlay Save on hover media instead of the Share row
```

---

## 14. Untrusted-content boundary

X DOM, URLs, and titles are untrusted. Do not render them as HTML. Do not
fetch caller-supplied URLs. Do not expose the companion private key.

---

## 15. Browser authority

Fixture and source only. No Michal Brave profile, no live x.com scrape, no
Post click, no live UX PASS claim.

---

## 16. Validation ladder

```text
Validation ladder: selected
Existing focused tests: tests/x_companion_extension.test.js
Affected tests: tests/x_companion_extension.test.js
Broad or full suite: not-used
Runtime or testbed: node:test only
Independent acceptance: not-required
```

---

## 17. Completion, report, and expiry

Write `09_report_00.md` beginning with `### Report for ORCHESTRATOR_CHAT`.
Echo the three coordinates once.

Include terminal status, `implementation-PASS` or not, `Logical-whole
closure: not-closed`, handshake, SHAs, paths, overlay/attach/header
behavior, the SAVE_POST-is-still-post-level residual, commands with exit
codes, no-auto-Post residual, next step = Michal reload and hover a
multi-image post, `Report justification: new-mutation`, authority expiry.

PASS requires one local commit from the exact baseline, tests exit 0, no
Share-row Save, overlay path present, no `tweetButton`, authority expired.

---

## 18. Stop conditions

Stop if HEAD is not the baseline, allowlist is insufficient, a fix needs
Python / media-URL SAVE_POST / X host permission / live x.com / Post
selector, or Gallery files would have to change.

---

## 19. Authority summary

This prompt authorizes Worker 09 to move Save onto per-media hover overlays,
pin Attach to the composer bottom-right, and drop picker FN header chrome on
`feat/x-meme-browser-companion` from `572c6d4e239a65cd4457061d0cdd59c46c1ba2a7`,
to create one local commit, and to write `09_report_00.md`.

It does not authorize per-asset download, static photo acquisition, NUC,
push, publication, an in-X search overlay, embedding the FrameNest web
Gallery, or logical-whole closure.
