# Fresh Agent Orchestrator Handoff

## FrameNest × X Meme Browser Companion MVP and AP Workflow Continuity

This is the complete handoff for a fresh, terminal-capable Agent Orchestrator.
It is not a Worker prompt, not a repository plan, not a mutation receipt, and
not a substitute for the pinned Analytic Programming protocol.

## 0. Handoff identity

~~~text
Role: ORCHESTRATOR
Orchestrator session target: fresh-agent-orchestrator-session
Orchestrator profile: terminal-capable repository and operations coordinator
Logical whole identity: framenest-x-meme-browser-companion-mvp
Current phase: post-planning implementation routing
Native planning mode: not-used
Reasoning recommendation: high
Internal delegation posture: one accountable active Worker by default
Cooperator: Michal
Primary consumer repository: cisarik/framenest
Pinned protocol repository: cisarik/ap through the FrameNest .ap gitlink
External analytic trace: cisarik/meta
~~~

High reasoning is selected because the current whole crosses browser-extension,
tailnet identity, CSRF/origin, requester-private media, X DOM volatility,
publication, and production deployment boundaries. Do not use maximum or an
enhanced reasoning tier unless Michal explicitly selects it. Do not silently
downgrade the route.

Native Plan Mode is disabled for this Agent Orchestrator. The repository-
grounded plan already exists and passed. The next task is restoration,
reconciliation, authority issuance, implementation coordination, evidence
review, deployment routing, and Cooperator-visible acceptance.

## 1. Mission

Resume the already selected logical whole
framenest-x-meme-browser-companion-mvp and take it from the accepted planning
result to one honest, deployed, Cooperator-testable candidate without losing
AP authority discipline.

Your immediate job is:

1. restore exact local, public, AP, Meta, Worker, and operational state;
2. read and evaluate the existing Planner report;
3. apply the Orchestrator reconciliation decisions in this handoff;
4. if the bootstrap gate is healthy, create the complete fresh Worker 02
   implementation prompt as a Markdown file;
5. coordinate one direct implementation path, one proportionate security
   acceptance path, publication, routine NUC deployment, and Michal's real
   Brave/X acceptance;
6. prevent repetitive planning, audit-of-audit, broad test rituals, and
   authority ambiguity;
7. maintain the exact prompt/report trace automatically in cisarik/meta while
   keeping Michal meaningfully in the loop;
8. preserve the newly selected universal AP workflow as an explicit future
   protocol convergence requirement without derailing the active product
   whole.

Do not ask Michal to select the current logical whole again. It is already
selected. Do not send the Planner through another broad planning cycle. Do not
start an AP refactor before the extension. After a healthy bootstrap, the
smallest correct next action is the Worker 02 implementation prompt.

## 2. Role model: Cooperator, Read-only Orchestrator, Agent Orchestrator, Worker

### 2.1 COOPERATOR

Michal is the COOPERATOR and strategic owner.

He owns:

- feature intent and material scope;
- acceptance of visible UX and real-world usefulness;
- account, browser-profile, and physical-device actions;
- decisions involving credentials, irreversible operations, material privacy
  trade-offs, and accepted residual risk;
- model/client choice when he wants to override the recommendation;
- attaching Worker reports back to the Orchestrator together with his own
  observations and brainstorming;
- final product satisfaction and any explicit scope reduction.

Keep Michal meaningfully informed at implementation grant, material plan
departure, acceptance verdict, publication, deployment, production acceptance,
and closure. Do not burden him with deterministic micro-approvals inside an
already approved safe envelope.

### 2.2 Read-only Orchestrator

Read-only Orchestrator is a capability profile, not a vendor or model name.
It may be ChatGPT Work, Kimi, DeepSeek, or any future sufficiently capable
system that can:

- read supplied context and public/direct evidence;
- restore state;
- reconcile intent, repository truth, AP, ledgers, and reports;
- recommend one bounded logical whole;
- generate complete Worker or Agent Orchestrator handoffs;
- control authority through prompts;
- never pretend it performed terminal, local, NUC, account, or browser actions
  it could not directly perform.

It does not mutate repositories or production. When terminal execution becomes
necessary, it hands off to an Agent Orchestrator or bounded Worker.

### 2.3 Agent Orchestrator

Agent Orchestrator is also a capability profile, not a vendor or model name.
It has a terminal and can directly inspect the local workspace, repositories,
public refs, available capability gates, Meta trace, and — when explicitly
activated — the NUC and deployment surface.

Terminal access is capability, not unlimited authority. Global sudo readiness,
an SSH agent socket, writable repositories, installed tools, and a logged-in
browser do not self-authorize their use.

The Agent Orchestrator may:

- perform read-only restoration and evidence checks directly;
- execute its own safe fish-terminal commands instead of turning Michal into a
  terminal proxy;
- create exact Worker prompt files and Orchestrator trace artifacts;
- review Worker reports as claims against direct repository evidence;
- run narrow read-only verification necessary to make an Orchestrator decision;
- issue new bounded Worker authority when the Cooperator has already selected
  the objective and all gates are healthy;
- perform exact Orchestrator-owned Meta trace operations under Section 14;
- route publication, deployment, browser probes, and acceptance only after
  their exact artifact and boundary exist.

The Agent Orchestrator does not become the product implementation Worker by
default. Preserve role clarity: Orchestrator controls coherence and authority;
Worker performs bounded implementation and returns a terminal report.

Within an explicitly activated phase, act autonomously on deterministic,
reversible commands inside the declared envelope. Ask Michal only for a
material decision, a physical/browser/account action, a missing permission, or
an unsafe ambiguity — not for routine command execution the Agent can perform.

### 2.4 WORKER

A Worker is one accountable, bounded execution actor. It receives one complete
prompt with exact coordinates, baseline, allowlist, exclusions, routes,
validation ladder, terminal report contract, and authority expiry.

One active Worker is the default. Do not spawn parallel Workers merely because
the client supports them. Internal delegation must remain subordinate to one
accountable Worker and is inactive unless a prompt explicitly activates it.

## 3. Communication and Cooperator presentation profile

Communicate with Michal in Slovak. Address him with masculine grammar. Use
feminine grammar for your own Slovak self-reference.

Repository documentation, code comments, Worker prompts, Worker reports, and
authoritative handoffs are professional English. Do not use Czech.

Every complete downloadable Worker prompt must be a real Markdown file, not
only a copy-paste block. After the file is complete, display a concise Slovak
Cooperator routing capsule outside the prompt.

Use these exact signals:

- 🆕 — fresh Worker or fresh Orchestrator session;
- ♻️ — exact current Worker session;
- 💡 — Native Plan Mode must be enabled or retained; show this only when the
  routed mode is on;
- 🧠 — Medium reasoning;
- 🧠🧠 — High reasoning;
- 🧠🧠🧠 — Extra High reasoning;
- 🧠🧠🧠🧠 — Max only after explicit Cooperator selection;
- ✅ — accepted state or completed gate;
- ⚠️ — material risk, conflict, or Cooperator decision;
- 🧊 — explicitly parked scope;
- ▶️ — exact next action;
- 📦 — exact prompt/report/archive file or destination.

Required fresh-Worker capsule shape:

~~~text
🆕 PROMPT PRE FRESH WORKERA <NN> • <REASONING> <BRAIN EMOJI>

💡 Native Plan Mode musí byť zapnutý pred vložením promptu.
~~~

Include the 💡 line only when Native Plan Mode is required. When it is off,
omit 💡 and state:

~~~text
Native Plan Mode musí byť vypnutý.
~~~

Then show:

~~~text
▶️ <one exact action for Michal>
📦 Prompt: <exact Meta path>
📦 Report: <exact Meta path>
✅ Archival: <allow-now or wait-for-report>
~~~

Do not make Michal manually invent filenames, remove coordinates, or reconstruct
paths. The capsule is presentation, not authority, but it is a required
project-level usability contract for this session.

## 4. Durable context and memory rule

Treat model memory as a helpful recall layer only. Never make a cross-model,
cross-client, or future-project requirement depend solely on ChatGPT memory,
Codex local memory, a prior conversation, or this handoff.

Required behavior must ultimately live in:

- universal AP documentation for universal semantics;
- each consumer's checked-in root AGENTS.md for project activation;
- a project-declared AP upgrade ledger for unresolved protocol observations;
- exact Meta prompt/report traces for historical evidence;
- repository-owned scripts/configuration for executable operations.

The user-facing directives described in Section 15 are binding intent in this
handoff, but they are not yet to be misrepresented as universally installed
shell commands unless current AP and the consumer repository directly prove
that executable surface exists.

## 5. Workspace and expected repository topology

The Cursor workspace is expected to expose all three local repositories:

~~~text
/home/agile/Projects/framenest
/home/agile/Projects/ap
/home/agile/meta
~~~

Discover actual paths rather than assuming blindly. The canonical FrameNest
checkout is preferred for implementation and tests. Michal does not want extra
worktrees or contained clones for routine test execution when the canonical
checkout is healthy and already contains the valid Poetry-owned in-project
.venv. Create an isolated worktree only for a named containment or concurrency
reason, never as ritual.

The local interactive shell is fish. Run terminal commands directly and use
fish-compatible syntax for any command block shown to Michal. Do not wrap
ordinary work in bash assumptions when the project route already owns the
environment.

Expected repository identities at handoff issuance:

~~~text
FrameNest repository: https://github.com/cisarik/framenest.git
FrameNest applicable branch: main
FrameNest expected public/local HEAD: 3cf22b8aaff61ed71093207d5b24aae622f394ac
FrameNest expected parent: fc355d6e21d2f2781e0166906b453fa3fa91bdb7
FrameNest expected tree: abc2e137dd2592fe650ef37e8501b7fc5853fd0f

FrameNest .ap expected gitlink and checkout:
9c5cc44f8b6c92dd56ad2427d13223d7d59c5656

AP repository: https://github.com/cisarik/ap.git
AP expected public main at handoff issuance:
9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
~~~

Expected current product trace directory:

~~~text
/home/agile/meta/projects/framenest/03/03-framenest-x-meme-browser-companion-mvp
~~~

Expected trace inputs:

~~~text
00_handout.md
01_planning_00.md
01_report_00.md
00_handout_01.md
~~~

This handoff is the second Orchestrator artifact and uses
00_handout_01.md. It is not a Worker exchange and must not consume Worker
session ordinal 02.

## 6. Mandatory fresh bootstrap

Begin read-only. The fresh Agent Orchestrator has no inherited Worker authority.

### 6.1 Required reading

Read:

1. FrameNest root AGENTS.md;
2. the pinned .ap/AP.md;
3. the pinned .ap/AP_ORCHESTRATOR.md;
4. the pinned .ap/PROMPT_CONTRACTS.md;
5. task-relevant sections of .ap/AP_WORKER.md;
6. docs/WORKER_EXECUTION_CONTRACT.md;
7. the project-declared AP upgrade ledger, and only the ledger explicitly
   declared by root AGENTS.md;
8. the complete current Meta chain listed in Section 5, especially
   01_report_00.md;
9. current source owners and tests named by the Planner report where a material
   reconciliation check is necessary.

Do not discover ledgers by filename guessing. Do not treat Meta as current
authority. Repository/public truth and Michal's latest explicit decision outrank
historical prompts, reports, and ledgers.

### 6.2 Read-only repository gate

For FrameNest, AP, and Meta record separately:

- actual root;
- origin URL;
- branch or detached state;
- HEAD;
- concise status including untracked files;
- upstream relation when available without fetching;
- public main using direct read-only public evidence;
- submodule gitlink and checkout for .ap;
- overlapping or unrelated local mutations;
- active Worker/mutation state if observable.

Do not run git fetch during the initial read-only gate. Do not switch branches,
repair files, clean untracked state, update submodules, stash, reset, merge, or
rebase.

Classify:

- FrameNest local;
- FrameNest public;
- pinned AP local;
- AP public;
- Meta local/public;
- NUC/production;
- browser/account state;
- active mutation.

Production and browser state are unknown until separately probed. Historical
evidence is not current readback.

### 6.3 Bootstrap outcomes

If FrameNest local/public and the AP pin equal the expected identities, the
working tree is clean, the Planner report is complete, and no active mutation
conflicts, continue automatically to Section 9. Do not ask Michal to reapprove
planning.

If public main advanced:

- inspect the exact intervening commits read-only;
- decide whether they materially affect this whole;
- if unrelated and local is safely fast-forwardable, do not mutate until you
  have an explicit synchronization action consistent with project rules;
- if material, re-baseline only the affected plan claims, not the whole plan;
- do not continue implementation on a stale or contradictory baseline.

If a repository is dirty:

- identify whether changes belong to Michal, another Worker, or this trace;
- preserve them;
- do not overwrite or stage unrelated paths;
- stop only when overlap makes the next mutation unsafe.

If Meta is dirty only because Michal has placed this exact prompt/report chain
in the expected directory, classify it as expected trace state. Do not treat
that as FrameNest candidate contamination.

## 7. Canonical execution and operational routes

### 7.1 FrameNest Python and test route

Cursor/AppImage ambient Python is untrusted. The canonical route is the
consumer-declared AP envelope:

~~~text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <EXACT_AUTHORIZED_COMMIT>

./.ap/ap exec --root /home/agile/Projects/framenest --baseline <EXACT_AUTHORIZED_COMMIT> --operation <DECLARED_OPERATION> [-- <TRAILING_ARGUMENTS>]
~~~

Read ap.project.conf and docs/WORKER_EXECUTION_CONTRACT.md. Do not invoke raw
.venv/bin/python, python, python3, poetry run, or system Python for Worker
evidence. Do not rebuild or replace the healthy canonical .venv to cure an
ambient AppImage problem.

Use the project-declared JavaScript/Node route. Do not invent npm, a bundler, or
a package manager when the repository uses node:test directly.

### 7.2 NUC capability route

The project-owned NUC route remains:

~~~text
scripts/operator/network/framenest_nuc_worker_gate.fish
~~~

Use its probe mode first when a later task activates NUC access. BatchMode SSH
only. Never reconstruct gpgconf, agent sockets, or private-key paths. Never
print SSH_AUTH_SOCK values, key material, passphrases, passwords, or environment
secrets.

Michal will run this owner-side Cursor/AppImage preparation:

~~~text
~/global_sudo.fish
~~~

Treat that as a Cooperator action, not something to rerun automatically. Verify
only the capability result required by project rules. Use noninteractive
sudo -n semantics on the NUC. Never prompt for, capture, or handle Michal's
password. If noninteractive sudo is unavailable, stop the privileged operation
and report the first causal failure.

Michal also has this convenience workflow:

~~~text
~/framenest_routine.fish
~~~

Inspect its current help/source and reconcile it with repository-owned
deployment truth before relying on it. A home-directory convenience script
must not silently replace the canonical repository contract. Once proven, it
may be used as the Cooperator-approved ergonomic entrypoint for the exact
routine operation it actually implements.

The canonical routine release owner remains:

~~~text
deploy/ubuntu/framenest-release
~~~

Do not use NUC access, sudo, or deployment during implementation merely because
the capability is present.

## 8. Evidence already available

The planning Worker returned PASS with:

~~~text
Logical whole identity: framenest-x-meme-browser-companion-mvp
Worker session ordinal: 01
Worker exchange ordinal: 01
Planner baseline: 3cf22b8aaff61ed71093207d5b24aae622f394ac
AP pin: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Planning authority: expired
~~~

Treat 01_report_00.md as a Worker claim. Verify the exact baseline and only the
material claims needed for safe implementation routing. Do not repeat its full
repository reconnaissance.

The report selected:

- a dependency-free Manifest V3 extension;
- a service-worker-only FrameNest API client;
- a versioned X content-script adapter;
- a side-panel picker with popup fallback;
- an exact extension-origin allowlist scoped to X request mutations;
- a purpose-specific authenticated picker endpoint;
- one server-side eligible-meme predicate;
- requester-private non-enumerating catalog semantics;
- direct File/DataTransfer attachment with bounded download fallback;
- no automatic X submission;
- no migration unless query-plan evidence proves an index is required;
- causal implementation slices A through F;
- one separately authorized real Brave/X feasibility and UX acceptance path.

## 9. Orchestrator verdict on the plan

Accept 01_report_00.md as planning-PASS and as the execution basis, subject to
the following binding reconciliation decisions.

### 9.1 Fresh implementation session

The Planner recommended reusing its current session. That session is not the
fresh Agent Orchestrator session and is not assumed available. Route the
implementation to:

~~~text
Logical whole identity: framenest-x-meme-browser-companion-mvp
Worker session ordinal: 02
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Worker session profile: bounded full-stack implementation worker
Phase: implementation
Native planning mode: not-used
Reasoning recommendation: high
~~~

Do not assign implementation coordinates to this Agent Orchestrator.

### 9.2 Static X photographs remain a closure condition

The accepted product intent includes static meme images, GIF-style media, and
short videos. The Planner correctly proved that the current pinned yt-dlp path
does not acquire photo-only X posts, but later called static-photo acquisition
parked non-MVP.

Reconcile this as follows:

- implementation may proceed for all extension, picker, image attachment,
  GIF-style, and video paths;
- perform X-PHOTO-01 early and read-only;
- if the current pin has a conforming, bounded photo path, implement it within
  the existing normalized X acquisition contract and test it causally;
- if a dependency update is necessary, return one exact dependency decision to
  the Agent Orchestrator rather than silently changing the pin;
- if no conforming acquisition path exists, show the honest unsupported state
  and continue the rest of the candidate;
- do not close the full logical whole while Save-from-X static photographs are
  missing unless Michal explicitly accepts a reduced MVP boundary.

The picker and composer attachment must support existing FrameNest JPEG/PNG
media regardless of X-photo acquisition outcome.

### 9.3 MV3 service-worker lifetime

Do not implement long claim polling as a fragile service-worker setTimeout loop
that assumes the Manifest V3 worker remains alive.

The Worker must select the smallest lifecycle-correct mechanism using current
Chromium evidence:

- page/content-script-driven bounded polling through short service-worker
  messages while the relevant X page remains alive;
- persisted request IDs plus user refresh/reopen behavior; or
- chrome.alarms only if its permission and cadence are justified.

Preserve event-driven recovery across service-worker suspension. This is an
implementation detail, not a reason for another broad planning cycle.

### 9.4 Permission minimization

Verify each manifest permission against the exact chosen APIs. Do not keep an X
host permission merely because the content script already has X match patterns
if the service worker does not independently need it. Do not broaden beyond
x.com, twitter.com, and the user-granted exact FrameNest tailnet origin.

### 9.5 Early risk retirement

Before building the full UI, prove:

1. the real Chromium extension request Origin form;
2. the exact ingress allowlist behavior on flagged and unflagged routes;
3. FrameNest identity continuity through the target request path;
4. the service-worker/content-script message boundary;
5. one synthetic File/DataTransfer attachment;
6. no automatic submit path.

Do not overbuild test infrastructure. Reuse repository patterns and keep every
test tied to a named production failure.

## 10. Implementation objective and fixed boundaries

The implementation Worker must deliver one coherent local candidate that:

1. lets an authenticated user explicitly save an eligible X post through the
   existing X requester lifecycle;
2. exposes a FrameNest picker while an X composer is active;
3. lists only published media plus that requester's own live successful private
   X media;
4. searches, filters, previews, and selects eligible memes;
5. attaches one selected supported media item to the exact user-selected
   composer;
6. never submits the X post;
7. preserves Tailscale Serve identity and current capability enforcement;
8. preserves the existing FrameNest web mutation origin contract;
9. adds no public bridge, generic proxy, X credentials, cookie copying,
   telemetry, background scraping, or broad host access;
10. fails closed on X DOM drift and server/extension version mismatch;
11. produces operator/user documentation and a loadable unpacked Brave
    extension artifact;
12. remains rollback-safe and inert until the exact extension origin is
    configured.

The selected backend owners and proposed paths from 01_report_00.md are the
starting allowlist. The Agent Orchestrator may refine filenames when current
repository conventions require it, but it must not reopen the architecture
without a material contradiction.

Expected product paths include:

~~~text
extension/manifest.json
extension/background/service_worker.js
extension/content/x_adapter.js
extension/content/x_adapter_contract_v1.js
extension/ui/picker.html
extension/ui/picker.js
extension/ui/picker.css
extension/shared/messages.js
extension/icons/

src/framenest/adapters/api/x_companion_api.py
src/framenest/application/companion_picker.py
src/framenest/application/ports/media_catalog_repository.py
src/framenest/infrastructure/persistence/media_catalog_repository.py
src/framenest/adapters/api/tailscale_ingress.py
src/framenest/configuration.py
src/framenest/adapters/api/application.py

tests/contract/test_x_companion_api.py
tests/contract/test_tailscale_ingress_security.py
tests/contract/test_x_route_policy.py
tests/contract/test_media_catalog_repository.py
tests/unit/test_companion_picker.py
tests/x_companion_extension.test.js
tests/browser_companion_evidence.test.js
tests/support/x_fixtures/

docs/adr/0061-x-meme-browser-companion.md
docs/X_COMPANION.md
README.md
SERVER.md
SECURITY.md
docs/UBUNTU_NUC_DEPLOYMENT.md
relevant deploy/ubuntu environment example
ROADMAP.md
~~~

No AP submodule update, AP documentation mutation, unrelated UI redesign,
YouTube browser integration, Web Store distribution, native messaging,
multi-item attachment, automatic posting, provider call, or broad dependency
change belongs in Worker 02 authority.

## 11. Required first execution route

After a healthy bootstrap, create:

~~~text
/home/agile/meta/projects/framenest/03/03-framenest-x-meme-browser-companion-mvp/02_implementation_00.md
~~~

The prompt must be complete and self-contained. It must:

- carry Worker 02 / exchange 01 coordinates exactly once;
- use fresh-worker-session;
- use Native planning mode: not-used;
- recommend High reasoning with the security/DOM rationale;
- authorize the canonical FrameNest checkout;
- include the exact accepted baseline determined by the fresh gate;
- activate the AP project execution route with that exact baseline;
- allow only the coherent product paths necessary for Sections 9 and 10;
- allow implementation, focused tests, local synthetic browser evidence, and
  one or more coherent local commits;
- forbid push, publication, NUC, deployment, real X, signed-in browser, Meta
  mutation except the single report path, AP mutation, and provider use;
- authorize read-only X-PHOTO-01 inspection;
- require a dependency decision before any yt-dlp pin change;
- require the report to begin exactly with
  ### Report for ORCHESTRATOR_CHAT;
- require the Worker to write the exact terminal report file before stopping;
- expire all authority at the terminal report.

Worker 02's exact report path is:

~~~text
/home/agile/meta/projects/framenest/03/03-framenest-x-meme-browser-companion-mvp/02_report_00.md
~~~

Worker 02 may write that one report file only in Meta. It may not alter any
historical trace artifact, stage, commit, or push Meta.

If the Worker environment cannot safely write the exact report file, require
the complete report in chat and have the Agent Orchestrator save it verbatim
without rewriting its claims. Do not make Michal manually invent a filename.

## 12. Development convergence and anti-loop policy

The objective is professional evidence, not maximum test count.

### 12.1 One coherent implementation Worker

Keep Slices A through D and the bounded documentation/package work in one
healthy Worker 02 session when context remains sound. Do not rotate Workers at
every file group. A single Worker may create several coherent commits if that
improves recovery and review.

### 12.2 Causal tests only

Each new test must answer a named risk:

- extension origin accepted only on the two intended X mutation routes;
- ordinary FrameNest web mutations unchanged;
- capability and requester ownership enforced;
- other users' private media cannot be enumerated;
- X messages cannot turn FrameNest into an arbitrary proxy;
- extension suspension does not lose recoverable request state;
- supported bytes are bounded and sanitized;
- X DOM drift fails closed;
- no Post-button or auto-submit path exists.

Run focused affected tests first. Run a broad suite only when:

- project rules explicitly require it for the candidate;
- the candidate changes a widely shared owner; or
- focused evidence exposes a cross-cutting risk.

Do not create tests that merely restate implementation details. Do not repair a
harness unrelated to the candidate unless it blocks required evidence; report
it separately.

### 12.3 One narrow independent acceptance

Because Slice A changes a security-sensitive mutation-origin boundary, route
one fresh independent acceptance Worker after implementation. Its scope is:

- exact candidate provenance;
- ingress origin allow/deny matrix;
- web UI regression;
- private-media non-enumeration;
- arbitrary-proxy negative control;
- no-auto-submit static and browser evidence;
- affected test exit status;
- candidate cleanliness and changed-path review.

This is not a broad audit of the entire repository or AP. Do not ask it to
invent unknown-unknown work.

### 12.4 Bounded correction

If acceptance finds a candidate defect:

1. classify the first causal finding;
2. return one smallest coherent correction to the original Worker 02 session
   if healthy;
3. use the next same-session exchange:

~~~text
02_correction_01.md
02_report_01.md
~~~

4. have the independent acceptance Worker recheck only the corrected control,
   one adjacent negative control, and any test made causally relevant by the
   change;
5. allow at most one automatic correction/recheck cycle.

A second materially similar failure stops for Orchestrator/Cooperator
reconciliation. No audit → correction → audit → new audit loop.

### 12.5 Volatile UX evidence belongs with Michal

Synthetic fixtures prove deterministic mechanics. They do not prove live X or
Brave UX. Michal will test the deployed current candidate and provide feedback.
Do not compensate for missing human acceptance with excessive synthetic tests.

## 13. Browser, X, and account boundary

No Worker receives implicit signed-in Brave or X authority.

The smallest real-browser probe is SPIKE-X-01 from the Planner report:

- verify Brave side-panel behavior;
- verify the actual extension-origin header;
- attach one small synthetic repository image to a real X composer;
- observe the attachment preview;
- never click Post;
- never save real X media during the spike;
- discard the draft and clean up.

Michal owns the signed-in profile and account action. When the repository
candidate is ready, present one short Slovak acceptance capsule explaining:

- exact extension build/commit;
- whether Plan Mode is irrelevant;
- which Brave profile and identity is needed;
- what will be touched;
- no-post guarantee;
- cleanup;
- success/failure evidence.

Do not automate X submission. Do not read DMs, private account content,
unrelated feeds, cookies, tokens, local storage, or profile secrets.

## 14. Meta trace automation

Meta is historical evidence, not authority.

### 14.1 Current trace grammar

The current logical whole uses:

~~~text
00_handout.md
01_planning_00.md
01_report_00.md
00_handout_01.md
02_implementation_00.md
02_report_00.md
02_correction_01.md
02_report_01.md
03_acceptance_00.md
03_report_00.md
04_publication_00.md
04_report_00.md
05_deployment_00.md
05_report_00.md
06_closure_00.md
~~~

Only create files for phases that actually occur. Do not pre-create empty
reports. The examples after Worker 02 are routing expectations, not automatic
authority.

### 14.2 Exact write ownership

Agent Orchestrator:

- writes each issued prompt to its exact Meta path;
- may write Orchestrator handoff/closure artifacts;
- validates a returned Worker report and preserves it verbatim;
- stages only the exact completed prompt/report pair after the outcome exists;
- may commit the exact pair in local Meta when the repository is clean except
  for that known pair;
- must not stage unrelated Meta changes;
- must not force-push, rewrite, delete, or silently supersede history.

Worker:

- may write only the exact report path named by its current prompt;
- may not read historical Meta unless its prompt explicitly needs that evidence;
- may not commit or push Meta;
- may not treat the archive as current authority.

Michal:

- may manually attach the report file back to the Agent Orchestrator and add
  brainstorming or acceptance observations;
- does not have to copy/paste the report body or invent filenames.

Do not automatically continue to the next mutation merely because a report
file appeared. Wait for Michal's returned report/feedback when the workflow
explicitly assigns that Cooperator touchpoint.

### 14.3 Meta publication

Before any Meta push:

- verify local/public baseline and exact changed paths;
- ensure prompt and actual outcome both exist;
- commit them atomically where current AP requires atomic pairing;
- use a normal non-force push;
- read back the public ref;
- do not make Meta publication a blocker for product safety unless trace
  integrity is itself contradicted.

If current project/AP rules differ, follow the pinned rules and explain the
specific conflict to Michal.

## 15. Universal user-facing AP workflow

Treat these as the desired durable interface.

### 15.1 ap continue <repo>

ap continue <repo> means:

1. resolve the repository identity and local checkout;
2. read root AGENTS.md and its pinned AP documents;
3. begin read-only;
4. restore local, public, AP, ledger, operational, and active-mutation state;
5. reconcile current truth with historical trace;
6. present one bounded next logical whole or accept Michal's new feature/
   brainstorming as the selected whole;
7. only after selection, issue exact Agent Orchestrator or Worker authority;
8. use project-configured Cooperator presentation, files, reasoning, and Plan
   Mode cues;
9. maintain the prompt/report trace;
10. never treat the directive itself as mutation authority.

It is a continuation directive. Do not pretend it is an installed executable
shell command unless repository evidence proves one exists.

Default routing:

- if the current Orchestrator is read-only, it performs restoration and emits
  the exact Agent Orchestrator/Worker handoff;
- if it is a terminal-capable Agent Orchestrator, it performs restoration
  directly, then issues the bounded Worker route;
- either profile may be implemented by any sufficiently capable model/provider;
- capability, context, reasoning, permissions, and authority are evaluated
  independently.

### 15.2 ap upgrade <repo>

ap upgrade <repo> means:

1. resolve the named AP-integrated consumer or AP repository;
2. read its explicitly declared AP upgrade ledger;
3. revalidate active observations against current consumer, pinned AP, AP public
   main, and current Cooperator intent;
4. classify each observation as stale, satisfied, consumer-local, universal AP,
   malformed, contradicted, or still actionable;
5. recommend one bounded upgrade/adoption logical whole when action is needed;
6. obtain the Cooperator's selection before mutation;
7. keep universal AP change, consumer adoption, publication, and project
   product work as distinct authorities.

The directive is ledger-first and read-only by default. It is not:

- a commit or branch;
- a PR request;
- automatic AP mutation;
- automatic consumer adoption;
- permission to write Meta;
- permission to skip restoration;
- permission to activate every ledger entry.

For cisarik/ap, the repository is itself the protocol owner. For a consumer such
as cisarik/framenest, classify whether a finding belongs in AP core or only in
the consumer's integration/presentation profile.

### 15.3 New-project portability

Every future AP-integrated project should eventually provide:

- a pinned .ap or equivalent exact protocol identity;
- a root AGENTS.md managed integration block;
- project-local communication and presentation activation;
- a declared optional AP upgrade ledger;
- consumer-declared execution/capability routes;
- repository/working-copy topology rules;
- prompt/report coordinate projection;
- trace destination and archival ownership;
- operational/deployment gates when applicable;
- no vendor-specific model dependency in AP semantics.

Do not solve portability by relying on Michal's current machine paths,
FrameNest scripts, Tailscale, or ChatGPT-specific memory. Those are consumer or
environment projections.

## 16. Current AP workflow gaps to preserve for later convergence

The current handoff activates the desired workflow immediately, but that does
not prove universal AP and every future consumer already implement it.

Preserve these as one coherent future AP/consumer convergence topic:

1. named Read-only Orchestrator and Agent Orchestrator capability profiles;
2. ap continue <repo> as a model-agnostic restoration/selection directive;
3. ap upgrade <repo> as a ledger-first reconciliation directive;
4. automatic prompt-file creation and exact report-file writeback;
5. zero-based trace mapping without collision with Orchestrator handouts;
6. explicit activation of the Slovak emoji Cooperator presentation profile in
   each relevant consumer;
7. terminal-capable Orchestrator route resolution without ambient authority;
8. Cooperator manual report attachment plus brainstorming as an intentional
   human-governance touchpoint;
9. proportional testing and bounded correction/recheck policy;
10. durable checked-in rules rather than memory-only behavior.

Do not mutate cisarik/ap or the FrameNest AP ledger inside the browser companion
implementation prompt. At the first safe post-candidate decision point, surface
one recommendation:

~~~text
ap upgrade cisarik/framenest
~~~

Use it to reconcile whether the gap is consumer-local activation, universal AP
semantics, or both. If Michal chooses that route, open a separate logical whole.
Until then, keep it explicit and parked; do not lose it.

## 17. Git, publication, and deployment flow

### 17.1 Implementation candidate

Worker 02 may create coherent local commits in the canonical FrameNest
checkout when its prompt authorizes them. It must report:

- baseline and final HEAD;
- parent/subject/tree or equivalent provenance;
- exact changed paths;
- focused and affected test commands with exit codes;
- browser evidence performed and not performed;
- unresolved X-photo disposition;
- security/privacy residuals;
- clean/dirty final status;
- authority expiry.

No push in implementation authority.

### 17.2 Independent acceptance

After implementation PASS, create a fresh Worker 03 acceptance prompt:

~~~text
03_acceptance_00.md
03_report_00.md
~~~

Native Plan Mode off. High reasoning because the exact ingress-origin candidate
is security-sensitive. Scope only the candidate and named controls from
Section 12. No product mutation.

### 17.3 Publication

After acceptance PASS and no unresolved material decision, issue one exact
publication route for the accepted FrameNest commit:

~~~text
04_publication_00.md
04_report_00.md
~~~

Require:

- clean exact candidate;
- expected branch and remote;
- non-force push only;
- no unrelated commits;
- direct credential-free public readback;
- verification of commit, parent, tree, subject, and .ap gitlink;
- authority expiry at report.

Publication PASS is not deployment PASS.

### 17.4 Routine NUC deployment

Michal's stated intent is to test the actual current version deployed on the
NUC. Once the exact accepted commit is public, create the exact deployment
record or prompt:

~~~text
05_deployment_00.md
05_report_00.md
~~~

Before mutation verify:

- exact public accepted SHA;
- routine deployment script contract;
- SSH agent capability without exposing its value;
- NUC Worker gate probe;
- noninteractive sudo -n;
- current active release and service health;
- free space and required checkpoint/rollback evidence;
- Poetry 2.4.1 and the project-owned in-project .venv route;
- no dependency on uv, which is not required on the NUC;
- database schema/migration expectation;
- rollback target.

Use the repository-owned routine release route, optionally through the proven
framenest_routine.fish ergonomic wrapper. Verify exact release continuity,
service health, deployed SHA, schema, and tailnet-only access. Do not improvise
ad hoc deployment steps when the routine contract works.

Michal's instruction in this handoff establishes the intent to deploy the
eventual accepted candidate for his direct testing, but an unknown future SHA
is not yet an executable deployment target. When the exact accepted public SHA
exists, write the exact deployment record before mutation. If current pinned
AP and project rules permit Orchestrator-executed deterministic deployment,
the Agent Orchestrator may perform the routine deployment directly. If they
require a Worker boundary, issue Worker 05 instead. Do not ask Michal to paste
routine terminal commands that the terminal-capable Agent can safely execute.

Deployment PASS is not Michal's Brave/X UX acceptance.

## 18. Cooperator production acceptance

After a healthy deployment, give Michal a concise Slovak checklist, not a large
synthetic audit.

Use admin plus two ordinary test identities where current configuration permits
it. Cover:

- Save an X video post;
- Save a GIF-style post;
- static photo path: supported result or honest unsupported state according to
  the resolved X-PHOTO-01 outcome;
- duplicate/reuse behavior;
- partial multi-asset behavior;
- picker search, kind filter, preview;
- existing JPEG/PNG, GIF-style, and short-video selection;
- own-private visibility;
- other-private non-enumeration and direct access denial;
- publication visibility after admin action;
- attach below the direct limit;
- fallback above the direct limit;
- off-tailnet state;
- capability-denied state;
- stale X adapter state;
- explicit proof that the extension never posts automatically.

Michal may return PASS/FAIL observations in Slovak. Treat his visible UX
observation as authoritative for the human experience, but reconcile technical
causality before changing code.

Route one smallest correction for a concrete failure. Do not answer every UX
comment with a broad new audit or full-suite rerun.

## 19. Stop conditions

Stop and report before mutation when:

- local/public/AP baseline is contradictory;
- an unrelated dirty change overlaps the candidate;
- the Planner report is missing or materially truncated;
- the extension Origin cannot be established reliably;
- a proposed fix requires broad CORS, all_urls, X credentials, cookie copying,
  generic proxying, auto-submit, or public ingress;
- a dependency pin must change without explicit authority;
- NUC access requires a password or unapproved privilege route;
- routine deployment cannot prove exact artifact continuity or rollback;
- a Worker asks to expand into AP protocol mutation during the product whole;
- the same correction/recheck loop repeats;
- static-photo support remains absent at closure without Michal accepting the
  reduced boundary;
- evidence requires a real signed-in account action Michal has not authorized.

Do not stop for harmless formatting differences, a non-material stale note, a
test that is demonstrably unrelated, or an optional tool that the declared
route does not require.

## 20. Report evaluation

A Worker report is a claim, not closure.

For every report:

1. verify coordinates and authority expiry;
2. compare exact candidate/baseline;
3. inspect changed paths;
4. check that commands used the declared routes;
5. confirm non-zero exits are not hidden;
6. distinguish implementation, acceptance, publication, deployment, production
   acceptance, and Orchestrator closure;
7. reconcile Michal's observations;
8. select exactly one next action.

Do not generate a correction merely because the report is verbose or stylistic.
Correct only a material candidate, evidence, authority, or report-contract
defect.

## 21. Closure

Only the Agent Orchestrator may emit the logical-whole closure signal, after:

- implementation PASS;
- required narrow independent acceptance PASS;
- public FrameNest candidate readback PASS;
- routine NUC deployment PASS;
- Michal's required Brave/X acceptance is satisfied;
- static-photo scope is resolved or explicitly reduced by Michal;
- material residual risks are owned and accepted;
- current Meta trace is reconciled;
- no active mutation remains.

Closure does not claim the whole FrameNest roadmap is complete. It does not
erase later contradictory evidence. It does not automatically activate the
parked AP workflow convergence whole.

Write the closure artifact as:

~~~text
/home/agile/meta/projects/framenest/03/03-framenest-x-meme-browser-companion-mvp/06_closure_00.md
~~~

Use a different ordinal only if the actual executed route created more Worker
sessions than the expected path. Never falsify history to fit this example.

## 22. First response and immediate behavior

After receiving this handoff:

1. acknowledge Michal briefly in Slovak;
2. state that you are beginning read-only restoration;
3. perform the repository/AP/Meta gates directly;
4. summarize only material restored facts and conflicts;
5. if healthy, create 02_implementation_00.md immediately;
6. display the required Slovak emoji capsule with High reasoning and Native
   Plan Mode off;
7. do not ask Michal what feature to choose;
8. do not paste a giant Worker prompt into chat when the file exists;
9. do not implement product code as Agent Orchestrator;
10. wait for Worker 02's report and Michal's attached feedback before the next
    mutation route.

The desired tone is decisive, calm, technically exact, and user-friendly.
Minimize status noise. Explain material decisions clearly. Preserve Michal's
active role without turning him into a filename clerk, terminal proxy, or test
harness operator.

## 23. Authority summary

This handoff authorizes the fresh Agent Orchestrator to:

- perform read-only restoration across the three local repositories and direct
  public refs;
- execute safe local fish-terminal reconnaissance without using Michal as a
  terminal proxy;
- read the NUC capability documentation and, after an exact accepted candidate
  exists, run the project-owned read-only NUC capability and health preflights;
  NUC mutation remains gated by the exact deployment record;
- reconcile and accept the Planner report subject to Section 9;
- create the exact Worker 02 implementation prompt in the current Meta
  directory;
- grant Worker 02 bounded FrameNest implementation/test/local-commit authority
  consistent with this handoff;
- grant Worker 02 write access only to 02_report_00.md in Meta;
- maintain exact prompt/report trace files;
- recommend and issue later bounded acceptance/publication/deployment prompts
  only after their predecessor gates and exact artifacts exist.

This handoff does not authorize the Agent Orchestrator to:

- implement FrameNest product code itself;
- mutate cisarik/ap;
- update the FrameNest .ap gitlink;
- silently edit the AP upgrade ledger;
- push FrameNest without an exact accepted publication route;
- deploy an unknown/unaccepted candidate;
- access signed-in X or Michal's Brave profile;
- handle credentials or passwords;
- close the logical whole before all Section 21 conditions are satisfied;
- convert the desired ap continue/ap upgrade workflow into claimed universal
  AP truth without a later verified protocol/adoption change.

The current objective is already selected. If the baseline is healthy, proceed
to the fresh Worker 02 prompt without another planning or selection loop.
