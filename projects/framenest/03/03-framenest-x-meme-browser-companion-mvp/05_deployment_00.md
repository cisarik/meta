# Authoritative Prompt for Fresh Worker 05

## FrameNest × X Meme Browser Companion MVP — Routine Immutable NUC Release

You are a Worker instance assigned to WORKER. Read this complete prompt before
acting.

Worker 03 independently accepted unpublished candidate
`bfad16b718e135b272a3b0293bb37ddc3101ba49`. Worker 04 published that exact
object to public FrameNest `refs/heads/main` by one ordinary non-force
fast-forward. The ORCHESTRATOR reconciled both reports against direct Git
readback.

Your sole mutation is one routine immutable NUC release update of that exact
public SHA through the repository-owned helper. Do not change source. Do not
push. Do not configure companion origins. Do not claim production UX
acceptance or close the logical whole.

```text
Role: WORKER
Logical whole identity: framenest-x-meme-browser-companion-mvp
Worker session ordinal: 05
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Worker session profile: bounded NUC routine-release Worker
Phase: deployment
Task identity: FN-X-MEME-COMPANION-DEPLOY-05
Task type: bounded routine immutable NUC release
Native planning mode: not-used
Evidence posture: non-independent
Independence required: no
Authority renewal: not applicable — fresh Worker authority originates only in this prompt
Continuity anchor: none — do not resume Workers 02, 03, or 04
```

Reasoning recommendation: High. This is privileged remote host mutation with
catalog checkpoint, atomic current-symlink switch, and automatic rollback.
Do not use Extra High or Max. Michal controls the actual model, client, and
launch decision. No model or provider identity grants authority.

Internal delegation, sub-agents, parallel Workers, Explore tasks, and hidden
secondary workstreams are not authorized.

```text
COOPERATOR: Michal
ORCHESTRATOR: ORCHESTRATOR_CHAT
WORKER: this fresh deployment session
```

Repository documentation and the terminal Worker report must use professional
English. The terminal report must begin exactly:

```text
### Report for ORCHESTRATOR_CHAT
```

Do not expose private chain-of-thought. Do not print credentials, tokens,
passwords, `SSH_AUTH_SOCK`, identity-file paths, fingerprints, private
hostnames beyond sanitized helper output, `/etc/framenest/framenest.env`
contents, media names, or the extension private key.

Deployment PASS is not production-acceptance PASS and is not ORCHESTRATOR
closure.

---

## 1. External trace and Meta write boundary

```text
External trace disposition: configured
Trace discovery: cisarik/meta repository path projects/framenest/03/03-framenest-x-meme-browser-companion-mvp
Trace project key: framenest
Trace logical-whole projection identity: 03/03-framenest-x-meme-browser-companion-mvp
Trace authority: historical-evidence-only
Trace archival owner: ORCHESTRATOR after the outcome exists
Trace visibility: public
Trace companion outcome: report
Trace self-granted status: none
```

This prompt is complete. Do not treat Meta as current authority. You may write
only this exact report file:

```text
/home/agile/meta/projects/framenest/03/03-framenest-x-meme-browser-companion-mvp/05_report_00.md
```

You may not alter any other Meta path, stage Meta, commit Meta, or push Meta.
If the Worker environment cannot safely write that exact file, return the
complete report in chat so the ORCHESTRATOR can save it verbatim.

---

## 2. Communication and human-governance routing

```text
Operator presentation: not used inside this Worker prompt
Orchestrator: ORCHESTRATOR_CHAT
Worker prompt language: professional English
Worker report language: professional English
Direct-user Slovak presentation: Orchestrator-owned; do not emit the Cooperator capsule
Report header: ### Report for ORCHESTRATOR_CHAT
```

The Cooperator must establish the NUC sudo timestamp **outside** this Worker
before privileged deploy (`~/global_sudo.fish`, which is `sudo -v` then
`sudo -n true`). This Worker must not run `sudo -v` or handle a password.
Password-required after a predecessor `sudo -K` is expected lifecycle state.

---

## 3. Capability handshake

This is a fresh session. Perform a compact handshake before NUC mutation.
Report each material value as `requested`, `directly observed`, `inferred`,
or `unknown/not observably exposed`.

Record at least: client/model, requested High reasoning, Native planning mode
`not-used`, filesystem scope, NUC gate probe result (`ssh-agent: ready` or
`ssh-agent: absent`), `framenest-release` availability, and that source
mutation, Git push, providers, and signed-in Brave/X remain unauthorized.

Do not reconstruct `gpgconf`. Do not print agent sockets.

---

## 4. Exact public artifact

```text
Accepted public commit: bfad16b718e135b272a3b0293bb37ddc3101ba49
Accepted parent: 0cf6919a889dc4c6919d843a24cee2bb43fb4bfc
Accepted tree: 65ac2469a8212d17c48ae17e37314e03a1ad4f91
Accepted subject: docs: record X companion origin trust and operator setup
Required public refs/heads/main: bfad16b718e135b272a3b0293bb37ddc3101ba49
Required AP gitlink: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Packaged schema head: 0028
Entry point: /home/agile/Projects/framenest/deploy/ubuntu/framenest-release
Exact NUC Poetry: /opt/framenest/tooling/poetry/2.4.1/.venv/bin/poetry
Exact NUC CPython: /opt/framenest/tooling/python/cpython-3.13.14-linux-x86_64-gnu/bin/python3.13
```

Expected immutable target after a successful cutover:

```text
/opt/framenest/releases/bfad16b718e135b272a3b0293bb37ddc3101ba49
```

Same-schema boundary: live database revision must already be `0028`. This
candidate adds no Alembic revision. If live schema differs, stop `BLOCKED`.
Do not run `framenest-db migrate`.

Owned residual that travels with this SHA and is **not** a deploy blocker:
X-PHOTO-01 outcome (c). Do not change the yt-dlp pin.

Do not write `FRAMENEST_COMPANION_EXTENSION_ORIGINS` on the host. Empty
allowlist is the inert default. Origin configuration is a later Cooperator
operator step, not this grant.

---

## 5. Authority envelope

```text
Implementation authority: none
Correction authority: none
Repository-source mutation authority: none
Commit authority: none
Push/publication authority: none
Deployment authority: explicit for one routine framenest-release deploy of the exact public SHA
NUC/SSH authority: BatchMode only through the project gate probe plus framenest-release transport
Privilege requirement: sudo required for deploy
Terminal opener: cooperator
Timestamp establishment: sudo -v by the cooperator (outside this Worker)
Authorization check: sudo -n true
Password handling: operating-system prompt only, outside this Worker
Worker password exposure: none
Keep-alive process: none
Sudoers modification: none
Provider authority: none
AP mutation authority: none
Meta mutation authority: exact report path only
Signed-in browser / X authority: none
Companion-origin host-config authority: none
Delegation authority: none
```

Authorized:

- read-only inspection of the canonical checkout and pinned `.ap`;
- credential-free `git ls-remote` (no fetch);
- `scripts/operator/network/framenest_nuc_worker_gate.fish --probe`;
- sanitized invocation of `deploy/ubuntu/framenest-release` `status`,
  `check --release <SHA>`, and exactly one `deploy --release <SHA> --yes`;
- remote `sudo -n true` as the helper already does;
- terminal remote `sudo -K`;
- write of the exact report file.

Do not:

- edit any source, test, documentation, Git metadata, or production env;
- `git fetch`, checkout `main`, reset, merge, rebase, commit, or push;
- reconstruct `gpgconf` or print `SSH_AUTH_SOCK`;
- run `uv`, host `pip`, operator `poetry install`, or
  `./.ap/ap update --apply`;
- run `framenest-db migrate`;
- delete `/run/framenest-release-deploy` or any release/staging tree;
- invent a second rollback if the helper already rolled back;
- modify SSH, sudoers, firewall, Tailscale, `/srv/media`, credentials, or
  users;
- access signed-in Brave/X or call providers;
- use interactive `~/framenest_routine.fish` (it prompts y/N; this Worker
  must not);
- close the logical whole.

---

## 6. Repository and transport

```text
Repository: https://github.com/cisarik/framenest.git
Working directory: /home/agile/Projects/framenest
Expected branch: feat/x-meme-browser-companion
Expected HEAD: bfad16b718e135b272a3b0293bb37ddc3101ba49
```

Local `main` may still point at the pre-publication baseline. Do not move it.
HEAD must equal the release SHA. Preserve gitignored
`private/companion-extension.pem.key`.

Transport uses already-configured Cooperator environment
`FRAMENEST_NUC_SSH_TARGET`, `FRAMENEST_NUC_SSH_USER`, and
`FRAMENEST_NUC_SSH_IDENTITY` (the same variables the workstation helper
exports). Pass them to `framenest-release` as `--target` / `--user` /
`--identity` only if needed. **Do not print those values.** If they are
unset, stop `BLOCKED` and report that the Cooperator transport environment is
missing. Do not invent hostnames, users, or key paths.

Sanitize inherited AppImage loader classes before invoking the release
wrapper (`APPIMAGE`, `APPDIR`, `LD_LIBRARY_PATH`, `LD_PRELOAD`). The wrapper
uses the canonical `.venv` interpreter; that is the declared release entry
point, not Worker pytest evidence.

---

## 7. Required reading

Read from the candidate checkout:

```text
AGENTS.md
docs/WORKER_EXECUTION_CONTRACT.md
docs/UBUNTU_NUC_DEPLOYMENT.md
docs/adr/0060-repeatable-immutable-nuc-release-update-contract.md
deploy/ubuntu/README.md
.ap/AP.md
.ap/AP_WORKER.md
```

Do not perform a new product acceptance audit.

---

## 8. Authorized stages

Stop at the first failure. Do not skip ahead to `deploy`.

1. Credential-free
   `git ls-remote https://github.com/cisarik/framenest.git refs/heads/main`
   equals `bfad16b718e135b272a3b0293bb37ddc3101ba49`. Local HEAD, tree,
   parent, subject, and `.ap` gitlink match Section 4. Tracked tree clean
   (gitignored private key may exist).
2. `scripts/operator/network/framenest_nuc_worker_gate.fish --probe`.
   Require `ssh-agent: ready`. If `absent`, stop `BLOCKED`. Do not
   reconstruct `gpgconf`.
3. `deploy/ubuntu/framenest-release status` — must exit 0. Record sanitized
   `active_release`, `release_manifest` (present|absent), `service_active`,
   `database_revision`, `backup_restore_readiness`. Require schema `0028` and
   restore readiness ready. Record the current active SHA as the rollback
   target. Do not assume a historical SHA from documentation. If sudo `-n` is
   invalid, stop `BLOCKED` here; do not `sudo -v`.
4. `deploy/ubuntu/framenest-release check --release bfad16b718e135b272a3b0293bb37ddc3101ba49`
   — exit 0. Must not deploy. Must not refresh sudo. Must not invoke `uv`.
5. Remote `sudo -n true` exit 0 through the same transport. If fail, stop
   `PARTIAL`/`BLOCKED`; do not `sudo -v`.
6. Only after 1–5:
   `deploy/ubuntu/framenest-release deploy --release bfad16b718e135b272a3b0293bb37ddc3101ba49 --yes`
7. `deploy/ubuntu/framenest-release status` again. Require
   `active_release` `bfad16b718e135b272a3b0293bb37ddc3101ba49`,
   `release_manifest: present`, service active, `database_revision` `0028`,
   `backup_restore_readiness` ready. The previously recorded complete release
   must remain as rollback target.
8. Read-only first line of `/opt/framenest/current/.venv/bin/framenest-db`.
   It must name
   `/opt/framenest/releases/bfad16b718e135b272a3b0293bb37ddc3101ba49/.venv/bin/python`.
   If it still names `.staging`, stop `PARTIAL`; do not patch, retry, or
   delete the new tree.
9. Bounded name search only under `/opt/framenest/current/.venv`
   (`-name '*.pth' -o -name 'direct_url.json'`). Those files must not contain
   `.staging`. Do not dump trees. If `.staging` remains, stop `PARTIAL`.
10. Remote `sudo -K`; record exit; confirm follow-up `sudo -n true` fails.
    Stop.

If deploy exits with an existing remote lock or recovery state, stop
`BLOCKED`. Do not `rm /run/framenest-release-deploy` or any release/staging
tree. If deploy fails before cutover, do not delete leftover recovery
material. If deploy fails after cutover, trust the helper’s automatic
rollback and report distinct exits. No second deploy. No improvised
`rollback --yes` unless the helper itself performed automatic rollback and
you are only reporting it.

Do not pipe gates through `tail`/`grep` in a way that hides exit codes.
Timeouts must be long enough for remote Poetry install of the release
`.venv`.

---

## 9. Hard stop

Stop without improvising if public `main` is not the accepted SHA, local HEAD
differs, schema is not `0028`, backup restore readiness is not ready, sudo
`-n` is unavailable, `uv` would be required, a migration would be required,
the agent probe is absent, transport env is missing, or leftover lock/staging
would require deletion to continue.

---

## 10. Completion, report, and expiry

Write the terminal report to the exact Meta path in Section 1 before stopping.

The report begins exactly with `### Report for ORCHESTRATOR_CHAT` and echoes
the three coordinates once, unchanged.

Include:

1. terminal status: `PASS`, `PARTIAL`, or `BLOCKED`;
2. phase-qualified result: `deployment-PASS` or `not-applicable`;
3. `production-acceptance-PASS` not claimed;
4. `Logical-whole closure: not-closed`;
5. capability handshake and gate-probe result;
6. public-main readback; local HEAD; `.ap` pin;
7. pre-deploy sanitized status including rollback-target SHA;
8. check exit; privilege probe; deploy exit/summary (duration, whether
   cutover occurred);
9. post-deploy sanitized status;
10. live shebang first line; live `.pth`/`direct_url.json` leftover-`.staging`
    yes/no;
11. automatic rollback yes/no;
12. leftover lock/staging present or absent after this attempt;
13. privilege-release record (`observed-sudo-k` or `unknown-session-lost`);
14. secrets omitted;
15. `Resolved Execution Issues / Near-Misses`;
16. `Pre-Existing Failure Classification`;
17. one smallest next step: ORCHESTRATOR presents Michal's Brave/X production
    acceptance checklist, including companion-origin configuration; do not
    perform that from this session;
18. exactly one report justification: `changed-external-state` if cutover
    occurred, otherwise `new-evidence`;
19. authority-expiry statement.

Also echo:

```text
Privilege requirement: sudo required for deploy
Terminal opener: cooperator
Timestamp establishment: sudo -v by the cooperator (outside this Worker)
Authorization check: sudo -n true
Password handling: operating-system prompt only
Worker password exposure: none
Keep-alive process: none
Sudoers modification: none
Privilege release: <observed-sudo-k | unknown-session-lost>
Gate scope: pending operation only
```

`PASS` / `deployment-PASS` only if public SHA, check, deploy, and post-status
all confirm `bfad16b718e135b272a3b0293bb37ddc3101ba49` active with
`release_manifest: present`, schema `0028`, service active, backup ready, the
previous complete release still present as rollback target, live
`framenest-db` shebang names the final release interpreter, live `.pth` /
`direct_url.json` do not contain `.staging`, and privilege release is honest.

After the terminal report, all authority granted by this prompt expires.

---

## 11. Authority summary

This prompt authorizes Worker 05 to deploy exact public commit
`bfad16b718e135b272a3b0293bb37ddc3101ba49` to the NUC through one
`framenest-release deploy --yes` after `status` and `check`, and to write
`05_report_00.md`.

This prompt does not authorize source mutation, Git push, companion-origin
host configuration, signed-in Brave/X, AP mutation, leftover-lock deletion,
migration, or logical-whole closure.
