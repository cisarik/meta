# Authoritative Prompt for Fresh Worker 07

## FrameNest Companion AI Review Inbox — living-doc convergence

You are a Worker instance assigned to WORKER. Read this complete prompt before
taking any action.

Workers 02–06 landed successor ADRs 0066–0071, Alembic **0031**, inbox GET,
opened/apply/G2, S1 chrome + badge + alarms, and the administrator review
overlay. That authority is expired. The COOPERATOR accepted this living-doc
slice as the last product-code-adjacent step of the envelope. The
ORCHESTRATOR independently verified W06 (`ap project check` PASS; Node suite
**73 passed**; HEAD `c8b757a92985c8b82704826f964ea3a2bdbe9526`).

Your task is **slice W07 only**: update living product, spec, roadmap, server,
security, companion, and README status from **implemented** repository truth.
Do **not** implement more product code. Do **not** edit accepted ADR bodies.
Do **not** deploy, enable the analysis flag, or close the logical whole.

Do not enter Native Plan Mode. Do not use Max. Extra High is requested; if the
client does not expose a measurable Extra High SKU, continue only while Plan
Mode stays off and Max is unused, and record that in the handshake. If Native
Plan Mode is on, stop `BLOCKED`.

```text
Role: WORKER
Persistent role identity: WORKER
Logical whole identity: framenest-companion-ai-review-inbox-mvp
Prior logical whole identities:
  framenest-x-meme-browser-companion-mvp
  framenest-x-companion-save-alias-mvp
  framenest-x-companion-save-category-mvp
  framenest-x-save-overlay-edit-subset-mvp
Worker session ordinal: 07
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Worker session profile: Fresh Implementation Worker
Phase: implementation
Task identity: FN-COMPANION-AI-REVIEW-INBOX-IMPL-07
Task type: bounded documentation candidate
Native planning mode: not-used
Reasoning recommendation: extra-high
Evidence posture: non-independent
Independence required: no
Authority renewal: not applicable — fresh Worker authority originates only in this prompt
Prior implementation report: Worker 06 / exchange 01 implementation-PASS at c8b757a; authority expired
Continuity anchor: none — do not resume Worker 06
Internal delegation posture: not-used
Accountable Worker: one WORKER
Material phase gate: yes
Changed material axis: documentation-authority-or-living-status
Routing reopened for: documentation-authority-or-living-status
Unchanged axes reopened: none
Ordinary-only trigger: no
Automatic model selection: off
Enhanced/maximum mode: not requested
```

```text
Implementation authority: explicit
Native planning mode: not-used
Worker session target: fresh-worker-session
Exact baseline: c8b757a92985c8b82704826f964ea3a2bdbe9526
Changed-path allowlist: Section 11
Implementation boundaries: Sections 8, 9, 10, 12, and 14
Independence required: no
```

```text
Evidence tier: E3
Evidence tier basis: living-status convergence after implemented W02–W06; no product-code mutation
Authorized implementation stages: causal slices 1–2 in Section 10
Combined implementation envelope: allowed for those two slices only
Implementation stage gates: Section 10; a failed gate stops the sequence
Independent acceptance: not-required (this session)
Rollback or recovery checkpoint: local Git commits on feat/x-meme-browser-companion; no push; no NUC
Activated stricter profile: INFOSEC.md R1 inline only
Terminal implementation report point: after living docs are committed and the contradiction gate passes
```

```text
COOPERATOR: Michal
ORCHESTRATOR: ORCHESTRATOR_CHAT
WORKER: this fresh implementation session
```

Internal delegation, sub-agents, parallel Workers, Explore tasks, and hidden
secondary workstreams are not authorized.

Repository documentation, code comments, test names, commit subjects, and the
terminal Worker report must use professional English. Czech is forbidden.
The terminal report must begin exactly:

```text
### Report for ORCHESTRATOR_CHAT
```

Implementation PASS is not acceptance, publication, deployment, or closure.
`Logical-whole closure: not-closed`.

Protocol-variant selection:

```text
Canonical repository identity: https://github.com/cisarik/ap.git
Immutable version identity: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Declared variant: stable
Governing variants in effect: one
Declaration location: project governing rules
Rules from non-governing variants: none
Migration required: no
```

---

## 1. External trace and Meta write boundary

```text
External trace disposition: configured
Trace discovery: cisarik/meta repository path projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp
Trace project key: framenest
Trace logical-whole projection identity: 03/09-framenest-companion-ai-review-inbox-mvp
Trace authority: historical-evidence-only
Trace archival owner: ORCHESTRATOR after the outcome exists
Trace visibility: public
Trace companion outcome: report
Trace self-granted status: none
Downloadable prompt filename: 07_implementation_00.md
Destination path: projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/07_implementation_00.md
Archival: wait-for-report
```

You may **read** (historical; this prompt wins on W07 scope):

```text
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/01_report_00.md
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/06_implementation_00.md
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/06_report_00.md
```

Frozen hashes at issuance (re-check; stop `BLOCKED` if drifted):

```text
01_report_00.md SHA-256 51e124c02009a6822ebb36afc8893187074c680cd139462d79e72cb61bab75ce
06_implementation_00.md SHA-256 11a1cab31e2a2b2aee287368cf569ee6ef858a2e5f01a0394c815394e994ce25
06_report_00.md SHA-256 1c5011a98bf363514e0a1e03f33018b88593679eeb32e44bf831864208a5d786
```

You may **write** only:

```text
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/07_report_00.md
```

Do not alter any other Meta path. Do not stage or commit Meta. If the report
file cannot be written, return it verbatim in chat.

---

## 2. Communication and human-governance routing

```text
Worker prompt language: professional English
Worker report language: professional English
Direct-user Slovak presentation: Orchestrator-owned
Report header: ### Report for ORCHESTRATOR_CHAT
Cooperator visibility: W07 grant already issued inside the accepted envelope
Human decision points remaining outside this grant: Cooperator living-doc/product acceptance, INFOSEC R3, NUC
Deterministic steps inside bounded authority: living-doc edits, contradiction grep, local commits
Internal delegation posture: not-used
```

---

## 3. Capability handshake

Fresh session. Compact handshake before mutation. Record requested / directly
observed / inferred / unknown. Capability does not grant authority. If Plan
Mode is on, stop `BLOCKED`.

```text
Validation ladder: selected
Inspection and provenance: required
Existing focused tests: Section 13
Broad or full suite: not-used
Independent acceptance: not-required
```

Browser, provider, NUC, publication, and deployment annexes are **not
activated**. Do not run `tests/browser_companion_evidence.test.js`.

---

## 4. Repository gate and exact baseline

```text
Expected canonical root: /home/agile/Projects/framenest
Repository checkout topology: standalone checkout
Working-copy topology: canonical checkout
Applicable branch: feat/x-meme-browser-companion
Expected HEAD: c8b757a92985c8b82704826f964ea3a2bdbe9526
Expected parent: 8b249cac7b183382bc7709391837e3caa4e739e4
Expected tree: e77f490a68db7cf74be60d8e5b826bca009d739c
Expected subject: test: cover companion review overlay contracts
Working tree: expected clean
Upstream: none configured (expected)
Schema head at baseline: Alembic 0031
```

```text
.ap gitlink and checkout: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Detached submodule checkout: acceptable
```

Issuance-time public refs (re-verify with `git ls-remote`, no fetch):

```text
cisarik/framenest refs/heads/main  045f33b44897a6f3949cc515792336396f1d33a1
cisarik/ap        refs/heads/main  9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
```

If local HEAD is not `c8b757a`, stop `BLOCKED` unless the only difference is
this Worker’s own later commits. Do not stash, reset, or clean.

This slice is **documentation**. Do not change Python, extension, Alembic, or
ADR bodies. Provenance gate after each commit:

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <EXACT SHA UNDER TEST>
```

No ambient Python. No full `test` operation. No `npm install`.

Spot-check before mutation (these are the contradictions W07 must retire in
**living** files, not in ADR history):

- `PRODUCT.md` / `SPEC.md` / `ROADMAP.md` / `README.md` still name current
  repository schema head `0030`. Historical “migration `0030` adds …” and
  dated NUC production schema `0028` may remain as history.
- `SPEC.md` still says X acquisition has “no automatic AI invocation, and no
  automatic publication.” Successor truth: administrator-owned X auto-analysis
  when the scheduler flag is on (default still false); companion review Save
  may publish when ADR-0049 readiness holds.
- `SECURITY.md` / `SERVER.md` / `docs/X_COMPANION.md` still say companion
  mutations are the **two** flagged X request routes and that the companion
  “does not change any other mutation route.” Successor truth: exactly **four**
  `companion_mutation` routes (X submit, X retry, review opened, review apply).
- `docs/X_COMPANION.md` still describes the side panel as iframe-the-website
  only. Successor truth: native S1 inbox above the surviving ADR-0063 iframe,
  badge + `alarms`, second review overlay, frozen ingest Save.
- `README.md` still says the companion “can choose a canonical content
  category at Save.” Successor truth: ingest Save is the frozen Edit-media
  subset (no radios). `README.md` also still lists AP gitlink
  `17b7e085139e9bcbb0e4953d26aef9b6687d541c`; living status must match the
  actual `.ap` gitlink `9c5cc44f8b6c92dd56ad2427d13223d7d59c5656` (do not
  upgrade AP).
- Website on-demand AI review remains session-only. Companion review is a
  **separate** durable administrator inbox. Do not collapse those two facts.

---

## 5. Required reading

- `AGENTS.md`, `.ap/AP.md`, `.ap/AP_WORKER.md`, `.ap/PROMPT_CONTRACTS.md`
- `.ap/INFOSEC.md` §3–§5 (R1 only)
- `docs/WORKER_EXECUTION_CONTRACT.md`
- Accepted `01_report_00.md` Sections 15–16 (living files; W07 gate)
- Successor ADRs **read only**: `docs/adr/0066-…` through `0071-…`
- Current living files in Section 11
- `docs/adr/README.md` (already notes successors; do **not** edit ADR bodies;
  index edits are allowed only if a living-status sentence in README.md needs
  a matching one-line ADR pointer, not a rewrite of the ADR index file)

Do not reopen `01_planning_00.md` as authority.

---

## 6. Goal

One coherent W07 candidate: living documents describe the shipped companion
review inbox without contradicting implemented W02–W06 contracts.

Gate from accepted planning:

```text
no contradictory “X never auto-analyzes,” “two mutations only,”
“explicit publication only,” “iframe-only,” or “4–10 tags” wording remains
in the allowlisted living files
```

After W07, a later reader of PRODUCT/SPEC/ROADMAP/SERVER/SECURITY/
`X_COMPANION.md`/README must be able to state, without reading ADRs first:

1. Administrator-owned X catalog events may enqueue generic analysis when
   `FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED` is true. The flag stays
   **default false**. Ordinary `x.request` does not enqueue. YouTube stays
   fail-closed.
2. Live generic suggestion contract is **v4**, **1–5 most significant tags**.
   Historical v3 JSON remains readable. Companion maps to existing catalog
   tags only; it does not create tags. Movie/genres stay out of companion.
3. Companion review inbox exists: GET list/detail; POST opened; POST apply.
   Exactly four `companion_mutation` routes. GETs work with empty
   `companion_extension_origins`; mutations fail closed on empty origins when
   the request carries the extension Origin.
4. After review Save of checkmarked fields, G2 may publish in the same
   transaction when title + description + ≥1 tag are present. Origin
   `companion_review`. NIM completion, ingest Save, and row open do **not**
   publish. Ordinary `GET /api/media` stays published-only. Website Publish
   remains `admin_explicit`.
5. Side panel: native inbox list **above** the surviving hosted iframe;
   toolbar badge from `unopened_count`; MV3 `alarms` (`framenest.review-inbox`,
   1 minute). No `notifications` permission. Review overlay is a sibling
   `extension/ui/review.html|js|css`, not WAR. Ingest Save stays frozen.
6. Repository schema head is Alembic **0031**. Dated NUC production at `0028`
   is history. This whole does **not** deploy or enable the flag.

---

## 7. Accepted decisions (do not re-litigate)

- Parent wholes stay not-closed. This identity remains
  `framenest-companion-ai-review-inbox-mvp`.
- Do not hide new product scope inside infrastructure prose. Document this
  shipped foundation honestly; do not claim desktop app, complete Cover
  Studio, persistent AI Drafts, notifications, NUC enablement, or INFOSEC R3.
- Do not edit accepted ADR **bodies** (0016/0020/0044/0045/0049/0061/0063/
  0065/0066–0071). Successor notes already exist in `docs/adr/README.md`.
- Closed ROADMAP whole “Processed Publish Workflow…” may keep its historical
  owner goal “analysis must never automatically publish.” That sentence is
  still true for **NIM completion**. Add companion G2 **elsewhere** as a later
  successor (review Save, not analysis auto-publish). Do not reopen that
  closed whole or rewrite its status line.
- Tags ✅ is replace, not union. Empty Tags ✅ is forbidden.
- Residual Cooperator-owned risks stay named, not “fixed” in docs: G2 can
  publish after Tags-only ✅ if W1 already seeded title+description; tag
  replace can drop overlay tags not in the NIM set; NIM cost when the flag
  is later enabled on NUC.
- Unpacked companion origin remains
  `chrome-extension://omiihmnlkmieaafaphohakcgmbggppap`. Do not print the
  private key.

---

## 8. Orchestrator binding reconciliations

1. **README.md is in this allowlist.** Accepted planning listed six living
   files. `AGENTS.md` Project Truth also names README. Leaving README at
   schema `0030`, “category at Save,” and a stale AP gitlink would fail the
   contradiction gate. Surgical README updates only.
2. **Current schema head vs migration history.** Change sentences that claim
   the **current** head is `0030`. Keep “Migration `0030` adds …” as a step
   in the chain. Add `0031` as companion review open-states and field-source
   receipts (`ContentPublicationOrigin.COMPANION_REVIEW`). Do not claim the
   Ubuntu NUC production release is on `0031`.
3. **“Explicit publication only.”** Narrow, do not delete ADR-0049. Ordinary
   Gallery stays published-only. Website Publish stays. Companion review Save
   is the narrow successor when readiness holds (ADR-0068).
4. **Do not edit `docs/UBUNTU_NUC_DEPLOYMENT.md`, `AGENTS.md`, DEVELOPMENT,
   or extension/Python.** Parked K. A later deploy grant owns the runbook.
5. **Do not close the logical whole** and do not write “ready for NUC.”
6. **AP gitlink in README** must match `.ap` `9c5cc44f…`. Do not change the
   submodule.
7. **Keep professional English.** Do not introduce Czech.
8. **If a test file asserts the old living wording**, stop `PARTIAL` and name
   the path. Do not expand into `tests/` unless this prompt already listed it
   (it does not).

---

## 9. File-by-file living truth

Surgical edits. Preserve surrounding voice and existing non-contradictory
facts. Cite successor ADRs where a living sentence used to rest on 0044/0049/
0061 alone.

### `PRODUCT.md`

- Status: schema head **0031**; administrator companion review inbox exists.
- Distinguish website session-only suggestion review from durable companion
  review of successful generic runs.
- Automatic analysis still default-off; administrator-owned X may enqueue
  when the flag is on (ADR-0066). YouTube remains suppressed.
- G2: review Save may publish when ready; analysis completion does not.
- Companion does not create tags; live generic contract v4, 1–5 tags.
- Movie/genres out of companion.

### `SPEC.md`

- Opening implemented-foundation paragraph: schema head **0031** and the
  companion review inbox as shipped, without claiming NUC deployment.
- Replace the `0028` paragraph’s “no automatic AI invocation, and no
  automatic publication” with successor-accurate MUST language:
  ordinary/unmapped X and YouTube still fail-closed; administrator-owned X
  may enqueue when the scheduler flag is on; companion review apply may
  publish with origin `companion_review` when readiness holds; NIM
  completion still MUST NOT publish.
- Add a bounded MUST block for companion review routes, `companion_mutation`
  (exactly four), empty-origins GET vs mutation, tag replace, five-tag v4,
  and movie exclusion. Keep YouTube suppression.

### `ROADMAP.md`

- Current schema head **0031**, with `0031` described as companion-review
  open state and field-source receipts. Keep the 0007–0030 chain.
- Phase 7 implemented list: add native S1 inbox, badge/`alarms`, review
  overlay, and administrator X auto-analysis policy. Do not mark Phase 7
  fully exited (broader adapters remain).
- Do not reopen closed Processed Publish. Optionally one sentence later in
  Phase 7 or persistence notes: companion review Save may publish when ready
  (ADR-0068); analysis still never auto-publishes.

### `SERVER.md`

- Companion is not “two flagged X request mutations” only. Name the four
  `companion_mutation` routes and the two review GETs.
- Empty allowlist: mutations fail closed for extension Origin; hosted
  `external_origin` and GET inbox remain as implemented.
- Tailscale membership is still not administrator authority.

### `SECURITY.md`

- Origin allowlist now covers X submit/retry **and** review opened/apply.
- Delete or rewrite “does not change any other mutation route” / “two
  flagged X request routes.”
- Keep: no CORS, `X-FrameNest-Request: 1`, default-empty allowlist, no
  content-script FrameNest fetches, YouTube still barred from automatic
  analysis.
- Dated NUC `0028` production sentence may stay as history.

### `docs/X_COMPANION.md`

This is the operator/user guide. Add, without turning it into an ADR:

- Native `#review-inbox` above the surviving iframe; empty copy exactly
  `No analyzed items.`; ordinary 403 hides the section and badge.
- Toolbar badge from `unopened_count` (`1`…`99` / `99+`); no title in the
  badge. Alarm `framenest.review-inbox`, period 1 minute. No
  `notifications`.
- Second overlay: local `ui/review.html` via `chrome.runtime.getURL` +
  `#media=<uuid>`; not WAR; not inside `#frame`. Ingest Save overlay
  remains the frozen capture form.
- Server allowlist section: four mutation routes, not two. GETs do not
  require the allowlist in the same way mutations do (state the implemented
  split honestly).
- Keep load-in-Brave steps, pinned origin, and “this document does not
  authorize NUC deployment / flag enablement.”
- Residual: live signed-in X DOM and INFOSEC R3 remain later grants.

### `README.md`

- Packaged Alembic head **0031**.
- Companion: frozen ingest Save (no category radios); administrator review
  inbox in the side panel; G2 after review Save when ready; auto-analysis
  still default-off with administrator-owned X eligibility when enabled.
- Fix the AP gitlink block to `9c5cc44f8b6c92dd56ad2427d13223d7d59c5656`.
- Add compact ADR pointers for 0066–0071 near the existing 0064 bullet.
- Do not rewrite the entire README or the non-goals list except to avoid
  the contradiction gate.

---

## 10. Causal slices

### Slice 1 — product/architecture living status

`PRODUCT.md`, `SPEC.md`, `ROADMAP.md`, `SERVER.md`.

Gate: current-head `0030` claims are gone; SPEC no longer says X never
auto-analyzes and never publishes; SERVER no longer says two mutations only.

### Slice 2 — trust surface, operator guide, repository status

`SECURITY.md`, `docs/X_COMPANION.md`, `README.md`.

Gate: Section 13 contradiction grep exits 0; Node suite still **73 passed**;
no `src/` / `extension/` / ADR-body diff.

At most **two** local commits. Example: `docs: record companion review inbox
in product living status` then `docs: update companion trust and operator
guide`. Combined envelope may produce one commit covering both slices if the
diff stays reviewable. No push.

---

## 11. Changed-path allowlist

```text
PRODUCT.md
SPEC.md
ROADMAP.md
SERVER.md
SECURITY.md
docs/X_COMPANION.md
README.md
```

**Meta**

```text
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/07_report_00.md
```

Forbidden: `src/`; `extension/`; `tests/`; Alembic; `docs/adr/*.md` bodies;
`docs/adr/README.md` unless a one-line living cross-link is strictly required
and no ADR body changes; `docs/UBUNTU_NUC_DEPLOYMENT.md`; `AGENTS.md`;
`.ap/`; NUC; push.

If the contradiction lives only in an ADR body, leave it: successor ADRs
already record the supersession. Living files must not repeat the superseded
claim as current truth.

---

## 12. Git, network, secret, side-effect

```text
Git authority: local commits only, at most two, on feat/x-meme-browser-companion; no amend of c8b757a or earlier; no rebase, reset, stash, clean, fetch-that-rewrites, or push; do not update git config; do not skip hooks
Network authority: git ls-remote to the two public refs; no provider; no NUC; no live FrameNest origin calls
Secret authority: none
Filesystem authority: read FrameNest and pinned .ap; write only Section 11 plus the Meta report
Side-effect authority: reversible local documentation/commits
Dependency authority: none
Browser authority: none
```

HEREDOC messages. Do not commit Meta or secrets. Amend only your own
not-yet-pushed commit, and only if a hook auto-modified files after success.

---

## 13. Tests and contradiction gate

Each candidate SHA:

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <SHA>
```

JavaScript regression (worktree root; expect **73 passed**):

```text
node --test tests/companion_review_extension.test.js tests/x_companion_extension.test.js tests/companion_web_bridge.test.js tests/x_acquisition_cockpit.test.js
```

Contradiction grep after edits (must find **no** current-truth hits in the
allowlisted files). Run from the repository root. Treat as a failed gate if
any line still asserts the superseded claim as present tense:

```text
rg -n -e 'two flagged X request' -e 'does not change any other mutation route' -e 'no automatic AI invocation' -e 'iframe-only' -e '4–10 tags' -e '4-10 tags' -e 'four to ten tags' -e 'can choose a canonical content category at Save' PRODUCT.md SPEC.md ROADMAP.md SERVER.md SECURITY.md docs/X_COMPANION.md README.md
```

Additional current-head check (must not match **current-head** sentences;
historical “Migration \`0030\`” / “revision \`0030\` adds” / NUC dated
`0028` are allowed):

```text
rg -n -e 'schema head `0030`' -e 'schema head is revision `0030`' -e 'through schema head `0030`' PRODUCT.md SPEC.md ROADMAP.md SERVER.md SECURITY.md docs/X_COMPANION.md README.md
```

AP gitlink check:

```text
rg -n '17b7e085139e9bcbb0e4953d26aef9b6687d541c' README.md
```

That last command must print no matches. `9c5cc44f8b6c92dd56ad2427d13223d7d59c5656`
must appear in the README AP gitlink block.

Do not run `FRAMENEST_RUN_BROWSER_EVIDENCE=1`. Do not run Python `test-focus`.
Do not call NVIDIA. Schema head on disk must remain Alembic **0031**
(unchanged by this Worker).

---

## 14. Negative authority

- Native Plan Mode, Max, sub-agents
- Push, NUC, sudo, provider, signed-in browser, browser-evidence flag
- Any `src/` / `extension/` / Alembic / ADR-body mutation
- Enabling `FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED`
- Adding `notifications`
- Re-polishing ingest Save
- INFOSEC R3
- Closing the logical whole
- Claiming production/NUC already runs this branch

---

## 15. INFOSEC R1 (inline)

Documentation only. Do not paste credentials, private keys, host-specific
identifiers, or live Tailscale node names. Keep the public unpacked origin
already in `docs/X_COMPANION.md`. Residual: G2 publication after review Save
and later R3 before deploy — name them, do not expand them.

---

## 16. Terminal report

Begin exactly:

```markdown
### Report for ORCHESTRATOR_CHAT
```

```text
Logical whole identity: framenest-companion-ai-review-inbox-mvp
Worker session ordinal: 07
Worker exchange ordinal: 01
```

Include: status; handshake; baseline ledger; changed paths; proof that each
Section 6 numbered fact is now stated in living docs; Section 13 commands and
exit codes (paste grep output if non-empty); git SHAs (no push); confirmation
of zero `src/` / `extension/` / ADR-body diff vs `c8b757a`; INFOSEC residual;
near-misses / pre-existing classification; smallest next step = Orchestrator
verifies living docs, then Cooperator acceptance. Do not self-issue R3, NUC,
or closure.

Write only the Meta path in Section 1.

---

## 17. Quality bar

`PASS` only if HEAD descends from `c8b757a` via this Worker; schema head
remains **0031**; no Python/extension/ADR-body diff; Section 13 grep gates
are clean; Node suite still 73 passed; `ap project check` PASS; living files
state the six Section 6 facts without the forbidden current-truth phrases;
no push.

`PARTIAL` for one named evidence gap. `BLOCKED` for baseline/Plan Mode/
allowlist contradiction.

---

## 18. Stop

After the terminal report, stop. Do not start INFOSEC R3, NUC enablement, or
logical-whole closure.
