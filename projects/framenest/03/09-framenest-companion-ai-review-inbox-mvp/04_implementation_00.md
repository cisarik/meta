# Authoritative Prompt for Fresh Worker 04

## FrameNest Companion AI Review Inbox — opened, apply, and G2 publication

You are a Worker instance assigned to WORKER. Read this complete prompt before
taking any action.

Worker 03 landed Alembic **0031**, the historical stored-suggestion codec, and
administrator inbox/history **GET** routes at
`1d9a71ed1e32ba9001f9f9d4f8935be34800aa6b`. That authority is expired. The
COOPERATOR accepted the architecture on 2026-08-23, including G2. The
ORCHESTRATOR independently verified W03 (`ap project check` PASS; focused suite
**211 passed** against `--baseline 1d9a71e`).

Your task is **slice W04 only**: mark-opened POST, review-apply POST, dual
apply capabilities on `RoutePolicy`, durable receipts, website-Save receipt
invalidation, and atomic G2 publication with origin `companion_review`. Do not
implement overlay, badge, alarms, living docs, or NUC.

Do not enter Native Plan Mode. Do not use Max. Extra High is requested; if the
client does not expose a measurable Extra High SKU, continue only while Plan
Mode stays off and Max is unused, and record that in the handshake. If Native
Plan Mode is on, stop `BLOCKED`.

```text
Role: WORKER
Persistent role identity: WORKER
Logical whole identity: framenest-companion-ai-review-inbox-mvp
Prior logical whole identities:
  framenest-x-meme-browser-companion-mvp
  framenest-x-companion-save-alias-mvp
  framenest-x-companion-save-category-mvp
  framenest-x-save-overlay-edit-subset-mvp
Worker session ordinal: 04
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Worker session profile: Fresh Implementation Worker
Phase: implementation
Task identity: FN-COMPANION-AI-REVIEW-INBOX-IMPL-04
Task type: bounded implementation candidate
Native planning mode: not-used
Reasoning recommendation: extra-high
Evidence posture: non-independent
Independence required: no
Authority renewal: not applicable — fresh Worker authority originates only in this prompt
Prior implementation report: Worker 03 / exchange 01 implementation-PASS at 1d9a71e; authority expired
Continuity anchor: none — do not resume Worker 03
Internal delegation posture: not-used
Accountable Worker: one WORKER
Material phase gate: yes
Changed material axis: mutation-authority-or-side-effect-class
Routing reopened for: mutation-authority-or-side-effect-class
Unchanged axes reopened: none
Ordinary-only trigger: no
Automatic model selection: off
Enhanced/maximum mode: not requested
```

```text
Implementation authority: explicit
Native planning mode: not-used
Worker session target: fresh-worker-session
Exact baseline: 1d9a71ed1e32ba9001f9f9d4f8935be34800aa6b
Changed-path allowlist: Section 11
Implementation boundaries: Sections 8, 9, 10, 12, and 14
Independence required: no
```

```text
Evidence tier: E3
Evidence tier basis: new administrator companion mutations, dual-capability authorization, atomic G2 publication; no overlay/NUC
Authorized implementation stages: causal slices 1–3 in Section 10
Combined implementation envelope: allowed for those three slices only
Implementation stage gates: Section 10; a failed gate stops the sequence
Independent acceptance: not-required (this session)
Rollback or recovery checkpoint: local Git commits on feat/x-meme-browser-companion; no push; no NUC
Activated stricter profile: INFOSEC.md R1 inline only
Terminal implementation report point: after slices 1–3 are committed and validated, before overlay/badge/alarms
```

```text
COOPERATOR: Michal
ORCHESTRATOR: ORCHESTRATOR_CHAT
WORKER: this fresh implementation session
```

Internal delegation, sub-agents, parallel Workers, Explore tasks, and hidden
secondary workstreams are not authorized.

Repository documentation, code comments, test names, commit subjects, and the
terminal Worker report must use professional English. Czech is forbidden.
The terminal report must begin exactly:

```text
### Report for ORCHESTRATOR_CHAT
```

Implementation PASS is not acceptance, publication, deployment, or closure.
`Logical-whole closure: not-closed`.

Protocol-variant selection:

```text
Canonical repository identity: https://github.com/cisarik/ap.git
Immutable version identity: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Declared variant: stable
Governing variants in effect: one
Declaration location: project governing rules
Rules from non-governing variants: none
Migration required: no
```

---

## 1. External trace and Meta write boundary

```text
External trace disposition: configured
Trace discovery: cisarik/meta repository path projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp
Trace project key: framenest
Trace logical-whole projection identity: 03/09-framenest-companion-ai-review-inbox-mvp
Trace authority: historical-evidence-only
Trace archival owner: ORCHESTRATOR after the outcome exists
Trace visibility: public
Trace companion outcome: report
Trace self-granted status: none
Downloadable prompt filename: 04_implementation_00.md
Destination path: projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/04_implementation_00.md
Archival: wait-for-report
```

You may **read** (historical; this prompt wins on W04 scope):

```text
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/01_report_00.md
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/03_implementation_00.md
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/03_report_00.md
```

Frozen hashes at issuance (re-check; stop `BLOCKED` if drifted):

```text
01_report_00.md SHA-256 51e124c02009a6822ebb36afc8893187074c680cd139462d79e72cb61bab75ce
03_implementation_00.md SHA-256 ca57ab788228e9aed88293da687470fdaedd8588309c9fd0e69b230adc0473c5
03_report_00.md SHA-256 061cfa7b91002fe899352158dda8ff13aff9ebf8ffbb7770806b3a0a292417a2
```

You may **write** only:

```text
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/04_report_00.md
```

Do not alter any other Meta path. Do not stage or commit Meta. If the report
file cannot be written, return it verbatim in chat.

---

## 2. Communication and human-governance routing

```text
Worker prompt language: professional English
Worker report language: professional English
Direct-user Slovak presentation: Orchestrator-owned
Report header: ### Report for ORCHESTRATOR_CHAT
Cooperator visibility: W04 grant already issued inside the accepted envelope
Human decision points remaining outside this grant: overlay/badge/alarms (W05–W06), living docs (W07), R3, NUC
Deterministic steps inside bounded authority: slices 1–3, tests, local commits
Internal delegation posture: not-used
```

---

## 3. Capability handshake

Fresh session. Compact handshake before mutation. Record requested / directly
observed / inferred / unknown. Capability does not grant authority. If Plan
Mode is on, stop `BLOCKED`.

```text
Validation ladder: selected
Inspection and provenance: required
Existing focused tests: Section 13
Broad or full suite: not-used (schema-head owners in Section 13 only if you edit them)
Independent acceptance: not-required
```

Browser, provider, NUC, publication, and deployment annexes are **not
activated**.

---

## 4. Repository gate and exact baseline

```text
Expected canonical root: /home/agile/Projects/framenest
Repository checkout topology: standalone checkout
Working-copy topology: canonical checkout
Applicable branch: feat/x-meme-browser-companion
Expected HEAD: 1d9a71ed1e32ba9001f9f9d4f8935be34800aa6b
Expected parent: 807a02f161c4b86b84f9fb7ec4b1658d3649b532
Expected tree: c456e4700cd0d46586f2e994518e2dcc0aa809d7
Expected subject: feat: expose administrator companion review inbox GET routes
Working tree: expected clean
Upstream: none configured (expected)
Schema head at baseline: Alembic 0031
```

```text
.ap gitlink and checkout: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Detached submodule checkout: acceptable
```

Issuance-time public refs (re-verify with `git ls-remote`, no fetch):

```text
cisarik/framenest refs/heads/main  045f33b44897a6f3949cc515792336396f1d33a1
cisarik/ap        refs/heads/main  9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
```

If local HEAD is not `1d9a71e`, stop `BLOCKED` unless the only difference is
this Worker’s own later commits. Do not stash, reset, or clean.

Canonical Python route only:

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <EXACT SHA UNDER TEST>
./.ap/ap exec --root /home/agile/Projects/framenest --baseline <EXACT SHA UNDER TEST> --operation test-focus -- <tests> -q -p no:cacheprovider
```

After the first commit, `--baseline` is that candidate SHA. No ambient
`.venv/bin/python` / `python` / `python3` / `poetry run`.

Spot-check before mutation:

- Schema head is **0031**. Do **not** add 0032. Tables
  `companion_review_open_states` and `companion_review_field_sources` already
  exist. `ContentPublicationOrigin.COMPANION_REVIEW` already exists.
- GET `/api/companion/review-inbox` and GET `.../{media_id}` exist with
  `media.workflow.read` and `companion_mutation=False`.
- `companion_mutation` is still exactly the two X POSTs. W04 adds exactly two
  more: opened and apply.
- `RoutePolicy` has no `additional_capabilities` yet. Add it here.
- `PublishContent` / SQLite `publish()` still insert
  `ContentPublicationOrigin.ADMIN_EXPLICIT`. **Leave that path unchanged.**
  Companion G2 must not call `PublishContent.execute`.
- `SaveMediaMetadata` always writes the full title/description/tag tuple.
  Companion apply must **not** call it for partial apply. Website Save must
  still go through `save_media_metadata`; that is the receipt-invalidation hook.
- Historical codec and `map_suggested_tags` already live in
  `application/companion_review.py`. Reuse them. Do not feed apply through
  live v4 `MediaSuggestion`.
- Ingest Save overlay (`extension/ui/save.*`) and X auto-analysis helper remain
  frozen.
- Live `PROMPT_VERSION` remains `framenest-media-suggestion-v4`;
  `TAG_MAX_COUNT = 5`. Do not change prompts.

---

## 5. Required reading

- `AGENTS.md`, `.ap/AP.md`, `.ap/AP_WORKER.md`, `.ap/PROMPT_CONTRACTS.md`
- `.ap/INFOSEC.md` §3–§5 (R1 only)
- `docs/WORKER_EXECUTION_CONTRACT.md`
- `docs/adr/0067-administrator-companion-review-inbox-and-mutation-trust.md`
- `docs/adr/0068-companion-review-save-and-readiness-triggered-publication.md`
- `docs/adr/0070-companion-exclusion-of-movie-workflows.md`
- `01_report_00.md` Sections 7 (tag mapping, receipts, opened semantics), 8
  (HTTP/capability/audit matrix and apply JSON), 10 (movie recheck), 11 (G2
  transaction), 14 (W04 gate)
- `src/framenest/adapters/api/tailscale_ingress.py` (`RoutePolicy`,
  `_mutation_origin_allowed`, capability check, audit-before-app)
- `src/framenest/adapters/api/companion_review_api.py`
- `src/framenest/adapters/api/application.py` (companion-review wiring)
- `src/framenest/application/companion_review.py`
- `src/framenest/infrastructure/persistence/companion_review_repository.py`
- `src/framenest/infrastructure/persistence/media_metadata_repository.py`
  (`save_media_metadata`, `derive_collection_state`)
- `src/framenest/infrastructure/persistence/content_publication_repository.py`
  (`publish` uses `ADMIN_EXPLICIT` — do not reuse for G2)
- `src/framenest/infrastructure/persistence/engine.py`
  (`run_in_immediate_transaction`)
- `src/framenest/domain/content_publication.py`
  (`derive_content_publication_readiness`, `MISSING_FIELD_ORDER`)
- `src/framenest/domain/identity_access.py`
  (`CAPABILITY_MEDIA_CONTENT_PUBLISH`, `CAPABILITY_METADATA_CANONICAL_WRITE`,
  `CAPABILITY_MEDIA_WORKFLOW_READ`, `CAPABILITY_METADATA_ALIAS_WRITE`)
- `tests/contract/test_x_route_policy.py`
- `tests/contract/test_companion_review_api.py`
- `tests/contract/test_tailscale_ingress_security.py` (companion origin +
  `test_audit_failure_blocks_the_privileged_action`)
- `tests/contract/test_content_publication_api.py` (audit-before-mutation
  pattern; website Publish stays 409 for not-ready — companion apply is 200)

---

## 6. Goal

One coherent W04 candidate:

1. Extend `RoutePolicy` with `additional_capabilities: tuple[str, ...] = ()`.
   Ingress requires the primary capability **and** every additional capability.
   Existing policies stay source-compatible. Audit records keep the **primary**
   capability; the distinct `audit_action` identifies companion apply.
2. `POST /api/companion/review-inbox/{media_id}/opened` marks the displayed
   successful generic run. Advancement is monotonic.
3. `POST /api/companion/review-inbox/{media_id}/apply` applies only checkmarked
   fields from the selected immutable run, upserts receipts, then atomically
   publishes with origin `companion_review` when ADR-0049 readiness holds and
   the item is unpublished. HTTP **200** including `not_ready`.
4. Website canonical Save removes field-source receipts whose digest no longer
   matches. Readers already suppress mismatched receipts (W03); writers must
   also delete them.

---

## 7. Accepted decisions (do not re-litigate)

From accepted `01_report_00.md` and Cooperator 2026-08-23:

- G2: publish after a **valid review Save** of checkmarked fields when
  canonical title, description, and ≥1 tag are then present. Not on NIM
  completion, ingest Save, row opening, or dropdown selection.
- Ordinary `GET /api/media` stays published-only. Website Publish remains and
  still uses `admin_explicit`.
- Tags ✅ **replaces** the complete canonical tag set with the remaining
  ordered mapped keys; it is not a union. Zero-tag companion apply is
  forbidden. Never create tags. Companion never submits movie/genres/
  collection/filename/source/category.
- Title and description are copied **server-side** from the selected stored
  run. The client never submits those strings.
- `fields` is a unique nonempty subset of `display_title`, `tags`,
  `description`.
- `tag_keys` must be empty unless `tags` is selected; when selected it must
  contain 1–5 unique keys that are an **ordered subsequence** of the server’s
  current eligible mapped keys for that run (status `mapped` only, suggestion
  order, first five). Chip removal is allowed; reordering is not.
- Source digests: SHA-256 over UTF-8 canonical JSON via existing
  `canonical_field_digest` (scalar JSON strings for title/description; ordered
  JSON array of **applied** tag keys for tags).
- Identical-value selection from a newer source run still upserts the receipt
  (source run / applied_at change; canonical value may be unchanged).
- Opened state is per normalized `actor_login_key` + `media_id`, referencing
  the displayed `analysis_run_id`. A new successful latest run remains
  unopened until that latest run is opened. Opening an older history run must
  **not** overwrite a newer already-opened marker (monotonic by inbox order
  `(completed_at_ms, analysis_run_id)`).
- Apply does **not** implicitly mark opened.
- Movie category recheck inside the apply/opened write transaction: concurrent
  change to movie → sanitized 409, no mutation.
- Empty `companion_extension_origins` fails closed for extension Origin on
  these POSTs. Hosted `external_origin` remains an allowed mutation Origin
  (same as other unsafe methods). GETs stay unblocked by empty origins.
- New POSTs require Origin in `{external_origin} ∪ companion_extension_origins`
  plus `X-FrameNest-Request: 1`. No CORS. No alias PUT.
- Both ingress **and** handler verify apply capabilities.
- Alias capability (`metadata.alias.write`) is insufficient for apply.
- Middleware audit must succeed **before** entering the write transaction.
  Audit failure → 500 `AUDIT_UNAVAILABLE`, no DB mutation.
- Residual Cooperator-owned product risk (do not “fix”): G2 can publish after
  Tags-only ✅ when W1 already seeded title+description; tag replace wipes
  overlay tags not in the NIM mapped set.

---

## 8. Orchestrator binding reconciliations

1. **Do not call `PublishContent.execute` or repository `publish()` for G2.**
   Those insert `admin_explicit`. Companion apply inserts
   `companion_review` inside the **same** `BEGIN IMMEDIATE` as metadata and
   receipts. Already-published rows keep their original timestamp and origin.
2. **Do not call `SaveMediaMetadata.execute` for companion apply.** It always
   writes all three user fields and would mutate unchecked fields. Implement
   a dedicated apply write that copies only selected fields and preserves
   category, acquisition source, creator, genres, and unselected
   title/description/tags. After a tags replace, call existing
   `derive_collection_state` on the **resulting** tag set. Collection may
   change only as that deterministic catalog consequence — never from the NIM
   `collection` field.
3. **Website Save receipt invalidation is required in W04.** In
   `SqliteMediaMetadataRepository.save_media_metadata`, after a real write
   (not the early `unchanged` return), delete receipts for
   `display_title` / `description` / `tags` whose stored `value_digest` no
   longer matches `canonical_field_digest` of the new canonical value.
   Unchanged fields keep their receipts. First-catalog writes in
   `upload_publication_repository` typically have no receipts; do not expand
   that module unless a focused test proves a receipt can exist there.
4. **`additional_capabilities` is this Worker.** Add the slot, constructor
   default `()`, and ingress loop. `__init__` must reject a nonempty
   additional tuple when `capability` is `None`. Existing `RoutePolicy(...)`
   call sites stay valid. GET review policies stay additional-empty.
5. **Companion mutation set becomes four routes:** the two existing X POSTs
   plus opened plus apply. Update
   `test_only_x_request_mutations_are_companion_flagged` (rename if the name
   would lie) and
   `test_companion_origin_is_accepted_only_on_flagged_x_request_routes`.
   Canonical-tag POST with a companion Origin must remain
   `MUTATION_ORIGIN_FORBIDDEN`.
6. **Apply HTTP 200 including `not_ready`.** Do not copy website Publish’s
   409-for-not-ready. Companion `not_ready` is a successful metadata/receipt
   transaction without a publication insert.
7. **Opened POST body is `{analysis_run_id}`.** The run must be a successful
   generic inbox-eligible run for that media (same filter as history). Missing
   media/run → 404. Wrong run (failed, other media, movie_identification, not
   generic-success) → 409. Movie media → 409. Stale older-than-current opened
   marker → HTTP 200 with **no overwrite**; response reports the durable
   marker that remains. Same run is idempotent (may refresh `opened_at_ms`).
8. **Apply `analysis_run_id` is the dropdown-selected run**, not necessarily
   the latest. Decode **that** run, map **that** run’s tags, copy
   title/description from **that** run.
9. **Stale mapping → 409** without mutation: submitted `tag_keys` that are
   not an ordered subsequence of the freshly recomputed eligible mapped keys.
10. **Do not add Alembic 0032.** Schema is complete. Do not edit ADR bodies,
    PRODUCT, SPEC, overlay files, or X auto-analysis policy.
11. **Identity mapping-before-coordinator from W02 stays.** Grow
    `CompanionReviewApiDependencies` with opened/apply use cases; include
    them only when catalog/persistence is available (503 when not).
12. **GET contracts stay.** List/detail shapes remain; receipts will start
    appearing after apply. Do not add `audit_action` to GETs.

---

## 9. HTTP contracts (POST)

| Method/path | Primary capability | Additional | `companion_mutation` | Audit |
|---|---|---|---|---|
| `POST /api/companion/review-inbox/{media_id}/opened` | `media.workflow.read` | none | true | `companion.review.open`; target `media` / `media_id` |
| `POST /api/companion/review-inbox/{media_id}/apply` | `media.content.publish` | `metadata.canonical.write` | true | `companion.review.apply_publish`; target `media` / `media_id` |

All responses `Cache-Control: no-store`. Bodies `extra=forbid`.

Opened request:

```json
{"analysis_run_id": "uuid"}
```

Opened 200 (post-state after the monotonic decision):

```json
{
  "media_id": "uuid",
  "opened_run_id": "uuid",
  "opened_at_ms": 0,
  "unopened": true
}
```

`unopened` uses the same law as GET list: true when the durable marker is
missing or differs from the **latest** successful generic run.

Apply request:

```json
{
  "analysis_run_id": "uuid",
  "fields": ["display_title", "tags", "description"],
  "tag_keys": ["existing-key"]
}
```

Apply 200 (every valid transaction, including `not_ready`):

```json
{
  "metadata_status": "created|updated|unchanged",
  "canonical": {
    "display_title": "string|null",
    "description": "string|null",
    "tags": [{"key": "string", "display_name": "string", "position": 0}],
    "field_sources": {
      "display_title": null,
      "tags": {
        "analysis_run_id": "uuid",
        "completed_at_ms": 0,
        "provider_id": "string",
        "model_id": "string",
        "applied_at_ms": 0
      },
      "description": null
    }
  },
  "publication": {
    "status": "published|already_published|not_ready",
    "state": "published|unpublished",
    "origin": "companion_review|admin_explicit|legacy_backfill|null",
    "published_at_ms": null,
    "ready": false,
    "missing_fields": ["display_title", "description", "tags"]
  }
}
```

`missing_fields` order remains `display_title`, `description`, `tags`.
`origin` on already-published items is the **preserved** origin, which may be
`admin_explicit` or `legacy_backfill`, not rewritten to `companion_review`.

Errors (sanitized; no suggestion bodies, no SQL, no stack):

- Missing media or unknown `analysis_run_id`: 404
- Malformed JSON / extra fields / empty or duplicate `fields` / `tags`
  selected with empty `tag_keys` / `tag_keys` present when `tags` not
  selected / non-unique keys: 422
- Movie, wrong run, stale mapping: 409
- Ordinary identity: ingress 403 `CAPABILITY_DENIED` (no titles)
- Extension Origin with empty allowlist, spoofed Origin, or missing
  `X-FrameNest-Request: 1`: 403 mutation origin/header codes
- Catalog unavailable: 503
- Repository failure: 500
- Audit failure: 500 `AUDIT_UNAVAILABLE` before any write

Handler apply must require **both** capabilities even if ingress already did.
Handler opened must re-check `media.workflow.read`.

---

## 10. Causal slices

### Slice 1 — authorization + website receipt invalidation

- `RoutePolicy.additional_capabilities` + ingress enforcement.
- Website `save_media_metadata` deletes stale receipts by digest.
- Route-policy tests updated for the *forthcoming* two POSTs only after those
  policies exist; if this slice commits first, keep the two-X assertion until
  slice 3 lands, or land policies as no-op-unwired stubs **only if** tests
  would otherwise lie. Prefer landing policies together with HTTP in slice 3
  and keeping slice 1 to the RoutePolicy type + website receipts + unit tests
  of additional-capability denial.

Gate: existing policies construct; additional default `()`; a synthetic
identity missing one additional capability is denied; website Save that
changes title/description/tags drops those receipts and preserves others.

### Slice 2 — application/repository opened + apply/G2

Port methods + `BEGIN IMMEDIATE` writes on `SqliteCompanionReviewRepository`.
Reuse W03 codec, mapping, digest, movie filter, and readiness helper.

Apply transaction order (accepted plan §11):

1. Load media, category, selected successful generic run, actor.
2. Decode stored suggestion with the historical codec.
3. Recompute canonical tag mapping; validate submitted keys.
4. Apply only selected fields; preserve all others.
5. Upsert receipts for selected fields (including same-value newer source).
6. Derive readiness with existing domain function.
7. If ready and unpublished, insert publication origin `companion_review`.
8. If already published, preserve timestamp/origin.
9. Commit once.

A database failure rolls back metadata, receipts, and publication together.

Gate: unit/persistence tests in Section 13 (monotonic opened, per-field
preserve, tag replace, subsequence, zero-tag forbidden, receipts, ready
publish, already-published idempotence, movie race, atomic rollback).

### Slice 3 — HTTP POST + Origin/audit/capability contracts

Router, `ROUTE_POLICIES`, `application.py` wiring, contract tests.

Gate: admin companion Origin 200; ordinary 403; empty origins reject
extension Origin; hosted origin still allowed; missing header 403; dual
capabilities; alias insufficient; audit failure blocks writes; G2 200
`not_ready` vs `published`; ordinary Gallery omits unpublished
companion-applied items and includes them after G2 publish; website Publish
still `admin_explicit`.

At most **three** local commits. Example: `feat:` RoutePolicy + receipt hook,
`feat:` opened/apply persistence, `feat:` HTTP. No push.

---

## 11. Changed-path allowlist

**Authorization / API / application**

- `src/framenest/adapters/api/tailscale_ingress.py`
- `src/framenest/adapters/api/companion_review_api.py`
- `src/framenest/adapters/api/application.py`
- `src/framenest/application/companion_review.py`
- `src/framenest/application/ports/companion_review_repository.py`
- `src/framenest/infrastructure/persistence/companion_review_repository.py`
- `src/framenest/infrastructure/persistence/media_metadata_repository.py`

**Tests (create or update)**

- `tests/unit/application/test_companion_review.py`
- `tests/unit/infrastructure/persistence/test_companion_review_repository.py`
- `tests/contract/test_companion_review_api.py`
- `tests/contract/test_x_route_policy.py`
- `tests/contract/test_tailscale_ingress_security.py`
- `tests/contract/test_media_metadata_repository.py`
- `tests/unit/application/test_media_metadata.py` **only if** a unit of the
  Save path must prove receipt deletion and the repository contract test is
  insufficient
- `tests/contract/test_media_metadata_api.py` **only if** an HTTP website-Save
  proof is required to show receipts disappear after Edit

Do **not** add a new Alembic version. Do **not** edit
`content_publication_repository.py` unless a one-line shared helper for
readiness load is strictly smaller than duplicating the existing private
loader; prefer duplicating a tiny read inside the companion repository over
changing website Publish origin.

**Meta**

- `/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/04_report_00.md`

Forbidden: ingest Save overlay, X auto-analysis helper, movie identification,
v4 prompt, overlay/badge/alarms, PRODUCT/SPEC/`X_COMPANION.md`, accepted ADR
bodies, `nvidia_nim.py`, NUC scripts, push.

If a current-head assertion lives in a file not listed and the focused suite
fails, stop `PARTIAL` and name the path.

---

## 12. Git, network, secret, side-effect

```text
Git authority: local commits only, at most three, on feat/x-meme-browser-companion; no amend of 1d9a71e or earlier; no rebase, reset, stash, clean, fetch-that-rewrites, or push; do not update git config; do not skip hooks
Network authority: git ls-remote to the two public refs; no provider; no NUC
Secret authority: none
Filesystem authority: read FrameNest and pinned .ap; write only Section 11 plus the Meta report
Side-effect authority: reversible local source/tests/commits
Dependency authority: none
Browser authority: none
```

Commit subject examples:

- `feat: add dual-capability companion review mutations`
- `feat: apply companion review fields and publish when ready`

HEREDOC messages. Do not commit Meta or secrets. Amend only your own
not-yet-pushed commit, and only if a hook auto-modified files after success.

---

## 13. Tests

Each candidate SHA:

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <SHA>
```

Minimum `test-focus` set (extend with any allowlisted file you actually edit):

```text
./.ap/ap exec --root /home/agile/Projects/framenest --baseline <SHA> --operation test-focus -- \
  tests/unit/application/test_companion_review.py \
  tests/unit/infrastructure/persistence/test_companion_review_repository.py \
  tests/contract/test_companion_review_api.py \
  tests/contract/test_x_route_policy.py \
  tests/contract/test_tailscale_ingress_security.py \
  tests/contract/test_media_metadata_repository.py \
  tests/unit/application/test_x_automatic_analysis_policy.py \
  tests/integration/persistence/test_companion_review_migration.py \
  -q -p no:cacheprovider
```

If you edit `tests/unit/application/test_media_metadata.py` or
`tests/contract/test_media_metadata_api.py`, add them. After the last commit,
run **all edited** test files in one `test-focus`. Do not run a full `test`
operation. Do not run JS companion tests. Do not call NVIDIA.

Required cases:

- Displayed-run opened write; actor isolation; latest-run unread after a newer
  success; monotonic refusal to replace a newer marker with an older run
  (200, durable marker unchanged).
- Origin/header fail-closed; empty companion allowlist rejects extension
  Origin on opened/apply; hosted `external_origin` still allowed; GETs still
  work with empty origins.
- Exactly four `companion_mutation` routes: two X POSTs + opened + apply.
  Alias PUT and canonical-tag POST remain unflagged.
- Apply requires both `media.content.publish` and `metadata.canonical.write`
  at ingress and handler. Ordinary user / alias-only identity 403. Synthetic
  identity with only one of the two 403.
- Per-field writes; unchecked title/description/tags preserved; category,
  source, creator, genres preserved.
- Tags replace, not union; ordered subsequence accepted; permutation rejected
  409; unknown/ambiguous/legacy_limit keys rejected; empty Tags selection
  422; no tag row created.
- Receipt create; same-value newer-run receipt update; website Save drops
  only changed-field receipts; GET/apply readers still hide digest-mismatched
  receipts if a row is left.
- Ready unpublished → insert `companion_review` (`publication.status=published`).
- Already published → `already_published`, origin/timestamp preserved.
- Not ready → HTTP 200, `not_ready`, no publication row.
- Movie race 409 and no metadata/receipt/publication writes.
- Forced failure after metadata write rolls back receipts and publication.
- Audit drop/failure prevents apply/opened DB mutation.
- Ordinary `GET /api/media` omits the item while unpublished after apply and
  includes it after G2 publish. Website Publish path still writes
  `admin_explicit` (existing tests remain green).
- Client cannot submit title/description (`extra=forbid`).
- W03 GET list/detail regressions remain green. W02 X auto-analysis tests
  remain green.

---

## 14. Negative authority

- Native Plan Mode, Max, sub-agents
- Push, NUC, sudo, provider, signed-in browser
- Overlay, badge, `alarms`, `notifications`, `extension/ui/review.*`
- Living PRODUCT/SPEC/`X_COMPANION.md` / ADR body edits
- Alembic 0032
- Changing website Publish to `companion_review` or auto-publishing from NIM
  completion / ingest Save / opened
- CORS, `all_urls`, alias PUT apply
- Creating canonical tags from companion apply
- Enabling `FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED`
- Closing the logical whole

---

## 15. INFOSEC R1 (inline)

- Suggestion title/description/tags remain untrusted plain text. No UI this
  slice, therefore no `innerHTML`. JSON only.
- Ordinary callers 403 on opened/apply; no title leakage via 404/empty
  confusion on list remains GET’s 403.
- Audit actions record media id / actor / primary capability / action name
  only. Do not put suggestion bodies, canonical title/description/tags,
  `tag_keys`, result JSON, credentials, or cookies in audit rows or logs.
- Origin + `X-FrameNest-Request` are CSRF-equivalent, not authorization.
  Dual apply capabilities remain mandatory.
- Empty companion origin allowlist must not enable extension POSTs.
- Digest defense: client never supplies title/description text; tag keys are
  validated as a subsequence of server mapping, not trusted as new names.
- Residual: G2 publishes structurally ready items without judging semantic
  quality; Cooperator-owned. Independent R3 remains later, before deploy.

---

## 16. Terminal report

Begin exactly:

```markdown
### Report for ORCHESTRATOR_CHAT
```

```text
Logical whole identity: framenest-companion-ai-review-inbox-mvp
Worker session ordinal: 04
Worker exchange ordinal: 01
```

Include: status; handshake; baseline ledger; changed paths; proof of Section 7
invariants (four companion mutations, dual apply capabilities, G2 origin,
200-including-not-ready, website receipt invalidation, monotonic opened);
commands/exit codes; git SHAs (no push); INFOSEC residual; near-misses /
pre-existing classification; smallest next step = Orchestrator issues W05
(native S1 list + badge + alarms) after verification. Do not self-issue W05.

Write only the Meta path in Section 1.

---

## 17. Quality bar

`PASS` only if HEAD descends from `1d9a71e` via this Worker; schema head
remains **0031**; opened and apply POSTs exist; `companion_mutation` is
exactly those two plus the two X POSTs; apply has primary
`media.content.publish` and additional `metadata.canonical.write`; G2 inserts
`companion_review` only when ready and unpublished; website Publish still
`admin_explicit`; ordinary Gallery stays published-only; website Save drops
stale receipts; focused tests via `ap exec` exit 0; no ambient Python; no
push; no overlay.

`PARTIAL` for one named evidence gap. `BLOCKED` for baseline/Plan Mode/
allowlist contradiction.

---

## 18. Stop

After the terminal report, stop. Do not start W05, overlay, living docs, or
deploy.
