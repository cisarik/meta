# Authoritative Prompt for Fresh Worker 03

## FrameNest Companion AI Review Inbox — Alembic 0031 and read model

You are a Worker instance assigned to WORKER. Read this complete prompt before
taking any action.

Worker 02 landed ADRs 0066–0071, generic suggestion v4, and administrator-owned
X automatic-analysis policy at `0c1d96ca8b724d440af0c4e3fd3d756fb5bd85cd`. That
authority is expired. The COOPERATOR accepted the architecture on 2026-08-23.
The ORCHESTRATOR independently verified W02 (allowlist, ancestry, focused
suite 386 passed).

Your task is **slice W03 only**: Alembic **0031**, historical stored-suggestion
codec, administrator inbox/history **GET** routes, and current-head schema
assertions. Do not implement mark-opened, apply, G2 publish, overlay, badge,
alarms, or NUC.

Do not enter Native Plan Mode. Do not use Max. Extra High is requested; if the
client does not expose a measurable Extra High SKU, continue only while Plan
Mode stays off and Max is unused, and record that in the handshake. If Native
Plan Mode is on, stop `BLOCKED`.

```text
Role: WORKER
Persistent role identity: WORKER
Logical whole identity: framenest-companion-ai-review-inbox-mvp
Prior logical whole identities:
  framenest-x-meme-browser-companion-mvp
  framenest-x-companion-save-alias-mvp
  framenest-x-companion-save-category-mvp
  framenest-x-save-overlay-edit-subset-mvp
Worker session ordinal: 03
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Worker session profile: Fresh Implementation Worker
Phase: implementation
Task identity: FN-COMPANION-AI-REVIEW-INBOX-IMPL-03
Task type: bounded implementation candidate
Native planning mode: not-used
Reasoning recommendation: extra-high
Evidence posture: non-independent
Independence required: no
Authority renewal: not applicable — fresh Worker authority originates only in this prompt
Prior implementation report: Worker 02 / exchange 01 implementation-PASS at 0c1d96c; authority expired
Continuity anchor: none — do not resume Worker 02
Internal delegation posture: not-used
Accountable Worker: one WORKER
Material phase gate: yes
Changed material axis: mutation-authority-or-side-effect-class
Routing reopened for: mutation-authority-or-side-effect-class
Unchanged axes reopened: none
Ordinary-only trigger: no
Automatic model selection: off
Enhanced/maximum mode: not requested
```

```text
Implementation authority: explicit
Native planning mode: not-used
Worker session target: fresh-worker-session
Exact baseline: 0c1d96ca8b724d440af0c4e3fd3d756fb5bd85cd
Changed-path allowlist: Section 11
Implementation boundaries: Sections 8, 9, 10, 12, and 14
Independence required: no
```

```text
Evidence tier: E3
Evidence tier basis: new durable schema, new administrator HTTP, historical codec; no companion mutations yet
Authorized implementation stages: causal slices 1–3 in Section 10
Combined implementation envelope: allowed for those three slices only
Implementation stage gates: Section 10; a failed gate stops the sequence
Independent acceptance: not-required (this session)
Rollback or recovery checkpoint: local Git commits on feat/x-meme-browser-companion; no push; no NUC
Activated stricter profile: INFOSEC.md R1 inline only
Terminal implementation report point: after slices 1–3 are committed and validated, before opened/apply/G2/overlay
```

```text
COOPERATOR: Michal
ORCHESTRATOR: ORCHESTRATOR_CHAT
WORKER: this fresh implementation session
```

Internal delegation, sub-agents, parallel Workers, Explore tasks, and hidden
secondary workstreams are not authorized.

Repository documentation, code comments, test names, commit subjects, and the
terminal Worker report must use professional English. Czech is forbidden.
The terminal report must begin exactly:

```text
### Report for ORCHESTRATOR_CHAT
```

Implementation PASS is not acceptance, publication, deployment, or closure.
`Logical-whole closure: not-closed`.

Protocol-variant selection:

```text
Canonical repository identity: https://github.com/cisarik/ap.git
Immutable version identity: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Declared variant: stable
Governing variants in effect: one
Declaration location: project governing rules
Rules from non-governing variants: none
Migration required: no
```

---

## 1. External trace and Meta write boundary

```text
External trace disposition: configured
Trace discovery: cisarik/meta repository path projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp
Trace project key: framenest
Trace logical-whole projection identity: 03/09-framenest-companion-ai-review-inbox-mvp
Trace authority: historical-evidence-only
Trace archival owner: ORCHESTRATOR after the outcome exists
Trace visibility: public
Trace companion outcome: report
Trace self-granted status: none
Downloadable prompt filename: 03_implementation_00.md
Destination path: projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/03_implementation_00.md
Archival: wait-for-report
```

You may **read** (historical; this prompt wins on W03 scope):

```text
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/01_report_00.md
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/02_implementation_00.md
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/02_report_00.md
```

Frozen hashes at issuance (re-check; stop `BLOCKED` if drifted):

```text
01_report_00.md SHA-256 51e124c02009a6822ebb36afc8893187074c680cd139462d79e72cb61bab75ce
02_implementation_00.md SHA-256 1341fb72ebfee8ac6c3d6b3b58ff4c34101be41b64f4205bf83e7085b012e213
02_report_00.md SHA-256 88ac9792b6283e8e70bd1435c9506e44df3fd9cf1f5c1c3e9afce1b5ff9290b4
```

You may **write** only:

```text
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/03_report_00.md
```

Do not alter any other Meta path. Do not stage or commit Meta. If the report
file cannot be written, return it verbatim in chat.

---

## 2. Communication and human-governance routing

```text
Worker prompt language: professional English
Worker report language: professional English
Direct-user Slovak presentation: Orchestrator-owned
Report header: ### Report for ORCHESTRATOR_CHAT
Cooperator visibility: W03 grant already issued inside the accepted envelope
Human decision points remaining outside this grant: opened/apply/G2 (W04), overlay/badge (W05–W06), living docs (W07), R3, NUC
Deterministic steps inside bounded authority: slices 1–3, tests, local commits
Internal delegation posture: not-used
```

---

## 3. Capability handshake

Fresh session. Compact handshake before mutation. Record requested / directly
observed / inferred / unknown. Capability does not grant authority. If Plan
Mode is on, stop `BLOCKED`.

```text
Validation ladder: selected
Inspection and provenance: required
Existing focused tests: Section 13
Broad or full suite: not-used (schema-head owners in Section 13 only)
Independent acceptance: not-required
```

Browser, provider, NUC, publication, and deployment annexes are **not
activated**.

---

## 4. Repository gate and exact baseline

```text
Expected canonical root: /home/agile/Projects/framenest
Repository checkout topology: standalone checkout
Working-copy topology: canonical checkout
Applicable branch: feat/x-meme-browser-companion
Expected HEAD: 0c1d96ca8b724d440af0c4e3fd3d756fb5bd85cd
Expected parent: 9ce158116b0cd59f9e8b2df1c7d4d56b8b208219
Expected tree: 5357c3aa52bf7d89419fecf7fe0549b13722ad4c
Expected subject: feat: cap generic NIM tags at five and allow admin X auto-analysis
Working tree: expected clean
Upstream: none configured (expected)
Schema head at baseline: Alembic 0030
```

```text
.ap gitlink and checkout: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Detached submodule checkout: acceptable
```

Issuance-time public refs (re-verify with `git ls-remote`, no fetch):

```text
cisarik/framenest refs/heads/main  045f33b44897a6f3949cc515792336396f1d33a1
cisarik/ap        refs/heads/main  9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
```

If local HEAD is not `0c1d96c`, stop `BLOCKED` unless the only difference is
this Worker’s own later commits. Do not stash, reset, or clean.

Canonical Python route only:

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <EXACT SHA UNDER TEST>
./.ap/ap exec --root /home/agile/Projects/framenest --baseline <EXACT SHA UNDER TEST> --operation test-focus -- <tests> -q -p no:cacheprovider
```

After the first commit, `--baseline` is that candidate SHA. No ambient
`.venv/bin/python` / `python` / `python3` / `poetry run`.

Spot-check before mutation:

- Live `PROMPT_VERSION` is `framenest-media-suggestion-v4`; `TAG_MAX_COUNT = 5`.
- `companion_mutation` is still exactly two X POSTs.
- `ContentPublicationOrigin` is `legacy_backfill` | `admin_explicit` only.
- No `companion_review_*` tables.
- `deserialize_suggestion_result` in `media_analysis_lifecycle.py` is a thin
  JSON dict parse used by website analysis status — do not silently break it.
  Historical inbox mapping is a **dedicated** codec, not a reuse of
  `MediaSuggestion` (v4 would reject historical 6–12 tag rows).
- Ingest Save overlay and X auto-analysis helper remain frozen.

---

## 5. Required reading

- `AGENTS.md`, `.ap/AP.md`, `.ap/AP_WORKER.md`, `.ap/PROMPT_CONTRACTS.md`
- `.ap/INFOSEC.md` §3–§5 (R1 only)
- `docs/WORKER_EXECUTION_CONTRACT.md`
- `docs/adr/0067-administrator-companion-review-inbox-and-mutation-trust.md`
- `docs/adr/0068-companion-review-save-and-readiness-triggered-publication.md`
- `docs/adr/0069-five-tag-generic-media-suggestion-contract.md`
- `docs/adr/0070-companion-exclusion-of-movie-workflows.md`
- `01_report_00.md` Sections 7 (schema, inbox query, tag mapping), 8 (GET
  contracts), 9 (historical codec), 10 (movie exclusion)
- `src/framenest/infrastructure/persistence/alembic_environment/versions/0021_content_publication.py`
- `src/framenest/infrastructure/persistence/alembic_environment/versions/0029_media_user_alias_overlay.py`
  (login-key CHECK style)
- `src/framenest/infrastructure/persistence/alembic_environment/versions/0030_x_claim_requested_content_category.py`
- `src/framenest/infrastructure/persistence/catalog_schema.py` (`media_content_publications`,
  `media_analysis_runs`, `canonical_tags`, `media_metadata`)
- `src/framenest/domain/content_publication.py`
- `src/framenest/domain/media_analysis_runs.py`
  (`AUTOMATIC_POST_CATALOG_ANALYSIS_DEFINITION`, `RESULT_SCHEMA_VERSION`)
- `src/framenest/domain/media_classification.py` (`AnalysisProfile.GENERIC_MEDIA`,
  `ContentCategory.MOVIE`)
- `src/framenest/adapters/api/tailscale_ingress.py` (`RoutePolicy`, GET companion
  picker as the Origin-policy model for non-mutation GETs)
- `src/framenest/adapters/api/x_companion_api.py` (no-store, identity, 422/503)
- `src/framenest/adapters/api/application.py` (router include pattern)
- `tests/contract/test_x_route_policy.py`
- `tests/integration/persistence/test_x_requested_category_migration.py`
- `tests/integration/test_persistence_migrations.py`

---

## 6. Goal

One coherent W03 candidate:

1. Alembic **0031** from **0030**: publication origin `companion_review`;
   tables `companion_review_open_states` and `companion_review_field_sources`;
   inbox/history indexes. Empty downgrade succeeds; populated review or
   `companion_review` publication refuses.
2. Dedicated historical suggestion codec (v1 result JSON, live v4 **or**
   historical v3 with up to 12 stored tags) plus deterministic existing-tag
   mapping used by GET detail.
3. Administrator GET inbox list and GET per-media history/detail. Ordinary
   callers 403 via `media.workflow.read`. No POST. `companion_mutation`
   remains the two existing X POSTs.

---

## 7. Accepted decisions (do not re-litigate)

From accepted `01_report_00.md` and Cooperator 2026-08-23:

- Inbox: one row per media from the **latest successful** generic run.
- Filter: `state = analyzed`, `analysis_definition = automatic_post_catalog`,
  result schema v1, `analysis_profile = generic_media` **or historical NULL**,
  non-null `completed_at_ms`, `COALESCE(content_category, 'general') != 'movie'`.
- Also exclude `analysis_definition = movie_identification` regardless of
  category.
- A later **failed** run does not hide an earlier successful generic run.
- Order `(completed_at_ms DESC, analysis_run_id DESC)`.
- Title: current non-blank canonical display title, else stored suggestion
  title.
- Default limit 25, max 100; fetch `limit + 1`.
- Cursor: opaque base64url canonical JSON `{completed_at_ms, id}`; invalid →
  422.
- `unopened_count` is the full filtered set for **this actor**, not the page.
- Unopened: missing actor/media opened row **or** `opened_run_id` ≠ latest
  successful run. (Opened writes are W04; W03 must still **read** the table so
  count is correct when empty.)
- History GET: same successful generic filter for one media, same keyset.
- Tag mapping (GET detail): trim, Unicode casefold; unique display-name match
  else unique key match; duplicate/ambiguous/unknown; historical mapped keys
  beyond five → `legacy_limit` and not selectable. Never create tags.
- GET responses `Cache-Control: no-store`.
- Movie category recheck is mandatory on detail; movie → 409 without leaking
  suggestion bodies.
- Website Analyze-by-AI successes appear because they share
  `automatic_post_catalog` / `generic_media`.
- No notifications, no overlay, no apply, no G2 insert in this Worker.

---

## 8. Orchestrator binding reconciliations

1. **Schema is W03 even though apply is W04.** Create both new tables and the
   origin enum/CHECK now so W04 does not migrate twice. W03 must not INSERT
   opened rows, field-source rows, or publications.
2. **`additional_capabilities` is W04.** Do not change `RoutePolicy`’s
   capability model except adding the two GET policies with primary
   `media.workflow.read` and `companion_mutation=False`.
3. **GETs are not companion mutations.** They follow picker GET: identity +
   capability, no Origin companion flag. Empty `companion_extension_origins`
   must **not** block these GETs (NUC today has empty origins; admin side
   panel still needs to list). Mutations stay fail-closed on empty origins
   (W04).
4. **Do not reuse `GET /api/media/{id}/automatic-analysis`.** That route is
   `gallery.read` and content-audience gated. Inbox is a new admin workflow
   surface.
5. **Historical codec isolation.** Do not feed historical JSON through
   `MediaSuggestion` / v4 `TAG_MAX_COUNT`. A dedicated decoder accepts stored
   v1 objects with 1–12 unique tags for history display. Live provider
   validation stays v4.
6. **Corrupt result_json:** sanitized 500 or a bounded 422-class error for
   that item — never raw JSON, never a stack, never a successful row with
   empty silent mapping that looks like “no tags from NIM”.
7. **Login-key CHECKs** match existing alias overlay: length 1–254, lowercase,
   no whitespace/control. `actor_login_key` / `applied_by_login_key` use the
   same.
8. **SHA-256 digest column** on field sources is created now
   (`value_digest` lowercase 64 hex). W04 writes it. W03 GET returns receipts
   only when digest still matches canonical state; empty table → all
   `field_sources` null.
9. **Foreign keys:** cascade from `logical_media` and `media_analysis_runs` as
   in the accepted plan. Use the project’s SQLite batch FK helpers where
   CHECK rebuilds require them (publication origin CHECK).
10. **Do not edit ADR bodies.** 0067 already says routes land in a later
    slice; code now implements the GET half. Living PRODUCT/SPEC is W07.
11. **application.py wiring** may grow. Keep identity mapping-before-coordinator
    from W02. Include the new router only when catalog/persistence is
    available, same spirit as other admin routers.
12. **Indexes** named in the plan: opened_run_id; field-source analysis_run_id;
    successful-inbox / per-media-history over definition/state/profile/
    completion/id/media. Name them stably. Prove EXPLAIN is not required;
    prove queries stay correct with fixtures large enough for pagination.

---

## 9. HTTP contracts (GET only)

| Method/path | Capability | `companion_mutation` | Body |
|---|---|---|---|
| `GET /api/companion/review-inbox?limit=&cursor=` | `media.workflow.read` | false | `{items, unopened_count, next_cursor}` |
| `GET /api/companion/review-inbox/{media_id}?limit=&cursor=` | `media.workflow.read` | false | canonical values, publication state, valid field-source receipts, suggestion history page, cursor |

List item: `media_id`, `title`, latest `analysis_run_id`, `completed_at_ms`,
`unopened`.

Detail suggestion entries: `analysis_run_id`, `completed_at_ms`,
`provider_id`, `model_id`, `prompt_version`, title, description, tags with
status `mapped` | `unknown` | `ambiguous` | `duplicate` | `legacy_limit`.
Never collection, filename, source, category, or genres.

Errors: missing media 404; malformed cursor/limit 422; movie 409 (detail);
catalog unavailable 503; repository failure sanitized 500. Ordinary identity:
ingress 403, no titles.

Handler must also require `media.workflow.read` (defense in depth), like
companion picker re-checks `x.request`.

---

## 10. Causal slices

### Slice 1 — 0031 + domain/schema

Migration, `catalog_schema.py`, `ContentPublicationOrigin.COMPANION_REVIEW`,
downgrade refuse rules, foreign-key verification, current-head test updates
listed in Section 11.

Gate: fresh DB to 0031; 0030→0031 populated catalog; empty downgrade;
populated open-state or `companion_review` publication refuses downgrade.

### Slice 2 — codec + repository + application

Historical decoder, tag mapping, inbox/history queries, unopened calculation
against empty opened table.

Gate: unit tests in Section 13 (one-row/media, movie exclusion, failed-later,
Analyze-by-AI inclusion, pagination, actor isolation with **empty** opened
rows ⇒ all unopened).

### Slice 3 — HTTP GET

Router, RoutePolicy, application include, contract tests. Still exactly two
`companion_mutation` flags.

Gate: admin 200, ordinary 403, no-store, 422 cursor, movie 409.

At most **three** local commits. Example: `feat:` migration/schema, `feat:`
read model, `test:` current-head — or fewer. No push.

---

## 11. Changed-path allowlist

**Schema / domain**

- `src/framenest/infrastructure/persistence/alembic_environment/versions/0031_companion_review_inbox.py` (create)
- `src/framenest/infrastructure/persistence/catalog_schema.py`
- `src/framenest/domain/content_publication.py`

**Application / persistence / API**

- `src/framenest/application/companion_review.py` (create)
- `src/framenest/application/ports/companion_review_repository.py` (create)
- `src/framenest/infrastructure/persistence/companion_review_repository.py` (create)
- `src/framenest/adapters/api/companion_review_api.py` (create)
- `src/framenest/adapters/api/application.py`
- `src/framenest/adapters/api/tailscale_ingress.py` (add two GET policies only)
- `src/framenest/infrastructure/persistence/media_metadata_repository.py`
  **only if** inbox title/canonical reads cannot be done inside
  `companion_review_repository` without it. Prefer not to touch it.

**New tests**

- `tests/unit/application/test_companion_review.py`
- `tests/unit/infrastructure/persistence/test_companion_review_repository.py`
- `tests/integration/persistence/test_companion_review_migration.py`
- `tests/contract/test_companion_review_api.py`

**Current-head / route freeze (update 0030 → 0031 or add GET policies)**

- `tests/contract/test_x_route_policy.py`
- `tests/contract/test_persistence_cli.py`
- `tests/integration/test_persistence_migrations.py`
- `tests/integration/test_process_sigterm_lifecycle.py`
- `tests/integration/persistence/test_device_registry_migration.py`
- `tests/integration/persistence/test_library_registry_migration.py`
- `tests/integration/persistence/test_media_catalog_migration.py`
- `tests/integration/persistence/test_media_cover_migration.py`
- `tests/integration/persistence/test_media_metadata_migration.py`
- `tests/integration/persistence/test_media_user_alias_overlay_migration.py`
- `tests/integration/persistence/test_upload_publication_migration.py`
- `tests/integration/persistence/test_upload_session_migration.py`
- `tests/integration/persistence/test_x_requested_category_migration.py`
- `tests/integration/persistence/test_x_requester_acquisition_migration.py`
- `tests/integration/persistence/test_content_publication_migration.py`
- `tests/integration/persistence/test_populated_0015_upgrade_to_0017.py`
- `tests/unit/infrastructure/backup/test_catalog_backup.py`
- `tests/unit/infrastructure/runtime/test_production_runtime.py`

Preserve tests that intentionally migrate **to** historical 0030 (upgrade path
fixtures). Only change assertions that mean “current head is 0030”.

**Meta**

- `/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/03_report_00.md`

Forbidden: ingest Save, X auto-analysis helper (unless a tiny import for
tests), movie identification, apply/opened POST, overlay, PRODUCT/SPEC,
accepted ADR bodies, `nvidia_nim.py`.

If a current-head assertion lives in a file not listed and the focused suite
fails, stop `PARTIAL` and name the path.

---

## 12. Git, network, secret, side-effect

```text
Git authority: local commits only, at most three, on feat/x-meme-browser-companion; no amend of 0c1d96c or earlier; no rebase, reset, stash, clean, fetch-that-rewrites, or push; do not update git config; do not skip hooks
Network authority: git ls-remote to the two public refs; no provider; no NUC
Secret authority: none
Filesystem authority: read FrameNest and pinned .ap; write only Section 11 plus the Meta report
Side-effect authority: reversible local source/tests/docs/commits
Dependency authority: none
Browser authority: none
```

Commit subject examples:

- `feat: add companion review inbox schema 0031`
- `feat: add administrator companion review inbox reads`

HEREDOC messages. Do not commit Meta or secrets. Amend only your own
not-yet-pushed commit, and only if a hook auto-modified files after success.

---

## 13. Tests

Each candidate SHA:

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <SHA>
```

Minimum `test-focus` set (extend with any current-head file you actually edit):

```text
./.ap/ap exec --root /home/agile/Projects/framenest --baseline <SHA> --operation test-focus -- \
  tests/unit/application/test_companion_review.py \
  tests/unit/infrastructure/persistence/test_companion_review_repository.py \
  tests/integration/persistence/test_companion_review_migration.py \
  tests/contract/test_companion_review_api.py \
  tests/contract/test_x_route_policy.py \
  tests/integration/test_persistence_migrations.py \
  tests/integration/persistence/test_x_requested_category_migration.py \
  tests/integration/persistence/test_content_publication_migration.py \
  tests/unit/infrastructure/backup/test_catalog_backup.py \
  tests/unit/infrastructure/runtime/test_production_runtime.py \
  tests/contract/test_persistence_cli.py \
  tests/unit/application/test_x_automatic_analysis_policy.py \
  -q -p no:cacheprovider
```

If you touch other current-head files in Section 11, add them to this
invocation. After the last commit, run **all edited** current-head files in
one `test-focus`. Do not run a full `test` operation. Do not run JS companion
tests. Do not call NVIDIA.

Required cases:

- 0030 → 0031 upgrade; empty 0031 downgrade; refuse downgrade with opened row
  or `companion_review` publication.
- Latest successful generic per media; movie excluded; movie-identification
  excluded; later failed run does not replace success; website-style generic
  success included.
- Keyset pagination; `unopened_count` totals; all items unopened when opened
  table empty; two actors do not share opened truth (table exists even if
  empty).
- Historical v3 JSON with 6–12 tags decodes; overflow `legacy_limit`; v4
  `MediaSuggestion` still rejects 6 tags.
- Mapping: unique display name, unique key, ambiguous display (no key
  fallback), unknown, duplicate.
- Admin GET 200; ordinary 403; bad cursor 422; movie detail 409; no-store.
- Exactly two `companion_mutation` routes remain the X POSTs.
- W02 policy tests still pass (no regression of admin X helper).

---

## 14. Negative authority

- Native Plan Mode, Max, sub-agents
- Push, NUC, sudo, provider, signed-in browser
- POST opened / apply / G2
- `additional_capabilities` RoutePolicy expansion (W04)
- Overlay, badge, `alarms`, `notifications`
- Editing 0066–0071 bodies, PRODUCT, SPEC, X_COMPANION
- CORS, `all_urls`, alias PUT apply
- Closing the logical whole
- Enabling `FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED`

---

## 15. INFOSEC R1 (inline)

- Inbox titles and suggestion fields are untrusted (X DOM + NIM). Plain-text
  JSON only. No `innerHTML` (no UI this slice).
- Ordinary callers must not learn titles via 200/empty-list confusion —
  **403**.
- Logs/audit: no suggestion/canonical title, description, tags, result JSON,
  private URLs, or credentials. GET list has **no** audit action (plan:
  avoid poll floods).
- Digest column exists; W03 does not trust client field text (no apply yet).
- Residual: unread opened table means every inbox item is unopened until W04.
  That is expected.

---

## 16. Terminal report

Begin exactly:

```markdown
### Report for ORCHESTRATOR_CHAT
```

```text
Logical whole identity: framenest-companion-ai-review-inbox-mvp
Worker session ordinal: 03
Worker exchange ordinal: 01
```

Include: status; handshake; baseline ledger; changed paths; proof of Section 7
invariants; commands/exit codes; git SHAs (no push); INFOSEC residual;
near-misses / pre-existing classification; smallest next step = Orchestrator
issues W04 (opened + apply + G2) after verification. Do not self-issue W04.

Write only the Meta path in Section 1.

---

## 17. Quality bar

`PASS` only if HEAD descends from `0c1d96c` via this Worker; schema head is
**0031**; GET list/detail exist; no POST review routes; two companion
mutations only; ordinary 403; movie excluded; historical v3 readable without
v4 validator; focused tests via `ap exec` exit 0; no ambient Python; no push.

`PARTIAL` for one named evidence gap. `BLOCKED` for baseline/Plan Mode/
allowlist contradiction.

---

## 18. Stop

After the terminal report, stop. Do not start W04, overlay, or deploy.
