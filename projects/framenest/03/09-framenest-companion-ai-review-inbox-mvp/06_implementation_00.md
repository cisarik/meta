# Authoritative Prompt for Fresh Worker 06

## FrameNest Companion AI Review Inbox — review overlay

You are a Worker instance assigned to WORKER. Read this complete prompt before
taking any action.

Worker 05 landed MV3 `alarms`, toolbar badge, and native S1 inbox chrome at
`feb9a69c5b3d47633f133a83f8dc4d75d3313299`. That authority is expired. The
COOPERATOR accepted a second overlay (review), separate from frozen ingest
Save. The ORCHESTRATOR independently verified W05 (`ap project check` PASS;
Node suite **70 passed**).

Your task is **slice W06 only**: local review overlay (`review.html|js|css`),
history dropdown, per-field checkmarks, mapped tag chips, opened POST when a
run is displayed, apply POST, stay-open result handling, and inbox refresh
after open/apply. Do **not** implement living docs (W07), NUC, or notifications.

Do not enter Native Plan Mode. Do not use Max. Extra High is requested; if the
client does not expose a measurable Extra High SKU, continue only while Plan
Mode stays off and Max is unused, and record that in the handshake. If Native
Plan Mode is on, stop `BLOCKED`.

```text
Role: WORKER
Persistent role identity: WORKER
Logical whole identity: framenest-companion-ai-review-inbox-mvp
Prior logical whole identities:
  framenest-x-meme-browser-companion-mvp
  framenest-x-companion-save-alias-mvp
  framenest-x-companion-save-category-mvp
  framenest-x-save-overlay-edit-subset-mvp
Worker session ordinal: 06
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Worker session profile: Fresh Implementation Worker
Phase: implementation
Task identity: FN-COMPANION-AI-REVIEW-INBOX-IMPL-06
Task type: bounded implementation candidate
Native planning mode: not-used
Reasoning recommendation: extra-high
Evidence posture: non-independent
Independence required: no
Authority renewal: not applicable — fresh Worker authority originates only in this prompt
Prior implementation report: Worker 05 / exchange 01 implementation-PASS at feb9a69; authority expired
Continuity anchor: none — do not resume Worker 05
Internal delegation posture: not-used
Accountable Worker: one WORKER
Material phase gate: yes
Changed material axis: mutation-authority-or-side-effect-class
Routing reopened for: mutation-authority-or-side-effect-class
Unchanged axes reopened: none
Ordinary-only trigger: no
Automatic model selection: off
Enhanced/maximum mode: not requested
```

```text
Implementation authority: explicit
Native planning mode: not-used
Worker session target: fresh-worker-session
Exact baseline: feb9a69c5b3d47633f133a83f8dc4d75d3313299
Changed-path allowlist: Section 11
Implementation boundaries: Sections 8, 9, 10, 12, and 14
Independence required: no
```

```text
Evidence tier: E3
Evidence tier basis: new review overlay and companion mutation clients; no living-docs/NUC; no Python server mutation
Authorized implementation stages: causal slices 1–3 in Section 10
Combined implementation envelope: allowed for those three slices only
Implementation stage gates: Section 10; a failed gate stops the sequence
Independent acceptance: not-required (this session)
Rollback or recovery checkpoint: local Git commits on feat/x-meme-browser-companion; no push; no NUC
Activated stricter profile: INFOSEC.md R1 inline only
Terminal implementation report point: after slices 1–3 are committed and validated, before living docs
```

```text
COOPERATOR: Michal
ORCHESTRATOR: ORCHESTRATOR_CHAT
WORKER: this fresh implementation session
```

Internal delegation, sub-agents, parallel Workers, Explore tasks, and hidden
secondary workstreams are not authorized.

Repository documentation, code comments, test names, commit subjects, and the
terminal Worker report must use professional English. Czech is forbidden.
The terminal report must begin exactly:

```text
### Report for ORCHESTRATOR_CHAT
```

Implementation PASS is not acceptance, publication, deployment, or closure.
`Logical-whole closure: not-closed`.

Protocol-variant selection:

```text
Canonical repository identity: https://github.com/cisarik/ap.git
Immutable version identity: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Declared variant: stable
Governing variants in effect: one
Declaration location: project governing rules
Rules from non-governing variants: none
Migration required: no
```

---

## 1. External trace and Meta write boundary

```text
External trace disposition: configured
Trace discovery: cisarik/meta repository path projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp
Trace project key: framenest
Trace logical-whole projection identity: 03/09-framenest-companion-ai-review-inbox-mvp
Trace authority: historical-evidence-only
Trace archival owner: ORCHESTRATOR after the outcome exists
Trace visibility: public
Trace companion outcome: report
Trace self-granted status: none
Downloadable prompt filename: 06_implementation_00.md
Destination path: projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/06_implementation_00.md
Archival: wait-for-report
```

You may **read** (historical; this prompt wins on W06 scope):

```text
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/01_report_00.md
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/05_implementation_00.md
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/05_report_00.md
```

Frozen hashes at issuance (re-check; stop `BLOCKED` if drifted):

```text
01_report_00.md SHA-256 51e124c02009a6822ebb36afc8893187074c680cd139462d79e72cb61bab75ce
05_implementation_00.md SHA-256 1a4511e9ba1ad182ef17828168c7007b29ba96056ce5eadd0cc8b586a2d5cfe6
05_report_00.md SHA-256 6887ac070629e119c069ceb033ac4ae21dce39b257b31fc7ebc12e330115c7b1
```

You may **write** only:

```text
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/06_report_00.md
```

Do not alter any other Meta path. Do not stage or commit Meta. If the report
file cannot be written, return it verbatim in chat.

---

## 2. Communication and human-governance routing

```text
Worker prompt language: professional English
Worker report language: professional English
Direct-user Slovak presentation: Orchestrator-owned
Report header: ### Report for ORCHESTRATOR_CHAT
Cooperator visibility: W06 grant already issued inside the accepted envelope
Human decision points remaining outside this grant: living docs (W07), R3, NUC
Deterministic steps inside bounded authority: slices 1–3, tests, local commits
Internal delegation posture: not-used
```

---

## 3. Capability handshake

Fresh session. Compact handshake before mutation. Record requested / directly
observed / inferred / unknown. Capability does not grant authority. If Plan
Mode is on, stop `BLOCKED`.

```text
Validation ladder: selected
Inspection and provenance: required
Existing focused tests: Section 13
Broad or full suite: not-used
Independent acceptance: not-required
```

Browser, provider, NUC, publication, and deployment annexes are **not
activated**. Do not run `tests/browser_companion_evidence.test.js`.

---

## 4. Repository gate and exact baseline

```text
Expected canonical root: /home/agile/Projects/framenest
Repository checkout topology: standalone checkout
Working-copy topology: canonical checkout
Applicable branch: feat/x-meme-browser-companion
Expected HEAD: feb9a69c5b3d47633f133a83f8dc4d75d3313299
Expected parent: 8861be593917680179ebc1cf70cbe423cf0ebe9e
Expected tree: 69983bea4aaf5f08296922c713345aa70a629f73
Expected subject: test: cover companion review inbox alarms, badge, and S1 chrome
Working tree: expected clean
Upstream: none configured (expected)
Schema head at baseline: Alembic 0031
```

```text
.ap gitlink and checkout: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Detached submodule checkout: acceptable
```

Issuance-time public refs (re-verify with `git ls-remote`, no fetch):

```text
cisarik/framenest refs/heads/main  045f33b44897a6f3949cc515792336396f1d33a1
cisarik/ap        refs/heads/main  9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
```

If local HEAD is not `feb9a69`, stop `BLOCKED` unless the only difference is
this Worker’s own later commits. Do not stash, reset, or clean.

This slice is **extension/JS**. Do not change Python. Provenance gate after
each commit:

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <EXACT SHA UNDER TEST>
```

JavaScript tests use Node’s built-in runner (Section 13). No `npm install`.
No ambient Python. No full `test` operation.

Spot-check before mutation:

- S1 `#review-inbox` sits between `#shell-status` and `#frame`. List items
  currently render **title only** with no click/open path.
- `pathFor("reviewInbox")` is GET `/api/companion/review-inbox` only. Detail
  and POST paths do not exist yet.
- `TYPES.REVIEW_INBOX` exists. Overlay mutation types must be added and
  `dropUnknown` must keep rejecting unknown types.
- Manifest WAR is picker + save only. Review files must **not** be added to
  WAR. Sidebar files are not WAR.
- Ingest Save (`extension/ui/save.*`, `x_adapter.js`) is frozen. Do not reuse
  `save.js`.
- Server apply/opened contracts already exist (W04). This Worker is a client.
  Empty `companion_extension_origins` still fail-closes extension POSTs on a
  real NUC; do not “fix” that in Python. Tests mock `fetch`.
- Live `PROMPT_VERSION` / schema **0031** stay untouched.

---

## 5. Required reading

- `AGENTS.md`, `.ap/AP.md`, `.ap/AP_WORKER.md`, `.ap/PROMPT_CONTRACTS.md`
- `.ap/INFOSEC.md` §3–§5 (R1 only)
- `docs/WORKER_EXECUTION_CONTRACT.md` (JavaScript Tests)
- `docs/adr/0071-native-side-panel-review-inbox-chrome.md`
- `01_report_00.md` Sections 7 (review overlay), 8 (HTTP bodies), 14 (W05/W06
  gate — overlay bullets are this Worker; ingest Save bullets are regressions)
- `extension/ui/sidebar.html|js|css` (S1 list, dialog patterns, iframe survival)
- `extension/shared/messages.js` (`pathFor`, `TYPES`, `dropUnknown`,
  `sanitizeReviewInboxItems`)
- `extension/background/service_worker.js` (`fetchJson`, `reviewInbox`,
  no `setInterval`)
- `src/framenest/adapters/api/companion_review_api.py` (`_detail_dict`,
  `_opened_dict`, `_apply_dict`, opened/apply request models) — **read only**
- `tests/companion_review_extension.test.js`
- `tests/x_companion_extension.test.js` (ingest Save invariants, WAR, tokens)
- `extension/ui/save.js` **only** as a negative example: do not import it,
  copy its form, or share its `#url=` ingest contract

---

## 6. Goal

One coherent W06 candidate:

1. Sibling `extension/ui/review.html|js|css`, loaded from the side panel as a
   **local** dialog/iframe via `chrome.runtime.getURL`. Not a web-accessible
   resource. Not mounted inside the hosted FrameNest iframe.
2. Clicking an inbox row opens the overlay for that `media_id`. GET detail
   through the service worker. POST opened for the **displayed** run.
3. History dropdown, field checkmarks (title / tags / description), mapped
   chips with drop reasons, disabled Save until ≥1 field is selected.
4. Apply POST copies title/description **server-side**; client sends only
   `{analysis_run_id, fields, tag_keys}`. Stay open on success; reload
   canonical/provenance/publication; clear selections. Error preserves
   selections.
5. After open/apply, refresh the S1 list/badge path (existing `REVIEW_INBOX`).
   `#frame` and ingest Save remain intact.

---

## 7. Accepted decisions (do not re-litigate)

From accepted `01_report_00.md` and Cooperator 2026-08-23:

- Review is a **second** overlay. Ingest Save stays Title → Tags →
  Description → enabled Save, no radios/Analyze, `#url=` only, POST
  `{url, alias}`.
- Overlay files are not WAR. Network is service-worker-only.
- Parent/child messages require **exact extension origin** and `event.source`.
  No `postMessage(..., "*")`.
- `textContent` / `createElement` only. No `innerHTML`. Titles and suggestion
  bodies are untrusted.
- Dropdown copy is local-formatted `completed_at_ms · exact model_id · title`,
  labelled as **run completion time**, not a provider timestamp.
- Switching runs resets checkmarks and removed chips and **never writes**
  apply. It **does** POST opened for the newly displayed run (server
  monotonicity already refuses older-over-newer).
- Save starts disabled; one or more selected fields enables it.
- Tags ✅ is replace, not union. Only `status === "mapped"` chips are
  selectable. Unknown / ambiguous / duplicate / `legacy_limit` stay visible
  with reason and cannot be applied. Chip removal is allowed. If no mapped
  chips remain, Tags cannot be checked (prevents zero-tag apply).
- Client never submits title/description strings. `tag_keys` is the remaining
  mapped keys in **suggestion order** (ordered subsequence). Empty `tag_keys`
  unless Tags is selected.
- Success keeps the overlay **open**, reloads canonical state, field-source
  receipts, and publication (`published|already_published|not_ready`), then
  clears selections. HTTP 200 including `not_ready` is success.
- Error preserves selections. Movie/wrong-run/stale-mapping 409 is a
  sanitized error without dumping suggestion JSON into logs.
- No collection, filename, source, category, or genres controls.
- Do not store suggestion bodies in `chrome.storage`.
- Ordinary 403 on detail/opened/apply: close overlay, hide inbox (existing
  403 path), no title leakage.

---

## 8. Orchestrator binding reconciliations

1. **Do not reuse `save.js` / `save.html`.** Different host (X page vs side
   panel), different POST, different fields. Copy visual **tokens**
   (`--background`, `--accent`, …) from sidebar/picker, not ingest behavior.
2. **Do not fetch from `review.js` or `sidebar.js`.** Add protocol types and
   `pathFor` names; `fetchJson` already sets `X-FrameNest-Request: 1` and
   supports POST JSON. Chrome will attach the extension Origin on SW fetches.
3. **`pathFor` must reject caller URLs.** Suggested names:
   - `reviewInboxDetail` → `/api/companion/review-inbox/{mediaId}`
   - `reviewInboxOpened` → `/api/companion/review-inbox/{mediaId}/opened`
   - `reviewInboxApply` → `/api/companion/review-inbox/{mediaId}/apply`
   `mediaId` must pass `isUuid`. No string concat of untrusted path segments.
4. **List rows need `media_id`.** W05 `renderReviewInboxList` writes title
   only. Extend it with `data-media-id` (UUID) on a `<button>`/`<li>` via
   `dataset` / `setAttribute`, still `textContent` for the title. Click opens
   overlay. Do not put titles in `chrome.storage`.
5. **Overlay chrome lives in the side panel**, not in `#frame`. Recommended:
   `<dialog>` in `sidebar.html` hosting `<iframe src="review.html">` (or
   `chrome.runtime.getURL("ui/review.html")`). Opening/collapsing the dialog
   must not call `clearFrame`, hide `#frame`, or rebuild the host iframe.
6. **Pass `media_id` without titles in the URL.** A hash such as
   `#media=<uuid>` is acceptable if `isUuid` gated, analogous to ingest
   `#url=` but **must not** accept X URLs. Do not put suggestion text in the
   hash.
7. **Opened POST is display, not apply.** Fire it when the overlay first
   shows a run and when the dropdown changes the displayed run. Do not fire
   it on mere list hover. Apply does not implicitly open; still POST opened
   when displaying.
8. **Apply body extra=forbid on the server.** Do not send `display_title`,
   `description`, or extra keys. `fields` unique nonempty subset of
   `display_title`, `tags`, `description`. `tag_keys` empty unless `tags` is
   selected; then 1–5 mapped keys in order.
9. **History page.** First GET detail uses server default limit (25). If
   `next_cursor` is present, a bounded “more” control may fetch the next
   page; it is not required if the first page is enough for the dropdown.
   Do not invent a second history store.
10. **Stay-open + inbox refresh.** After successful apply **and** after
    opened, ask the sidebar to run the existing inbox refresh (`REVIEW_INBOX`
    / `startInboxPoll`’s `refreshInbox`) so `unopened` and badge update.
    Do not add `setInterval` to the service worker.
11. **Do not add `notifications`.** Do not edit Python, ADRs, PRODUCT/SPEC,
    `save.*`, or `x_adapter.js`.
12. **Do not print extension private keys.** Reuse existing test origin
    fixtures already in the repo.

---

## 9. Client contracts

### Protocol types (add; keep `dropUnknown` strict)

Suggested:

```text
REVIEW_INBOX_DETAIL
REVIEW_INBOX_OPENED
REVIEW_INBOX_APPLY
```

Payloads are UUID `mediaId` plus opened/apply bodies as on the wire. Worker
returns sanitized `{ok, error, status, body}` without logging titles.

### Overlay UI

- History `<select>` (or equivalent) of successful generic runs.
- Three field rows with checkboxes: Title, Tags, Description.
- Suggestion title/description as text, not inputs that POST strings.
- Canonical current values and publication state (`unpublished` /
  `published`, `ready`, `missing_fields` in order `display_title`,
  `description`, `tags`).
- Valid field-source receipts when present (run id / model / applied time).
  Do not invent provenance the server did not return.
- Mapped chips removable; dropped tags listed with `status`.
- Save button disabled until ≥1 legal field is checked.
- Close control. Escape closes the dialog without apply.

Dropdown option label: local-formatted completion time, then `model_id`,
then title, separated by ` · `. Visible label/caption must say this is run
completion time.

### Manifest

WAR unchanged (picker + save only). `permissions` stay
`alarms`, `sidePanel`, `storage`. No `notifications`.

### Visual tokens

`review.css` must include the same companion gallery tokens already asserted
for picker (`--background`, `--surface`, `--accent`, `#00ff41`, …). Dark
side-panel chrome, not a new theme.

---

## 10. Causal slices

### Slice 1 — protocol + worker clients

`pathFor` detail/opened/apply, new message types, `fetchJson` GET/POST
wrappers, UUID guards. No overlay HTML required yet if tests can drive the
worker.

Gate: `pathFor` rejects non-UUIDs and caller URLs; dropUnknown rejects
unknown overlay types; POST bodies are the W04 JSON only.

### Slice 2 — overlay + sidebar wiring

`review.html|js|css`, sidebar dialog, list click, dropdown/fields/chips,
opened-on-display, apply stay-open, 403 close, iframe survival.

Gate: `#frame` survives; review files not in WAR; no `innerHTML`; Save
disabled until a field is selected; Tags cannot be checked with zero mapped
chips.

### Slice 3 — tests

Extend `tests/companion_review_extension.test.js`. Keep
`tests/x_companion_extension.test.js` ingest/Attach/WAR assertions green
**without** editing `save.*`. Update WAR assertions to prove review files
are absent.

Gate: Section 13 Node command exits 0.

At most **three** local commits. Example: `feat:` worker review routes,
`feat:` review overlay UI, `test:` overlay contracts. No push.

---

## 11. Changed-path allowlist

**Extension**

- `extension/shared/messages.js`
- `extension/background/service_worker.js`
- `extension/ui/sidebar.html`
- `extension/ui/sidebar.js`
- `extension/ui/sidebar.css`
- `extension/ui/review.html` (create)
- `extension/ui/review.js` (create)
- `extension/ui/review.css` (create)
- `extension/manifest.json` **only if** a comment-free WAR assertion requires
  proving review files are absent without changing resources (default: do
  **not** change the WAR list)

**Tests**

- `tests/companion_review_extension.test.js`
- `tests/x_companion_extension.test.js` (WAR / permissions / token list if
  review.css must join the token assertion; do not weaken ingest tests)
- `tests/companion_web_bridge.test.js` **only if** handshake breaks because
  of sidebar dialog messaging

**Meta**

- `/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/06_report_00.md`

Forbidden: Python under `src/` or `tests/*.py`; Alembic; ADRs; PRODUCT/SPEC/
`X_COMPANION.md`; `extension/ui/save.*`; `extension/content/x_adapter.js`;
`extension/ui/picker.*` unless a strictly smaller shared-token extraction
passes every picker test (default: do not touch picker); NUC; push.

If a current assertion lives in a JS file not listed and the suite fails,
stop `PARTIAL` and name the path.

---

## 12. Git, network, secret, side-effect

```text
Git authority: local commits only, at most three, on feat/x-meme-browser-companion; no amend of feb9a69 or earlier; no rebase, reset, stash, clean, fetch-that-rewrites, or push; do not update git config; do not skip hooks
Network authority: git ls-remote to the two public refs; no provider; no NUC; no live FrameNest origin calls
Secret authority: none
Filesystem authority: read FrameNest and pinned .ap; write only Section 11 plus the Meta report
Side-effect authority: reversible local source/tests/commits
Dependency authority: none
Browser authority: none
```

Commit subject examples:

- `feat: add companion review overlay worker routes`
- `feat: open administrator companion review overlay`

HEREDOC messages. Do not commit Meta or secrets. Amend only your own
not-yet-pushed commit, and only if a hook auto-modified files after success.

---

## 13. Tests

Each candidate SHA:

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <SHA>
```

JavaScript (worktree root):

```text
node --test tests/companion_review_extension.test.js tests/x_companion_extension.test.js tests/companion_web_bridge.test.js tests/x_acquisition_cockpit.test.js
```

Do not run `FRAMENEST_RUN_BROWSER_EVIDENCE=1`. Do not run Python
`test-focus`. Do not call NVIDIA.

Required overlay cases (new or extended):

- Review files exist and are **not** in WAR. Sidebar/review not web-accessible.
- `pathFor` detail/opened/apply: UUID only; non-UUID / evil strings → null.
- dropUnknown still rejects unknown types; new types accepted.
- Worker GET detail; POST opened `{analysis_run_id}`; POST apply
  `{analysis_run_id, fields, tag_keys}` with `X-FrameNest-Request`. No
  title/description in apply JSON. No `fetch(payload.url)`.
- List click opens overlay for `data-media-id`. `#frame` still present;
  `clearFrame` is not called.
- Parent/child messages: exact extension origin; no wildcard target.
- Dropdown label includes local-formatted `completed_at_ms`, `model_id`,
  title; switching resets checkmarks/chips and POSTs opened, not apply.
- Save disabled with zero fields; enabled with one legal field.
- Mapped chip removal; Tags checkbox disabled when no mapped chips remain;
  dropped statuses rendered without being submitted.
- Success 200 `not_ready` and `published` keep overlay open, clear
  selections, show receipts/publication; error keeps selections.
- 403 closes overlay and does not leave titles in the dialog DOM.
- `innerHTML` absent from `review.js` / new sidebar review paths.
- Ingest Save invariants remain (existing tests, do not weaken):
  - Title → Tags → Description → Save
  - No radios / Analyze
  - Save enabled
  - Host Enter submits; Description Enter inserts newline; highlighted tag
    suggestion is accepted
  - `#url=` only; POST remains `{url, alias}`
- Attach / web-bridge handshake stays green.
- Service worker still has no `setInterval`. Alarms/`notifications` freeze
  from W05 remains.

Prefer executable `chrome` / `fetch` fakes over regex-only proofs for
opened/apply.

---

## 14. Negative authority

- Native Plan Mode, Max, sub-agents
- Push, NUC, sudo, provider, signed-in browser, browser-evidence flag
- Living PRODUCT/SPEC/`X_COMPANION.md` / ADR body edits (W07)
- Python / Alembic
- `"notifications"` or `chrome.notifications`
- Ingest Save overlay edits (`save.*`, `x_adapter.js`)
- CORS, `all_urls`, alias PUT, creating canonical tags
- Enabling `FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED`
- Closing the logical whole

---

## 15. INFOSEC R1 (inline)

- Suggestion title/description/tags are untrusted. `textContent` /
  `createElement` only. No `innerHTML`.
- Ordinary 403 must not leak titles via overlay leftovers, storage, badge,
  or logs.
- Apply/opened audit is server-side (W04). Client must not put suggestion
  bodies, `tag_keys` dumps, or credentials into `console` or storage.
- Network only from the service worker via `pathFor`.
- Overlay iframe is extension-origin, not WAR, not hosted FrameNest origin.
- Residual: G2 can publish structurally ready items after Save; Cooperator-
  owned. Independent R3 remains later, before deploy.

---

## 16. Terminal report

Begin exactly:

```markdown
### Report for ORCHESTRATOR_CHAT
```

```text
Logical whole identity: framenest-companion-ai-review-inbox-mvp
Worker session ordinal: 06
Worker exchange ordinal: 01
```

Include: status; handshake; baseline ledger; changed paths; proof of Section 7
invariants (local overlay not WAR, opened-on-display, apply without client
title strings, stay-open, iframe survival, ingest Save unchanged); commands/
exit codes; git SHAs (no push); INFOSEC residual; near-misses / pre-existing
classification; smallest next step = Orchestrator issues W07 (living docs)
after verification. Do not self-issue W07.

Write only the Meta path in Section 1.

---

## 17. Quality bar

`PASS` only if HEAD descends from `feb9a69` via this Worker; schema head
remains **0031**; no Python server diff; `review.html|js|css` exist and are
not WAR; opened/apply go through `pathFor` + `fetchJson`; overlay stay-open
and ingest Save tests pass; `#frame` survives; Node suite in Section 13
exits 0; no push.

`PARTIAL` for one named evidence gap. `BLOCKED` for baseline/Plan Mode/
allowlist contradiction.

---

## 18. Stop

After the terminal report, stop. Do not start W07 living docs, R3, or deploy.
