# Authoritative Prompt for Fresh Worker 05

## FrameNest Companion AI Review Inbox — native S1 list, badge, and alarms

You are a Worker instance assigned to WORKER. Read this complete prompt before
taking any action.

Worker 04 landed opened/apply POSTs, dual-capability `RoutePolicy`, receipts,
and atomic G2 publication at
`9a77591ff52ea2ca7c6458d850694b5e951e465a`. That authority is expired. The
COOPERATOR accepted S1 (native list above the surviving iframe), badge/list
without `notifications`, and later `alarms`. The ORCHESTRATOR independently
verified W04 (`ap project check` PASS; focused suite **165 passed** against
`--baseline 9a77591`).

Your task is **slice W05 only**: MV3 `alarms`, toolbar badge from
`unopened_count`, and a native collapsible review-inbox list in the side panel
**above** the still-mounted hosted iframe. Do **not** implement the review
overlay (`review.html|js|css`), apply/opened client calls, living docs, or NUC.

Do not enter Native Plan Mode. Do not use Max. Extra High is requested; if the
client does not expose a measurable Extra High SKU, continue only while Plan
Mode stays off and Max is unused, and record that in the handshake. If Native
Plan Mode is on, stop `BLOCKED`.

```text
Role: WORKER
Persistent role identity: WORKER
Logical whole identity: framenest-companion-ai-review-inbox-mvp
Prior logical whole identities:
  framenest-x-meme-browser-companion-mvp
  framenest-x-companion-save-alias-mvp
  framenest-x-companion-save-category-mvp
  framenest-x-save-overlay-edit-subset-mvp
Worker session ordinal: 05
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Worker session profile: Fresh Implementation Worker
Phase: implementation
Task identity: FN-COMPANION-AI-REVIEW-INBOX-IMPL-05
Task type: bounded implementation candidate
Native planning mode: not-used
Reasoning recommendation: extra-high
Evidence posture: non-independent
Independence required: no
Authority renewal: not applicable — fresh Worker authority originates only in this prompt
Prior implementation report: Worker 04 / exchange 01 implementation-PASS at 9a77591; authority expired
Continuity anchor: none — do not resume Worker 04
Internal delegation posture: not-used
Accountable Worker: one WORKER
Material phase gate: yes
Changed material axis: mutation-authority-or-side-effect-class
Routing reopened for: mutation-authority-or-side-effect-class
Unchanged axes reopened: none
Ordinary-only trigger: no
Automatic model selection: off
Enhanced/maximum mode: not requested
```

```text
Implementation authority: explicit
Native planning mode: not-used
Worker session target: fresh-worker-session
Exact baseline: 9a77591ff52ea2ca7c6458d850694b5e951e465a
Changed-path allowlist: Section 11
Implementation boundaries: Sections 8, 9, 10, 12, and 14
Independence required: no
```

```text
Evidence tier: E3
Evidence tier basis: MV3 alarms, toolbar badge, native side-panel inbox chrome; no overlay/NUC; no Python server mutation
Authorized implementation stages: causal slices 1–3 in Section 10
Combined implementation envelope: allowed for those three slices only
Implementation stage gates: Section 10; a failed gate stops the sequence
Independent acceptance: not-required (this session)
Rollback or recovery checkpoint: local Git commits on feat/x-meme-browser-companion; no push; no NUC
Activated stricter profile: INFOSEC.md R1 inline only
Terminal implementation report point: after slices 1–3 are committed and validated, before review overlay
```

```text
COOPERATOR: Michal
ORCHESTRATOR: ORCHESTRATOR_CHAT
WORKER: this fresh implementation session
```

Internal delegation, sub-agents, parallel Workers, Explore tasks, and hidden
secondary workstreams are not authorized.

Repository documentation, code comments, test names, commit subjects, and the
terminal Worker report must use professional English. Czech is forbidden.
The terminal report must begin exactly:

```text
### Report for ORCHESTRATOR_CHAT
```

Implementation PASS is not acceptance, publication, deployment, or closure.
`Logical-whole closure: not-closed`.

Protocol-variant selection:

```text
Canonical repository identity: https://github.com/cisarik/ap.git
Immutable version identity: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Declared variant: stable
Governing variants in effect: one
Declaration location: project governing rules
Rules from non-governing variants: none
Migration required: no
```

---

## 1. External trace and Meta write boundary

```text
External trace disposition: configured
Trace discovery: cisarik/meta repository path projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp
Trace project key: framenest
Trace logical-whole projection identity: 03/09-framenest-companion-ai-review-inbox-mvp
Trace authority: historical-evidence-only
Trace archival owner: ORCHESTRATOR after the outcome exists
Trace visibility: public
Trace companion outcome: report
Trace self-granted status: none
Downloadable prompt filename: 05_implementation_00.md
Destination path: projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/05_implementation_00.md
Archival: wait-for-report
```

You may **read** (historical; this prompt wins on W05 scope):

```text
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/01_report_00.md
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/04_implementation_00.md
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/04_report_00.md
```

Frozen hashes at issuance (re-check; stop `BLOCKED` if drifted):

```text
01_report_00.md SHA-256 51e124c02009a6822ebb36afc8893187074c680cd139462d79e72cb61bab75ce
04_implementation_00.md SHA-256 d744be8d4b927d04c5dc3810e1c8be34d4efc2d3502fdab5ee1581ccfc0f90ef
04_report_00.md SHA-256 e77dc67d516ba98db3288298b67cd38dd01a075b1bbe4705d99937501e1f76dc
```

You may **write** only:

```text
/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/05_report_00.md
```

Do not alter any other Meta path. Do not stage or commit Meta. If the report
file cannot be written, return it verbatim in chat.

---

## 2. Communication and human-governance routing

```text
Worker prompt language: professional English
Worker report language: professional English
Direct-user Slovak presentation: Orchestrator-owned
Report header: ### Report for ORCHESTRATOR_CHAT
Cooperator visibility: W05 grant already issued inside the accepted envelope
Human decision points remaining outside this grant: review overlay (W06), living docs (W07), R3, NUC
Deterministic steps inside bounded authority: slices 1–3, tests, local commits
Internal delegation posture: not-used
```

---

## 3. Capability handshake

Fresh session. Compact handshake before mutation. Record requested / directly
observed / inferred / unknown. Capability does not grant authority. If Plan
Mode is on, stop `BLOCKED`.

```text
Validation ladder: selected
Inspection and provenance: required
Existing focused tests: Section 13
Broad or full suite: not-used
Independent acceptance: not-required
```

Browser, provider, NUC, publication, and deployment annexes are **not
activated**. Do not run `tests/browser_companion_evidence.test.js`.

---

## 4. Repository gate and exact baseline

```text
Expected canonical root: /home/agile/Projects/framenest
Repository checkout topology: standalone checkout
Working-copy topology: canonical checkout
Applicable branch: feat/x-meme-browser-companion
Expected HEAD: 9a77591ff52ea2ca7c6458d850694b5e951e465a
Expected parent: 1d9a71ed1e32ba9001f9f9d4f8935be34800aa6b
Expected tree: beb37a20b4cc26e9b4ff942c9ad0770b7a85c24c
Expected subject: feat: apply companion review fields and publish when ready
Working tree: expected clean
Upstream: none configured (expected)
Schema head at baseline: Alembic 0031
```

```text
.ap gitlink and checkout: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Detached submodule checkout: acceptable
```

Issuance-time public refs (re-verify with `git ls-remote`, no fetch):

```text
cisarik/framenest refs/heads/main  045f33b44897a6f3949cc515792336396f1d33a1
cisarik/ap        refs/heads/main  9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
```

If local HEAD is not `9a77591`, stop `BLOCKED` unless the only difference is
this Worker’s own later commits. Do not stash, reset, or clean.

Python remains forbidden to invoke ambiently. This slice is **extension/JS**.
If you do not change Python, do not invent a Python mutation. You may still
run:

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <EXACT SHA UNDER TEST>
```

after each commit as a provenance gate. Do **not** run a full Python `test`
operation. JavaScript tests use Node’s built-in runner from the worktree root
(see Section 13). No `npm install`. No ambient `.venv/bin/python`.

Spot-check before mutation:

- Manifest permissions are exactly `sidePanel` and `storage`. `alarms` is
  absent. The `action` **key** (toolbar button, no `default_popup`) exists;
  there is no `"action"` permission string — do not invent one.
- `web_accessible_resources` is picker + save only. Sidebar files are not WAR.
- Service worker has `fetchJson` + `pathFor` and **no** `setInterval`.
- `claimSnapshot` currently omits `assets`; terminal X poll JSON already
  includes `assets[].media_id`. Do not add a server route.
- GET `/api/companion/review-inbox` already exists (W03). This Worker is a
  client of that GET. Do not POST opened/apply from the side panel.
- Ingest Save overlay (`extension/ui/save.*`, `extension/content/x_adapter.js`)
  is frozen.
- Existing test
  `manifest permissions stay minimized and omit X host access` asserts
  `alarms === false`. You must update that assertion when adding `alarms`.

---

## 5. Required reading

- `AGENTS.md`, `.ap/AP.md`, `.ap/AP_WORKER.md`, `.ap/PROMPT_CONTRACTS.md`
- `.ap/INFOSEC.md` §3–§5 (R1 only)
- `docs/WORKER_EXECUTION_CONTRACT.md` (JavaScript Tests)
- `docs/adr/0063-companion-side-panel-web-host.md`
- `docs/adr/0071-native-side-panel-review-inbox-chrome.md`
- `01_report_00.md` Sections 7 (S1 + badge/alarms), 14 (W05/W06 extension
  gate — **W05 owns list/badge/alarms only**; overlay bullets are W06)
- `extension/manifest.json`
- `extension/shared/messages.js` (`pathFor`, `TYPES`, `dropUnknown`)
- `extension/background/service_worker.js` (`fetchJson`, `configureOrigin`,
  `resetState`, `claimSnapshot`, inflight storage — **no setInterval**)
- `extension/ui/sidebar.html`, `sidebar.js`, `sidebar.css`
- `tests/x_companion_extension.test.js` (manifest, sidebar chrome, ingest Save
  subset, Attach, `setInterval` ban on the worker)
- `tests/companion_web_bridge.test.js` (iframe host handshake / Attach)
- `tests/x_acquisition_cockpit.test.js` (must stay green; do not edit unless a
  listed ingest assertion lives only there)

---

## 6. Goal

One coherent W05 candidate:

1. Add `"alarms"` to extension `permissions`. Keep the existing `action`
   toolbar key. Do **not** add `"notifications"`, `"tabs"`, `host_permissions`,
   or `externally_connectable`.
2. Named one-minute badge alarm whenever a valid FrameNest origin is
   configured (service-worker startup, `onInstalled`, `onStartup`). Reset /
   disconnect clears the alarm and badge.
3. Each alarm (and connect) performs one bounded inbox GET via the existing
   service-worker `fetchJson` path. Badge text is `1`…`99` or `99+`; zero,
   403, disconnect, or request failure clears it.
4. Native collapsible `review-inbox` section **between** existing sidebar
   header/status chrome and the existing iframe. Titles only, newest first,
   bounded scroll. Ordinary 403 hides the section and badge with no title
   leakage. Empty administrator copy: `No analyzed items.`
5. Network access remains service-worker-only. Sidebar talks to the worker
   through existing protocol messages (`dropUnknown`). Do not `fetch` from
   sidebar/content scripts.

---

## 7. Accepted decisions (do not re-litigate)

From accepted `01_report_00.md`, ADR-0071, and Cooperator 2026-08-23:

- S1: native list under the FrameNest wordmark **above** the still-mounted
  iframe. Never unmount the iframe. Do not replace the iframe with the inbox.
  Do not clone the in-page picker into the side panel.
- Show titles only. Newest first. Bounded scrolling list.
- Empty administrator state: `No analyzed items.`
- Ordinary identities: inbox GET 403 → section **and** badge disappear; no
  titles in DOM, storage, badge, or logs.
- Persist **explicit** collapse preference in `chrome.storage`. An empty inbox
  collapses automatically (that is not an explicit dismiss). A new item
  auto-expands only when the user has **not** explicitly collapsed/dismissed
  the section. Expanding the section clears the explicit-dismiss flag.
- Badge count is the server `unopened_count` (full actor set), **not** the
  length of the returned page and **not** failed runs.
- Add `"alarms"`; retain toolbar `action`; no `"notifications"`.
- Alarm cadence: one named alarm, `periodInMinutes: 1`.
- Side panel **additionally** polls every 15 seconds **while the sidebar
  document is visible/loaded**, and immediately after connect. (Opened/apply
  immediate refresh is W06; do not invent overlay success hooks here.)
- Do **not** store titles or suggestion bodies in `chrome.storage`. Allowed
  durable client facts: origin, inflight claims, collapse boolean, optional
  last-seen newest `analysis_run_id` / media UUIDs, awaiting-analysis media
  UUIDs + expiry. No suggestion JSON.
- A terminal successful X claim may retain **only media IDs** and a 30-minute
  “awaiting analysis” hint. Remove the hint when that media appears in the
  inbox list or the deadline passes. The hint is **not** opened-state truth
  and does **not** drive the badge alarm.
- Parent/child web messages stay exact FrameNest origin + source. Inbox is
  not a `postMessage` web-host feature.
- Use `textContent` / DOM nodes only. No `innerHTML`.
- Review overlay files, checkmarks, apply, and mark-opened HTTP from the
  client are **W06**. W05 list rows must not open a missing `review.html`.

---

## 8. Orchestrator binding reconciliations

1. **Do not add a permission named `"action"`.** Chrome has no such
   permission. Keep the manifest `action` object (default title/icons, no
   popup). Add only `"alarms"` to `permissions`.
2. **Do not use `setInterval` in the service worker.** Existing tests forbid
   it because MV3 workers suspend. Badge refresh is `chrome.alarms`. The
   15-second poll lives in **sidebar.js** (the side-panel page stays loaded
   while visible). Clear that timer on disconnect / page hide.
3. **Inbox GET is not a companion mutation.** Do not send it as a POST. Do
   not require a new Origin policy. `fetchJson` already sets
   `X-FrameNest-Request: 1`. Reuse it. Add a `pathFor` name such as
   `reviewInbox` → `/api/companion/review-inbox`. Reject caller-supplied
   URLs/ids the same way other `pathFor` cases do. Optional `limit` query
   must be built from a bounded integer, not concatenated untrusted strings.
   Badge-only fetches may use `limit=1` because `unopened_count` is the full
   set. The visible list uses the server default (25, max 100).
4. **Do not POST opened or apply.** Viewing the list is not “displaying a
   run” in the overlay sense. Opened writes stay W06.
5. **Do not create `extension/ui/review.html|js|css`.** Do not add them to
   WAR. Sidebar/review chrome is not web-accessible.
6. **Awaiting-analysis hint uses existing poll JSON.** `GET /api/x/requests/{id}`
   already returns `assets[].media_id`. Extend `claimSnapshot` (or a sibling
   helper) to collect UUID media ids from cataloged assets on terminal
   success. Persist `{media_id, expires_at_ms}` only. Copy is generic (no
   titles), e.g. awaiting analysis. Inflight claim records remain claim-id
   based; do not keep titles from X post text in this new store.
7. **Collapse keys must not include titles.** Suggested shape:
   `reviewInboxExplicitCollapsed: boolean` plus optional
   `reviewInboxSeenRunId: uuid`. Detect “new item” by newest
   `analysis_run_id` from the GET, not by storing titles.
8. **403 vs empty.** Empty admin list shows the section (collapsed) with
   `No analyzed items.` Ordinary 403 **hides** the section entirely. Do not
   treat 403 as an empty admin inbox.
9. **Identity.** Prefer the inbox GET as the gate (403). You may call
   existing `identity` GET, but hiding must still follow inbox 403 so a
   stale identity cache cannot leak titles.
10. **Iframe survival.** `clearFrame()` today removes `src` on disconnect —
    that stays for disconnect. Collapsing the inbox must **not** call
    `clearFrame`, must not `hidden=true` the iframe, and must not rebuild
    the iframe node. Attach handshake tests must remain green.
11. **Do not edit Python, ADRs, PRODUCT/SPEC, save overlay, or x_adapter.js.**
12. **Do not print or add new extension private keys.** The existing test
    fixture origin already in `tests/x_companion_extension.test.js` may be
    reused as a non-composer origin. Do not copy signing keys into the
    report.

---

## 9. Client contracts

### Manifest

```text
permissions: alarms, sidePanel, storage   (order may be sorted)
optional_permissions: downloads
optional_host_permissions: https://*.ts.net/*
action: keep default_title/icons; no default_popup
no notifications, tabs, cookies, host_permissions, externally_connectable
```

### Service worker

- Alarm name: a single stable string, e.g. `framenest.review-inbox`.
- Create/ensure the alarm when origin is valid: worker startup,
  `onInstalled`, `onStartup`, and successful `CONFIGURE_ORIGIN`.
- `RESET` / disconnect: `chrome.alarms.clear` that name; `chrome.action.setBadgeText({ text: "" })`;
  hide/clear any in-memory inbox cache; remove awaiting-analysis records.
- Alarm handler: one GET inbox; set badge from `unopened_count`; on 403 /
  not_configured / network_failed / non-OK → clear badge. Never put a title
  in badge text.
- New protocol type(s) for the sidebar, for example `REVIEW_INBOX` (list
  fetch) and optionally `REVIEW_INBOX_HINTS` if hints are worker-owned.
  `dropUnknown` must keep rejecting unknown types. Do not add overlay
  apply/open types.
- Do not `fetch(payload.url)`. Continue using `pathFor`.

### Sidebar

Insert a `<section id="review-inbox">` (or equivalent stable id) **after**
`#shell-status` / header chrome and **before** `#frame`. Contents:

- Collapse control (button; `aria-expanded`).
- Bounded `ol`/`ul` of titles via `textContent`.
- Empty state node with exactly `No analyzed items.`
- Optional awaiting-analysis hint node (no titles, no media filenames).

Collapsed CSS must not unmount `#frame`. List overflow is internal
(`max-height` + `overflow-y: auto`). Newest first (server already orders
that way; do not re-sort by title).

15 s poll: `setInterval` **only** in sidebar.js, cleared on disconnect and
when the document is hidden if you listen to `visibilitychange`. Do not add
`setInterval` to the service worker.

### Storage (local)

Allowed keys beyond today’s origin/inflight/acknowledged:

- explicit collapse boolean
- optional last-seen newest run id (UUID)
- awaiting-analysis records: media UUID + expiry ms, cap the list (≤16)

Forbidden: titles, descriptions, tag strings, suggestion JSON, result_json.

---

## 10. Causal slices

### Slice 1 — protocol, pathFor, alarms, badge

`messages.js` path + types; service worker alarm lifecycle; badge from
`unopened_count`; reset clears alarm/badge. No sidebar layout yet is
acceptable if tests can still target worker helpers.

Gate: permissions include `alarms` and not `notifications`; worker has no
`setInterval`; `pathFor("reviewInbox")` is the inbox GET; caller-supplied
URLs still rejected.

### Slice 2 — S1 sidebar chrome

HTML/CSS/JS list, collapse, empty/403, 15 s poll, iframe survival,
awaiting-analysis hint.

Gate: iframe node remains; Attach handshake copy unchanged; ordinary 403
hides titles; empty admin copy exact; explicit collapse persisted without
titles.

### Slice 3 — tests

New `tests/companion_review_extension.test.js` plus updates to existing
manifest/sidebar assertions. Keep ingest Save and Attach tests green
without editing `save.*` / `x_adapter.js`.

Gate: Section 13 JS command exits 0.

At most **three** local commits. Example: `feat:` alarms/badge, `feat:` S1
list, `test:` companion review extension. No push.

---

## 11. Changed-path allowlist

**Extension**

- `extension/manifest.json`
- `extension/shared/messages.js`
- `extension/background/service_worker.js`
- `extension/ui/sidebar.html`
- `extension/ui/sidebar.js`
- `extension/ui/sidebar.css`

**Tests**

- `tests/companion_review_extension.test.js` (create)
- `tests/x_companion_extension.test.js` (update assertions that this slice
  makes false: `alarms`, sidebar DOM order, permissions sort)
- `tests/companion_web_bridge.test.js` **only if** a handshake assertion
  must move; prefer not to touch it if iframe survival stays intact
- `tests/x_acquisition_cockpit.test.js` **only if** a listed ingest
  assertion fails solely there — do not change cockpit product copy

**Meta**

- `/home/agile/meta/projects/framenest/03/09-framenest-companion-ai-review-inbox-mvp/05_report_00.md`

Forbidden: Python under `src/` or `tests/*.py`; Alembic; ADRs; PRODUCT/SPEC/
`X_COMPANION.md`; `extension/ui/save.*`; `extension/content/x_adapter.js`;
`extension/ui/picker.*` unless a one-line comment-free CSS token extraction
is strictly smaller **and** every ingest/Attach test still passes (default:
do not touch picker); `review.html|js|css`; NUC scripts; push.

If a current assertion lives in a JS file not listed and the focused Node
suite fails, stop `PARTIAL` and name the path.

---

## 12. Git, network, secret, side-effect

```text
Git authority: local commits only, at most three, on feat/x-meme-browser-companion; no amend of 9a77591 or earlier; no rebase, reset, stash, clean, fetch-that-rewrites, or push; do not update git config; do not skip hooks
Network authority: git ls-remote to the two public refs; no provider; no NUC; no live FrameNest origin calls
Secret authority: none
Filesystem authority: read FrameNest and pinned .ap; write only Section 11 plus the Meta report
Side-effect authority: reversible local source/tests/commits
Dependency authority: none
Browser authority: none
```

Commit subject examples:

- `feat: poll companion review inbox for the toolbar badge`
- `feat: add native side-panel review inbox chrome`

HEREDOC messages. Do not commit Meta or secrets. Amend only your own
not-yet-pushed commit, and only if a hook auto-modified files after success.

---

## 13. Tests

Each candidate SHA:

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <SHA>
```

JavaScript (worktree root, Node built-in runner):

```text
node --test tests/companion_review_extension.test.js tests/x_companion_extension.test.js tests/companion_web_bridge.test.js tests/x_acquisition_cockpit.test.js
```

Do not run `FRAMENEST_RUN_BROWSER_EVIDENCE=1`. Do not run Python
`test-focus` unless you violate the Python ban (then stop `BLOCKED` and
revert). Do not call NVIDIA.

Required cases (new file plus updated assertions):

- Manifest: `alarms` present; `notifications` absent; `action` key retained
  without `default_popup`; sidebar/review files not in WAR; save/picker WAR
  unchanged.
- Alarm created on configure/startup/install when origin is valid; cleared
  on reset; named; period 1 minute. Worker source still has no `setInterval`.
- Badge: `1`…`99`, `99+`, cleared on 0 / 403 / failure / reset. Badge uses
  `unopened_count`, not `items.length`.
- `pathFor` inbox GET; rejects caller URLs; no `fetch(payload.url)`.
- S1 DOM order: header/status → review-inbox → iframe. Iframe id `frame`
  survives collapse/empty/403.
- Empty admin: `No analyzed items.` Ordinary 403: section hidden, no titles
  in HTML text.
- Collapse preference persists without titles; empty auto-collapse; new-item
  auto-expand only without explicit dismiss.
- Awaiting-analysis: media UUID + 30 min; removed when id appears in inbox
  or expired; does not change badge math.
- Ingest Save invariants remain (existing tests, do not weaken):
  - Title → Tags → Description → Save
  - No radios / Analyze
  - Save enabled
  - Host Enter submits; Description Enter inserts newline; highlighted tag
    suggestion is accepted
  - `#url=` only; POST remains `{url, alias}`
- Attach: iframe host handshake and composer attach tests stay green.
- No `innerHTML` in new sidebar inbox rendering.

Reuse existing `extractNamedFunction` / `vm` patterns. Prefer executable
fakes of `chrome.alarms` / `chrome.action` / `chrome.storage` over
source-regex-only proofs for alarm/badge behavior.

---

## 14. Negative authority

- Native Plan Mode, Max, sub-agents
- Push, NUC, sudo, provider, signed-in browser, browser-evidence flag
- `review.html|js|css`, checkmarks, apply POST, opened POST
- `"notifications"` permission or `chrome.notifications`
- Python / Alembic / ADR / PRODUCT / SPEC / `X_COMPANION.md`
- Ingest Save overlay edits (`save.*`, `x_adapter.js`)
- CORS, `all_urls`, alias PUT
- Enabling `FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED`
- Closing the logical whole

---

## 15. INFOSEC R1 (inline)

- Inbox titles are untrusted (X + NIM). Render with `textContent` / DOM
  createElement only. No `innerHTML`.
- Ordinary 403 must not leak titles via empty-state confusion, badge text,
  storage, or error strings.
- Service worker logs/audit: no titles, descriptions, tags, result JSON, or
  credentials. Alarm/badge code paths handle 403 as hide/clear.
- Network only from the service worker to the configured FrameNest origin
  through `pathFor`.
- `chrome.storage` must not become a second suggestion store.
- Residual: one-minute alarm is a battery/network cost the Cooperator
  accepted. Independent R3 remains later, before deploy.

---

## 16. Terminal report

Begin exactly:

```markdown
### Report for ORCHESTRATOR_CHAT
```

```text
Logical whole identity: framenest-companion-ai-review-inbox-mvp
Worker session ordinal: 05
Worker exchange ordinal: 01
```

Include: status; handshake; baseline ledger; changed paths; proof of Section 7
invariants (alarms not notifications, badge from `unopened_count`, S1 above
iframe, 403 hide, no title storage, no overlay, ingest/Attach still green);
commands/exit codes; git SHAs (no push); INFOSEC residual; near-misses /
pre-existing classification; smallest next step = Orchestrator issues W06
(review overlay) after verification. Do not self-issue W06.

Write only the Meta path in Section 1.

---

## 17. Quality bar

`PASS` only if HEAD descends from `9a77591` via this Worker; schema head
remains **0031**; no Python server diff; `alarms` added and `notifications`
absent; native list exists above `#frame`; worker has no `setInterval`;
Node suite in Section 13 exits 0; ingest Save tests still pass without
`save.*` edits; no overlay files; no push.

`PARTIAL` for one named evidence gap. `BLOCKED` for baseline/Plan Mode/
allowlist contradiction.

---

## 18. Stop

After the terminal report, stop. Do not start W06 overlay, living docs, or
deploy.
