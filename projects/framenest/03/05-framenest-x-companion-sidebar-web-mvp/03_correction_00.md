# Authoritative Prompt for Fresh Worker 03

## FrameNest × X Companion Side Panel — Cooperator visual correction

You are a Worker instance assigned to WORKER. Read this complete prompt before
taking any action.

Worker 02 produced local candidate `91283a70fcee039dd20f43bae5bf90e5901f01e8`
(`implementation-PASS`, non-independent). That authority is expired. Do not
resume the Worker 02 chat. Do not enter Native Plan Mode. Do not reopen
ADR-0061/0062, `companion_mutation`, NUC deployment, publication, overlay
editing, or ordinary-tab Gallery restyle.

The COOPERATOR live-looked the unpacked companion against origin
`https://framenest-nuc.tail247768.ts.net` (NUC still public `bfad16b`). He
classified:

1. **PASS** — toolbar opens the side panel and the real FrameNest web is
   visible under the shell.
2. **Misleading error** — red `FrameNest could not be framed in this panel.`
   while the Gallery iframe is clearly loaded. He does not understand it.
3. **FAIL (picker search vs Gallery)** — in-page picker first-hit preview
   works; query `Cardano` shows `No eligible memes` while Gallery shows
   Cardano cards. **Do not expand picker audience in this correction.** See
   Section 8.4.
4. **FAIL (Attach control)** — he still sees bottom-right “Open original
   media”. He now wants **both**: keep open-original bottom-right; put 📎
   **top-left** on the card image (side panel is on the right; attach aims
   left onto X). Ordinary tabs stay open-original only.
5. **Cannot test Attach** — no 📎 for admin or ordinary user. Root cause is
   Section 8.1 (NUC web has no `companion_host.js`). Do not deploy NUC.

He also amended Surface C chrome (still this whole, not a new whole):

- Green OS-like title bar at the very top; black wordmark `FrameNest`.
- Settings control in that bar. The Settings **modal must leave the in-page
  quick-attach popup** and live in the side panel. That move is in-scope here
  (his “priority 2” together with the framing-copy fix).
- Far-right in the green bar: the solid green action that is currently
  Connect. When connected, the same control reads **Disconnect** (runs
  existing `RESET`). Hide origin label, origin input, and a separate Reset
  button from the main chrome. Origin field remains inside Settings for
  first-run and origin change.
- Shell chrome must read as FrameNest; the iframe below remains the real web.

```text
Role: WORKER
Persistent role identity: WORKER
Logical whole identity: framenest-x-companion-sidebar-web-mvp
Prior logical whole identities:
  framenest-x-meme-browser-companion-mvp
  framenest-x-companion-save-alias-mvp
Worker session ordinal: 03
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Worker session profile: Bounded Correction Worker
Phase: implementation
Task identity: FN-X-COMPANION-SIDEBAR-CHROME-03
Task type: bounded visual correction of side-panel chrome, settings home, handshake copy, and hosted Attach placement
Native planning mode: not-used
Reasoning recommendation: extra-high
Evidence posture: non-independent
Independence required: no
Authority renewal: not applicable — fresh Worker authority originates only in this prompt
Continuity anchor: none — do not resume Worker 02
Prior authorities: expired
Internal delegation posture: not-used
Accountable Worker: one WORKER
Material phase gate: no
Changed material axis: none
Routing reopened for: none
Unchanged axes reopened: none
Ordinary-only trigger: yes
```

```text
Implementation authority: explicit
Native planning mode: not-used
Worker session target: fresh-worker-session
Exact baseline: 91283a70fcee039dd20f43bae5bf90e5901f01e8
Changed-path allowlist: Section 9
Implementation boundaries: Sections 7, 8, 10, and 16
Independence required: no
```

```text
Evidence tier: E1
Evidence tier basis: reversible local extension/web UX correction; no new authZ route; no production mutation
Authorized implementation stages: handshake copy + shell chrome/settings + hosted Attach placement + tests + docs sentence + local commit
Combined implementation envelope: allowed
Independent acceptance: not-required (this session). Later R3 remains a separately issued grant.
Rollback or recovery checkpoint: local Git commits on feat/x-meme-browser-companion; no push
Activated stricter profile: none
Terminal implementation report point: after commits and focused tests
```

Reasoning recommendation: Extra High, already selected for this whole. Do not
use Max. Do not silently downgrade. If Extra High cannot be provided, or if
Native Plan Mode is on, stop `BLOCKED`.

```text
COOPERATOR: Michal
ORCHESTRATOR: ORCHESTRATOR_CHAT
WORKER: this fresh bounded correction session
```

Repository documentation, code comments, test names, commit subjects, and the
terminal Worker report must use professional English. Czech is forbidden. The
terminal report must begin exactly:

```text
### Report for ORCHESTRATOR_CHAT
```

Correction PASS is not acceptance, publication, NUC, or logical-whole closure.
This Worker must not claim that live NUC Gallery now shows 📎; that requires
the stored origin to serve this branch’s `companion_host.js`. Michal’s next
look is Reload unpacked of the **extension shell** plus, if he points at a
build that includes `companion_host.js`, the 📎 placement.

---

## 1. External trace and Meta write boundary

```text
External trace disposition: configured
Trace discovery: cisarik/meta repository path projects/framenest/03/05-framenest-x-companion-sidebar-web-mvp
Trace project key: framenest
Trace logical-whole projection identity: 03/05-framenest-x-companion-sidebar-web-mvp
Trace authority: historical-evidence-only
Trace archival owner: ORCHESTRATOR after the outcome exists
Trace visibility: public
Trace companion outcome: report
Trace self-granted status: none
Cooperator delivery / trace destination: configured
Downloadable prompt filename: 03_correction_00.md
Destination path: projects/framenest/03/05-framenest-x-companion-sidebar-web-mvp/03_correction_00.md
Archival: wait-for-report
```

You may **read** (historical only):

```text
/home/agile/meta/projects/framenest/03/05-framenest-x-companion-sidebar-web-mvp/01_report_00.md
/home/agile/meta/projects/framenest/03/05-framenest-x-companion-sidebar-web-mvp/02_report_00.md
```

You may **write** only:

```text
/home/agile/meta/projects/framenest/03/05-framenest-x-companion-sidebar-web-mvp/03_report_00.md
```

Do not stage, commit, or push Meta. Do not create a handoff file.

---

## 2. Communication and human-governance

```text
Worker prompt language: professional English
Worker report language: professional English
Direct-user Slovak presentation: Orchestrator-owned; do not emit the Cooperator capsule
Report header: ### Report for ORCHESTRATOR_CHAT
Deterministic steps inside bounded authority: chrome + settings move + handshake copy + Attach placement + tests + local commit
Human decision points remaining: live Brave/X look, whether Cardano is a meme (out of this slice), R3, publication, NUC
```

Do not ask Michal for a whole-product PASS/FAIL checklist.

---

## 3. Capability handshake

Fresh session. Record requested vs directly observed vs inferred vs unknown for
client, Extra High, Native planning mode `not-used`, filesystem, tests, local
commit, and that push/NUC/provider/signed-in X/AP mutation remain unused.
Capability does not grant authority. Do not probe credentials.

```text
Repeated-gate or reasoning-loop stop: configured
Broad gate: once per materially changed candidate
Narrow before re-broad: required
Unchanged hypothesis, candidate, and failing gate: not-progress
Escalate only on: named missing evidence
Downgrade after: convergence
Cost cannot falsify evidence: yes
```

```text
Validation ladder: selected
Inspection and provenance: required
Existing focused tests: tests/x_companion_extension.test.js tests/companion_web_bridge.test.js tests/catalog_card_ai_quick_action.test.js
Affected tests: those same files
New causal regression: no “could not be framed” when iframe loaded; picker has no Settings modal; connected shell hides origin input; hosted cards keep open-original and add top-left 📎; ordinary tabs have no 📎
Broad or full suite: not-used
Runtime or testbed: declared AP envelope + node --test
Independent acceptance: not-required
```

```text
Development envelope activation: activated
Development envelope identity: FrameNest canonical checkout on feat/x-meme-browser-companion
Declared reversible class: reversible local source, tests, docs, and local Git commits
Working-copy topology: canonical-checkout
Topology rationale: correction of the unpublished Worker 02 candidate
Irreversible exclusions: secrets, destruction, accounts, public exposure, unrelated owner data, publication, closure, NUC, push, signed-in X, provider calls
```

---

## 4. Canonical repositories and immutable baseline

```text
Expected canonical root: /home/agile/Projects/framenest
Expected branch: feat/x-meme-browser-companion
Exact baseline: 91283a70fcee039dd20f43bae5bf90e5901f01e8
.ap gitlink: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Working tree: expected clean
Do not create a worktree
```

If HEAD is not `91283a7`, stop `BLOCKED` unless the only difference is this
Worker’s own later commits. Do not `git fetch`. If public `main` advanced past
`bfad16b`, inspect read-only and stop only on material conflict.

Save freeze: do not edit `extension/ui/save.html|css|js`. Attach float: do not
edit positioning in `x_adapter.js`.

---

## 5. Required reading

- `AGENTS.md`, `.ap/AP.md`, `.ap/AP_WORKER.md`, `.ap/PROMPT_CONTRACTS.md`
- `docs/WORKER_EXECUTION_CONTRACT.md`
- `extension/ui/sidebar.html|js|css`, `extension/ui/picker.html|js|css`
- `src/framenest/adapters/api/web/app.js` (`renderCatalogCard`,
  `companionWebHosted`)
- `src/framenest/adapters/api/web/companion_host.js`
- `docs/X_COMPANION.md`
- Do not edit ADR-0061, ADR-0062, or ADR-0063 in place. Placement of 📎 is UX,
  not a new trust surface.

Park `docs/AP_UPGRADE_OBSERVATIONS.md`.

---

## 6. Canonical execution routes

No ambient Python. After the first authorized commit, `--baseline` is that SHA.

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <EXACT_AUTHORIZED_COMMIT>
./.ap/ap exec --root /home/agile/Projects/framenest --baseline <EXACT_AUTHORIZED_COMMIT> --operation runtime-info
```

Python tests are not required unless you touch a Python owner (you should not).
JavaScript:

```text
node --test tests/x_companion_extension.test.js tests/companion_web_bridge.test.js tests/catalog_card_ai_quick_action.test.js
```

NUC gate is named only to close route resolution. **Not activated.**

---

## 7. Negative authority

You must not:

- push, fetch, switch branch, merge, rebase, stash, reset, clean, amend, tag,
  or update submodules;
- deploy, write NUC origins, or run `framenest-release`;
- edit Save popup files; edit Attach float positioning;
- expand `GET /api/x/companion/media` / picker audience to all Gallery items;
- add CORS, `all_urls`, `externally_connectable`, or `companion_mutation`;
- thaw ordinary-tab Gallery CSS globally;
- remove open-original in extension-hosted web (Cooperator reversed replace);
- implement alias editor, language, or Analyze execution;
- claim independent acceptance or close the whole.

---

## 8. Binding Cooperator / Orchestrator decisions

### 8.1 Handshake copy (why the red sentence is wrong)

Live `sidebar.js` starts a 10s timer in `hostFrame`. If no `WEB_READY`, it
calls `showFramingError()` → `FrameNest could not be framed in this panel.`

Cooperator screenshot: that sentence is visible **and** the FrameNest Gallery
(search, cards, “YouTube FN downloads”) is visible in the same panel. Framing
**succeeded**. Handshake **failed** because the iframe document is NUC
`bfad16b`, which does not ship `companion_host.js`, so it never posts
`WEB_READY`. Then `companionWebHosted()` stays false and 📎 never renders.

Replace the copy. Distinguish:

- **Iframe failed to load** (`error` on the iframe, or it stays blank): honest
  framing failure. Still no new tab.
- **Iframe loaded but no `WEB_READY`:** do **not** say it could not be framed.
  Say that this FrameNest server cannot host companion Attach yet (older web
  without the companion host). Keep the iframe visible. Do not hide Gallery.

Do not add `X-Frame-Options` / `frame-ancestors`. Do not deploy NUC to “fix”
📎 in this exchange.

### 8.2 Side-panel chrome

Replace the always-visible origin form with:

1. A compact **green title bar** (OS-window chrome, FrameNest `#00ff41` /
   `#39ff14`). Black text wordmark `FrameNest` on the left.
2. A Settings control in that bar (icon is fine). Settings UI is a side-panel
   dialog/sheet — not the in-page picker modal.
3. Far right of the green bar: one solid green button. Disconnected: **Connect**
   (uses existing `CONFIGURE_ORIGIN` after reading origin from Settings if the
   header has no origin field). Connected: the same button reads **Disconnect**
   and runs existing `RESET`. Do not show a second Reset in the title bar.
4. Origin label + input are **not** on the main chrome. They live in Settings
   so first-run still works.
5. Status line may remain, but must not use the false framing sentence.

Below the title bar: the iframe is the real web, full remaining height.

### 8.3 Settings leave the quick-attach popup

Remove the hamburger and Settings `<dialog>` from `ui/picker.html|js|css`.
If origin is empty, picker status is `Connect FrameNest in the side panel`
(or equivalent professional English). Keep Search memes, kind filter, JPEG
preview, arrows, Attach. Do not put the full website in the popup.

### 8.4 Picker `Cardano` / “No eligible memes”

Picker remains `GET /api/x/companion/media` (meme category, published **or**
caller’s own live X media). Gallery lists the whole catalog. **Do not** change
that audience in this correction. Report it as parked residual, not as a
search bug to “fix” by widening the API.

### 8.5 Hosted Attach placement

Cooperator reversed “replace open-original”. In `renderCatalogCard`, when
`companionWebHosted()` and a supported location exists:

- keep `<a class="catalog-card__action--open-original">` bottom-right;
- add 📎 `<button>` **top-left** (`--top-left` + `--attach`), title/aria-label
  `Attach to X composer`;
- admin pencil stays bottom-left; 🧠 stays top-right. Do not collide 📎 with
  🧠 (top-left vs top-right).

Ordinary tabs: no 📎. `downloadIcon()` stays unused. No global Gallery restyle
beyond `--top-left` / `--attach` tokens copied from existing overlay actions.

Update `tests/catalog_card_ai_quick_action.test.js` accordingly.

---

## 9. Changed-path allowlist

```text
extension/ui/sidebar.html
extension/ui/sidebar.js
extension/ui/sidebar.css
extension/ui/picker.html
extension/ui/picker.js
extension/ui/picker.css
src/framenest/adapters/api/web/app.js
src/framenest/adapters/api/web/styles.css
tests/x_companion_extension.test.js
tests/companion_web_bridge.test.js
tests/catalog_card_ai_quick_action.test.js
docs/X_COMPANION.md
```

`app.js` / `styles.css`: only `renderCatalogCard` overlay classes and the new
`--top-left` / `--attach` rules. Picker: remove Settings, keep preview.

Forbidden: Save files, ADR-0061/0062/0063 bodies, `tailscale_ingress.py`,
`companion_picker.py`, `manifest.json` unless a test forces a one-line proof
(prefer not), `.ap/`, Meta except the report, `private/`.

---

## 10. Git authority

```text
Fetch: forbidden
Branch: feat/x-meme-browser-companion
Stage: allowlisted paths only; never git add -A
Commit: one or two local commits after gates
Amend: forbidden
Push: forbidden
```

Suggested subjects:

```text
fix: tell the truth when companion web handshake is missing
feat: give the companion side panel an OS-like FrameNest chrome
```

Fewer commits are allowed. Do not commit Meta or secrets.

---

## 11. Untrusted-content / surfaces

```text
Browser authority: none
Provider: not-used
NUC / publication / deployment: not-used
```

Do not log origins beyond already-stored chrome.storage patterns. Do not print
the private key. Do not Reload Michal’s Brave.

---

## 12. Completion, report, and expiry

Write the terminal report to the exact Meta path in Section 1.

Echo once:

```text
Logical whole identity: framenest-x-companion-sidebar-web-mvp
Worker session ordinal: 03
Worker exchange ordinal: 01
```

Include: status; `Phase-qualified result: implementation-PASS` or
`not-applicable`; `Logical-whole closure: not-closed`; handshake;
baseline/final HEAD; changed paths; proof Save/ADR-0061/0062/0063-in-place
untouched; proof picker has no Settings dialog; proof hosted path keeps
open-original and adds top-left 📎; proof ordinary path has no 📎; commands
and exit codes; residual that NUC `bfad16b` still cannot show 📎 until a later
deploy; `new-mutation`; authority expiry; smallest next step = Michal Reload
unpacked of the shell chrome.

PASS requires the chrome/settings/copy/placement gates and tests green, not a
live NUC 📎.

---

## 13. Final stop rule

After the report, stop. Do not deploy, push, or start R3.

Slovak capsule data for Orchestrator presentation (not Worker instructions):

```text
🆕 PROMPT PRE FRESH WORKERA 03 • EXTRA HIGH 🧠🧠🧠
Native Plan Mode musí byť vypnutý.
▶️ Otvor nový Worker chat, vypni Plan Mode, vlož tento súbor, počkaj na 03_report_00.md.
📦 Prompt: /home/agile/meta/projects/framenest/03/05-framenest-x-companion-sidebar-web-mvp/03_correction_00.md
📦 Report: /home/agile/meta/projects/framenest/03/05-framenest-x-companion-sidebar-web-mvp/03_report_00.md
✅ Archival: wait-for-report
```
