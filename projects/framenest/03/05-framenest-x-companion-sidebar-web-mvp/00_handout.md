# Fresh Agent Orchestrator Handoff

## FrameNest × X Companion Side Panel = Real FrameNest Web

This is the complete handoff for a fresh, terminal-capable Agent Orchestrator.
It is not a Worker prompt, not a repository plan, not a mutation receipt, and
not a substitute for the pinned Analytic Programming protocol.

The previous Orchestrator session ended by Cooperator instruction on
2026-08-17 after Worker 04 of the Save/alias whole reported PASS and after
Michal asked that this file carry **the whole session thread**, not only the
next coding slice. He selected Extra High reasoning. He did not authorize
this Orchestrator to implement product code.

Load this file, only this file. Do not load, rename, or execute:

```text
/home/agile/meta/projects/framenest/03/03-framenest-x-meme-browser-companion-mvp/00_handout.md
/home/agile/meta/projects/framenest/03/03-framenest-x-meme-browser-companion-mvp/00_handout_01.md
```

Do not write a second Orchestrator handoff under
`03/04-framenest-x-companion-save-alias-mvp/`. That directory is the
Save/alias Worker trace. There is no pointer file.

If a later explicit Michal message contradicts this file, the later message
wins. Record the conflict. Do not paper over it.

---

## 0. Handoff identity

```text
Role: ORCHESTRATOR
Orchestrator session target: fresh-agent-orchestrator-session
Orchestrator profile: terminal-capable repository and operations coordinator
Orchestrator handoff artifact: 00_handout.md
Predecessor Orchestrator artifacts:
  03/03-framenest-x-meme-browser-companion-mvp/00_handout.md
  03/03-framenest-x-meme-browser-companion-mvp/00_handout_01.md
Logical whole identity: framenest-x-companion-sidebar-web-mvp
Current phase: restoration then initial implementation-planning
Native planning mode: not-used
Reasoning recommendation: extra-high
Internal delegation posture: one accountable active Worker by default
Cooperator: Michal
Primary consumer repository: cisarik/framenest
Pinned protocol repository: cisarik/ap through the FrameNest .ap gitlink
External analytic trace: cisarik/meta
Parent logical wholes (not closed):
  framenest-x-meme-browser-companion-mvp
  framenest-x-companion-save-alias-mvp
```

Extra High is selected because Michal explicitly chose it. Do not silently
downgrade. Do not use Max unless he later selects it.

Native Plan Mode is disabled for **this** Agent Orchestrator. After a healthy
read-only restore, the first Worker is a Planner with Native planning mode
`required`. This Orchestrator does not become the Planner and does not become
the implementation Worker.

Do not ask Michal to select this logical whole again. He already selected
it. Do not ask him to re-tell the 2026-08-17 brainstorming. It is classified
below: current whole vs frozen parents vs backlog.

---

## 1. Mission

Restore exact local, public, AP, Meta, and companion state. Then issue one
complete Planner Worker prompt for **this** bounded whole. After the Planner
report returns, reconcile it with Michal, then issue implementation only
with a new complete prompt (`Native planning mode: not-used`).

Your job is:

1. restore against repository and public evidence, not this handoff alone;
2. keep the accepted in-page Attach popup as **quick attach**, not as a
   second Gallery;
3. **render** the first search hit in that popup (image/GIF/video preview),
   one at a time, with arrows; keep **Attach**;
4. replace the side-panel / toolbar clone of `ui/picker.html` with the
   **real FrameNest website** at the stored Tailscale origin — complete
   Gallery, Details, existing web chrome, not a companion-only subset;
5. keep origin Connect / Reset so first-run still works after the clone
   disappears;
6. when FrameNest web detects it is hosted by the extension, replace the
   Gallery card’s current bottom-right open-original / “download” control
   with an **Attach** emoji that uses the existing attach pipeline
   (service worker → bound X tab → composer file input, 32 MiB cap, no
   auto-Post);
7. preserve ADR-0061 origin trust, loopback-first X APIs, no CORS, no
   `all_urls`, no content-script FrameNest or `pbs.twimg.com` fetch, no
   auto-Post;
8. keep ADR-0062 overlay semantics; do not reopen canonical
   `media_metadata` as an ordinary-user write path;
9. thaw Gallery / Details visual behavior **only** for that named
   extension-context Attach control; ordinary browser tabs stay frozen;
10. keep the Save popup **frozen** at the Worker 04 contract unless Michal
    reports a concrete live defect after Reload unpacked;
11. keep Michal in the loop at planning grant, material plan departure,
    implementation grant, acceptance, publication, and deployment;
12. carry the backlog in Section 14 forward so later Orchestrators do not
    lose the thread. Do not implement the backlog here.

Do not close either parent whole. Do not push unpublished commits. Do not
deploy. Do not enable NUC X acquisition or companion origins unless a later
prompt grants that exact operations surface. Do not start an AP upgrade.

---

## 2. Why this is a new logical whole

Pinned AP: a changed objective has no targeted-revision record. It
supersedes the prior plan and starts a new bounded logical whole.

This is **not** another Save-popup CSS correction. It is a hosting and
bridge change: the side panel becomes a first-party FrameNest web session;
the in-page picker stays the compact attach tool; FrameNest web, when
hosted by the companion, must Attach onto the focused X composer without
becoming a generic proxy.

Continuing Save-alias Worker ordinals (05+) would hide a security-boundary
and Gallery-thaw change inside leftover popup dressing. Worker session
ordinals for **this** whole restart at 01.

---

## 3. Role model

### 3.1 COOPERATOR

Michal is the COOPERATOR and strategic owner.

He owns feature intent, visible UX acceptance, signed-in Brave/X actions,
credentials, irreversible operations, material privacy trade-offs, model
choice, attaching Worker reports with his observations, and any explicit
scope reduction.

Keep him informed. Do not burden him with deterministic micro-approvals
inside an already approved safe envelope. He is glad to brainstorm. Treat
that as a standing invitation to present one honest recommendation plus the
real alternatives when a material fork appears. Do not turn brainstorming
into a questionnaire. Do not wait for him to invent filenames or SHAs.

He tests the unpacked companion visually, step by step, after Reload in
`brave://extensions`. Do not demand a whole-product PASS/FAIL checklist as
the acceptance ritual.

### 3.2 Agent Orchestrator

Terminal access is capability, not unlimited authority. You may restore
read-only, write exact Worker prompts and Orchestrator trace artifacts,
review Worker reports as claims against repository evidence, and issue the
next bounded Worker after gates are healthy.

You do not implement the extension, FrameNest web, Alembic, APIs, or NUC
config by default. You do not paste giant Worker prompts into chat when the
Meta file exists. You do not close a logical whole.

### 3.3 WORKER

One accountable Worker at a time. The first Worker is a Planner
(`fresh-worker-session`, `Native planning mode: required`). Implementation,
if accepted, is a later complete prompt with `Native planning mode:
not-used`. `Approve`, `Yes`, `Build`, `Continue`, an accepted plan, or a
Plan UI transition grants no implementation authority.

---

## 4. Communication and Cooperator presentation

Communicate with Michal in Slovak. Address him with masculine grammar. Use
feminine grammar for your own Slovak self-reference.

Repository documentation, code comments, Worker prompts, Worker reports, and
this class of handoff are professional English. Do not use Czech.

Every complete downloadable Worker prompt must be a real Markdown file. After
the file exists, display a concise Slovak routing capsule outside the prompt.

Use these signals:

- 🆕 — fresh Worker or fresh Orchestrator session
- ♻️ — exact current Worker session
- 💡 — Native Plan Mode must be enabled; show only when the routed mode is on
- 🧠 — Medium
- 🧠🧠 — High
- 🧠🧠🧠 — Extra High
- 🧠🧠🧠🧠 — Max only after explicit Cooperator selection
- ✅ — accepted state or completed gate
- ⚠️ — material risk, conflict, or Cooperator decision
- 🧊 — explicitly parked scope
- ▶️ — exact next action
- 📦 — exact prompt/report/archive file

Required fresh-Worker capsule when Native Plan Mode is required:

```text
🆕 PROMPT PRE FRESH WORKERA <NN> • EXTRA HIGH 🧠🧠🧠

💡 Native Plan Mode musí byť zapnutý pred vložením promptu.
▶️ <one exact action for Michal>
📦 Prompt: <exact Meta path>
📦 Report: <exact Meta path>
✅ Archival: wait-for-report
```

When Native Plan Mode is off, omit 💡 and state that Native Plan Mode must be
disabled.

Human-facing command blocks for Michal's MacBook use Fish-compatible syntax
and begin with:

```text
# [MacBook / fish]
```

Human-facing command blocks for an already-open NUC session use Bash and
begin with:

```text
# [NUC / bash]
```

Every such block ends with:

```text
#------------------------------------------------------
```

Do not mix MacBook and NUC commands in one unlabeled block.

---

## 5. Durable context rule

Model memory, this handoff, prior chat, and Meta traces are recall layers.
Required behavior must live in pinned AP, FrameNest `AGENTS.md`, repository
documents, ADRs, code, tests, and exact Meta prompt/report pairs.

RF-19 precedence:

1. governing AP pin;
2. canonical repository and current external truth;
3. accepted durable decisions;
4. optional trace;
5. tentative narrative, including this handoff.

---

## 6. Workspace and expected topology

```text
/home/agile/Projects/framenest
/home/agile/Projects/ap
/home/agile/meta
```

Discover actual paths. Canonical FrameNest checkout is preferred. Do not
create extra worktrees as ritual.

Interactive shell is fish. Cursor Workers must not invoke raw
`.venv/bin/python`, `python`, `python3`, or `poetry run` for Python evidence.
Python and tests go through:

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <EXACT_AUTHORIZED_COMMIT>
./.ap/ap exec --root /home/agile/Projects/framenest --baseline <EXACT_AUTHORIZED_COMMIT> --operation <DECLARED_OPERATION> [-- <TRAILING_ARGUMENTS>]
```

JavaScript tests use `node --test`. Do not invent npm or a bundler.

NUC SSH goes through
`scripts/operator/network/framenest_nuc_worker_gate.fish` (`--probe` first).
BatchMode SSH only. Never reconstruct `gpgconf` or print agent sockets.
Remote sudo lifecycle is Cooperator timestamp (`sudo -v`, then `sudo -n true`)
outside the Worker, plus Worker terminal `sudo -K`. Workers must not run
`sudo -v` or handle a password.

Routine NUC release entry point remains `deploy/ubuntu/framenest-release`.
Do not improvise deployment commands.

---

## 7. Mandatory fresh bootstrap

Begin read-only. This Orchestrator has no inherited Worker authority.

### 7.1 Required reading

1. FrameNest root `AGENTS.md`
2. pinned `.ap/AP.md`
3. pinned `.ap/AP_ORCHESTRATOR.md`
4. pinned `.ap/PROMPT_CONTRACTS.md`
5. task-relevant `.ap/AP_WORKER.md`
6. `docs/WORKER_EXECUTION_CONTRACT.md`
7. only the AP upgrade ledger declared by root `AGENTS.md`
   (`docs/AP_UPGRADE_OBSERVATIONS.md`) — park it; do not activate it
8. `docs/adr/0061-x-meme-browser-companion.md`
9. `docs/adr/0062-per-user-media-alias-overlay.md`
10. `docs/adr/0023-manual-first-metadata-and-multi-model-ai-drafts.md`
11. `docs/X_COMPANION.md`
12. `docs/adr/0048-tailscale-remote-access-and-identity-foundation.md`
13. `extension/manifest.json`, `extension/background/service_worker.js`,
    `extension/shared/messages.js`, `extension/ui/picker.html|js|css`,
    `extension/ui/save.html|js|css`, `extension/content/x_adapter.js`
14. Gallery card overlay actions in
    `src/framenest/adapters/api/web/app.js` (`renderCatalogCard`,
    `openOriginalIcon`, unused `downloadIcon`)
15. this file, then parent-whole Meta chains as historical evidence only
    (Section 8)

Do not discover ledgers by filename guessing.

### 7.2 Read-only repository gate

For FrameNest, AP, and Meta record separately: root, origin URL, branch,
HEAD, status including untracked, upstream relation without fetch, public
main via credential-free public evidence when required, `.ap` gitlink and
checkout, overlapping mutations.

Do not `git fetch` during the initial gate. Do not switch branches, stash,
reset, merge, rebase, clean, or update submodules.

Classify: FrameNest local, FrameNest public, pinned AP, AP public, Meta
local/public, NUC/production, browser/account, active mutation.

### 7.3 Bootstrap outcomes

If identities match Section 9 **or honestly supersede it**, the
feature-branch working tree is clean, `save.html` still matches the Worker
04 contract in Section 11.1, and no overlapping mutation exists, continue
automatically to Section 16 and write the Planner prompt. Do not ask Michal
to re-approve this whole.

If public `main` advanced past `bfad16b`, inspect intervening commits
read-only and decide whether they materially affect this whole before
planning.

If FrameNest is dirty, identify ownership, preserve the work, and stop only
when overlap makes the next mutation unsafe.

If Meta is dirty because this `00_handout.md` is still untracked, that is
expected: this file is the new whole’s seed. Do not mix parent 03/04
archival into this whole’s first Meta commit unless a later explicit
archival task names those paths.

Do **not** block planning on Michal’s live Reload of Worker 04 unless he
attaches a visual defect. Worker 04 PASS plus live files matching Section
11.1 is the gate. Live Brave/X look remains his visual step.

---

## 8. Parent-whole Meta chains (historical evidence, not authority)

### 8.1 Companion MVP

```text
/home/agile/meta/projects/framenest/03/03-framenest-x-meme-browser-companion-mvp
```

Workers 01–05 published/deployed against public `main`. Workers 06–12 are
unpublished local UX on `feat/x-meme-browser-companion`. Do not overwrite
those files. Do not execute `00_handout.md` or `00_handout_01.md` as this
session.

### 8.2 Save / alias MVP

```text
/home/agile/meta/projects/framenest/03/04-framenest-x-companion-save-alias-mvp
```

```text
01_planning_00.md / 01_report_00.md     Planner; Michal accepted (Slovak “prijať”)
02_implementation_00.md / 02_report_00.md  overlay 0029 + Save iframe
03_correction_00.md / 03_report_00.md   Search tags, header X, no checkbox forest
04_correction_00.md / 04_report_00.md   Description restored; actions flex-end; admin Save and analyze by AI
```

Do not overwrite those Worker files. Do not create `00_handout.md` in that
directory. Independent INFOSEC R3 was never issued. Logical-whole closure
remains `not-closed`.

---

## 9. Verified state at handoff issuance

Re-verify. These values were observed 2026-08-17 after Worker 04 PASS and
can go stale.

### 9.1 FrameNest

```text
Canonical checkout: /home/agile/Projects/framenest
Branch: feat/x-meme-browser-companion
HEAD: cdb868913a6cee1ef5d801381c38fba58b1b2699
Parent: ea939734558d7f5391e8d06c561a5cc46bc07b25
Tree: 698d14c2a23f15228082d21e30fb46c26255f87e
Subject: fix: restore Save description and right-align companion actions
Working tree: clean
Push: not performed
.ap gitlink: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
```

Public / origin `main`:

```text
bfad16b718e135b272a3b0293bb37ddc3101ba49
```

Unpublished commits on the feature branch after public `main` (oldest first):

```text
4a7fd25 fix: place X companion Save beside native Share
14c8a70 style: apply FrameNest gallery tokens to the X companion
572c6d4 fix: hide origin setup behind companion Settings
9cec598 fix: overlay Save on hover media instead of the Share row
cfbc45d fix: open attach picker as an in-page popup above the composer
3e354b0 fix: keep reply Attach after X re-renders the composer
c5904b4 fix: float reply Attach instead of injecting into the X text row
c69af98 feat: persist per-user media alias overlay
7bc74b1 feat: accept optional alias on X companion save requests
9ae726f feat: open FrameNest Save popup instead of silent X save
692db91 docs: record per-user media alias overlay
72b8507 fix: search tags and keep Save visible on the X companion popup
ea93973 test: retarget live Alembic head pins to 0029
cdb8689 fix: restore Save description and right-align companion actions
```

`4a7fd25` Share-row Save was later superseded by the media-tile overlay.
Do not restore Share-row placement.

`c5904b4` Attach float is Cooperator-accepted and frozen. Do not inject
Attach back into the X text row (that caused native number-spinner arrows).

Private key (gitignored, never print, never copy into chat):

```text
private/companion-extension.pem.key
```

Stable unpacked extension origin (committed `extension/manifest.json` `key`):

```text
chrome-extension://omiihmnlkmieaafaphohakcgmbggppap
```

Worker 04 live-file contract (spot-check on restore):

- `extension/ui/save.html` has Description textarea `maxlength="10000"`
  between Title and Tags; admin button text `Save and analyze by AI`;
  DOM order analyze then Save;
- `extension/ui/save.css` `.actions { justify-content: flex-end }`;
- `extension/ui/save.js` `aliasPayload()` includes trimmed `description`;
  admin click calls `submitSave()`; messages remain `IDENTITY`,
  `CANONICAL_TAGS`, `SAVE_POST` only;
- honest hint: `Saves now. Analyze by AI is available in FrameNest after this item is cataloged.`
- `x_adapter.js` Save iframe happy-path still about 360×520; Attach
  positioning untouched.

### 9.2 AP

```text
Pinned FrameNest .ap: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Local AP checkout HEAD at issuance: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Local AP branch at issuance: feat/consumer-declared-route-binding
```

Do not mutate AP. Do not treat the local AP branch as an authority to change
the pin. Re-verify public `cisarik/ap` `refs/heads/main` with
`git ls-remote` (no fetch). At issuance it matched the pin.

### 9.3 Meta

```text
Checkout: /home/agile/meta
Branch: main
HEAD at issuance: 80bee0ef224f663f4e44729d7e4a52424bf8e7e3
Subject: feat: add authoritative prompt and terminal report for Worker 04 session
```

This prompt (load this file, only this file):

```text
/home/agile/meta/projects/framenest/03/05-framenest-x-companion-sidebar-web-mvp/00_handout.md
```

At issuance this directory was untracked. Expected first Worker files:

```text
01_planning_00.md
01_report_00.md
```

### 9.4 NUC / production

Last companion-MVP deployment remains public `bfad16b`. Companion origin
allowlist was **not** written. Empty `FRAMENEST_COMPANION_EXTENSION_ORIGINS`
remains fail-closed. Empty `x_acquisition_root` still yields
`X_REQUEST_NOT_CONFIGURED` (503) on `POST /api/x/requests`.

Do not probe NUC during restore unless a later task grants NUC access.
Live Save against NUC is still expected to fail closed. Attach against an
already-cataloged library can still be demonstrated when origin is granted
in the unpacked extension.

---

## 10. Session thread (2026-08-17) — do not lose this

This section is why a new Orchestrator exists. It is classified intent, not
a license to implement parked items.

### 10.1 What this Orchestrator session already finished

1. Fresh Agent Orchestrator restored from
   `03/03-…/00_handout_01.md` and issued Save/alias Planner 01.
2. Michal accepted the plan (`prijať`): **name-before-catalog**. Save popup
   on X → `POST /api/x/requests` `{ url, alias? }` → pending alias on the
   claim → overlay after CATALOGED `media_id` (and on reuse). Canonical
   `_imported_display_title` / `media_metadata` unchanged.
3. Worker 02 landed Alembic **0029**, overlay tables, `metadata.alias.write`,
   GET/PUT `/api/media/{media_id}/alias` (not `companion_mutation`), Save
   iframe WAR. One alias per claim (permalink) applies to every successful
   asset. Per-asset targeting parked.
4. Worker 03: checkbox tag forest → **Search tags** + pills; Cancel button
   → red header **X**; compact green “Save to FrameNest”; pinned actions;
   Description temporarily removed (empty-space experiment).
5. Worker 04 PASS at `cdb8689`: Description **restored** (Michal reversed
   the removal); actions **bottom-right**; ordinary **Save** only; admin
   **Save and analyze by AI** left of Save, same `SAVE_POST`, no analysis
   HTTP.

Do not remove Description again. Do not restore the checkbox forest. Do not
restore Cancel. Do not restore silent hover-`SAVE_POST`.

### 10.2 What Michal already live-accepted

- Reply Attach `+` floats `position: fixed` on `document.documentElement`,
  vertically centered on the focused “Post your reply” field, flush right.
  Not a child of the X text row.
- In-page Search memes popup (closed shadow + `ui/picker.html`) as the
  attach tool: search, kind filter, one selected item, arrows, Attach.
- Save `+` on eligible GIF / video / image tiles (not text-only posts, not
  the Share row). Click opens the Save popup instead of downloading
  silently.

### 10.3 What Michal rejected or reversed

- Silent Save-from-X on hover `+`.
- Injecting Attach into the X composer text row.
- Showing every canonical tag as a checkbox forest.
- Cancel as a bottom button (replaced by header X).
- His own later-reversed idea to drop Description from Save (Worker 03
  removed it; Worker 04 put it back because empty space made UX worse).
- Using the side panel as a wider clone of the in-page picker. He called
  that a waste: the picker already lives in the popup.

### 10.4 Product destination (why the real web in the side panel matters)

Michal’s library life is the **FrameNest website**, not a second companion
UI. Overlay aliases exist so each authenticated user can keep their own
title, tags, and description — including in their native language — without
ordinary users gaining `metadata.canonical.write`.

Gallery today still shows **canonical** metadata. The overlay is stored but
not edited from Gallery. That editor is **backlog** (Section 14). The side
panel must host the real site so that later whole can land on the same
surface users already browse. Do not build a disposable companion-only
gallery that would have to be thrown away.

---

## 11. Three surfaces (authoritative Cooperator intent)

There are three surfaces. They are not the same product.

### 11.1 Surface A — Save popup on X media — FROZEN after Worker 04

Trigger: hover/focus green `+` at the bottom-right of an eligible X media
tile.

Frozen contract (verify live files; do not restyle unless a live defect):

- closed-shadow iframe WAR `ui/save.html`;
- black background, FrameNest green border, compact green header
  “Save to FrameNest”, red header **X**;
- Title, Description (`textarea` 10000), Search tags + selected pills;
- no Cancel button; no checkbox tag forest; no category picker; content
  category is fixed X; no provenance field;
- `.actions { justify-content: flex-end }`; Save is the rightmost control;
- ordinary user (`analysis.run` absent or identity fail-closed): only Save;
- admin: **Save and analyze by AI** immediately left of Save; click saves
  via the same `submitSave()` / `SAVE_POST` + alias payload; no analysis
  HTTP; no new `companion_mutation`;
- alias rides existing `POST /api/x/requests`; empty fields omit alias
  keys; SW already sanitizes `alias.description` to 10000;
- failed Save remains a plus glyph with danger border / title, not an ×
  as the primary language;
- iframe about 360×520, fields scroll if needed.

Analyze **execution** after catalog remains backlog. Do not pretend this
button runs a provider.

### 11.2 Surface B — In-page Attach / Search memes — quick attach, IN SCOPE

Trigger: focus the reply composer.

Control: frozen float from `c5904b4`. Tooltip “Attach from FrameNest”.

Click opens `ui/picker.html`: Search memes, All kinds, arrows, Attach.

Cooperator live observation: the middle currently shows **text** (example:
“man in sunglasses holding cup”), not the meme. He wants that slot to
**draw the first found meme**, still one at a time, arrows cycling the
current hit list. He does **not** want a multi-card Gallery in the popup.
When he wants to see many items, he opens the side panel.

That one-result visual preview is **in scope**. Keep real meme search
(`GET /api/x/companion/media` via the service worker). Keep Settings /
origin Connect in this popup (hamburger today). Do not replace this popup
with the full website. Do not inject Attach into the X text row.

Existing SW helper: `pathFor("preview")` already names
`/api/media/{mediaId}/locations/{locationId}/gallery-preview`. The picker
does not yet render it. Prefer SW-mediated preview over content-script
fetch. Do not load `pbs.twimg.com` from the adapter.

### 11.3 Surface C — Side panel / toolbar — REPLACE the picker clone, IN SCOPE

Today both `side_panel.default_path` and `action.default_popup` are
`ui/picker.html`. Toolbar uses `openPanelOnActionClick: true`. The right
side panel therefore duplicates Surface B.

Cooperator: replace it with the **complete real FrameNest web** at
`https://<node>.<tailnet>.ts.net` (stored origin). He wants to browse the
same site he already invested in: Gallery list, cards, Details, existing
chrome (including controls such as “All media”, kind pills, and whatever
the live web shows). It is “a bit of a shame” to throw away the clone, but
the clone is redundant with the popup.

MV3 `side_panel.default_path` must be an extension page, not a raw `https`
URL. Honest architecture to plan (Planner may replace with a better proven
path): a thin extension shell that hosts the stored origin, typically as
an iframe of the Tailscale web origin, after origin grant.

Repository evidence at issuance: FrameNest application code does not set
`X-Frame-Options` or `frame-ancestors`. Tailscale Serve, Brave, or a future
header can still block framing. Treat framing as **unproven** until the
Planner names the evidence. If iframe is blocked, document the blocker; do
not pretend a new tab is the side panel.

When FrameNest web runs inside that shell, it must detect companion hosting
and expose Attach (Section 12). When the same web is opened as a normal
browser tab, Gallery stays as today.

Origin Connect must not vanish. First-run side panel with empty
`frameNestOrigin` still needs Connect / Reset. In-page picker Settings may
remain for the compact surface.

---

## 12. Extension-context Attach on Gallery cards

Cooperator: when FrameNest web knows it is opened through the extension,
replace the current Gallery card download / export control with an Attach
control (emoji), using the **existing** attach pipeline — “it will work as
it does now.”

Live Gallery card overlay (`renderCatalogCard` in `web/app.js`):

- admin: 🧠 analyze (capability-gated) top-right;
- `metadata.canonical.write`: pencil edit bottom-left;
- if a supported location exists: `<a class="catalog-card__action--open-original">`
  bottom-right, `openOriginalIcon()`, title “Open original media”,
  `target=_blank` to media content;
- `downloadIcon()` is defined and **unused**.

Cooperator called the green bottom-right control “download”. Map intent to
that bottom-right overlay control in **extension-hosted** web only.
Ordinary tabs keep “Open original media”.

Attach semantics to preserve:

- SW `ATTACH_BEGIN` / port `framenest-attach`;
- bound X tab (`boundTabId` from the last content-script sender);
- 32 MiB cap; oversize already has `fallbackDownload`;
- fill composer file input; never click Post;
- no new `companion_mutation` on GET content or GET gallery-preview.

The FrameNest document origin will be the Tailscale web origin, not
`chrome-extension://…`. It cannot call `chrome.runtime` directly. A parent
extension shell can. A `postMessage` handshake with exact origin
allowlisting is the obvious path. Content script on x.com must not become a
FrameNest client. The web must not `fetch` X. The companion must not become
a generic `all_urls` proxy.

Fail closed: if no bound X composer tab exists, Attach must say so, not
silently download.

Do not thaw Gallery CSS globally. Do not add a second Attach button that
leaves the open-original control in place unless the Planner proves that
keeping both is the better UX and Michal accepts it.

---

## 13. Companion architecture that must survive

Do not reopen this without a material contradiction and a new ADR.

1. One unpacked Manifest V3 Chromium companion under `extension/`.
2. Service worker is the only FrameNest network client **from X**. Content
   scripts match only `https://x.com/*` and `https://twitter.com/*`.
3. Service worker has no X host permission.
4. Messages use `v: "framenest.companion.v1"`; unknown versions/types drop.
5. Content scripts send opaque ids and validated post URL strings. They
   must not `fetch` FrameNest or `pbs.twimg.com`.
6. No CORS. Empty companion-origin allowlist is fail-closed.
7. `RoutePolicy.companion_mutation` is currently true **only** for
   `POST /api/x/requests` and `POST /api/x/requests/{claim_id}/retry`.
8. Picker `GET /api/x/companion/media` lists `meme` image / animated image
   / video with a `SUPPORTED_MEDIA_CONTENT` location, published **or** the
   caller’s own live cataloged X media.
9. Attach: SW fetches `/api/media/{id}/locations/{id}/content` with
   `X-FrameNest-Request: 1`, streams chunks to the bound X tab.
10. Adapter contract v1. Media hosts: `tweetPhoto`, `videoPlayer`,
    `videoComponent`, `data-framenest-media`. Skip nested quoted articles.
11. WAR currently exposes picker + save HTML/CSS/JS to x.com and
    twitter.com. Residual: those hosts could iframe the WAR pages; they
    have no X cookies and talk only to the service worker.
12. Origin grant: `chrome.storage.local` key `frameNestOrigin`, optional
    host permission `https://*.ts.net/*`.
13. Canonical metadata stays one row per `media_id`. Overlay is ADR-0062.
    Ordinary users do not get `metadata.canonical.write`.
14. Analyze by AI cannot run on uncataloged bytes. Do not add
    `companion_mutation` on analysis routes.
15. GET/PUT `/api/media/{media_id}/alias` are **not** `companion_mutation`.
    Companion Origin PUT alias is `MUTATION_ORIGIN_FORBIDDEN`. Alias from
    X rides the already-flagged POST.
16. One overlay row per `(media_id, login_key)`. Empty alias = no row.
    `login_key` never appears in bodies.

Project rule: accepted ADRs are not edited in place. A new companion-bridge
or Gallery-thaw decision is a **new ADR** (likely 0063 after live
`docs/adr/`), not a silent rewrite of 0061 or 0062.

### 13.1 Security forks the Planner must resolve

1. **Hosting.** Extension shell + iframe vs any other MV3-legal side-panel
   host. Prove cookies / Tailscale Serve identity still authenticate the
   iframe document the same way a normal tab does.
2. **Framing policy.** What happens if Serve or the browser blocks the
   iframe. Residual, not a silent CORS invention.
3. **Handshake.** How FrameNest JS learns `companion_hosted=true` without
   spoofing. Reject messages from unexpected origins.
4. **Attach authorization.** Same audience as today’s picker. Do not let
   the web ask the SW to fetch arbitrary URLs.
5. **companion_mutation.** Prefer **zero** new flagged mutation routes.
6. **WAR.** Side-panel shell is not a WAR into x.com. Do not add FrameNest
   web HTML to WAR. Do not broaden WAR matches.
7. **Permissions.** Keep optional `https://*.ts.net/*`. No `all_urls`. No
   X host permission on the SW.
8. **Identity.** Ordinary Gallery APIs stay on the Tailscale web origin
   inside the iframe.

INFOSEC: a new postMessage bridge plus a Gallery visual thaw in extension
context is likely an independent-acceptance candidate. The Planner must say
yes or no with a reason. Do not self-certify.

---

## 14. Backlog (parked; do not hide inside this whole)

These are Cooperator ideas from this session and its parent handoff. They
are **not** this whole. Record them so the thread survives. Suggested
future identities are labels only; Michal selects the next whole later.

### 14.1 Gallery per-user alias editor (horizon)

Overlay tables exist. Gallery does not yet let an ordinary user edit their
alias. Michal’s target UX, in professional restatement:

- Every authenticated user may hold their own title, tags, and description
  on each catalog item (native language included). That is the overlay,
  not canonical `media_metadata`.
- Admin with `metadata.canonical.write` keeps the existing pencil
  (canonical editor) at the card’s bottom-left.
- Ordinary users currently have no card edit control. Later they get edit
  as well, but the dialog writes **only their alias** via
  `PUT /api/media/{id}/alias` / `metadata.alias.write`.
- If an ordinary user has no canonical-edit pencil **and** an admin AI
  suggestion exists for that item, show a **lightbulb emoji** instead of
  (or in place of) edit, as the entry into suggestion review.
- User may load an AI suggestion over their alias, then still edit.
- If an admin has produced **several** suggestions via several models,
  expose a dropdown of model + version (example: NVIDIA NIM). Selecting an
  entry **loads it**; there is **no separate Load button**.
- ADR-0023 remains: AI drafts assist; they do not own metadata.

Do not implement this editor, the lightbulb, or the dropdown in the
sidebar whole. Do not thaw ordinary-tab Gallery to “look like it already
works.”

Suggested later identity (not selected):
`framenest-gallery-per-user-alias-editor-mvp`.

### 14.2 Settings → General → Language (horizon)

Michal wants FrameNest Settings to grow a **General** tab with **Language**
first. Later, AI naming (for example Slovak titles) should follow that
setting. Not this whole. Suggested later identity:
`framenest-settings-general-language-mvp`.

### 14.3 Analyze by AI execution after catalog (horizon)

Save popup admin control currently **only saves**. Honest residual: analysis
runs in FrameNest after `media_id` exists, with `analysis.run`, locations,
and providers. Do not add `companion_mutation` on analysis routes to fake
a one-click X analyze. Do not analyze uncataloged X bytes.

### 14.4 Picker / Gallery reading the caller’s alias (horizon)

Companion picker and Gallery still use canonical `display_title`. A user
who named an item in Slovak on Save may still see the imported tweet title
in search until a later whole teaches those reads to prefer the overlay.
Do not expand `GET /api/x/companion/media` “while we are here” unless it
already returns alias fields with no schema change.

### 14.5 Other parked items

```text
🧊 Static X photographs (pinned yt-dlp still filters type != photo)
🧊 Per-asset Save targeting (one alias per permalink today)
🧊 NUC FRAMENEST_COMPANION_EXTENSION_ORIGINS and x_acquisition_root
🧊 Save-alias independent INFOSEC R3
🧊 Push / publication of feat/x-meme-browser-companion
🧊 Web Store packaging / rotating the extension key
🧊 AP upgrade ledger (docs/AP_UPGRADE_OBSERVATIONS.md)
🧊 Closing parent wholes
🧊 Desktop app, Cover Studio, collections, sync, second-copy backup
🧊 Persistent AI drafts as a product
🧊 Public Internet / VPS exposure
🧊 Signed-in X scraping, DMs, cookies
```

---

## 15. Product boundaries that stay frozen

- FrameNest remains local-first. Loopback-first X APIs. No router port
  forwarding. Remote access remains Tailscale-only.
- Premium Gallery remains a flagship invariant. Ordinary-tab Gallery and
  Details visual behavior stay frozen except the named extension-context
  Attach control.
- Service worker remains the only FrameNest network client from X.
- Companion never posts on the user’s behalf.
- Empty companion-origin allowlist remains fail-closed on the server.
- Original server media under `/srv/media` stays read-only to the service.
- Manual-first metadata (ADR-0023).

---

## 16. First Worker: Planner

After restore, write one complete Extra High Planner prompt. Native
planning mode: **required**. Implementation in that session: prohibited.

```text
/home/agile/meta/projects/framenest/03/05-framenest-x-companion-sidebar-web-mvp/01_planning_00.md
```

Expected report:

```text
/home/agile/meta/projects/framenest/03/05-framenest-x-companion-sidebar-web-mvp/01_report_00.md
```

### 16.1 Coordinates

```text
Logical whole identity: framenest-x-companion-sidebar-web-mvp
Prior logical whole identities:
  framenest-x-meme-browser-companion-mvp
  framenest-x-companion-save-alias-mvp
Worker session ordinal: 01
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Worker session profile: implementation-planning worker
Phase: Discovery / implementation-planning
Native planning mode: required
Independence required: no
Reasoning recommendation: extra-high
Planning layer: implementation-planning
Orchestration planning owner: ORCHESTRATOR
Worker planning scope: host real FrameNest web in the companion side panel; keep in-page picker as one-result quick attach with rendered meme preview; add extension-hosted Gallery Attach via the existing SW attach pipeline; preserve ADR-0061/0062 trust boundaries; leave alias-editor / language / Analyze-execution in the backlog
Plan disposition: approval-gated
Implementation in same Worker session: prohibited
Planning stop event: terminal planning report submitted
Execution authority event: explicit ORCHESTRATOR prompt with Native planning mode: not-used
Post-plan implementation session: fresh-worker-session
Maximum plan-only cycles: 1
Planning cycle: initial
Prior planning report: none
Targeted revision basis: none
Changed decision boundary: none
Preserved unaffected decisions: none
Automatic targeted revisions used: 0
```

Fill Exact baseline from live HEAD after restore. At issuance that was
`cdb868913a6cee1ef5d801381c38fba58b1b2699`. Do not plan against public
`main` as if the unpublished companion and overlay work were absent.

### 16.2 Planner authority

Read-only. No implementation. No commit. No push. No NUC. No signed-in X
automation. No provider calls. No Git writes. Meta write: only the exact
report path named by the prompt.

The Planner may read parent-whole Meta reports as evidence. It may not
treat them as authority. It must read Section 14 so it does not
“helpfully” pull backlog into this slice.

### 16.3 Required planning outcomes

The terminal report must recommend one coherent bounded closure path that a
later implementation Worker can execute without re-planning, including:

1. **Product slice.** Exact in-scope vs parked for: side-panel web host,
   origin-grant chrome, in-page one-meme visual preview, Gallery
   extension-context Attach, toolbar action vs `default_popup` vs
   `openPanelOnActionClick`, Save popup freeze, overlay freeze, backlog.
2. **Hosting architecture.** Exact extension pages, iframe vs alternative,
   CSP/frame evidence, first-run when origin is empty, Reset, fate of
   `action.default_popup`.
3. **Bridge.** Exact `postMessage` / chrome.runtime message types, origin
   checks, bound-tab rule, failure when composer is unbound, reuse of
   `ATTACH_BEGIN` vs a new type.
4. **In-page picker visual.** How the first result is **rendered** (prefer
   existing `gallery-preview` via SW). Arrows cycle the current result
   list. No multi-card layout in the popup. Keep Search memes + kind
   filter + Attach.
5. **Gallery thaw.** Exact DOM control, exact detection of companion
   hosting, ordinary-tab behavior unchanged, emoji/label, no global
   `styles.css` restyle.
6. **ADR.** Whether a new ADR is required. Do not edit 0061/0062 in place.
7. **companion_mutation and allowlist.** Prove no new flagged mutation, or
   justify one with independent acceptance.
8. **Tests.** Causal owners: extension `node --test`, bridge tests, web
   detection tests that do not require signed-in X, contract tests if a
   new ADR/header/message type appears.
9. **Causal implementation slices** with allowlists. Independent
   acceptance if the security boundary changes. Publication and NUC remain
   later grants.
10. **Residual risks** Michal must accept: unpublished feature branch, WAR
    residual, framing unknown until proven, NUC still on `bfad16b`,
    bound-tab Attach only works when an X composer tab has talked to the
    SW, Gallery still showing canonical titles until a later whole.

### 16.4 Planner must not

- reopen the accepted Attach **float** as a design exercise;
- replace the in-page popup with the full FrameNest website;
- propose CORS, `all_urls`, content-script FrameNest fetch, or auto-Post;
- expand `companion_mutation` onto Gallery or analysis routes without a
  new ADR and a security reason;
- thaw ordinary-tab Gallery / Details;
- implement Gallery alias editing, lightbulb, model dropdown, language
  settings, or Analyze execution;
- remove Save Description or restore the tag checkbox forest;
- claim the side panel already hosts FrameNest web;
- claim Gallery already displays per-user aliases;
- authorize itself to implement;
- require Michal to re-select the logical whole;
- plan NUC origin writes or push.

### 16.5 Advisory sequencing (not authority)

A reasonable slice order for the Planner to accept or replace:

1. Thin side-panel shell + origin-empty Connect + iframe of stored origin.
2. Handshake so FrameNest web knows it is companion-hosted.
3. Gallery bottom-right control → Attach in that context only, via existing
   `ATTACH_BEGIN`.
4. In-page picker: render the selected item preview (one at a time) using
   SW `gallery-preview`; keep arrows + Attach.
5. Operator docs (`X_COMPANION.md`) and a new ADR if the bridge is a new
   trust surface.
6. Separately later: independent acceptance, publication, NUC origins,
   then backlog wholes in Section 14.

---

## 17. After the Planner report

Treat the report as a claim. Reconcile against repository evidence and
Michal’s attached observations.

Then:

- accept and issue one implementation prompt (`Native planning mode:
  not-used`, Exact baseline = live HEAD, exact allowlist);
- or authorize the one targeted planning revision;
- or reject and stop.

Do not start implementation because a Markdown plan file appeared. Do not
run a second automatic revision.

---

## 18. Git, publication, and NUC

This Orchestrator session’s default Git authority is **none** beyond writing
Meta prompt/report files for this whole.

Do not commit FrameNest. Do not push. Unpublished companion, overlay, and
Save-popup commits stay on `feat/x-meme-browser-companion` until an
explicit publication grant.

Do not fast-forward local `main`. Do not deploy the feature branch because
Attach or Save was accepted locally.

NUC routine update, if later authorized, remains:

```text
deploy/ubuntu/framenest-release status
deploy/ubuntu/framenest-release check --release <40-hex-SHA>
```

Deployment never follows automatically from a check. Companion origins and
`x_acquisition_root` are host configuration, not Git.

Poetry / CPython on NUC for routine updates:

```text
Poetry: /opt/framenest/tooling/poetry/2.4.1/.venv/bin/poetry
CPython: /opt/framenest/tooling/python/cpython-3.13.14-linux-x86_64-gnu/bin/python3.13
```

`uv` is not a routine release-update tool.

---

## 19. Meta trace for this whole

Agent Orchestrator:

- writes each issued prompt to its exact path;
- validates returned reports and preserves them verbatim;
- stages only the completed prompt/report pair when archival is authorized;
- must not force-push or silently supersede history.

Worker:

- may write only the exact report path in its prompt;
- may not commit or push Meta.

Do not automatically continue to implementation because a report file
appeared. Wait for Michal when the workflow assigns that touchpoint. He
typically attaches the report file and adds observations.

Do not mix parent 03 or 04 pairs into this whole’s first commit.

---

## 20. Out of scope unless Michal later selects them

Everything in Section 14, plus:

- AP upgrade / `ap upgrade cisarik/framenest`
- reconstructing gpgconf / printing SSH_AUTH_SOCK
- enabling NUC X acquisition as part of sidebar implementation

---

## 21. Stop conditions

Stop and talk to Michal when:

- Extra High or Native Plan Mode cannot be provided as routed;
- HEAD is not the expected feature-branch candidate and the divergence is
  material;
- live `save.html` no longer matches Section 11.1;
- the Planner wants CORS, auto-Post, `all_urls`, or content-script fetches;
- the Planner wants to put the full FrameNest website into the in-page
  Attach popup;
- the Planner wants to implement the alias editor, language tab, or
  Analyze execution inside this whole;
- Analyze by AI is proposed against uncataloged X media as if it were
  cataloged;
- NUC password or unapproved privilege is required;
- the same correction/recheck loop repeats;
- a newer Cooperator decision contradicts this handoff.

Do not stop for harmless formatting, a stale comment, or an optional tool
the declared route does not require.

---

## 22. First response and immediate behavior

After receiving this handoff:

1. Acknowledge Michal briefly in Slovak. Confirm Extra High, Native Plan
   Mode off for you, Planner next with Plan Mode on.
2. State that you are beginning read-only restoration.
3. Perform the repository / AP / Meta gates directly.
4. Confirm HEAD and `save.html` against Section 9.1 / 11.1.
5. Summarize only material restored facts and conflicts.
6. If healthy, write
   `…/05-framenest-x-companion-sidebar-web-mvp/01_planning_00.md`
   immediately.
7. Display the required Slovak Extra High + 💡 capsule.
8. Do not ask Michal what feature to choose.
9. Do not ask him to repeat the brainstorming.
10. Do not paste the Worker prompt into chat.
11. Do not implement product code.
12. Wait for the Planner report and Michal’s attached feedback before
    implementation.

Recommended first Slovak facts so he does not have to remember the prior
chat:

- Save popup is frozen: Description, Search tags, Save vpravo, admin
  “Save and analyze by AI” len uloží.
- In-page Attach ostáva quick attach: jeden vykreslený meme, šípky,
  Attach. Side panel bude celý FrameNest web.
- Gallery Attach emoji len v extension kontexte.
- Alias editor, žiarovka, jazyk, skutočné Analyze — backlog, nie tento
  celok.
- Žiadne CORS, auto-Post, NUC v tomto celku.

---

## 23. Success for this Orchestrator session

Success is not a coded side panel on day one.

Success is:

- restored evidence that matches or honestly supersedes Section 9;
- Save popup left frozen at Worker 04 unless a live defect appears;
- one complete Extra High Planner prompt on disk that a Worker can execute
  without reconstructing this chat;
- backlog in Section 14 still visible so the next whole after sidebar does
  not forget aliases-in-Gallery, language, or Analyze-after-catalog;
- a later accepted plan that a bounded implementation Worker can execute
  without discovering that the side panel cannot host Tailscale web, or
  that Attach from Gallery would require CORS.

Show discipline. Separate the three surfaces. Carry the backlog. Then move.
