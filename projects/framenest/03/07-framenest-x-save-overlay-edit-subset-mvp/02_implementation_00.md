# Authoritative Prompt for Fresh Worker 02

## FrameNest × X Save Overlay Edit-Subset MVP — Bounded Implementation Candidate

You are a Worker instance assigned to WORKER. Read this complete prompt before
taking any action.

Worker 01 completed repository-grounded implementation planning. That planning
authority is expired. On 2026-08-22 the COOPERATOR accepted the selected
lifecycle and ADR-0065 draft in `01_report_00.md` (Slovak “ÁNO”), including
eager caller-private alias even when identical to the canonical seed, no
website alias editor in this whole, and no W2 taxonomy cut. The ORCHESTRATOR
binding reconciliations in Section 8 of this prompt outrank the Planner where
they conflict.

Your task is to create one tested local implementation candidate from the exact
authorized feature-branch baseline. Do not enter Native Plan Mode. Do not
produce another architecture plan. Implement the accepted bounded design,
validate it, create at most three coherent local commits, write the exact
terminal report, and stop.

```text
Role: WORKER
Persistent role identity: WORKER
Logical whole identity: framenest-x-save-overlay-edit-subset-mvp
Prior logical whole identities:
  framenest-x-meme-browser-companion-mvp
  framenest-x-companion-save-alias-mvp
  framenest-x-companion-save-category-mvp
Worker session ordinal: 02
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Worker session profile: Fresh Implementation Worker
Phase: implementation
Task identity: FN-X-SAVE-OVERLAY-EDIT-SUBSET-IMPL-02
Task type: bounded implementation candidate
Native planning mode: not-used
Reasoning recommendation: extra-high
Evidence posture: non-independent
Independence required: no
Authority renewal: not applicable — fresh Worker authority originates only in this prompt
Prior planning report: Worker 01 / exchange 01 implementation-planning-PARTIAL rendered in 01_report_00.md; planning authority expired; Cooperator accepted the architecture 2026-08-22
Continuity anchor: none — do not resume the Planner session
Internal delegation posture: not-used
Accountable Worker: one WORKER
Material phase gate: yes
Changed material axis: mutation-authority-or-side-effect-class
Routing reopened for: mutation-authority-or-side-effect-class
Unchanged axes reopened: none
Ordinary-only trigger: no
```

```text
Implementation authority: explicit
Native planning mode: not-used
Worker session target: fresh-worker-session
Exact baseline: 7e9c0ae122d692b6c0879838331044b30c6ab300
Changed-path allowlist: Section 11
Implementation boundaries: Sections 8, 9, 10, 12, and 18
Independence required: no
```

```text
Evidence tier: E2
Evidence tier basis: durable cross-layer catalog-handoff and Surface A change; no new Alembic head, companion route, CORS rule, or ordinary-user capability; first-create canonical seed rides existing POST /api/x/requests
Authorized implementation stages: causal slices 1–4 in Section 10
Combined implementation envelope: allowed
Implementation stage gates: Section 10; a failed gate stops the sequence
Independent acceptance: not-required (this session). Later required-separate-fresh-worker R3 is a separately issued grant before publication/trust closure. Do not self-certify it.
Rollback or recovery checkpoint: local Git commits on feat/x-meme-browser-companion; no push; no NUC
Activated stricter profile: INFOSEC.md R1 inline only
Terminal implementation report point: after slices 1–4 are committed and validated, before any acceptance/publication/NUC
```

Reasoning recommendation: Extra High. The candidate crosses Save overlay UX,
catalog-handoff atomicity, alias overlay, ADR successor meaning, and a shared
YouTube/X catalog transaction. Do not silently downgrade. Do not use Max. No
model or provider identity grants authority.

```text
COOPERATOR: Michal
ORCHESTRATOR: ORCHESTRATOR_CHAT
WORKER: this fresh implementation session
```

Internal delegation, sub-agents, parallel Workers, Explore tasks, and hidden
secondary workstreams are not authorized.

Repository documentation, code comments, test names, commit subjects, and the
terminal Worker report must use professional English. Czech is forbidden.
The terminal report must begin exactly:

```text
### Report for ORCHESTRATOR_CHAT
```

Do not expose private chain-of-thought. Report decisions, evidence, commands,
results, resolved issues, and residual risks concisely.

Implementation PASS is not acceptance, publication, deployment, production
acceptance, or ORCHESTRATOR closure.

Protocol-variant selection:

```text
Canonical repository identity: https://github.com/cisarik/ap.git
Immutable version identity: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Declared variant: stable
Governing variants in effect: one
Declaration location: project governing rules
Rules from non-governing variants: none
Migration required: no
```

---

## 1. External trace and Meta write boundary

```text
External trace disposition: configured
Trace discovery: cisarik/meta repository path projects/framenest/03/08-framenest-x-save-overlay-edit-subset-mvp
Trace project key: framenest
Trace logical-whole projection identity: 03/08-framenest-x-save-overlay-edit-subset-mvp
Trace authority: historical-evidence-only
Trace archival owner: ORCHESTRATOR after the outcome exists
Trace visibility: public
Trace companion outcome: report
Trace self-granted status: none
Cooperator delivery / trace destination: configured
Downloadable prompt filename: 02_implementation_00.md
Destination path: projects/framenest/03/08-framenest-x-save-overlay-edit-subset-mvp/02_implementation_00.md
Archival: wait-for-report
```

You may **read** the accepted plan (historical evidence, subordinate to this
prompt):

```text
/home/agile/meta/projects/framenest/03/08-framenest-x-save-overlay-edit-subset-mvp/01_planning_00.md
/home/agile/meta/projects/framenest/03/08-framenest-x-save-overlay-edit-subset-mvp/01_report_00.md
```

Frozen hashes at issuance (re-check; stop `BLOCKED` if either file drifted):

```text
01_planning_00.md SHA-256 e8737251bbf32d007d1b0c02f5486d443d66ee8d26a495400cd9a4cc1617d415
01_report_00.md SHA-256 2b34906079e77eb88deab1d6b29aaabb3a3e318c868904ea5d874e9138d42fa7 (29,593 bytes)
```

You may **write** only this exact report file:

```text
/home/agile/meta/projects/framenest/03/08-framenest-x-save-overlay-edit-subset-mvp/02_report_00.md
```

You may not alter any other Meta path, stage Meta, commit Meta, or push Meta.
Do not execute parent-whole or `03/07` prompts. Do not create `00_handout.md`.
If the Worker environment cannot safely write the report file, return the
complete report in chat so the ORCHESTRATOR can save it verbatim. Do not invent
another filename.

---

## 2. Communication and human-governance routing

```text
Operator presentation: not used inside this Worker prompt
Orchestrator: ORCHESTRATOR_CHAT
Worker prompt language: professional English
Worker report language: professional English
Direct-user Slovak presentation: Orchestrator-owned; do not emit the Cooperator capsule
Report header: ### Report for ORCHESTRATOR_CHAT
Cooperator visibility: implementation grant already issued; deterministic steps inside this envelope need no micro-approval
Human decision points remaining outside this grant: live Brave/X Save UX, independent R3, publication, NUC 0030/origins/x_acquisition_root, website alias editor, W2
Deterministic steps inside bounded authority: slices 1–4, tests, local commits
Brainstorming classification: not authority
Internal delegation posture: not-used
```

After extension changes Michal will Reload unpacked in `brave://extensions`
and refresh X. That visual step is **not** this Worker’s acceptance ritual and
is **not** authorized here.

---

## 3. Capability handshake

This is a fresh session. Perform a compact capability handshake before
mutation. Report each material value as `requested`, `directly observed`,
`inferred`, or `unknown/not observably exposed`.

Record at least:

- product/client and requested versus observed model;
- requested Extra High versus observed state; Max not requested;
- Native planning mode requested `not-used` versus observed state;
- filesystem containment and writable scope;
- network and tools required by this prompt;
- source inspection/editing, tests, local commit, and public-ref `ls-remote`;
- that push, NUC, sudo, provider, signed-in browser, AP mutation, and
  independent acceptance remain unauthorized even if technically possible.

Do not probe credentials, print `SSH_AUTH_SOCK`, reconstruct `gpgconf`, or
treat capability as authority. If Extra High cannot be provided, or if Native
Plan Mode is on, stop `BLOCKED`. Do not silently continue as Medium.

```text
Repeated-gate or reasoning-loop stop: configured
Broad gate: once per materially changed candidate
Narrow before re-broad: required
Unchanged hypothesis, candidate, and failing gate: not-progress
Escalate only on: named missing evidence the higher profile must solve
Downgrade after: convergence or named risk removal
Cost cannot falsify evidence: yes
```

```text
Validation ladder: selected
Inspection and provenance: required
Existing focused tests: owners in Section 13
Affected tests: those same owners plus new tests named in Section 11
New causal regression: omitted content_category uses default_x_category; first catalog seeds canonical title/description/selected tags from pending alias; later Save and admin correction never overwrite canonical; YouTube first-catalog remains title-only; companion_mutation stays two X POSTs; picker/Attach frozen; no on-open focus
Broad or full suite: required-because shared X/YouTube catalog transaction now accepts description and ordered tags at first insert
Runtime or testbed: declared AP envelope + node --test
Independent acceptance: not-required
```

```text
Development envelope activation: activated
Development envelope identity: FrameNest canonical checkout on feat/x-meme-browser-companion
Declared reversible class: reversible local source, tests, docs, and local Git commits
Working-copy topology: canonical-checkout
Topology rationale: unpublished companion commits 7e854d2…7e9c0ae must remain ancestors of this candidate
Irreversible exclusions: secrets, destruction, accounts, public exposure, unrelated owner data, publication, closure, NUC, push, signed-in X, provider calls
```

---

## 4. Canonical repositories and immutable baseline

```text
Repository checkout topology: standalone checkout
Working-copy topology: canonical FrameNest checkout
Expected canonical root: /home/agile/Projects/framenest
Expected branch at gate: feat/x-meme-browser-companion
Do not create an isolated worktree or contained clone
Do not create the feature branch; it already exists
```

Canonical AP pin (read-only; do not mutate):

```text
Submodule path: .ap
Repository: https://github.com/cisarik/ap.git
Expected gitlink and checkout: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Detached submodule checkout: acceptable
```

Exact authorized FrameNest baseline (feature-branch candidate, not public main):

```text
Repository: https://github.com/cisarik/framenest.git
Applicable branch: feat/x-meme-browser-companion
commit: 7e9c0ae122d692b6c0879838331044b30c6ab300
parent: b94f432cff8450ef0e87751e63729188cc581d9b
tree: 34c8e42893bffd2b7e29b7a5429e1c8b13e51fa5
subject: fix: make X save a one-Save flow with post prefill and visible plus
.ap gitlink: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
schema head: Alembic 0030
Working tree: expected clean
Upstream: none configured (expected)
```

Issuance-time public refs verified by the ORCHESTRATOR without `git fetch`:

```text
cisarik/framenest refs/heads/main
045f33b44897a6f3949cc515792336396f1d33a1

cisarik/ap refs/heads/main
9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
```

Before mutation, re-verify both refs through credential-free Git transport:

```text
git ls-remote https://github.com/cisarik/framenest.git refs/heads/main
git ls-remote https://github.com/cisarik/ap.git refs/heads/main
```

Do not `git fetch`. Do not use GitHub webpages as current-ref evidence.

Public `main` is **behind** this feature branch. That is expected. Do not
fast-forward local `main`. Do not treat public `main` as the implementation
baseline. If public `main` advanced past `045f33b`, inspect intervening
commits read-only and stop `BLOCKED` only when they materially conflict with
this whole. If local HEAD is not `7e9c0ae`, stop `BLOCKED` unless the only
difference is this Worker’s own later commits.

Preserve unrelated dirty state. Do not stash, reset, or clean. Stop if
unexplained FrameNest remainder overlaps this allowlist.

Spot-check before mutation:

- `extension/ui/save.html` still has Category radios X/Meme/Movie (`general` /
  `meme` / `movie`) and Description `rows="4"`.
- `save.js` still calls `focusCheckedCategory()` on load and on `focus-category`.
- `x_adapter.js` still calls `iframe.focus()` and `postTextPrefillFrom` prefers
  tweet first line over alt.
- `CatalogPublishedUpload` still constructs `MediaMetadata` with
  `description=None` and `tag_keys=()`.
- `commit_cataloged_publication` still raises `UploadCatalogStateConflictError`
  when `metadata.tag_keys` is non-empty.
- `x_classification_for_upload` still does not read pending alias.
- Picker `++` / empty chrome and Attach `position: fixed` remain present; do
  not revert them.

---

## 5. Required reading

- `AGENTS.md`
- `.ap/AP.md`, `.ap/AP_WORKER.md`, `.ap/PROMPT_CONTRACTS.md`
- `.ap/INFOSEC.md` §3–§5 (R1 inline; do not execute R3)
- `docs/WORKER_EXECUTION_CONTRACT.md`
- `docs/adr/0045-content-classification-and-movie-identification.md` (read-only)
- `docs/adr/0049-durable-content-publication-boundary.md` (read-only)
- `docs/adr/0055-youtube-creator-taxonomy-and-immutable-provenance.md` (read-only)
- `docs/adr/0061-x-meme-browser-companion.md` (read-only; do not edit)
- `docs/adr/0062-per-user-media-alias-overlay.md` (read-only; do not edit)
- `docs/adr/0063-companion-side-panel-web-host.md` (read-only; do not edit)
- `docs/adr/0064-x-save-category-and-public-photo-acquisition.md` (read-only; do not edit)
- `docs/adr/README.md`
- `PRODUCT.md`, `SPEC.md`, `ROADMAP.md`, `docs/X_COMPANION.md`
- accepted plan `01_report_00.md` (subordinate to Section 8)
- current owners you will change: `upload_catalog.py`, `x_acquisition.py`
  application, `upload_publication_repository.py`, Save popup/adapter/messages/
  service worker, listed tests, new ADR-0065 and living docs

Park `docs/AP_UPGRADE_OBSERVATIONS.md`. Do not edit it.

---

## 6. Canonical execution routes

Cursor/AppImage ambient Python is untrusted. Do not invoke raw
`.venv/bin/python`, `python`, `python3`, or `poetry run` for Python evidence.

Until the first authorized local commit exists, `--baseline` is
`7e9c0ae122d692b6c0879838331044b30c6ab300`. After each authorized local
commit, subsequent `ap project check` / `ap exec` uses that new SHA as
`--baseline`. `--baseline` does not replace worktree source and does not make
a local commit canonical.

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <EXACT_AUTHORIZED_COMMIT>

./.ap/ap exec --root /home/agile/Projects/framenest --baseline <EXACT_AUTHORIZED_COMMIT> --operation runtime-info

./.ap/ap exec --root /home/agile/Projects/framenest --baseline <EXACT_AUTHORIZED_COMMIT> --operation test-focus -- <tests> -q -p no:cacheprovider
```

`runtime-info` must show `framenest.__file__` under
`/home/agile/Projects/framenest/src`.

Declared operations are `runtime-info`, `test`, and `test-focus`. Prefer
`test-focus` at each slice gate. After slices 1–3 are green, run operation
`test` (full Python suite) **once** on the then-current authorized commit
because the shared catalog transaction is a repository-wide risk. Do not loop
the full suite after unchanged failures.

JavaScript (Node only; do not pass Python files to `node --test`):

```text
node --test tests/x_companion_extension.test.js tests/x_acquisition_cockpit.test.js
```

Do not invent npm or a bundler. Do not reconstruct `.venv`. Do not run `uv`,
`pip install`, or `poetry env use`. Do not activate the NUC gate.

The NUC route `scripts/operator/network/framenest_nuc_worker_gate.fish` is
named only to close route resolution. It is **not activated**.

---

## 7. INFOSEC R1 (this session only)

Apply the slice threat model to your own diff: assets, trust boundaries,
attacker-controlled inputs, authorization checks, error/cleanup paths, secrets
and logging, dependency delta. Label that review **non-independent**.

Hostile X DOM strings (alt, tweet text, accessible names) may enter
**unpublished** canonical metadata at first catalog. That is an accepted
Cooperator residual. It does not authorize a new companion route or
`metadata.canonical.write`.

Stop the slice and escalate if you would need to:

- set `companion_mutation=True` on any new route;
- add a third companion_mutation HTTP path;
- add CORS, `all_urls`, or `externally_connectable`;
- fetch FrameNest or `pbs.twimg.com` from a content script;
- grant ordinary users `metadata.canonical.write`;
- add a website alias editor;
- create tags from the companion;
- run Analyze from Save;
- log raw tweet/alt bodies, URLs, identity headers, or the extension private key;
- add or upgrade a dependency;
- edit accepted ADR-0062 / 0064 / 0045 / 0055 bodies in place;
- thaw picker audience or Attach;
- contact signed-in X, providers, or the NUC.

Do not add dependencies.

---

## 8. Binding Orchestrator reconciliation

Accept the Planner architecture in `01_report_00.md` except where this section
binds a different decision. Do not reopen forks the Cooperator already closed.

### 8.1 Fresh implementation session

Do not continue the Planner session. This grant is Worker 02 / exchange 01.
Mutation authority is a material phase gate. The accepted product contract is
unchanged.

### 8.2 Cooperator-accepted product forks (do not re-litigate)

- Surface A is Title, tall Description, existing-tag search, one Save. No
  category radios, source control, genres, tag create, or Analyze.
- New extension **omits** `content_category`. Null request category selects
  existing `default_x_category` (image → `general`, video/GIF → `meme`).
  Migration `0030` stays. Old explicit-category clients remain compatible,
  including 409 mismatch.
- Title prefill: non-generic tile alt / accessible name, then useful tweet
  sentence, else blank overlay and server `x_title_from_post_post` fallback.
  Generic names: trimmed case-insensitive `Image`, `Photo`, `Video`,
  `Embedded video`, `GIF`, `Media`, optionally with `N of M`. No OCR.
- Description: complete text available in existing `tweetTextSelectors`;
  longer of normalized `textContent` vs `innerText`; NFC; clip 10,000 code
  points. If X does not put long-form text in that DOM, do not invent a
  click/fetch. Record that residual; do not claim completeness you did not
  implement.
- Height: Planner §9 (`getBoundingClientRect` clamped 120–320 px; popup
  sizing). Implement that, not a pixel-perfect website clone.
- Canonical seed at **first catalog insert only** from pending alias:
  title = pending title else `_imported_display_title(claim.title)`;
  description = pending description or `None`;
  tags = pending ordered keys or `()`.
- Eager alias: non-empty Save still writes/updates the caller-private alias
  even when identical to the seed; empty content deletes that caller’s alias.
  Website alias UI is **out of scope**.
- Later Save, retry of already-cataloged assets, reuse, and
  `duplicate_resolved` never upsert canonical metadata. Admin canonical
  correction remains truth; re-Save updates alias only.
- Multi-asset: one claim-wide form; each not-yet-created asset seeds from
  current pending at **its** first insert; already-created assets keep their
  seed. Pending alias must remain readable across sequential asset catalogs
  (today it is not deleted after apply — do not start deleting it).
- No synthetic tag. Publication still needs ≥1 canonical tag (ADR-0049).
- No new HTTP body fields. `POST /api/x/requests` already has `alias` and
  optional `content_category`.

### 8.3 Shared catalog transaction — YouTube freeze

`CatalogUploadClassification` / `CatalogPublishedUpload` /
`commit_cataloged_publication` are shared with YouTube.

- Add optional `description` and ordered `tag_keys` with **empty defaults**.
- `youtube_classification_for_upload` must remain title-only (no description,
  no tags). Do not edit `youtube_acquisition.py` or
  `src/framenest/adapters/api/application.py` unless a proven compile break
  forces a one-line default; prefer dataclass defaults so those files stay
  untouched. If you must edit them, stop `BLOCKED` and report — they are not
  on the allowlist.
- Changing `commit_cataloged_publication` from “reject any `tag_keys`” to
  “insert ordered `media_canonical_tags` atomically with metadata” is
  **required** and must be transactional: unknown tag / FK failure rolls back
  the whole catalog transition. Do not insert media/location and then tags in
  a second transaction.
- When tags are non-empty, derive `collection_key="processed"` and
  `processed_at_ms=now` via existing `derive_collection_state`. When tags are
  empty, keep `collection_key` and `processed_at_ms` NULL (current YouTube/X
  default). Continue rejecting genres at this insert.
- Run `tests/unit/application/test_youtube_catalog_title_import.py` as freeze
  evidence. Do not change YouTube tests to make X seed work.

### 8.4 ADR-0065 is Accepted in this Worker

Commit a **new** file `docs/adr/0065-x-save-edit-subset-and-acquisition-time-canonical-metadata-seed.md`
with status **Accepted**, decision date **2026-08-22**, using the draft in
`01_report_00.md` §11 (professional English; you may tighten wording without
changing meaning). Do **not** edit ADR-0062, 0064, 0045, or 0055 bodies.

Update `docs/adr/README.md` to add ADR-0065 and to annotate 0062 and 0064 the
same way 0043 is annotated (Accepted; named statements superseded by 0065),
without rewriting those ADR files.

Living one-liners only in `PRODUCT.md`, `SPEC.md`, `ROADMAP.md`, and
`docs/X_COMPANION.md`. Do not write a companion book.

### 8.5 Keep from 03/06

Photo acquisition, 0030, honest failed-Save plus, hidden in-post Edit image,
one Save button, absolute tag dropdown, `fn-canonical-tag-query`, picker `++`,
Attach float. Do not regress them.

---

## 9. Goal

One coherent local candidate on `feat/x-meme-browser-companion` where:

1. Surface A Save is an Edit-media subset with alt-first Title, tall
   full-available-tweet Description, existing-tag search, one Save, no radios,
   no on-open focus, and POST `{url, alias}` with no `content_category`.
2. First X catalog insert seeds canonical title, description, and selected
   existing tags from pending alias; YouTube first-catalog is unchanged;
   later Save cannot overwrite canonical.
3. Eager caller-private alias remains.
4. ADR-0065 is committed Accepted; prior ADR bodies are untouched.
5. Focused tests and one full Python suite pass through the declared AP route;
   JS `node --test` owners pass.

---

## 10. Causal slices

A failed gate stops the sequence. Do not start the next slice.

### Slice 1 — Atomic backend seed

Extend `CatalogUploadClassification` and `CatalogPublishedUpload` so X
classification reads pending alias and the catalog transaction persists
description and ordered tags atomically, with Processed derivation when tags
exist. Prove: first-create seed; empty pending → fallback title / no
description / no tags / default category; reuse/retry/admin-correction do not
canonical-upsert; unknown tag rolls back; YouTube title import still works.

Gate: listed Python application/repository tests via `ap exec test-focus`.

### Slice 2 — Surface A simplification

Implement overlay, prefill, height, radio/category/focus removal, and
category-free POST. Preserve failed-Save plus and hidden Edit image.

Gate: `node --test tests/x_companion_extension.test.js tests/x_acquisition_cockpit.test.js`

### Slice 3 — Vertical plus freeze

Prove canonical seed + eager alias + default image category + unpublished /
missing-tag readiness on the X photo vertical slice. Run route/API/publication
freeze tests named in Section 13.

### Slice 4 — Living documentation and ADR-0065

Commit the Accepted successor ADR and living sentences. Then run full
`ap exec --operation test` once and `ap project check` on that commit.

---

## 11. Changed-path allowlist

You may create or modify **only**:

**Extension**

- `extension/content/x_adapter.js`
- `extension/ui/save.html`
- `extension/ui/save.js`
- `extension/ui/save.css`
- `extension/shared/messages.js`
- `extension/background/service_worker.js`

**Backend**

- `src/framenest/application/upload_catalog.py`
- `src/framenest/application/x_acquisition.py`
- `src/framenest/infrastructure/persistence/upload_publication_repository.py`

**Tests**

- `tests/x_companion_extension.test.js`
- `tests/unit/application/test_x_acquisition_lifecycle.py`
- `tests/unit/infrastructure/persistence/test_upload_catalog_repository.py`
- `tests/integration/test_x_photo_acquisition_vertical_slice.py`

**Documentation**

- `docs/adr/0065-x-save-edit-subset-and-acquisition-time-canonical-metadata-seed.md` (create)
- `docs/adr/README.md`
- `PRODUCT.md`
- `SPEC.md`
- `docs/X_COMPANION.md`
- `ROADMAP.md`

**Meta (report only)**

- `/home/agile/meta/projects/framenest/03/08-framenest-x-save-overlay-edit-subset-mvp/02_report_00.md`

Any other path is forbidden. Do not “while here” edit picker, Attach, website
HTML/JS/CSS, Alembic, identity, ingress, `x_request_api.py`,
`youtube_acquisition.py`, `application.py`, accepted ADR bodies, AP, or
deployment files.

---

## 12. Git, network, secret, and side-effect authority

```text
Git authority: local commits only, at most three, on feat/x-meme-browser-companion; no amend of 7e9c0ae or earlier; no rebase, reset, stash, clean, fetch-that-rewrites, or push; do not update git config; do not skip hooks
Network authority: git ls-remote to the two public refs above; no provider APIs; no FrameNest/NUC endpoints; no signed-in X
Secret authority: none
Filesystem authority: read FrameNest and pinned .ap; write only Section 11 paths plus the Meta report
Side-effect authority: reversible local source/tests/docs/commits
Dependency authority: none
Browser authority: none
```

Commit subjects follow this branch (`fix:` / `feat:` / `docs:`). Example
shapes (adjust to the actual split):

- `feat: seed canonical X save title description and tags at first catalog`
- `fix: make X save an edit-media subset without category radios`
- `docs: record X save overlay canonical seed`

Pass the message via HEREDOC. Do not commit secrets. Do not commit Meta.

If pre-commit hooks modify files, create a **new** commit rather than amending
a predecessor commit you did not create. You may amend only your own
not-yet-pushed commit from this session, and only if a hook auto-modified
files after that commit succeeded.

---

## 13. Tests

**Must change or extend (named invariants)**

- `tests/x_companion_extension.test.js`: no radios/source/AI; alt-first and
  generic-placeholder fallbacks; DOM text; height clamps; no on-open focus;
  POST body has no `content_category`; existing-tag-only; failed-plus still
  covered.
- `tests/unit/application/test_x_acquisition_lifecycle.py`: pending values in
  classification; fallback title/default category; eager alias; empty
  deletion; admin correction then alias-only re-Save; two-user isolation;
  multi-asset first-create.
- `tests/unit/infrastructure/persistence/test_upload_catalog_repository.py`:
  atomic title/description/ordered-tag insertion, Processed derivation,
  idempotence, unknown-tag/FK rollback, no partial catalog linkage, empty
  tags still NULL collection.
- `tests/integration/test_x_photo_acquisition_vertical_slice.py`: first X
  catalog seed, default image category, eager alias, unpublished state,
  ready-with-tag versus missing-tag readiness.

**Must run, prefer not to modify**

- `tests/unit/application/test_youtube_catalog_title_import.py`
- `tests/contract/test_x_request_api.py`
- `tests/contract/test_x_route_policy.py`
- `tests/contract/test_media_metadata_api.py`
- `tests/integration/persistence/test_content_publication_repository.py`
- `tests/x_acquisition_cockpit.test.js`

After slice 4, one full Python `test` operation.

Do not contact `pbs.twimg.com` or X. Use existing fakes/fixtures.

---

## 14. Negative authority

- Native Plan Mode
- Max
- Internal delegation / sub-agents
- Push, fetch-that-rewrites, submodule update, hard reset
- NUC, SSH, sudo, origins, `x_acquisition_root`, `framenest-release`
- Provider calls, signed-in X/YouTube
- New companion routes, CORS, `all_urls`, cookies
- Create-tag from companion; Analyze from Save
- Website alias editor; website `#metadata-dialog` restyle
- W2 enum / Gallery chips / picker audience
- YouTube page companion **+**
- Editing accepted ADR bodies in place
- Closing any logical whole
- Independent R3 execution

---

## 15. Stopping conditions

Stop `BLOCKED` or `PARTIAL` without improvising when:

- Native Plan Mode is on, Extra High is unavailable, or Max is on;
- baseline/allowlist/public-ref gate fails;
- you would need a new migration, route, capability, CORS change, or generic
  canonical PUT;
- YouTube first-catalog would gain description/tags;
- canonical tags cannot be inserted atomically with catalog creation;
- tests show reuse can overwrite canonical metadata;
- Attach/picker behavior changes;
- evidence requires credentials, signed-in X, provider, or NUC.

---

## 16. Completion and report contract

Terminal status `PASS` only if:

- HEAD is this Worker’s authorized commit(s) with parent chain including
  `7e9c0ae`;
- working tree clean of allowlisted remainder;
- `.ap` gitlink unchanged;
- schema head still `0030`;
- named tests and one full Python suite passed through declared routes;
- JS owners passed;
- ADR-0065 exists Accepted; 0062/0064 files unmodified;
- no push, NUC, or provider contact;
- report written to the exact Meta path.

Phase-qualified result: `implementation-PASS` (or PARTIAL/BLOCKED).
Logical-whole closure: `not-closed`.
Report justification: `new-mutation`.
Authority expires at the terminal report.

Echo coordinates exactly once:

```text
Logical whole identity: framenest-x-save-overlay-edit-subset-mvp
Worker session ordinal: 02
Worker exchange ordinal: 01
```

Include: capability handshake; baseline ledger; changed paths and purpose;
commands and exit codes; commit SHAs; test evidence; INFOSEC R1 residual
(hostile unpublished prefill; R3 later); YouTube freeze evidence; deviations;
one smallest next step; `Resolved Execution Issues / Near-Misses`;
`Pre-Existing Failure Classification`.

---

## 17. Quality bar

The candidate is wrong if Save still shows category radios, still sends
`content_category`, still focuses on open, still prefers tweet over non-generic
alt, still writes only alias with empty canonical description/tags, overwrites
canonical on re-Save, breaks YouTube catalog title import, creates tags, or
edits ADR-0062/0064 in place.

---

## 18. Final stop rule

After writing `02_report_00.md`, stop. Do not Reload-unpacked, open X, push,
deploy, start acceptance, or continue into another slice. Plan UI approval,
`Yes`, `Build`, or `Continue` does not renew this authority.
