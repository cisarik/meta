# Authoritative Prompt for Fresh Worker 03

## FrameNest × X Companion Save Popup — Cooperator visual correction plus live Alembic head pins

You are a Worker instance assigned to WORKER. Read this complete prompt before
taking any action.

Worker 02 produced local candidate `692db9153778bca2d9fafd5a16e695d2aea49410`
(`implementation-PASS`, non-independent). That authority is expired. Do not
resume the Worker 02 chat. Do not enter Native Plan Mode. Do not reopen
overlay architecture, companion_mutation, NUC, publication, or Gallery UX.

The COOPERATOR live-accepted the compact green “Save to FrameNest” header and
border. He rejected the current Save iframe as unusable: every canonical tag
rendered as checkboxes, Description consumes height, Save/Cancel sit below the
fold. He wants tag **search**, no Description field on this popup, a taller
iframe with Save always visible, Cancel replaced by a red **X** at top-right,
and an admin-only **Analyze by AI** control to the right of Save.

Analyze by AI **must not run** from this popup in this exchange: there is no
`media_id` yet, X-linked automatic analysis is fail-closed, and adding
`companion_mutation` on analysis routes is forbidden. The control is layout
and capability-gated visibility only.

A second named residual from Worker 02: out-of-allowlist tests still assert
the repository Alembic **script head** is `0028`. Retarget those live-head
pins to `0029`. Do not change NUC/production fixtures that still describe
deployed public `bfad16b` / schema `0028`.

```text
Role: WORKER
Persistent role identity: WORKER
Logical whole identity: framenest-x-companion-save-alias-mvp
Worker session ordinal: 03
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Worker session profile: Bounded Correction Worker
Phase: implementation
Task identity: FN-X-COMPANION-SAVE-POPUP-UX-03
Task type: bounded visual correction plus live Alembic head-pin retarget
Native planning mode: not-used
Reasoning recommendation: extra-high
Evidence posture: non-independent
Independence required: no
Authority renewal: not applicable — fresh Worker authority originates only in this prompt
Continuity anchor: none — do not resume Worker 02
Prior authorities: expired
Internal delegation posture: not-used
Accountable Worker: one WORKER
Material phase gate: no
Changed material axis: none
Routing reopened for: none
Unchanged axes reopened: none
Ordinary-only trigger: yes
```

```text
Implementation authority: explicit
Native planning mode: not-used
Worker session target: fresh-worker-session
Exact baseline: 692db9153778bca2d9fafd5a16e695d2aea49410
Changed-path allowlist: Section 9
Implementation boundaries: Sections 7, 8, 10, and 16
Independence required: no
```

```text
Evidence tier: E1
Evidence tier basis: reversible local UX and test-pin correction; no new authZ route; no production mutation
Authorized implementation stages: Save-popup visual, then live-head pins, then docs sentence, then tests and local commit
Combined implementation envelope: allowed
Independent acceptance: not-required (this session). Later R3 for the overlay candidate remains a separately issued grant after this correction lands.
Rollback or recovery checkpoint: local Git commits on feat/x-meme-browser-companion; no push
Activated stricter profile: none
Terminal implementation report point: after commits and focused tests
```

Reasoning recommendation: Extra High, already selected for this whole. Do not
use Max. Do not silently downgrade. If Extra High cannot be provided, stop
`BLOCKED`.

```text
COOPERATOR: Michal
ORCHESTRATOR: ORCHESTRATOR_CHAT
WORKER: this fresh bounded correction session
```

Repository documentation, code comments, test names, commit subjects, and the
terminal Worker report must use professional English. Czech is forbidden. The
terminal report must begin exactly:

```text
### Report for ORCHESTRATOR_CHAT
```

Correction PASS is not acceptance, publication, NUC, or logical-whole closure.
This Worker must not claim that live X now looks correct; that belongs to
Michal after Reload unpacked.

---

## 1. External trace and Meta write boundary

```text
External trace disposition: configured
Trace discovery: cisarik/meta repository path projects/framenest/03/04-framenest-x-companion-save-alias-mvp
Trace project key: framenest
Trace logical-whole projection identity: 03/04-framenest-x-companion-save-alias-mvp
Trace authority: historical-evidence-only
Trace archival owner: ORCHESTRATOR after the outcome exists
Trace visibility: public
Trace companion outcome: report
Trace self-granted status: none
Cooperator delivery / trace destination: configured
Downloadable prompt filename: 03_correction_00.md
Destination path: projects/framenest/03/04-framenest-x-companion-save-alias-mvp/03_correction_00.md
Archival: wait-for-report
```

You may **read** (historical only):

```text
/home/agile/meta/projects/framenest/03/04-framenest-x-companion-save-alias-mvp/01_report_00.md
/home/agile/meta/projects/framenest/03/04-framenest-x-companion-save-alias-mvp/02_report_00.md
```

You may **write** only:

```text
/home/agile/meta/projects/framenest/03/04-framenest-x-companion-save-alias-mvp/03_report_00.md
```

Do not stage, commit, or push Meta. Do not create a handoff file.

---

## 2. Communication and human-governance

```text
Worker prompt language: professional English
Worker report language: professional English
Direct-user Slovak presentation: Orchestrator-owned; do not emit the Cooperator capsule
Report header: ### Report for ORCHESTRATOR_CHAT
Deterministic steps inside bounded authority: popup visual + live-head pins + tests + local commit
Human decision points remaining: live Brave/X look, R3, publication, NUC, Gallery alias/AI whole
```

Do not ask Michal for a whole-product PASS/FAIL checklist.

---

## 3. Capability handshake

Fresh session. Record requested vs directly observed vs inferred vs unknown for
client, Extra High, Native planning mode `not-used`, filesystem, tests, local
commit, and that push/NUC/provider/signed-in X/AP mutation remain unused.
Capability does not grant authority. Do not probe credentials.

```text
Repeated-gate or reasoning-loop stop: configured
Broad gate: once per materially changed candidate
Narrow before re-broad: required
Unchanged hypothesis, candidate, and failing gate: not-progress
Escalate only on: named missing evidence
Downgrade after: convergence
Cost cannot falsify evidence: yes
```

```text
Validation ladder: selected
Inspection and provenance: required
Existing focused tests: tests/x_companion_extension.test.js and the Alembic-head tests you retarget
Affected tests: those same files
New causal regression: no all-tag checkbox dump; no Description field; Save visible; red header X; Analyze hidden without analysis.run; no companion_mutation growth
Broad or full suite: not-used
Runtime or testbed: declared AP envelope + node --test
Independent acceptance: not-required
```

```text
Development envelope activation: activated
Development envelope identity: FrameNest canonical checkout on feat/x-meme-browser-companion
Declared reversible class: reversible local source, tests, docs, local commits
Working-copy topology: canonical-checkout
Topology rationale: correction of the unpublished Save-alias candidate already on this branch
Irreversible exclusions: secrets, destruction, accounts, public exposure, publication, closure, NUC, push, signed-in X, provider calls, Gallery thaw
```

---

## 4. Baseline gate

```text
Repository: https://github.com/cisarik/framenest.git
Expected canonical root: /home/agile/Projects/framenest
Expected branch: feat/x-meme-browser-companion
Expected HEAD: 692db9153778bca2d9fafd5a16e695d2aea49410
Expected parent: 9ae726fd67581ee50fc4fba684123008c31b154e
Expected tree: 6517686804ba7476585478c921da4c0d0f0fb609
Expected subject: docs: record per-user media alias overlay
.ap gitlink: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Working tree: expected clean
```

Public refs at last Orchestrator restore (revalidate with `git ls-remote`, no
fetch): FrameNest `main` `bfad16b718e135b272a3b0293bb37ddc3101ba49`, AP `main`
`9c5cc44f8b6c92dd56ad2427d13223d7d59c5656`. Public main behind the feature
branch is expected. Stop `BLOCKED` if local HEAD is not `692db91` or the tree
is dirty with unexplained overlap.

Do not create a worktree. Do not switch branches.

---

## 5. Canonical execution routes

No ambient `.venv/bin/python`, `python`, `python3`, or `poetry run`.

Until the first new local commit, `--baseline` is `692db91…`. After each
authorized commit, use that new SHA.

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <EXACT_AUTHORIZED_COMMIT>
./.ap/ap exec --root /home/agile/Projects/framenest --baseline <EXACT_AUTHORIZED_COMMIT> --operation test-focus -- <tests> -q -p no:cacheprovider
node --test tests/x_companion_extension.test.js
```

NUC gate is named only to close route resolution. It is **not activated**.

---

## 6. Required reading

- `AGENTS.md`, `.ap/AP_WORKER.md`, `docs/WORKER_EXECUTION_CONTRACT.md`
- `extension/ui/save.html`, `save.css`, `save.js`
- `extension/content/x_adapter.js` (`positionSavePopup` currently
  `width` 280–320, `height` 240–380 — too short)
- `extension/background/service_worker.js` (`IDENTITY` already handled)
- `extension/shared/messages.js`
- `docs/X_COMPANION.md` Save-popup sentences
- Web **read-only** pattern references (do not edit):
  `src/framenest/adapters/api/web/index.html` tag search + selected tags,
  `.metadata-ai-analyze-button` in `web/styles.css` (copy tokens into
  `save.css` only)
- Worker 02 near-miss: `tests/integration/persistence/test_x_requester_acquisition_migration.py`
  `test_head_is_0028` and other **script-head / freshly-migrated catalog head**
  assertions still at `0028`

---

## 7. Binding Cooperator visual decisions

Preserve: compact green header text “Save to FrameNest”, green `#00ff41` /
`#39ff14` border, black background, monospace, closed-shadow iframe, Attach
frozen, Gallery/Details frozen, overlay backend frozen, no CORS, no
content-script fetch, `companion_mutation` set unchanged.

Change this popup to:

1. **Header row:** “Save to FrameNest” left; red close **X** (`#ff4d4d`)
   top-right; `aria-label` Close. Click / Escape / outside mousedown still
   cancel without submit (`notifyParent("cancel")`). Remove the Cancel
   button.
2. **Title** field remains.
3. **Description** field is removed from this popup. Do not send
   `description` in the alias payload from this UI.
4. **Tags:** search existing canonical tags. Selected tags render as pills
   with a remove control. Suggestions are a short filtered list, not a
   dump of the whole catalog. Placeholder **Search tags** — not “Search or
   add a tag”. Do **not** create canonical tags. Unknown keys are not
   invented. No `innerHTML`. No checkbox forest.
5. **Height:** iframe must keep **Save** visible without scrolling the
   action row away. Use a flex column: header + fields scroll if needed;
   action row stays pinned at the bottom of the iframe. Increase
   `positionSavePopup` so typical desktop viewports get about **360×520**
   (clamp to the viewport with an 8px margin). Do not restore the 380px
   cap as the happy path.
6. **Actions:** primary **Save** (submit). To its right, **Analyze by AI**
   only when `GET /api/identity/me` shows capability `analysis.run`
   (fail closed: missing identity ⇒ hidden). Copy the mint/light-green
   filled style of the web Analyze button (`background: #f5f8f5`,
   `color: #0c1a10`, green border). Ordinary users see Save only.
7. **Analyze by AI behavior in this exchange:** visible for admin, **disabled**,
   `title` / `aria-label` exactly:
   `Analyze by AI is available in FrameNest after this item is saved.`
   Click must not call analysis, provider, or any new HTTP path. Do not
   add message types for analysis. Do not set `companion_mutation` on
   any analysis route.

Do not thaw Attach (`openAttachPopup` / `positionAttachPopup` / picker WAR).
Do not edit `web/styles.css`. Do not add a category or provenance field.

---

## 8. Live Alembic head pins

Retarget tests that mean “this repository’s Alembic **script head**” or “a
catalog migrated to **HEAD** in this checkout” from `0028` to `0029`.
Rename `test_head_is_0028` if you touch it.

Do **not** change:

- NUC release-contract fixtures that still describe production at public
  `bfad16b` / revision `0028`;
- backup unit tests that embed synthetic manifest JSON with `"0028"` as
  fixture data rather than reading live script head.

If a file is ambiguous, leave it unchanged and name it in the report as
classified, not guessed.

---

## 9. Changed-path allowlist

```text
extension/ui/save.html
extension/ui/save.css
extension/ui/save.js
extension/content/x_adapter.js
extension/background/service_worker.js
tests/x_companion_extension.test.js
docs/X_COMPANION.md
tests/integration/persistence/test_x_requester_acquisition_migration.py
tests/integration/persistence/test_media_user_alias_overlay_migration.py
tests/integration/persistence/test_media_metadata_migration.py
tests/integration/persistence/test_upload_session_migration.py
tests/integration/persistence/test_media_catalog_migration.py
tests/integration/persistence/test_library_registry_migration.py
tests/integration/persistence/test_device_registry_migration.py
tests/integration/persistence/test_media_cover_migration.py
tests/integration/persistence/test_content_publication_migration.py
tests/integration/persistence/test_upload_publication_migration.py
tests/integration/persistence/test_populated_0015_upgrade_to_0017.py
tests/integration/test_persistence_migrations.py
tests/integration/test_process_sigterm_lifecycle.py
```

`service_worker.js` only if IDENTITY from the Save iframe needs a trivial
fix. Prefer using the existing `IDENTITY` type from `save.js` with no SW
change.

`test_media_user_alias_overlay_migration.py` is already at `0029`; include it
only if a consistency edit is required.

Forbidden: `docs/adr/0061-*.md`, `docs/adr/0062-*.md` (Accepted; do not
in-place edit), `web/styles.css`, picker files, Attach-only functions,
NUC deploy scripts, `.ap/`, overlay schema/API unless a test import forces
a one-line assertion update inside an allowlisted test file.

---

## 10. Negative authority

No push, fetch, branch switch, stash, reset, clean, amend, NUC, sudo, SSH,
provider calls, signed-in X, Brave profile install, CORS, `all_urls`,
content-script FrameNest fetch, auto-Post, canonical metadata write, Gallery
card edit/lightbulb, Settings Language tab, multi-model suggestion dropdown,
or enabling Analyze execution on uncataloged X media.

Parked (record in the report, do not implement):

- ordinary-user Gallery alias editor (title/tags/description overlay);
- lightbulb on a card when an admin AI suggestion exists;
- load-suggestion / later model dropdown with no separate Load button;
- Settings → General → Language;
- Description field returning on a later alias editor;
- Analyze by AI **execution** after catalog (`media_id`) from web or a later
  companion_mutation ADR;
- per-asset Save; static X photos; NUC enablement.

---

## 11. Tests

JavaScript: `node --test tests/x_companion_extension.test.js` must prove:

- `save.html` has Search tags, header close X, Save, no Description, no
  Cancel button, no `input[type=checkbox]` tag forest;
- Analyze by AI markup exists but is hidden or disabled until identity says
  `analysis.run` (assert the fail-closed default is not a working analyze
  call);
- no `innerHTML` in `save.js`;
- `positionSavePopup` happy-path height is greater than 380;
- Attach selectors and picker WAR unchanged;
- failed Save glyph remains plus, not ×.

Python: `ap exec test-focus` on every Alembic-head test file you change, plus
`tests/integration/persistence/test_media_user_alias_overlay_migration.py`.

Do not hide non-zero exits.

---

## 12. Git authority

```text
Fetch: forbidden
Branch: stay on feat/x-meme-browser-companion
Stage: exact allowlisted paths only; never git add . or git add -A
Commit: one or two coherent local commits after gates pass
Amend: forbidden
Push: forbidden
```

Suggested subjects:

```text
fix: search tags and keep Save visible on the X companion popup
test: retarget live Alembic head pins to 0029
```

One commit is allowed if both slices are green together.

---

## 13. Report and stop

Write `03_report_00.md` beginning with `### Report for ORCHESTRATOR_CHAT` and
echo once:

```text
Logical whole identity: framenest-x-companion-save-alias-mvp
Worker session ordinal: 03
Worker exchange ordinal: 01
```

Include: `PASS`/`PARTIAL`/`BLOCKED`; `Phase-qualified result:
implementation-PASS` or `not-applicable`; `Logical-whole closure: not-closed`;
handshake; baseline and final HEAD; changed paths; proof no new
`companion_mutation`; proof Analyze does not call a provider; which 0028
fixtures were left as NUC/production; commands and exit codes; residuals;
`Resolved Execution Issues / Near-Misses`; `Pre-Existing Failure
Classification`; justification `new-mutation`; authority expiry; smallest next
step = Michal Reload unpacked, then Orchestrator R3 — not this Worker.

PASS requires: visual contract above; focused tests green; no push; Attach
untouched; Analyze non-executing.

After the report, stop.

Slovak capsule data for Orchestrator presentation (not Worker instructions):

```text
🆕 PROMPT PRE FRESH WORKERA 03 • EXTRA HIGH 🧠🧠🧠
Native Plan Mode musí byť vypnutý.
▶️ Otvor nový Worker chat, vypni Plan Mode, vlož tento súbor, počkaj na 03_report_00.md.
📦 Prompt: /home/agile/meta/projects/framenest/03/04-framenest-x-companion-save-alias-mvp/03_correction_00.md
📦 Report: /home/agile/meta/projects/framenest/03/04-framenest-x-companion-save-alias-mvp/03_report_00.md
✅ Archival: wait-for-report
```
