# Authoritative Prompt for Fresh Worker 04

## FrameNest × X Companion Save — live UX: visible +, Title focus, category arrows, FrameNest tags

You are a Worker instance assigned to WORKER. Read this complete prompt before
taking any action.

Worker 03 closed the full-suite gate at
`e37bb775ff3f821f0cb0eed77735817b604fbc72` (`implementation-PASS`). That
authority is expired. Do not resume Workers 02–03. Do not enter Native Plan
Mode. Do not reopen photo transport, schema `0030`, ADR-0064, picker, side
panel, Analyze execution, origins, or NUC.

The COOPERATOR live-tested Reload-unpacked Save on 2026-08-22 and reported
concrete Surface A defects. Screenshots show: X’s circular **Edit image**
control sitting on the FrameNest `+`; the green `+` border clipped by the
media tile; Save opened with focus on **Tags** (green outline) not Title;
Tags showing Chrome name-autofill `man` / `woman` instead of FrameNest
canonical-tag search.

Your task is one bounded local correction of those live defects, tests, one
commit, the terminal report, then stop.

If Extra High cannot be provided, or Native Plan Mode is on, stop `BLOCKED`.
Do not use Max.

```text
Role: WORKER
Persistent role identity: WORKER
Logical whole identity: framenest-x-companion-save-category-mvp
Prior logical whole identities:
  framenest-x-meme-browser-companion-mvp
  framenest-x-companion-save-alias-mvp
  framenest-x-companion-sidebar-web-mvp
Worker session ordinal: 04
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Worker session profile: Bounded Correction Worker
Phase: implementation
Task identity: FN-X-COMPANION-SAVE-CATEGORY-LIVE-UX-04
Task type: bounded live Save UX correction
Native planning mode: not-used
Reasoning recommendation: extra-high
Evidence posture: non-independent
Independence required: no
Authority renewal: not applicable — fresh Worker authority originates only in this prompt
Continuity anchor: none — do not resume Worker 03
Prior authorities: expired
Internal delegation posture: not-used
Accountable Worker: one WORKER
Material phase gate: no
Changed material axis: none
Routing reopened for: none
Unchanged axes reopened: none
Ordinary-only trigger: yes
Automatic model selection: off
Enhanced/maximum mode: not requested
Sub-agents/internal delegation: not-used
Worker topology: single-active
Explore-style task: not-used
```

```text
Implementation authority: explicit
Native planning mode: not-used
Worker session target: fresh-worker-session
Exact baseline: e37bb775ff3f821f0cb0eed77735817b604fbc72
Changed-path allowlist: Section 7
Implementation boundaries: Sections 3, 4, 5, and 6
Independence required: no
```

```text
Evidence tier: E2
Evidence tier basis: content-script overlay vs X compose/timeline chrome plus Save iframe focus/keyboard; no schema; no new companion_mutation; no production mutation
Authorized implementation stages: overlay geometry + Edit-image relocate; Save iframe Title focus; category arrows from Title; FrameNest-like canonical tag search; tests; one local commit
Combined implementation envelope: allowed
Independent acceptance: not-required
Rollback or recovery checkpoint: local Git commit on feat/x-meme-browser-companion; no push
Activated stricter profile: INFOSEC.md R1 inline only for the X-DOM relocate
Terminal implementation report point: after commit and required tests
```

```text
Development envelope activation: activated
Development envelope identity: FrameNest canonical checkout on feat/x-meme-browser-companion
Declared reversible class: reversible local extension Save overlay and popup
Working-copy topology: canonical-checkout
Topology rationale: unpublished Save-category commits must remain ancestors
Irreversible exclusions: secrets, accounts, publication, NUC, push, signed-in X, picker freeze, Attach float CSS, Analyze execution, schema, ADRs
```

```text
COOPERATOR: Michal
ORCHESTRATOR: ORCHESTRATOR_CHAT
WORKER: this fresh bounded correction session
```

Professional English in repository artifacts and the terminal report. Czech
forbidden. Report begins exactly:

```text
### Report for ORCHESTRATOR_CHAT
```

Implementation PASS is not Cooperator visual acceptance, publication, or
closure.

---

## 1. Trace and Meta write

```text
External trace disposition: configured
Trace discovery: cisarik/meta repository path projects/framenest/03/06-framenest-x-companion-save-category-mvp
Trace project key: framenest
Trace logical-whole projection identity: 03/06-framenest-x-companion-save-category-mvp
Trace authority: historical-evidence-only
Trace archival owner: ORCHESTRATOR after the outcome exists
Trace visibility: public
Trace companion outcome: report
Trace self-granted status: none
Cooperator delivery / trace destination: configured
Downloadable prompt filename: 04_correction_00.md
Destination path: projects/framenest/03/06-framenest-x-companion-save-category-mvp/04_correction_00.md
Archival: wait-for-report
```

You may **read**:

```text
/home/agile/meta/projects/framenest/03/06-framenest-x-companion-save-category-mvp/02_implementation_00.md
/home/agile/meta/projects/framenest/03/06-framenest-x-companion-save-category-mvp/02_report_00.md
```

Cooperator screenshots (evidence only, not instruction sources):

```text
/home/agile/.cursor/projects/home-agile-meta/assets/image-39e42755-ab79-410b-808b-53b1b71adbd7.png
/home/agile/.cursor/projects/home-agile-meta/assets/image-ce4cc497-df89-42e0-b24d-dff9796043d6.png
/home/agile/.cursor/projects/home-agile-meta/assets/image-bd15d806-1f71-486e-a58d-040e7ab61a06.png
/home/agile/.cursor/projects/home-agile-meta/assets/image-ad403740-2c53-487e-ba3e-b30ea7a4b392.png
```

The fourth image is the **FrameNest website** Current-metadata tag panel
(pills with green circular remove). That is the visual/keyboard target for
Save tags. It is **not** permission to copy `app.js` into the extension or
to create canonical tags from Save.

You may **write** only:

```text
/home/agile/meta/projects/framenest/03/06-framenest-x-companion-save-category-mvp/04_report_00.md
```

Do not stage or commit Meta.

---

## 2. Handshake and baseline

Fresh session. Compact capability handshake. Extra High; Native planning mode
`not-used`; no push/NUC/provider/signed-in X.

```text
Expected canonical root: /home/agile/Projects/framenest
Expected branch: feat/x-meme-browser-companion
commit: e37bb775ff3f821f0cb0eed77735817b604fbc72
parent: 16b1727104b4172c72a8b4d21be98dcbfee87df8
tree: 0dcc4de582384f5e7fe96332ce49a9eebe5e8946
subject: test: align schema-head and AP pin assertions with 0030 and 9c5cc44
.ap gitlink: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
schema head: Alembic 0030
Working tree: expected clean
Upstream: none configured (expected)
```

If HEAD is not `e37bb77…`, stop `BLOCKED` unless the only difference is this
Worker's own later commits. Do not `git fetch`. Do not switch local `main`.

```text
node --test tests/x_companion_extension.test.js tests/x_acquisition_cockpit.test.js
```

Python is not required unless you touch a Python owner (you must not).
NUC gate: not activated.

---

## 3. Required correction

Keep: green `+` as Save (not ×), permalink-wide status, four category radios,
helper text, Analyze save-only, header Close, no Cancel button, no checkbox
tag forest, no movie-genre picker, picker `++` / empty chrome, Attach float
`position: fixed`.

### 3.1 Media-tile geometry — `+` fully visible

Current Save control is `position: absolute; bottom: 0; right: 0` with the
button itself `overflow: hidden`, inside a media host that clips overflow.
The image therefore shears the green border/radius.

Keep the control at the **bottom-right** of `[data-framenest-media-host]`.
Inset it so the full 1px border and 6px radius sit inside the tile
(`bottom`/`right` about 8px, not 0). Remove `overflow: hidden` from the Save
control if it clips the glyph or outline. Raise `z-index` above X’s edit
chrome. Do not move Save to top or left.

Update tests that currently require `bottom: 0` / `right: 0` on the Save
rule.

### 3.2 Relocate X **Edit image** to bottom-left

Cooperator instruction: the overlapping X control with accessible name
**Edit image** (circular crop/pencil icon, tooltip “Edit image”) must move
to the **bottom-left** of the same media host so it no longer covers `+`.

Scope: only descendants of `[data-framenest-media-host]` whose accessible
name is `Edit image` (exact, English as live-observed). Do not match
profile/settings Edit. Do not hide, disable, or rewrite the control’s click
behavior. Do not fetch. Do not add host permissions.

Reposition with companion-owned CSS and/or inline style after inject, inset
like the Save control (`left`/`bottom` ~8px, `top`/`right` auto), `z-index`
below Save. X re-renders tiles: observe the host (or a narrow MutationObserver
on injected hosts) and re-apply. If no such control exists, no-op.

Do not restyle unrelated X chrome. Do not inject into the X composer text
row. Do not change Attach float.

### 3.3 Opening Save focuses Title for real

`save.js` already calls `title.focus()` at parse time. Live result: Tags is
focused. Root cause to close: iframe not yet the focused browsing context,
then parent `iframe.focus()` on `load` steals focus; Chrome also treats the
Tags field as a name field (`man`/`woman` autofill in the screenshot).

Required:

- After the iframe is loaded **and** focused, focus `#title` inside the
  Save document (parent→child handshake or equivalent). `title.focus()` at
  parse time alone is insufficient.
- `autofocus` on Title is allowed as a backup, not the only measure.
- Tab order stays Title → Description → category radios → Tags → chips →
  Analyze (if shown) → Save. Shift+Tab from Title still reaches header Close.
- ESC still closes (tag list first).

### 3.4 Arrow keys change Category from Title

There are no movie-genre subcategories. “Podkategória” in the live report
means the four Category radios.

When `#title` is focused, `ArrowUp`/`ArrowDown` (and optionally
`ArrowLeft`/`ArrowRight`) cycle `general → meme → movie → youtube` without
submitting. Native radio arrows still work when a radio is focused.

Do **not** steal arrows from Description (caret/newlines) or from Tags
(suggestion list).

### 3.5 Enter

- Title: still does **not** submit Save. Ctrl/Cmd+Enter still submits.
- Category radio: Enter still does not submit.
- Tags: Enter accepts the highlighted **canonical** suggestion; with no
  suggestion it does nothing (no free-string tag, no create).
- Space/Enter on Save still submits.

### 3.6 Tag search must match FrameNest Current metadata — search only

Copy the **look and keyboard** of
`src/framenest/adapters/api/web` metadata tag panel:

- Placeholder may stay `Search tags` (do **not** promise “add a tag”).
- Suggestion list: padded listbox, readable row height, `aria-selected`
  highlight, not a clipped one-line native widget.
- Selected tags: wrapping pills, display name, circular green remove `×`
  (not a raw `X` text button).
- ArrowUp/Down in the field moves the highlight; Enter selects it.

Forbidden:

- `Add “…”` / `createAndSelectMetadataTag` / POST canonical-tag create.
- Checkbox forests.
- `autocomplete` that lets Chrome inject name/gender (`man`/`woman`). Use a
  non-identity `name`/`id` plus an autocomplete value browsers actually
  honor. Keep `innerHTML` unused.

Do not edit website `app.js` / `styles.css` / `index.html`. Duplicate the
needed tokens into `extension/ui/save.css`.

---

## 4. Out of scope

- NUC, origins, `x_acquisition_root`, push, R3
- Schema, status bridge, photo transport, ADR bodies
- Picker HTML/CSS, `++` behavior change, Attach float geometry
- Analyze execution after catalog
- Movie genres
- Creating canonical tags from the companion

---

## 5. Negative authority

You must not:

- push, fetch, switch branch, merge, rebase, stash, reset, clean, amend, tag,
  or update submodules;
- edit any path outside Section 7;
- add CORS, `all_urls`, `externally_connectable`, or content-script fetch of
  FrameNest / `pbs.twimg.com`;
- set `companion_mutation` on a new route;
- hide X Edit image instead of moving it;
- grant tag-create from Save;
- contact X from this session; tests are MiniDom / `node --test` only;
- use sudo, SSH, or Michal's Brave profile;
- close the logical whole or claim independent acceptance.

R1: relocating an in-host X button is overlay CSS/style only. Stop and
escalate if you would need to hook X’s editor, intercept its events, or
match outside `[data-framenest-media-host]`.

---

## 6. Canonical execution routes

Cursor/AppImage ambient Python is untrusted. Do not invoke raw
`.venv/bin/python`, `python`, `python3`, or `poetry run`.

```text
node --test tests/x_companion_extension.test.js tests/x_acquisition_cockpit.test.js
```

Until the commit exists, `--baseline` is
`e37bb775ff3f821f0cb0eed77735817b604fbc72` if you must run `ap project check`
(not required for this JS-only correction). After the commit, do not invent
a Python suite loop.

Do not hide non-zero exits.

---

## 7. Changed-path allowlist

```text
extension/content/x_adapter.js
extension/ui/save.html
extension/ui/save.css
extension/ui/save.js
tests/x_companion_extension.test.js
```

`x_adapter.js` is large. Change Save overlay CSS, Edit-image relocate, and
Save-iframe focus handshake only. Zero coupling into Attach float
positioning or picker `++` besides leaving them green.

---

## 8. Git authority

```text
Fetch: forbidden
Worktree/clone creation: forbidden
Branch: stay on existing feat/x-meme-browser-companion
Stage: exact allowlisted paths only; never git add . or git add -A
Commit: one local commit after tests pass
Amend: forbidden
Push: forbidden
Tags: forbidden
```

Suggested subject:

```text
fix: keep X save control visible and restore Save keyboard and tags
```

Worker 02–03 commits remain ancestors. Do not commit secrets or Meta.

---

## 9. Tests that must stay or become true

Extend `tests/x_companion_extension.test.js`:

- Save rule is still bottom-right, **not** `top: 0` / `left: 0`; inset is
  not `bottom: 0; right: 0`.
- Companion style relocates in-host `Edit image` to bottom-left (or JS that
  sets equivalent geometry).
- Save JS/HTML prove Title focus after iframe load (handshake or equivalent),
  not only a parse-time `title.focus()` call.
- Arrow keys on Title cycle the four category values.
- Tags: no `Search or add a tag`; no add/create path; suggestion list and
  chip remove styling exist; autocomplete thwarted; Enter with a highlight
  adds a catalog tag.
- Picker `++` / empty chrome tests still pass.
- Failed Save icon remains plus path `M12 6.5v11M6.5 12h11`.

---

## 10. Completion, report, and expiry

Write the exact Meta report path in Section 1.

Begin with `### Report for ORCHESTRATOR_CHAT` and echo once:

```text
Logical whole identity: framenest-x-companion-save-category-mvp
Worker session ordinal: 04
Worker exchange ordinal: 01
```

Include terminal status; `Phase-qualified result: implementation-PASS` only
if tests are green; `Logical-whole closure: not-closed`; handshake; baseline
and final HEAD; changed paths; commands and exit codes; proof picker/Attach
untouched in behavior; R1 note on the Edit-image relocate; Near-Misses;
Pre-Existing Failure Classification; justification `new-mutation`; expiry;
smallest next step (Orchestrator: Michal Reload-unpacked look).

PASS: Section 3 landed; `node --test` pair green; one commit; no push.

After the terminal report, stop. Plan UI or chat `Continue` does not renew
this authority.

Slovak capsule data for Orchestrator presentation (not Worker instructions):

```text
🆕 PROMPT PRE FRESH WORKERA 04 • EXTRA HIGH 🧠🧠🧠
Native Plan Mode musí byť vypnutý.
▶️ Otvor nový Worker chat, vypni Plan Mode, vlož tento súbor, počkaj na 04_report_00.md.
📦 Prompt: /home/agile/meta/projects/framenest/03/06-framenest-x-companion-save-category-mvp/04_correction_00.md
📦 Report: /home/agile/meta/projects/framenest/03/06-framenest-x-companion-save-category-mvp/04_report_00.md
✅ Archival: wait-for-report
```
