# Fresh Agent Orchestrator Handoff

## FrameNest × X Companion — Save video, GIF, and static image with category

This is the complete handoff for a fresh, terminal-capable Agent Orchestrator.
Load **this file only**. It is not a Worker prompt, not a repository plan, not
a mutation receipt, and not a substitute for the pinned Analytic Programming
protocol.

The predecessor Orchestrator session (logical whole
`framenest-x-companion-sidebar-web-mvp`) ended by Cooperator instruction on
2026-08-20 after NUC was cut over to public `main` `045f33b` / Alembic
**0029**, after Gallery 📎 was live-accepted, and after in-page picker
search-first work landed locally. The Cooperator then asked for one more
bounded picker correction (Worker 09: hide empty Attach chrome; `++` opens
the picker) and, **after that logical whole**, a fresh Orchestrator whose
**first product work** is X companion **Save/download** for **video, GIF, and
static image**, with **content-category selection**, because the user does
not only save memes.

If a later explicit Michal message contradicts this file, the later message
wins. Record the conflict. Do not paper over it.

```text
STOP: Do not execute 03/03, 03/04, or 03/05 00_handout*.md as current authority.
STOP: Do not continue Save-alias Worker ordinals 05+ under 03/04.
STOP: Do not start infinite sidebar redesign. Green side-panel chrome is accepted.
STOP: Do not treat Surface A Save freeze in 05/00_handout_01 §11.1 as still
      binding for THIS whole. Cooperator 2026-08-20 superseded it (record below).
STOP: Do not edit ADR-0062 or ADR-0063 in place.
STOP: Do not run ~/framenest_routine.fish to paper over schema or origins.
STOP: Do not enable NUC companion origins or x_acquisition_root without an
      exact later grant. Live NUC Save is still fail-closed.
STOP: Do not hide static X photo acquisition (X_NO_SUPPORTED_MEDIA / yt-dlp
      type != photo) inside a category dropdown.
STOP: Do not implement product code yourself by default. Issue Workers.
STOP: Do not close parent wholes
      framenest-x-meme-browser-companion-mvp or
      framenest-x-companion-save-alias-mvp.
```

---

## 0. Handoff identity

```text
Role: ORCHESTRATOR
Orchestrator session target: fresh-agent-orchestrator-session
Orchestrator profile: terminal-capable repository and operations coordinator
Orchestrator handoff artifact: 03/06-framenest-x-companion-save-category-mvp/00_handout.md
Predecessor Orchestrator artifacts (historical evidence only):
  03/03-framenest-x-meme-browser-companion-mvp/00_handout.md
  03/03-framenest-x-meme-browser-companion-mvp/00_handout_01.md
  03/05-framenest-x-companion-sidebar-web-mvp/00_handout.md
  03/05-framenest-x-companion-sidebar-web-mvp/00_handout_01.md
Logical whole identity (after Stage 0): framenest-x-companion-save-category-mvp
Predecessor whole (must finish or be Cooperator-closed first):
  framenest-x-companion-sidebar-web-mvp
Current phase: restore; finish picker Worker 09 if not live-accepted;
  then plan and implement X Save for video/GIF/static with category
Native planning mode: required for the Save-category whole (Planner Extra High)
Reasoning recommendation: extra-high
Internal delegation posture: one accountable active Worker by default
Cooperator: Michal
Primary consumer repository: cisarik/framenest
Pinned protocol repository: cisarik/ap through the FrameNest .ap gitlink
External analytic trace: cisarik/meta
Parent logical wholes (not closed):
  framenest-x-meme-browser-companion-mvp
  framenest-x-companion-save-alias-mvp
Predecessor whole 05: close only after picker live-accept (Worker 09)
This whole: not-started until Stage 0 is done
```

Michal selected Extra High. Do not silently downgrade. Do not use Max unless
he later selects it.

Native Plan Mode is **off** for leftover sidebar Worker 09 (bounded
correction). Native Plan Mode is **on** for the Save-category Planner.
Do not issue a Save implementation Worker from a frozen 05 prompt or from
this file alone.

Do not ask Michal to re-select the companion product. He already chose to
continue the Brave extension. Do not ask him to re-tell the 2026-08-17
brainstorming; it is classified below. Do ask him to accept or reject the
**Stage 0 vs Stage 1** sequence if restore shows Worker 09 already
live-accepted.

---

## 1. Mission

Restore exact local, public, AP, Meta, and NUC state. Then:

**Stage 0 — finish the sidebar picker whole if it is not done.** Worker 09
is already issued (see §8.3). If `09_report_00.md` is missing, or picker
empty-chrome / `++` is not Cooperator-accepted, stay on
`framenest-x-companion-sidebar-web-mvp`. Do not open Save category inside
that correction. Do not restyle the side panel.

**Stage 1 — this whole.** After Michal live-accepts the picker (or
explicitly closes 05 with residual), select
`framenest-x-companion-save-category-mvp` and run a **Planner Extra High**
whose first product slice is: **download/save from X of video, GIF, and
static image, with an explicit content-category choice**, keyboard-first,
user-friendly, without turning Save into a second Gallery.

Your job is:

1. restore against repository, public refs, and NUC `framenest-release
   status`, not this handoff alone;
2. keep the three surfaces distinct (§11) except the explicit Surface A
   unfreeze for category + media-kind honesty;
3. refuse to hide new Save scope inside leftover sidebar polish;
4. treat unpublished local commits `7e854d2` + `d8f0fc9` + (Worker 09 SHA)
   as **not on NUC** until a later publication grant;
5. carry §14 backlog so later Orchestrators do not lose alias-in-Gallery,
   language, Analyze-after-catalog, NUC companion origins, or R3;
6. keep Michal in the loop at Planner accept, implementation grant,
   publication, origins, and closure.

Do not start an AP upgrade. Do not close either parent whole.

---

## 2. Why this is a new whole (and why 05 must end first)

Implementation PASS ≠ publication PASS ≠ deployment PASS ≠ production
acceptance ≠ ORCHESTRATOR closure.

| Gate | State at handoff issuance (2026-08-20 evening) |
|---|---|
| Whole 05 Planner 01 | PASS; Michal `Prijimam` |
| Whole 05 Workers 02–05 | on public `main` `045f33b` |
| Whole 05 Worker 06 | NUC cutover `045f33b` / DB **0029** / service active (Worker claim; re-verify) |
| Whole 05 Worker 07 | local `7e854d2` Gallery 📎 ACK; Cooperator live-accepted 📎 |
| Whole 05 Worker 08 | local `d8f0fc9` search-first picker; live empty-chrome still defective |
| Whole 05 Worker 09 | **issued**; may be in flight or not yet accepted |
| Independent INFOSEC R3 | never issued (sidebar bridge + overlay + Save) |
| Public `main` | `045f33b44897a6f3949cc515792336396f1d33a1` |
| NUC `active_release` | claimed `045f33b` (Worker 06); re-verify |
| NUC companion origins | **empty** — two X POSTs fail-closed |
| `x_acquisition_root` | empty — `POST /api/x/requests` → `X_REQUEST_NOT_CONFIGURED` (503) |
| Whole 05 closure | **not-closed** until picker accept |
| This whole | **not started** |

Do not continue infinite picker redesign after Worker 09 is accepted. The
Cooperator named the next product slice: Save/download with category.

---

## 3. Role model

### 3.1 COOPERATOR

Michal is the COOPERATOR and strategic owner.

He owns feature intent, visible UX acceptance, signed-in Brave/X actions,
credentials, irreversible operations, material privacy trade-offs, model
choice, and any explicit scope reduction.

He tests the unpacked companion after Reload in `brave://extensions`. Do
not demand a whole-product PASS/FAIL checklist. He is glad to brainstorm.
Present one honest recommendation plus real alternatives at material forks.
Do not turn brainstorming into a questionnaire. Do not wait for him to
invent filenames or SHAs.

Green OS-like side-panel chrome is **accepted**. In-page picker is quick
attach, not a second Gallery. Reply Attach `+` stays a float; do not inject
into the X text row.

### 3.2 Agent Orchestrator

Terminal access is capability, not unlimited authority. You may restore
read-only, write exact Worker prompts and Orchestrator trace artifacts,
review reports as claims, and — when he again says so — execute bounded
Git/NUC operations yourself without a Worker.

You do not implement extension, FrameNest web, Alembic, or NUC config by
default. You do not paste giant Worker prompts into chat when the Meta file
exists. Communicate with Michal in Slovak (masculine for him; feminine for
yourself). Repository artifacts stay professional English. Czech forbidden.

### 3.3 WORKER

One accountable Worker at a time. `Approve`, `Yes`, `Build`, `Continue`, or
this file do not grant implementation or deploy authority.

Cursor/AppImage ambient Python is untrusted. Python evidence goes through
`./.ap/ap project check` and `./.ap/ap exec` with an exact authorized
`--baseline`. JS tests: `node --test`. NUC SSH only through
`scripts/operator/network/framenest_nuc_worker_gate.fish` (`--probe` first).
Routine NUC updates use only `deploy/ubuntu/framenest-release`. Workers
must not run `sudo -v`.

---

## 4. Communication and Cooperator presentation

Use these signals:

- 🆕 — fresh Worker or fresh Orchestrator session
- ♻️ — exact current Worker session
- 💡 — Native Plan Mode must be enabled; show only when the routed mode is on
- 🧠🧠🧠 — Extra High
- ✅ — accepted state or completed gate
- ⚠️ — material risk, conflict, or Cooperator decision
- 🧊 — explicitly parked scope
- ▶️ — exact next action
- 📦 — exact prompt/report/archive file

When Native Plan Mode is off, omit 💡 and say it must be disabled.

Human-facing command blocks:

```text
# [MacBook / fish]
```

```text
# [NUC / bash]
```

Every such block ends with:

```text
#------------------------------------------------------
```

Do not mix workstation and NUC commands in one unlabeled block. Do not
print `SSH_AUTH_SOCK`, identity paths, Tailscale hostnames, IPs, or sudo
passwords. Do not reconstruct `gpgconf` in Worker reports. Live origin
shape is `https://<node>.<tailnet>.ts.net` with no path — never write the
real values into artifacts.

---

## 5. Durable context rule

RF-19 precedence:

1. governing AP pin;
2. canonical repository and current external truth;
3. accepted durable decisions;
4. optional trace;
5. tentative narrative, including this handoff.

Required reading: FrameNest `AGENTS.md`, `.ap/AP.md`, `.ap/AP_ORCHESTRATOR.md`,
`.ap/PROMPT_CONTRACTS.md`. Workers also read `.ap/AP_WORKER.md` and
`docs/WORKER_EXECUTION_CONTRACT.md`.

---

## 6. Workspace and expected topology

```text
Canonical FrameNest: /home/agile/Projects/framenest
Expected branch: feat/x-meme-browser-companion (no upstream; expected)
Do not switch local main (stale pointer 3cf22b8… on purpose)
Meta: /home/agile/meta
AP pin (.ap gitlink): 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
```

Unpacked extension origin (committed `extension/manifest.json` `key`):

```text
chrome-extension://omiihmnlkmieaafaphohakcgmbggppap
```

Private key (gitignored, never print): `private/companion-extension.pem.key`.

Origin grant must match `https://*.ts.net`. Local `http://127.0.0.1` cannot
be the companion origin.

---

## 7. Do not load or execute

```text
/home/agile/meta/projects/framenest/03/03-framenest-x-meme-browser-companion-mvp/00_handout.md
/home/agile/meta/projects/framenest/03/03-framenest-x-meme-browser-companion-mvp/00_handout_01.md
/home/agile/meta/projects/framenest/03/04-framenest-x-companion-save-alias-mvp/
/home/agile/meta/projects/framenest/03/05-framenest-x-companion-sidebar-web-mvp/00_handout.md
/home/agile/meta/projects/framenest/03/05-framenest-x-companion-sidebar-web-mvp/00_handout_01.md
```

`03/05/00_handout.md` is Planner-era seed. `03/05/00_handout_01.md` was
current for the sidebar/NUC Orchestrator. Both are **historical** once you
are executing this file. Use them as evidence, not as live grants.

Do not create `00_handout.md` under `03/04`. Do not continue that whole’s
Worker ordinals.

---

## 8. Meta chains (historical evidence, not authority)

### 8.1 Companion MVP (parent, not closed)

```text
/home/agile/meta/projects/framenest/03/03-framenest-x-meme-browser-companion-mvp
```

### 8.2 Save / alias MVP (parent, not closed)

```text
/home/agile/meta/projects/framenest/03/04-framenest-x-companion-save-alias-mvp
```

Planner + Workers 01–04: overlay 0029, Save iframe, Search tags, Description,
admin **Save and analyze by AI** (save-only). Independent R3 never issued.

### 8.3 Sidebar web MVP (predecessor; close after picker accept)

```text
/home/agile/meta/projects/framenest/03/05-framenest-x-companion-sidebar-web-mvp
```

```text
00_handout.md / 00_handout_01.md     historical Orchestrator authority
01 … 05                             side panel + Connect in Settings → 045f33b
06_operations_00.md / 06_report_00.md
                                    NUC path A: deploy exit 13 → exact-path lock
                                    delete (keep NEW_TREE) → migrate 0028→0029
                                    from NEW_TREE → rollback --yes
07_correction_00.md / 07_report_00.md
                                    7e854d2 startAttach waits for ACK
08_correction_00.md / 08_report_00.md
                                    d8f0fc9 search-first compact picker
09_correction_00.md / 09_report_00.md
                                    hide empty Attach chrome; ++ opens picker
                                    (issued 2026-08-20; may still be in flight)
```

Worker 09 prompt (Stage 0, Extra High, Plan Mode **off**):

```text
/home/agile/meta/projects/framenest/03/05-framenest-x-companion-sidebar-web-mvp/09_correction_00.md
```

If that file exists and `09_report_00.md` does not, the next action is a
**fresh Worker chat** with that prompt, not a Save Planner.

---

## 9. Verified state at handoff issuance

Re-verify. These values were observed 2026-08-20 on the CachyOS workstation
and claimed by Worker 06 for NUC. They can go stale.

### 9.1 FrameNest (directly observed this predecessor session)

```text
Canonical checkout: /home/agile/Projects/framenest
Branch: feat/x-meme-browser-companion (no upstream configured; expected)
HEAD: d8f0fc96098d343ecc6bd781bc1b2ff7a98aa327
Parent: 7e854d251af841b3ef4a2ddaf130081e330c6f8d
Subject: fix: make in-page meme picker search-first and compact
Working tree: clean (before Worker 09)
Local main pointer: 3cf22b8aaff61ed71093207d5b24aae622f394ac
  (stale on purpose; do not switch local main)
.ap gitlink / checkout: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Alembic head on published SHA 045f33b: 0029
```

Unpublished locally (not on public `main`, not on NUC):

```text
7e854d251af841b3ef4a2ddaf130081e330c6f8d  fix: wait for composer ACK before claiming Gallery Attach
d8f0fc96098d343ecc6bd781bc1b2ff7a98aa327  fix: make in-page meme picker search-first and compact
<Worker 09 SHA when it lands>
```

Public / origin `main`:

```text
045f33b44897a6f3949cc515792336396f1d33a1
```

NUC still serves **`045f33b`**. Picker `++`, empty-chrome fix, and Gallery
📎 ACK are **not** on the live origin until a later non-force publication
and routine same-schema `framenest-release` of that new SHA. Worker 07+08
are same-schema relative to 0029 (no Alembic). Do not assume they need
migrate. Do not `deploy --yes` a SHA whose packaged Alembic head is not
0029 without a new schema grant.

`c5904b4` Attach float is Cooperator-accepted. Do not inject Attach back
into the X text row.

`companion_host.js` first appears in `00fcf2c` (after overlay `0029`). Live
NUC now has that web. 📎 is live-accepted.

### 9.2 AP

```text
Pinned FrameNest .ap: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
```

Do not mutate AP. Ledger `docs/AP_UPGRADE_OBSERVATIONS.md` stays parked.

### 9.3 NUC / production (Worker 06 claim; re-verify)

Worker 06 `deployment-PASS` (not production-acceptance, not independent
acceptance):

```text
active_release: 045f33b44897a6f3949cc515792336396f1d33a1
database_revision: 0029
service_active: active
backup_restore_readiness: ready
```

Companion origin allowlist was **not** written. Empty
`FRAMENEST_COMPANION_EXTENSION_ORIGINS` remains fail-closed for
`POST /api/x/requests` and `POST /api/x/requests/{claim_id}/retry`. Empty
`x_acquisition_root` still yields `X_REQUEST_NOT_CONFIGURED`. **Live Save
against NUC is still expected to fail closed** after this whole’s UI lands,
until a later exact host-config grant.

Gallery 📎 of already-cataloged library does **not** need those origins.

Privilege lifecycle: Cooperator `sudo -v`; Worker `sudo -n` then `sudo -K`.
Password-required after predecessor `sudo -K` is expected, not a broken NUC.

Routine updates use exactly:

```text
Poetry: /opt/framenest/tooling/poetry/2.4.1/.venv/bin/poetry
CPython: /opt/framenest/tooling/python/cpython-3.13.14-linux-x86_64-gnu/bin/python3.13
```

`uv` is not part of routine release update. Deployed releases have no
`.git`; provenance is `.framenest-release-sha` and
`.framenest-release-manifest.json`.

---

## 10. Session thread — do not lose this

### 10.1 Product destination

Michal’s library life is the **FrameNest website**, not a second companion
UI. The side panel hosts the real site. Overlay aliases exist so each
authenticated user can keep their own title, tags, and description without
ordinary `metadata.canonical.write`. Gallery still shows **canonical**
metadata. The overlay editor is backlog (§14.1).

He wants FrameNest **usable in practice**, keyboard-friendly, not an
infinite redesign.

### 10.2 What is already finished (do not redo)

- MV3 companion; SW is the only FrameNest client **from X**; no CORS;
  no `all_urls`; no `externally_connectable`.
- Side panel hosts real FrameNest web; green OS chrome; Settings under the
  title bar; Connect in Settings.
- Gallery 📎 top-left + open-original bottom-right when companion-hosted.
  Ordinary tabs: no 📎. `startAttach` waits for composer ACK (`7e854d2`).
- Save popup (Surface A) exists: Title, Description, Search tags, header X,
  **Save and analyze by AI** (admin, save-only) + **Save**.
- In-page picker (Surface B) is quick meme attach, not Gallery-wide search.
  Cardano missing from picker is **correct** (Meme audience).
- Public `main` `045f33b`. NUC cutover to that SHA / 0029.

### 10.3 What Michal live-accepted

- Reply Attach `+` float (`position: fixed`), not in the X text row.
- Green side-panel chrome.
- Gallery 📎: shell Attached and the meme appears in Post your reply.
- Save `+` on eligible X media tiles (GIF / video / image overlay).
- Picker as search, not a second Gallery. **All kinds** removed (Worker 08).

### 10.4 What Michal rejected or reversed

- Silent Save-from-X on hover `+`.
- Injecting Attach into the X composer text row.
- Canonical tag checkbox forest.
- Cancel as a bottom button (header X instead).
- Dropping Description from Save.
- Side panel as a wider clone of the in-page picker.
- Replacing Gallery open-original with 📎 (he wanted **both**). ADR-0063
  still says “replace”; **do not edit ADR-0063 in place**. Live code and
  Cooperator intent win.

### 10.5 2026-08-20 picker brainstorm (Stage 0; Worker 09)

Live empty picker still painted **"Type to search memes"** and a green box
with **`<` `>`** and **Attach** with no hit. CSS:
`.picker-preview { display: flex }` overrides `[hidden]`.

Required (Worker 09, already prompted):

- no instructional empty-search copy;
- hide preview chrome (arrows + Attach + box) until a hit exists;
- arrows + Left/Right only when cycling two or more hits;
- compact host when chrome is hidden;
- typing **`++`** in Post your reply opens the picker, consumes the token,
  does not trigger on `C++`, does not toggle-close if already open.

Do not expand Stage 0 into Save.

### 10.6 Material conflict — Surface A category freeze SUPERSEDED

`03/05/00_handout_01.md` §11.1 froze Save:

> no category picker; content category is fixed X

ADR-0062 frozen surfaces likewise keep Gallery/Details canonical and do not
define a Save category control. ADR-0062 decision 4 lists Title,
Description, Tags, Save, Cancel — no category field.

**Cooperator 2026-08-20 (later message wins):** the next Orchestrator’s
first work is Save/download of **video, GIF, and static image**, and the
user must be able to **choose a category**, because they do not only
download memes.

Record: this is a **changed objective** relative to the 05 freeze. It is
why this is a **new logical whole**, not Worker 10 of sidebar polish. Do
not edit ADR-0062 in place. Planner may recommend a new ADR or a
superseding decision record. Do not paper over the conflict by adding a
dropdown “quietly”.

---

## 11. Three surfaces (updated)

There are three surfaces. They are not the same product.

### 11.1 Surface A — Save popup on X media — UNFROZEN for this whole

Trigger: hover/focus green `+` at the bottom-right of an eligible X media
tile.

Current live contract (verify files; then Planner changes this whole):

- closed-shadow iframe WAR `ui/save.html`;
- Title, Description (`textarea` 10000), Search tags + pills;
- no Cancel button; no checkbox tag forest; **no category picker today**;
- admin: **Save and analyze by AI** left of Save; click still only
  `SAVE_POST` + alias; no analysis HTTP; no new `companion_mutation`;
- alias rides existing `POST /api/x/requests`;
- failed Save remains a plus glyph with danger border;
- Analyze **execution** after catalog remains backlog.

**This whole’s job** is to make Save honest for **video, GIF, and static
image**, and to let the user pick **content category** at save time, with
keyboard-first UX (Tab/arrows/Enter/Escape; no mouse-only dead-ends).

Category is **canonical classification at acquisition**, not the per-user
alias overlay (alias is title / description / tag_keys). Do not stuff
category into alias to dodge `metadata.canonical.write`. X Save already
writes canonical `content_category` via `default_x_category` at catalog
handoff.

Repository truth today (`src/framenest/domain/x_acquisition.py`
`default_x_category`):

- `XMediaType.IMAGE` → `ContentCategory.GENERAL`
- otherwise (video, animated_gif) → `ContentCategory.MEME`

So GIF/video from X are always cataloged as Meme unless the Planner
replaces that default with an explicit user choice. Static **supported**
image assets, if any reach catalog, already default to General. That is
**not** the same as ordinary X **photos**.

### 11.2 Surface B — In-page Attach / Search memes — FROZEN after Worker 09

Quick attach only. Meme-category API. Floating `+`. After Worker 09: empty
search has no Attach chrome; `++` opens picker. Do not replace with the
website. Do not widen to Gallery-wide search in this Save whole.

### 11.3 Surface C — Side panel — FROZEN

Green OS chrome, Settings under title bar, Connect in Settings, hosted
Gallery 📎. Do not restyle unless Michal attaches a live defect.

---

## 12. Companion architecture that must survive

Do not reopen this without a material contradiction and a new ADR.

1. One unpacked Manifest V3 Chromium companion under `extension/`.
2. Service worker is the only FrameNest network client **from X**. Content
   scripts match only `https://x.com/*` and `https://twitter.com/*`.
3. Service worker has no X host permission.
4. X messages use `v: "framenest.companion.v1"`; web bridge uses
   `v: "framenest.companion.web.v1"`; unknown versions/types drop.
5. Content scripts send opaque ids and validated post URL strings. They
   must not `fetch` FrameNest or `pbs.twimg.com`.
6. No CORS. Empty companion-origin allowlist is fail-closed **on the two
   X POSTs**.
7. `RoutePolicy.companion_mutation` is true **only** for
   `POST /api/x/requests` and `POST /api/x/requests/{claim_id}/retry`.
   Category-on-Save should ride the existing POST if a body field is added;
   do not invent a third companion_mutation route without a Planner
   decision and ADR.
8. Picker `GET /api/x/companion/media` stays **meme** image / animated
   image / video. Save-category work must not silently widen picker
   audience.
9. Attach: SW fetches content with `X-FrameNest-Request: 1`. 32 MiB cap.
   Never click Post.
10. WAR exposes picker + save HTML/CSS/JS to x.com and twitter.com only.
11. Canonical metadata stays one row per `media_id`. Overlay is ADR-0062.
12. Analyze by AI cannot run on uncataloged bytes.
13. Accepted ADRs are not edited in place.
14. Independent INFOSEC R3 remains recommended and **never self-certified**.

---

## 13. First work of THIS whole (after Stage 0)

### 13.1 User-facing objective

From X, the user can save **video, GIF, and static image** into FrameNest
and **choose what the item is** (category), because the library is not
memes-only. The flow must be usable **from the keyboard**.

This is **acquisition + Save UX**, not Gallery redesign and not picker
redesign.

### 13.2 Honest media-kind blocker (do not hide)

Pinned extractor (`src/framenest/infrastructure/x/downloader.py`) filters
`m['type'] != 'photo'`. Ordinary static X photographs **do not emit**
supported assets. Photo-only posts terminate as typed
`X_NO_SUPPORTED_MEDIA`. Comments in that adapter say static photo
acquisition is deferred and **not** part of the current real contract.

`default_x_category` talking about `XMediaType.IMAGE` does not mean X
photos already download.

Planner must split:

- **A. Category selection** for media the pipeline already acquires
  (video, animated GIF, and any still image the extractor actually emits);
- **B. Making ordinary X photos downloadable** (extractor/contract change;
  likely a real implementation slice, tests, and possibly INFOSEC/privacy
  review — still no CORS, still SW-only from X).

Do not ship a category dropdown that pretends photos save when the claim
fails `X_NO_SUPPORTED_MEDIA`. If Michal wants photos in the same whole,
the Planner must say so as an explicit decision boundary, not as UI
theatre.

Hover **+** may already appear on tiles that later fail. Honesty in the
Save popup / failed-glyph path is in scope for this whole if photos stay
included.

### 13.3 Classification tensions the Planner must resolve

`ContentCategory` today: `general`, `meme`, `movie`, `youtube`
(ADR-0045 + ADR-0055). Acquisition source is orthogonal.

Tensions (recommend; do not silently decide in an implementation Worker):

- Offering **youtube** on an X permalink is probably wrong. Escalate.
- Offering **movie** may imply movie genres (ADR-0045). Do not add a genre
  checkbox forest. If movie is offered, Planner says how genres are
  deferred.
- Ordinary users lack `metadata.canonical.write`. Setting category **at
  X ingest** is the legitimate write path (same as today’s default). Later
  Gallery canonical pencil stays admin.
- Alias overlay must remain title/description/tags, not a second category.
- Keyboard: category control must be reachable without a mouse; Escape
  still closes Save; Enter should not accidentally Save while the user is
  only moving category.
- Do not reopen tag checkbox forest. Search tags stays.
- Admin **Save and analyze by AI** stays save-only unless a later whole
  grants analysis execution.
- NUC Save still fail-closed without origins. Local loopback evidence is
  the implementation path unless Michal grants origins.

### 13.4 Recommended first Worker after Planner accept

Not specified here — the Planner names allowlist, schema vs same-schema,
tests, and whether photos are in or parked. Default recommendation:

1. Planner Extra High, Native Plan Mode **on**, one targeted revision
   allowed.
2. One implementation Worker Extra High, Plan Mode **off**, no push, no
   NUC origins unless granted.
3. Cooperator live-accepts Save UX on unpacked extension against a
   configured origin (loopback or, if granted, NUC).

Publication of 07+08+09 (picker) may happen before or after Save work;
it is a **separate grant**. Do not mix push into the first Save Worker.

### 13.5 Suggested later identity (already selected by this file)

```text
framenest-x-companion-save-category-mvp
Meta: /home/agile/meta/projects/framenest/03/06-framenest-x-companion-save-category-mvp
```

This file is `00_handout.md` in that directory. Worker ordinals start at
01 for the Planner.

---

## 14. Backlog (parked; do not hide inside Save-category)

### 14.1 Gallery per-user alias editor (horizon)

Suggested later identity: `framenest-gallery-per-user-alias-editor-mvp`.

### 14.2 Settings → General → Language (horizon)

### 14.3 Analyze by AI execution after catalog (horizon)

### 14.4 Picker / Gallery reading the caller’s alias (horizon)

### 14.5 Other parked items

```text
🧊 NUC FRAMENEST_COMPANION_EXTENSION_ORIGINS and x_acquisition_root
🧊 Independent INFOSEC R3 (sidebar bridge + overlay + Save)
🧊 Per-asset Save targeting (one alias per permalink today)
🧊 Web Store packaging / rotating the extension key
🧊 AP upgrade ledger (docs/AP_UPGRADE_OBSERVATIONS.md)
🧊 Closing parent wholes 03 and 04
🧊 Desktop app, Cover Studio, collections, sync, second-copy backup
🧊 Persistent AI drafts as a product
🧊 Public Internet / VPS exposure
🧊 Signed-in X scraping, DMs, cookies
🧊 Settings language tab
```

Static X photographs are **not** automatically parked: they are a candidate
decision boundary **inside this whole** (§13.2). If the Planner parks them,
record that explicitly.

---

## 15. Product boundaries that stay frozen

- FrameNest remains local-first. Loopback-first X APIs. No router port
  forwarding. Remote access remains Tailscale-only.
- Premium Gallery remains a flagship invariant. Ordinary-tab Gallery and
  Details visual behavior stay frozen except the named extension-context
  Attach control.
- Service worker remains the only FrameNest network client from X.
- Companion never posts on the user’s behalf.
- Empty companion-origin allowlist remains fail-closed on the two X POSTs
  until an exact grant writes it.
- Original server media under `/srv/media` stays read-only to the service.
- Manual-first metadata (ADR-0023).
- Helper never runs `framenest-db migrate` and never hides migration
  authority.

---

## 16. Restore then route (your first hour)

Do not implement. Do not issue a Save Worker before restore + Stage 0.

1. Read `AGENTS.md` and pinned AP docs. Confirm `.ap` gitlink
   `9c5cc44f…`.
2. Verify FrameNest branch, HEAD, unpublished commits, public `main`.
3. Read Worker 09 report if present; compare picker files to §10.5.
4. If Worker 09 is not PASS or not live-accepted: present the existing
   `09_correction_00.md` capsule; do not write a second overlapping picker
   prompt unless the report is BLOCKED on a real defect in that prompt.
5. Re-verify NUC `framenest-release status` only if you need live facts
   (gate `--probe` first). Do not deploy.
6. Present restored state in Slovak: Stage 0 vs Stage 1; unpublished SHAs;
   Save still fail-closed on NUC; photo extractor gap.
7. After picker accept, close whole 05 with Cooperator (residual: R3,
   unpublished picker commits, empty origins). Then issue Planner 01 of
   **this** whole.

Do not ask Michal to invent the whole identity. It is already
`framenest-x-companion-save-category-mvp`.

---

## 17. Closure of this whole (later)

Do not close until:

- Planner accepted;
- implementation Worker PASS;
- Cooperator live-accepted Save UX for the media kinds the plan included;
- residuals (photos parked or shipped; origins still empty; R3 still
  unissued; unpublished SHAs) are named honestly.

Parents stay open.

---

## 18. Slovak capsule for the next human session

```text
🆕 FRESH AGENT ORCHESTRATOR • EXTRA HIGH 🧠🧠🧠
📦 Jediný handoff: /home/agile/meta/projects/framenest/03/06-framenest-x-companion-save-category-mvp/00_handout.md
▶️ Najprv restore. Ak Worker 09 (picker ++ / prázdny Attach) nie je live-accept, dokonči 05.
💡 Save-category Planner: Native Plan Mode zapnutý.
Native Plan Mode pri Workerovi 09 musí byť vypnutý.
```
