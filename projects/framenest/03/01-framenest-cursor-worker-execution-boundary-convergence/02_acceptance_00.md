# FrameNest — Worker 02 independent acceptance: Cursor Worker execution boundary

🆕 **PROMPT FOR FRESH WORKER 02 • HIGH REASONING**

**Native Plan Mode: OFF.** Work directly. Do not spawn subagents. Do not ask
for another Worker. Do not close the logical whole.

You are one fresh WORKER instance under Analytic Programming. You are not the
ORCHESTRATOR. If this chat implemented candidate `fc355d6…`, or was Worker 01
of this whole, stop `BLOCKED` before inspection.

The NUC-release whole
`framenest-repeatable-immutable-nuc-release-deployment-contract` is
**CLOSED: PASS**. Do not reopen it. Do not deploy, roll back, or mutate the
live NUC.

```text
Persistent role identity: WORKER
Logical whole identity: framenest-cursor-worker-execution-boundary-convergence
Worker session ordinal: 02
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Native planning mode: not-used
Worker session profile: Fresh Independent Audit
Phase: Independent Audit / acceptance
Task identity: FN-CURSOR-WORKER-EXEC-BOUNDARY-ACCEPT-02
Reasoning recommendation: High
Recommendation basis: independent acceptance of a trust-boundary documentation and SSH-gate change; verifier must not have implemented the candidate
Automatic model selection: off
Enhanced/maximum mode: not requested
Sub-agents/internal delegation: not-used
Worker topology: single-active
Material phase gate: yes
Changed material axis: acceptance owner and evidence class
Ordinary-only trigger: no
Routing reopened for: independent acceptance
Unchanged axes reopened: none
Evidence tier: E2
Evidence tier basis: reversible repository contract/docs/gate change; no production cutover; independence required because this is the primary fresh acceptance
Combined implementation envelope: prohibited
Correction authority: none
Publication authority: none
Deployment authority: none
NUC mutation authority: none
Git authority: none
Meta write authority: none
Canonical repository mutation: none
Independent acceptance: required-fresh-independent
Acceptance independence: required-fresh-independent
Primary fresh acceptances used: 1
Automatic corrections used: 0
Correction re-acceptance: not-applicable
Named missing-evidence probe: none
```

Read this complete prompt before acting. Do not rewrite or summarize AP.

## 1. Independence gate

This prompt must reach a genuinely fresh Worker session.

You must not have participated in Worker 01 implementation of
`fc355d6e21d2f2781e0166906b453fa3fa91bdb7`. Do not reuse that conversation.
Worker 01's `implementation-PASS` is a **claim only**.

If this is not a fresh session, Native Plan Mode is active, or you materially
implemented the candidate, stop and return `BLOCKED`.

## 2. Candidate

```text
Repository: /home/agile/Projects/framenest
Public repository: https://github.com/cisarik/framenest.git
Acceptance candidate: fc355d6e21d2f2781e0166906b453fa3fa91bdb7
Acceptance tree: 00704b16a308ace5e349db1582691876e26dd613
Acceptance parent: 5abb2adfcd1d5f3391df9c3044b4b81ac1aac923
Subject: fix: bind Cursor Workers to declared AP exec and capability routes
Local branch: fix/cursor-worker-execution-boundary
Required public refs/heads/main: 5abb2adfcd1d5f3391df9c3044b4b81ac1aac923
Required AP pin: 17b7e085139e9bcbb0e4953d26aef9b6687d541c
Repository checkout topology: standalone checkout
Working directory: /home/agile/Projects/framenest
```

Public `main` is intentionally still the parent. Local `main` is stale and
must remain untouched. Do not merge, rebase, push, or fast-forward anything.
Preserve owner untracked paths; do not enumerate their contents.

Changed paths (Worker 01 claim; re-read the tree):

```text
AGENTS.md
docs/AP_UPGRADE_OBSERVATIONS.md
docs/OPERATOR_NETWORK.md
docs/WORKER_EXECUTION_CONTRACT.md
scripts/operator/network/README.md
scripts/operator/network/framenest_nuc_worker_gate.fish
tests/contract/test_operator_network_scripts.py
tests/contract/test_worker_execution_contract.py
```

`ap.project.conf` and `.ap` must be unchanged.

## 3. Protocol and trace

```text
Canonical AP identity: https://github.com/cisarik/ap.git
Immutable version identity: 17b7e085139e9bcbb0e4953d26aef9b6687d541c
Declared variant: stable
Governing variants in effect: one
Rules from non-governing variants: none
Migration required: no
External trace disposition: configured
Trace discovery: /home/agile/meta/projects/framenest/03/01-framenest-cursor-worker-execution-boundary-convergence/
Trace project key: framenest
Trace logical-whole projection identity: 01-framenest-cursor-worker-execution-boundary-convergence
Trace authority: historical-evidence-only
Trace archival owner: Cooperator Michal; Worker must not archive
Trace visibility: private
Trace companion outcome: report
Trace self-granted status: none
Expected later archival pair after the report exists: 02_acceptance_00.md + 02_report_00.md
```

```text
Orchestrator-to-Worker prompt language: professional English
Formal Worker report language: professional English
Required report header: ### Report for ORCHESTRATOR_CHAT
Logical-whole closure: not-closed
```

## 4. Mandatory reading

- root `AGENTS.md` (Cursor Worker Execution Boundary plus managed AP block)
- `.ap/AP.md`, `.ap/AP_WORKER.md`
- `.ap/PROMPT_CONTRACTS.md` Upgrade Observation Ledger Contract (field
  completeness only)
- `docs/WORKER_EXECUTION_CONTRACT.md`
- `ap.project.conf` (confirm unchanged)
- `tests/contract/test_ap_project_contract.py`
- `tests/contract/test_worker_execution_contract.py`
- `scripts/operator/network/framenest_nuc_worker_gate.fish`
- `tests/contract/test_operator_network_scripts.py` (probe/attach tests)
- `docs/OPERATOR_NETWORK.md` Cursor Worker SSH gate section
- `docs/AP_UPGRADE_OBSERVATIONS.md`
- Worker 01 report as a claim only (Meta `01_report_00.md` if present; do not
  write Meta)

## 5. Goal

Independently accept or reject candidate `fc355d6…` against the frozen
outcomes of this whole. Do not implement a correction. Do not improve prose
opportunistically. One concrete finding returns as `PARTIAL`/`BLOCKED` with
the exact defect; the Orchestrator routes any correction.

## 6. Acceptance owner map and control matrix

```text
Acceptance candidate: fc355d6e21d2f2781e0166906b453fa3fa91bdb7
Acceptance owner map: AGENTS.md (high-priority Cursor route rule); docs/WORKER_EXECUTION_CONTRACT.md (command hierarchy and classification); scripts/operator/network/framenest_nuc_worker_gate.fish (SSH probe/attach); docs/OPERATOR_NETWORK.md + scripts/operator/network/README.md (gate docs); tests/contract/test_worker_execution_contract.py and test_operator_network_scripts.py (locks); docs/AP_UPGRADE_OBSERVATIONS.md (one untriaged ledger entry)
Acceptance allowlist: the eight changed paths above; no other mutation
Acceptance risk claims: Cursor Workers use declared AP exec; encodings crash classifies as ambient-route violation and reruns once via AP exec; SSH uses the project gate without reconstructed gpgconf or printed sockets; sudo -K then password-required is expected lifecycle independent of SSH_AUTH_SOCK; AP pin and envelope unchanged; one non-authorizing ledger observation
Acceptance control matrix:
  positive: AP exec section precedes raw .venv/poetry examples; those examples are limited to a clean human development shell never rendered into Cursor Worker prompts; encodings fail-fast owned; --probe prints only ssh-agent: ready|absent; BatchMode SSH remains the NUC transport; sudo lifecycle owned in the execution contract; ledger Entry identity exact; focused tests pass through AP exec from this Cursor parent
  negative: no .ap / ap.project.conf / CPython 3.14 / .venv rebuild / uv/pip/poetry repair; no second SSH stack; no sudoers/NUC/GPG/key/rc/Cursor edits; no push/publication/deploy; no live BatchMode true required; secrets omitted
Acceptance independence: required-fresh-independent
```

## 7. Required checks

Work on the candidate checkout. Do not create a worktree unless the current
HEAD is already `fc355d6…` (expected). If HEAD differs, stop `BLOCKED`.

### 7.1 Identity

- `HEAD == fc355d6e21d2f2781e0166906b453fa3fa91bdb7`
- tree `00704b16a308ace5e349db1582691876e26dd613`
- parent `5abb2ad…`
- public `main` still `5abb2ad…`
- `.ap` gitlink `17b7e085…`
- `ap.project.conf` byte-identical to parent for this file
- tracked tree clean aside from pre-existing owner untracked paths
- diff vs parent is only the eight allowlisted paths

### 7.2 Semantic review (direct, not a second implementation)

Confirm:

1. `AGENTS.md` states Cursor/AppImage is untrusted; Python/tests through
   `./.ap/ap exec`; NUC SSH through the project gate; sudo lifecycle is
   Cooperator timestamp plus Worker `sudo -K`; Workers must not `sudo -v`.
2. `docs/WORKER_EXECUTION_CONTRACT.md` presents canonical AP exec **before**
   raw `poetry run pytest` / `.venv/bin/python`. The raw examples sit under
   **Clean Human Development Shell Only** and say they must never be rendered
   into Cursor Worker prompts.
3. Encodings startup signature classifies as ambient-route violation; no
   Python inventory; rerun once through AP exec.
4. SSH: `--probe` then BatchMode form; do not reconstruct `gpgconf`; do not
   print the socket.
5. Sudo: timestamp independent of `SSH_AUTH_SOCK`; `sudo -K` then
   password-required is expected lifecycle, including with
   `timestamp_timeout=1440`.
6. Ledger: exactly one entry
   `consumer-declared-execution-and-capability-route-binding`, state
   `untriaged`, `Entry authority: non-authorizing`, header/activation
   snapshot unchanged. Accepting this FrameNest candidate does **not**
   accept or implement the AP observation.

### 7.3 Named residual — parent agent vs gate

Worker 01 left this residual: `--probe` does not export `SSH_AUTH_SOCK` into
the Cursor parent. The contract treats a parent without that variable as
expected and requires later NUC SSH to use the gate, which attaches internally.

Decide one of:

- **coherent** — future NUC helpers including `framenest-release` remain
  separately bound to the gate by Orchestrator prompts; not a candidate defect;
- **finding** — the whole required a parent-usable attach without reconstructing
  `gpgconf`, and `--probe` alone does not satisfy that for helpers that inherit
  the Worker environment.

Do not invent a third SSH mechanism. Do not "fix" it in this session.

### 7.4 Validation (dogfood the candidate route)

From this actual Cursor parent, using `--baseline` exactly
`fc355d6e21d2f2781e0166906b453fa3fa91bdb7` (execution authority only; not
publication):

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline fc355d6e21d2f2781e0166906b453fa3fa91bdb7
./.ap/ap exec --root /home/agile/Projects/framenest --baseline fc355d6e21d2f2781e0166906b453fa3fa91bdb7 --operation runtime-info
./.ap/ap exec --root /home/agile/Projects/framenest --baseline fc355d6e21d2f2781e0166906b453fa3fa91bdb7 --operation test-focus -- tests/contract/test_ap_project_contract.py tests/contract/test_worker_execution_contract.py tests/contract/test_operator_network_scripts.py -q -p no:cacheprovider
```

Also run:

```text
fish -n scripts/operator/network/framenest_nuc_worker_gate.fish
scripts/operator/network/framenest_nuc_worker_gate.fish --probe
```

`--probe` may print only `ssh-agent: ready` or `ssh-agent: absent`. Never
print the socket. Live BatchMode `true` is **not** required. No `sudo`. No
full suite.

If a raw Python invocation emits the encodings signature, classify it and
rerun once through AP exec. Do not inventory Pythons or rebuild `.venv`.

## 8. Authority

You may: read the candidate; run the commands in §7.4; classify the residual
in §7.3.

You may not: edit files; commit; push; publish; deploy; SSH a NUC command
other than the local `--probe`; use `sudo`; write Meta; mutate `.ap` or the
ledger state; close the whole.

## 9. Hard stop

Stop without improvising if independence is compromised, HEAD is not the
candidate, the diff exceeds the allowlist, AP exec cannot run `runtime-info`
from this parent after one encodings reroute, or a correction would be
required to claim PASS.

## 10. Report contract

Begin exactly:

```markdown
### Report for ORCHESTRATOR_CHAT
```

Echo unchanged: logical whole identity, session 02, exchange 01,
`fresh-worker-session`, Native planning mode `not-used`.

Include:

- `PASS` | `PARTIAL` | `BLOCKED`;
- phase-qualified result `acceptance-PASS` | `not-applicable`;
- `Logical-whole closure: not-closed`;
- candidate SHA/tree/parent; public main; AP pin;
- allowlist equality;
- semantic review of the six §7.2 items;
- §7.3 residual: `coherent` or `finding` with one causal sentence;
- AP exec and `--probe` evidence (sanitized);
- secrets omitted;
- one smallest next step (Orchestrator publication grant or one correction;
  not closure by this Worker);
- report justification `final-acceptance`;
- authority expiry;
- Near-Misses; Pre-Existing Failure Classification.

`PASS` / `acceptance-PASS` only if every positive control holds, no negative
control is violated, and §7.3 is `coherent` (a `finding` cannot be PASS).

```text
Report justification: final-acceptance
Authority expiry: all Worker 02 exchange 01 acceptance authority expires
at this terminal report
```
