# Fresh Agent Orchestrator Restoration

## FrameNest 03/10 — connect, UX re-baseline, then close (deploy is done)

You are a **fresh** terminal-capable Agent Orchestrator for FrameNest under
Analytic Programming. Load **this file completely** before any Worker prompt.
It is current authority for rotation. It is not a Worker prompt and not a
closure of the logical whole.

The predecessor Orchestrator is rotating **before Cooperator UX PASS** because
session quality collapsed (connect/test blocked; Orchestrator asked to pack
state). Do **not** pretend `framenest-companion-review-inbox-ux-history-mvp`
is closed. Do **not** reopen NUC deploy as the primary object. Deploy already
succeeded.

If a later explicit Michal message contradicts this file, the later message
wins. Record the conflict.

```text
STOP: Do not execute 03/03–03/09 00_handout*.md as current authority.
      03/09 remains not-closed; do not resume its Worker ordinals.
STOP: Do not treat 03/10 00_handout.md chrome (two lists) as still binding.
      Cooperator 2026-08-24 superseded it: one merged history.
STOP: Do not implement FrameNest product code yourself. Issue Workers.
STOP: Do not treat publication-PASS or NUC cutover as UX PASS.
STOP: Do not enable FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED.
STOP: Do not scrape ~/framenest_routine.fish, ~/nuc_update.fish, or sibling
      home wrappers for host, user, identity, sockets, or secrets.
      Do not print those values into Meta, Worker prompts, or reports.
STOP: Do not reconstruct gpgconf. Do not sudo -v. Do not handle a password.
STOP: Do not run NUC deploy/migrate from Cursor. Cooperator already did.
STOP: Do not weaken origin/public-main gates or edit home Fish wrappers
      “so unpublished SHAs deploy.”
STOP: Do not close THIS whole until Michal’s UX PASS or explicit waiver.
STOP: Do not close 03/09 or parent X-companion wholes from this restore.
STOP: Extra High. No Max unless Michal selects it. Plan Mode off except
      when you issue a Planner (then Native planning mode: required).
```

Paste seed (pointer only):

```text
Resume this AP-integrated FrameNest project as a fresh Orchestrator.
Read /home/agile/meta/projects/framenest/03/10-framenest-companion-review-inbox-ux-history-mvp/10_orchestrator_restore_00.md
completely before any Worker prompt.
Begin read-only. Restore canonical state. Do not mutate. Do not implement
product code. 03/10 is NOT closed. NUC deploy of public main
0eeaf350801e181025b271676d8f2fbb487db3d8 / schema 0032 already succeeded.
Do not make deploy the next whole. First object: why the companion cannot
Connect to the live NUC origin, then sequential UX re-baseline of merged
history / badge / stale-context / preserving Apply / Settings Save, then
close 03/10 only on Michal UX PASS. Communicate in Slovak. Feminine
self-reference. Extra High. No Max.
```

---

## 0. Handoff identity

```text
Role: ORCHESTRATOR
Orchestrator session target: fresh-agent-orchestrator-session
Cooperator: Michal
Canonical checkout: /home/agile/Projects/framenest
Meta trace:
  /home/agile/meta/projects/framenest/03/10-framenest-companion-review-inbox-ux-history-mvp/
Logical whole (open): framenest-companion-review-inbox-ux-history-mvp
Predecessor whole (open): framenest-companion-ai-review-inbox-mvp (03/09)
Parents (open, do not close): X-companion ingest/save/category wholes
Branch that carried the work: feat/x-meme-browser-companion
Public main / intended NUC release (verify live):
  0eeaf350801e181025b271676d8f2fbb487db3d8
Packaged / live schema after Cooperator cutover (verify live): 0032
AP pin (verify gitlink == .ap HEAD):
  9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Worker ordinals already used in 03/10: 01–09
Next Worker ordinal if you issue one: 10
Reasoning: extra-high
Language: Slovak with Cooperator; English in repo/Meta/Worker artifacts
Orchestrator grammatical gender: feminine
```

Re-verify HEAD, `origin/main`, `.ap` pin, and Cooperator-reported
`framenest-release status` before any grant. Do not invent SHAs.

---

## 1. What this session actually finished

Treat Worker reports as claims. The predecessor verified these against Git
and/or Cooperator paste:

| Surface | State | SHA / note |
|---|---|---|
| Merged title-bar history (analyzed green, pending dark; one list; no `#review-inbox`) | committed + published | D1 on the 7-commit FF |
| Stale `Extension context invalidated` copy | committed + published | `0c71d07` in that FF |
| First-use canonical seed `x` / `𝕏` on Save | committed + published | D2 |
| Apply preserve-and-append; overflow HTTP 409 `COMPANION_REVIEW_TAG_LIMIT_CONFLICT`; Alembic `0032` `companion_review_tag_sources`; no backfill | committed + published | D3; independent staged audit then commit Worker 06 |
| ADR-0073 + living docs; ADR-0068/0072 **bodies untouched**; index successor notes | committed + published | D4 `dba16e6` |
| Settings: title-bar Connect/Disconnect only; `#settings-save` **under** origin; disabled unless dirty | committed + published | Worker 08 `0eeaf350…` |
| Public `main` fast-forward | publication-PASS | Worker 09; 7 commits; non-force |
| NUC | **done by Cooperator, not Cursor** | `~/nuc_update.fish` then `~/framenest_routine.fish` |

NUC paste (sanitized): check of public main `0eeaf350…` against live
`6e20fc12…` / schema `0031` → `framenest-release: migration-required` (expected)
→ Cooperator accepted migrate + rollback-cutover → migrate JSON
`current_revision=0032` `head_revision=0032` → rollback-cutover complete →
status `active_release=0eeaf350…`, `service_active=active`,
`database_revision=0032`, backup ready. Routine then skipped deploy because
live NUC already equals public main.

**Do not spend this restore on “how to deploy.”** That path works. Same-schema
helper still never migrates; the home wrapper handled the schema jump with an
explicit migrate then forward cutover. Do not weaken `local HEAD == public
main`. Do not put private Tailscale hostnames, SSH sockets, or sudo material
in Meta.

---

## 2. Binding chrome (supersedes 03/10 00_handout two-list)

Cooperator 2026-08-24, still in force:

1. One merged history under the green title bar. Not unread-list plus
   analyzed-only history.
2. Color: analyzed green; pending (own Saves without successful analysis) dark.
3. Pending own-saves visible. Badge = `unopened_count` only; pending does not
   increment it. Click does not remove rows.
4. Pending overlay: no successful analysis yet; no opened mutation.
5. Save freeze: Title → Tags → Description → Save; no radios; no Analyze.
6. Apply **union**: keep stored keys, append submitted mapped AI keys;
   submitted cap 5; stored cap 32; 409 on overflow; zero-tag rule unchanged.
7. Default Save tag `x` / `𝕏`. No YouTube analogue.
8. S1: native chrome above surviving `#frame`. Never unmount the iframe.
9. Ordinary identity 403 hides history/badge; iframe/Attach stay.
10. Exactly four `companion_mutation` routes. Movie exclusion. No
    `notifications` permission.
11. Auto-analysis flag stays default/off unless Michal later grants enablement.

ADR-0073 records this. Do not edit ADR-0068 or ADR-0072 in place.

Settings (Worker 08, visually confirmed by Cooperator screenshot 2026-08-24
evening): origin field; **Save** below it; title-bar **Connect**. Help text
says Save writes settings; Connect/Disconnect attach or clear the session.

---

## 3. Immediate object (do this; do not skip to horizon)

Cooperator cannot Connect and therefore cannot finish UX. Screenshot showed
Settings open, origin filled with the NUC tailnet URL, Save visible, title-bar
Connect still showing **Connect** (session not attached).

Hypotheses to test against **repository + live behavior**, not folklore:

- Save persisted origin but session attach still requires title-bar Connect.
- Host permission / extension origin allowlist on NUC vs unpacked ID.
- Origin trailing slash vs acceptor.
- Handshake / framing timeout against Tailscale HTTPS (loopback-only bind is
  intentional; companion must use the tailnet origin, not `127.0.0.1` for NUC).
- Stale unpacked extension vs published SHA (reload unpacked from this
  checkout after Connect).
- `companion_extension_origins` / equivalent NUC env — **read-only recon
  first**. Mutating NUC env needs an exact later grant.

Issue at most one Fix Worker after recon. Extra High. No Max. Plan Mode off.
Python evidence only via `./.ap/ap project check` / `./.ap/ap exec` with exact
`--baseline`. No ambient `.venv` Python. No NUC SSH from Cursor unless a later
task names `scripts/operator/network/framenest_nuc_worker_gate.fish`.

After Connect works, sequential UX (PASS/FAIL per step, one defect at a time):

1. One history list; no `#review-inbox`.
2. Analyzed green / pending dark; click keeps the row.
3. Pending overlay; no opened call.
4. Badge = unopened only.
5. `#frame` and Attach survive.
6. Ordinary 403 hides history, not iframe.
7. Reload unpacked → specific stale-context copy, not raw invalidated error.
8. Save preselects `𝕏`; no synthetic chip if seed missing.
9. Apply preserves manual tags; receipts in existing panel.
10. Settings Save dirty/disabled; no second Connect in the sheet.

Close 03/10 only after Michal UX PASS (or waiver) on that list.

Then generate the **post-03/10 product-horizon** restoration (Section 16 of
`00_handout.md` inventory). Do not start Flag / R3 / YT+ / W2 / Movie /
Drafts / Cover from 03/10.

---

## 4. Reading order

1. `/home/agile/Projects/framenest/AGENTS.md`
2. `.ap/AP.md`, `.ap/AP_ORCHESTRATOR.md`, `.ap/PROMPT_CONTRACTS.md`
3. **This file** (current restore authority)
4. `00_handout.md` in the same Meta folder — **historical**; chrome Section 7
   two-list is superseded by Section 2 here
5. `docs/WORKER_EXECUTION_CONTRACT.md`
6. Product truth: `README.md`, `PRODUCT.md`, `SPEC.md`, `ROADMAP.md`,
   `SECURITY.md`, `SERVER.md`, `docs/X_COMPANION.md`
7. ADR-0063, 0067–0073 (0073 successor for merged history)
8. Worker reports 01–09 in this Meta folder (claims, not truth)
9. `docs/UBUNTU_NUC_DEPLOYMENT.md` + ADR-0060 (same-schema helper never
   migrates; Cooperator wrapper already performed the 0031→0032 jump)

Python: `./.ap/ap project check` / `./.ap/ap exec` with exact `--baseline`.
Companion JS: existing Node tests under `tests/*companion*extension*.test.js`.

---

## 5. Negative space

- Rebuild inbox persistence or four mutation routes
- Enable auto-NIM so the queue fills
- CORS / `all_urls` / content-script FrameNest fetch
- Ordinary-user inbox
- Notifications permission
- Ingest Save overlay polish
- Force-push, unpublished SHA deploy, home-script rewrites
- Closing parents “because 03/10 shipped”
- Horizon wholes as drive-by scope

---

## 6. Horizon table (selection later; do not start)

| Id | Whole | Notes |
|---|---|---|
| **Flag** | NUC enable automatic media analysis | Ops grant; cost; not chrome |
| **R3** | Independent INFOSEC R3 of companion_mutation expansion | Parked 03/09 |
| **Parents** | Visual closeout of parent X-companion wholes | Ingest Save frozen |
| **Notify** | `notifications` permission | Parked; badge exists |
| **YT+** | YouTube page `+` | Parked |
| **W2** | Meme-as-tag / still-short-movie | FROZEN |
| **Movie** | Movie identification companion | FROZEN |
| **Drafts** | Multi-model drafts | FROZEN |
| **Cover** | Cover Studio completeness | Still required later |
| **Park** | Stop / more evidence | Always legal |

Find frozen/parked language in `ROADMAP.md`. Continuation bootstrap if Michal
has no next whole: read AGENTS.md + pinned AP; restore; select exactly one
bounded whole before mutation.

---

## 7. Predecessor note (non-authorizing)

03/10 implementation, docs, publication, and NUC schema 0032 cutover are
done. The owner still cannot use the companion against the live server, so
the chrome object is unfinished. Your job is attach + patient UX, then close
this whole honestly, then hand the product horizon forward.

Thank Michal. He is the Cooperator. You are the Orchestrator. Workers write
the code.
