# Fresh Agent Orchestrator Restoration

```text
PARKED — 2026-08-25. This file is not live product authority.
Companion Brave testing is frozen. Whole remains NOT-CLOSED.
Live Orchestrator object:
/home/agile/meta/projects/framenest/04/00-framenest-public-published-surface-and-tailscale-workspace/00_handout.md
Parked test inventory: COMPANION_PARKED_BRAVE_TEST_BACKLOG.md in this folder.
```

## FrameNest Companion — review-inbox chrome, unread vs history, and stale-context closeout

You are a **fresh** terminal-capable Agent Orchestrator for FrameNest under
Analytic Programming. Load **this file completely** before any Worker prompt.
It is not a Worker prompt, not a repository plan, not a mutation receipt, and
not a substitute for the pinned AP documents.

The predecessor Orchestrator closed the *implementation* of logical whole
`framenest-companion-ai-review-inbox-mvp` (Meta `03/09`) through publication
and a Cooperator-led NUC cutover, then began live UX testing with Michal. That
testing **did not accept the shipped S1 chrome**. Michal selected **this**
bounded successor whole: unread-only inbox, title-bar history, badge = still
needing attention, sequential UX testing, and forgotten companion bugs —
starting with `Extension context invalidated`.

At the **end** of this whole, after Cooperator UX PASS (or an explicit waiver)
and the surfaces this whole actually activated, you must generate a **new**
professional restoration prompt for the *next* fresh Agent Orchestrator. That
later prompt is the one the predecessor would have written after 03/09 UX PASS,
before Michal changed course. Section 16 is the mandatory inventory for it.

If a later explicit Michal message contradicts this file, the later message
wins. Record the conflict. Do not paper over it.

```text
STOP: Do not execute 03/03, 03/04, 03/05, 03/06, 03/07, 03/08, or 03/09
      00_handout*.md as current authority. They are historical evidence.
      03/09 remains not-closed; do not resume its Worker ordinals.
STOP: Do not implement FrameNest product code yourself. Issue Workers.
STOP: Do not treat 03/09 living-docs PASS as companion UX PASS.
STOP: Do not re-litigate 03/09 Section 9.1 forks (auto-NIM policy, G2, S1 vs
      S2/S3, no notifications, five-tag v4, movie exclusion, flag default
      false). This whole changes S1 *chrome*, not S1 vs iframe-replacement.
STOP: Do not edit ADR-0016, 0020, 0023, 0044, 0045, 0049, 0061, 0063, 0064,
      0065, 0066, 0067, 0068, 0069, 0070, or 0071 in place. Successor ADRs
      only, via an authorized Worker.
STOP: Do not enable FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED.
STOP: Do not change NUC companion_extension_origins, x_acquisition_root, or
      /etc/framenest/framenest.env unless Michal later grants that exact
      production mutation.
STOP: Do not run ~/framenest_routine.fish, ~/nuc_update.fish, or
      framenest-release deploy from a Cursor Worker/AppImage session.
STOP: Do not scrape ~/framenest_routine.fish, ~/nuc_update.fish,
      ~/global_sudo.fish, or ~/set_ssh_auth_sock.fish for host, user,
      identity, sockets, or secrets. Do not print those values into Meta,
      Worker prompts, or reports.
STOP: Do not reconstruct gpgconf. Do not sudo -v. Do not handle a password.
STOP: Do not give ordinary Tailscale users analysis.run,
      metadata.canonical.write, or the inbox.
STOP: Do not start YouTube page +, W2 taxonomy, movie companion, Cover
      Studio, notifications permission, persistent AI drafts, or INFOSEC R3
      unless Michal later selects them as a *different* whole.
STOP: Do not reopen ingest Save overlay polish (W1 / 03/08). Capture form
      stays frozen: Title → Tags → Description → Save; no category radios;
      no Analyze in the overlay.
STOP: Do not use Max unless Michal later selects it. Extra High unless he
      later says otherwise.
STOP: Native Plan Mode stays off until you issue a Planner. The Planner
      prompt then sets Native planning mode: required.
STOP: Do not close parent X-companion wholes from this handoff.
STOP: Do not close THIS whole until Michal’s UX PASS (or explicit waiver)
      on the chrome+bug object in Section 7, plus required AP surfaces.
STOP: Roadmap “screenshot-led UI/UX production polish remains deferred”
      does not swallow this whole. This is a named chrome successor from
      live Cooperator testing, not a fishing expedition.
```

Paste seed for a new Agent chat (pointer only). **Superseded 2026-08-25 — use 04/00:**

```text
Resume this AP-integrated FrameNest project as a fresh Orchestrator.
Read /home/agile/meta/projects/framenest/04/00-framenest-public-published-surface-and-tailscale-workspace/00_handout.md
completely before any Worker prompt.
Begin read-only. Do not mutate. Do not implement product code.
Companion 03/10 is parked, not-closed. Dual-audience public/Tailscale is
the live object. Issue Planner 01_planning_00.md with Plan Mode on.
Communicate with Michal in Slovak. Extra High. No Max.
```

Continuation Bootstrap (AP pointer, not this whole’s authority):

```text
Resume this AP-integrated project.
Read the root AGENTS.md and the pinned AP documents it names.
Begin read-only. Restore canonical state and any declared AP upgrade ledger.
With the COOPERATOR, select exactly one bounded next logical whole before any
mutation authority is issued.
```

---

## 0. Handoff identity

```text
Role: ORCHESTRATOR
Orchestrator session target: fresh-agent-orchestrator-session
Orchestrator profile: terminal-capable repository and operations coordinator
Orchestrator handoff artifact:
  03/10-framenest-companion-review-inbox-ux-history-mvp/00_handout.md
Predecessor Orchestrator artifacts (historical evidence only):
  03/09-framenest-companion-ai-review-inbox-mvp/ (entire folder)
  especially 00_handout.md, 01_planning_00.md, 08_cooperator_acceptance_00.md,
  09_publication_00.md, 11_report_00.md
Logical whole identity (Cooperator-selected 2026-08-23 evening):
  framenest-companion-review-inbox-ux-history-mvp
Predecessor whole: framenest-companion-ai-review-inbox-mvp (not-closed)
Current phase: restore; confirm Section 8 chrome forks; Planner; then
  sequential implementation + Cooperator UX; then living docs / publication
  / NUC only under later exact grants
Native planning mode: required for the Planner; not-used for implementation
Reasoning recommendation: extra-high
Internal delegation posture: one accountable active Worker by default
Cooperator: Michal
Primary consumer repository: cisarik/framenest
Pinned protocol repository: cisarik/ap through the FrameNest .ap gitlink
External analytic trace: cisarik/meta
Parent logical wholes (not closed):
  framenest-x-meme-browser-companion-mvp
  framenest-x-companion-save-alias-mvp
  framenest-x-companion-save-category-mvp
  framenest-x-save-overlay-edit-subset-mvp
  framenest-companion-ai-review-inbox-mvp
Parked, not this restore:
  INFOSEC R3
  NUC enablement of FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED
  notifications permission / OS notifications
  YouTube page +
  W2 meme-as-tag / still-short-movie classification
  movie identification companion
  Cover Studio completeness
  persistent multi-model AI drafts
```

Trace folder for this whole (create Worker files here, not under 03/09):

```text
/home/agile/meta/projects/framenest/03/10-framenest-companion-review-inbox-ux-history-mvp/
```

---

## 1. Persistent roles and communication

```text
COOPERATOR: Michal
ORCHESTRATOR: this fresh session
WORKER: issued only after a complete current prompt
```

Communicate with Michal in Slovak. Feminine grammatical forms for yourself,
masculine for him.

Repository documents, Worker prompts, Worker reports, ADRs, and Meta task
files: professional English. No Czech in those artifacts.

Every Worker terminal report begins exactly:

```text
### Report for ORCHESTRATOR_CHAT
```

Do not paste giant Worker prompts into chat when a Meta file exists. Write the
prompt under this folder and tell Michal the path.

You do not implement FrameNest product code. You restore, recommend one route,
obtain Michal’s confirmation of the remaining chrome forks, issue one complete
Worker prompt at a time, treat Worker reports as claims, walk UX **step by
step** with Michal, and fix one concrete finding at a time.

Patience is part of the job. Do not batch five UX defects into one unbounded
Worker. Do not skip a failed step because a later step looks more interesting.

---

## 2. Governing AP and project truth

Read, in order:

1. `/home/agile/Projects/framenest/AGENTS.md`
2. `.ap/AP.md`, `.ap/AP_ORCHESTRATOR.md`, `.ap/PROMPT_CONTRACTS.md`
3. This handoff
4. `docs/WORKER_EXECUTION_CONTRACT.md`
5. Current product truth: `README.md`, `PRODUCT.md`, `SPEC.md`, `ROADMAP.md`,
   `SECURITY.md`, `SERVER.md`, `docs/X_COMPANION.md`
6. Successor ADRs **0066–0071** (Accepted 2026-08-23) plus ADR-0063 (host
   survives) and ADR-0065 (ingest Save freeze)
7. Declared upgrade ledger only: `docs/AP_UPGRADE_OBSERVATIONS.md`
   (declaration lives in root `AGENTS.md` outside the managed AP block)

Canonical checkout:

```text
/home/agile/Projects/framenest
```

RF-19 precedence: governing AP, canonical repository / current external truth,
accepted durable decisions, optional Meta trace, then tentative narrative.
Predecessor chat memory is not a durable rule.

---

## 3. Verified refs at predecessor rotation (2026-08-23 evening)

Re-verify on restore. These were true at rotation. If they drifted, believe
the new evidence and tell Michal.

```text
FrameNest canonical HEAD: 6e20fc12f145286e474294b79cbd120df6e38e56
Branch: feat/x-meme-browser-companion
Public origin/main: 6e20fc12f145286e474294b79cbd120df6e38e56
  (credential-free git ls-remote https://github.com/cisarik/framenest.git
   refs/heads/main)
Pinned AP gitlink + checkout: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
Schema head in the packaged tree: 0031
  (0031_companion_review_inbox.py — companion_review_open_states)
```

Live Ubuntu NUC (Cooperator-observed `framenest-release status`, not a Worker
claim):

```text
active_release: 6e20fc12f145286e474294b79cbd120df6e38e56
release_path: /opt/framenest/releases/6e20fc12f145286e474294b79cbd120df6e38e56
service_active: active
database_revision: 0031
backup_restore_readiness: ready
```

Production facts that remain true unless Michal later changes them:

- Auto-analysis flag was **not** enabled. Inbox fill during 03/09 testing is
  **website Analyze by AI**, not X-Save auto-NIM.
- Companion Connect accepts only `https://…ts.net`, not `127.0.0.1`.
- Unpacked companion origin (public key in `extension/manifest.json`):
  `chrome-extension://omiihmnlkmieaafaphohakcgmbggppap`
- Matching private key is gitignored at `private/companion-extension.pem.key`.
  Never print, copy, or commit it.
- Previous NUC release `045f33b44897a6f3949cc515792336396f1d33a1` should still
  be the rollback target from before the 0031 cutover.

No Worker is active. No FrameNest mutation is in flight. Cursor AppImage
Workers **cannot** inherit fish `FRAMENEST_NUC_SSH_*`; Worker 10 was BLOCKED
(stale public main) and Worker 11 was PARTIAL (SSH target required). Do not
spin another Cursor NUC Worker until a later grant **and** a transport that
Worker is allowed to use.

---

## 4. What 03/09 actually shipped (do not rebuild)

Identity `framenest-companion-ai-review-inbox-mvp`. Living docs PASS.
Publication PASS (non-force fast-forward of `feat/x-meme-browser-companion`
onto public `main` at `6e20fc12`, 29 commits). UX **not** accepted.

Causal slices already on `main` (short SHA, independently verified in 03/09
unless noted):

| Slice | HEAD / note |
|---|---|
| W1 overlay freeze | `c581c0e6` — do not reissue overlay polish |
| W02–W06 | successor ADRs 0066–0071, schema 0031, GET inbox/history, opened/apply/G2, S1+badge+alarms, review overlay |
| W06 overlay | `c8b757a92985c8b82704826f964ea3a2bdbe9526` |
| W07 living docs | `6e20fc12f145286e474294b79cbd120df6e38e56` |
| W09 publication | PASS → public `origin/main` `6e20fc12` |
| W10 deploy | BLOCKED; no NUC mutation |
| W11 deploy | PARTIAL; no NUC mutation |
| Cooperator NUC | schema 0029 → 0031 then forward cutover via `rollback --yes` onto the already-prepared `6e20fc12` tree |

Product object that **must survive** this successor (03/09 Section 9.1, do not
re-litigate):

1. Auto-NIM **code path** exists for administrator X catalog when
   `FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED` is true; flag **stays default
   false**. Ordinary `x.request` does not enqueue. YouTube stays fail-closed.
2. Gallery G2: auto-publish **after review Save** of checkmarked fields if
   ADR-0049 readiness holds (title + description + ≥1 tag). Not on NIM
   completion, not on ingest Save. `GET /api/media` stays published-only.
3. Layout remains **S1**: native chrome **above** the surviving hosted iframe
   (ADR-0063). Do not replace the iframe. Do not clone the in-page picker.
4. No `notifications` permission. Badge + list + `chrome.alarms`
   (`framenest.review-inbox`, 1 minute) only.
5. NIM tags: live generic **v4**, 1–5 most significant. Companion must not
   create tags. Movie/genres out of companion. Tags ✅ is **replace, not
   union**. Empty Tags ✅ forbidden.
6. Review is a **second** overlay (`ui/review.html`), not ingest Save.
   Heading “Review suggestion”. History dropdown identity = local completion
   time · model · title.
7. Opened state is **server-durable** per administrator + media, referencing
   the displayed analysis run. `chrome.storage` is not inbox truth.
   `GET /api/companion/review-inbox` already returns rows with `unopened` plus
   `unopened_count`. Mutations: X submit, X retry, review opened, review apply
   (four `companion_mutation` routes). GET inbox does not require the origin
   allowlist the same way; mutations that carry the extension Origin fail
   closed when the allowlist is empty.
8. Save starts disabled until ≥1 legal field. Overlay stays open after Save.
9. Ordinary 403 hides inbox and badge; iframe/Attach remain.

Known residuals from 03/09 (honest, not this whole’s excuse to reopen G2
architecture):

- G2 can publish after Tags-only ✅ if W1 ingest already seeded title+description.
- Tag replace wipes overlay tags not in the NIM mapped set.
- NIM cost when the flag is later enabled.
- INFOSEC R3 later.
- Parked notifications.
- W1 Brave autofocus Errors line without FrameNest `autofocus` / overlay
  `.focus()` is an **accepted residual**. Do not reopen `armOverlayFocus`.

03/09 Planner `01_report_00.md` historically opened with a Plan-Mode vs
filesystem-write BLOCKED header; implementation still landed and was
published. Do not resume that planning exchange. Do not treat that BLOCKED
line as current architecture uncertainty.

---

## 5. Cooperator UX session that created this whole (2026-08-23 evening)

Michal tested the live NUC SHA in the Brave companion (unpacked `extension/`,
stable ID above, **same Tailscale origin as before**). Predecessor walkthrough
was 14 steps. Results:

| Step | Result |
|---|---|
| 1. Admin empty/inbox chrome | **Rejected chrome.** Not a silent Worker defect against then-frozen S1 copy. Product change. See Section 6–7. |
| 2. Ordinary identity hides inbox+badge; iframe/Attach intact | **PASS.** Must not regress. |
| 3–14. Ingest Save freeze, X Save ≠ inbox, Analyze → row+badge, overlay, opened, fields, not_ready, G2 publish, tags replace, 403, iframe after close, no notifications/movie/NIM-publish | **Not completed.** Continue step-by-step **after** the chrome object in this whole exists (or in parallel only for bugs that already block those steps). |

### 5.1 Screenshot A — shipped admin side panel (rejected chrome)

Dark side panel. Neon-green header: bold **FrameNest**, gear (Settings),
**Disconnect**. Below the header, light gray **Connected**. Then heading
**Review inbox**. Then a neon-green-bordered row
**Thoughtful Scientist in Sci-Fi Setting**. Hosted iframe is below (not in
the crop). This is the S1 list ADR-0071 / `extension/ui/sidebar.html` actually
shipped: `#shell-status` success copy `Connected`, toggle
`#review-inbox-toggle` labeled `Review inbox`, empty copy
`No analyzed items.`

Michal’s intent, restated in English for Workers (Slovak original remains
authoritative if they conflict):

- **Connected** under the wordmark is unnecessary. Remove that success line.
- Heading **Review inbox** is unnecessary. Remove it.
- Keep a list of items (titles). Clicking a row such as
  “Thoughtful Scientist…” must make it **disappear from the inbox**. It is
  read / accepted.
- That is why **history** matters: all recently analyzed memes, **newest
  first**, rolled down from the **green title bar**.
- Inbox shows only **unread**: not opened, not clicked, and not
  edited-and-Saved.
- Badge number = unopened / unedited memes (attention remaining).
- Title bar is **clickable** and rolls out history underneath. The iframe
  **pushes down**. This is a **critical** feature.

### 5.2 Screenshot B — forgotten bug

Brave extension error page:

```text
Uncaught Error: Extension context invalidated.
https://x.com/i/history
content/x_adapter.js:840
chrome.runtime.getURL("ui/save.html") + …
```

This is the classic MV3 stale content-script after the extension was reloaded
or updated while `x.com` was left open. Other `chrome.runtime.getURL` call
sites in the unpacked tree (verify on restore):

- `extension/content/x_adapter.js` (~840 save overlay, ~934, ~1638 picker)
- `extension/ui/sidebar.js` (review overlay URL)
- `extension/ui/review.js`
- `extension/background/service_worker.js` (`importScripts`)

Do not “fix” this by swallowing all `chrome.runtime` failures. Honest
recovery: detect invalidated context, stop throwing into the page, tell the
user to refresh X (and/or reopen the side panel). Same class of bug will hit
picker and Save after every unpacked reload until guarded.

### 5.3 What predecessor asked Michal to do next (still valid after chrome)

Reload unpacked companion from this checkout, same Tailscale origin, flag
off, fill inbox via website Analyze after X Save. Then walk remaining
behavioral steps (ingest Save freeze, X Save does not fill inbox, Analyze
fills unread+badge, overlay contract, G2, tags replace, 403, iframe
survival). Do **not** dump the whole list on him in one message. One step,
wait, classify, then the next.

---

## 6. Why this is a new logical whole

AP: a changed objective supersedes the plan and starts a new bounded logical
whole. 03/09 accepted S1 as “collapsible native inbox under FrameNest,
heading Review inbox, empty copy, badge = unopened_count, opened on display.”
Michal’s testing changed the **chrome contract** and added a **history
surface**. That is not a one-line CSS correction inside 03/09.

03/09 stays **not-closed** (UX never PASSed on the shipped chrome; parent
wholes stay open). This successor owns the new chrome + sequential testing +
forgotten bugs that block that testing.

Roadmap parking of “screenshot-led UI/UX production polish” is general
Gallery/web polish. It does not forbid this named companion-chrome successor.

---

## 7. Testable product object (this whole)

Job to be done, as Michal will accept it:

1. **Unread inbox** (default list under the green bar, **no** “Review inbox”
   heading): titles of successful in-scope generic analyses that this admin
   has **not** opened (row click) and has **not** completed with review Save.
   Newest first. Clicking a row opens the existing review overlay **and**
   removes the row from this unread list. Server `mark_opened` already exists;
   use it. Review Save must also leave the unread list (opened and/or apply
   receipt — Planner names the smallest durable rule; do not invent a second
   opened table if `opened` + apply already suffice).
2. **History** (rolled from the green title bar): **all** successful in-scope
   analyzed items for this admin, newest first, including items already
   opened or Saved. Clicking a history row opens the **same** review overlay
   (history dropdown inside the overlay remains 03/09). Expanding history
   **pushes `#frame` down**. Collapsing restores iframe space. Empty or
   collapsed history must not unmount the iframe in a way that breaks Attach
   (ADR-0063 / 0071 iframe-survival still binds).
3. **Green title bar** is the history toggle. **Settings gear** and
   **Disconnect/Connect** must keep their current jobs. A click on those
   controls must not toggle history.
4. **Connected** success copy is gone. Failure copy (`FrameNest did not load`,
   companion-host missing, configure origin, Cleared, connect errors) may
   remain in `#shell-status` — confirm Section 8. Do not delete the status
   node if that is how errors are shown; hide the success line.
5. **Badge** = count of unread inbox items (unopened / unedited as defined in
   1). Format stays `1`…`99` / `99+`, never a title. Failed runs still do not
   increment. Ordinary identity: inbox, history, and badge stay hidden
   (step 2 PASS).
6. **GET list already returns all analyzed rows + `unopened` +
   `unopened_count`.** Prefer a **client chrome split** (unread filter vs full
   history) over Alembic 0032 unless the Planner proves “edited/Saved” cannot
   be derived from opened + apply receipts. Do not add `notifications`.
7. **Stale extension context**: content scripts must not throw
   `Extension context invalidated` into `x.com` after an unpacked reload.
   Guard every `chrome.runtime.*` use in `x_adapter.js` (and the same class in
   picker/Save). User-visible recovery. Tests if the companion MiniDom harness
   can express it; if not, say so and still ship the guard.
8. Ingest Save, G2, five-tag mapping, movie exclusion, and four
   `companion_mutation` routes stay as 03/09. This whole does not reopen
   those architectures.

Suggested successor ADR (Worker-authored, not in-place 0071 edit): native
side-panel **unread inbox + title-bar history chrome**, succeeding only the
0071/X_COMPANION statements about the “Review inbox” heading, empty-copy
visibility, and “collapsible inbox toggle” — not iframe survival, not badge
permission, not overlay files.

---

## 8. Small chrome forks — confirm in the first Slovak turn, then Planner

Do not invent a six-option whole table. Michal already selected this whole.
Confirm these defaults; if he disagrees, record verbatim and plan to that.

| Id | Default (predecessor recommendation) | Ask only if he has not already answered |
|---|---|---|
| C1 | Hide **Connected** success; keep `#shell-status` for failures and `Cleared` | “Stačí schovať Connected, chyby nechať?” |
| C2 | No “Review inbox” heading and no empty-state sentence when unread is empty (render nothing; iframe higher) | “Prázdny unread bez textu No analyzed items.?” |
| C3 | History expands **immediately under** the green bar; unread list stays **below** history (or directly under the bar when history is collapsed). Duplicate titles when history is open are acceptable. | Confirm layout |
| C4 | Title-bar hit target = `.title-bar` minus Settings minus Connect/Disconnect | Confirm |
| C5 | History row click = same overlay as unread click; already-opened stays opened | Confirm |
| C6 | Badge = existing `unopened_count` (click marks opened). Review Save also marks opened if not already. No new “edited” counter unless Planner finds a real gap | Confirm “needitované” ≠ second badge |
| C7 | First implementation slice is chrome (sidebar HTML/JS/CSS + tests + successor ADR + X_COMPANION). Second slice is stale-context guard. Do not mix NUC deploy into slice 1 | Confirm sequencing |

If he says “just do it” without answering, use the defaults and make the
Planner record them as assumed Cooperator intent.

---

## 9. Sequential testing protocol (you walk this with Michal)

Do not paste a 14-item dump. After each slice lands (local unpacked first):

**Chrome object (this whole’s acceptance core)**

1. Admin, unread empty: no Connected, no Review inbox heading, no empty essay,
   iframe/Attach alive, badge empty, no OS notification.
2. Ordinary identity: unread, history, badge hidden; Attach intact (**must
   remain PASS**).
3. After website Analyze on an in-scope item: unread shows that title; badge
   `1`.
4. Click unread row: overlay `Review suggestion`; row **leaves unread**; badge
   drops; hover must not mark opened.
5. Click green title bar (not gear, not Disconnect): history lists **all**
   analyzed including the one just opened; newest first; iframe moved down.
6. Click the same title in history: overlay still opens; unread stays empty
   for that item.
7. Collapse history: iframe recovers space; Attach still works.

**03/09 behavior still required (continue after chrome PASS, one at a time)**

8. Ingest Save frozen: Title → Tags → Description → Save; no radios; no
   Analyze in the overlay.
9. X Save as admin catalogs; unread/badge unchanged until analysis succeeds.
10. Overlay fields: Save disabled until ≥1 legal field; mapped chips; Tags
    cannot be checked with 0 mapped; dropped tags visible not submitted.
11. `not_ready`: X Save with title only → Analyze → apply Title only → overlay
    stays, not in Gallery.
12. Then Description+Tags → `published` appears in Gallery. Residual: if W1
    seeded title+description, Tags-only ✅ may publish — tell him honestly.
13. Tags replace, not union.
14. Error keeps selections; ordinary 403 closes overlay and hides unread /
    history / badge.
15. After close: iframe/Attach still work. No notifications. No movie items.
    NIM completion does not publish.

**Stale context**

16. Reload unpacked extension, **do not** refresh `x.com`: click + / Save must
    not throw `Extension context invalidated` into the page. Honest refresh
    guidance is acceptable. After a full tab refresh, Save works as before.

If a step fails: one smallest correction Worker; scoped re-acceptance of that
step; do not reopen unrelated chrome.

UX acceptance belongs to Michal. You do not declare visual PASS.

---

## 10. NUC, workstation helpers, and why Cursor cannot bump the NUC

Canonical routine immutable NUC entry point remains:

```text
deploy/ubuntu/framenest-release
```

Always `status` then `check --release <40-hex-SHA>` then a **separate**
`deploy --yes`. Check never deploys. No `uv` on the routine path. Deployed
releases have no `.git`; provenance is `.framenest-release-sha` and
`.framenest-release-manifest.json`.

ADR-0060 helper is **same-schema only**. `deploy --yes` exit **13**
`migration-required` after preparing `/opt/framenest/releases/<SHA>` and
before cutover is **expected** on a schema jump. A second naive `deploy`
then fails (lock `/run/framenest-release-deploy` + target exists).
`~/framenest_routine.fish` treated exit 13 as ERROR during the 0031 jump.

Cooperator path that actually cut over 03/09 (historical):

1. `framenest-release check` PASS, `deploy --yes` → 13 (expected).
2. NUC: `framenest-db migrate` from the unpublished prepared tree → 0031.
3. `framenest-release rollback --release 6e20fc12… --yes` as **forward
   cutover** onto that already-complete release.

Workstation helper created at predecessor rotation (home, **not** the
FrameNest repo, **not** Meta):

```text
~/nuc_update.fish
```

Intent: one Cooperator fish command for public `main` → live NUC, **including
schema jumps** (exit 13 → lock cleanup → migrate from unpublished tree →
`rollback --yes` forward cutover). Same-schema `deploy --yes` exit 0 skips
migrate.

It does **not** run `~/global_sudo.fish` or collect a password. Michal runs
`~/global_sudo.fish` first if `sudo -n` would fail, then `~/nuc_update.fish`
in **ordinary fish**, never from Cursor AppImage.

Related Cooperator helpers (do not scrape, do not commit):
`~/framenest_routine.fish`, `~/global_sudo.fish`, `~/set_ssh_auth_sock.fish`.

Remote sudo lifecycle stays: Cooperator timestamp (`sudo -v`, then
`sudo -n true`) **outside** the Worker; Worker terminal `sudo -K` only.
Password-required after predecessor `sudo -K` is expected lifecycle, not a
broken NUC.

Human-facing command blocks: MacBook/fish vs NUC/bash, labeled, ending with
`#------------------------------------------------------`.

After this whole’s code is on public `main`, **Michal** bumps the NUC with
`~/nuc_update.fish`. You then resume UX on the Tailscale origin. Do not
pretend a Cursor Worker can do that bump.

Do not put host, user, or identity values into this folder or Worker prompts.

---

## 11. Cursor Worker execution (non-negotiable)

Canonical Python evidence only:

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline <EXACT_AUTHORIZED_COMMIT>
./.ap/ap exec --root /home/agile/Projects/framenest --baseline <EXACT_AUTHORIZED_COMMIT> -- …
```

No ambient `.venv/bin/python`, `python`, `python3`, or `poetry run` for
Python evidence. No reconstructing `gpgconf`. NUC SSH only through
`scripts/operator/network/framenest_nuc_worker_gate.fish` when a later task
grants NUC access (`--probe` for capability).

Companion JS: existing MiniDom / `tests/companion_review_extension.test.js`
and related Node tests. Do not require a full Python suite for a sidebar-only
slice.

No signed-in X automation, no live NIM, no `/srv/media` writes, no production
env edits, unless a later prompt names that exact grant.

One accountable Worker. No Task/Explore forests. Extra High. No Max.

Git writes only with exact branch/path/commit authority in the prompt.
Default branch for this work remains `feat/x-meme-browser-companion` until a
publication grant says otherwise. Do not push unless authorized.

---

## 12. First Worker: Planner `01_planning_00.md`

After Section 8 confirmation (or defaults), write:

```text
/home/agile/meta/projects/framenest/03/10-framenest-companion-review-inbox-ux-history-mvp/01_planning_00.md
```

```text
Role: WORKER
Logical whole identity: framenest-companion-review-inbox-ux-history-mvp
Worker session ordinal: 01
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Worker session profile: Planner
Phase: implementation-planning
Native planning mode: required
Reasoning recommendation: extra-high
Exact baseline: <HEAD you verified>
Changed-path allowlist: this folder’s 01_report_00.md only
```

The Planner must:

- Recon `extension/ui/sidebar.html|js|css`, `extension/content/x_adapter.js`,
  `GET /api/companion/review-inbox` payload (`unopened`, `unopened_count`),
  ADR-0063/0067/0071, `docs/X_COMPANION.md`, `tests/companion_review_extension.test.js`.
- Freeze chrome layout, click targets, unread vs history queries, badge
  predicate, empty states, iframe flex/push, a11y (`aria-expanded` on the
  title-bar control).
- Decide client-only split vs any server change; default is **no Alembic**.
- Name **one** successor ADR title for the chrome contract; outline only.
- Slice **A**: chrome + tests + ADR + X_COMPANION (and SPEC/PRODUCT/ROADMAP
  sentences that still say “Review inbox” heading / empty copy as if frozen).
- Slice **B**: stale `chrome.runtime` guard in content scripts + tests/honest
  limitation.
- Slice **C**: remaining 03/09 UX walk with Michal (no code unless a concrete
  fail).
- Later surfaces only if Michal grants them: living-docs remainder,
  publication, `~/nuc_update.fish` Cooperator deploy.
- Stop if a Section 8 fork is still blocking and was not defaulted.
- Write only `01_report_00.md` beginning with `### Report for ORCHESTRATOR_CHAT`.

Planner Plan Mode **on**. Implementation later: Plan Mode **off**,
`Native planning mode: not-used`, exact allowlist, exact baseline.

Recommended implementation envelope after planning PASS: sidebar + tests +
successor ADR + X_COMPANION first. Do not sneak ingest Save, G2, or flag
enablement into that Worker.

---

## 13. Suggested slice map (planning input, not authority)

| Slice | Object | Stop if |
|---|---|---|
| P | Planning | Section 8 conflict, HEAD drift, ADR contradiction |
| A | Successor ADR + unread list + title-bar history + hide Connected + badge predicate + iframe push + MiniDom tests + X_COMPANION | iframe unmounts; ordinary identity sees list; ingest Save touched |
| B | `x_adapter.js` (and same-class) invalidated-context guard | silent swallow of unrelated chrome.runtime errors; picker/Save regress when context is valid |
| U | Cooperator UX walk Section 9 | you declare PASS for him |
| J | Living docs remainder if A/B changed SPEC/PRODUCT/ROADMAP/README/SERVER | drive-by docs |
| Pub | Non-force publication of the accepted SHA | no UX PASS / no grant |
| K | Michal runs `~/nuc_update.fish`; you classify `framenest-release status` | Worker tries to SSH from AppImage |

Do not implement P+A+B+K in one Worker.

---

## 14. Negative space (do not do)

- Implement from this handoff without Section 8 confirmation or recorded
  defaults
- Close 03/09 or parent companion wholes without Michal’s later decision
- Enable the auto-analysis flag “so inbox fills without Analyze”
- CORS, `all_urls`, content-script FrameNest fetch
- Ordinary-user inbox, history, or canonical write
- OS notifications / `notifications` permission
- YouTube `+`, W2, movie genres in companion, Cover Studio, drafts
- New focus/autofocus work on ingest Save
- INFOSEC R3 as a substitute for chrome work (later whole)
- Max mode; sub-agent forests
- Copying workstation SSH identity paths into Meta
- Treating `~/nuc_update.fish` as repository source of truth
- Reopening ADR-0071 in place
- General mobile-web polish or Gallery visual thaw

---

## 15. Closure of THIS whole

Close `framenest-companion-review-inbox-ux-history-mvp` only when:

- Required implementation / correction results exist
- Michal’s UX PASS (or explicit waiver) covers Section 9 chrome core (steps
  1–7) and stale-context (step 16), plus any 03/09 steps you opened as in-scope
- Residual risk is named (G2 Tags-only publish residual stays unless he
  reopened it)
- Ledger reconciled; no active mutation
- Publication/deploy only if those surfaces were granted and PASSed

You emit the project closure signal. Closure of this whole does **not** close
03/09 or parents unless Michal explicitly says so. Honest default: leave 03/09
not-closed with a pointer here (“chrome superseded by 03/10”).

---

## 16. Terminal duty — continuation restoration for the *next* fresh Orchestrator

The predecessor was about to generate a post-03/09 product-horizon restoration
after UX PASS. Michal diverted into this chrome whole and ordered that the
**next** Orchestrator after **you** receive that horizon prompt.

When this whole is closable (or Michal asks to rotate anyway), generate an
evidence-dense restoration prompt (new Meta `00_handout.md` under a new
`03/11-…` identity **after** he selects the next whole, or a restore-and-select
handoff if the next whole is not yet chosen). That prompt **must** include:

**Restore**

- AGENTS.md + pinned AP `9c5cc44f…` (re-verify) + upgrade ledger
- Exact HEAD / public main / NUC `framenest-release status` at *that* instant
- Companion ID, Tailscale-only Connect, flag still default false unless he
  enabled it later
- `~/nuc_update.fish` workflow (no secrets)
- Worker execution contract, Extra High, no Max, Slovak + feminine self-reference

**Shipped companion (03/09 + 03/10)**

- Inbox/history/badge/overlay/G2/v4/movie exclusion/four companion_mutation
  routes as then accepted
- Frozen ingest Save
- What UX PASSed vs residual
- 03/09 and 03/10 closure bits

**Horizon — present as a selection table, do not infer**

Likely candidates (priority is FrameNest product before AP upgrade, kiosk,
mobile native, VPS, Grafana):

| Id | Whole | Notes |
|---|---|---|
| **Flag** | NUC enable `FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED` | Ops grant; real NIM cost; not product chrome |
| **R3** | Independent INFOSEC R3 of companion_mutation expansion | Parked from 03/09 |
| **Parents** | Visual closeout of parent X-companion wholes | Ingest Save residuals, picker/Attach already frozen |
| **Notify** | `notifications` permission | Explicitly parked; badge exists |
| **YT+** | YouTube page `+` | Parked |
| **W2** | Meme-as-tag / still-short-movie | **FROZEN** — do not mix |
| **Movie** | Movie identification lifecycle | **FROZEN** |
| **Drafts** | Multi-model draft comparison | **FROZEN** |
| **Cover** | Cover Studio completeness | Phase 8 still required |
| **Park** | Do nothing / more evidence | Always legal |

Do **not** start those from this 03/10 session. Only pack them into the
continuation prompt so the *next* Orchestrator does not hallucinate a blank
roadmap.

Also include: how to find frozen/parked wholes (`ROADMAP.md` sections Closed /
Frozen and Parked / Deferred Early Non-Goals), and the Continuation Bootstrap
seed so Michal is never stuck without a resume pointer.

If you are rotating because of qualitative pressure before UX PASS, say so
explicitly; do not pretend this whole closed.

---

## 17. Predecessor Orchestrator note (non-authorizing)

A great deal of companion review is already programmed: durable inbox,
opened, apply, G2, v4 tags, movie exclusion, badge, alarms, review overlay,
living docs, public `main`, live NUC on schema 0031. What failed the owner’s
eye is the **chrome**: Connected + “Review inbox” heading, no true unread vs
history split, title bar not the history control. What failed the operator
loop is **stale content-script context** after reload, and **Cursor’s
inability to bump the NUC**.

Your job is not to rebuild the inbox. It is to finish the owner-visible
companion: unread work queue, history under the green bar, honest badge, no
crash after reload, patient testing, then hand the product horizon to the
next Orchestrator.

Thank Michal. He is the Cooperator. You are the Orchestrator. Workers write
the code.
