# FrameNest — independent production acceptance / Gate E (5abb2ad)

🆕 **PROMPT FOR FRESH WORKER 29 • HIGH REASONING**

**Native Plan Mode: OFF.** Work directly. Do not spawn subagents. Do not ask
for another Worker. Do not close the logical whole.

You are one fresh WORKER instance under Analytic Programming. You are not the
ORCHESTRATOR. If this chat implemented, corrected, published, recovered, or
deployed `5abb2ad…`, or was Worker 28, stop `BLOCKED` before inspection.

```text
Persistent role identity: WORKER
Logical whole identity: framenest-repeatable-immutable-nuc-release-deployment-contract
Worker session ordinal: 29
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Native planning mode: not-used
Worker session profile: Fresh Independent Production Acceptance
Phase: production acceptance / Gate E
Task identity: FN-NUC-RELEASE-GATE-E-29
Reasoning recommendation: High
Recommendation basis: independent live production-acceptance on Michal's test NUC; three real mapped identities; privileged status readback; authenticated Tailscale Serve browser; no implementation Worker may self-certify this gate
Automatic model selection: off
Enhanced/maximum mode: not requested
Sub-agents/internal delegation: not-used
Worker topology: single-active
Material phase gate: yes
Changed material axis: acceptance owner and evidence class
Ordinary-only trigger: no
Routing reopened for: independent production-acceptance / Gate E
Unchanged axes reopened: none
Evidence tier: E3
Evidence tier basis: live production-acceptance and privileged remote readback on a personal test NUC; access-control is observed not mutated; catalog, service, identity map, Serve, and release symlink stay unchanged; independence is required
Combined implementation envelope: prohibited
Independent acceptance: required-fresh-independent
Acceptance independence: required-fresh-independent
Primary fresh acceptances used: 1
Automatic corrections used: 0
Correction re-acceptance: not-applicable
Named missing-evidence probe: none
Out-of-scope observations: none unless a non-blocking pre-existing fact must be recorded
Rollback or recovery checkpoint: not-applicable; do not checkpoint, restore, deploy, or roll back
Activated stricter profile: none
Canonical repository mutation: none
Correction authority: none
Publication authority: none
Deployment authority: none
Implementation authority: none
Evidence posture: independent only if this session did not implement or previously accept the candidate
```

Read this complete prompt before acting. Do not rewrite or summarize the AP
protocol.

## 1. Independence gate

This prompt must reach a genuinely fresh Worker session.

You must not have participated in:

- Worker 28 recovery, correction, publication, or deploy of `5abb2ad…`;
- any earlier implementation or mutation of candidate
  `5abb2adfcd1d5f3391df9c3044b4b81ac1aac923`;
- any prior audit that already claimed Gate E or production-acceptance-PASS
  for this SHA.

If this is not a fresh session, Native Plan Mode is active, or you materially
participated in the candidate, stop before repository inspection and return a
truthful `BLOCKED` report.

Worker 28's terminal report is a **claim only**. Re-read live public and NUC
evidence. Do not inherit its PASS.

## 2. Candidate and live target

```text
Repository: /home/agile/Projects/framenest
Public repository: https://github.com/cisarik/framenest.git
Acceptance candidate: 5abb2adfcd1d5f3391df9c3044b4b81ac1aac923
Acceptance tree: 4f5505c65f883a2eeba10d670e0a76f45c0f1a2a
Acceptance parent: f5fbdce5669997f15c28ed6ffdad4cda849df4ee
Subject: fix: load production EnvironmentFile and wait for NUC readiness
Required public refs/heads/main: 5abb2adfcd1d5f3391df9c3044b4b81ac1aac923
Required AP pin: 17b7e085139e9bcbb0e4953d26aef9b6687d541c
Packaged schema head: 0028
Live target: Michal's test NUC via already-configured SSH host alias framenest-nuc
Entry point for host readback: /home/agile/Projects/framenest/deploy/ubuntu/framenest-release
Exact NUC Poetry: /opt/framenest/tooling/poetry/2.4.1/.venv/bin/poetry
Exact NUC CPython: /opt/framenest/tooling/python/cpython-3.13.14-linux-x86_64-gnu/bin/python3.13
Previous live release retained after Worker 28 claim: 148b6c2012809944262399c1a166e85082606fbf
```

Expected local checkout:

```text
Repository checkout topology: standalone checkout
Working directory: /home/agile/Projects/framenest
Expected branch: fix/nuc-release-environmentfile-readiness
Expected HEAD: 5abb2adfcd1d5f3391df9c3044b4b81ac1aac923
```

Local `main` is stale and intentionally untouched. Do not checkout, merge,
rebase, reset, or fast-forward it. Preserve owner untracked paths; do not
enumerate their contents.

## 3. Protocol and trace

```text
Canonical AP identity: https://github.com/cisarik/ap.git
Immutable version identity: 17b7e085139e9bcbb0e4953d26aef9b6687d541c
Declared variant: stable
Governing variants in effect: one
Rules from non-governing variants: none
Migration required: no
External trace disposition: configured
Trace discovery: /home/agile/meta/projects/framenest/03/00-framenest-repeatable-immutable-nuc-release-deployment-contract/
Trace project key: framenest
Trace logical-whole projection identity: 00-framenest-repeatable-immutable-nuc-release-deployment-contract
Trace authority: historical-evidence-only
Trace archival owner: Cooperator Michal; Worker must not archive
Trace visibility: private
Trace companion outcome: report
Trace self-granted status: none
Expected later archival pair after the report exists: 29_acceptance_00.md + 29_report_00.md
```

```text
Orchestrator-to-Worker prompt language: professional English
Formal Worker report language: professional English
Required report header: ### Report for ORCHESTRATOR_CHAT
Logical-whole closure: not-closed
```

## 4. Mandatory reading

Read only what this gate needs:

- root `AGENTS.md`
- `.ap/AP.md` and `.ap/AP_WORKER.md`
- `docs/WORKER_EXECUTION_CONTRACT.md`
- `docs/UBUNTU_NUC_DEPLOYMENT.md` (Routine Immutable Release Update and
  Tailscale Remote Access Ingress)
- `docs/adr/0060-repeatable-immutable-nuc-release-update-contract.md`
- `docs/adr/0048-tailscale-remote-access-and-identity-foundation.md`
- `src/framenest/domain/identity_access.py` for exact role/capability sets

Do not reopen product, X/Brave, AP-upgrade, or workstation-Python wholes.

## 5. Goal

Independently verify that public and live `5abb2ad…` is the current healthy
test-NUC release, and that Gate E real-user smoke passes for the three existing
private mapped identities:

```text
one administrator
two ordinary users
```

Verify only:

1. deployed release identity;
2. service, health, and database readiness;
3. login and role separation for those three identities;
4. Gallery and Details access;
5. ordinary-user restrictions;
6. administrator controls (read-only observation).

Do not broaden into new feature acceptance, upload/X/YouTube acquisition,
Cover Studio, backup restore, provider calls, or UX redesign.

## 6. Authority

### Positive authority

You may:

- read the canonical checkout and public Git transport;
- attach `gpgconf --list-dirs agent-ssh-socket` to this Worker process when
  `SSH_AUTH_SOCK` is absent, then prove one BatchMode SSH command succeeds;
- use the already-configured `framenest-nuc` SSH Host alias / IdentityFile as
  `--target` / `--user` / `--identity` when process `FRAMENEST_NUC_SSH_*` is
  unset; never print those values;
- use noninteractive remote `sudo -n` after the COOPERATOR-established global
  timestamp; never use `sudo -S`, handle a password, or run `sudo -v`;
- run read-only `framenest-release status` and optional read-only
  `framenest-release check --release 5abb2adfcd1d5f3391df9c3044b4b81ac1aac923`;
- read bounded live release markers, service properties, schema, backup
  restore-readiness, Serve/Funnel status class, and the live identity-map
  **role counts**;
- invoke release-local `framenest-production check-database-ready` and
  `check-health` through oneshot `systemd-run` with the unit `EnvironmentFile`
  (the binary ignores a process-only `FRAMENEST_ENV_FILE` selector);
- use authenticated-same-origin-browser against the live configured Serve
  origin, including Cursor IDE browser tools when available;
- call product-supported authenticated HTTP GET endpoints required by this
  gate, including `GET /api/identity/me`, `GET /api/media`, one published
  `GET /api/media/{media_id}`, and `GET /api/admin/media`;
- for ordinary-user restriction evidence only, attempt the same admin GET and
  observe sanitized denial;
- take screenshots into an untracked disposable location; do not commit them;
- run local Python only through `./.ap/ap exec` with `--baseline` exactly
  `5abb2adfcd1d5f3391df9c3044b4b81ac1aac923` if a local helper is unavoidable.

### Negative authority

You may not:

- edit any repository path, `.ap`, lockfile, systemd unit, env file, sudoers,
  identity map, Serve/Funnel, Tailscale, Mullvad, firewall, or account;
- create commits, push, publish, deploy, roll back, migrate, restore, or
  checkpoint;
- run `framenest-release deploy` or `rollback`;
- switch `/opt/framenest/current` or restart `framenest.service`;
- mutate catalog, media, covers, uploads, publication, removal, analysis, or
  backup state;
- click Save, Publish, Remove, Analyze, Import, Scan, Acquire, or Upload in
  the UI;
- spoof `Tailscale-User-*` or any identity header;
- curl the application UDS with injected identity;
- inspect or print cookies, tokens, passwords, authorization headers, complete
  `PATH` / `LD_LIBRARY_PATH`, identity-map login strings, Serve origin URL,
  private network values, database paths, or private media filenames;
- repair `.venv`, run `uv sync`, `poetry install`, `pip install`, or search
  for an alternate Python after an `encodings` startup crash;
- run the full Python or JS suite;
- write Meta;
- close the logical whole.

## 7. Environment and Cursor/AppImage rule

Cursor/AppImage ambient execution is untrusted.

If a raw `.venv/bin/python`, `python`, `python3`, or `poetry run` invocation
emits `Failed to import encodings` / `No module named 'encodings'`, classify it
immediately as an ambient-execution violation. Do not inventory Pythons, set
`PYTHONPATH` to repair it, or rebuild `.venv`. Rerun the same local helper
once through:

```text
./.ap/ap project check --root /home/agile/Projects/framenest --baseline 5abb2adfcd1d5f3391df9c3044b4b81ac1aac923
./.ap/ap exec --root /home/agile/Projects/framenest --baseline 5abb2adfcd1d5f3391df9c3044b4b81ac1aac923 --operation <id>
```

If the canonical envelope passes, continue and mention the ambient violation
briefly. If the canonical envelope itself fails, stop as an environment
limitation with sanitized evidence. This gate does not require a local pytest
suite.

Do not run `framenest_nuc_worker_gate.fish`.

## 8. Execution

Stop at the first unmet gate. Do not improvise a substitute identity, header,
or deploy.

### 8.1 Preflight

- Credential-free `git ls-remote https://github.com/cisarik/framenest.git refs/heads/main`
  equals `5abb2adfcd1d5f3391df9c3044b4b81ac1aac923`.
- Local HEAD, tree `4f5505c65f883a2eeba10d670e0a76f45c0f1a2a`, and `.ap`
  gitlink `17b7e085139e9bcbb0e4953d26aef9b6687d541c` match.
- Tracked tree clean. Owner untracked paths may exist; preserve them.
- No active Git lock, merge, or rebase.
- If `SSH_AUTH_SOCK` is absent, attach the GPG agent SSH socket and prove
  BatchMode SSH. Report only `gpg agent-ssh-socket attached` or `not needed`.
- Remote `sudo -n true` must exit 0. If it fails, stop `BLOCKED`; the
  COOPERATOR must establish the timestamp outside this Worker. Do not
  `sudo -v`.

### 8.2 Deployed release identity, service, health, database

Using `deploy/ubuntu/framenest-release` from the canonical checkout:

1. `framenest-release status` must exit 0.
2. Record only sanitized fields: `active_release`, `release_manifest`
   present|absent, service active/substate/result, database current/head,
   backup `restore_readiness`.
3. Prove `/opt/framenest/current` equals
   `/opt/framenest/releases/5abb2adfcd1d5f3391df9c3044b4b81ac1aac923`.
4. Marker and manifest SHA match `5abb2ad…`. Manifest `ap_gitlink` matches the
   required AP pin.
5. `framenest.service` is `active` / `running` / `success`, `ExecMainStatus=0`,
   `WorkingDirectory=/opt/framenest/current`.
6. Database is `at_head` `0028`/`0028`.
7. Backup restore readiness is `ready`. Off-device copy may remain
   unconfigured; record it as a pre-existing non-blocking observation, not a
   Gate E failure.
8. Release-local `check-database-ready` is `ready`/`0028` and `check-health`
   is `ready`, using `systemd-run` + unit `EnvironmentFile`.
9. `/run/framenest-release-deploy` and `/opt/framenest/current.next` are absent.
10. Previous `148b6c…` directory may remain; record present|absent. Do not
    delete it and do not roll back to it.
11. Funnel must report no configuration. Serve must remain the only remote
    application ingress. Do not change Serve. If Funnel is configured, stop
    `BLOCKED`.
12. Optional: `framenest-release check --release 5abb2adfcd1d5f3391df9c3044b4b81ac1aac923`
    is allowed and must not deploy.

If live current is not exactly `5abb2ad…`, stop. Do not deploy.

### 8.3 Three identities — retrieve privately, report only labels

Retrieve the three existing private test identities from live
`FRAMENEST_IDENTITY_MAP` on the NUC. Do not copy login strings into the
report, screenshots that you will quote, Meta, or chat.

Assign stable labels without printing logins:

```text
administrator  = the mapped admin used for this gate
ordinary-A     = first ordinary user
ordinary-B     = second ordinary user
```

If more than one admin or more than two users exist, select exactly one admin
and two users by a deterministic private sort of normalized login keys and
state only that a deterministic subset was chosen. If the live map does not
contain at least one `admin` and two `user` entries, stop `BLOCKED` and report
only the counts.

Expected capability sets, copied from
`src/framenest/domain/identity_access.py`:

```text
ordinary user:
  gallery.read, media.original.read, media.download,
  upload.submit, youtube.request, x.request

administrator:
  ordinary set plus
  metadata.canonical.write, upload.manage, analysis.run,
  provider.operate, media.import, library.scan, media.workflow.read,
  media.content.publish, youtube.acquire, x.acquire, media.catalog.remove
```

### 8.4 Authenticated login and role separation

Authoritative UI/API readback is **authenticated-same-origin-browser** against
the live configured `FRAMENEST_EXTERNAL_ORIGIN`. Do not print that origin.

```text
Identity header spoofing: none
Credential inspection: none
Authoritative readback mechanism: authenticated-same-origin-browser
Product-supported mechanism: Tailscale HTTPS Serve -> GET /api/identity/me
```

For each label `administrator`, `ordinary-A`, `ordinary-B` obtain a genuine
Tailscale-authenticated browser session as that mapped login. Then:

1. Load the web shell.
2. `GET /api/identity/me` must return `role` `admin` or `user` as expected,
   `provenance` `tailscale-serve`, and the exact capability set above.
3. In the report, replace login and display_name with the role label.

The workstation Tailscale identity may map to only one of the three labels.
Use it for that label. For the other labels, use only already-authenticated
Tailscale sessions that the COOPERATOR has made available to this Worker
(separate browser profile/tab already logged in as that tailnet member).

If a required identity has no genuine authenticated session:

- do not spoof headers or use the UDS as a fake user;
- do not claim PASS;
- stop `PARTIAL` or `BLOCKED`;
- name the missing role label only;
- smallest expansion: the COOPERATOR authenticates that mapped Tailscale login
  in a Worker-usable browser session, outside this prompt's mutation scope.

Do not send passwords, cookies, or identity-map values through chat.

### 8.5 Gallery and Details

For **each** of the three authenticated identities:

- Gallery loads published catalog items (`GET /api/media` succeeds).
- Open Details for one already-published item (`GET /api/media/{media_id}`
  succeeds).
- Do not report private media filenames, original paths, or identity logins.
  Report `gallery-PASS` / `details-PASS` per role label.

If Gallery has zero published items, stop `PARTIAL` and say so. Do not import,
publish, or seed media.

### 8.6 Ordinary-user restrictions

For **each** of `ordinary-A` and `ordinary-B`:

- `GET /api/admin/media` is denied (sanitized 403, not a data leak).
- UI hides administrator chrome: Details Edit, admin-media workflow, catalog
  removal, cover authoring, YouTube/X acquire, analysis/provider operate.
- Do not perform an upload, X/YouTube request, or other mutation to prove a
  restriction.

### 8.7 Administrator controls (observe, do not mutate)

For `administrator` only:

- `GET /api/admin/media` succeeds.
- UI shows Details Edit and the admin-media workflow control.
- Open the admin workflow list read-only.
- Confirm catalog-removal and publication controls are **visible**.
- Do not Save metadata, publish, remove, analyze, import, scan, acquire, or
  upload.

### 8.8 Browser stall guard

Record one Browser Stall Guard block if any browser episode fails. At most two
recovery attempts. After the guard triggers, `Browser repair after trigger: none`.
Missing browser evidence never becomes PASS.

### 8.9 Privilege release

Only after all required evidence is captured, run remote `sudo -K` once and
prove `sudo -n true` no longer succeeds. If the exact session is lost before
that, record `unknown-session-lost` with session-loss evidence; do not claim
`sudo -K` ran.

## 9. Acceptance control matrix

```text
Acceptance candidate: 5abb2adfcd1d5f3391df9c3044b4b81ac1aac923
Acceptance owner map: live test-NUC production at that SHA via Tailscale Serve
Acceptance allowlist: none; repository mutation forbidden
Acceptance risk claims: public/live SHA equality; service/health/schema/backup readiness; three mapped identities login and role separation; Gallery/Details; ordinary-user denial of admin workflow; administrator controls visible without mutation
Acceptance control matrix:
  positive: status identity 5abb2ad…; service active; db 0028/0028; health ready; restore_readiness ready; three GET /api/identity/me role/capability matches; three Gallery+Details; admin GET /api/admin/media 200; admin chrome visible
  negative: no deploy/rollback/restart; no catalog mutation; no header spoofing; ordinary GET /api/admin/media 403; ordinary admin chrome hidden; Funnel absent; secrets omitted
Acceptance independence: required-fresh-independent
```

## 10. Hard stop conditions

Stop without improvising only if:

- independence is compromised;
- public `main` is not `5abb2ad…`;
- SSH cannot be established after one GPG-agent-socket attachment attempt;
- remote `sudo -n true` is invalid at the initial gate;
- live current is not `5abb2ad…`;
- service, schema, health, or restore-readiness fails;
- Funnel is configured;
- the live identity map lacks one admin and two users;
- a required identity cannot be authenticated without spoofing;
- a privileged mutation outside this prompt would be required;
- browser stall guard triggers and required UI evidence remains absent.

A Cooperator-owned rendered-UX preference is not yours to waive. A missing
identity session is missing evidence, not a product defect to patch.

## 11. Cost and loop controls

- One Worker, one session, no subagents, no new Orchestrator.
- No full suite. No local pytest unless a local AP-exec helper is required
  for an encodings reroute, which this gate should not need.
- No second identity-map parse after the role counts are known.
- No Serve, Tailscale, or `.venv` repair loop.
- Unchanged hypothesis + unchanged missing identity + unchanged failing GET
  is not progress; stop and return.

## 12. Report contract

Begin exactly:

```markdown
### Report for ORCHESTRATOR_CHAT
```

Echo unchanged: logical whole identity, Worker session ordinal 29, Worker
exchange ordinal 01, Worker session target `fresh-worker-session`, Native
planning mode `not-used`.

Include:

- standard terminal status `PASS` | `PARTIAL` | `BLOCKED`;
- phase-qualified result `production-acceptance-PASS` | `not-applicable`;
- `Logical-whole closure: not-closed`;
- public-main readback; local HEAD; AP pin;
- sanitized live release identity, service, schema, health, backup;
- identity-map role counts only;
- per-label login/role/capability result with logins redacted;
- Gallery/Details per label;
- ordinary-user restriction result;
- administrator control result (no mutation);
- Funnel absent|present;
- privilege-release record (`observed-sudo-k` | `unknown-session-lost` |
  `not-applicable-no-sudo`);
- authenticated-readback block;
- Browser Stall Guard block, or `not-triggered` with
  `Verification succeeded: yes`;
- secrets omitted;
- residual risks, including pre-existing off-device backup if still disabled;
- one smallest next step for the Orchestrator (closure if PASS; otherwise the
  exact missing evidence). Do not recommend a new implementation Worker unless
  live evidence contradicts `5abb2ad…`;
- report justification `final-acceptance`;
- authority expiry;
- `Resolved Execution Issues / Near-Misses`;
- `Pre-Existing Failure Classification`.

Activated annexes required: production acceptance, privilege lifecycle,
authenticated readback, browser stall guard. Omit provider, publication,
deployment-mutation, and recovery annexes.

`PASS` / `production-acceptance-PASS` only if every control in §9 holds and no
required browser evidence is missing. Do not claim publication, deployment,
implementation, or logical-whole closure.

```text
Report justification: final-acceptance
Authority expiry: all Worker 29 exchange 01 production-acceptance / Gate E
authority expires at this terminal report
```
