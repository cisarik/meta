# Fresh ORCHESTRATOR Restoration Handoff

## FrameNest In-Process Lifecycle Runtime Contract

You are a fresh persistent ORCHESTRATOR for the FrameNest project developed under Analytic Programming.

Read this restoration handoff completely before issuing Worker authority.

Do not continue the preceding logical whole.

It is closed.

The new objective is to plan and deliver one bounded runtime-correctness logical whole for the lifecycle of FrameNest's in-process coordinators under the production systemd stop constraint.

Do not begin implementation before repository-grounded planning has passed the Plan-to-Execution Gate.

---

## 1. Persistent roles and communication

Persistent roles:

```text
COOPERATOR
ORCHESTRATOR
WORKER
```

COOPERATOR:

```text
Michal
```

Communicate with Michal in Slovak.

Use feminine grammatical gender for yourself in Slovak and masculine grammatical gender when addressing Michal.

Worker prompts and Worker terminal-report contracts are written in professional English.

Every standard Worker terminal report begins exactly:

```text
### Report for ORCHESTRATOR_CHAT
```

Michal controls model, agent, provider, reasoning configuration, cost and whether a prompt is launched.

Do not hard-code any model or provider as protocol authority.

Do not silently change Michal's route.

Recommend the lowest sufficient reasoning level for each Worker task and explain the recommendation briefly.

---

## 2. Governing Analytic Programming state

Canonical AP repository:

```text
cisarik/ap
/home/agile/Projects/ap
```

Directly verified public AP `refs/heads/main` on 2026-08-13:

```text
041de310ea33ed1b47dd8f5fbfcc2829d1a32514
```

Subject:

```text
docs: converge ADR-0014 lifecycle status
```

FrameNest public `main` consumes exactly that generation.

Before issuing Worker authority, directly re-establish current AP public `refs/heads/main` using Git transport:

```text
git ls-remote https://github.com/cisarik/ap.git refs/heads/main
```

Do not use GitHub webpages, search results, browser caches or remembered state as authoritative current-ref evidence.

If the ORCHESTRATOR runtime cannot perform direct Git transport, report that limitation truthfully and obtain exact fresh ref evidence through the authorized repository-local Worker restoration gate. Do not silently substitute cached web evidence.

Existing evidence-backed AP backlog candidate:

```text
AP public-ref verification transport and fallback contract
```

Do not mutate `cisarik/ap` during the new FrameNest logical whole.

Do not open an AP implementation whole merely because this observation exists.

Worker 1 in the preceding analysis whole reported:

```text
AP empirical observations: none
```

The native Plan-mode wrapper placed YAML and a title before Worker 1's standard terminal-report heading. The report body itself was complete and authority-unambiguous. Treat this as a non-material presentation deviation, not a new AP defect or authority to start another analysis cycle.

---

## 3. Closed predecessor: analysis and selection

Closed logical whole:

```text
FrameNest Runtime Architecture, Maintainability and Backlog Rebaseline
```

Identity:

```text
framenest-runtime-architecture-maintainability-and-backlog-rebaseline
```

Final state:

```text
CLOSED: PASS
```

Worker route completed:

```text
fresh Worker 1
read-only repository evidence
Native Plan Mode required
one plan-only cycle
non-independent evidence posture
no delegation
```

Worker 1 terminal result:

```text
Standard terminal status: PASS
Phase-qualified result: not-applicable
Report justification: new-evidence
```

Worker 1 authority expired at the terminal report.

No Worker 2 exists for the closed analysis whole.

No repository mutation, commit, publication, deployment, production mutation, provider call, AP mutation or Meta mutation occurred.

The ORCHESTRATOR independently verified the current public refs and the decisive code evidence before closure.

Do not reopen this analysis whole unless concrete contradictory repository evidence invalidates the selected causal boundary.

---

## 4. Directly verified public restoration anchors

Direct Git transport on 2026-08-13 established:

```text
cisarik/ap refs/heads/main
041de310ea33ed1b47dd8f5fbfcc2829d1a32514

cisarik/framenest refs/heads/main
a72be476f5634394287082be07380d03fa7ccd4d

cisarik/meta refs/heads/main
d3bb8a591b8e510d68521527c75bc1f2ff51bd2b
```

FrameNest public commit:

```text
a72be476f5634394287082be07380d03fa7ccd4d
```

Parent:

```text
d4c3402a4765b39cee0d8e2063d5ec8be161caf6
```

Tree:

```text
5f8afa3d2705fd9a60d8375e963699e9be5e9335
```

Subject:

```text
chore: adopt current AP generation
```

Public FrameNest `.ap` gitlink:

```text
041de310ea33ed1b47dd8f5fbfcc2829d1a32514
```

These are restoration anchors, not substitutes for fresh current-state verification by the successor ORCHESTRATOR and Worker.

---

## 5. Current local FrameNest restoration context

Repository:

```text
cisarik/framenest
/home/agile/Projects/framenest
```

Worker 1 observed the local canonical checkout read-only:

```text
branch: feat/ap-baseline-bound-execution-adoption
upstream: none
local HEAD: d4c3402a4765b39cee0d8e2063d5ec8be161caf6
tracked/index state: clean
origin/main: a72be476f5634394287082be07380d03fa7ccd4d
```

Local HEAD subject:

```text
fix: repin AP adoption to published execution envelope
```

The local branch is intentionally behind accepted public `main` by the repository-only AP adoption commit.

Local production `src/` was observed identical to public `main`; the public difference was exactly:

```text
.ap
README.md
tests/contract/test_ap_integration.py
```

Local `.ap` checkout and local HEAD gitlink were:

```text
4862380f351ddd74e1c141a4babe2d0f0b43979d
```

The local `.ap` checkout was detached and clean, valid for local HEAD but one accepted AP generation behind public FrameNest `main`.

Pre-existing untracked owner material was preserved:

```text
.accept-immut-work/
.playwright-mcp/
.w6-immut-work/
REPRO_DIR=/
uv.lock
```

`uv.lock` is not project authority.

Do not reset, clean, stash, delete, overwrite or cosmetically normalize the canonical worktree.

Do not checkout public `main` in the canonical worktree merely to make local and public SHAs equal.

The successor planning Worker must freshly inspect:

```text
public refs/heads/main
local HEAD
branch/detached state
tracked/index state
relevant untracked state
origin identity
.ap gitlink for the intended baseline
.ap checkout and cleanliness
ap.project.conf
AGENTS.md
docs/WORKER_EXECUTION_CONTRACT.md
```

Planning remains read-only.

If later implementation is authorized, strongly prefer an isolated candidate worktree based on the exact accepted public baseline rather than disturbing the canonical owner checkout. The implementation prompt must explicitly authorize any worktree, branch, submodule or Git-ref mutation it requires.

---

## 6. Production and deployment restoration context

Last accepted deployment evidence before the repository-only AP adoption whole:

```text
6bf6f1d542d46c4365ae430b39eff197c2f3db87
schema 0028
```

The AP adoption commit had:

```text
runtime impact: none
deployment impact: none
schema impact: none
production impact: none
```

Therefore repository public `main` and production runtime intentionally differ.

Living documentation still contains an older production SHA:

```text
aec2f0091c10aed2fc2033dac154a0d9651b2b6d
```

This is a stale operator-status projection, not authority to reopen the closed documentation-convergence or recovery wholes.

The closed analysis phase did not SSH to production. Current live production SHA was not freshly read back.

Before any future deployment or production acceptance, an explicitly authorized read-only production preflight must establish the current release, service state, schema and rollback anchor.

Do not deploy merely to equalize repository and production SHAs.

---

## 7. Infrastructure and host boundaries

Development workstation:

```text
CachyOS
```

Production host:

```text
Intel NUC6i5SYH
Ubuntu
headless
```

Current network context supplied by the COOPERATOR:

```text
NUC and CachyOS workstation are on the same router over Wi-Fi
```

Known working SSH invocation:

```text
ssh -i ~/.ssh/id_ed25519_framenest_nuc_cachyos michal@framenest-nuc
```

No HDMI monitor is currently available for the NUC. This does not block headless administration.

Explicitly deferred and out of scope:

```text
VPS
Kiosk
exit-node work
broad network redesign
```

Do not infer public reachability from an expected future public ISP address.

No NUC, SSH, firewall, router, Tailscale or production authority exists during the initial planning phase.

---

## 8. Selected new logical whole

Name:

```text
FrameNest In-Process Lifecycle Runtime Contract
```

Identity:

```text
framenest-in-process-lifecycle-runtime-contract
```

Purpose:

> Establish a bounded, observable and testable lifecycle contract for FrameNest's six in-process coordinators so startup, runner failure and application shutdown behave safely within the production systemd stop constraint while preserving durable recovery and existing product behavior.

This is a runtime-correctness and operational-integrity whole.

It is not a product-feature, UI/UX, distributed-job-queue, schema or acquisition-domain redesign whole.

The next immediate phase is repository-grounded implementation planning.

Do not begin mutation until that plan has been reviewed and explicitly accepted by the successor ORCHESTRATOR.

---

## 9. Why this candidate was selected

Worker 1 compared six serious candidates:

```text
C1 in-process lifecycle runtime / stop contract
C2 packaged web-client module boundary
C3 living production-SHA documentation rebaseline
C4 YouTube/X acquisition unification
C5 NUC security hardening
C6 JavaScript/Python duplicated domain constants
```

The ORCHESTRATOR accepted C1.

Reasons:

- it is backed by direct current-code evidence rather than architectural taste;
- it crosses a runtime correctness and data-integrity boundary;
- it can affect restart behavior for SQLite, staging media and child processes;
- it is bounded without reopening accepted upload, YouTube, X, backup or UI wholes;
- the high-churn `app.js` candidate is currently dominated by the owner's UI/UX deferral and would otherwise be speculative restructuring;
- YouTube/X unification contradicts their accepted intentional separation;
- NUC hardening remains genuinely open but was explicitly not to be auto-selected and lacks current host evidence in this analysis;
- stale production-status documentation is real but too small and orthogonal to dominate the next engineering whole;
- no safe FrameNest test-retirement candidate was evidenced.

Do not reinterpret file size alone as the selection rationale.

---

## 10. Independently verified causal evidence

The ORCHESTRATOR independently cloned exact public FrameNest commit:

```text
a72be476f5634394287082be07380d03fa7ccd4d
```

and directly confirmed:

1. Production systemd contract:

```text
KillSignal=SIGTERM
TimeoutStopSec=30s
Restart=on-failure
```

2. Uvicorn process model:

```text
workers=1
```

3. FastAPI lifespan starts six coordinators and shuts them down sequentially through nested `finally` blocks:

```text
media analysis
upload catalog
upload publication
upload validation
YouTube acquisition
X acquisition
```

Shutdown order is reverse startup order, but every shutdown is awaited sequentially without one explicit process-level deadline.

4. YouTube acquisition shutdown:

- marks stopping;
- wakes the runner;
- cancels the runner when active;
- awaits the cancelled runner.

5. X acquisition shutdown:

- requests shutdown;
- wakes the runner;
- awaits the runner without cancelling an in-flight acquisition.

6. Upload validation, catalog, publication and media-analysis coordinators own thread-pool executors and call:

```text
ThreadPoolExecutor.shutdown(wait=True, cancel_futures=False)
```

without a coordinator or process-level deadline.

7. YouTube downloader default timeout:

```text
7_200 seconds
```

and its child-process termination path allows TERM and KILL grace periods.

8. The YouTube runner catches broad `Exception` in its loop and converts it to:

```text
progressed = False
```

This can hide repeated runner work failures from lifecycle-level visibility even though durable claim logic may still record some operation failures.

These are verified static facts.

The following is a risk inference, not an observed production incident:

> Sequential unbounded shutdown can exceed the 30-second systemd envelope, after which systemd may force-kill the process while SQLite, staging state, executor work or a child process is active.

No field SIGKILL, corrupt database or lost-media incident was established during the analysis whole.

The new logical whole must prove or safely refute the risk through focused tests rather than reporting the inference as an observed incident.

---

## 11. Governing shutdown constraint

The production unit's:

```text
TimeoutStopSec=30s
```

is the external process-level maximum, not automatically the internal application deadline.

The planning Worker must define:

- an explicit internal graceful-stop budget below the external 30-second limit;
- a safety reserve for Uvicorn, event-loop cleanup, engine disposal and systemd scheduling;
- whether deadlines are global, per-component or both;
- how one monotonic absolute deadline propagates across sequential cleanup;
- what work is cancelled, checkpointed, abandoned for durable recovery or allowed to finish;
- what evidence proves that no child process or non-daemon thread silently defeats the deadline.

Do not lengthen `TimeoutStopSec` merely to hide unbounded application behavior.

Do not assume that any of these alone proves bounded process exit:

```text
asyncio task cancellation
asyncio.wait_for around a blocking thread
ThreadPoolExecutor.shutdown(wait=False)
cancel_futures=True
setting a stop flag
```

Python executor threads and blocking OS operations require explicit semantic analysis and focused proof.

---

## 12. Required planning questions

Fresh planning Worker 1 must answer, from exact repository evidence:

1. What lifecycle states and operations are genuinely shared across the six coordinators?
2. Which differences are intentional domain behavior and must remain distinct?
3. Is a structural `Protocol`, a small lifecycle adapter, a supervisor, deadline helpers or a narrower composition-root change the smallest coherent design?
4. How is partial-startup rollback preserved when coordinator startup fails?
5. How is reverse-order shutdown preserved without nested unbounded waits?
6. How is one total internal deadline allocated beneath the 30-second systemd constraint?
7. How are in-flight asyncio tasks, blocking executor calls and subprocess groups treated differently?
8. Can running thread work be made cooperatively cancellable, bounded at its I/O boundary or safely abandoned to durable recovery?
9. How is runner death or repeated failure made observable without leaking secrets, causing noisy loops or changing product outcomes?
10. Which coordinator methods should remain `start`, `notify`, `drain`, `shutdown` and `runner_done`, and which should not be forced into a false generic abstraction?
11. Which existing recovery invariants guarantee safe continuation after a cancelled or killed operation?
12. Which focused tests can prove bounded stop with injected millisecond-scale budgets instead of sleeping for 30 seconds?
13. Which process-level integration test can send SIGTERM to a temporary FrameNest process and verify timely exit, child cleanup and recoverable SQLite/staging state?
14. Which existing upload, YouTube, X, publication, validation, analysis and recovery tests form the smallest sufficient regression set?
15. Does `deploy/systemd/framenest.service` require any mutation, or is it only the immutable alignment constraint?

The Worker must distinguish verified defects, risk inferences, accepted design differences and optional refactoring.

---

## 13. Provisional causal surface

Planning must inspect at least:

```text
src/framenest/adapters/api/application.py
src/framenest/server.py
src/framenest/application/upload_validation_coordinator.py
src/framenest/application/upload_catalog_coordinator.py
src/framenest/application/upload_publication_coordinator.py
src/framenest/application/media_analysis_coordinator.py
src/framenest/application/youtube_acquisition.py
src/framenest/application/x_acquisition.py
src/framenest/infrastructure/youtube/downloader.py
src/framenest/infrastructure/x/downloader.py
src/framenest/infrastructure/media_analysis/process.py
deploy/systemd/framenest.service
```

It must then identify exact focused existing tests and the smallest proposed new-test surface.

This is an inspection surface, not yet a mutation allowlist.

The planning Worker must not expand from coordinator lifecycle sections into unrelated domain state machines merely because some classes share a file.

---

## 14. Provisional inclusions

Subject to the accepted repository-grounded plan, the coherent whole may include:

- explicit coordinator lifecycle semantics;
- bounded startup rollback and reverse-order shutdown supervision;
- an internal deadline safely below the 30-second external stop limit;
- cancellation or bounded termination of in-flight subprocess work;
- safe treatment of executor-backed blocking work;
- runner liveness and failure observability;
- focused fake-slow-work tests;
- focused lifespan and process-level SIGTERM tests;
- preservation and verification of durable startup recovery.

Do not force all coordinators into one abstraction if the evidence supports a smaller composition-root or adapter solution.

---

## 15. Explicit exclusions

Do not include:

```text
YouTube/X domain-model unification
generic distributed job queue
multiprocess leases
SQLite WAL redesign
schema or Alembic migration
upload or acquisition product-state changes
provider calls
app.js split
broad UI/UX work
backup or restore redesign
NUC hardening
UFW or AppArmor
VPS
Kiosk
Tailscale or exit-node work
static X photo acquisition
multi-model metadata comparison
Tauri or desktop integration
Cover Studio expansion
multi-device synchronization
```

Do not reopen closed product wholes merely because their coordinators participate in process shutdown.

Preserve existing upload, publication, analysis, YouTube and X outcomes.

Preserve schema `0028` unless new contradictory evidence makes the logical whole impossible without a separately approved objective change.

---

## 16. Closed-state protection

Do not reopen these without concrete regression evidence:

```text
Technical MVP
ordinary-user private upload
administrator review/publication
admin batch actions
durable media removal
automated catalog backup and restore verification
requester-private YouTube acquisition
YouTube/X creator taxonomy
requester-private X acquisition
off-device catalog recovery
operator-workstation pull snapshot/recovery
repository authority and Worker execution-contract convergence
current AP generation consumer convergence
runtime architecture/maintainability/backlog rebaseline
```

Lifecycle changes may touch coordinator code associated with closed features only to establish the selected process lifecycle contract. They must not redesign accepted product behavior.

---

## 17. Python environment constraint

Canonical FrameNest virtual environment:

```text
/home/agile/Projects/framenest/.venv
```

Its `pyvenv.cfg` previously identified uv-managed CPython 3.13.9 with base executable:

```text
/home/agile/.local/share/uv/python/cpython-3.13.9-linux-x86_64-gnu/bin/python3.13
```

A pre-existing Cursor AppImage environment defect was independently established: inherited Cursor-provided `LD_LIBRARY_PATH` caused Python prefix/stdlib discovery failures, including:

```text
Failed to import encodings module
ModuleNotFoundError: No module named 'encodings'
```

Invocation-local removal of inherited `LD_LIBRARY_PATH` allowed the physical CPython interpreter to operate.

Do not:

```text
delete or rebuild .venv
run poetry env use
run uv sync merely to repair the anomaly
install or change dependencies without explicit authority
attribute the anomaly to the selected lifecycle candidate
```

No Worker may invoke `cursor`, `code`, `xdg-open`, GUI applications or AppImages unless explicitly authorized.

---

## 18. Immediate first Worker route

Unless fresh restoration evidence reveals a prerequisite that blocks responsible planning, issue exactly one:

```text
fresh Worker 1
```

Profile:

```text
FrameNest In-Process Lifecycle Runtime Contract Planner
```

Phase:

```text
repository-grounded implementation planning
```

Mandatory routing fields:

```text
Worker session target: fresh-worker-session
Native planning mode: required
Planning layer: implementation-planning
Orchestration planning owner: ORCHESTRATOR
Worker planning scope: exact bounded implementation design for the FrameNest in-process lifecycle runtime and shutdown contract
Plan disposition: approval-gated
Implementation in same Worker session: prohibited
Planning stop event: terminal planning report submitted
Execution authority event: explicit ORCHESTRATOR prompt with Native planning mode: not-used
Post-plan implementation session: fresh-worker-session
Maximum plan-only cycles: 1
Evidence posture: non-independent
Authority renewal: not applicable — fresh Worker authority originates only in the new prompt
```

Implementation authority:

```text
none
```

Repository mutation authority:

```text
none
```

Publication authority:

```text
none
```

Deployment authority:

```text
none
```

Production mutation authority:

```text
none
```

Provider authority:

```text
none
```

AP mutation authority:

```text
none
```

Meta mutation authority:

```text
none
```

Delegation/sub-agents:

```text
not authorized
```

💡 Native Plan Mode

Recommended reasoning:

```text
Extra High
```

Reason:

The design must reconcile asyncio cancellation, blocking executor behavior, child-process groups, durable recovery, partial-startup rollback and one process-level deadline without creating a false generic abstraction or reopening accepted domain behavior.

The recommendation is advisory. Michal chooses the actual route.

---

## 19. Planning Worker expected output

Require one decision-complete plan containing approximately:

```text
fresh restoration gate
verified current public/local/AP-pin state
exact lifecycle behavior matrix for all six coordinators
exact external and proposed internal stop-budget model
blocking-thread and subprocess cancellation semantics
minimal chosen architecture with rejected alternatives
startup and shutdown ordering
runner-failure observability contract
durable recovery invariants
exact proposed mutation allowlist
explicit exclusions
focused test matrix
existing regression-test selection
candidate Git/worktree strategy preserving owner state
publication boundary
deployment and production-acceptance boundary
rollback strategy
residual risks and stop conditions
AP empirical observations, if any
```

Do not ask for a generic refactor catalogue, full security audit, full test run or implementation.

One safe, bounded and decision-complete plan is the target.

Do not repeat planning without new evidence. Maximum plan-only cycles is one.

---

## 20. Plan-to-execution gate

Worker 1's terminal planning report expires its authority.

The successor ORCHESTRATOR must review the plan against:

- exact current repository evidence;
- the 30-second external constraint and internal reserve;
- Python executor and child-process semantics;
- closed-state protection;
- bounded path allowlist;
- durable recovery;
- testability without real provider calls;
- no schema/product/UI expansion;
- safe candidate-worktree strategy.

An approved plan does not authorize implementation.

Implementation requires a new complete prompt with:

```text
Worker session target: fresh-worker-session
Native planning mode: not-used
```

The new logical whole resets Worker numbering. The planning Worker is Worker 1; a separately authorized fresh implementation session would be Worker 2.

Recommended implementation reasoning is normally High or the lowest sufficient level justified by the accepted plan. Extra High should not be used automatically after design uncertainty has been resolved.

The implementation prompt must define:

```text
exact accepted baseline
isolated worktree/branch strategy
exact mutation allowlist
explicit prohibitions
focused validation
commit authority
push/publication authority, normally none
deployment authority, none
production authority, none
terminal report contract
```

Do not grant implementation, publication, deployment and production authority implicitly in one broad prompt.

---

## 21. Anticipated evidence chain

The likely chain, subject to the accepted plan and successor ORCHESTRATOR judgment, is:

```text
fresh Worker 1 — read-only implementation planning
ORCHESTRATOR plan acceptance
fresh Worker 2 — bounded implementation candidate
fresh Worker 3 — independent acceptance of the exact immutable candidate
fresh publication Worker — ordinary non-force fast-forward publication
separately authorized deployment/preflight route if deployment is required
production acceptance focused on SIGTERM/restart and durable-state integrity
ORCHESTRATOR closure
```

Fresh independent acceptance is expected because the change affects process shutdown, child-process cancellation and SQLite/staging integrity.

Do not predetermine Worker ordinals beyond actual sessions. Number sequentially from Worker 1 within this new logical whole.

Do not audit an audit or create recursive acceptance loops.

One bounded correction normally returns to the implementing Worker under renewed authority; re-acceptance is then scoped according to the changed surface and risk.

---

## 22. Focused acceptance expectations

The accepted candidate should eventually prove, proportionately:

1. all started coordinators are cleaned up in reverse order after normal operation;
2. partial startup failure cleans up only successfully started resources and disposes the engine;
3. one global internal shutdown deadline cannot be multiplied by six sequential per-coordinator waits;
4. fake slow asyncio work is cancelled or safely handed to durable recovery;
5. fake blocking executor work cannot silently invalidate the process-level deadline;
6. YouTube and X child-process groups receive bounded termination and are reaped;
7. runner failure becomes observable and does not leave the service silently stalled;
8. accepted upload, publication, analysis, YouTube and X state-machine outcomes remain unchanged;
9. temporary SQLite and staging state remains recoverable after forced interruption;
10. a temporary process receiving SIGTERM exits inside the proven internal envelope with reserve below systemd's 30-second maximum;
11. no provider calls or production data are required for automated acceptance;
12. schema remains `0028` unless an explicit separately approved objective change exists.

Use injected small deadlines and deterministic fakes for most automated tests. Do not make the suite sleep for the real 30-second production limit.

Production SIGTERM/restart acceptance, if required, needs separate explicit host and service-mutation authority, a current release preflight, rollback anchor and no-active-job/state reconciliation.

---

## 23. Candidate boundaries and rollback

Expected publication route:

```text
ordinary non-force fast-forward only after exact-candidate acceptance
```

No force push.

No schema migration is expected.

Rollback should remain the previously accepted release after verifying that the candidate introduced no incompatible durable state.

Durable claims should remain recoverable through existing startup reconciliation.

Do not claim that rollback is safe merely because schema is unchanged; acceptance must verify that lifecycle changes do not introduce an incompatible transient-state assumption.

---

## 24. Test-burden and documentation findings carried forward

The preceding analysis established:

- Python production: approximately 59,624 LOC across 210 files;
- packaged web: approximately 17,592 LOC;
- Python tests: approximately 68,303 LOC across 197 files;
- JavaScript tests: approximately 14,621 LOC across 21 files;
- no evidence-backed obsolete FrameNest tests safe to delete;
- authorization, privacy, migration, restore and integrity tests have high preservation value;
- `test_local_web_application.py` has heavy implementation-detail coupling, but this does not authorize a web-client refactor in the selected whole;
- `app.js` is the largest churn concentration, but UI/UX/module work remains deferred until a concrete later boundary.

Do not start a test-reduction whole by analogy with AP's retired monolithic suite.

Do not repair unrelated stale documentation inside the lifecycle candidate unless the accepted plan proves it is causally necessary.

---

## 25. Backlog classification carried forward

Preserve these classifications:

```text
NUC Security Hardening: genuinely open, not selected now
portable/rebuildable sidecars: genuinely open, not selected now
living production-SHA projection: stale/regression, not selected as engineering whole
ROADMAP/SPEC/X comments: superseded or stale, misrouting risk only
multi-model draft comparison: frozen
movie richer taxonomy/reasoning: frozen
responsive/mobile screenshot work: parked/deferred by owner
multi-device synchronization: deferred/too broad
YouTube/X unification: rejected as speculative against accepted design
test retirement: not supported by current evidence
```

Do not let these adjacent items expand the selected lifecycle objective.

---

## 26. Meta repository

Repository:

```text
cisarik/meta
/home/agile/meta
```

Directly verified public Meta `main` on 2026-08-13:

```text
d3bb8a591b8e510d68521527c75bc1f2ff51bd2b
```

Meta is historical evidence only.

Meta is not AP semantic authority and not FrameNest product authority.

Current documented archive grammar uses date-free ordering:

```text
projects/<project>/<archive-sequence>/<logical-whole-sequence>-<logical-whole-identity>/
```

Do not trust remembered local Meta state. Establish current public/local state before any separately authorized archival work.

Do not mutate Meta in the initial planning phase.

Do not invent Worker reports or archive artifacts for sessions that did not happen.

---

## 27. Security treatment

This is not a general security audit.

No concrete authorization hole was hunted or established during the closed analysis whole.

Lifecycle planning must still preserve:

```text
identity and capability boundaries
ordinary-user privacy
SQLite integrity
filesystem staging/publication integrity
safe child-process termination
no secret leakage in lifecycle error reporting
```

Do not inspect secrets, `.env`, browser profiles, credentials or unrelated private data.

Do not exploit production.

Do not call external AI or acquisition providers.

If planning reveals a concrete material security defect, report exact evidence and stop before expanding scope.

---

## 28. Closure target for the new logical whole

The new logical whole may close only when proportionate evidence establishes, where applicable:

1. an accepted repository-grounded lifecycle design;
2. a bounded implementation candidate from the exact accepted baseline;
3. focused automated validation;
4. fresh independent acceptance of the exact immutable candidate;
5. ordinary non-force publication and direct public readback;
6. deployment only if required and separately authorized;
7. production SIGTERM/restart acceptance only if required and separately authorized;
8. durable-state and rollback reconciliation;
9. no active Worker authority or mutation;
10. ledger and residual-risk reconciliation.

Implementation PASS, Acceptance PASS, Publication PASS, Deployment PASS and Production acceptance PASS remain distinct.

None alone is ORCHESTRATOR closure.

Only the ORCHESTRATOR may emit the project closure signal.

---

## 29. Immediate ORCHESTRATOR actions

After reading this handoff:

1. restore current AP semantic authority through direct Git transport;
2. establish fresh FrameNest public/local/AP-pin state without repairing owner state;
3. preserve every closed logical whole and explicit owner deferral;
4. declare the active whole `framenest-in-process-lifecycle-runtime-contract`;
5. issue exactly one fresh read-only Worker 1 implementation-planning prompt;
6. require Native Plan Mode;
7. recommend Extra High reasoning for the planning task;
8. authorize no implementation, publication, deployment, production, provider, AP or Meta mutation;
9. require the Worker to define the internal budget below the 30-second external limit and prove thread/subprocess semantics;
10. review the terminal plan and make a direct accept, reject or exact-missing-evidence decision;
11. if accepted, issue a separate complete fresh Worker 2 implementation prompt with Native planning mode not-used and the exact approved allowlist;
12. keep acceptance, publication and deployment authority separate.

Do not ask Michal to retell history recoverable from this handoff and the repositories.

The correct next movement is:

```text
fresh ORCHESTRATOR restoration
-> fresh Worker 1 lifecycle-contract implementation planning
-> ORCHESTRATOR plan decision
-> separately authorized fresh Worker 2 implementation
-> risk-proportionate independent acceptance
-> publication
-> deployment/production acceptance only if required
-> ORCHESTRATOR closure
```

Nothing else is currently authorized.

