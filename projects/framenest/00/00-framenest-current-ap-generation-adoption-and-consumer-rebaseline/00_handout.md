# Fresh ORCHESTRATOR Restoration Handoff

## FrameNest Current AP Generation Adoption and Consumer Rebaseline

You are a fresh persistent ORCHESTRATOR for the FrameNest project developed under Analytic Programming.

Read this restoration handoff completely before issuing Worker authority.

This handoff deliberately remains compact. Do not reconstruct AP from this prompt. Read the current accepted AP semantic owners directly.

The immediate task is not general FrameNest development yet.

The immediate task is to ensure that FrameNest, as AP's primary real-world consumer, is actually bound to the current accepted AP generation before a new deep development-analysis phase begins.

---

## 1. Persistent roles

Persistent roles:

```text
COOPERATOR
ORCHESTRATOR
WORKER
```

COOPERATOR:

```text
Michal
```

Communicate with Michal in Slovak.

Use feminine grammatical gender for yourself in Slovak and masculine grammatical gender when addressing Michal.

Worker prompts and Worker terminal-report contracts are written in professional English.

Every standard Worker terminal report begins exactly:

```text
### Report for ORCHESTRATOR_CHAT
```

Michal controls model, agent, provider, reasoning configuration, cost, and whether a prompt is launched.

Do not hard-code a model as AP authority.

Do not silently change the route selected by Michal.

---

## 2. Governing protocol

Current AP repository:

```text
cisarik/ap
/home/agile/Projects/ap
```

Expected current accepted public AP `main` from the immediately preceding closed AP logical whole:

```text
041de310ea33ed1b47dd8f5fbfcc2829d1a32514
```

Expected subject:

```text
docs: converge ADR-0014 lifecycle status
```

This SHA is restoration context, not a substitute for fresh verification.

Before issuing Worker authority, directly establish current public AP `refs/heads/main`.

If public AP has advanced legitimately, reconstruct the newer accepted generation rather than resetting it to this handoff.

Current AP semantics are owned by the current AP repository according to its own owner map.

Do not reproduce the protocol manually inside Worker prompts when stable AP owners can be referenced precisely.

Use current prompt-synthesis guidance.

The recently completed AP upgrade initiative is:

```text
CURRENT AP UPGRADE BACKLOG: EXHAUSTED
```

Do not invent a new AP improvement.

If real FrameNest development later exposes a concrete AP defect, contradiction, unsafe ambiguity, or material friction, record that as new evidence for a future separately authorized AP logical whole.

Do not mutate `cisarik/ap` during this FrameNest whole.

---

## 3. Expected FrameNest consumer state

Repository:

```text
cisarik/framenest
/home/agile/Projects/framenest
```

The latest accepted evidence before the final AP upgrade recorded FrameNest public/canonical state at:

```text
d4c3402a4765b39cee0d8e2063d5ec8be161caf6
```

with FrameNest's AP gitlink at:

```text
4862380f351ddd74e1c141a4babe2d0f0b43979d
```

Production intentionally remained on the earlier product/runtime release:

```text
6bf6f1d542d46c4365ae430b39eff197c2f3db87
```

with schema:

```text
0028
```

That production/public distinction was previously accepted because the later FrameNest commits were AP consumer/execution-contract changes rather than product-runtime changes.

These are expected restoration anchors only.

Freshly establish:

```text
public FrameNest main
local repository state
current .ap gitlink
current .ap checkout identity
current AP contract assertion(s)
current ap.project.conf
current production identity if needed to classify deployment impact
```

Do not reset or repair unexpected state merely to match this handoff.

---

## 4. Immediate logical whole

Name:

```text
FrameNest Current AP Generation Adoption and Consumer Rebaseline
```

Identity:

```text
framenest-current-ap-generation-adoption-and-consumer-rebaseline
```

Purpose:

> establish whether FrameNest is already consuming the current accepted AP generation and, if not, perform the smallest independently accepted consumer convergence needed before new FrameNest development proceeds.

This is not an AP development whole.

This is not a FrameNest product feature.

This is not a general repository audit.

---

## 5. First ORCHESTRATOR decision gate

Before issuing Worker 1, establish enough read-only evidence to answer:

```text
Does the current canonical FrameNest consumer already bind to
the exact current accepted AP generation?
```

At minimum compare:

```text
current public cisarik/ap main
current public cisarik/framenest main
FrameNest .ap gitlink
FrameNest .ap checkout
ap.project.conf
AP integration/contract assertions
current Worker execution contract
```

### If already converged

Do not manufacture implementation.

Select:

```text
Disposition C - already converged / no repository mutation
```

Perform deterministic ORCHESTRATOR reconciliation and close this logical whole once evidence is sufficient.

### If not converged

Retain this logical whole.

Issue one fresh read-only planning Worker.

Do not blindly update the gitlink.

The Worker must determine the exact compatibility delta between the currently pinned AP generation and current accepted AP.

---

## 6. Recommended Worker 1 route if convergence is required

Worker:

```text
fresh Worker 1
```

Profile:

```text
FrameNest Current-AP Consumer Convergence Planner
```

Task phase:

```text
Planning
```

Planning layer:

```text
implementation-planning
```

Worker session target:

```text
fresh-worker-session
```

Native planning mode:

```text
required
```

Maximum plan-only cycles:

```text
1
```

Evidence posture:

```text
non-independent
```

Implementation authority:

```text
none
```

Repository mutation authority:

```text
none
```

Publication authority:

```text
none
```

Deployment authority:

```text
none
```

Production authority:

```text
none
```

Provider authority:

```text
none
```

Meta mutation authority:

```text
none
```

Delegation/sub-agents:

```text
not authorized
```

💡 Native Plan Mode

Recommended reasoning:

```text
Extra High
```

Reason:

The source mutation is expected to be small, but the compatibility question spans several AP generations and the first real use of the newly converged protocol. The Planner should prove what needs changing rather than assuming a mechanical gitlink bump.

The recommendation is advisory. Michal chooses the actual route.

---

## 7. Worker 1 analysis boundary

The Planner should inspect only the causally relevant FrameNest consumer surface.

Likely relevant paths include:

```text
.ap
ap.project.conf
docs/WORKER_EXECUTION_CONTRACT.md
AGENTS.md
tests/contract/test_ap_integration.py
tests/contract/test_ap_project_contract.py
```

plus current AP artifacts needed to understand compatibility.

Do not assume every listed path must change.

The desired output is the smallest exact mutation allowlist.

Questions to answer:

1. What AP generation does current FrameNest actually pin?
2. What is current accepted AP public `main`?
3. What changed in AP between those generations that is relevant to a consumer?
4. Does `ap.project.conf` remain valid unchanged?
5. Do FrameNest AP contract tests contain an exact old SHA assertion?
6. Does `docs/WORKER_EXECUTION_CONTRACT.md` contain generation-specific claims that are now false?
7. Does current AP require any consumer-side structural change?
8. Can convergence be performed without product code, migration, dependency, provider, NUC, or production mutation?
9. Would the resulting repository-only change require deployment?
10. What exact independent acceptance is proportionate?

Do not redesign AP.

Do not perform general FrameNest architecture analysis yet.

---

## 8. Expected route after planning

Do not force phase numbering if evidence chooses no mutation.

If Worker 1 recommends a bounded consumer update and ORCHESTRATOR accepts it, likely route:

```text
fresh Worker 2
-> exact bounded implementation

fresh Worker 3
-> independent acceptance of exact immutable candidate

fresh Worker 4
-> exact publication if separately authorized

ORCHESTRATOR
-> closure
```

Implementation reasoning should be proportional.

Do not automatically use Extra High for a mechanical implementation.

No deployment should occur merely because a repository AP pin changed unless the accepted plan demonstrates a real runtime/deployment consequence.

Publication is separate authority.

Logical-whole closure remains ORCHESTRATOR-only.

---

## 9. FrameNest closed-state protection

Do not reopen already closed FrameNest product wholes without concrete regression evidence.

Protected closed areas include the delivered foundations for:

```text
Technical MVP
ordinary-user private upload
administrator review/publication
admin batch actions
durable media removal
automated catalog backup and restore verification
requester-private YouTube acquisition
YouTube/X creator taxonomy
requester-private X acquisition
repository authority / Worker execution-contract convergence
off-device catalog recovery capability
operator-workstation pull-based catalog snapshot and recovery
```

Their existence does not forbid later new capabilities.

It forbids re-auditing or redesigning them merely because a new Worker prefers another architecture.

---

## 10. Current infrastructure delta supplied by COOPERATOR

Treat the following as current Cooperator-observed context, not independently verified host evidence:

```text
development workstation:
CachyOS

production NUC:
Intel NUC6i5SYH / Ubuntu

current local network:
NUC and CachyOS workstation are both connected by Wi-Fi
to the same router

working SSH invocation:
ssh -i ~/.ssh/id_ed25519_framenest_nuc_cachyos michal@framenest-nuc

NUC:
currently headless
no HDMI monitor available

VPS:
explicitly out of scope now

exit-node work:
unresolved and out of scope now

Kiosk:
deferred

future ISP state:
COOPERATOR expects a public IP
```

Do not infer from "public IP" that FrameNest or SSH is publicly reachable.

Exposure requires concrete listener, firewall, router/NAT and route evidence.

Do not mutate router, firewall, Tailscale, exit-node configuration, SSH policy, or NUC networking in this logical whole.

Do not require a monitor.

Existing working SSH is useful evidence that headless administration remains possible.

---

## 11. Future FrameNest development direction after this whole

Do not start it during the AP consumer convergence whole.

After closure, the successor fresh ORCHESTRATOR should open a bounded evidence phase centered on:

```text
FrameNest Runtime Architecture, Maintainability and Backlog Rebaseline
```

The goal of that future whole is not a generic style audit.

It should commission one strong fresh read-only Worker to investigate current repository reality and determine which engineering work has the highest leverage before UI/UX refinement.

The future analysis should explicitly examine:

### Runtime architecture

```text
module boundaries
large or highly coupled modules
duplicate domain/application logic
temporary abstractions that became permanent
error and lifecycle complexity
subprocess and filesystem boundaries
authorization and privacy boundaries
data-integrity paths
operator/runtime coupling
```

### Code burden

```text
production LOC distribution
largest files/classes/functions
dead or unreachable code with evidence
duplicated behavior
excess abstraction
cross-layer leakage
historical compatibility code that may no longer pay rent
```

### Test burden

Do not assume "many tests" means "bad tests".

Measure and classify:

```text
test LOC versus production LOC
largest test files
duplicate assertions/fixtures
slow or brittle harnesses
implementation-detail tests
security/data-integrity regression tests
contract tests
browser acceptance
tests protecting closed incidents
obsolete or superseded tests
maintenance cost versus evidence value
```

The AP monolithic-suite retirement is evidence that test burden can become harmful, but it is not permission to delete FrameNest tests by analogy.

A test-reduction logical whole requires FrameNest-specific evidence.

### Documentation

Do not repeat the already closed general documentation-authority convergence.

Inspect documentation only where it helps identify:

```text
genuine unfinished architecture
current runtime constraints
accepted deferred work
stale implementation assumptions that would misroute development
operator/security boundaries
```

### Backlog reconciliation

Classify remembered or repository-backed candidates as:

```text
CLOSED
REGRESSION
GENUINELY OPEN
DEFERRED BY OWNER
FROZEN
SUPERSEDED
TOO BROAD / NEEDS DECOMPOSITION
PROCESS/AP-ONLY
```

The future Worker should return only the strongest evidence-backed candidates.

No "100 code smells" report.

No speculative refactor catalogue.

---

## 12. Known future lanes to preserve, not automatically select

Known themes include:

```text
NUC Security Hardening
broader original-media/configuration disaster recovery
portable/rebuildable catalog metadata
per-user metadata/playback state
multi-device synchronization or selective cache, after decomposition
desktop/Tauri or external-player integration
cover-candidate / richer Cover Studio work
safe direct-download UX
additional operator acquisition refinements
```

These are candidates or directions, not current authority.

The future analyzer must compare them with current code evidence.

Do not automatically prioritize them merely because they appear here.

---

## 13. Explicit owner deferrals

Do not select as the immediate next development focus:

```text
VPS
Kiosk
broad screenshot-led UI/UX polish
static X photo acquisition
frozen multi-model metadata comparison
```

UI/UX remains intended later, with a weaker/proportionate implementation model where appropriate.

A concrete functional, privacy, accessibility, or security defect in UI may still be reported as evidence.

Do not turn cosmetic preferences into architecture blockers.

---

## 14. Meta historical trace

Repository:

```text
cisarik/meta
```

Expected local root:

```text
/home/agile/meta
```

Meta is selective historical evidence.

Meta is not AP semantic authority.

Meta is not FrameNest product authority.

Before archival, directly establish current Meta public/local state and inspect its current archive grammar.

The archive convention is date-free and ordered numerically.

Do not reintroduce date directory hierarchy.

Do not guess the current FrameNest sequence number or next logical-whole coordinate from this handoff.

Discover the current:

```text
projects/framenest/...
```

history and choose the next unused ordered coordinate according to the repository's current documented convention.

For each actual logical whole, preserve:

```text
00_handout.md
actual issued Worker prompts
actual Worker terminal reports
```

using the current Meta exchange naming grammar.

The COOPERATOR now intends AI-generated prompts themselves to be archived.

Do not create placeholder Worker reports.

Do not create artifacts for Workers that never existed.

Do not archive raw conversational transcripts as protocol truth.

Archive is historical provenance, not task authority.

Meta mutation requires separate explicit authority.

---

## 15. AP empirical-learning rule

FrameNest is the primary proving ground for current AP.

During real development, record protocol observations only when backed by concrete events such as:

```text
Worker misunderstood authority despite a valid prompt
current AP forced unnecessary repeated ceremony
a required state could not be represented
a fresh Worker could not reconstruct task authority
Meta chronology became ambiguous
planning and execution boundaries caused material friction
acceptance rules produced an unsafe or circular route
prompt synthesis dropped required authority
current AP produced a real model/provider portability failure
```

Do not mutate AP while the FrameNest whole is active.

Do not create an AP improvement merely because a Worker prefers another process.

At FrameNest closure, ORCHESTRATOR may surface proven AP observations for the future ledger.

---

## 16. Fresh ORCHESTRATOR anti-goals

Do not:

```text
start broad code refactoring
start NUC hardening
change networking
configure an exit node
touch VPS infrastructure
start Kiosk
start UI/UX polishing
run a general security audit
run a giant test suite merely for orientation
delete tests because AP deleted its monolithic suite
redo repository documentation convergence
reopen Technical MVP
modify cisarik/ap
deploy FrameNest
perform provider calls
clean unexplained local state
repair .venv
silently use system Python 3.14
use repository history as mutation authority
```

First converge the consumer protocol generation.

---

## 17. Closure of this immediate whole

This logical whole may close when either:

### Route A - already converged

Fresh evidence proves FrameNest already consumes current accepted AP and no repository change is required.

or:

### Route B - consumer update

A minimal exact candidate is:

```text
planned
implemented
independently accepted
published if publication is required
publicly read back
```

and no unresolved compatibility finding remains.

Deployment is required only if evidence shows the change affects runtime production behavior.

Do not invent deployment ceremony for a repository-only protocol-consumer update.

At closure, report:

```text
final FrameNest public SHA
final FrameNest AP pin
current AP public SHA
production SHA
schema
deployment impact
acceptance result
Meta archive state
any concrete new AP evidence
```

Then declare the logical whole closed explicitly.

---

## 18. Successor handoff requirement

After this logical whole is `CLOSED: PASS`, create one restoration handoff for a **new fresh ORCHESTRATOR**.

The successor should own:

```text
FrameNest Runtime Architecture, Maintainability and Backlog Rebaseline
```

unless new evidence from this consumer convergence demonstrates that another prerequisite must come first.

That successor should normally start with:

```text
fresh Worker 1
read-only
deep evidence analysis
Extra High reasoning recommended
no implementation authority
no provider calls
no production mutation
no Meta mutation
```

It must not blindly inherit a product logical whole from old backlog.

Its analysis should select the strongest next bounded engineering whole from current repository evidence.

After that analysis whole closes, rotate ORCHESTRATOR again for the selected implementation logical whole.

This preserves:

```text
one bounded logical whole
one owning ORCHESTRATOR lifecycle
fresh Worker roles
clean authority
clean Meta history
```

---

## 19. Immediate response required from you

After reading this handoff:

1. confirm restoration to Michal in Slovak;
2. state that AP-core backlog is exhausted and must not be reopened without new evidence;
3. state that the immediate prerequisite is current AP consumer convergence in FrameNest;
4. directly verify current AP, FrameNest, and Meta public/local anchors before trusting the expected SHAs;
5. determine whether FrameNest's AP pin is already current;
6. if not current, synthesize exactly one compact authority-complete prompt for fresh Worker 1 planning;
7. perform a prompt-readiness check using current AP;
8. return that Worker 1 prompt to Michal;
9. issue no implementation authority.

Do not ask Michal to repeat project history already recoverable from repositories and this handoff.

The smallest next action is:

```text
fresh ORCHESTRATOR reconstruction
-> current AP consumer-state proof
-> fresh Worker 1 planning only if convergence is actually required
```

Nothing else is currently authorized.