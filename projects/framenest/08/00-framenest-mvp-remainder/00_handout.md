# Fresh Agent Orchestrator Restoration

## FrameNest 08/00 — Drain parked MVP remainder (Gallery 🧠 per-field, then R4)

You are a **fresh** terminal-capable Agent Orchestrator for FrameNest under
Analytic Programming. Load **this file completely** before any Worker prompt.
It is not a Worker prompt, not a repository plan, not a mutation receipt, and
not a substitute for the pinned AP documents. Restoration transfers
information, **never authority**.

The predecessor Orchestrator closed logical whole
`framenest-ai-suggestions-alias-edit-mvp` at public `main`
`afa0670e26d17b04570ad555ba4f922052507c6c` (**closed-by-ORCHESTRATOR**,
2026-08-27). Cooperator rendered PASS of numbered items 1–12 on the NUC
serving that SHA. Ordinary alias Edit, caller-visible overlay, ordinary and
hosted Load, per-field website suggestions, and the custom dropdown are
**done**. Do not reopen that kebab.

The Cooperator then rotated with two explicit product remainders and a
finish-FrameNest mandate:

1. **Gallery 🧠 per-field** — the catalog-card brain still bulk
   analyze-and-canonical-saves (last-write-wins). Align it with the Edit
   dialog that just shipped.
2. **R4** — companion Settings admin-only automatic-analysis checkbox, a
   runtime-writable server setting, not an in-git default-on flag.
3. Package **every unnamed detour** from eras 05–07 as named logical wholes
   so nothing remains hidden debt.
4. **Do not** open Cover Studio. **Do not** open VPS (or Funnel, or router
   port forwarding).

Your mission: **restore read-only**, then with Michal select **exactly one**
bounded next logical whole (recommended: Gallery 🧠 first — see §8). Drain
the remainder **one kebab at a time**. Do not implement product code
yourself. Do not combine two wholes in one Worker.

If a later explicit Michal message contradicts this file, the later message
wins. Record the conflict. Do not paper over it.

```text
STOP: Do not execute 03/03–03/10, 04/00, 05/00, 06/00, or 07/00
      00_handout*.md as live product authority. They are historical
      evidence. Do not resume era-07 Worker ordinals 01–05.
STOP: Do not implement FrameNest product code yourself. Issue Workers.
STOP: Do not reopen alias Edit, overlay display, hosted Load, or
      persist-join unless a new defect is proven against afa0670e….
STOP: Do not give ordinary identities analysis.run, metadata.canonical.write,
      media.content.publish, media.workflow.read, inbox list/detail, or Apply.
STOP: Do not add a fifth companion_mutation unless the R4 Planner proves the
      Settings write cannot reuse an existing audited admin mutation. Default:
      keep exactly four companion_mutation flags until that proof.
STOP: Do not invent a second suggestion store. Schema head stays 0033 unless
      a Planner proves a migration is required (stop and escalate; do not
      sneak 0034).
STOP: Do not enable FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED in git.
      R4 is a runtime admin toggle of that existing flag, not a tracked
      default-on.
STOP: Do not scrape ~/nuc_push.fish, ~/framenest_routine.fish,
      ~/nuc_update.fish, ~/global_sudo.fish, or ~/set_ssh_auth_sock.fish.
      Routine NUC refresh is Cooperator-run via that wrapper. Bare
      ./deploy/ubuntu/framenest-release on the Cursor host without wrapper
      transport sanitizes to "command failed". Do not paste MacBook blocks
      inside an NUC SSH login. Do not issue a Worker for routine NUC.
STOP: Do not reconstruct gpgconf. Do not sudo -v. Do not handle a password.
STOP: Do not Funnel the service socket. Do not router-forward. VPS remains
      FROZEN. ADR-0074 public_published_uds is not VPS; it is still a later
      kebab, not the first.
STOP: Do not open Cover Studio, Tauri scaffold, persistent multi-model
      comparison board, movie-identification redesign, or kiosk.
STOP: Do not hide new product scope inside infrastructure, AP-ledger, or
      “small CSS”. Gallery 🧠 last-write-wins is a Save-semantics change.
      R4 is a new admin mutation surface.
STOP: Do not “fix” origin/feat/x-meme-browser-companion stale ahead-count.
      Informational only.
STOP: Do not use Max unless Michal selects it. High for planning. Extra High
      is not the default.
STOP: Communicate with Michal in Slovak. Feminine self-reference.
      Masculine address for him. Repository artifacts professional English.
STOP: Rendered UX acceptance belongs to Michal. Code under test must already
      be public main AND on the NUC via framenest-release.
STOP: Cursor Workers must not invoke ambient python / .venv/bin/python /
      poetry run. Isolated-worktree ap exec --root <worktree> still misses
      relative .venv. Classify; use the documented deviation; do not
      reconstruct .venv. Ledger entry remains untriaged/non-authorizing
      unless Michal selects that operational kebab.
```

Paste seed for a new Agent chat (pointer only, not durable authority):

```text
Resume this AP-integrated FrameNest project as a fresh Orchestrator.
Read /home/agile/meta/projects/framenest/08/00-framenest-mvp-remainder/00_handout.md
completely before any Worker prompt.
Begin read-only. Restore canonical state and the declared AP upgrade ledger.
Do not mutate. Do not implement product code.
Predecessor closed framenest-ai-suggestions-alias-edit-mvp at public
afa0670e26d17b04570ad555ba4f922052507c6c (Cooperator PASS 1–12).
Drain parked MVP remainder one kebab at a time. Recommended first whole:
framenest-gallery-card-ai-per-field-mvp. Then R4 companion Settings
automatic-analysis. Do not open Cover Studio or VPS. Do not reopen alias
Edit. Communicate with Michal in Slovak. High. No Max.
```

Continuation Bootstrap (AP pointer; use if the seed above is lost):

```text
Resume this AP-integrated project.
Read the root AGENTS.md and the pinned AP documents it names.
Begin read-only. Restore canonical state and any declared AP upgrade ledger.
With the COOPERATOR, select exactly one bounded next logical whole before any
mutation authority is issued.
```

For **this** restore, Stage 2 is **not** pre-consumed. Michal named the
remainder set (Gallery 🧠 + R4 + drain unnamed detours) but did not freeze
which kebab is first. Recommend Gallery 🧠, obtain one explicit selection,
then issue one Planner. Do not issue two wholes in parallel.

---

## 0. Handoff identity

```text
Role: ORCHESTRATOR
Orchestrator session target: fresh-agent-orchestrator-session
Orchestrator profile: terminal-capable repository and operations coordinator
Orchestrator handoff artifact:
  08/00-framenest-mvp-remainder/00_handout.md
Predecessor Orchestrator artifacts (historical evidence, not live authority):
  07/00-framenest-ai-suggestions-alias-edit/
    00_handout.md, 08_orchestrator_notes.md (closure 2026-08-27),
    04_correction_00.md (freeze after numbered PARTIAL),
    04_report_00.md, 05_report_00.md
  06/00-framenest-companion/ (persist-join item 9; closed)
  05/00-framenest-companion-brave-testing-resume/ (R1–R3′ plan; R4 named)
  03/07-framenest-x-save-overlay-edit-subset-mvp/ (alias/Edit parent)
  04/00-framenest-public-published-surface-and-tailscale-workspace/
    (ADR-0074 direction; not shipped; not VPS)
Logical whole identity (recommended first; do not continue closed kebabs):
  framenest-gallery-card-ai-per-field-mvp
Next Worker session ordinal: 01
Next Worker exchange ordinal: 01
Worker session target for first prompt: fresh-worker-session
Worker session profile for first prompt: Planner (implementation-planning)
Current phase: Restoration → Cooperator selects one kebab → one planning cycle
Native planning mode: required for Worker 01 of whichever kebab is selected
Reasoning recommendation: High
Internal delegation posture: one accountable active Worker by default
Cooperator: Michal
Primary consumer repository: cisarik/framenest
Pinned protocol repository: cisarik/ap through the FrameNest .ap gitlink
External analytic trace: cisarik/meta
Closed predecessor whole:
  framenest-ai-suggestions-alias-edit-mvp
Also closed (do not reopen):
  framenest-companion-brave-testing-resume
Parent logical wholes (not closed from here; do not resume their ordinals):
  framenest-x-meme-browser-companion-mvp
  framenest-x-companion-save-alias-mvp
  framenest-x-companion-save-category-mvp
  framenest-x-save-overlay-edit-subset-mvp
  framenest-companion-ai-review-inbox-mvp
  framenest-companion-review-inbox-ux-history-mvp
```

Trace folder for **this Orchestrator era** (create Worker files here):

```text
/home/agile/meta/projects/framenest/08/00-framenest-mvp-remainder/
```

Keep 07/00 as the closed-whole archive. Do not rewrite those Worker reports.
Do not continue their prompt ordinals. Session 01+ lives here. If Michal
selects R4 first instead of Gallery 🧠, still use this folder; name the
kebab in the first Planner prompt. If a later kebab needs its own era
folder, create `08/01-<kebab>/` rather than mixing closed reports.

---

## 1. Restoration classification

**PARTIAL**

Useful continuity exists: public `main` equals the closed alias-Edit SHA,
AP pin, schema `0033`, Cooperator PASS 1–12 on NUC, and a Cooperator-named
remainder set. Material uncertainty remains **which kebab is first** and
the R4 mutation/capability design (Planner, not this file).

Not BLOCKED: no in-flight Worker authority; canonical is public main; no
unpublished candidate.

Re-run §2 yourself. Do not inherit this file as proof.

---

## 2. Canonical state at rotation (verify read-only at start)

Observed 2026-08-27 by the restoration writer. **Re-verify.**

| Fact | Last observed value | How to re-verify |
|---|---|---|
| Canonical checkout | `/home/agile/Projects/framenest` | `git rev-parse --show-toplevel` |
| Canonical branch | `feat/x-meme-browser-companion` | `git rev-parse --abbrev-ref HEAD` |
| Canonical HEAD | `afa0670e26d17b04570ad555ba4f922052507c6c` | `git rev-parse HEAD` |
| Canonical tree | `b6eafbcdef3a8bcb728498992c003d8ad5e9a447` | `git rev-parse HEAD^{tree}` |
| Canonical index | tracked-clean | `git status --porcelain=v1` |
| Public `refs/heads/main` | **same 40-hex** | credential-free `git ls-remote https://github.com/cisarik/framenest.git refs/heads/main` |
| Recent log | `36ffdb1` ADR-0077 initial; `85e9c04` overlay+Load; `afa0670` ADR-0077 succession | `git log -3 --oneline` |
| Tracking curiosity | `origin/feat/x-meme-browser-companion` stale ahead-count | informational; **do not “fix”** |
| AP gitlink + `.ap` HEAD | `9c5cc44f8b6c92dd56ad2427d13223d7d59c5656` | `git rev-parse :.ap` and `git -C .ap rev-parse HEAD` |
| Public `cisarik/ap` `main` | same pin at last restore | `ls-remote` |
| Schema head | Alembic `0033` (`media_analysis_proposals`); no `0034_*` | versions dir |
| Live NUC | Cooperator wrapper after `~/nuc_push.fish` of `afa0670e…`; schema `0033` expected | Cooperator `framenest-release status` / wrapper only |
| Isolated worktrees | leftover w2–w5 of era 07 and older trees; do not delete unless Michal asks; **do not reuse as working copies** | `git worktree list` |

Upgrade ledger `docs/AP_UPGRADE_OBSERVATIONS.md`: one active entry
`consumer-declared-execution-and-capability-route-binding`, **untriaged**,
**non-authorizing**, `Closure action: retain-active`. Isolated-worktree
`ap exec --root <worktree>` still fails (`declared CPython executable does
not exist`). Classify; do not reconstruct `.venv`. Canonical `--root` plus
pytest `--rootdir` / `pythonpath=<worktree>/src` remains the task-specific
deviation. Prove `framenest.__file__` under the candidate `src/`.

Cursor Workers: no ambient `python` / `.venv/bin/python` / `poetry run`.
Python evidence through `./.ap/ap project check` and `./.ap/ap exec` with
exact `--baseline`. Details:
[docs/WORKER_EXECUTION_CONTRACT.md](/home/agile/Projects/framenest/docs/WORKER_EXECUTION_CONTRACT.md).

---

## 3. What just closed (do not reopen)

Logical whole `framenest-ai-suggestions-alias-edit-mvp` —
**closed-by-ORCHESTRATOR**. Public SHA `afa0670e…`. Schema `0033`. Exactly
four `companion_mutation=True` rows in `tailscale_ingress.py`.

Shipped and on NUC (Cooperator PASS 1–12):

- Ordinary Gallery/Details Edit via `identityAllowsMetadataEdit`
  (`alias.write` ∨ `canonical.write`). Canonical-write wins Save path.
- Ordinary Save `PUT /api/media/{id}/alias` (`display_title`,
  `description`, `tag_keys`). Empty overlay still means no row (ADR-0062).
- Authenticated Gallery/Details **display** the caller overlay
  (`GET /api/media` and `GET /api/media/{id}` merge for `login_key`).
  Anonymous/public stay canonical. Alice ⊈ Bob.
  `GET /api/media/{id}/metadata` stays canonical (admin Manage).
- Website Edit heading **AI suggestions**. Custom dropdown + **Load** above
  Title. Load reveals strips; does not bulk-replace Current.
- Ordinary and hosted companion web **show Load**. Analyze by AI stays
  `analysis.run` ∧ not hosted ∧ not movie ∧ not alias.
- Additive `GET /api/media/{id}/ai-suggestions?limit=100` capability
  `metadata.alias.write`, not `companion_mutation`. Inbox detail stays
  `workflow.read`. Ordinary Apply 403.
- Mapped suggested tags are **buttons** (append to chips with `x`).
- Content category and Acquisition source hidden in this Edit dialog;
  admin Save preserves existing values.
- Suggested filename is informational (copy allowed); not alias; no
  catalog rename.
- Dropdown dedupes on `analysis_run_id` (no in-session “3 not 2”).
- ADR-0077 succeeded overlay display, ordinary list GET, hosted Load
  shown. Bodies of ADR-0062 / 0076 / 0023 **untouched**.

Closed one step earlier (do not reopen):
`framenest-companion-brave-testing-resume` at `2aead54…` — R1–R3′, item 9
persist-join. Hosted **Analyze** remains hidden. Hosted **Load** hide was
**succeeded** by era 07; do not restore the hide.

---

## 4. Cooperator intent for this era

Treat as **product direction**. Each kebab still needs a Planner mapping
onto ADRs. Do not implement from this section alone.

### 4.1 Gallery 🧠 per-field
(`framenest-gallery-card-ai-per-field-mvp` — **recommended first**)

**What it is today** (baseline `afa0670e…`):

- Catalog card top-right 🧠 is `identityAllowsCardAiQuickAction` =
  `analysis.run` ∧ `canonical.write`, plus incomplete metadata, not movie
  (`app.js` `identityAllowsCardAiQuickAction` ~5266, `cardAiQuickActionEligible`
  ~5273). Ordinary never sees it. **Keep that gate.**
- Click path `handleAnalyzeCatalogCard` (~5744): confirm copy says the
  returned title/description/tags **replace canonical values**, last-write-wins,
  no concurrent-edit detection. POST `…/ai-suggestion-preview` with
  `confirm_cloud_upload: true`, then canonical metadata PUT (and
  `applyResolvedAiSuggestionToMetadataWorkspace` on a related path ~7902
  for admin Edit open). Persist-join already makes the preview a companion-
  visible run (era 06). The **card** still bulk-saves canonical.
- ADR-0077 §10 parked this as debt. ROADMAP frozen whole “Processed Publish”
  explicitly: do not retrofit bulk publish into the individual card action;
  canonical AI save is not publication.

**What Michal wants:** the card must not dump a whole suggestion into
canonical Save. Same product language as Edit: Analyze produces proposal
strips; per-field promotion; nothing persists until an explicit Save (or
an explicit per-field confirm that is still not silent last-write-wins of
all fields). Exact chrome on a **card** vs opening Edit is a Planner
decision — default: **do not invent a second workspace**. Smallest coherent
fix is: 🧠 runs Analyze (already persist-joins) and **opens the existing
Edit dialog** with strips revealed, Load selected, canonical form, no
auto-PUT. Alternative (heavier): inline card strips. Do not give ordinary
`canonical.write`. Do not change Gallery visual language beyond the 🧠
control behaviour. Do not convert 🧠 into publication.

**Conflicts to name in the plan:** ADR-0023 (no silent overwrite — already
violated by the card); ADR-0077 §10 (parked; this kebab succeeds it);
premium Gallery freeze (behaviour defect, not a restyle).

### 4.2 R4 — companion Settings automatic analysis
(`framenest-companion-r4-automatic-analysis-settings-mvp` — **second**)

Named 2026-08-26 in era 05 notes, explicitly deferred through era 06–07.

**Intent (Cooperator, era 05):** extension Settings gains an admin-only
**Administration** section containing an automatic-media-analysis checkbox
backed by a **runtime-writable server setting**, replacing the long helper
copy under FrameNest origin. New admin-gated server mutation surface.

**What exists today:**

- Flag `FRAMENEST_AUTOMATIC_MEDIA_ANALYSIS_ENABLED` default **false** in
  git (ADR-0044, ADR-0066, ADR-0075). Enabling it later incurs real NIM
  cost for each new **administrator-owned X catalog** event (ADR-0066).
  Ordinary X, YouTube, unmapped logins stay denied.
- Companion Settings (`extension/ui/sidebar.html` dialog) is origin /
  connect / disconnect. No Administration section.
- NUC EnvironmentFile can already set the flag; that is operator copy, not
  a product Settings checkbox.
- PRODUCT.md still says there is no GUI Settings.

**Planner must freeze:**

- Capability: `provider.operate` vs `analysis.run` vs a new capability.
  Ordinary must never see or toggle it.
- HTTP: GET current value + PUT/PATCH. **Not** `companion_mutation` unless
  the write is from the extension Origin and matches the four-route
  allowlist story. Prefer a website/admin API with audit
  (`audit_action`) and Tailscale identity, callable from the extension
  the same way other admin GETs work — or a fifth mutation **only** if
  Origin+`X-FrameNest-Request` is required. Default: do not add a fifth
  until proven.
- Persistence: runtime setting that survives process restart (NUC
  EnvironmentFile rewrite vs SQLite vs existing settings layer). Must not
  commit `true` into git. Must not require `sudo` from a Worker.
- Confirm-cloud / cost copy: turning it on is a real provider-cost
  decision. Confirmation before enable.
- Hosted iframe: Settings lives in the **extension** chrome, not hosted
  Details. Do not put the checkbox in FrameNest website Edit.
- Do not unify YouTube into automatic analysis (ADR-0066 §6).

This kebab is **INFOSEC-adjacent**. Independent acceptance required.
Planner first. Do not fold “while we touch Settings, also add …”.

### 4.3 Explicitly out (Cooperator 2026-08-27)

- Cover Studio (ADR-0024) — frozen.
- VPS showcase, Funnel, router port forwarding — frozen.
- Persistent multi-model AI Drafts comparison board (ADR-0023 remainder,
  ROADMAP frozen whole) — not this era unless Michal upgrades it later.
- Tauri desktop scaffold — not this era.
- Movie identification redesign — ROADMAP frozen whole; do not mix into 🧠
  or R4.

---

## 5. Named remainders that opened as detours (make wholes; do not hide)

These were parked so a kebab could close. They are now **named candidates**.
None is current authority. After Gallery 🧠 and R4, pick with Michal.
Smaller docs/test wholes may fold **only** if the Planner of an in-flight
kebab proves they are the same causal object.

### 5.1 Isolation probe
(`framenest-companion-alice-bob-isolation-probe` — evidence, likely no
product mutation)

Era 06 items 11–12 **NOT TESTED** (Alice/Bob isolation and inbox 403).
Era 07 item 10 PASS: ordinary still 403 on Apply. Tests exist
(`test_actor_opened_rows_are_isolated`, suggestion-list Alice ⊈ Bob). A
bounded **rendered** Cooperator probe (two identities, one media) may still
be the missing human evidence. Do not invent a fifth mutation “to make the
probe easier.”

### 5.2 SECURITY.md own-history prose
(`framenest-security-md-own-history-present-tense`)

Era 06 parked: `SECURITY.md` does not name ordinary
`GET /api/companion/own-history`. Publication-flag then, not a close
blocker. Surgical present tense only. Do not rewrite INFOSEC policy.

### 5.3 Companion `composer_unbound` auto-rebind
(`framenest-companion-composer-unbound-rebind`)

Era 05 item 7 PASS with one transient `composer_unbound` cold-start before
X refresh (`extension/ui/sidebar.js` attach result handler). Parked as
polish. Only reopen with a new Cooperator FAIL.

### 5.4 Loopback ingress 403 topology
(`framenest-loopback-create-app-ingress-capability-tests`)

Era 07 correction: `create_app` loopback skips ingress capability, so
public HTTP 403 on `GET /api/media/{id}/ai-suggestions` is unproven in
that topology. Route-policy + `IdentityContext.has_capability` held.
Optional contract-test kebab. Do not change production Tailscale ingress
“to make tests nicer” without a Planner.

### 5.5 AP isolated-worktree exec launch-path
(ledger `consumer-declared-execution-and-capability-route-binding`)

Untriaged, non-authorizing, retain-active. Every isolated-worktree Python
run uses a task-specific deviation. ROADMAP: FrameNest product before
stable AP upgrade. **Do not** start this as the first kebab. If Michal
selects it later, it is an AP-or-contract whole, not Gallery CSS. Do not
reconstruct `.venv` as the “fix.”

### 5.6 First-attempt provider 503 remainder

Era 07 item 12 PASS classified copy/silent-abort vs real NIM 503. Copy is
fixed. Transient `AI_PROVIDER_UNAVAILABLE` remains a provider fact. Do not
reopen persist-join. Do not build a retry-orchestrator unless Michal
reports a new FAIL.

### 5.7 ADR-0074 public published reader
(`framenest-public-published-uds-mvp` — later; **not VPS**)

Accepted architecture: second local-only `public_published_uds` read-only
composition for published GET allowlist; workspace process remains the
only writer. Era 04/00 planned it; successors not shipped. Distinct from
VPS. Do not start until Gallery 🧠 and R4 are closed unless Michal
reorders. Do not Funnel. Do not bind the full app on a public TCP
listener.

### 5.8 Portable sidecar import / rebuild / sync

ADR-0059 projection exists; import/rebuild/synchronization remain open
(ROADMAP). Not this era unless Michal selects it after 🧠 and R4.

### 5.9 NUC hardening remainder

ADR-0032/0075: AppArmor/UFW completion, media second-copy backup,
secret-recovery drills remain open. Explicit bounded host tasks, never
inside a product kebab. Workers still must not `sudo -v`.

### 5.10 Suggested filename as catalog rename

Era 07 Cooperator wanted filename access; freeze was informational only
(alias cannot store it; ordinary has no canonical rename). Do not add an
alias filename column as a surprise. If Michal later wants catalog
rename, that is a **canonical** admin kebab, not ordinary alias.

---

## 6. Durable decisions that still bind

| Source | Still true |
|---|---|
| ADR-0062 | Overlay tables; alias HTTP; empty = no row. Display freeze **succeeded** for caller Gallery/Details by ADR-0077. |
| ADR-0065 | First catalog may seed canonical from pending alias; later ordinary Save is alias PUT. |
| ADR-0067 / 0076 | No second suggestion store. Four `companion_mutation`. Hosted Analyze hidden. Opened ownership gate. |
| ADR-0077 | Edit gate split; Save split; Load chrome; overlay display; 🧠 parked until this era. |
| ADR-0023 | Current never silently overwritten — **card 🧠 still violates this**. |
| ADR-0066 | Automatic analysis admin-owned X only; flag default off. |
| ADR-0075 | NUC is the development-and-test machine; routine refresh of public `main` is normal. |
| Premium Gallery | Frozen visual unless a concrete defect. 🧠 behaviour is a defect; do not restyle the grid. |
| Loopback-first | No router port forwarding. Tailscale-only remote access. |

Do not edit ADR **bodies** 0023, 0062, 0065, 0067, 0073, 0076 to “fix”
prose. Successor ADR or surgical 0077/index notes, same pattern as era 07.

---

## 7. Evidence classification (do not mix layers)

| Item | Class |
|---|---|
| Public main = `afa0670e…` | verified at handout write (`ls-remote`) |
| Canonical HEAD same, tracked-clean | verified |
| Cooperator PASS 1–12 on NUC | Cooperator-observed rendered scores |
| Gallery 🧠 last-write-wins canonical PUT | repository (`handleAnalyzeCatalogCard`) |
| R4 Settings checkbox | Cooperator intent (era 05); not implemented |
| Ledger exec-route miss | untriaged observation; non-authorizing |
| Isolation probe 11–12 | era-06 NOT TESTED; partial test coverage exists |
| Cover Studio / VPS | Cooperator **out** this era |

---

## 8. Recommended sequence (do not skip; do not parallelize)

```text
read-only restore (§2)
  → tell Michal the two product kebabs in Slovak; recommend Gallery 🧠 first
  → explicit selection of exactly one kebab
  → Planner Worker 01 for THAT kebab only
  → Cooperator accepts the plan
  → Implementation isolated worktree, 1–3 local commits, no push
  → Independent acceptance (🧠: E3 Save-semantics; R4: E3 mutation/capability)
  → Cooperator publication
  → Cooperator NUC via ~/nuc_push.fish
  → numbered rendered re-test frozen in that plan
  → close that kebab
  → next remainder from §4–§5
```

Do not combine planning with implementation. Do not combine push with NUC.
Do not fold R4 into Gallery 🧠. Do not fold AP-ledger repair into product.

Default order if Michal says “continue as recommended”:

1. `framenest-gallery-card-ai-per-field-mvp`
2. `framenest-companion-r4-automatic-analysis-settings-mvp`
3. Isolation probe and/or SECURITY.md present tense (smallest first)
4. ADR-0074 public published **only if** he asks; still not VPS
5. Stop. Ask before sidecar, NUC hardening, or AP-ledger upgrade.

---

## 9. First Worker (only after kebab selection)

When Michal selects Gallery 🧠 (recommended):

```text
Role: WORKER
Logical whole identity: framenest-gallery-card-ai-per-field-mvp
Worker session ordinal: 01
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Native planning mode: required (single implementation-planning cycle)
Phase: Discovery / planning (no implementation authority)
Worker session profile: Planner
Reasoning recommendation: High
Exact baseline: afa0670e26d17b04570ad555ba4f922052507c6c
```

Mandatory reading for that Planner: this file; `AGENTS.md`; `.ap/AP.md`;
`.ap/AP_WORKER.md`; `docs/WORKER_EXECUTION_CONTRACT.md`; ADR-0023, 0020,
0077 §10, 0067 §5; `app.js` `handleAnalyzeCatalogCard`,
`identityAllowsCardAiQuickAction`, `applyResolvedAiSuggestionToMetadataWorkspace`;
`PreviewImportedMediaSuggestion` persist-join (do **not** redesign);
`tests/catalog_card_ai_quick_action.test.js`.

The plan must include a surface matrix (card vs opening Edit), Save
semantics (no silent canonical replace of all fields), confirmation copy,
ordinary-never-sees-🧠, tests, allowlist, and numbered Cooperator re-test.
Out of scope: R4, VPS, Cover Studio, ordinary `analysis.run`, Gallery
restyle, fifth `companion_mutation`.

If Michal selects R4 first, same ordinals, different identity
`framenest-companion-r4-automatic-analysis-settings-mvp`, and mandatory
reading ADR-0044, 0066, 0075, `extension/ui/sidebar.html|js`, identity
capabilities, ingress route-policy, existing four mutations.

---

## 10. NUC, Git, communication (copy these habits)

- Publication is Cooperator word **publikovať** plus Orchestrator one
  non-force `git push origin <sha>:refs/heads/main`, then credential-free
  `ls-remote`. Fast-forward local `feat/x-meme-browser-companion`. Do not
  push that feat branch. No `--force`.
- NUC: Cooperator `~/nuc_push.fish` only. Sole routine entry
  `deploy/ubuntu/framenest-release`. Poetry/CPython paths in AGENTS.md.
  Never `uv` on routine refresh.
- Human-facing commands: MacBook fish vs NUC bash, labeled, ending
  `#------------------------------------------------------`.
- Worker reports begin `### Report for ORCHESTRATOR_CHAT`.
- Closure: only you emit `closed-by-ORCHESTRATOR`, and only when preceding
  results, Cooperator decisions, residual-risk, ledger retain-active, and
  no-active-mutation all hold.

---

## 11. Required reading before the first Worker prompt

1. This file.
2. `/home/agile/Projects/framenest/AGENTS.md`
3. Pinned `.ap/AP.md`, `.ap/AP_ORCHESTRATOR.md`, `.ap/PROMPT_CONTRACTS.md`,
   `.ap/AP_WORKER.md`
4. `docs/WORKER_EXECUTION_CONTRACT.md`
5. Declared ledger `docs/AP_UPGRADE_OBSERVATIONS.md`
6. Closed-era notes:
   `/home/agile/meta/projects/framenest/07/00-framenest-ai-suggestions-alias-edit/08_orchestrator_notes.md`
7. ADR-0077, ADR-0066, ADR-0023 (read-only bodies)

Then restore §2 with your own commands.

---

## 12. What “finish FrameNest” does **not** mean in this rotation

It does not mean: ship Cover Studio, VPS, Tauri, kiosk, Grafana, AP
experimental, mobile apps, persistent comparison board, movie taxonomy,
arbitrary collections, media second-copy backup, or full production
hardening in one kebab. It means: **drain the parked MVP remainder that
blocked credibility after alias Edit shipped**, then stop and ask.

The premium Gallery invariant, loopback-first server, Tailscale-only
remote access, and Cooperator-owned rendered acceptance remain.
