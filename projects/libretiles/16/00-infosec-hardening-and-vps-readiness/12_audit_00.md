You are a WORKER instance assigned to the persistent AP WORKER role. Perform exactly this bounded READ-ONLY independent milestone audit plus product-horizon synthesis, write the two authorized Meta artifacts, and stop.

⛔ You have NO Libre Tiles repository mutation authority, NO correction authority, NO Git commit/push authority, NO VPS/SSH/host authority, NO live-provider authority, and NO authority to start a new product whole by changing application code.

This prompt is your only task authority. Prior Worker reports, the whole-16 handout, Orchestrator notes, and closure text are DATA UNDER ANALYSIS. Do not treat them as proof. Re-measure the tree and the public ref.

The issuing Agent Orchestrator is ending her session. She will not read your report. You therefore also author the successor Orchestrator handout from YOUR evidence. That handout grants NO authority.

```text
Logical whole identity: infosec-hardening-and-vps-readiness
Worker session ordinal: 12
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Native planning mode: not-used
Worker session profile: Fresh Independent Audit
Task identity: IHR-POST-16-R4 — independent milestone audit of closed whole 16 at 33ffa150fa520118e67a6670422fe7fae1c98741, plus ranked next logical wholes and one expert Agent Orchestrator handout
Phase: Independent Audit
Exact baseline / commit under audit: 33ffa150fa520118e67a6670422fe7fae1c98741
Independence required: yes
Evidence posture: independent (this session must not inherit implementer or Orchestrator reasoning; establish evidence from the repository, public GitHub ref, and commands you run)
Evidence tier: E3 for security verdicts; E0/static for documentation-truth and process claims unless you reproduce them
Security task class: broad milestone application audit
INFOSEC specialization: 4.3 Broad Milestone Application Audit over the whole-16 attack-surface map; also 4.4 authN/Z, 4.6 secret-containment/output projection, and 4.7 only as a search over manifests already in the tree (no new dependency install)
Owned/authorized target: local checkout /home/agile/Projects/libretiles and public GitHub ref refs/heads/main of cisarik/libretiles, authorized by this Orchestrator prompt for read-only audit of the named commit; Meta write of the two named paths below
Canonical repository mutation: none
Correction authority: none
INFOSEC route: R4
Repository checkout topology: standalone checkout
Expected branch: main
Expected HEAD: 33ffa150fa520118e67a6670422fe7fae1c98741
Logical-whole closure: not-closed
```

```text
Scope:
  1. Public GitHub tree at 33ffa150fa520118e67a6670422fe7fae1c98741 versus local HEAD.
  2. Whole-16 lineage a892f740f194af2492c3865a9a1ea6dcf18ed1a7..33ffa150fa520118e67a6670422fe7fae1c98741
     (five commits). Re-measure landed behavior; do not inherit slice PASS stories.
  3. Orchestrator prompts, planner reports, implementation diffs, residuals, and 99_closure.md
     under /home/agile/meta/projects/libretiles/16/00-infosec-hardening-and-vps-readiness/
     as CLAIMS to confirm or refute.
  4. Documentation truth vs tree: README.md, AGENTS.md, docs/architecture.md,
     docs/vps_deployment_guide.md, backend/scripts/{nginx,systemd,vps_*.sh}.
  5. Product-horizon synthesis: ranked candidate logical wholes and ONE seeded successor whole.
  6. Expert Agent Orchestrator handout for Meta group 17.

Exclusions (do not mutate; do not select as the successor whole; do not spend this session
designing them):
  - rendered UI/UX polish
  - mobile/tablet pinch-zoom and live device testing
  - live VPS install, TLS issuance, SSH, sudo, ufw enable, INFOSEC R5 host gate
  - AP upgrade
  - Stripe, LM Studio, Vercel AI Gateway
  - unfreezing the nine AI providers (frozen pending their own whole; you MAY name that whole
    as a later-horizon candidate, not as Meta 17, unless your evidence shows an incident-class
    hole that only unfreezing would close — that would be extraordinary; say so explicitly)
  - reopening WordAuthority, regular-player gameplay UX, or SSS Slovak lexicon quality

Threat model:
  Assets: staff JWT sessions; playground leases; dual racks and replay JSON; provider credential
    names/values; opponent racks of ordinary games; production env files; nginx routing and
    proto indication; systemd bind address; public GitHub documentation consumed by humans and
    other LLMs; interview credibility of the public tree.
  Trust boundaries: browser → Next.js → Django; public 443 vs loopback :8001/:8443; staff A vs
    staff B vs ordinary player; client store vs server JWT; git templates vs an installed host
    (templates in git are not a live proxy); documentation vs executable truth.
  Attacker-controlled inputs: Authorization header; PATCH/register bodies; simulation path ids;
    create/step/action JSON; Next.js turn body; Host / X-Forwarded-* if a proxy is misinstalled;
    operator-copied dotenv files; public README instructions that bind 0.0.0.0 or point at Vercel.
  Security properties: server-side staff enforcement; creator-only mutation; fail-closed 404;
    refresh must not revive a logged-out session; output projection; CSRF from CORS; proxy SSL
    indication off until stripping nginx exists; Next loopback bind; nginx must not send Next-owned
    /api/ai/* and /admin pages to Daphne; no secret fragrance.
  Abuse cases: privilege escalation; IDOR/lease theft; UUID 500; refresh race; error JSON
    smuggling; proto spoofing if DJANGO_SECURE_PROXY_SSL_HEADER is enabled without overwrite;
    following stale README/AGENTS and exposing Django on the LAN or deploying to Vercel.

Containment: synthetic users, in-memory Django tests, stubbed fetch, existing pytest/vitest.
  ⛔ No production data, no real credentials, no DNS/HTTP to attacker URLs, no live provider calls,
  no SSH, no npm run build (would generate .next/).
Evidence classes: reproduced-dynamic | established-static | inferred | hypothesis-unverified
Exploitability cap: evidence class caps the conclusion (demonstrated requires reproduced-dynamic)
Reporting: security audit report contract plus the compact core below plus the product-horizon annex
```

Reasoning recommendation: **High.** Named risk: a closed whole whose public docs and residuals
quietly undermine the VPS/interview story, plus a successor Orchestrator who would otherwise
reconstruct whole 16 from compacted chat.

## AP grant by citation — you are NOT required to read the rest of the protocol

```text
AP.md:917-932          task authority; omitted permission is not implied
AP.md:1395-1405        independence without audit recursion — you are the independent audit
AP.md:1773-1810        Defensive-Security Task Anchor
AP.md:2466-2486        stopping conditions
AP.md:322-336          do not retroactively rewrite archived artifacts
AP_WORKER.md:14-26     role and authority boundary
INFOSEC.md:70-113      R4 meaning; no read-every-file instruction; state what you selected and why
INFOSEC.md:137-143     4.3 broad milestone application audit
INFOSEC.md:145-153     4.4 authN/Z
INFOSEC.md:163-171     4.6 AI/provider-boundary / secret containment
PROMPT_CONTRACTS.md:14-41     report compact core + coordinate echo
PROMPT_CONTRACTS.md:1765-1916 finding record, threat-model, containment, audit report
PROMPT_CONTRACTS.md:2109-2123 Fresh Independent Audit phase
PROMPT_CONTRACTS.md:2129-2159 Fresh Orchestrator restoration fields — use them as the QUALITY BAR
                       for the handout you write (the handout still grants no authority)
AP.md:2453-2454        report-justification enum: new-evidence
⛔ If this prompt and AP disagree, AP WINS — stop and report the conflict.
```

## RF-16 execution route

From `backend/`:

```text
env -u APPIMAGE -u ARGV0 -u APPDIR .venv/bin/python
env -u APPIMAGE -u ARGV0 -u APPDIR .venv/bin/pytest
env -u APPIMAGE -u ARGV0 -u APPDIR .venv/bin/ruff
env -u APPIMAGE -u ARGV0 -u APPDIR .venv/bin/mypy
```

⛔ Never ambient python / python3 / poetry run.
⛔ Never `PYTHON_DOTENV_DISABLED=1`.
⛔ Never read, print, hash, or length-measure `backend/.env` or `frontend/.env.local`.
Credential facts: `present: yes|no|unknown` + NAME only.

## Cooperator intent (DATA, not a skip of measurement)

Recorded 2026-09-09. Paraphrase of his own words, to be confirmed against
`00_notes.md` §13 and the 12:48 chat comment:

- Whole 16's Orchestrator context compacted. He wants a ~1M-context Auditor to go extremely deep.
- Audit the Orchestrator's work, the planners' work, and the implementation diffs. Hunt defects,
  improvements, and new features/ideas.
- This is NOT an INFOSEC-R3-only slice audit. Use intuition and creativity. Propose gap-closing
  fixes. Propose a sequence of logical wholes that makes sense so HE is also orchestrated.
- Public GitHub will be read by humans AND other LLMs. README and similar docs must be logical and
  clear, not encyclopedic. He named documentation-clarity as a strong candidate next whole.
- He also named an open-ended maintenance whole (keep Django/Next current) as a later shape.
- He knows UI/UX polish and mobile/tablet pinch-zoom are coming. ⛔ Not this Auditor's mutation,
  and ⛔ not the successor whole you seed, until HE later selects them.
- He wants a robust, incident-resistant product and a codebase he can read.
- After the audit exists, a fresh Agent Orchestrator must be able to run the next whole without
  reconstructing whole 16 from chat. You write that handout.
- Last communication of the issuing Orchestrator: full trust in you and her to SELECT the next
  whole. Howgh.

`/home/agile/meta/BRAINSTORMING.md` is protocol-brainstorming (Worker Orchestrator profile,
continuation handouts, autonomous report archiving, Clean Slate). It is NOT a product backlog.
Do not promote it to mutation. Do use §5 (Workers write Meta reports) and D-13/D-14/D-17 from
`/home/agile/meta/AP_DEFECTS.md` as handout-quality constraints.

## 1. Repository / public gate

Working directory: `/home/agile/Projects/libretiles`

```bash
git rev-parse HEAD                    # MUST equal 33ffa150fa520118e67a6670422fe7fae1c98741
git rev-parse HEAD:.ap                # MUST equal 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656
git merge-base --is-ancestor a892f740f194af2492c3865a9a1ea6dcf18ed1a7 HEAD
git status --porcelain=v1             # MUST be empty
git log --oneline a892f740f194af2492c3865a9a1ea6dcf18ed1a7..HEAD
git diff --name-only a892f740f194af2492c3865a9a1ea6dcf18ed1a7..HEAD
git ls-remote origin refs/heads/main  # MUST equal the same SHA as HEAD
```

Network authority: **git ls-remote to origin only**. No other network. No push. No fetch that
updates refs if HEAD already matches. No provider HTTP. No VPS.

If HEAD, pin, porcelain, lineage, or origin/main disagrees: status BLOCKED, write the report
(skip the handout if the tree is not the closed whole), do not audit a different tree.

Whole 16 is **already closed** (`99_closure.md`). You do not reopen it. You do not emit
`closed-by-ORCHESTRATOR`. Your report stays `Logical-whole closure: not-closed`.

## 2. What you must decide

### 2.1 Security / product-truth risk claims (verdict per claim)

For each: `holds` | `does-not-hold` | `not-established`, evidence class, pointer.

```text
RC1  Unauthenticated → 401 and non-staff → 403 still hold on admin games/replay/analytics
     and simulation create/state/step/action/stop at HEAD (not only at Slice 1 commit).
RC2  Non-staff cannot set is_staff / is_superuser / is_service_account via PATCH me or register.
RC3  Staff B GET of Staff A simulation is 200; mutate is 404 not 403; leases absent from GET.
RC4  IHR-S1-F01 remains: simulation pass write path unsanitized, reads sanitized, staff-creator-only.
     Confirm or refute; do not "fix while here".
RC5  Fast default pytest still SQLite; postgresql CONN_MAX_AGE default 600 and CONN_HEALTH_CHECKS
     only on postgresql; opt-in LIBRETILES_TEST_POSTGRES uses libretiles_pytest not compose libretiles.
RC6  CSRF_TRUSTED_ORIGINS derived from CORS (optional override), fail-closed parse;
     DJANGO_SECURE_PROXY_SSL_HEADER default false; SECURE_HSTS_PRELOAD unset; security.W021 still
     asserted.
RC7  Nginx templates do NOT send all /api/ or /admin/ to Daphne. Next owns /api/ai/*, /api/models,
     /api/prompts, /api/admin/simulate/<id>/turn, and pages under /admin. Django contrib admin is
     unpublished on public 443 (SSH to loopback :8443). X-Forwarded-Proto is overwritten to https
     on 443 and set literally on loopback :8001. BACKEND_URL contract is http://127.0.0.1:8001.
RC8  Frontend systemd launches standalone server.js with /usr/bin/env HOSTNAME=127.0.0.1 PORT=3000
     after EnvironmentFile; no 0.0.0.0; deploy copies public/. and .next/static/.; does not copy
     dotenv files into the standalone tree.
RC9  WordAuthority.accepts_tokens is untouched by whole 16. Regular player gameplay pages were not
     redesigned. Zustand persist version remains 6.
RC10 Public docs vs tree: AGENTS.md still claiming Vercel frontend hosting; README throttle prose
     still saying DEBUG=true; README still teaching runserver 0.0.0.0:8000; architecture.md
     Production vs Vercel-AI-SDK-library distinction. Measure each. Stale-doc is a finding if a
     human or LLM following it would mis-deploy or mis-bind.
RC11 billing/migrations leftover exists and is not in INSTALLED_APPS (A4: do not revive).
RC12 No credential_env_name / api_key / filesystem-path fragrance in admin/replay/simulation JSON
     at HEAD (re-use existing tests; do not plant live secrets).
```

### 2.2 Process claims (Orchestrator / Planner / implementer) — confirm or refute

```text
P1  Independent R3 occurred only on Slice 1. Slices 2–5 were accepted on E2/R1 (and Slice 4/5
    with isolated static tests). Is that proportionate to the landed attack surface, or a
    residual audit hole? Verdict + why. Do not demand a full-repo audit as a ritual.
P2  The opening 00_handout.md §5.3 said proxy /api/ and /admin/ to :8000. Did later work correct
    the PRODUCT, or only the templates, while AGENTS.md/README still lie? Quote, do not retype
    inventories (D-13).
P3  Name any slice that overclaimed relative to its diff (tests green, contract missing; or
    docs updated, bind still wrong; or commit body vs tree).
P4  Name any prompt defect that caused Worker waste (wrong ruff invocation, heredoc join,
    Plan-mode Meta PARTIAL). Classification: process residual vs product defect.
P5  Closure residuals table in 99_closure.md §4 — walk it against the tree. Mark any row that
    is stale, overstated, or missing.
```

### 2.3 Creative / horizon work (required; still evidence-tagged)

Intuition is authorized. Invention without a pointer is `hypothesis-unverified`.

Produce **at least five** candidate successor logical wholes, ranked. For each:

```text
kebab-id:
one-sentence objective:
why now (leverage vs interview / open-source readers / incident risk / rot):
why not now (cost, Cooperator parked items, missing host name, frozen forks):
first slice sketch (one bounded slice, not a novel):
out of scope for that whole:
```

Mandatory candidate families to consider (you may merge, split, or reject with evidence):

```text
C-docs     Public documentation logical clarity (README/AGENTS/architecture) — Cooperator named this.
C-maint    Django 5.2.x / Next 16.3.x currency and lockfile hygiene — he named an open-ended shape.
C-hygiene  Bounded residuals: F01 serializer, game.0008 alias guard, billing orphan removal.
           Do not mix HSTS preload (Cooperator decision 5 forbids adding it) into a "fix leftovers" whole.
C-locale   Second-opinion review of the eight machine-authored interface catalogs (AGENTS.md not-done).
C-ci       CI/SBOM — parked; only promote if you can bound it.
C-cspjwt   JWT localStorage and style-src 'unsafe-inline' — parked; only promote with a real threat story.
C-engine   Stronger AI search beyond prompt-only — AGENTS.md not-done; engine already authors moves.
C-providers Nine-provider unfreeze — FROZEN; later-horizon unless incident-class evidence.
C-vpslive  Named-host R5 install — requires a host name he has not given; do not seed this as Meta 17.
C-ui       UI/UX polish and mobile pinch-zoom — EXPLICITLY out of Meta 17.
```

**Select exactly ONE** kebab-id as Meta 17. Selection rules:

1. Must not be C-ui or C-vpslive.
2. Prefer the highest-leverage NEXT step that a public reader, an interviewer, or an incident
   reviewer would thank you for — not the most exciting feature.
3. Prefer a whole with a closed boundary and an obvious first Planner slice.
4. The issuing Orchestrator's prior hypothesis (not a skip-audit instruction): a
   `public-docs-and-stale-truth` whole that makes README/AGENTS/architecture match the VPS
   standalone tree, without becoming an encyclopedia, is the strongest named candidate.
   You MAY select it. You MAY select something else if your measurements show a higher-leverage
   bounded whole. If you disagree, say why in one paragraph.
5. Do not hide new product scope inside a "docs" or "maintenance" whole (FrameNest lesson,
   applicable here): if you select docs, the allowlist is documentation plus tests that freeze
   the claims; it is not a secret UI rewrite.

List the non-selected candidates in the handout's **Forward horizon** as later wholes, not as
Slice 2 of Meta 17.

## 3. How to measure (minimum; widen with a reason)

Read, do not inherit:

```text
/home/agile/meta/projects/libretiles/16/00-infosec-hardening-and-vps-readiness/
  00_handout.md, 00_notes.md, 99_closure.md
  01_planning_00.md … 11_implementation_00.md and every *_report_00.md
/home/agile/meta/BRAINSTORMING.md
/home/agile/meta/AP_DEFECTS.md          (D-13, D-14, D-17)
/home/agile/meta/projects/libretiles/PROJECT_CONTEXT.md  (locked forks §5, formed-word invariant)
/home/agile/Projects/libretiles/AGENTS.md
/home/agile/Projects/libretiles/README.md
/home/agile/Projects/libretiles/docs/architecture.md
/home/agile/Projects/libretiles/docs/vps_deployment_guide.md
```

Diff the five commits. Sample the landed files; you are not required to read every file in the
repository (INFOSEC 4.3). State what you selected and what you excluded.

Re-run existing tests (no new tests, no mutation):

From `backend/`:

```bash
env -u APPIMAGE -u ARGV0 -u APPDIR .venv/bin/pytest \
  tests/test_admin_infosec_hardening.py \
  tests/test_admin_replay_api.py \
  tests/test_admin_simulation_api.py \
  tests/test_admin_analytics_api.py \
  tests/test_vps_templates.py \
  -q
```

Also run any existing production-settings / CSRF / HSTS tests you find by name that Slice 3 added.
If a file path in this prompt is wrong, search; do not invent a substitute suite.

Optional health snapshot (do not fail the audit solely because the full default suite is slow):

```bash
env -u APPIMAGE -u ARGV0 -u APPDIR .venv/bin/ruff check game gamecore accounts catalog
env -u APPIMAGE -u ARGV0 -u APPDIR .venv/bin/mypy config game gamecore accounts catalog
```

From `frontend/`:

```bash
npx vitest run src/lib/api.test.ts src/app/api/admin/simulate/[id]/turn/route.test.ts
```

You MAY run additional **read-only** focused pytest/vitest that already exist.
⛔ Do not run `npm run build`. ⛔ Do not install packages. ⛔ Do not hit live HTTP services.
⛔ Do not enable `LIBRETILES_RUN_BENCHMARKS`. ⛔ Do not require `LIBRETILES_TEST_POSTGRES=1`
unless a container is already up; if it is not, record RC5 as established-static from settings
and tests, not as a live Postgres proof.

If you write a short local probe script, synthetic data only, no network, no `.env`, delete
before you finish (containment ledger).

## 4. Findings

Every security finding uses the Security Finding Record fields
(Finding ID `IHR-P16-Fnn`, Status, Severity, Confidence, Evidence class, … through Redaction
requirements). Include `rejected-false-positive` when a suspected hole is disproved.

```text
Severity medium or higher → Acceptance-blocking: blocking (you do not correct it)
low or info → you may mark non-blocking
```

Documentation-truth defects that would cause a wrong production bind or a Vercel deploy are
security-adjacent: file them as findings, not as "style". Encyclopedic README length is a
horizon item, not a CVE.

Do not overstate exploitability. Do not reopen Cooperator decision 5 (HSTS preload stays unset).
Do not treat F01 as a reason to un-close whole 16.

## 5. Deliverable B — expert Agent Orchestrator handout (after the audit, not instead of it)

Write a complete `00_handout.md` for a **fresh** Agent Orchestrator. Quality bar: Libre Tiles
`15/00-admin-frontend-console/00_handout.md` and `16/00-infosec-hardening-and-vps-readiness/00_handout.md`,
plus D-13 Handout Integrity Record, plus PROMPT_CONTRACTS Fresh Orchestrator restoration fields
(readiness: contradiction review, omission review, stale-state, what was NOT re-measured).

Path (create the directory):

```text
/home/agile/meta/projects/libretiles/17/00-<YOUR-SELECTED-KEBAB>/00_handout.md
```

Also write a seed notes file that grants nothing:

```text
/home/agile/meta/projects/libretiles/17/00-<YOUR-SELECTED-KEBAB>/00_notes.md
```

`00_notes.md` contains only: artifact class, that the directory was created by Worker session 12
of closed whole 16, the selected kebab, the baseline SHA, AP pin, and that the successor
Orchestrator appends from here. No slice plan in notes.

### Handout must include

```text
Handout Integrity Record (all six D-13 rows; Coordinate review: honest; paraphrased enumerations
  are a defect declaration)
This file grants NO authority of any kind
Predecessor: whole 16 CLOSED at 33ffa150fa520118e67a6670422fe7fae1c98741
AP pin: 9c5cc44f8b6c92dd56ad2427d13223d7d59c5656 — DO NOT upgrade
Meta group: 17
Dispatch: Cooperator-manual. The Orchestrator does NOT start Workers. Michal pastes prompts
  into a FRESH Worker session (Plan ON = planner; Plan OFF = implementation or audit).
BRAINSTORMING §5 / D-17: every Worker writes its terminal report to
  /home/agile/meta/projects/libretiles/17/00-<kebab>/<ordinal>_report_<exchange>.md
  atomically (temp + rename). Chat is a 3-line notification. Michal is not a courier.
  THE COOPERATOR COMMITS META HIMSELF. Orchestrator writes files; does not commit or push Meta.
D-14: ano / ok / Pokračuj / A CONTINUES selected scope; NEVER selects a new slice or whole.
  Selection Echo before any new slice.
Emoji status + exactly one Cooperator action per message.
Slovak to Michal, masculine forms; Orchestrator self-reference feminine.
Worker prompts/reports: English.
Field-check every prompt: python3 /home/agile/meta/projects/libretiles/apfieldcheck.py <prompt.md>
  Exit 0 required.
RF-16: from backend/, env -u APPIMAGE -u ARGV0 -u APPDIR .venv/bin/python — never ambient python,
  never poetry run, never PYTHON_DOTENV_DISABLED=1, never read .env.
Git: explicit paths only; one commit per slice; non-force push only when the prompt grants it;
  pre-push ls-remote equality.
Frozen product decisions (do not reopen): A1 player UX; A2 IsAdminUser; A3 WordAuthority;
  A4 no forensic reconstruction / do not revive billing; A5 HSTS preload unset; A7 fast pytest
  SQLite; persist version 6; nine providers frozen pending their own whole; formed-word invariant;
  no Stripe / LM Studio / Vercel AI Gateway.
Whole-16 facts the successor must not rediscover from chat: nginx path split; BACKEND_URL
  http://127.0.0.1:8001; standalone ExecStart env prefix; copy public/. and .next/static/.;
  Django admin on loopback :8443; SECURE_PROXY_SSL_HEADER default false until stripping nginx
  is installed. Quote from 99_closure.md or the tree, do not retype long inventories.
Carry residuals (not "fix while here" unless THIS whole owns them): F01, game.0008, HSTS W021,
  billing orphan, AGENTS Vercel, README DEBUG/0.0.0.0, unbound throttle scopes, JWT localStorage,
  style-src unsafe-inline, no CI/SBOM, Slovak lexicon parked, live VPS/R5 never granted.
Parked until Michal selects them: UI/UX polish, mobile/tablet pinch-zoom, live device testing.
First bounded step: independently re-measure HEAD / pin / porcelain / origin/main, then Selection
  Echo Slice 1 of THIS whole, then issue 01_planning_00.md (fresh-worker-session, Native planning
  mode: required).
Tactical slices: a small number of bounded slices. Label every implement-this item that is not a
  frozen Cooperator decision as HYPOTHESIS for the first Planner (D-13). 00_handout.md of whole 16
  treated several already-landed items as work to do — do not repeat that.
Forward horizon: the other ranked wholes, explicitly NOT this whole.
Invariants: no regular UX rewrite; no WordAuthority fork; no AP upgrade; no live host without a
  named host and a later grant; no mixing HSTS preload "while here".
```

The handout's consumer is a fresh Orchestrator who has not seen this chat. Write so she can
work. Do not dump the entire Meta 16 archive into it. Point at `99_closure.md`, `12_report_00.md`,
and the five SHAs.

## 6. Side-effect authority

```text
Libre Tiles repository: READ-ONLY. ⛔ No edits, commits, or pushes.
Meta writes: REQUIRED, atomically (temp + rename), ONLY these paths:
  1) /home/agile/meta/projects/libretiles/16/00-infosec-hardening-and-vps-readiness/12_report_00.md
     File MUST begin exactly: ### Report for ORCHESTRATOR_CHAT
     Language: English.
  2) /home/agile/meta/projects/libretiles/17/00-<kebab>/00_handout.md
  3) /home/agile/meta/projects/libretiles/17/00-<kebab>/00_notes.md
⛔ No other Meta path. ⛔ Do not edit 99_closure.md or 00_notes.md of whole 16.
⛔ No Meta git commit.
Temporary probe files: if any, delete before the report; record them in the containment ledger.
Sub-agents/internal delegation: bounded authority — reading and measurement only; you remain
  the one accountable Worker; internal delegation is NOT independent audit. If you delegate,
  disclose Internal delegation used / Internal subtasks / Evidence a subtask CLAIMED that you
  did not re-measure (BRAINSTORMING.md §1.5 shape).
```

## 7. Stopping conditions

Stop and write what you can if: repository/public gate fails; a deliverable would require
Libre Tiles mutation or forbidden network; secrets would be exposed; this prompt and AP
disagree; or the audit evidence for RC1–RC12, P1–P5, the ranked wholes, and the handout is
complete.

Do not correct the application. Do not deploy. Do not close a new logical whole. Do not
select UI/mobile as Meta 17.

## 8. Report contract

Echo unchanged:

```text
Logical whole identity: infosec-hardening-and-vps-readiness
Worker session ordinal: 12, Worker exchange ordinal: 01
```

Compact core:

```text
status: PASS | PARTIAL | BLOCKED
Phase-qualified result: not-applicable
  (audit is not implementation-PASS; do not invent audit-PASS)
Start commit: 33ffa150fa520118e67a6670422fe7fae1c98741
End commit: 33ffa150fa520118e67a6670422fe7fae1c98741
Changed files and purpose: none — Libre Tiles read-only; Meta writes listed
Commit/push result: not-applicable
Logical-whole closure: not-closed
Report justification: new-evidence
Authority expiry: this audit grant expires at the report
Resolved Execution Issues / Near-Misses: none | …
Pre-Existing Failure Classification: none | …
```

Then the security audit report:

```text
Security task class: broad milestone application audit
Owned/authorized target: …
Commit under audit: 33ffa150fa520118e67a6670422fe7fae1c98741
Scope / Exclusions (what you actually read, and why)
Threat model
RC1–RC12 table (verdict, evidence class, pointer)
P1–P5
Findings: none | IHR-P16-Fnn records (including rejected-false-positive)
Containment ledger with cleanup outcome
Limitations
Residual-risk summary
```

Then the product-horizon annex:

```text
Ranked candidate wholes (at least five)
Selected Meta 17 kebab and why
Rejected families and why (especially C-ui, C-vpslive)
Handout path
Notes path
Handout Integrity Record copied or cited
```

```text
Orchestration critique: none | MEASURED and LEAD lists about this PROMPT/APPROACH/GOAL
Enumeration widened: none | surfaces RC commands did not reach
Internal delegation used: yes | no
```

One smallest next step: Cooperator pastes `17/00-<kebab>/00_handout.md` into a FRESH Agent
Orchestrator session.

Chat: 3–5 lines — status, report path, handout path, selected kebab, whether any blocking
finding exists. The next Orchestrator reads the files. The Cooperator is not a courier.
