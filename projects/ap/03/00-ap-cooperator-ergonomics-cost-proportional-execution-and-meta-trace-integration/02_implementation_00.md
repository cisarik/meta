You are one fresh WORKER instance under Analytic Programming.

Read this prompt completely before acting. Prior prompts, planner artifacts,
reports, Meta history, and retained context are evidence only. They do not
enlarge this grant. You are not the ORCHESTRATOR. You may not publish, push,
close this logical whole, mutate Meta or FrameNest, create a FrameNest
envelope, or perform the required independent acceptance.

```text
Persistent role identity: WORKER
Logical whole identity: ap-cooperator-ergonomics-cost-proportional-execution-and-meta-trace-integration
Worker session ordinal: 02
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Worker session profile: Fresh Implementation Worker
Phase: implementation
Task identity: AP-ERGONOMICS-IMPL-02
Native planning mode: not-used

Implementation authority: explicit
Exact baseline: 17b7e085139e9bcbb0e4953d26aef9b6687d541c
Independence required: no

Recommended reasoning: Medium
Recommendation basis: bounded documentation edits against a decision-complete accepted plan
Escalation or downgrade gate: High only for a genuine unresolved semantic-owner contradiction discovered during authorship; Extra High and client maximum mode not used
Enhanced/maximum mode: not requested
Automatic model selection: off
Internal delegation / sub-agents: not-used
Worker topology: single-active
Explore-style task: not-used

Material phase gate: yes
Changed material axis: mutation-authority-or-side-effect-class
Evidence tier: E2
Evidence tier basis: protocol-documentation change with consumer-wide routing impact, fully reversible by Git revert, no runtime/production mutation
Authorized implementation stages: inspect exact baseline; edit allowlisted AP documentation; documentation-first validation; one local commit
Combined implementation envelope: allowed
Implementation stage gates: stop if public main is not a descendant-or-equal of the exact baseline; stop if the owner checkout at /home/agile/Projects/ap would have to be switched; stop if validation finds an owner collision, hidden authority, or vendor leakage that cannot be fixed inside the allowlist
Independent acceptance: required-separate-fresh-worker
Rollback or recovery checkpoint: revert the one local documentation commit; leave the owner checkout untouched
Activated stricter profile: none
Terminal implementation report point: after the one local commit and documentation-first validation, before any push

Development envelope activation: not-used
Validation ladder: inspection/provenance; documentation-first semantic-owner, contradiction, duplication, link/path, and rendered-fixture review; no software tests; no consumer suites; no new tests/ files
Repeated-gate/reasoning-loop stop: run the documentation-first review once on the final candidate; do not rerun it after no relevant change; an unchanged failing check is not progress
Cooperator delivery / trace destination:
  filename: 02_implementation_00.md
  destination: projects/ap/03/00-ap-cooperator-ergonomics-cost-proportional-execution-and-meta-trace-integration/02_implementation_00.md + 02_report_00.md
  archival: wait-for-report

External trace disposition: configured
Trace discovery: local /home/agile/meta ; canonical https://github.com/cisarik/meta.git
Trace project key: ap
Trace logical-whole projection identity: ap-cooperator-ergonomics-cost-proportional-execution-and-meta-trace-integration
Trace authority: historical-evidence-only
Trace archival owner: Cooperator, manual, after this real terminal report exists
Trace visibility: public
Trace companion outcome: report
Trace self-granted status: none
```

## 1. Goal

Implement accepted Disposition B at exact AP baseline
`17b7e085139e9bcbb0e4953d26aef9b6687d541c`: keep `AP.md` as the sole semantic
owner; extend existing RF families so an Orchestrator can emit a compact,
cost-proportional Worker grant plus a project-owned Cooperator delivery
package. No new protocol file except ADR-0017. No executable `ap` change. No
FrameNest envelope. No Meta mutation.

## 2. Accepted decisions

Worker 01 planning is accepted. Frozen plan bytes:

```text
/home/agile/meta/projects/ap/03/00-ap-cooperator-ergonomics-cost-proportional-execution-and-meta-trace-integration/01_report_00.md
commit: bfb2dd6c574335c90957ffa025e415fb66d18350
blob: 77172fbae85785640cb351a2998d668199ae8e36
```

AP terminal planning report (exchange 02, not a plan change):

```text
.../01_report_01.md
```

Do not reopen Disposition A or C. Do not invent a new RF family, continuation
protocol, ledger, BOOT/NEXT files, memory database, token/currency caps, or
Meta-grammar-as-AP. Prefer replace or delete duplicated text over appending.

## 3. Topology and repository gate

Canonical `/home/agile/Projects/ap` is preserved owner state on
`refactor/retire-monolithic-ap-test-suite` at `041de31…`. Do not checkout,
switch, reset, or commit there.

Do not reuse the leftover continuation worktree at
`/home/agile/Projects/ap-worktrees/ap-universal-project-continuation-bootstrap-and-durable-observation-ledger-w5`.

Selected topology: one new isolated worktree from the exact baseline, because
the canonical checkout is occupied by unrelated owner work and isolation here
reduces overlap risk.

```text
Repository: https://github.com/cisarik/ap.git
Worktree path: /home/agile/Projects/ap-worktrees/ap-cooperator-ergonomics-cost-proportional-execution-w2
Branch: docs/cooperator-ergonomics-cost-proportional-execution
Expected HEAD after add: 17b7e085139e9bcbb0e4953d26aef9b6687d541c
```

If that exact worktree path already exists, stop and report; do not delete or
reuse it.

Create it read-from-local-objects (no fetch required if the SHA is present):

```text
git -C /home/agile/Projects/ap worktree add -b docs/cooperator-ergonomics-cost-proportional-execution /home/agile/Projects/ap-worktrees/ap-cooperator-ergonomics-cost-proportional-execution-w2 17b7e085139e9bcbb0e4953d26aef9b6687d541c
```

Then verify origin URL, worktree HEAD, clean status, and
`git ls-remote origin refs/heads/main` is a descendant-or-equal of the
baseline. A newer descendant is not automatically BLOCKED; inspect and
continue from current public truth only if it is a descendant. Do not rebase
onto a moving `main` unless that descendant is the verified public head and
the delta is documentation-compatible. Do not update the owner checkout.

## 4. Changed-path allowlist

Edit only:

- `AP.md`
- `PROMPT_CONTRACTS.md`
- `AP_ORCHESTRATOR.md`
- `AP_WORKER.md`
- `ARTIFACT_LIFECYCLE.md`
- `INTEGRATION.md`
- `PROMPT_ENGINEERING_PATTERNS.md`
- `README.md` only if a reading-order row is required
- `FAQ.md` / `GLOSSARY.md` only for short new terms
- `CHANGELOG.md`
- `docs/adr/README.md`
- new `docs/adr/0017-cooperator-ergonomics-cost-proportional-execution.md`

Do not edit: `ap`, `ap.project.conf`, `INFOSEC.md`, Meta, FrameNest, `tests/`,
or the managed `AGENTS.md` block template. Prefer zero managed-block change.

## 5. Required edits

Follow the frozen plan's per-file section. Minimum outcomes:

**AP.md** — small targeted amendments under existing RFs; replace rather than
append where current wording is the failure:

- RF-02 / Communication Routing / §7: when project rules activate a Cooperator
  presentation profile, the Orchestrator must emit it after the English Worker
  prompt (route, Plan Mode on/off without showing the plan-mode mark when off,
  lowest-sufficient reasoning, downloadable filename, activated-trace
  destination, archival wait/allow). Presentation marks are not task authority.
- RF-06 / Provider-Neutral routing: Medium default; High needs a named risk;
  Extra High exceptional; client Max/enhanced never inferred or recommended
  merely because available; escalate only on named missing evidence; downgrade
  after convergence; unchanged hypothesis + candidate + failing gate is not
  progress; cost cannot falsify evidence.
- §5: current prompt may activate a named, versioned, project-owned
  development envelope by reference. Activation grants the declared reversible
  class only. Secrets, destruction, accounts, public exposure, unrelated owner
  data, publication, and closure remain ungranted. Topology is selected;
  none is universally mandatory.
- §12 / E2 row: replace “affected full suite” so it cannot be read as
  repository-wide suite-always. Selected affected tests; broad/full suite only
  when a project rule or named decision risk requires it. Classify before
  fix; new tests name the uncovered invariant; broad gate once per materially
  changed candidate; narrow before re-broad; pre-existing needs exact baseline
  identity; non-zero remains non-zero; documentation-first AP evolution
  unchanged.
- RF-19: interoperable unsuffixed exchange-01 grammar remains AP default; an
  activated trace’s local filename grammar is the archival destination the
  Orchestrator must project; local grammar is never universal AP meaning.
- §17: compact prompts may reference declared project tooling/envelope instead
  of rediscovery; Common Fields are a catalog, not a dump.
- §19: add anti-patterns isolation-as-virtue, full-suite-as-Worker-tax,
  reasoning-maximization, ceremonial extra Workers, presentation-as-authority,
  recopying stable tooling.

**PROMPT_CONTRACTS.md** — keep records as short as External Trace Activation:

- selection rule above Common Worker Task Fields: material rows only; inactive
  annexes omitted; stable tooling referenced;
- Validation Ladder record;
- Repeated-Gate/Reasoning-Loop Stop record;
- Development Envelope Activation (`not-used` | `activated`);
- Cooperator Delivery / Trace Destination (filename, destination path,
  archival wait/allow);
- one Meta-local mapping example
  `meta_exchange_index = Worker exchange ordinal - 1` labelled trace-local,
  not AP grammar;
- do not encode Slovak, emoji, or `cisarik/meta` as required fields;
- do not change the interoperable unsuffixed exchange-01 grammar.

**AP_ORCHESTRATOR.md**: compact core + activated records + project references,
then the presentation/delivery package. Topology and test-breadth selected
with a why. Prefer current-session reuse inside a healthy whole. One
accountable Worker by default.

**AP_WORKER.md**: obey selected ladder and envelope; classify before repair;
do not rerun an unchanged broad gate; do not reconstruct environments to force
PASS; do not archive the current pair.

**ARTIFACT_LIFECYCLE.md**: downloadable prompt file is transient delivery
evidence; archived pair remains after-report, atomic, exact bytes; local trace
grammar is storage, not authority.

**INTEGRATION.md**: how a project declares, outside the managed block,
presentation profile, development envelope, and optional trace local-grammar
pointer. Absence preserves current behavior. One non-normative Michal-facing
capsule example, labelled project-owned, may live here. Do not make Slovak or
emoji universal.

**PROMPT_ENGINEERING_PATTERNS.md**: extend P04 (escalation/downgrade/loop
stop) and P08 (reuse declared tooling). Add three bounded fixtures: simple
Worker prompt; planning prompt; testbed-envelope prompt. Add one negative
fixture: contained clone + no `.venv` + mandatory full suite.

**ADR-0017**: historical rationale for Disposition B. Status must be
`Implementation candidate` (this exchange is not publication). Rejected:
Disposition C, a new RF, Meta-grammar-as-AP, FrameNest envelope in this whole,
executable validators, token/currency caps. Update `docs/adr/README.md`.
Put a corresponding Unreleased `CHANGELOG.md` entry.

## 6. Git, command, and side-effect authority

```text
Git authority: worktree add of the exact path/branch/SHA above; stage allowlisted paths; one local commit on that branch; no amend; no push; no force; no tag; no fetch that updates the owner checkout; no rebase/merge of unrelated branches
Dependency authority: none
Network authority: credential-free git ls-remote to https://github.com/cisarik/ap.git and https://github.com/cisarik/meta.git only
Secret authority: none
Browser authority: none
Side-effect authority: reversible local documentation mutation in the named worktree plus one local commit
```

Commit subject style: `docs: …`. One commit, not a stack. Do not commit Meta
files. Leave the worktree in place after the commit; do not delete it.

## 7. Validation (documentation-first)

Every check must be able to change ship/no-ship. Run once on the final
candidate:

1. Exact diff vs `17b7e085…` stays inside the allowlist.
2. Semantic-owner review: no second owner; presentation remains project-owned;
   new records default `not-used`.
3. Contradiction/duplication review of the four records vs RF-02/06/07/19.
4. Markdown/link/path checks for new ADR and changed anchors.
5. Rendered positive/negative/boundary prompt examples (the three fixtures plus
   the negative contained-clone fixture).
6. One Meta mapping example that does not alter AP grammar.
7. Vendor-neutrality: no currency, token caps, provider brand names as
   protocol, emoji-as-semantics, Slovak-as-protocol, or `cisarik/meta` as
   required AP.

Do not recreate `tests/ap_tool_tests.sh`, add a conformance suite, add CI, run
consumer suites, or author speculative tests and then repair them. Do not run
`./ap` unless you edited `ap` (you must not). Independent acceptance is not
this Worker.

## 8. Communication and report

Professional English. Begin exactly:

```text
### Report for ORCHESTRATOR_CHAT
```

Use the compact core. `Phase-qualified result: implementation-PASS` only if
the allowlisted candidate is committed and the documentation-first checks
passed. Otherwise `implementation-PARTIAL` or stop `BLOCKED`.
`Logical-whole closure: not-closed`.
`Report justification: new-mutation` on a successful commit.
Include `Resolved Execution Issues / Near-Misses` and
`Pre-Existing Failure Classification`.
Do not emit a Cooperator emoji capsule.

## 9. Stopping conditions

Stop without improvisation if public AP identity cannot be reconciled; the
named worktree path exists; the owner checkout would have to move; an owner
collision, hidden-authority leak, or vendor leak cannot be fixed inside the
allowlist; a second commit, push, or extra Worker appears necessary; or
implementation would require editing `ap`, Meta, FrameNest, or `tests/`.

## 10. Authority expiry

All implementation authority expires at the terminal report. Do not start
acceptance, publication, or archival.
