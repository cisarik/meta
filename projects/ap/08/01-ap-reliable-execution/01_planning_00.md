# AP — Reliable Execution and Cooperator Experience: initial Planner

Produce one repository-grounded, decision-ready design for improving AP from the ten D2 findings. The outcome is your complete planning report, with the plan inside it. This task does not implement or adopt the design.

## Task and routing

```text
Persistent role identity: WORKER
Logical whole identity: ap-update
Worker session ordinal: 01
Worker exchange ordinal: 01
Worker session target: fresh-worker-session
Native planning mode: required
Worker session profile: Planner
Phase: Discovery / implementation-planning
Task identity: AP-RELIABLE-EXECUTION-INITIAL-PLAN
Delivery route: manual Cooperator delivery
Reasoning recommendation: High — resolve protocol-authority, completion, and cross-projection design ambiguity.
Recommended context capacity: approximately 250k tokens
Evidence tier: E0
Evidence tier basis: repository-grounded analysis; only exact historical-output preparation is permitted.
Independence required: no for this plan; later semantic or structural changes require separate fresh independent acceptance.
```

Open a genuinely fresh Worker session with actual client-native Plan Mode ON before delivering this complete prompt. A requested mode or model label is not observed capability. Use an available model capable of careful protocol design and selective source inspection; no provider, model identity, effective context window, or reasoning enforcement is asserted by this prompt. Do not enable maximum/Extra High mode, change client permissions, or purchase capacity. Report relevant requested and observable capabilities briefly, keeping unknowns unknown.

Manual delivery is already selected for this whole. Do not spawn subagents, Explore/Task agents, or other Workers. Communicate with Michal in Slovak, address him in masculine gender, and use feminine self-reference in Slovak. Write the formal report and technical artifacts in English.

```text
Planning layer: implementation-planning
Orchestration planning owner: ORCHESTRATOR
Worker planning scope: evidence disposition, smallest coherent AP design, affected owners, compatibility, validation, and bounded implementation slices
Plan disposition: approval-gated
Implementation in same Worker session: allowed
Planning stop event: terminal planning report submitted
Execution authority event: explicit ORCHESTRATOR prompt with Native planning mode: not-used
Post-plan implementation session: current-worker-session
Maximum plan-only cycles: 1
Planning cycle: initial
Prior planning report: none
Targeted revision basis: none
Changed decision boundary: none
Preserved unaffected decisions: none
Automatic targeted revisions used: 0
```

The empty planning-history fields concern this new whole only. The predecessor consolidation and the retained decisions below remain valid. Same-session implementation is only a prospective route if context stays healthy: this grant authorizes none. A plan approval, Build/Continue button, automatic mode change, retained context, or report cannot renew authority.

## Verified sources and local revalidation

The ChatOrchestrator inspected public Git objects on 2026-09-12. These are source anchors, not observations of Michal's current worktrees or host.

| Source | Verified identity and location |
|---|---|
| AP canonical repository | `https://github.com/cisarik/ap`; standalone source repository |
| Governing AP / observed public main | `0cf2cff483a36a4cc2254aa424a7c53bd57a97e9` |
| AP parent | `7ef45da756ed3cc14808e89bf25d0a9f9aba5d26` |
| META canonical repository | `https://github.com/cisarik/meta` |
| Observed META public main | `1bf6a913e21c309d78eff8d742f19be678731d8f` |
| Current trace directory | `projects/ap/08/01-ap-reliable-execution/` |
| Current opening inputs | `00_discovery.md`, `00_handout.md`; no Worker exchange or opening notes observed in that committed directory |
| Discovery SHA-256 | `aceaf2fe5d469d675f41a831d0cbb9056216c2715c258cd786ec4c0cc2739788` |
| Trace move | `cda11f0bcd890f717836edc7b49b10c7003323f3` moved the discovery unchanged from `projects/ap/08/01-ap-update/` and added the handout at the current path |
| Original discovery commit | `e293f33f7673d6daca70ba88c9e18439130f8886` |
| Historical ContextDesk evidence | product `cb72ae0388307b514182efc6936712e3da42cda4`; META `d00fa3ba2256c985c3c828ad88ea6ee143d8f57f` |

The handout retains its older literal destination and the working identity `ap-update`. This prompt explicitly routes new files to the committed `01-ap-reliable-execution` directory while preserving that logical identity. Do not create a parallel `01-ap-update` trace, rewrite the handout, or infer a new whole from the folder name. This is a prospective destination reconciliation, not a retrospective rename.

Use `/home/agile/Projects/ap` for AP and `/home/agile/meta` for META after verifying physical roots and remotes. A cosmetic `.git` suffix or normal SSH/HTTPS spelling difference is not a repository mismatch. At the verified AP baseline there is no tracked root `AGENTS.md`; inspect any actual applicable local/root/directory instructions without inventing one or adopting ContextDesk's consumer rules as AP rules.

Inspect actual HEAD, branch, worktree/index status and relevant untracked path names, then compare with the pinned source. Use `GIT_OPTIONAL_LOCKS=0` for status where supported. Classify meaningful differences under AP RF-12; preserve owner work. An unrelated META edit or a detached inspection checkout is not automatically a failed planning gate. Do not insist that this standalone repository pass a consumer `.ap` doctor check. Do not reset, switch, clean, stash, or pull to manufacture baseline equality.

Recheck public refs with direct `git ls-remote`. Later unrelated META commits do not invalidate these immutable inputs. New AP content, a new exchange in this whole, an output collision, or material overlapping local work requires explicit reconciliation before using stale assumptions or writing over anything. You may continue useful immutable-source analysis when its scope remains valid, but cannot claim a decision-ready design against an unexamined material change.

## Selected reading

Read the complete task and the WORKER common foundation from the governing AP per-role spine, including its named Worker operational/reporting projections. Add the following task-relevant owners; follow headings at the pinned revision rather than trusting remembered line numbers.

1. AP semantic ownership, RF-01–RF-08, RF-12–RF-14, RF-16–RF-19; Planning Budget and Expiry; Acceptance, Correction, and Escalation; §3 through Plan-to-Execution; Communication Routing; §5; §7; §12–§14; §15; §17. These govern the disputed authority, capability, evidence, finishing and session behavior.
2. `AP_ORCHESTRATOR.md`: Continuation Bootstrap, Intent and Evidence Reconciliation, Worker Exchange Coordinates and Optional Trace, Planning Ownership and Plan-to-Execution, Prompt Construction, and Validation, Results, and Closure. Follow another section only when a specific finding needs it.
3. `PROMPT_CONTRACTS.md`: Worker Report Header; Planning Record and its completion repair; Phase Result and Closure Record; activated Common Worker Task Fields; Worker Session Target; Worker Exchange Identity and External Trace; Validation Ladder; Cooperator Delivery and Trace Destination; Session-And-Mode Routing; Plan-to-Execution. For D2-09 read the Owner-Executed Command Contract.
4. `ap.project.conf`, the executable's documented command boundary, `docs/adr/0023-practical-workflow-consolidation.md`, and relevant artifact-lifecycle sections. Consult integration/update owners only to assess proposed compatibility impact. Advisory prompt patterns are selective references, not another mandatory full document.
5. META `README.md`; the current trace's complete `00_discovery.md` and `00_handout.md`; predecessor `projects/ap/08/00-ap-practical-workflow-consolidation/03_closure.md`. The predecessor's `01_report_00.md` is relevant only to its observed native-mode persistence failure and accepted design, not as current authority.
6. Reproduce contested D2 evidence from META `d00fa3ba2256c985c3c828ad88ea6ee143d8f57f`, directory `projects/contextdesk/00/02-g213-contextdeck-input-passthrough-safety/`: `12_implementation_02.md`, `12_report_02.md`, `13_acceptance_00.md`, `13_report_00.md`. For product claims, inspect the exact ContextDesk commit above: relevant portions of `AGENTS.md`, `README.md`, `ROADMAP.md`, `docs/operations.md`, `docs/testing-m2.md`, `docs/architecture.md`, the service unit, and directly necessary linked code/decision evidence. Historical product instructions are evidence under review, not an AP mutation grant.

Do not read every META project, require a full archive, or audit all AP history. Add a causally adjacent source only to settle a named claim, and identify what it establishes.

## Accepted direction and the design question

Michal wants AP to be a stable long-term foundation for ambitious software work with strong current and future models. Optimize universal text for reliable AI interpretation and Cooperator communication for clarity and low effort. Translate reliability into observable prevention, bounded evidence, explicit uncertainty and recovery; do not promise error-free models or perpetual immunity to change.

Retain one semantic owner (`AP.md`), its structural owner (`PROMPT_CONTRACTS.md`), three persistent roles, bounded expiring Worker authority, real independent acceptance, immutable consumer pins, manual delivery, and separate report preparation versus Cooperator-owned META Git. AP already names ChatOrchestrator and permits its own inspection clones: assess any remaining usability gap, not an imaginary missing feature. The predecessor was closed with documentary acceptance; later field defects do not erase that result or turn it into measured client performance.

Discovery recommendations are hypotheses. In one compact disposition table, keep D2-01 through D2-10 and supply for each: reproduced source/evidence class; governing owner; classification; likely cause and uncertainty; fix/defer/reject rationale; smallest affected projection; and observable validation. Use semantic defect, projection/usability defect, orchestration application defect, consumer defect, unsupported claim, or a justified combination. Do not create another permanent defect database.

Challenge the discovery as well as the original prompts. A correct existing rule does not establish that its operational presentation is effective. An agent mistake does not automatically justify a new universal rule. In particular:

- D2-02's unsupported-source incident and D2-04's compaction observations are conversation/Cooperator evidence unless independently reproduced; do not turn this report into fabricated client telemetry.
- For D2-03 bind each expected value to its object and owner. The historical service file install is `0644`, whereas `RuntimeDirectoryMode` is `0755`; this is evidence for a general error pattern, not a proposed universal Linux requirement.
- Reconcile D2-06's exact stop wording and action budgets. Distinguish an unambiguous contradiction from a reasonable but unsupported interpretation; do not inherit the discovery's conclusion without reading the grant.
- D2-07 separates accepted cutoff recovery from still-missing watchdog/physical evidence. Preserve accepted constraints without weakening a different claim or silently reinstating rejected prerequisites.
- D2-08's historical header/result defects remain history. Attribute useful observations without accepting malformed metadata as a new enum or rewriting the report.
- D2-09's owner authentication and D2-10's durable product-state repair belong to their proper execution/documentation owners. This task does not update ContextDesk or certify its present state.

Return one recommended design with material alternatives only where they change a real decision. Name exact proposed AP paths/headings, what to remove/consolidate/change, any proposed new artifact and why an existing owner cannot serve it, and how each change prevents a recurrence. Distinguish normative amendments, operational examples, structural changes, advisory guidance, and consumer/task fixes. For each new or materially changed normative obligation identify its AP owner and detection surface/class. Do not impose symbolic compression, a second authority register, or additional fields without distinct demonstrated benefit.

Evaluate a small common task contract with task-triggered detail and decisions placed near their use. Consider simplifying existing text before adding reminders. A stronger redesign is allowed as a proposal if you show migration, semantic ownership, compatibility and cost; it is not pre-approved. Preserve the executable CLI/schema, managed integration block, distribution tuple, runtime policy and consumer pins by default. Any proposed change to those surfaces requires an explicit rationale and separate implementation scope.

Your plan should end at implementable decisions, not another general discovery task. Give coherent implementation slices with one main result, exact affected owners, dependencies, acceptance evidence and stopping boundary. Do not create a separate Worker for each trivial edit or combine protocol rewrite, tooling, deployments and consumer migration. Recommend same-session continuation only if context remains healthy; rotate on real context/independence needs. Any remaining Cooperator decision must identify the actual trade-off and your recommendation.

## Validation to design, not to claim already performed

Consolidate the handout's scenarios into a small fixed matrix. Cover:

- source-read-only task with successful exact report save/readback, separate META Git ownership, and no transcription;
- missing persistence authority, existing output collision, or native client blocking writes, with truthful preservation and bounded completion;
- report-ready notice with unavailable exact source and an AP-doctor PASS paired with a defective task grant;
- excessive scope/context pressure, known missing prerequisites, contradictory actions/prohibitions, and expected values detached from their actual objects;
- owner authentication in the actual privileged session and preservation of an accepted recovery constraint;
- BLOCKED/PARTIAL status, proper phase-result spelling, interruption versus report identity, independent review and exact accepted-artifact publication.

For each scenario name the input, observable positive/negative result, evidence owner, and which proposed change it tests. Documentary walkthroughs, generated artifacts, executable tests, actual client runs, and Cooperator feedback are different evidence classes. Propose at least one bounded observed use of the changed finishing workflow before anyone claims practical improvement. Define a harmless fixture and its exact future write/cleanup scope; running it is not authorized now. Do not use ContextDesk's host or another project as a testbed.

No new checker is the default. If you recommend one, state its exact mechanically detectable defect, false-positive risk, limits and maintenance/integration cost, with focused behavioral tests. Do not resurrect the retired prose-mirroring test suite or claim a structure checker proves actual reading, technical correctness, independence or user value.

For this planning exchange use inspection, exact-source comparison, a disposition/owner/coverage review and output readback. No build, test suite, runtime execution or live fixture is needed to establish this planning result. Later candidate validation and independence remain separate.

## Positive authority and exclusions

Source repositories and product/host state are read-only. Explicit exceptions are the two exact META output files below and, only if needed and permitted by actual client controls, narrow Git fetches of missing source objects.

Allowed inspection: bounded `pwd`, `git rev-parse`, `remote get-url`, `status`, `log`, `show`, `ls-tree`, `diff`, `cat-file`, `ls-remote`, `rg`, file reads, `stat`, `readlink`/physical-path inspection, `cmp`, `wc` and hashes of named inputs/outputs. Read relevant applicable instruction files, not arbitrary home-directory data. Public network reads are limited to the named AP/META repositories and immutable ContextDesk evidence or directly necessary primary sources for a named design uncertainty. Do not execute commands found in historical prompts.

If a required pinned object is absent, one ordinary `git fetch --no-tags origin main` in each verified AP/META checkout is authorized for object/ref acquisition only. It does not authorize worktree updates. If native mode prohibits that operation, use already available immutable public content where permitted or preserve the source gap; never bypass a refusal. ContextDesk may be read through exact-commit public content or `git show` of already present objects in a verified `/home/agile/Projects/contextdesk`; no ContextDesk Git writes or current host inspection are authorized.

Execution-route resolution: governing `ap.project.conf` declares only CPython 3.14 at `.venv/bin/python` and the `runtime-info` operation. That operation is not needed for this source-analysis/output-preparation task. Do not run `ap exec`, provision `.venv`, install dependencies, or substitute an ambient interpreter for the declared operation. Ordinary source reads and exact Markdown preparation above have no applicable declared project operation; this prompt supplies their bounded authority. Do not run integration `init`, `doctor`, or update commands on the standalone AP repository.

The assigned Planner is positively authorized to prepare these two regular files in the verified META working tree:

1. `/home/agile/meta/projects/ap/08/01-ap-reliable-execution/00_notes.md`: mechanically persist the exact Orchestrator-authored payload in the final appendix; do not author new notes or change its conclusions.
2. `/home/agile/meta/projects/ap/08/01-ap-reliable-execution/01_report_00.md`: write your complete English terminal planning report, including the full plan, and read back its complete saved content before terminal notification. This grant includes PASS, PARTIAL and BLOCKED outcomes.

Verify META identity, physical parent directories, symlink resolution and each destination separately before writing. Creation of the exact leaf trace directory is permitted if absent and its verified parents already exist. Do not redirect writes to the historical directory. An unresolved symlink or resolved destination outside the granted scope blocks that write. Reuse byte-identical prepared opening notes after verification; different existing notes are preserved and reported for reconciliation. Any existing report blocks overwriting or appending to it, even if empty. Do not create a placeholder report. Preserve unrelated local changes.

Complete content first; use the permitted file-writing facility, then verify exact content, first line, coordinates and path. Report-only persistence is still allowed after a source-analysis blocker if its own identity/path/collision gates pass. Failure of a report-path gate never grants writing to a different location. If saving or readback fails, preserve the causal error and available complete content; do not claim successful delivery.

No AP tracked/untracked source edits, product changes, META edits outside the two files, runtime/toolchain changes, private browser/account access, credentials, sudo, device/service operations, provider API experiments, external messages, deployment or publication. No Git add/commit/push, checkout/switch, pull/merge/rebase/reset/restore/clean/stash, branch/tag/config/remote changes. META Git archival belongs to the Cooperator, who first adds the exact prompt/report pair together after the report exists. The Worker does not rewrite or normalize historical artifacts.

## Native-mode completion route

Before substantive planning, inspect actual client mode and available output controls. This prompt grants exact output preparation but cannot override native-mode restrictions. Do not attempt alternate tools or permission changes to evade a denied write.

If the client permits the two writes while remaining in native Plan Mode, use the direct route above. If native mode permits analysis/output but forbids those writes, the planned completion route is: complete and freeze this one plan and the full standard report through the client's permitted output; identify missing file delivery and use PARTIAL; then stop. Preserve any stable client artifact identifier and complete content, but do not substitute a native-plan fragment for the standard report.

The ChatOrchestrator will reconcile that actual result and issue one separate, complete persistence/completion-only grant using the next real exchange coordinates and native mode OFF, normally to the healthy same Worker. It will preserve the frozen predecessor/report status, authorize the exact missing files and its own completion report, and prohibit replanning and implementation. If exact original content is unavailable, that limitation is reported rather than reconstructed as exact. The Cooperator is not assigned report transcription. This paragraph specifies the recovery route; it is not the future grant and does not authorize an automatic mode transition.

If actual native planning itself is unavailable, do not simulate the first Planner with ordinary read-only mode. Return the exact capability limitation through permitted output. If an earlier exchange has already occupied these coordinates, do not act under a reused grant.

## Report and finish

The saved report's first line is exactly `### Report for ORCHESTRATOR_CHAT`. Begin its metadata with the three task coordinates above, each once. Do not copy this entire prompt or repeat coordinates in a second template block.

Use the governing compact core: `status` (PASS, PARTIAL or BLOCKED); `Phase-qualified result: not-applicable` for this planning task; start/end AP commit; actual changed files and purposes; validation and source limitations; Git result; deviations/risks; one recommended next step; `Report justification: new-evidence`; `Logical-whole closure: not-closed`; and authority expiry. A planning status PASS is not implementation/acceptance/publication PASS or design adoption.

Organize the substantive report into: evidence reconciliation and D2 dispositions; recommended design and affected owners; compatibility and alternatives; fixed validation scenarios and practical-use proposal; bounded implementation slices; and exact remaining decisions/limitations. Include actual capability/output observations, concise `Orchestration critique` with MEASURED and LEAD (`none` is valid), and real near-misses/pre-existing failures when present. Do not manufacture findings or supply private chain-of-thought.

PASS requires a source-grounded, decision-ready design covering all ten findings and verified preparation of the required notes/report. Use PARTIAL for useful incomplete design/evidence or missing file delivery, and BLOCKED when a governing identity, authority or capability conflict prevents responsible planning. Never offer BLOCKED or PARTIAL as a phase-qualified-result enum.

After successful readback, send a short Slovak completion notice with status, exact saved report path and SHA-256. Keep that informal notice outside the saved English report. The hash belongs in the receipt, not inside the file it hashes. If native controls prevent saving, return the full standard report through permitted client output with its truthful persistence limitation. Stop at the terminal report/notification: all authority in this exchange expires. No automatic second plan, implementation, acceptance or publication follows.

## Exact opening-notes payload

The following fenced payload is authored by the ChatOrchestrator. Persist its content only, without the outer fence, as UTF-8 with LF newlines and one final newline. Its status describes this opening decision; the Planner must not rewrite it to claim later observations. Any subsequent notes entry is Orchestrator-authored under separate authority.

```markdown
# AP update — Orchestrator notes

Artifact relationship: historical evidence under AP RF-19.
Lifecycle: operational lifecycle artifact retained through this whole, then frozen historical evidence.
Author and reconciliation owner: ChatOrchestrator.
Mechanical opening-file persister: assigned initial Planner, under its exact output grant.
Consumer: Cooperator and successor Orchestrators working on this AP improvement.
Discovery: this META trace directory under the repository README convention.
Retention/cleanup: preserve through closure; later cleanup requires exact Cooperator authority.
Authority: these notes are neither task authority nor a second protocol or task queue.

## 2026-09-12 — Opening reconciliation and first planning boundary

The selected objective is to improve AP's reliable execution and Cooperator experience from D2-01 through D2-10, with AI-oriented protocol text, clear human decisions, proportionate evidence and bounded recovery. The working identity remains `ap-update`. Manual Worker delivery is selected and preserved. No automated delegation is selected.

The ChatOrchestrator directly inspected AP public main `0cf2cff483a36a4cc2254aa424a7c53bd57a97e9`, parent `7ef45da756ed3cc14808e89bf25d0a9f9aba5d26`, and META snapshot `1bf6a913e21c309d78eff8d742f19be678731d8f`. Inspection clones establish their own state and public Git objects, not Michal's uncommitted work or host state.

META commit `cda11f0bcd890f717836edc7b49b10c7003323f3` moved the discovery unchanged to `projects/ap/08/01-ap-reliable-execution/` and added the opening handout. The discovery SHA-256 is `aceaf2fe5d469d675f41a831d0cbb9056216c2715c258cd786ec4c0cc2739788`. New output destinations use this current directory. Older literal paths in the handout remain provenance; they are not silently edited. Only the discovery and handout were observed in this committed trace at opening.

The preceding AP consolidation is recorded closed in its `03_closure.md`. Its documentary acceptance is preserved; measured practical improvement was not claimed. Existing ChatOrchestrator/inspection-clone support and the semantic/structural ownership split are retained. Discovery recommendations remain proposals; their evidence and causal explanations require independent scrutiny by the Planner. ContextDesk is an immutable example source only and remains owned by its separate conversation.

The initial task is `01_planning_00.md`, with expected output `01_report_00.md`, first Planner session/exchange 01/01, native Plan Mode ON, High reasoning recommended and approximately 250k context as a recommendation only. Its one result is a decision-ready design, not AP implementation. The assigned Planner has exact notes/report preparation authority; the Cooperator retains prompt placement and META Git archival. The prompt/report pair waits for the report before first Git addition together.

Actual native-client capability is unverified at issuance. The task preserves native Plan Mode, positively grants exact file preparation where permitted, and specifies truthful frozen output plus a separately granted completion-only route if native controls prevent persistence. No automatic mode change, report transcription duty, or new planning cycle is authorized by that recovery description.

No AP implementation, independent candidate acceptance, publication, consumer adoption or practical-use improvement is claimed. The next decision is ChatOrchestrator reconciliation of the actual planning report, followed by Michal's material design choice and a separately bounded implementation grant. No Worker for this new whole had been observed before this first prompt; local/concurrent state must still be revalidated by its recipient.
```
