### Report for ORCHESTRATOR_CHAT

**Direct answer:** `27_deployment_00.md` was **authored**, not **dispatched**.
The Cooperator did not paste that prompt into a fresh Worker session. No
Worker 27 ran. The same bounded host mutation (install the Session 26
acl-guard rule, reload, targeted add/change, session-user immediate and
delayed readback) was done as Orchestrator-led owner execution in the
same chat. Archive the prompt file with this report as an issued-but-not-
dispatched prompt plus the truthful owner-path outcome. Do **not** start
that Worker now: the target udev file is already present and the prompt’s
own fail-closed rule would `BLOCKED`.

This file occupies the Session 27 report slot next to
`27_deployment_00.md`. It is **not** a dispatched-Worker execution of that
prompt. Read the identity block before any phase result.

```text
Actual persistent role identity: ORCHESTRATOR
Persistence grant: COOPERATOR explicit request to write this file
Logical whole identity: g213-contextdeck-input-passthrough-safety
Issued Worker session ordinal: 27
Issued Worker exchange ordinal: 00
AP-valid first exchange for this session: 01
META filename index: 27_report_00.md  (meta_exchange_index 00 = first exchange)
Worker session target named in the issued prompt: fresh-worker-session
That Worker: not dispatched
Native planning mode: not-used
Delivery of this file: Cooperator-requested META persistence; no commit/push
```

The issued prompt `27_deployment_00.md` named a fresh independent G3
deployment and INFOSEC R6 re-audit Worker. The Cooperator did **not** paste
that prompt into a Worker. The Cooperator instead directed
Orchestrator-led, owner-executed installation of the one Session 26 udev
file, a narrowly targeted udev reload/trigger, and session-user ACL
readbacks. This report is the truthful terminal record of that owner path
plus the Cooperator’s closing direction for this logical whole.

Do not invent a Worker 27 that did not run. Do not treat this file as
independent acceptance. Do not issue another G3/G8 Worker because
independence was skipped. The Cooperator waived a second R6 re-audit in
order to stop the audit loop and advance the product.

```text
Issued task identity: CONTEXTDESK-G3-ACL-REPROBE-DEPLOY-REAUDIT
Actual task identity: CONTEXTDESK-G3-ACL-GUARD-OWNER-DEPLOY
Phase named in issued prompt: deployment
Actual phase: owner-executed host recovery + Orchestrator reconciliation
Finding identity: G3-ACL-REPROBE-01
status: PASS
phase-qualified result: deployment-PASS
Independence: not-established (Cooperator-waived; do not re-open)
INFOSEC verified-closed by fresh auditor: not claimed
Host G3-ACL-REPROBE-01: host-mitigated on the authorized reference host
Report justification: changed-external-state
Logical-whole closure: not-closed
Cooperator direction: park this whole; do not continue it; open the next whole
Product Git publication: none in this exchange
META Git publication owner: COOPERATOR
Authority: expires at this report
```

Product, AP, and host identities at this writing (verify against origin
before routing; this file is not a substitute for public readback):

```text
Product remote/branch: https://github.com/cisarik/contextdesk / main
Product HEAD: ca6052e816d4884ddeac9d7499a42c2aca089e7e
Required parent of that commit: ab10491c49d0b6574b6953a02935a4664c39d7c2
AP gitlink and checkout: 0cf2cff483a36a4cc2254aa424a7c53bd57a97e9
Runtime broker baseline (unchanged): cb72ae0388307b514182efc6936712e3da42cda4
Installed broker SHA-256: 8e80f2d78530b01d9cda4ab77c4ff75d2888055dadce35fbb9483a15651940a6
Installed unit SHA-256: 286154ba6f443adced5dc965b0f0c4da2dfae2a44e4f77edf888c6ef96c30503
Installed sleep hook SHA-256: 91e47888a90f3b2c1853f8bd454f1bdeb342a507efb1084767e09480d4eeac8e
Installed 61- SHA-256: c31ed56d14ba2cd94e73b2883a860c456965ea4a7fb42623aac011cd03d9ea70
Installed 62- SHA-256: 070c7c98229a89f89b1ac42253e1a57fa4da927fd299090ed93a81e622a7a5ad
Installed 99-uinput SHA-256: f10d2d709d047d1648448572cff2cbe35552aa08fe07830a5361cd948471b7b2
Installed 99-acl-guard SHA-256: da87276d2997080205303e5ffcbc812b179d6d0747f32ce36e2d58bd8f3511e5
Handoff still current until you replace it: 00_handout_02.md
```

Local META at this writing had origin/main equal to
`ef79edab1f298b02ab10d9db979296ae7997da22` (Session 26 pair archival).
`27_deployment_00.md` was present and untracked. This report file is new.
A file `28_canvas.md` may exist as a non-protocol canvas export; it is **not**
an AP exchange, not a handout, and must not be committed as if it were
Worker 28.

Speak-to-Cooperator language in the originating session was Slovak. This
report is English. No subagents. No broker start, ARM, DISARM, grab, live
source open, input-remapper configuration change, product commit, or META
commit/push occurred under this record.

---

## 0. What you must do after reconciling this report

You are the current ChatOrchestrator. This is your last required M2 routing
act. The Cooperator’s closing instruction is:

1. Reconcile this report as a claim/evidence package. Do not dispatch the
   Worker named in `27_deployment_00.md`.
2. Park `g213-contextdeck-input-passthrough-safety`. Do not issue session 28
   in this whole for udev, uninstall, G3 re-audit, G4 grab, autostart, or
   another “smallest next step” from an old report.
3. Author a **new** continuity handoff in this directory:
   `00_handout_03.md`. It supersedes `00_handout_02.md`. Use it as the M2
   park record: named G4 slices, G3 host-mitigated, docs drift named, G8
   deferred, this Session 27 identity honesty.
4. Author a **fresh ChatOrchestrator prompt** for a **new logical whole**,
   not a continuation of M2 work. The Cooperator will paste that prompt
   into a genuinely fresh Orchestrator session. That successor must be able
   to start without this chat.
5. The Cooperator archives the exact Session 27 pair
   (`27_deployment_00.md` + this report) in the same first-add META commit
   when they choose to publish. They own META add/commit/push. Do not
   rewrite historical reports.

The Cooperator’s product goal, stated in the closing turn, is the finished
ContextDeck cockpit: keyboard, session, virtual desktops, and the rest of
the plan of record. That is **not** one Worker and **not** one logical
whole. The next bounded whole is M3. Collapse M3+M4+M5+remap into one
identity and you will recreate this loop at a larger scale.

---

## 1. Session 27 — what actually happened

### 1.1 Issued prompt versus executed path

| Item | Issued `27_deployment_00.md` | What ran |
|---|---|---|
| Actor | Fresh independent WORKER | COOPERATOR owner terminal + this ORCHESTRATOR |
| Independence | required-separate-fresh-worker | not-established; waived |
| INFOSEC | R6 correction then fresh re-audit | owner recovery only; no auditor Worker |
| Authority | one udev file, reload, targeted add/change, immediate and delayed readback | same mutation class, simpler sequencing |
| Broker / ARM / grab | forbidden | not done |
| Product Git | none | none |
| Shell | not specified as fish | Cooperator login shell is fish; bash heredocs failed |
| Report author | that WORKER | this ORCHESTRATOR under Cooperator grant |

The issued prompt remains historical evidence of the Orchestrator’s
intended Worker. It must stay byte-identical in the archive. This report
does not execute it after the fact.

The issued prompt wrote `Worker exchange ordinal: 00`. AP RF-19 first
exchange is `01`. META `_00` is the first-exchange filename. Treat the
issued `00` as a prompt defect, not as a second exchange.

### 1.2 Host mutation performed

Exactly one new host policy file was installed from product commit
`ca6052e816d4884ddeac9d7499a42c2aca089e7e`:

```text
Source: packaging/udev/99-contextdeck-input-acl-guard.rules
Target: /etc/udev/rules.d/99-contextdeck-input-acl-guard.rules
Type: regular file
Mode: 644
Owner: root:root
Byte identity versus source: equal
SHA-256: da87276d2997080205303e5ffcbc812b179d6d0747f32ce36e2d58bd8f3511e5
```

Neighboring ContextDesk udev files were not replaced. Their source SHA-256
values above matched the already-installed files. The broker unit remained
`static/inactive/dead`, `MainPID=0`, no runtime directory, no virtual
device. input-remapper remained enabled/active. No `systemctl daemon-reload`
and no sysusers run.

After the file was proven present, udev rules were reloaded and add/change
was triggered only for:

- G213 `event*` nodes matched by USB `046d:c336` (not all input, not hidraw);
- `/dev/port`;
- the `i2c-dev` class (the product’s guarded i2c set).

`/dev/uinput` was not triggered. Direct `setfacl` on live nodes was not used.
The only ACL effect intended is the late rule `RUN+="/usr/bin/setfacl -b %N"`
on those three classes.

### 1.3 Execution near-misses (do not repeat)

```text
Resolved Execution Issues / Near-Misses:
(1) Combined install+trigger+python-verify was too large. First attempt
    never produced W-G3-INSTALL-DONE. Cause: SRC used $PWD/packaging/udev/
    while the owner had already cd’d into packaging/udev, so test -f failed
    under set -e. Resolution: later PUT used absolute source and destination
    paths. Residual: none for the file once PUT succeeded.
(2) A verify run as root reported EVENT 0/2, hidraw 0/2, UINPUT_SESSION=no
    while TARGET=absent. Cause: the checker compared named ACL entries to
    the process user; root is not the seat user, so session ACLs looked
    absent and hidraw/uinput looked stripped. That snapshot was not G3
    restore and was not a preservation failure. Resolution: all acceptance
    readbacks used the graphical session user. Residual: future owner
    blocks must assert WHO!=root.
(3) APPLY was first run after leaving the root shell, as an unprivileged
    user. No W-G3-APPLY-DONE. udev reload/trigger require privilege.
    Resolution: APPLY from the session user’s fish with sudo. Residual: none.
(4) A bash heredoc python3 - <<'PY' printed nothing in the Cooperator’s
    environment. Login shell is fish. Resolution: python3 -c with a
    fish-safe single-quoted script. Residual: successor prompts that ask
    the Cooperator to run host commands must be fish-first.
(5) Issued Worker 27 was not dispatched. Residual: this report must never
    be misread as that Worker’s independent re-audit.
```

```text
Pre-Existing Failure Classification:
Session 25 left G3 insecure after remove+rollback+udev trigger. TAG-="uaccess"
does not delete a materialized POSIX ACL. Delayed userspace (OpenRGB and/or
logind) restored session ACLs on event/port/i2c. That was the hole this
install closes. Uninstall-without-ContextDesk-rules returning distro OpenRGB
uaccess is expected, not a G3 pass. Product README/ROADMAP still claim live
suspend and LED/all-control are open; Sessions 22 and 23 already accepted
those named slices. P1 in the milestone table still says Planned while G1
is closed. Those are documentation drift, not missing G4 evidence.
```

### 1.4 ACL readbacks (session user, semantic only)

Immediate owner readback after `W-G3-APPLY-DONE`, a second owner readback,
and a later Orchestrator readback as the session user all agreed:

| Check | Required | Observed |
|---|---|---|
| G213 event session ACL | none | `0/2` |
| `/dev/port` session ACL | none | `no` |
| `/dev/i2c-*` session ACL | none | `0/8` |
| G213 hidraw session ACL | present (lighting) | `2/2` |
| `/dev/uinput` session ACL | present | `yes` |
| `/dev/uinput` broker ACL | present | `yes` |
| Target rule | present | `present` |
| Broker unit | static/inactive/dead | observed |
| input-remapper | unchanged enabled/active | observed |

No device node was opened for I/O. No key names, scan values, node numbers,
serials, UIDs/GIDs, host names, addresses, or raw ACL dumps are recorded.

This matches the Session 26 design: strip leftover session ACLs on the
guarded classes; do not touch hidraw or uinput.

Host-mitigated is true for this deployed ACL scope on this host at this
time. It is not whole-G3 packaging certification, not install/remove/
rollback completeness, not production readiness, and not autostart.

### 1.5 Privilege

Privileged steps were Cooperator-owned (`install` as root, then `sudo`
from the session-user fish for udev). The Worker-side agent shell did not
hold a sudo timestamp (`sudo -n` failed there). The Cooperator was asked
to run `sudo -k` in the fish that authenticated. Release on that tty is
Cooperator-owned; this report does not claim observed `sudo -k` on the
owner tty.

### 1.6 Finding disposition

```text
Finding ID: G3-ACL-REPROBE-01
Title: session POSIX ACL can remain or return after udev re-probe
Severity: high (keystroke confidentiality on G213 event nodes; raw I/O on
          /dev/port and i2c)
Status: host-mitigated on the authorized reference host
Verified-closed by independent auditor: no
Cooperator residual-risk decision: accept host-mitigated + park M2;
          do not spend another R6 cycle
Assets preserved: hidraw lighting ACL; uinput session ACL; broker uinput ACL
Assets restored: no session ACL on G213 event nodes, /dev/port, i2c-*
Not claimed: general OpenRGB vulnerability; all hosts; uninstall safety;
            delayed restore after a future remove of this rule
```

Repeating Session 25’s remove+trigger **will reopen the hole** unless the
acl-guard file is present when re-probe runs. Uninstall completeness and
G3-while-installed are opposite goals. Do not test them as one gate.

---

## 2. The logical whole — honest map

Identity: `g213-contextdeck-input-passthrough-safety`

Objective (plan of record): a narrow libevdev/uinput broker for the Logitech
G213 Prodigy only (`046d:c336`). Explicit authenticated ARM. 1:1
pass-through. Crash, hang, and suspend must leave the real keyboard usable.
No autostart. No remapping in this whole. Session app never opens raw
keyboard nodes.

That objective is **implemented and physically evidenced**. The remaining
pain was closure-criteria inflation plus a packaging test that reopened G3.

### 2.1 Session ledger (01–27)

| Session | Phase | Result | What moved | What it did not |
|---|---|---|---|---|
| 01 | planning | PASS | M2 plan routable | G1/G3/G4 as later gates |
| 02–05 | implementation | PASS | S1–S4 engine, udev, watchdog, IPC | live G4 |
| 06 | acceptance | PASS | S4 IPC | physical G4 |
| 07 | acceptance | BLOCKED | — | G4; preconditions |
| 08 | implementation | PARTIAL | production ARM in repo | host install (no sudo TTY) |
| 09 | acceptance | BLOCKED | — | candidate vs HEAD; recovery path |
| 10 | acceptance | BLOCKED | — | same recovery blocker |
| 11 | implementation | PASS | late uinput ACL after seat uaccess | host udev install |
| 12 | impl / plan / impl | PASS | AP pin, cutoff, production safety | live grab |
| 13 | acceptance | BLOCKED | — | cb72ae0 not installed |
| 14 | deployment | PASS | inactive broker/unit install | ARM |
| 15 | acceptance | PARTIAL | cutoff without ARM | pass-through slice |
| 16 | acceptance | PASS | SSH, ARM, sample pass-through, cutoff, recovery | whole G4 |
| 17 | implementation | PASS | docs for slice 16 | G4 close |
| 18 | acceptance | PARTIAL | watchdog slice incomplete | held-modifier death |
| 19 | acceptance | PASS | armed watchdog + held modifier | whole G4 |
| 20 | implementation | PASS | sleep hook source | live suspend |
| 21 | deployment | PASS | hook installed | live suspend |
| 22 | acceptance | PASS | one live suspend/resume | hibernate/hybrid |
| 23 | acceptance | PASS | LED return; 18/18 host-remappable | firmware-only keys |
| 24 | acceptance | PASS | one non-G213 remapper mapping during grab | general remapper |
| 25 | deployment | PARTIAL | file/identity remove+rollback | G3 restore; candidate install |
| 26 | implementation | PASS | source acl-guard `ca6052e` | host |
| 27 | deployment slot | PASS owner path | host install of that rule + readbacks | independent R6; Worker 27 |

Completed Worker sessions before this file: 26. This slot is owner
recovery, not a 27th dispatched Worker.

Unique partition of 01–26: live G4 PASS = 5 (16, 19, 22, 23, 24);
build/deploy/docs PASS = 13; PARTIAL = 4 (08, 15, 18, 25); BLOCKED = 4
(07, 09, 10, 13).

### 2.2 Named live G4 slices you must not rerun

From `00_handout_02.md`, still valid, plus Session 24 which that handoff
had not yet recorded:

- **16** — SSH recovery before start/ARM; explicit ARM; sampled G213
  pass-through; matching-invocation cutoff death; post-death typing.
- **19** — GUI ARM; armed watchdog abort with held modifier; no automatic
  restart/re-grab; post-death typing.
- **22** — one real suspend/resume; sleep-hook stop before freeze; one
  conditional post-resume start, always disarmed; STATUS-only reconnect;
  post-resume typing.
- **23** — compositor LED return to physical if00; `led-write-failed`
  semantics; all eighteen measured host-remappable controls; explicit
  DISARM and inactive cleanup.
- **24** — one existing non-G213 input-remapper mapping kept its semantic
  result before/during/after one armed G213 pass-through; `if00=1/1`
  and `if01=1/1`.

Do not repeat 16, 19, 22, 23, or 24. Do not treat “not whole-G4” wording
in those reports as a command to invent another G4 slice.

Runtime for those slices remains broker `cb72ae0` installed inactive.
Later source commits `ab10491` (sleep hook) and `ca6052e` (acl-guard) did
not replace that broker binary.

### 2.3 Gates — paperwork versus product

| Gate | Product truth | Paperwork trap |
|---|---|---|
| G1 | Closed. 18 host-remappable; Game Mode and Backlight firmware-only; code 166 unresolved, not in catalog | Milestone table may still say P1 Planned |
| G2 | Closed IRL in M1 | none |
| G3 model | Guard/grant/uinput/acl-guard in tree | “G3 open” after every udev nuance |
| G3 on this host | Session-user event/port/i2c ACL absent after Session 27 owner deploy; hidraw+uinput preserved | Independent R6 re-audit unused; do not reopen |
| G4 named slices | 16, 19, 22, 23, 24 accepted | README/ROADMAP still say live suspend / LED / all-control open |
| G4 full | Not a useful remaining M2 program. Leftover is packaging/autostart (G8/M5), hibernate, general remapper | “full G4 open” as infinite Worker fuel |
| G6 license | Unresolved | do not copy GPL-3 G213Tray |
| G7 power actions | Separate; IRL pending from M1 | not an M2 grab gate |
| G8 autostart | Blocked by design until production packaging | cannot close inside M2 |

### 2.4 Why this whole stalled (anti-stall, RF-17 / RF-08)

After Session 23 the handoff already had the right order: remapper slice,
then packaging, then docs, then closure evaluation. Session 24 did the
remapper slice. Session 25 fused two incompatible claims:

- **Uninstall completeness:** after ContextDesk udev is gone, the host
  looks like distro+OpenRGB, which **has** session `uaccess` on event,
  port, and i2c. That is a successful uninstall.
- **Installed G3:** while ContextDesk rules are installed, the session
  user must **not** have those ACLs.

Session 25 proved file rollback and then triggered udev. Tags stayed off;
POSIX ACLs came back. That became finding G3-ACL-REPROBE-01. Session 26
published `setfacl -b` as a late rule. The issued Session 27 would have
been a lawful one-shot deploy+re-audit. A **second** remove/rollback after
that would delete the guard, trigger OpenRGB uaccess, and reopen the hole.
That is the loop. AP: an unchanged hypothesis, unchanged candidate, and
unchanged failing gate is not progress. Do not audit an audit.

“Smallest next step” lines in Worker reports are not a standing backlog.
Named-slice PASS reports that say `Logical-whole closure: not-closed` are
correct about closure; they are not a mandate to keep issuing Workers
until every deferred gate is perfect.

### 2.5 Daily usable M2 path (already proven)

For live grab trials, one independent recovery route is enough: SSH from
another device **or** a second physical keyboard, demonstrated before
start/ARM. Do not demand both.

Daily use, no autostart:

```text
systemctl start contextdeck-broker.service     # never enable
session app: explicit GUI ARM
type on the G213
GUI DISARM
systemctl stop contextdeck-broker.service
```

The unit has no `[Install]` section. No automatic ARM after crash, resume,
or client reconnect. That is the safety contract, not a missing feature of
M2.

Source notes that remain **out of M2 park** unless a later whole names
them: silent uinput write errors; virtual-device capabilities from
`passthroughCapabilities()` versus measured source bits; logind session
lookup vs user-manager-launched session apps. They are not G3.

---

## 3. Product vision the successor must not lose

ContextDeck is a Linux/KDE Plasma 6/Wayland cockpit for **one** keyboard,
the Logitech G213 Prodigy. Five RGB zones, not per-key. The Cooperator’s
stated end state is a total custom cockpit: keyboard behavior, session,
virtual desktops, and the rest of `ROADMAP.md`.

Plan of record (do not reorder without a Cooperator decision):

| # | Identity | Role toward the cockpit | Status after this report |
|---|---|---|---|
| M1 | `g213-contextdeck-mvp-context-lighting` | App context + five-zone lighting | Done, accepted IRL |
| M2 | `g213-contextdeck-input-passthrough-safety` | Safe exclusive grab + pass-through | **Park.** Named G4 evidenced. G3 host-mitigated. G8 not in this whole |
| M3 | `g213-contextdeck-workspace-aware-lighting` | Zones as slots; desktop + app lighting | **Next whole** |
| M4 | `g213-contextdeck-workspace-session-manager` | Virtual desktops, app assignment, launch, maximize | After M3 |
| remap | (not a current M2 leftover) | Per-app consume-and-inject on F-keys/media | After M2 grab + a dedicated whole |
| deck | `g213-contextdeck-deck-layer` | Hold-to-layer (e.g. Super) | After remap |
| M5 | `g213-contextdeck-system-integration-and-autostart` | Login autostart, packaging lifecycle | After M2 park + M4; G8 lives here |

Target experience already written in ROADMAP: foreground Cursor → keyboard
takes Cursor’s color and later Cursor’s key policy; switch to the browser
→ color and mappings follow; tray is quiet status, not a cockpit of
restarts.

M3 does **not** need the input broker. Subscribe
`org.kde.KWin.VirtualDesktopManager` from the session app. Five zones are
a hard ceiling. Schema 3 with migration from 2. No per-key RGB fiction.
No broker ARM in M3 unless the Cooperator later changes the objective.

M4 is the session/desktop orchestrator the Cooperator named in the closing
turn. It depends on M3, not on more G3 udev.

Remapping is what makes F5 send a chosen shortcut. It is **not** M2. M2
only had to prove the grab is survivable. That is done.

Do not open M5/autostart while chasing G3. Autostart of an unproven grab
was forbidden for M2; M5 is the place to decide it after the cockpit
exists.

---

## 4. Required successor artifacts

### 4.1 `00_handout_03.md` (this M2 directory)

Supersedes `00_handout_02.md`. Must contain:

- Honest closure answer: M2 **parked**, not production-closed, not G8.
- Named slices 16, 19, 22, 23, 24 and Session 27 owner G3 deploy.
- Identity honesty: Worker 27 was not dispatched; this report is the
  owner-path record.
- Product HEAD `ca6052e…`, AP `0cf2cff…`, broker runtime `cb72ae0…`,
  four udev SHA-256 values, unit/hook/broker hashes.
- Docs drift list: README/ROADMAP still deny Sessions 22–23; P1 row vs
  closed G1.
- Forbidden: another install/remove/rollback; another G3 re-audit; live
  grab merely to “finish G4”; autostart.
- Next identity: `g213-contextdeck-workspace-aware-lighting`.
- One recovery route is enough if a later whole grabs.
- Cooperator shell for owner commands: **fish**. No bash heredocs.

### 4.2 New META directory

```text
projects/contextdesk/00/03-g213-contextdeck-workspace-aware-lighting/
```

Open with Orchestrator-authored `00_notes.md` and `00_handout.md` for M3.
Do not reuse the M2 directory as the live whole. M2 history stays here.

### 4.3 Fresh ChatOrchestrator prompt

Genuinely fresh session. Read-only Orchestrator. Manual delivery.
Slovak with the Cooperator. English Worker prompts. No Task/subagent
dispatch. No host grab in the first M3 planning exchange.

The prompt must include the payload in §5 so the successor does not
depend on this chat. After public-ref verification, the successor’s
first Cooperator-facing reply (Slovak) must state: M2 parked; named
slices; G3 host-mitigated; next whole M3; no G3/G8 continuation.

First M3 Worker is a **Planner** (`Native planning mode: required`) for
`g213-contextdeck-workspace-aware-lighting` only. Aggressive MVP like M1:
something the Cooperator can see on the five zones, not a year of schema
theory. Then a separate implementation prompt. Do not plan M4/M5/remap
inside that Planner except as classified future wholes.

---

## 5. Payload to copy into the fresh ChatOrchestrator prompt

Use this as the body of the successor prompt. Fill any SHA that origin
disagrees with after `git fetch`; never retarget silently.

```text
You are a genuinely fresh, strictly read-only ChatOrchestrator for
ContextDesk / ContextDeck under the pinned AP in the product `.ap/`
gitlink. Access profile: ChatOrchestrator. Delivery: manual. You do not
dispatch subagents. You do not edit, commit, or push product or META.
You do not install, enable, start, stop, ARM, or grab on the Cooperator
host. You compose one complete Worker prompt at a time. The Cooperator
carries every prompt and report.

Cooperator-facing language: Slovak. Worker prompts and formal reports:
English. Cooperator owner-terminal commands: fish, not bash heredocs.

This is a NEW logical whole, not a continuation of M2 implementation.
Do not restore M2 as the selected whole. Do not issue Workers in
g213-contextdeck-input-passthrough-safety.

Parked predecessor:
- Identity: g213-contextdeck-input-passthrough-safety
- Trace: projects/contextdesk/00/02-g213-contextdeck-input-passthrough-safety/
- Park record to read after public clone: 00_handout_03.md (must exist
  before or with this prompt; if origin still has only 00_handout_02.md,
  also read 27_report_00.md and treat 00_handout_02.md as stale on
  “next is remapper/packaging inside M2”).
- Session 27 report tells the identity honesty: Worker 27 was not
  dispatched; G3 acl-guard was owner-installed.

New logical whole:
- Identity: g213-contextdeck-workspace-aware-lighting
- META directory to open:
  projects/contextdesk/00/03-g213-contextdeck-workspace-aware-lighting/
- First Worker: Planner, Native planning mode required, session 01
  exchange 01, no host mutation, no broker, no udev, no grab.

Product goal (horizon, not this whole’s scope): a total cockpit for this
one G213 — lighting, session, virtual desktops, later remapping, later
autostart — as already classified in product ROADMAP.md. This whole
delivers only workspace-aware lighting: five honest zones as slots
(desktop indicators + app color), schema 3 with migration from 2,
VirtualDesktopManager from the session app, IRL-visible on hardware.
M4 session manager, remapping, deck layer, and M5 autostart are future
wholes. Naming them in the plan is allowed. Implementing them is not.

Hard product invariants (unchanged):
- G213 only, USB 046d:c336, Linux/KDE Plasma 6/Wayland/systemd.
- Five zones, never per-key RGB.
- Game Mode and Backlight are firmware-only; never substitute PrintScreen
  or Pause.
- No keylogging; no raw events in reports.
- Session app does not open raw keyboard devices.
- Broker stays static/inactive; no autostart; no ARM in M3.
- input-remapper is not reconfigured.
- License unresolved; do not copy G213Tray.

Verify in inspection clones (not the Cooperator host):
- Product: https://github.com/cisarik/contextdesk  main
- Expected product HEAD at park (confirm origin): 
  ca6052e816d4884ddeac9d7499a42c2aca089e7e
- AP gitlink: 0cf2cff483a36a4cc2254aa424a7c53bd57a97e9
- ./.ap/ap doctor PASS
- META: https://github.com/cisarik/meta.git
- Required public files: M2 00_handout_02.md and/or 00_handout_03.md;
  23/24/25/26 prompt/report pairs; 27_deployment_00.md and 27_report_00.md
  once archived.

Read before routing: pinned AP.md + AP_ORCHESTRATOR.md +
PROMPT_CONTRACTS.md; product AGENTS.md, ROADMAP.md (M3 section),
docs/architecture.md, docs/hardware/g213-zone-map.md,
docs/hardware/g213-control-matrix.md. Treat README M2 sentences that
still say “live suspend not accepted” as drift.

Cooperator-owned decisions already taken:
- Stop the M2 G3/G8 audit loop.
- Park M2 with named G4 slices 16, 19, 22, 23, 24 and host-mitigated
  G3-ACL-REPROBE-01.
- Next whole is M3, not another M2 session.
- Residual risk of skipping independent R6 re-audit of the udev guard
  is accepted for this park.

Forbidden first moves:
- Dispatching the old 27_deployment_00.md Worker.
- udev, setfacl, uninstall, broker start, ARM, grab.
- Planning M4/M5/remap as the selected whole.
- Requiring both SSH and a second keyboard.
- Recommending client maximum reasoning merely because it exists.

First Slovak reply to the Cooperator must state: verified product/AP/META
identities; M2 parked; five named live slices; G3 host-mitigated not
independently re-audited; next is M3 Planner; cockpit horizon preserved;
no autostart claim.

Then open the M3 trace directory (Cooperator persists notes/handout) and
issue one complete Planner prompt for M3 only.
```

---

## 6. Smallest next step

Current ChatOrchestrator: write `00_handout_03.md` and the fresh
ChatOrchestrator prompt for whole
`g213-contextdeck-workspace-aware-lighting`, using §4–§5. Stop M2 Worker
issuance.

Cooperator: archive `27_deployment_00.md` + this report together when
publishing META. Do not commit `28_canvas.md` as a Worker exchange. Do
not start Worker 27. Do not start the broker as part of this park.

Fresh ChatOrchestrator: M3 Planner. Not G3.

```text
Smallest next step: park M2 in 00_handout_03.md and issue a fresh
ChatOrchestrator prompt whose selected whole is
g213-contextdeck-workspace-aware-lighting.
```

---

## 7. Orchestration critique

```text
Orchestration critique:
MEASURED: owner-installed 99-contextdeck-input-acl-guard.rules from ca6052e
is byte-identical at the system udev path, mode 644 root:root; targeted
reload/trigger completed; session-user immediate and later readbacks show
event 0/2, port no, i2c 0/8, hidraw 2/2, uinput session+broker present;
broker inactive. Evidence: W-G3-PUT-DONE; W-G3-APPLY-DONE; WHO= session
user not root; SHA-256 da87276d… match. Effect: G3-ACL-REPROBE-01
host-mitigated; M2 has no remaining G3 deploy gate that justifies another
Worker. Smallest correction: do not dispatch 27_deployment_00.md; do not
rerun Session 25 uninstall as G3.
LEAD: README/ROADMAP drift will make a naive successor re-open G4; the
M3 handoff must state Sessions 22–24 as accepted named slices. Cheapest
check: successor reads 27_report_00.md and 00_handout_03.md before
ROADMAP status tables. Fish-first owner commands prevent the bash-heredoc
silence that delayed Session 27.
```

---

## 8. Claims this report does not make

- Whole M2 or whole G4 closed.
- Production readiness, independent install/remove/rollback, autostart.
- Hibernate or hybrid-sleep support.
- General input-remapper coexistence.
- Independent INFOSEC `verified-closed` for G3-ACL-REPROBE-01.
- That source commit `ca6052e` alone fixed the host (the file had to be
  installed).
- That Worker 27 ran.
- That M3 is already authorized to implement.

```text
Logical-whole closure: not-closed
```

Authority for this record expires at this report. The issued Worker 27
grant in `27_deployment_00.md` was never activated and must not be
activated after this park.

```text
Cooperator delivery / trace destination: configured
Delivery mode: Cooperator-requested file persistence of this report
Downloadable prompt filename: 27_deployment_00.md
Prompt authorship: ORCHESTRATOR (issued; Worker not dispatched)
Prompt persistence: file already present; archive with this report
Report filename: 27_report_00.md
Report persistence owner: ORCHESTRATOR under explicit COOPERATOR grant
Git publication owner: COOPERATOR
Archival: wait-for-this-report; first-add pair with 27_deployment_00.md
```
