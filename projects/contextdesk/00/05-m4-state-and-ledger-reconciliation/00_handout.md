# Fresh full Orchestrator handoff — ContextDesk / ContextDeck after M4 code acceptance

You are a genuinely fresh, full-capability ORCHESTRATOR for ContextDesk (the
application is also called ContextDeck). This is the continuity record from the
successor full-Orchestrator session that closed the M4 code-acceptance gates for
Slice A and Slice B. It is not a Worker prompt, not a plan, not an acceptance
report, and not authority to claim closure. Reconcile it against the current
public repositories and the pinned Analytic Programming (AP) protocol before
taking action.

Handoff artifact filename: `11_handout.md`
META destination:
`projects/contextdesk/00/04-g213-contextdeck-workspace-session-manager/11_handout.md`
Handoff authorship: ORCHESTRATOR
Handoff persistence/archive owner: COOPERATOR or the successor full ORCHESTRATOR
only after exact-byte verification
Paired orchestration notes: `00_notes.md` in the same directory
Continue only through separately bounded logical wholes.

## 1. Successor capability profile and access

Successor capability profile: `Orchestrator` (full project orchestration).
Access profile: full, not read-only ChatOrchestrator.
Primary project: `https://github.com/cisarik/contextdesk`
Evidence archive: `https://github.com/cisarik/meta.git`

The successor is expected, when its actual execution environment exposes those
capabilities, to run repository commands and scripts, inspect source and tests,
read and write the ContextDesk product repository, read and write the META
repository, create commits, push canonical branches, and verify public refs.
Capability must be directly observed in the new session; this text never turns
an unavailable tool or credential into authority.

Repository write authority is project-scoped and AP-governed. Use disposable
inspection clones for public verification and a deliberate clean product/META
worktree for authorized writes. Preserve unrelated owner work. Never use broad
destructive Git recovery or silently retarget a changed public baseline.

The COOPERATOR (Michal) remains the human owner of material objective changes,
cost, privacy, irreversibility, physical behavior, host-side action, and
residual risk. Do not infer host or device authority from repository write
authority. Host commands, service changes, input-device access, desktop-manager
mutation, application launch, bridge reload, physical keyboard tests,
credentials, and external-account actions each require their own explicit
bounded COOPERATOR decision, recovery plan, and AP surface gate.

**COOPERATOR direction carried forward (2026-09-14):**

- Manual testing plus brainstorming plus UI/UX polish are folded into future
  programming wholes, not run as a separate manual step.
- An image-capable Orchestrator/Worker is preferred for later visual/UX work.
- M3 physical five-zone testing stays deferred; M3 remains not-closed.
- The M4 live IRL run is **deferred** by explicit COOPERATOR decision; M4 remains
  not-closed. It needs its own mutation-class authority, a named checkpoint, and
  one demonstrated recovery route when it is eventually run.

## 2. Language and routing contract

- Speak **Slovak** with Michal/COOPERATOR.
- Write every Worker prompt and every formal Worker report in **English**.
- Keep each task one complete bounded prompt, one fresh or explicitly renewed
  Worker session, one terminal report, and one reconciliation.
- Manual delivery is preserved: the COOPERATOR carries every prompt and report.
  Do not spawn, auto-dispatch, parallelize, or silently route Workers.
- Prepare the complete prompt, write it to the exact META trace path, tell the
  COOPERATOR the exact file and routing state, and let the COOPERATOR manually
  carry it to the chosen fresh Worker. A Worker prompt must tell that Worker to
  write its terminal report into the exact META trace path and read it back
  completely.
- Default topology is one active Worker. A parallel or internal-audit exception
  needs an explicit bounded COOPERATOR decision, disjoint ownership, and AP
  evidence; it never creates independent acceptance automatically.

## 3. Verified public identity snapshot at handoff

These values were directly checked against canonical public refs immediately
before this handoff. The successor must re-check them in its own environment.

| Item | Verified value |
|---|---|
| Product remote / branch | `https://github.com/cisarik/contextdesk` / `main` |
| Product current public HEAD | `293158887e4228a42b9c64bda7d4f0bd32fb4ec9` (docs-only status reconciliation) |
| Product parent | `db9ddc1f923f44e26f7f1d58df7446306bbdf64a` |
| M4 Slice A accepted code candidate | `aca6c68542bbc9f1b8ee891415a04b0a95e8372e` |
| M4 Slice B accepted code candidate | `db9ddc1f923f44e26f7f1d58df7446306bbdf64a` |
| Product AP gitlink and checkout | `0cf2cff483a36a4cc2254aa424a7c53bd57a97e9` |
| Public AP `main` (not adopted) | `717ecb6cfb7c71eda12ff4e3e0b02f28101c59bf` |
| META remote / branch | `https://github.com/cisarik/meta.git` / `main` |
| META current public HEAD | `cfd5cee1298b76ad21afd685de9565c8191d6f7b` |
| M4 trace | `projects/contextdesk/00/04-g213-contextdeck-workspace-session-manager/` |
| M3 trace | `projects/contextdesk/00/03-g213-contextdeck-workspace-aware-lighting/` |
| M2 trace | `projects/contextdesk/00/02-g213-contextdeck-input-passthrough-safety/` |
| M1 trace | `projects/contextdesk/00/01-g213-contextdeck-mvp-context-lighting/` |

AP `main` has advanced beyond the pinned gitlink. That is expected for a
consumer pin and is **not** an adopted update. Do not move the gitlink.

Key M4 artifact SHA-256 values (verify against the actual files):

| Artifact | SHA-256 |
|---|---|
| `01_report_00.md` (accepted plan) | `44f01b82155a6ad584f8d1f0d5851e7020dbf4b6b1d330b1862ec06c05e34fdc` |
| `01_report_01.md` (plan completion) | `4a968b7694f5c8294c8674351827511f826a903db9a96aae9e83670360239f94` |
| `02_report_00.md` (Slice A implementation) | `7570ee7999e684bb037fed63c7a2edc79f15fde1f2afe3a44ae1f2d69d7f8ce6` |
| `03_report_00.md` (Slice A PARTIAL) | `8dce3dc3f98848c230c75b9e9f004430be78253b1c60aea60de01e043ba0c3d3` |
| `05_report_00.md` (Slice A acceptance-PASS) | `deaa4c55e58bc0ff3e136d7e1b3c92e79f7a63b9bc3547df6fc00d3e0f841458` |
| `06_report_00.md` (Slice B preflight PASS) | `fecceb7715ea4b07e7b0873f4a84d8cc66633584ddefd9086f87d96687758cbd` |
| `07_report_00.md` (Slice B implementation) | `910ff7432bea944cce7473e30f2ed66c3fabc48b6cca9dc3d9692fd6d0cc8c93` |
| `08_report_00.md` (Slice B PARTIAL) | `010fc3e4ba1c864453b2ee0125de2c4946640e08426ba941fb0794d09a098521` |
| `09_report_00.md` (Slice B correction) | `1f61475821f9603bc3f4ca40c516c6a843819a159025d4b2a49679e402168b8b` |
| `10_report_00.md` (Slice B full re-acceptance PASS) | `43c9f286f4678aa52b0b36b61091d48da78308e45edd0edd160ba4eb59198e13` |

M4 pair commits are add-only: `6f14316` (01 pair), `c7e1b73` (completion),
`640b65d` (02), `06439c9` (03), `f64c634` (04), `977c841` (05), `6c1d6c6` (06),
`0475e01` (07), `b25718c` (08), `cfd5cee` (09). The `10_acceptance_00.md` and
`10_report_00.md` pair is prepared and staged in the COOPERATOR-owned META
worktree but **not yet committed**; the successor must verify or complete that
first-add before relying on a public `10` pair.

## 4. Current truthful state

### 4.1 M4 workspace session manager — Slice A and Slice B code-accepted, live IRL deferred

- **Slice A** (schema 4 named sessions/assignments, `rows`/wrapping observation,
  pure dry-run `WorkspacePlan`, `Plochy` editor) was implemented (`c7c8eb9`),
  independently returned `PARTIAL` for a missing diagnostics-privacy regression
  (`03_report_00.md`), corrected (`aca6c68`), and code-accepted
  (`05_report_00.md`).
- **Slice B** (fail-closed `DesktopMutator` with user-local checkpoint/revert,
  typed `ApplicationLauncher`, `PlacementResolver` + bridge `PlacementHint`,
  opt-in non-logging title fallback) was preflighted PASS (`06_report_00.md`),
  implemented (`5087277`), independently returned `PARTIAL` for a non-fail-closed
  `current` step and two documentation/path mismatches (`08_report_00.md`),
  corrected (`db9ddc1`), and code-accepted by a full-fresh re-acceptance
  (`10_report_00.md`).
- The product status reconciliation commit `2931588` records this in
  `README.md`/`ROADMAP.md`.

Preserve this wording wherever M4 state is restated:

> M4 workspace session manager is code-accepted (Slice A on `aca6c68`, Slice B on
> `db9ddc1`); its live IRL run is deferred by explicit COOPERATOR decision. M4 is
> not closed, and code acceptance is not live or physical acceptance.

### 4.2 M3 workspace-aware lighting — code-accepted, physical observation deferred

> M3 workspace-aware lighting is code-accepted on `502ae75...`; its physical
> five-zone IRL observation is deferred by explicit COOPERATOR decision. M3 is
> not closed, and code acceptance is not physical acceptance.

### 4.3 M2 input passthrough safety — parked

> The named live G4 slices are accepted, but the M2 logical whole remains open.
> M2 is parked with G3 host-mitigated on the authorized reference host; the
> next bounded whole is M4 workspace session manager.

Accepted named live G4 slices (do not rerun; do not reopen): Session 16, 19, 22,
23, 24 as recorded in the M2 trace. `G3-ACL-REPROBE-01` is host-mitigated but was
not independently re-audited. Do not claim whole M2, whole G4, independent G3
closure, install/remove/rollback readiness, production readiness, autostart
safety, hibernate/hybrid-sleep support, or general coexistence.

### 4.4 M1 context lighting — accepted IRL (historical)

M1 is recorded as COOPERATOR-accepted IRL for five-zone lighting and context.
That is historical evidence, not a later-session retest; it does not close G7 or
any other unrelated gate.

## 5. Product vision and hard invariants

ContextDesk is a Linux/KDE Plasma 6/Wayland cockpit for one Logitech G213
Prodigy. Preserve the product vision and these invariants:

- G213 only, USB `046d:c336`; no other keyboards, no generic remapper, no
  Windows/macOS. Target CachyOS/Arch, Plasma 6, KWin, Wayland, systemd,
  C++20/Qt6/KF6.
- Exactly five RGB zones, not per-key RGB. Never design or show per-key color as
  a real capability. Zone order: Left Area, Middle Area, Right Area, Arrow and
  Homekeys, Numpad.
- Foreground context is event-driven via the KWin scripting API — no
  `xdotool`/`wmctrl`/`xprop`, no title polling. Prefer KWin
  `desktopFileName`/`resourceClass`/`resourceName` identity.
- The session app never opens raw keyboard event nodes; only the input broker
  does, and it starts disabled. No automatic ARM after restart, resume, crash, or
  reconnect.
- One persistent OpenRGB SDK connection on loopback (protocol 5); one RGB
  backend at a time.
- Typed action/launch model only: no arbitrary shell strings. Fail-safe default
  is pass-through; unset means inherit.
- G213 controls in scope: F1–F12, Previous, Play/Pause, Next, Mute, Volume
  Down, Volume Up, Game Mode, Backlight. Game Mode and Backlight are
  firmware-only; never substitute PrintScreen or Pause.
- Never log ordinary typed keystrokes, raw event lines, key names, scan values,
  typed content, serials, host addresses, host keys, passwords, private paths,
  desktop IDs/names, or window captions in code logs, prompts, reports, or META.
- Suspend goes through logind/systemd policy, never `/sys/power/state`.
  Display-off must not alter KScreen topology.
- input-remapper is not inspected or reconfigured.
- `kwinrulesrc` is never written by M4; no autostart until the M5 gates pass.
- Licensing is unresolved (root `LICENSE` is MIT but the COOPERATOR has not
  selected the license; gate G6). Never copy external code (G213Tray is
  GPL-3.0-or-later) before an explicit compatible decision.

## 6. Candidate next wholes and open COOPERATOR decisions

The M4 code is accepted but M4 is not closed. The successor must present the
restored state and obtain the COOPERATOR decision on exactly one next whole.
Candidate horizons (none is authority):

1. **M4 live IRL** (deferred): the `docs/testing-m4.md` run. COOPERATOR-owned
   host mutation and launch; needs explicit mutation-class authority, a named
   checkpoint, and one demonstrated recovery route.
2. **M3 physical five-zone IRL** (`docs/testing-m3.md`), still deferred.
3. **M2 remaining G4 / independent G3 re-audit** — separate, requires its own
   authority and recovery posture.
4. **M5 `g213-contextdeck-system-integration-and-autostart`** — blocked until
   M2, M4, G6, and G8 prerequisites pass.
5. **Super-key deck layer** — future whole after M3, recorded in `ROADMAP.md`.
6. **Ledger/documentation polish** — the carried M4 ledger candidates below.
7. **G6 licensing decision** — blocks release.

M4 non-blocking ledger candidates carried for later work: ADR 0002/0004 status
phrasing; the `.desktop` id predicate accepting trivially short forms;
`KApplicationTrader` named only in the specification; the late-`desktopCreated`
checkpoint-disclosure gap; QML runtime validated only at build time; and a
possible orphaned old-path checkpoint from the never-accepted parent candidate.

## 7. AP bootstrap is mandatory

Before the first routing decision, read the consumer root `AGENTS.md` and the AP
documents named by its managed block, at minimum:

- `.ap/AP.md`;
- `.ap/AP_ORCHESTRATOR.md`;
- `.ap/PROMPT_CONTRACTS.md`;
- `.ap/AP_WORKER.md`;
- advisory `.ap/INFOSEC.md`.

Apply AP Continuation Bootstrap: restore and reconcile read-only from AP,
current canonical repositories, direct public refs, accepted durable records,
then historical trace; classify verified facts, Worker observations,
COOPERATOR decisions, proposals, inferences, and missing evidence separately;
select exactly one bounded logical whole (or preserve an already selected
whole); and issue one complete prompt with its own fresh/current target,
coordinates, capability route, evidence tier, authority, allowlist,
persistence, and stop conditions.

Verify the AP tuple and run `./.ap/ap doctor` (expected PASS, variant `stable`).
The `.ap` submodule is a pinned detached checkout equal to the containing repo
gitlink; do not attach or update it.

## 8. M4 trace and documentation owners

M4 trace directory:
`projects/contextdesk/00/04-g213-contextdeck-workspace-session-manager/`.

META local naming contract (from `meta/README.md`):
`<worker-session>_<phase>_<meta-exchange-index>.md` and
`<worker-session>_report_<meta-exchange-index>.md`, where
`meta_exchange_index = Worker exchange ordinal - 1`. Opening artifact is
`00_notes.md`; this handoff is `11_handout.md`; later closures use their own
sequence, one higher than the largest used.

Durable documentation owners in the product repo:

- `README.md` = front door;
- `ROADMAP.md` = single human plan of record (updated by the ORCHESTRATOR after
  each reconciliation);
- `docs/specification.md` = behavior/terminology;
- `docs/architecture.md` = accepted architecture and process;
- `docs/adr/` = formal decisions;
- `docs/operations.md` = exact host commands;
- `docs/testing.md`, `docs/testing-m2.md`, `docs/testing-m3.md`,
  `docs/testing-m4.md` = IRL procedures;
- hardware evidence under `docs/hardware/`.

Registered tests are owned by `CMakeLists.txt` (`add_test` names); the current
suite is 21 tests (including `test_workspace_mutator`,
`test_placement_resolver`, `test_application_launcher`). No lint config and no CI
exist.

## 9. Mandatory first routing rule for a newly selected whole

For the first newly generated Worker prompt of a newly selected whole, use an
**initial Planner** with `fresh-worker-session`, `Native planning mode:
required`, manual COOPERATOR delivery, and a complete English document:
exact candidate/baseline; public-safe path allowlist; mandatory reading; one
measurable planning outcome; no-subagent posture; report filename/destination;
complete META readback; explicit out-of-scope claims; and terminal authority
expiry. Preserve an already selected/planned whole instead of replanning it.

The Planner's terminal report expires planning authority. Implementation
receives a separate complete prompt with `Native planning mode: not-used` and
explicit implementation authority after the plan is accepted.

## 10. Repository and META operating rules

At each bootstrap use fresh container-local clones and canonical HTTPS remotes.
Verify public refs directly with `git ls-remote`; a local `origin/main` is not
proof. If cache/DNS/HTTPS fails, record the bounded symptom, use a fresh clone,
check container-local name resolution, and retry with
`git -c http.version=HTTP/1.1`. Never use a stale clone, mirror, host
credential, or silent alternate commit.

For every new Worker exchange: select the whole and coordinates; write the
complete English prompt to its exact META path before delivery; require the
Worker to persist/verify and read back the prompt; require the Worker to write
the complete English terminal report directly to the paired META path and read
it back; have the COOPERATOR manually deliver the prompt to one Worker;
reconcile the report as a claim; and commit/push the exact prompt/report pair to
META only after the report exists, preserving earlier trace files. The COOPERATOR
owns META add/commit/push in this ChatOrchestrator workflow unless a later
explicit grant moves it.

Do not create `NEXT_WORKER.md`, a session diary, an automated dispatcher, or a
speculative report. A handoff is not a Worker exchange and does not consume a
Worker session ordinal. In each trace, exchange `01` is stored with suffix `_00`.

## 11. Worker prompt contract for the successor

Every prompt must be a complete English document containing, exactly once at the
opening, the persistent role, logical-whole identity, Worker session and exchange
ordinals, session target, phase, Worker profile, reasoning recommendation,
evidence tier, and Native planning mode. It must state exact immutable
product/AP/META candidate and public verification gates; one measurable outcome;
positive authority and exact path allowlist; explicit out-of-scope claims;
mandatory reading and fail-closed preflight; host/device/secret/subagent
restrictions; private-safe logging/reporting rules; exact prompt/report META
destinations, persistence owner, readback, and archival owner; and the complete
terminal report header, result vocabulary, evidence, next step, critique,
closure line, and authority-expiry sentence.

Planner prompts use Native Plan Mode `required` and never implement.
Implementation prompts use `not-used`, receive the reconciled plan, mutate only
their exact allowlist, publish one candidate, and never self-accept. Acceptance
prompts are fresh and read-only against an immutable candidate. Correctors
receive one smallest concrete finding and are followed by the required scoped or
full-fresh re-acceptance.

## 12. No-silent-claims rule

Never claim any of the following without the separately required evidence:

- M4 live desktop mutation, launch, placement/maximize, title fallback, or M4
  closure;
- M3 physical five-zone visibility, device-default restoration, or M3 closure;
- M2 or whole-G4 closure; independent G3 closure;
- production readiness, deployment, installation, rollback, autostart,
  hibernate, hybrid sleep, or system integration;
- general input-remapper coexistence;
- M5 integration, the deck layer, per-key RGB, or measured control-to-zone
  placement;
- any fresh rerun of the historical Session 27 deployment.

Keep implementation-PASS, acceptance-PASS, production acceptance, and
logical-whole closure as separate facts. Worker reports always use
`Logical-whole closure: not-closed`; only the Orchestrator can close after all
AP, COOPERATOR, evidence, residual-risk, and active-mutation conditions are
satisfied.

## 13. Open COOPERATOR decisions for the successor to obtain early

1. Which bounded whole is next (M4 live IRL, M3 physical, M2/G4, M5, deck layer,
   ledger/documentation, or G6 licensing).
2. If M4 live IRL: the explicit mutation-class authority, the named checkpoint,
   the recovery route, and the timing.
3. Whether to complete the pending META `10` pair first-add.
4. Confirm M3 physical testing remains deferred or schedule it.
5. Whether G6 licensing is to be resolved before further release-oriented work.

## 14. Immediate successor checklist

1. Verify actual client, model, reasoning, permission, filesystem, network,
   command, Git, and write capabilities without probing credentials.
2. Read AP, root `AGENTS.md`, current ContextDesk source/docs, and the full
   M2/M3/M4 public trace.
3. Fresh-clone and verify product `2931588…` (or a later verified descendant),
   AP `0cf2cff…`, and META `cfd5cee…` (or later) against direct public refs; run
   `./.ap/ap doctor`.
4. Record truthfully: M4 Slice A and Slice B are code-accepted but live IRL is
   deferred and M4 is not closed; M3 is code-accepted with physical deferred;
   M2 is parked; complete the pending META `10` pair first-add if needed.
5. Present the restored state in Slovak with one evidence-backed
   recommendation, and obtain the section 13 COOPERATOR decisions.
6. For the selected whole, generate the initial Planner prompt (section 9) as
   Worker session `01` / exchange `01`, write it into the correct META trace, and
   leave manual delivery to the COOPERATOR.
7. Continue one bounded ContextDesk whole at a time, with all prompts and reports
   durably archived so Michal never has to copy/paste reports.

This handoff ends here. It grants no automatic Worker dispatch, physical host
operation, live desktop mutation, or logical-whole closure. All subsequent
authority must be issued by the fresh full Orchestrator under the current AP and
explicit COOPERATOR governance.

## 15. Closure sentence

Use this wording until a later whole has its own accepted evidence:

> M4 workspace session manager is code-accepted (Slice A on `aca6c68`, Slice B
> on `db9ddc1`) with its live IRL run deferred; M3 is code-accepted with physical
> observation deferred; M2 remains parked. No M4 or M3 logical whole is closed.
> The successor whole begins with a fresh Planner selected with the COOPERATOR.
