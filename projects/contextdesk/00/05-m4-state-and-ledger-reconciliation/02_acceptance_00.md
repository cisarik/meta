Logical whole identity: m4-state-and-ledger-reconciliation
Worker session ordinal: 02
Worker exchange ordinal: 01
Persistent role identity: WORKER
Worker session target: fresh-worker-session
Worker session profile: Fresh Independent Acceptance Worker
Phase: acceptance
Task identity: CONTEXTDECK-M4-STATE-LEDGER-RECONCILIATION-ACCEPTANCE
Native planning mode: not-used
Delivery route: manual COOPERATOR delivery to a genuinely fresh independent Worker session
Reasoning recommendation: High
Reasoning basis: this acceptance is the sole independent gate for durable
public state text across fourteen files plus a semantic validation predicate;
an incorrect PASS would be carried into acceptance and closure without another
audit, so the claim-preservation and causality checks need named-risk care.
Recommended context capacity: approximately 250k tokens; advisory only
Evidence tier: E2
Internal delegation: prohibited

# ContextDeck — fresh independent acceptance of the M4 state & ledger reconciliation

You are a genuinely fresh independent WORKER. You did not plan, implement,
repair, accept, or report any part of this whole and did not participate in
M1–M4. You receive only this complete prompt. Native Plan Mode must be OFF. Do
not use subagents.

The whole reconciled the product repository's durable state documentation with
the already-accepted M1–M4 code state and disposed of six carried M4 ledger
candidates. The implementation Worker applied the ORCHESTRATOR-accepted plan
plus two ORCHESTRATOR-authorized widened edits and one supplied tense text, and
published candidate `235d467c752958694dad4be7bcc31e66406dbdcc`. Because the
candidate changes a semantic validation predicate, AP requires a fresh
independent acceptance, not a scoped re-check.

Perform one read-only independent acceptance of the immutable public candidate.
Treat the plan and the implementation report as claims. Inspect, test, and probe
independently, persist one terminal report, and stop. You have no correction,
product publication, host, desktop, launch, or device authority.

## Acceptance and Correction Record

Acceptance candidate: `235d467c752958694dad4be7bcc31e66406dbdcc`
Acceptance owner map: ORCHESTRATOR-accepted plan `01_report_00.md` (SHA-256
`8b282335b703db2a6133bce97d26e89481fe0b5f45b196e339ce8ee3d3d33cf0`);
ORCHESTRATOR implementation grant `01_implementation_01.md` (SHA-256
`bbb8b0d4bccbf557e8e6733351c00f0f5a15ca044eaf8cccdc76de8eb2deb004`) with its
envelope decisions (two widened paths, supplied `docs/testing-m4.md` tense text,
access-profile edits excluded, one normal product push); implementation report
`01_report_01.md` (SHA-256
`ec0b38a1ac23ee969ebb3f35d706c08525e2804a5b1137cf8158c3ee16386595`)
Acceptance allowlist: read-only inspection of the exact candidate, the 14
changed product paths between `2931588...` and `235d467...`, directly referenced
unchanged owner paths, the pinned AP, and the public META continuity records
named below
Acceptance risk claims: (1) the strengthened typed desktop-id predicate rejects
the identified pathological forms while accepting every well-formed id used by
accepted schema-4 documents, with causal non-vacuous regressions; (2) the
durable state text now records the accepted M1–M4 state and standing deferrals
with no widened claim and no remaining contradiction inside the plan's scope;
(3) the six carried ledger candidates are disposed in `ROADMAP.md`; (4) the
access-profile lines are byte-identical to the baseline; (5) no out-of-scope
product change, dependency, or hidden scope expansion exists; (6) the focused
and full registered suites pass from a detached candidate
Acceptance control matrix: A1 through A8 below
Acceptance independence: required-fresh-independent
Primary fresh acceptances used: 1 (this session)
Automatic corrections used: 0
Correction re-acceptance: not-applicable
Named missing-evidence probe: none at issuance; classify any discovered missing
evidence in the report
Out-of-scope observations: ledger-candidates only

Implementation authority: none
Product mutation allowlist: empty
META Git mutation authority: none
Temporary probe authority: bounded as specified below
Independence required: yes

## Immutable public identities

Canonical product: `https://github.com/cisarik/contextdesk`
Required public branch: `main`
Candidate and required public `main`:
`235d467c752958694dad4be7bcc31e66406dbdcc`
Required candidate parent and reconciliation baseline:
`293158887e4228a42b9c64bda7d4f0bd32fb4ec9`
Required AP gitlink and checkout:
`0cf2cff483a36a4cc2254aa424a7c53bd57a97e9`

Canonical META: `https://github.com/cisarik/meta.git`
Required public META baseline:
`77dff2ade3d1a1c4522c9f7a0d5b21f98f3573d8` (a later verified descendant is
acceptable only after ancestry and changed-path review proves no artifact of
this whole changed)
Required trace destination:
`projects/contextdesk/00/05-m4-state-and-ledger-reconciliation/`

Required accepted-plan SHA-256:
`8b282335b703db2a6133bce97d26e89481fe0b5f45b196e339ce8ee3d3d33cf0`
Required implementation prompt SHA-256:
`bbb8b0d4bccbf557e8e6733351c00f0f5a15ca044eaf8cccdc76de8eb2deb004`
Required implementation report SHA-256:
`ec0b38a1ac23ee969ebb3f35d706c08525e2804a5b1137cf8158c3ee16386595`

No alternate commit, mirror, stale remote-tracking ref, or local retained clone
may substitute for these identities. If public product `main` has moved, stop
before testing and report the exact discrepancy; do not silently retarget.

## Read-only preflight

Use fresh disposable inspection clones in the Worker container, never a
COOPERATOR checkout. Before substantive acceptance:

1. Verify this is a genuinely fresh session with no implementation ancestry,
   Native Plan Mode OFF, exact `02/01` coordinates, and no subagents.
2. Clone the canonical product with submodules and META over HTTPS. Fetch `main`,
   compare direct `git ls-remote` with the required public tips, and detach at
   the exact required commits.
3. If transport or cache state is stale, discard only the Worker-owned clone and
   retry the canonical HTTPS remote in a new disposable location; a safe retry
   may use `git -c http.version=HTTP/1.1`. Do not change host DNS or Git config
   and do not use credentials or a mirror.
4. Verify clean worktrees, candidate parent, candidate subject, exact AP gitlink
   and checkout, and `./.ap/ap doctor` PASS (variant `stable`).
5. Verify META ancestry, the required hashes above with complete readback, the
   `00_handout.md` opening record (SHA-256
   `3ee8ac42f93adc1ae6788b39a29573f3b56bf892b37ed2be4d47d1575094e65f`), and
   that no earlier artifact of this whole was altered.
   Archival observation to record factually, not as a candidate defect: the
   implementation prompt was first-added as `f36bf9e` before the report existed
   and the report was first-added separately as `77dff2a`; both files are
   byte-correct and the split first-add is a COOPERATOR-owned archival
   deviation already recorded by the ORCHESTRATOR.
6. Verify the candidate changed exactly the 14 paths listed below, with the
   expected diffstat `14 files changed, +227/-128`, and that no `.ap`, broker,
   packaging, UI, bridge, CMake, or dependency path changed.
7. Verify existing build tools only. Install nothing.

Use detached read-only source inspection. Put all build products outside the
product checkout in a newly created Worker-owned temporary directory.

## Mandatory reading

- `.ap/AP.md`: RF-03, RF-05, RF-06, RF-07, RF-12, RF-16, RF-18, RF-19,
  acceptance/correction and escalation, phase-qualified results, stopping;
- `.ap/AP_WORKER.md`;
- `.ap/PROMPT_CONTRACTS.md`: Worker Report Header, Acceptance and Correction
  Record, Phase Result and Closure Record, exchange/trace, report expiry;
- `.ap/INFOSEC.md` only as advisory defensive review guidance;
- the accepted plan `01_report_00.md` (all of §3) and the implementation report
  `01_report_01.md`;
- product `AGENTS.md`, `README.md`, `ROADMAP.md`, `docs/specification.md`,
  `docs/architecture.md`, `docs/operations.md`, `docs/testing-m4.md`,
  `docs/testing-m3.md`, `docs/adr/0001-*` through `docs/adr/0004-*`, and
  `docs/adr/README.md`;
- the complete candidate diff and every changed product file, especially
  `src/core/Types.h` and `tests/unit/test_application_launcher.cpp`;
- unchanged adjacent owners as needed: `src/workspace/ApplicationLauncher.{h,cpp}`,
  `src/core/Persistence.{h,cpp}`, `src/workspace/WorkspacePlan.{h,cpp}`,
  `CMakeLists.txt`;
- the M2 record `27_report_00.md` §2.3 and its pre-existing-classification
  paragraph, for the G4-remainder drift the sweep corrected.

## Continuity and product invariants

Use this exact wording wherever M4 state is restated:

> M4 workspace session manager is code-accepted (Slice A on `aca6c68`, Slice B on
> `db9ddc1`); its live IRL run is deferred by explicit COOPERATOR decision. M4 is
> not closed, and code acceptance is not live or physical acceptance.

Use this exact wording wherever M3 state is restated:

> M3 workspace-aware lighting is code-accepted on `502ae75...`; its physical
> five-zone IRL observation is deferred by explicit COOPERATOR decision. M3 is
> not closed, and code acceptance is not physical acceptance.

Preserve the substantive M2 park claims (named live G4 slices accepted, M2 whole
open, G3 host-mitigated, full G4 open). Preserve these limits while reviewing:

- G213 only, USB `046d:c336`; five RGB zones, never per-key.
- Session app never opens raw keyboard nodes; broker stays static/inactive.
- Event-driven KWin identity; no `xdotool`/`wmctrl`/`xprop`; no title polling.
- Captions, titles, desktop names, and desktop UUIDs are sensitive: never logged
  or persisted in diagnostics, prompts, reports, or META.
- `kwinrulesrc` is never written; no autostart; no M5 scope.

This acceptance does not cover M2/G4/G3 closure, M3 closure, M4 closure, physical
or live desktop/launch/placement behavior, deployment, autostart,
hibernate/hybrid sleep, general input-remapper coexistence, remapping, deck, M5,
per-key RGB, or license selection.

## Cumulative candidate changed paths

The candidate `2931588...` → `235d467...` must change only these 14 paths:

```text
README.md
AGENTS.md
ROADMAP.md
docs/specification.md
docs/architecture.md
docs/operations.md
docs/testing-m4.md
docs/testing-m3.md
docs/adr/0001-broker-suspend-resume-sleep-hook.md
docs/adr/0002-host-desktop-mutation-authority.md
docs/adr/0003-workspace-assignment-schema.md
docs/adr/0004-typed-application-launch.md
src/core/Types.h
tests/unit/test_application_launcher.cpp
```

This is an inspection scope, not mutation authority.

## Measurable outcome and acceptance matrix

Return one independent verdict on whether the exact public candidate satisfies
the accepted plan and its ORCHESTRATOR envelope, with no widened claim and no
hidden scope. Mark every row `PASS`, `FAIL`, or `NOT TESTED`, with direct
evidence.

### A1 — Identity, provenance, and scope

- exact product/AP/META public identities and ancestry; candidate parent and
  subject; public `main` equality;
- prompt/report/plan hashes with complete readback; the 14-path set equality and
  diffstat; no `.ap`, broker, packaging, UI, bridge, CMake, dependency, license,
  or generated-file change;
- clean read-only checkouts; the recorded split-first-add archival observation;
- no scope escape.

### A2 — Typed desktop-id predicate semantics and compatibility

- the strengthened `workspaceDesktopIdLooksValid` matches the accepted plan's
  accept/reject table: rejects `.desktop`, `..desktop`, `a..desktop`,
  `.a.desktop`, `a.`, `.a`, `a..b`, `a`; accepts `a.desktop`,
  `org.example.A.desktop`, `org.kde.dolphin.desktop`, `org.kde.dolphin`,
  `kde.dolphin`;
- call sites and shared users inherit the predicate without other behavior
  change; the compatibility statement for already-saved schema-4 documents is
  accurate (only pathological values become invalid, failing `load` through the
  existing preserved-bytes fail-safe path).

### A3 — Causal regression evidence

- inspect test bodies, not names or counts: `desktopIdPredicateBoundaries`,
  `wellFormedIdsStillLaunchFromTheLauncher`, and the extended
  `invalidDesktopIdsAreRejected` cover the new boundaries non-vacuously;
- a disposable probe of the un-fixed parent (`2931588...`) with the candidate's
  test file proves the new rejection slots fail there and pass on the
  candidate;
- focused `test_application_launcher` and the full registered 21-test suite pass
  from the detached candidate with real counts; no test weakened, skipped, or
  looped; no real launch occurs (seam only).

### A4 — README, AGENTS.md, and ROADMAP state accuracy

- `README.md`: front-door block carries the exact M3/M4 wording and the
  smallest truthful successor clause; no `implementation candidate`, no `not
  accepted and not live-verified`; the "Named workspace sessions (code-accepted;
  live IRL deferred)" bullet, the M4 status-table row, and the Hardware
  evidence row are truthful with the accepted named slices;
- `AGENTS.md`: "Current repository state" records M1–M4 truthfully with the M2
  park claims and the exact M3/M4 wording; the stale roles bullet is replaced;
  the access-profile lines (`~52–55`, `~95–96`) are byte-identical to the
  baseline;
- `ROADMAP.md`: M4 bullet, current-whole bullet, parked-M2 paragraph, P1 row
  (**Done**, G1 closed), G7 pending line, G4 gate row, M3-backlog sentence, M4
  heading and classification, ledger-disposition paragraph, "G4 remains open"
  paragraph, and "Next remaining M2 work" paragraph are truthful; no remaining
  `M4 is now its own current whole`.

### A5 — Owner-document consistency

- `docs/specification.md`: `KService::serviceByStorageId` naming (no
  `KApplicationTrader` anywhere in `docs/` or `src/`) and the
  late-`desktopCreated` checkpoint disclosure;
- `docs/architecture.md` S1 and `docs/operations.md` E1–E8 corrections match the
  accepted G4 remainder (production/autostart readiness, hibernate/hybrid-sleep,
  general input-remapper coexistence);
- `docs/adr/0001`–`0004` statuses are truthful; no `accepted for the M4 tree`,
  no `Implementation-candidate`, no `accepted for the M2 tree`;
- `docs/testing-m3.md` and `docs/testing-m4.md` carry the corrected tense and
  the QML sentence; no stale `still need G4`, `one named slice`, or
  `Host suspend/resume acceptance remains open` in the changed documents.

### A6 — Ledger dispositions and bounded claims

- each of the six carried M4 ledger candidates has an accurate disposition in
  the ROADMAP ledger paragraph (`fixed` for ADR status phrasing, the `.desktop`
  predicate, `KApplicationTrader` naming, and the checkpoint disclosure;
  `parked` for QML runtime; `invalidated` for the orphaned old-path checkpoint);
- no second live task queue was created;
- verify the two recorded out-of-scope carried observations are unchanged by
  the candidate and are not candidate defects: the `ROADMAP.md` M2-backlog
  sleep-hook bullet ("Not autostart. Not live suspend evidence.") and the
  `docs/testing-m2.md` remainder lines (outside the candidate allowlist);
- no claim inflation: no M2/G4/G3 closure, no M3/M4 closure, no live behavior,
  no autostart, no production, no license selection, no physical acceptance.

### A7 — Independent validation from the detached candidate

- configure, build, focused test, and full registered suite from the exact
  detached candidate with build products outside the checkout, using the
  allowed commands below;
- record real counts, the first causal failure, and any deviation;
- confirm private-bus or bus-free test isolation and that no real KWin/OpenRGB/
  device/broker/host/desktop/launch/bridge-reload operation occurs.

### A8 — Independence, trace integrity, and report identity

- genuinely fresh session with no implementation participation and no
  inherited context; Native Plan Mode OFF; read-only product posture;
- temporary probe state is exact, owned, non-secret, and cleaned with reported
  outcome;
- prompt and report persistence/readback at the exact trace destinations; no
  META Git mutation by this Worker; report header and coordinates `02/01`.

Acceptance-PASS requires all A1–A8 PASS and both leads resolved. Any failed row
is a concrete finding with exact evidence; return `PARTIAL` if a material item
cannot be established read-only.

## Two mandatory adversarial leads

### Lead L1 — predicate boundaries and the accuracy of the `fixed` disposition

Independently confirm the strengthened predicate matches the plan's accept/reject
table and that every accepted form can resolve to a KService id while the
rejected forms cannot. Then assess whether any remaining accepted form is
trivially short in the sense named by the carried ledger item (for example
single-character-label dotted forms such as `a.b`), and state whether the
`fixed` disposition is accurate or should be narrowed to `parked`/partial.
Consider also that the reverse-DNS branch was deliberately left unchanged for
accepted schema-4 compatibility. Report `confirmed`, `disproved`, or
`unresolved` with exact evidence and consequence.

### Lead L2 — claim preservation and the carried documentation remainder

Independently confirm that (a) the exact M4 and M3 wording appears wherever
those states are restated and no claim was widened anywhere in the changed
documents; (b) the M2 park claims survive; and (c) the two recorded out-of-scope
carried observations are genuinely outside the candidate and accurately
described. State whether leaving those two observations unresolved should block
acceptance of this candidate or belongs to a later bounded documentation touch.
Report `confirmed`, `disproved`, or `unresolved` with exact evidence and
consequence.

## Allowed validation

Use only existing tools. Run from the exact detached candidate with the build
directory outside the checkout:

```sh
env -u LD_LIBRARY_PATH -u QT_PLUGIN_PATH cmake -S . -B <owned-temp>/build -G Ninja
env -u LD_LIBRARY_PATH -u QT_PLUGIN_PATH cmake --build <owned-temp>/build

env -u LD_LIBRARY_PATH -u QT_PLUGIN_PATH \
  ctest --test-dir <owned-temp>/build --output-on-failure \
  -R '^(test_application_launcher|test_profile_persistence|test_profile_resolver|test_workspace_plan)$'

env -u LD_LIBRARY_PATH -u QT_PLUGIN_PATH \
  ctest --test-dir <owned-temp>/build --output-on-failure

dbus-run-session -- <owned-temp>/build/test_application_launcher \
  desktopIdPredicateBoundaries invalidDesktopIdsAreRejected \
  wellFormedIdsStillLaunchFromTheLauncher

git diff --check 293158887e4228a42b9c64bda7d4f0bd32fb4ec9..235d467c752958694dad4be7bcc31e66406dbdcc
git diff --name-only 293158887e4228a42b9c64bda7d4f0bd32fb4ec9..235d467c752958694dad4be7bcc31e66406dbdcc
git status --short
```

For the causality probe, construct one Worker-owned disposable copy of the
un-fixed parent tree (read-only `git archive` of `2931588...` into a new owned
temporary directory outside the product checkout), overlay the candidate's
`tests/unit/test_application_launcher.cpp`, configure and build in a second
external directory, and run only the three named slots; report the exact result
and clean up completely.

Record real counts and the first causal failure. Do not weaken, skip, or loop
tests to manufacture green output.

## Exact META trace persistence

External trace disposition: configured
Trace discovery: META README and the exact trace destination
Trace project key: contextdesk
Trace logical-whole projection identity: 05-m4-state-and-ledger-reconciliation
Trace authority: historical-evidence-only
Trace archival owner: COOPERATOR
Trace visibility: public
Trace companion outcome: report
Trace self-granted status: none

Cooperator delivery / trace destination: configured
Downloadable prompt filename: 02_acceptance_00.md
Destination path: projects/contextdesk/00/05-m4-state-and-ledger-reconciliation/
Report filename: 02_report_00.md
Prompt authorship: ORCHESTRATOR
Prompt persistence owner: ORCHESTRATOR
Report persistence owner: WORKER
Git publication owner: COOPERATOR
Archival: wait-for-report

The ORCHESTRATOR persists this exact prompt bytes to
`projects/contextdesk/00/05-m4-state-and-ledger-reconciliation/02_acceptance_00.md`
before delivery. The WORKER may write only the report file at
`projects/contextdesk/00/05-m4-state-and-ledger-reconciliation/02_report_00.md`
in the COOPERATOR-owned META working tree.

Before substantive acceptance, verify the already-persisted prompt file exists
and is byte-identical to the received prompt; read it back completely. Stop on a
non-identical or unsafe collision. Do not alter earlier files, create a handout,
or write any other META path.

After acceptance, write the complete terminal report first to the report path,
read it back completely, and verify the header, coordinates, content, and
filename. Do not overwrite a non-identical existing report. Do not stage,
commit, push, pull, merge, rebase, or otherwise mutate META Git history or refs.
The COOPERATOR first-adds the exact prompt/report pair together in one commit
only after the report exists.

## Terminal report contract

The report must begin exactly:

`### Report for ORCHESTRATOR_CHAT`

It must echo the opening persistent-role and three coordinate fields exactly
once, with their values unchanged, and include:

- status: `PASS`, `PARTIAL`, or `BLOCKED`;
- phase-qualified result: `acceptance-PASS` only when A1–A8 all pass and both
  leads are resolved;
- acceptance candidate, owner map, allowlist, risk claims, and control matrix
  as issued, plus any correction to the record;
- start and end candidate commit; verified product/AP/META identities;
- the per-row A1–A8 verdicts with direct evidence;
- both lead verdicts (`confirmed`, `disproved`, or `unresolved`) with evidence
  and consequence;
- focused and full CTest results with counts from actual output and the
  causality-probe result;
- explicit statement that no real KWin/OpenRGB/device/broker/host/desktop/
  launch/bridge-reload operation occurred and no product/META Git mutation was
  performed;
- the recorded split-first-add archival observation;
- deviations, resolved issues/near-misses, pre-existing failure classification,
  residual risks, and missing evidence;
- exactly one smallest next step (ORCHESTRATOR reconciliation of this acceptance;
  no IRL, correction, deployment, or closure is authorized by this report);
- exactly one `Report justification: final-acceptance`;
- `Resolved Execution Issues / Near-Misses: none` or a complete entry;
- `Pre-Existing Failure Classification: none` or a complete entry;
- compact critique:

  `Orchestration critique:`

  `MEASURED: none | <verified finding; evidence; effect; smallest correction>`

  `LEAD: none | <unverified possibility; cheapest useful check>`

- exactly one `Logical-whole closure: not-closed` line;
- the exact authority-expiry sentence below.

Explicit non-claims: no implementation-PASS at this phase, no publication-PASS,
deployment-PASS, production readiness, physical acceptance, M2/G4 closure,
independent G3 closure, M3 closure, M3 physical observation, M4 closure, M4
live desktop/launch/placement behavior, autostart, hibernate/hybrid sleep,
general input-remapper coexistence, remapping, deck behavior, M5 integration,
per-key RGB, license selection, or measured control-to-zone placement; no claim
that tests prove live behavior; no claim that Session 27 was an independent
Worker result.

Authority for this Worker expires at this terminal report.

## Stop conditions

Stop before substantive acceptance on a non-fresh or contaminated session,
Native Plan Mode mismatch, coordinate/authority contradiction, product/AP/META
identity or public-ref failure, hash mismatch, dirty/unexplained worktree,
unsafe trace path/collision, or need for a non-allowlisted path, package
install, host/desktop mutation, launch, device access, secret, or subagent.

Stop `PARTIAL` when a material matrix row or lead cannot be established
read-only, when the causality probe cannot be constructed, or when a required
test fails; preserve the first causal failure and report exact evidence. Never
weaken or loop a gate; never convert missing evidence into PASS.

Stop immediately after the terminal report.
