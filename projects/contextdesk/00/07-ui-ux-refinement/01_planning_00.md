Logical whole identity: ui-ux-refinement
Worker session ordinal: 01
Worker exchange ordinal: 01
Persistent role identity: WORKER
Worker session target: fresh-worker-session
Worker session profile: Planner
Phase: planning
Task identity: CONTEXTDECK-UI-UX-REFINEMENT-PLAN
Native planning mode: required
Delivery route: manual COOPERATOR delivery to a genuinely fresh Worker session
Reasoning recommendation: High
Reasoning basis: the plan must choose durable UI/UX structure across six
settings sections, nine QML files, the tray surface, 83 `app.*` bindings, an
accessibility layer, and one bounded COOPERATOR-executed runtime launch;
Standard reasoning cannot settle cross-page consistency and accessibility or
runtime trade-offs that later implementation slices inherit.
Recommended context capacity: approximately 250k tokens; advisory only
Evidence tier: E2
Evidence-tier basis: the eventual whole is cross-cutting but reversible
user-visible UI work with no production, durable-data, security-boundary,
broker-semantics, packaging, dependency, or license mutation; the single
runtime launch is a bounded, reversible, COOPERATOR-executed observation with
the broker inactive. This planning exchange itself is read-only.
Internal delegation: prohibited

# ContextDeck — plan the UI/UX refinement whole

You are a genuinely fresh WORKER assigned only to repository-grounded
implementation planning. Use the client's actual Native Plan Mode. You did not
participate in M1–M4 implementation, the code-health refactoring, or any
acceptance, and you inherit no authority from any previous session. Do not
dispatch subagents.

This is a planning grant only. It authorizes no product implementation, no
source mutation, no host operation, no application launch, no desktop mutation,
no deployment, no acceptance execution, and no Git publication. Stop at the
terminal planning report.

## Why this whole exists

The session application's codebase is now decomposed and tractable
(`code-health-and-refactoring` closed on `ba87ba0...`), and `ui/*.qml` was
deliberately untouched by that whole. This whole refines the application's user
interface and experience before any release-oriented work: visual language and
page consistency, flows and states, accessibility, tray behavior, and one
bounded runtime validation of the QML surface that has so far never been
executed.

The COOPERATOR confirmed the scope on 2026-09-24: all five UI areas plus
runtime QML validation through one bounded, COOPERATOR-executed launch;
physical testing remains deferred; G6 licensing remains deferred to a
release-oriented whole; a small forward documentation reconciliation is
included.

## Planning record and Plan-to-Execution gate

Planning cycle: initial
Prior planning report: none
Targeted revision basis: none
Changed decision boundary: none
Preserved unaffected decisions: every accepted M1–M4 code decision and
deferral; the M2 park claims and named accepted G4 slices; the five-zone,
G213-only, event-driven-KWin, typed-action, disabled-broker, no-autostart, and
one-OpenRGB-connection invariants; the 21 registered CTest names and their
command spellings; the META trace grammar; manual COOPERATOR delivery; the
COOPERATOR decision that physical testing and G6 licensing remain deferred; and
the COOPERATOR-confirmed access-profile change from the closed 05 plan §3.2 A3
/ §3.3 B1 (the one policy item intentionally changed in this whole)
Automatic targeted revisions used: 0

Planning layer: implementation-planning
Orchestration planning owner: ORCHESTRATOR
Worker planning scope: one repository-grounded, decision-complete plan for
`ui-ux-refinement` that (a) inventories the current UI/UX at the exact
candidate, (b) defines a concrete visual-language, flows-and-states,
accessibility, and tray direction with exact file targets and preserved
invariants, (c) selects and orders slices with exact changed-path allowlists,
per-slice validation, and the full registered suite as the broad gate, (d)
designs the one bounded COOPERATOR-executed runtime QML validation with
public-safe evidence rules, (e) designs the cumulative implementation and
acceptance route or returns a justified split proposal, and (f) plans the small
opening documentation reconciliation with exact replacement text
Plan disposition: approval-gated
Implementation in same Worker session: allowed
Planning stop event: terminal planning report submitted
Execution authority event: a separate complete ORCHESTRATOR implementation
prompt whose native planning-mode value is `not-used`
Post-plan implementation session: current-worker-session
Maximum plan-only cycles: 1

The ORCHESTRATOR reconciles the plan before any implementation prompt. Native
Plan approval, "continue", retained context, or the existence of this prompt
never grants execution authority.

## Verified public candidate

Canonical product repository: `https://github.com/cisarik/contextdesk`
Expected branch: `main`
Exact product candidate:
`ba87ba08c17ad965d008c4fc1d59f42a7c7700c9`
Required parent:
`58a10bb71d697a48b5a94e63e5a8c0f9eacc4a9b`
Closed whole baseline (do not reopen):
`235d467c752958694dad4be7bcc31e66406dbdcc`
M4 accepted Slice A candidate: `aca6c68542bbc9f1b8ee891415a04b0a95e8372e`
M4 accepted Slice B candidate: `db9ddc1f923f44e26f7f1d58df7446306bbdf64a`
M3 code-accepted candidate: `502ae75571358ec95d33c836084b5e2253850731`
M2 inactive-install candidate (historical; do not reopen):
`cb72ae0388307b514182efc6936712e3da42cda4`
Required AP gitlink and checkout:
`0cf2cff483a36a4cc2254aa424a7c53bd57a97e9`

Canonical META repository: `https://github.com/cisarik/meta.git`
Verified public META baseline at preparation:
`a2c11ad880f641814dcc0fbc1018ad87a81b3684`
This whole's trace destination:
`projects/contextdesk/00/07-ui-ux-refinement/`
Closed predecessor trace:
`projects/contextdesk/00/06-code-health-and-refactoring/`
Documentation-reconciliation source:
`projects/contextdesk/00/05-m4-state-and-ledger-reconciliation/01_report_00.md`
§3.2 A3 / §3.3 B1
M4 trace:
`projects/contextdesk/00/04-g213-contextdeck-workspace-session-manager/`
M3 trace:
`projects/contextdesk/00/03-g213-contextdeck-workspace-aware-lighting/`
M2 trace:
`projects/contextdesk/00/02-g213-contextdeck-input-passthrough-safety/`
M1 trace:
`projects/contextdesk/00/01-g213-contextdeck-mvp-context-lighting/`

Before planning, verify without changing repository state:

1. Product root, canonical remote, active `main`, clean worktree, exact HEAD
   `ba87ba08...`, exact parent `58a10bb...`, AP gitlink/checkout equality
   `0cf2cff...`, and `./.ap/ap doctor` PASS with variant `stable`.
2. Direct `git ls-remote` for product `main` equals the exact candidate. Do not
   fetch, pull, switch, reset, clean, stash, rebase, merge, or retarget the
   COOPERATOR-owned checkout.
3. Direct `git ls-remote` for META `main` equals the exact baseline above or a
   verified later descendant. The COOPERATOR may commit this whole's opening
   artifacts (`00_handout.md`, `00_notes.md`) and the prepared prompt at any
   time; the prompt and the report are first-added together only after the
   report exists. If the public META head is later, inspect its changed paths
   and accept it only if it does not contradict this prompt.
4. This whole's trace directory exists as a real directory containing the
   ORCHESTRATOR-authored `00_notes.md` and the opening `00_handout.md` (the
   predecessor full-Orchestrator continuity record). If readable, the opening
   handout's SHA-256 is
   `11d30cf2b1711121be442bc049d8cd8f219972095da392e44907eff34fdd48d7`. Do not
   alter `00_notes.md` or `00_handout.md`.
5. Apart from the exact report preparation authorized below, preserve all owner
   work and stop on unexplained state.

If the product candidate, parent, AP pin, public ref, trace identity, or
repository state differs materially, stop before planning and return `BLOCKED`.
Do not silently use a nearby commit or an old clone.

## Objective and scope

Refine the session application's presentation and validate its QML surface once
at runtime. Product semantics stay fixed: typed action semantics, when an
action fires, persistence, IPC and wire formats, broker behavior, KWin bridge
behavior, workspace mutation logic, launch behavior, product claims, safety
boundaries, and the meaning of any existing control. Presentation is in scope:
visuals, layout, spacing, focus and accessibility metadata, empty/error/busy/
disabled/dirty display, tray presentation, and a user-facing string only when
the plan gives its exact current text, exact replacement, and the reason.
Do not translate the UI, do not anglicize Slovak strings, and do not rewrite
copy in bulk. Existing strings are mixed Slovak and English; leave each one
unless a named slice changes that exact string. Repository documentation stays
English.

COOPERATOR-confirmed scope:

1. Visual language and page consistency across the shell and six sections: Stav
   (`OverviewPage`), Farby (`ColorsPage` + `LightingPresetEditor`), Aplikácie
   (`ApplicationsPage`), Plochy (`WorkspacePage`), Diagnostika
   (`DiagnosticsPage`), Pokročilé (`ControlsPage`), plus `ZoneHero` and
   `Main.qml`.
2. Flows and states: empty, error, disabled, busy, confirmation, and
   post-action feedback states; dialog behavior; unsaved or dirty indicators;
   the M2 "inactive" demotion presentation.
3. Accessibility: keyboard navigation and focus order, accessible names, roles,
   and descriptions, contrast, scaling, and screen-reader legibility. Measured
   opening gap: only four `Accessible.name` occurrences exist in `ui/`
   (`OverviewPage.qml:70`, `LightingPresetEditor.qml:231`, `:260`, `:336`).
4. Tray behavior: indicator state, tooltip, menu actions, and show/hide
   settings behavior (`src/app/TrayController.{h,cpp}`).
5. Runtime QML validation: exactly one bounded, COOPERATOR-executed launch of
   the session application during the whole, with the broker inactive and no
   autostart, producing a numbered acceptance checklist and public-safe
   evidence.
6. Opening documentation reconciliation (small, first slice): the stale
   "current whole" wording in `ROADMAP.md:43-46` and the `README.md` status
   block, plus the access-profile wording from the closed 05 plan §3.2 A3 /
   §3.3 B1 applied to `AGENTS.md:67-69`, `AGENTS.md:105-106`, and
   `ROADMAP.md:7`.

Explicit non-goals and preserved invariants:

- No change to product semantics, product claims, safety boundaries, the five-zone
  model, G213-only scope, event-driven KWin identity, typed actions,
  disabled-by-default broker, the no-autostart rule, or the one-OpenRGB-
  connection rule.
- No per-key RGB representation, ever.
- No license selection or licensing documentation change (G6 is deferred to a
  release-oriented whole).
- No physical testing, device access, broker operation, ARM, desktop mutation,
  service change, or host mutation by the Worker.
- No new dependencies unless the plan names why an existing Qt6/KF6 component
  cannot satisfy the need and marks it as requiring a separate
  dependency-authority grant; a UI/UX refinement should not need one.
- Do not read or copy `06-code-health-and-refactoring/01_report_00.md`; it is a
  superseded client-native artifact containing a non-public path. Read only the
  accepted reports and the 05 plan text named above.
- The Worker must not launch the application; the runtime validation is planned
  for the COOPERATOR to execute.

## Candidate work areas (ORCHESTRATOR observations at the candidate; re-verify all)

These are evidence-backed candidates, not decisions. You select, merge, split,
order, and bound them. Every size, count, and line reference below is an
observation to re-verify at the exact candidate.

U1 — Shell and visual language. `ui/Main.qml` (104 lines):
`Kirigami.ApplicationWindow`, `minimumWidth: 880`, `minimumHeight: 560`,
default size 1040×680, `pageStack.replace` navigation, and a
`Kirigami.GlobalDrawer` with six checkable actions (Stav, Farby, Aplikácie,
Plochy, Diagnostika, Pokročilé). Page sizes: `OverviewPage.qml` 87,
`ColorsPage.qml` 66, `ApplicationsPage.qml` 172, `WorkspacePage.qml` 343,
`DiagnosticsPage.qml` 236, `ControlsPage.qml` 89, `LightingPresetEditor.qml`
473, `ZoneHero.qml` 134. Proposed direction to ground: one consistent page
scaffold, a spacing and typography scale, header and action placement, and a
shared empty/error/busy presentation.

U2 — Flows and states. Review every page for empty, error, disabled, busy,
confirmation, and post-action feedback states; the lighting save/apply path;
workspace preview/apply/revert states; diagnostics pause/resume; the M2 banner
and the advanced-controls demotion; and dialog/confirmation patterns
(`confirmSuspend`, `confirmArmPassThrough` in the tray). Identify where a state
is silent, ambiguous, or inconsistent across pages.

U3 — Accessibility. Only four `Accessible.name` occurrences exist; keyboard
navigation, focus order, roles and descriptions, contrast, and scaling are
unmeasured. Propose a bounded accessibility layer with exact QML targets and a
checkable acceptance list that does not require a screen reader on the Worker
side.

U4 — Tray. `src/app/TrayController.{h,cpp}` owns a `KStatusNotifierItem`,
rebuilds its menu, emits `showSettingsRequested`, and confirms Suspend and ARM
pass-through. Presentation strings come from the controller. Review tooltip,
menu labels, indicator state, and the settings-window lifecycle (closing
settings must not stop the tray).

U5 — Runtime QML validation. Accepted QML runtime evidence does not exist.
`ROADMAP.md` parks QML runtime as build-time only; historical M1 IRL acceptance
is lighting and context evidence, not runtime acceptance of this UI tree.
Re-verify that boundary. Do not write that the application was never started
if the trace shows otherwise. Design exactly one bounded COOPERATOR-executed
launch from the exact accepted candidate: build commands, preconditions
(broker unit not started, no ARM, no autostart, no device operation, no
desktop mutation), what the COOPERATOR observes, the numbered PASS/FAIL/NOT
TESTED checklist, abort handling, and public-safe evidence. The COOPERATOR may
show screenshots in the manual chat so a later Worker can see the surface;
those images are not committed to the product repository or to META. The
written checklist contains no window captions, desktop names or IDs, host
details, or local paths. State exactly what a single bounded run can and
cannot establish.

U6 — Documentation reconciliation. Use the exact replacement text in
`05-m4-state-and-ledger-reconciliation/01_report_00.md` §3.2 A3 and §3.3 B1 for
the access-profile lines. Replace the stale current-whole wording truthfully:
`code-health-and-refactoring` is closed, and this whole is `ui-ux-refinement`.
Do not change any license text; G6 stays open and deferred.

## Required planning outcome

Return exactly one decision-complete plan (the terminal report) containing at
least:

1. Current-state UI/UX inventory at the exact candidate: shell, pages,
   components, tray, the 83 unique `app.*` bindings, existing states, and
   accessibility posture; every claim tied to exact candidate paths.
2. The concrete UI/UX direction: visual-language rules; a per-page refinement
   list; flows and states; accessibility requirements; tray behavior. For each
   item: exact file targets, preserved invariants (binding names, behavior,
   product claims), and whether any user-visible string changes, with exact
   before/after text.
3. Slice selection and ordering with exact changed-path allowlists (expected:
   `ui/*.qml`, possibly `src/app/` presentation-only files, and the
   documentation paths), per-slice focused validation, and the full registered
   21-test suite as the broad gate, with exact commands. The first
   implementation slice is the documentation reconciliation only. Each later
   slice covers one primary surface — the shared scaffold, one page, or the
   tray — plus only the files that surface requires. Do not put all nine QML
   files in one slice.
4. The runtime validation design: exact build commands from the exact
   candidate, preconditions and abort rules, the numbered COOPERATOR checklist,
   public-safe evidence rules, and the explicit limits of what the run
   establishes.
5. The implementation exchange and commit sequence: which slices share one
   Worker exchange, expected commit subjects, cumulative candidate definitions,
   and where the full suite runs.
6. The acceptance design: one fresh independent acceptance after the final
   slice (plus at most one correction re-audit), or a justified split proposal;
   the exact final-acceptance control matrix, including the falsifiers for
   binding, behavior, string, and visual claims, and the honest limits of
   compile-time-only evidence.
7. The opening documentation reconciliation slice: exact replacement text for
   each changed line or paragraph, including the exact access-profile text from
   the 05 plan and the current-whole correction.
8. A risk register: QML binding regression, behavior and string drift,
   accessibility regressions, visual regressions that only a runtime run could
   reveal, runtime launch safety, scope creep into behavior changes, and
   dependency temptation — each with detection and rollback.
9. Exact planning assumptions and named missing evidence; if one material
   design choice cannot be settled from repository evidence, return `PARTIAL`
   naming it — do not guess.

The plan must be sufficient for the ORCHESTRATOR to issue bounded
implementation prompts without another planning cycle. It must not propose any
change outside the confirmed scope.

## Reading and inspection list

Read the pinned AP Worker minimum spine and, at minimum:

- `.ap/AP.md` — Semantic Authority, RF-03/RF-06/RF-12/RF-16/RF-18/RF-19,
  orchestration and implementation planning, Plan-to-Execution, Worker
  responsibilities, validation, stopping conditions;
- `.ap/AP_WORKER.md`;
- `.ap/PROMPT_CONTRACTS.md` — Worker report header, Planning Record, Worker
  Session Target, exchange and trace contract, validation ladder, delivery
  record, Session-and-Mode, Plan-to-Execution;
- `AGENTS.md` including the managed AP block;
- `README.md`;
- `ROADMAP.md` (current-state, M2/M3/M4, and the documentation anchors);
- `docs/specification.md`, `docs/architecture.md`, `docs/operations.md`;
- `docs/adr/` (all current ADRs and `README.md`);
- all nine `ui/*.qml` files in full;
- `src/app/` presentation-facing units: `AppController.{h,cpp}`,
  `PresentationModel.{h,cpp}`, `LightingEdit.{h,cpp}`,
  `ProfileDocumentEditor.{h,cpp}`, `WorkspaceSessionEditor.{h,cpp}`,
  `WorkspaceApplyController.{h,cpp}`, `TrayController.{h,cpp}`,
  `SessionApplication.{h,cpp}`, `main.cpp`;
- `tests/unit/test_workspace_lighting.cpp` and the test registration in
  `CMakeLists.txt` plus `cmake/contextdeck-tests.cmake`;
- this whole's `00_handout.md` and `00_notes.md`;
- `05-m4-state-and-ledger-reconciliation/01_report_00.md` §3.2 A3 / §3.3 B1 for
  the exact access-profile text;
- `docs/testing-m3.md` and `docs/testing-m4.md` only as needed to preserve
  their boundaries;
- older traces only as needed to preserve M2/M3/M4 boundaries.

You may inspect the exact candidate in a fresh disposable clone or in a clean
checkout at the exact candidate. Every finding must be tied to the exact
candidate. No host reconnaissance: do not inspect device nodes, private
configuration values, logs, home paths, other repositories, credentials, or
unrelated host state. Do not configure, build, launch, or execute the
application or tests in this planning exchange; read source, tests, and build
definitions only.

## Authority and side-effect boundary

Product source mutation: prohibited
Product Git mutation: prohibited
Product commit/push: prohibited
AP mutation/update: prohibited
Host mutation: prohibited
Application launching: prohibited (the runtime validation is COOPERATOR-executed
and planned only)
Desktop create/remove/rename: prohibited
`kwinrulesrc` write: prohibited
Host service operation: prohibited
Broker start/stop/ARM/grab/probe: prohibited
Input-remapper operation: prohibited
OpenRGB/KWin installation or runtime operation: prohibited
Physical-device probing or physical acceptance: prohibited
Dependency installation/update: prohibited
Secrets/credentials/private data: prohibited
Network authority: reads from the two canonical HTTPS remotes only
(`https://github.com/cisarik/contextdesk.git`,
`https://github.com/cisarik/meta.git`): `git ls-remote`, read-only fetches or
clones into Worker-owned temporary directories, and direct public content
reads. No alternate mirror, no external research, no provider calls.
Side effects: read-only inspection plus the exact META trace-file preparation
below only

No raw input, raw event lines, key names, scan values, typed content, serials,
host addresses, host keys, passwords, private paths, desktop names or IDs,
window captions, or unredacted tool logs may appear in the prompt, report, or
trace. Use only semantic state and public repository-relative paths.

No recovery route is required because this exchange must not start, open, probe,
grab, launch, or mutate anything. If any step would cross that boundary, stop.

## Validation ladder

Validation ladder: selected
Inspection and provenance: required
Existing focused tests: identify exact existing tests per slice from
`CMakeLists.txt` and `cmake/contextdeck-tests.cmake`; do not configure, build,
or execute them in this planning exchange
Affected tests: propose exact focused tests per slice, including any new causal
regression with the named behavior or uncovered gap it closes; note that QML
presentation changes are only partly testable through the existing C++ facade
test
New causal regression: required where a slice creates a behavior-preservation
risk that no existing test covers; otherwise none, with the reason and the
counterevidence
Broad or full suite: the full registered 21-test CTest suite is the planned
broad gate for every implementation slice, because binding and behavior
preservation across UI changes is the named decision risk; state the exact
commands from the candidate
Runtime or testbed: planned as the one bounded COOPERATOR-executed application
launch defined in the plan; not executed in this planning exchange
Independent acceptance: stated by the plan from the accepted implementation
content (cumulative fresh acceptance, or the split proposal's per-whole
acceptance)

The planning result is non-implementation evidence. It does not accept code,
behavior, or visuals.

## Trace persistence contract

External trace disposition: configured
Trace discovery: META README and the exact destination below
Trace project key: contextdesk
Trace logical-whole projection identity: 07-ui-ux-refinement
Trace authority: historical-evidence-only
Trace archival owner: COOPERATOR
Trace visibility: public
Trace companion outcome: report
Trace self-granted status: none

Cooperator delivery / trace destination: configured
Downloadable prompt filename: 01_planning_00.md
Destination path: projects/contextdesk/00/07-ui-ux-refinement/
Report filename: 01_report_00.md
Prompt authorship: ORCHESTRATOR
Prompt persistence owner: ORCHESTRATOR
Report persistence owner: WORKER
Git publication owner: COOPERATOR
Archival: wait-for-report

The ORCHESTRATOR persists this exact prompt bytes to
`projects/contextdesk/00/07-ui-ux-refinement/01_planning_00.md` before
delivery. The WORKER may write only the report file at
`projects/contextdesk/00/07-ui-ux-refinement/01_report_00.md` in the
COOPERATOR-owned META working tree.

Before substantive planning, verify the already-persisted prompt file exists and
is byte-identical to the received prompt; read it back completely. Stop on a
non-identical or unsafe collision. Do not alter `00_notes.md` or
`00_handout.md`, create a handout, or write any other META path.

After planning, write the complete terminal report first to the report path,
read it back completely, and verify the header, coordinates, content, and
filename. Do not overwrite a non-identical existing report. Do not stage,
commit, push, pull, merge, rebase, or otherwise mutate META Git history or refs.
The COOPERATOR archives the exact prompt/report pair together in one first-add
commit only after the report exists.

If the exact META destination is not reachable from your environment, do not
write to any other path; state the exact limitation in the report body and
return the complete report through the client output so the COOPERATOR can
persist it exactly. Do not reconstruct or prettify earlier artifacts.

## Terminal report contract

The report must begin exactly:

`### Report for ORCHESTRATOR_CHAT`

It must echo the opening persistent-role and three coordinate fields exactly
once, with their values unchanged.

Include:

- status: `PASS`, `PARTIAL`, or `BLOCKED`;
- phase-qualified result: `not-applicable` (planning is not implementation);
- start and end product commit (expected unchanged exact candidate);
- verified product/AP/META identities and clean-state result;
- source-grounded current-state findings, each tied to the exact candidate;
- the complete decision-ready plan covering all measurable-outcome items above,
  including exact per-slice allowlists and, where documentation text changes,
  exact replacement text or anchor-based edit instructions;
- the implementation exchange and commit sequence and the acceptance design;
- the E2 implementation/acceptance evidence envelope and validation rationale;
- explicit statement that the plan made no product, AP, host, service,
  desktop-configuration, launch, device, dependency, or Git publication
  mutation;
- trace prompt/report persistence and complete-readback result;
- deviations, risks, unresolved decisions, and missing evidence;
- exactly one smallest next step: ORCHESTRATOR reconciliation of this plan and,
  only if accepted, a separate implementation Worker prompt;
- exactly one `Report justification: new-evidence`;
- `Resolved Execution Issues / Near-Misses: none` or a complete entry;
- `Pre-Existing Failure Classification: none` or a complete entry;
- compact critique:

  `Orchestration critique:`

  `MEASURED: none | <verified finding; evidence; effect; smallest correction>`

  `LEAD: none | <unverified possibility; cheapest useful check>`

- exactly one `Logical-whole closure: not-closed` line;
- the exact authority-expiry sentence below.

Explicit non-claims in the report:

- no implementation-PASS, acceptance-PASS, publication-PASS, deployment-PASS,
  production readiness, M2/G4 closure, G3 independent re-audit, autostart,
  hibernate/hybrid sleep, general input-remapper coexistence, M3 closure, M3
  physical five-zone observation, M4 closure, M4 live desktop/launch/placement
  behavior, M5 integration, remapping, deck-layer behavior, per-key RGB, license
  selection, or physical hardware acceptance;
- no claim that any UI/UX or QML runtime behavior was exercised or validated in
  this planning exchange; the runtime validation is a planned COOPERATOR-executed
  step and is not evidence until executed and reported;
- no claim that the planned refinement preserves behavior until the later
  implementation and its acceptance establish it;
- no claim that the application was launched, or that any host, desktop,
  broker, or device state was touched;
- no claim that Session 27 was a fresh Worker result.

Authority for this Worker expires at this terminal report.

## Stop conditions

Stop and report `BLOCKED` before substantive planning if the session is not
genuinely fresh, Native Plan Mode is not actually enabled, coordinates or
authority are contradictory, product/AP/META identity fails, the worktree has
unexplained changes, the continuity records are missing, the trace path
collides or is unsafe, required repository evidence cannot be read, or
completing the plan would require a prohibited mutation, private data,
host/device/desktop access, an application launch, an additional dependency, or
subagents.

Stop with `PARTIAL` if the repository can be analyzed but one material design
choice or required evidence item remains unresolved, or if a justified split
proposal is the only tractable route. Do not implement, touch the live desktop,
launch the application, or silently widen scope to force PASS.

Stop with `PASS` only when the complete decision-ready plan, exact slice
boundaries, the runtime-validation design, and the exact later implementation
boundary are persisted and read back. Then submit the terminal report and do no
further work under this grant.
