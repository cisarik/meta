# Fresh full Orchestrator handoff — ContextDesk after `code-health-and-refactoring`

You are a genuinely fresh, full-capability ORCHESTRATOR for ContextDesk (the
application is also called ContextDeck). This is the continuity record from the
full-Orchestrator session that closed the `code-health-and-refactoring` logical
whole and prepared the next one. It is not a Worker prompt, not a plan, not an
acceptance report, and not authority to claim closure. Reconcile it against the
current public repositories and the pinned Analytic Programming (AP) protocol
before taking action.

Handoff artifact filename: `00_handout.md`
META destination:
`projects/contextdesk/00/07-ui-ux-refinement/00_handout.md` (opening record of
the selected next whole)
Handoff authorship: ORCHESTRATOR (code-health whole)
Handoff persistence/archive owner: COOPERATOR (or the successor full
ORCHESTRATOR after exact-byte verification)
Paired orchestration notes: the successor authors `00_notes.md` in the new
whole's trace directory at opening
Restoration readiness (author's classification): **PASS** — public refs
verified directly at closure, the prior whole closed with its record written,
no active Worker, no active mutation
Continue only through separately bounded logical wholes.

## 1. Successor capability profile and access

Successor capability profile: `Orchestrator` (full project orchestration).
Access profile: full project orchestration when the session environment exposes
those capabilities; not read-only ChatOrchestrator.
Primary project: `https://github.com/cisarik/contextdesk`
Evidence archive: `https://github.com/cisarik/meta.git`

The successor is expected, when its actual execution environment exposes those
capabilities, to run repository commands and scripts, inspect source and tests,
read and write the ContextDesk product repository, read and write the META
repository, create commits, push canonical branches, and verify public refs.
Capability must be directly observed in the new session; this text never turns
an unavailable tool or credential into authority.

Repository write authority is project-scoped and AP-governed. Use disposable
inspection clones for public verification and a deliberate clean product/META
worktree for authorized writes. Preserve unrelated owner work. Never use broad
destructive Git recovery or silently retarget a changed public baseline.

The COOPERATOR (Michal) remains the human owner of material objective changes,
cost, privacy, irreversibility, physical behavior, host-side action, and
residual risk. Repository write authority does not imply host or device
authority. Host commands, service changes, input-device access, desktop
mutation, application launch, bridge reload, physical keyboard tests,
credentials, and external-account actions each require their own explicit
bounded COOPERATOR decision, recovery plan, and AP surface gate.

Policy note: `AGENTS.md` still declares the `ChatOrchestrator` access profile
while recent successor wholes ran as full local Orchestrator sessions. That
wording is an explicit, still-open COOPERATOR policy item (section 12). Do not
silently change it.

## 2. Language and routing contract

- Speak **Slovak** with Michal/COOPERATOR.
- Write every Worker prompt and every formal Worker report in **English**.
- Keep each task one complete bounded prompt, one fresh or explicitly renewed
  Worker session, one terminal report, and one reconciliation.
- Manual delivery is preserved: the COOPERATOR carries every prompt and report.
  Do not spawn, auto-dispatch, parallelize, or silently route Workers.
- Prepare the complete prompt, write it to the exact META trace path, tell the
  COOPERATOR the exact file and routing state, and let the COOPERATOR manually
  carry it to the chosen fresh Worker.
- Default topology is one active Worker. A parallel or internal-audit exception
  needs an explicit bounded COOPERATOR decision, disjoint ownership, and AP
  evidence; it never creates independent acceptance automatically.
- Interaction convention established with the COOPERATOR: when the COOPERATOR
  writes "Pokracuj", the last Worker's report is expected in the META working
  tree and the Orchestrator reads and reconciles it. If the expected report is
  missing, say so instead of inventing progress.

## 3. Verified public identity snapshot at handoff

These values were directly checked immediately before this handoff. The
successor must re-check them in its own environment.

| Item | Verified value |
|---|---|
| Product remote / branch | `https://github.com/cisarik/contextdesk` / `main` |
| Product current public HEAD | `ba87ba08c17ad965d008c4fc1d59f42a7c7700c9` |
| `code-health-and-refactoring` baseline | `235d467c752958694dad4be7bcc31e66406dbdcc` |
| Whole commit chain | `5b2b25b` (S1) → `308aaa2` (S2) → `1e7e9d5` (S4) → `a130b06` (S6) → `4ec3732` (S3) → `58a10bb` (S5) → `ba87ba0` (S7) |
| M4 Slice A / Slice B accepted | `aca6c68542bbc9f1b8ee891415a04b0a95e8372e` / `db9ddc1f923f44e26f7f1d58df7446306bbdf64a` |
| M3 code-accepted | `502ae75571358ec95d33c836084b5e2253850731` |
| M2 inactive-install candidate | `cb72ae0388307b514182efc6936712e3da42cda4` |
| Product AP gitlink and checkout | `0cf2cff483a36a4cc2254aa424a7c53bd57a97e9` |
| Public AP `main` observed during the whole | `7478ddb07d2c3911f79e1aa1441f0115a31c45d8` (ahead of the pin; not adopted) |
| META remote / branch | `https://github.com/cisarik/meta.git` / `main` |
| META public `main` floor at closure | `11245c5...` plus the pending closure commits (acceptance pair, notes, closure, handout); verify actual state |
| Closed whole trace | `projects/contextdesk/00/06-code-health-and-refactoring/` |
| New whole trace (this handout) | `projects/contextdesk/00/07-ui-ux-refinement/` |
| Predecessor whole trace | `projects/contextdesk/00/05-m4-state-and-ledger-reconciliation/` |
| M4 trace | `projects/contextdesk/00/04-g213-contextdeck-workspace-session-manager/` |
| M3 trace | `projects/contextdesk/00/03-g213-contextdeck-workspace-aware-lighting/` |
| M2 trace | `projects/contextdesk/00/02-g213-contextdeck-input-passthrough-safety/` |
| M1 trace | `projects/contextdesk/00/01-g213-contextdeck-mvp-context-lighting/` |

Key closed-whole artifact SHA-256 values (verify against the actual files):

| Artifact | SHA-256 |
|---|---|
| `06-.../01_planning_00.md` (initial Planner prompt) | `b9969040e1b735bdeda3800f88f4f70048339720ddcea788fa52ba9da649ac7e` |
| `06-.../01_report_01.md` (rendered accepted plan) | `4d4dc9d6ddaf7241e41786aa1c1412f8aea794b814c4c088e5228761dea0ecd6` |
| `06-.../01_planning_02.md` (targeted revision prompt) | `689ecf30c15dc770efdb7bac3b237b8273789b5bbbfb873f326214dfa29097d2` |
| `06-.../01_report_02.md` (revision report) | `9ada854757a91979e4afcac9ca211ee3e859d0ee57b20f2a1a3baab99e0ffc45` |
| `06-.../03_acceptance_00.md` (acceptance prompt) | `e3152f8aeeca92af320c44ead82b9e2b4ab28d2aecb742047d5df11fd27a6cf0` |
| `06-.../03_report_00.md` (acceptance report) | `6515925554e7f7528c39d4ed28ce56e95c910b764b9622fa54299945a325bcd8` |
| `06-.../04_closure.md` (closure record) | `883ef4b1ecbcf61820657580f114a291430c24065fef8a012b8a665324cbebaa` |

The implementation prompt/report pairs `01_implementation_03.md` +
`01_report_03.md` (S1), `01_implementation_04.md` + `01_report_04.md` (S2),
`01_implementation_05.md` + `01_report_05.md` (S4+S6),
`02_implementation_00.md` + `02_report_00.md` (S3+S5), and
`02_implementation_01.md` + `02_report_01.md` (S7) are archived in the same
trace with their SHA-256 values recorded in `00_notes.md` and in the reports.

Exception artifact: `01_report_00.md` is **not** a report. It is a
client-native plan artifact (non-English, containing a local machine path) that
was mistakenly written to the exchange-01 report path and later mistakenly
committed in META `0327a30`. The valid exchange-01 planning outcome is the
rendered report `01_report_01.md` produced by the completion exchange. See
section 12 for the open remediation.

Filing notes (prospective supersession): the closed whole's closure record was
filed by the COOPERATOR as `06-.../04_closure.md` (renamed from the draft name
`01_closure.md` before archival; bytes identical, SHA-256
`883ef4b1ecbcf61820657580f114a291430c24065fef8a012b8a665324cbebaa`). Its
successor-continuity line and the `00_notes.md` reference to a `02_handout.md`
inside the closed trace are superseded by this handout at
`projects/contextdesk/00/07-ui-ux-refinement/00_handout.md`. The committed notes
and closure text are historical and are not rewritten.

## 4. Current truthful state

### 4.1 `code-health-and-refactoring` — closed by ORCHESTRATOR (2026-09-15)

- Objective: reduce structural complexity, duplication, and mixed
  responsibilities through behavior-preserving refactoring before the later
  UI/UX whole, without changing behavior, visuals, product claims, or safety
  boundaries.
- Result: accepted plan `01_report_01.md` corrected by one targeted revision
  `01_report_02.md`; seven implementation commits; fresh independent acceptance
  `03_report_00.md` with all 12 control-matrix items PASS and adversarial probes
  P1–P5; direct public `main` equals the candidate `ba87ba0...`; 39 changed
  paths, each commit inside its slice allowlist; full registered 21-test suite
  21/21 from the exact candidate and from each accepted slice; `ui/*.qml`
  byte-identical; 83 unique `app.*` bindings unchanged; `Persistence.h`
  byte-identical; `src/broker/IpcProtocol.*`, `AGENTS.md`, and
  `docs/architecture.md` unchanged.
- Closure record `04_closure.md`; `Logical-whole closure:
  closed-by-ORCHESTRATOR`; notes frozen. No live host, device, desktop, launch,
  broker, packaging, or license operation occurred anywhere in that whole.

### 4.2 Architecture after the refactor (what the successor inherits)

- `AppController` remains the sole QML type and test-facing facade behind the
  single `app` context property; its implementation now delegates to cohesive
  units under `src/app/`: `LightingEdit`, `ProfileDocumentEditor`,
  `WorkspaceSessionEditor`, `WorkspaceApplyController`, `PresentationModel`.
- The `contextdeck_app` static library holds the facade and those units; the
  `contextdeck` executable and `test_workspace_lighting` link it.
- `src/core/Persistence.cpp` is slim IO plus `ProfileStore::load/save/
  toJsonBytes`; parse/serialize codecs live under `src/core/persistence/`
  (`JsonCommon`, `KeysJson`, `LightingJson`, `MatchJson`, `WorkspaceJson`,
  `DocumentCodec`) behind the unchanged `src/core/Persistence.h`.
- `src/context/WorkspaceStateCodec.{h,cpp}` holds the pure workspace decode
  codec; `src/context/InventoryPayload.{h,cpp}` holds the pure inventory payload
  parser; receiver lifecycle remains in the receiver classes.
- Build/test infrastructure: `cmake/contextdeck-tests.cmake` provides
  `contextdeck_add_unit_test()`; the 21 registered test names and COMMAND
  spellings are unchanged. `tests/support/FakeVirtualDesktopMap.h` is a shared
  test helper.
- `ui/*.qml` was not touched by the whole; the visual and binding surface is
  exactly as before the refactor.
- `remappingState` was removed as a dead placeholder; nothing replaced it.

### 4.3 M4, M3, M2, M1 — restate exactly

> M4 workspace session manager is code-accepted (Slice A on `aca6c68`, Slice B on
> `db9ddc1`); its live IRL run is deferred by explicit COOPERATOR decision. M4 is
> not closed, and code acceptance is not live or physical acceptance.

> M3 workspace-aware lighting is code-accepted on `502ae75...`; its physical
> five-zone IRL observation is deferred by explicit COOPERATOR decision. M3 is
> not closed, and code acceptance is not physical acceptance.

For M2, preserve the substantive park claims word-for-word wherever they are
restated: the named live G4 slices (Sessions 16, 19, 22, 23, 24) are accepted,
the M2 logical whole remains open, M2 is parked with G3 host-mitigated on the
authorized reference host, and full G4 remains open. M1 is recorded as
COOPERATOR-accepted IRL (historical evidence, not a later-session retest).

Do not claim whole M2, whole G4, independent G3 closure, install/remove/rollback
readiness, production readiness, autostart safety, hibernate/hybrid-sleep
support, or general coexistence. Do not rerun or reopen the named slices; do not
issue, revive, or simulate `27_deployment_00.md`; do not reopen G3.

### 4.4 Carried observations (active, non-authorizing)

1. QML runtime validation is parked (see the closure record): compile-time and
   structural evidence only; the application was never launched. A contained
   offscreen smoke test needs a separate explicit COOPERATOR decision.
2. Two long lines after the S7 documentation touch (`README.md` status block,
   `docs/testing-m2.md` closing paragraph) are a cosmetic docs item.
3. `Co-authored-by: Cursor` trailers on `1e7e9d5` and `a130b06` are a
   tool-attribution observation.
4. Access-profile policy item: `AGENTS.md` (access-profile paragraph and
   hard-rule section) and `ROADMAP.md` line 7 still declare `ChatOrchestrator`.
5. META privacy: `01_report_00.md` with a local machine path is committed in
   `0327a30` and still in the public live tree; forward removal is an open
   COOPERATOR remediation action.
6. Standing deferrals: M4 live IRL, M3 physical five-zone observation, the
   M2/G4/G3 remainders, M5, the deck layer, and G6 licensing.

### 4.5 Repository content state at `ba87ba0...`

- Registered CTest suite: 21 `add_test` names owned by `CMakeLists.txt`
  (`cmake/contextdeck-tests.cmake` provides the helper); the full suite is
  roughly 70 s. No lint configuration and no CI exist.
- `build/` and build products are git-ignored; the tree is clean.
- The repository is public-facing English documentation; the product is
  Linux/KDE Plasma 6/Wayland for one Logitech G213 Prodigy.

## 5. Selected next whole

COOPERATOR direction (2026-09-15): proceed to the next logical whole;
`ui-ux-refinement` is opened by this handout as the next whole, because the
closed whole existed to prepare it. The successor confirms the exact scope and
candidate work with the COOPERATOR in Discovery before issuing any prompt.

Why `ui-ux-refinement` is the immediately tractable whole: the codebase is now
decomposed and tractable, `AppController` is a stable facade, and `ui/*.qml` is
untouched, so UI/UX work starts from a clean base; it needs no input-device,
broker, service, or physical-testing step, and it naturally resolves the parked
QML runtime validation through bounded COOPERATOR-observed desktop runs of the
session application.

Alternatives considered and their current disposition (deep review at closure):

- **M5 / G8 production-autostart readiness**: highest integrity value, but it
  requires host operations (install/enable/rollback, ACLs, services) and
  live/physical verification; autostart stays forbidden until those gates pass,
  and physical testing is deferred by the COOPERATOR. Blocked until that
  decision changes.
- **M3 physical five-zone observation and M4 live IRL runs**: acceptance
  activities that need the live desktop and the physical keyboard; deferred by
  explicit COOPERATOR decision. They are not construction wholes.
- **Hibernate/hybrid-sleep and general input-remapper coexistence**: the
  remaining M2/G4 work; needs live suspend cycles and a real input-remapper
  environment; same deferral.
- **G6 licensing decision**: a COOPERATOR-owned decision plus a small
  documentation action, not a whole for a Worker; obtain it early in the next
  whole or as a stand-alone bounded task before release-oriented work.
- **Super-key deck layer**: a product feature brainstorm that would touch the
  input path and the broker; premature while G4/M5 gates remain open.
- **Release/distribution readiness whole**: depends on M5 and G6; premature.

## 6. Hard invariants and safety constraints

- Logitech G213 Prodigy only, USB `046d:c336`; no other keyboards, no generic
  remapper, no Windows/macOS. Target CachyOS/Arch, Plasma 6, KWin, Wayland,
  systemd, C++20/Qt6/KF6.
- Exactly five RGB zones, never per-key RGB. Zone order: Left Area, Middle
  Area, Right Area, Arrow and Homekeys, Numpad.
- Foreground context is event-driven through the KWin scripting API — no
  `xdotool`/`wmctrl`/`xprop`, no title polling. Prefer KWin
  `desktopFileName`/`resourceClass`/`resourceName` identity.
- The session app never opens raw keyboard event nodes; only the input broker
  does, and it starts disabled. Never automatically ARM after restart, resume,
  crash, or reconnect. No live grab without the handout-§29 safety acceptance
  and an independently verified recovery route.
- One persistent OpenRGB SDK connection on loopback (protocol 5); one RGB
  backend at a time.
- Typed action/launch model only: no arbitrary shell strings. Fail-safe default
  is pass-through; unset means inherit.
- Game Mode and Backlight are firmware-only; never substitute PrintScreen or
  Pause.
- Never log ordinary typed keystrokes, raw event lines, key names, scan values,
  typed content, serials, host addresses, host keys, passwords, private paths,
  desktop IDs/names, or window captions in code logs, prompts, reports, or META.
- Suspend goes through logind/systemd policy, never `/sys/power/state`.
  Display-off must not alter KScreen topology. input-remapper is not inspected
  or reconfigured. `kwinrulesrc` is never written. No autostart until the M5
  gates pass.
- Licensing is unresolved (root `LICENSE` is MIT but the COOPERATOR has not
  selected the license; gate G6). Never copy external code before an explicit
  compatible decision.
- Behavior preservation was the closed whole's claim; it does not license
  behavior changes now. Any later change follows its own accepted whole.

## 7. AP bootstrap is mandatory

Before the first routing decision, read the consumer root `AGENTS.md` and the AP
documents named by its managed block, at minimum:

- `.ap/AP.md`;
- `.ap/AP_ORCHESTRATOR.md`;
- `.ap/PROMPT_CONTRACTS.md`;
- `.ap/AP_WORKER.md`;
- advisory `.ap/INFOSEC.md`.

Apply AP Continuation Bootstrap: restore and reconcile read-only from AP,
current canonical repositories, direct public refs, accepted durable records,
then historical trace; classify verified facts, Worker observations, COOPERATOR
decisions, proposals, inferences, and missing evidence separately; select
exactly one bounded logical whole (or preserve an already selected whole); and
issue one complete prompt with its own fresh/current target, coordinates,
capability route, evidence tier, authority, allowlist, persistence, and stop
conditions.

Verify the AP tuple and run `./.ap/ap doctor` (expected PASS, variant `stable`).
The `.ap` submodule is a pinned detached checkout equal to the containing repo
gitlink; do not attach or update it.

## 8. Mandatory first routing rule for a new whole

For the first newly generated Worker prompt of the next whole, use an **initial
Planner** with `fresh-worker-session`, `Native planning mode: required`, manual
COOPERATOR delivery, and a complete English document: exact candidate/baseline;
public-safe path allowlist; mandatory reading; one measurable planning outcome;
no-subagent posture; report filename/destination; complete META readback;
explicit out-of-scope claims; and terminal authority expiry. The Planner holds
repository-grounded planning only; it never implements.

Trace coordinates for a new whole: Worker session `01`, exchange `01`; prompt
`01_planning_00.md`; report `01_report_00.md`; destination
`projects/contextdesk/00/07-ui-ux-refinement/`.

The Planner's terminal report expires planning authority. Implementation
receives a separate complete prompt with `Native planning mode: not-used` and
explicit implementation authority after the plan is accepted.

## 9. Repository and META operating rules

At each bootstrap use fresh container-local clones and canonical HTTPS remotes.
Verify public refs directly with `git ls-remote`; a local `origin/main` is not
proof. If cache/DNS/HTTPS fails, record the bounded symptom, use a fresh clone,
check container-local name resolution, and retry with
`git -c http.version=HTTP/1.1`. Never use a stale clone, mirror, host
credential, or silent alternate commit.

For every new Worker exchange: select the whole and coordinates; write the
complete English prompt to its exact META path before delivery; require the
Worker to persist/verify and read back the prompt; require the Worker to write
the complete English terminal report directly to the paired META path and read
it back; have the COOPERATOR manually deliver the prompt to one Worker;
reconcile the report as a claim; and commit/push the exact prompt/report pair to
META only after the report exists, preserving earlier trace files. The
COOPERATOR owns META add/commit/push unless a later explicit grant moves it.
Product commits and pushes each need their own explicit grant in the Worker
prompt.

Do not create `NEXT_WORKER.md`, a session diary, an automated dispatcher, or a
speculative report. A handoff is not a Worker exchange and does not consume a
Worker session ordinal. In each trace, exchange `01` is stored with suffix
`_00`.

META local naming: `projects/<project>/<archive-sequence>/<logical-whole-sequence>-<logical-whole-identity>/`;
Worker files `<worker-session>_<phase>_<meta-exchange-index>.md` and
`<worker-session>_report_<meta-exchange-index>.md` with
`meta_exchange_index = Worker exchange ordinal - 1`. Handouts and closures share
their own sequence inside a whole (`00_handout.md` opening; the next is
`01_closure.md` or `01_handout.md`).

Public-safety lesson from this whole: a client-native plan artifact must never
be treated as a report or committed as one. Exchange `01` of a plan that used
client-native planning may leave a non-report artifact on the report path; the
sanctioned repair is the AP Planner-Artifact Report Completion Repair, which
renders the standard English report to the next exchange coordinates, and the
misplaced artifact is retired, never committed. Never place local machine
paths, host details, or private data in prompts, reports, notes, closures, or
handouts.

## 10. Worker prompt contract for the successor

Every prompt must be a complete English document containing, exactly once at
the opening, the persistent role, logical-whole identity, Worker session and
exchange ordinals, session target, phase, Worker profile, reasoning
recommendation, evidence tier, and Native planning mode. It must state exact
immutable product/AP/META candidate and public verification gates; one
measurable outcome; positive authority and exact path allowlist; explicit
out-of-scope claims; mandatory reading and fail-closed preflight;
host/device/secret/subagent restrictions; private-safe logging/reporting rules;
exact prompt/report META destinations, persistence owner, readback, and
archival owner; and the complete terminal report header, result vocabulary,
evidence, next step, critique, closure line, and authority-expiry sentence.

Planner prompts use Native Plan Mode `required` and never implement.
Implementation prompts use `not-used`, receive the reconciled plan, mutate only
their exact allowlist, publish one candidate, and never self-accept. Acceptance
prompts are fresh and read-only against an immutable candidate. Correctors
receive one smallest concrete finding and are followed by the required scoped
or full-fresh re-acceptance.

For refactoring slices, require behavior-preservation evidence: the full
registered suite from the exact candidate, focused tests for the refactored
unit, and an explicit statement that no observable behavior, string, signal,
property, or visual changed. A slice that changes a semantic validator, a
structural field, or runtime behavior is not behavior-preserving and must
follow the normal acceptance route instead.

## 11. No-silent-claims rule

Never claim any of the following without the separately required evidence:

- M4 live desktop mutation, launch, placement/maximize, title fallback, or M4
  closure;
- M3 physical five-zone visibility, device-default restoration, or M3 closure;
- M2 or whole-G4 closure; independent G3 closure;
- production readiness, deployment, installation, rollback, autostart,
  hibernate, hybrid sleep, or system integration;
- general input-remapper coexistence;
- M5 integration, the deck layer, per-key RGB, or measured control-to-zone
  placement;
- any fresh rerun of the historical Session 27 deployment;
- UI/UX behavior, QML runtime behavior, or visual outcomes never exercised;
- that the closed whole's refactoring changed nothing observable beyond what
  its suite, probes, and byte-level checks actually established.

Keep implementation-PASS, acceptance-PASS, production acceptance, and
logical-whole closure as separate facts. Worker reports always use
`Logical-whole closure: not-closed`; only the Orchestrator can close after all
AP, COOPERATOR, evidence, residual-risk, and active-mutation conditions are
satisfied.

## 12. Open COOPERATOR decisions for the successor to obtain early

1. Confirm the `ui-ux-refinement` scope: which candidate work it includes
   (pages, flows, visual language, accessibility, tray behavior), and whether
   any part needs anything beyond session-application UI work.
2. Decide the `AGENTS.md`/`ROADMAP.md` access-profile wording (full local
   Orchestrator vs the current `ChatOrchestrator` declaration). The exact
   proposed replacement text from the closed 05 plan §3.2 A3 / §3.3 B1 remains
   unimplemented.
3. Confirm that physical testing remains deferred (M4 live IRL, M3 physical,
   M2 remainders).
4. Decide whether G6 licensing should be resolved before release-oriented work.
5. Decide the META privacy remediation: the recommended forward removal commit
   for `01_report_00.md` (history retains the bytes; a history rewrite is
   destructive, needs explicit authority, and is not recommended), or an
   explicit decision to carry it.
6. Optionally decide whether a contained offscreen QML smoke test is authorized
   for a later bounded exchange.

## 13. Immediate successor checklist

1. Verify actual client, model, reasoning, permission, filesystem, network,
   command, Git, and write capabilities without probing credentials.
2. Read AP, root `AGENTS.md`, current ContextDesk source/docs, the closed whole
   trace `06-...`, and this handout.
3. Fresh-clone and verify product `ba87ba0...` (or a later verified
   descendant), AP `0cf2cff...`, and META `11245c5...` or later against direct
   public refs; run `./.ap/ap doctor`.
4. Author the opening `00_notes.md` in
   `projects/contextdesk/00/07-ui-ux-refinement/` with the reconciliation
   record, selected delivery, and the first exchange reference.
5. Present the restored state in Slovak with one evidence-backed
   recommendation, and obtain the section 12 COOPERATOR decisions.
6. Issue the initial Planner prompt (section 8) as Worker session `01` /
   exchange `01`, write it into
   `projects/contextdesk/00/07-ui-ux-refinement/`, and leave manual delivery to
   the COOPERATOR.
7. Continue one bounded whole at a time, with all prompts and reports durably
   archived.

## 14. Status sentence

Use this wording until a later whole has its own accepted evidence:

> `code-health-and-refactoring` is closed by ORCHESTRATOR (acceptance-PASS on
> `ba87ba08c17ad965d008c4fc1d59f42a7c7700c9`; publication-PASS; QML runtime
> validation parked; META privacy remediation open). M4, M3, and M2 logical
> wholes remain not closed with their standing deferrals; M1 remains historical
> IRL acceptance. The next whole is `ui-ux-refinement`, opened by this handout.
