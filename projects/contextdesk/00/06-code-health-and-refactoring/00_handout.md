# Fresh full Orchestrator handoff — ContextDesk / ContextDeck before the code-health whole

You are a genuinely fresh, full-capability ORCHESTRATOR for ContextDesk (the
application is also called ContextDeck). This is the continuity record from the
full-Orchestrator session that closed `m4-state-and-ledger-reconciliation` and
prepared the next logical whole. It is not a Worker prompt, not a plan, not an
acceptance report, and not authority to claim closure. Reconcile it against the
current public repositories and the pinned Analytic Programming (AP) protocol
before taking action.

Handoff artifact filename: `00_handout.md`
META destination:
`projects/contextdesk/00/06-code-health-and-refactoring/00_handout.md`
Handoff authorship: ORCHESTRATOR
Handoff persistence/archive owner: COOPERATOR or the successor full ORCHESTRATOR
only after exact-byte verification
Paired orchestration notes: the successor authors `00_notes.md` at opening
Restoration readiness (author's classification): **PASS** — public refs verified
directly, prior whole closed with its record committed, no active Worker, no
active mutation
Continue only through separately bounded logical wholes.

## 1. Successor capability profile and access

Successor capability profile: `Orchestrator` (full project orchestration).
Access profile: full project orchestration when the session environment exposes
those capabilities; not read-only ChatOrchestrator.
Primary project: `https://github.com/cisarik/contextdesk`
Evidence archive: `https://github.com/cisarik/meta.git`

The successor is expected, when its actual execution environment exposes those
capabilities, to run repository commands and scripts, inspect source and tests,
read and write the ContextDesk product repository, read and write the META
repository, create commits, push canonical branches, and verify public refs.
Capability must be directly observed in the new session; this text never turns
an unavailable tool or credential into authority.

Repository write authority is project-scoped and AP-governed. Use disposable
inspection clones for public verification and a deliberate clean product/META
worktree for authorized writes. Preserve unrelated owner work. Never use broad
destructive Git recovery or silently retarget a changed public baseline.

The COOPERATOR (Michal) remains the human owner of material objective changes,
cost, privacy, irreversibility, physical behavior, host-side action, and
residual risk. Do not infer host or device authority from repository write
authority. Host commands, service changes, input-device access, desktop-manager
mutation, application launch, bridge reload, physical keyboard tests,
credentials, and external-account actions each require their own explicit
bounded COOPERATOR decision, recovery plan, and AP surface gate.

Policy note: `AGENTS.md` still declares the `ChatOrchestrator` access profile
while the last two wholes ran as full local Orchestrator sessions with direct
access to the project checkout. That wording is an explicit, still-open
COOPERATOR policy item (section 4.3). Do not silently change it; reconcile it
with the COOPERATOR.

**COOPERATOR direction carried forward (2026-09-14):**

- Physical keyboard testing is **not** being scheduled now. M4 live IRL and M3
  physical five-zone observation remain deferred; M2 remains parked.
- The next whole is **code health / logical refactoring**; any improvement or
  fix is welcome.
- The whole's purpose is to make the codebase clean and tractable **before a
  later UI/UX whole run by another Orchestrator**; UI/UX work itself is out of
  scope here.
- Manual delivery, one active Worker, no subagents; the successor keeps the
  COOPERATOR meaningfully informed and obtains the material decisions.

## 2. Language and routing contract

- Speak **Slovak** with Michal/COOPERATOR.
- Write every Worker prompt and every formal Worker report in **English**.
- Keep each task one complete bounded prompt, one fresh or explicitly renewed
  Worker session, one terminal report, and one reconciliation.
- Manual delivery is preserved: the COOPERATOR carries every prompt and report.
  Do not spawn, auto-dispatch, parallelize, or silently route Workers.
- Prepare the complete prompt, write it to the exact META trace path, tell the
  COOPERATOR the exact file and routing state, and let the COOPERATOR manually
  carry it to the chosen fresh Worker.
- Default topology is one active Worker. A parallel or internal-audit exception
  needs an explicit bounded COOPERATOR decision, disjoint ownership, and AP
  evidence; it never creates independent acceptance automatically.

## 3. Verified public identity snapshot at handoff

These values were directly checked against canonical public refs immediately
before this handoff. The successor must re-check them in its own environment.

| Item | Verified value |
|---|---|
| Product remote / branch | `https://github.com/cisarik/contextdesk` / `main` |
| Product current public HEAD | `235d467c752958694dad4be7bcc31e66406dbdcc` |
| Product previous HEAD | `293158887e4228a42b9c64bda7d4f0bd32fb4ec9` |
| M4 Slice A / Slice B accepted | `aca6c68542bbc9f1b8ee891415a04b0a95e8372e` / `db9ddc1f923f44e26f7f1d58df7446306bbdf64a` |
| M3 code-accepted | `502ae75571358ec95d33c836084b5e2253850731` |
| M2 inactive-install candidate | `cb72ae0388307b514182efc6936712e3da42cda4` |
| Product AP gitlink and checkout | `0cf2cff483a36a4cc2254aa424a7c53bd57a97e9` |
| Public AP `main` (not adopted) | `717ecb6cfb7c71eda12ff4e3e0b02f28101c59bf` |
| META remote / branch | `https://github.com/cisarik/meta.git` / `main` |
| META current public HEAD | `f283f24a0f13fcbfe7819700ff8eaaac33817968` |
| Closed whole trace | `projects/contextdesk/00/05-m4-state-and-ledger-reconciliation/` |
| New whole trace | `projects/contextdesk/00/06-code-health-and-refactoring/` |
| M4 trace | `projects/contextdesk/00/04-g213-contextdeck-workspace-session-manager/` |
| M3 trace | `projects/contextdesk/00/03-g213-contextdeck-workspace-aware-lighting/` |
| M2 trace | `projects/contextdesk/00/02-g213-contextdeck-input-passthrough-safety/` |
| M1 trace | `projects/contextdesk/00/01-g213-contextdeck-mvp-context-lighting/` |

Key closed-whole artifact SHA-256 values (verify against the actual files):

| Artifact | SHA-256 |
|---|---|
| `05-.../01_report_00.md` (accepted plan) | `8b282335b703db2a6133bce97d26e89481fe0b5f45b196e339ce8ee3d3d33cf0` |
| `05-.../01_implementation_01.md` | `bbb8b0d4bccbf557e8e6733351c00f0f5a15ca044eaf8cccdc76de8eb2deb004` |
| `05-.../01_report_01.md` (implementation) | `ec0b38a1ac23ee969ebb3f35d706c08525e2804a5b1137cf8158c3ee16386595` |
| `05-.../02_acceptance_00.md` | `a4993a3d907d0c6cac8d7d0233dc97f69e4f1425d40e648a0a7123c193f9e212` |
| `05-.../02_report_00.md` (acceptance) | `6e38aeed261103800556374e26ef0bcfb9cecc4a0dc4138580faf6df63705b06` |
| `05-.../01_closure.md` | `dd746636c802e7c391e0afa51a2fcecde27af1b1b5be31d96712bbbef3004fab` |
| `05-.../00_handout.md` (opening) | `3ee8ac42f93adc1ae6788b39a29573f3b56bf892b37ed2be4d47d1575094e65f` |

AP `main` has advanced beyond the pinned gitlink. That is expected for a
consumer pin and is **not** an adopted update. Do not move the gitlink.

## 4. Current truthful state

### 4.1 `m4-state-and-ledger-reconciliation` — closed by ORCHESTRATOR

- Objective: reconcile the product repository's durable state documentation with
  the already-accepted M1–M4 code state and dispose of the six carried M4 ledger
  candidates.
- Result: accepted plan; one implementation commit `235d467...`
  (`Reconcile M4 state docs and dispose of carried ledger candidates`), 14 paths,
  `+227/-128`; fresh independent acceptance `02_report_00.md` (A1–A8 all PASS,
  both adversarial leads confirmed, causal parent-overlay probe, focused CTest
  4/4, full registered 21/21); direct public `main` equals the candidate.
- Ledger dispositions: ADR status phrasing, typed `.desktop`-id predicate,
  `KApplicationTrader` naming, and the late-`desktopCreated` checkpoint
  disclosure are **fixed**; QML runtime validation is **parked** with the
  deferred M4 live IRL run; the orphaned old-path checkpoint is **invalidated**
  with no action.
- Closure record `01_closure.md`; `Logical-whole closure:
  closed-by-ORCHESTRATOR`; notes frozen. No live host, device, desktop, launch,
  broker, packaging, or license operation occurred anywhere in that whole.

### 4.2 M4, M3, M2, M1 — unchanged; restate exactly

> M4 workspace session manager is code-accepted (Slice A on `aca6c68`, Slice B on
> `db9ddc1`); its live IRL run is deferred by explicit COOPERATOR decision. M4 is
> not closed, and code acceptance is not live or physical acceptance.

> M3 workspace-aware lighting is code-accepted on `502ae75...`; its physical
> five-zone IRL observation is deferred by explicit COOPERATOR decision. M3 is
> not closed, and code acceptance is not physical acceptance.

For M2, preserve the substantive park claims word-for-word wherever they are
restated: the named live G4 slices (Sessions 16, 19, 22, 23, 24) are accepted,
the M2 logical whole remains open, M2 is parked with G3 host-mitigated on the
authorized reference host, and full G4 remains open. M1 is recorded as
COOPERATOR-accepted IRL (historical evidence, not a later-session retest).

Do not claim whole M2, whole G4, independent G3 closure, install/remove/rollback
readiness, production readiness, autostart safety, hibernate/hybrid-sleep
support, or general coexistence. Do not rerun or reopen the named slices; do not
issue, revive, or simulate `27_deployment_00.md`; do not reopen G3.

### 4.3 Active carried observations from the closed whole

1. **Two documentation lines outside the closed sweep** (the acceptance
   explicitly confirmed they do not block): the `ROADMAP.md` M2-backlog
   sleep-hook bullet (~429–432) ending "Not autostart. Not live suspend
   evidence. ADR 0001."; and `docs/testing-m2.md` (~12–14 and ~250–251)
   remaining-G4 lists naming LED return / all-control / live suspend /
   input-remapper. Absorb them into this whole or park them with evidence.
2. **Access-profile policy item.** `AGENTS.md` (~52–55, ~95–96) and
   `ROADMAP.md` (~7) still declare `ChatOrchestrator`, while the last two
   successor handoffs describe a full local Orchestrator session. The exact
   full-local wording proposed in the closed plan §3.2 A3 / §3.3 B1 remains
   unimplemented and requires an explicit COOPERATOR decision before any change.
3. **Transient wording.** `README.md` and `ROADMAP.md` call
   `m4-state-and-ledger-reconciliation` the "current bounded whole"; the next
   reconciliation should update that phrasing.
4. **Standing deferrals.** M4 live IRL, M3 physical five-zone observation, the
   M2/G4/G3 remainders, M5, the Super-key deck layer, and the G6 licensing
   decision remain separately governed future decisions.
5. **Documentation-accuracy item for re-verification.**
   `ROADMAP.md` (~456–459) still lists "production safety gaps already visible
   in source (silent uinput write errors; virtual-device capabilities from
   `passthroughCapabilities()` rather than measured source bits / LED return
   path; logind `sd_pid_get_session` vs user-manager-launched session apps)".
   Current repository evidence indicates the first two are already addressed:
   `src/broker/ForwardingEngine.cpp` fails closed on a sink write failure
   (`sink-write-failed`, returns false and stops the ingest loop), and
   `src/broker/ProductionRuntime.cpp` applies
   `applyMeasuredCapabilities()` before grab with `passthroughCapabilities()`
   used only by tests. `src/broker/SessionIpc.cpp` (`LogindSeatAuthorizer`) has
   an exactly-one-eligible-session fallback for pids without a session. Re-verify
   each item against code and tests at the working candidate and correct the
   list with exact evidence; do not repeat unverified claims.

### 4.4 Repository content state at `235d467...`

- Registered CTest suite: 21 `add_test` names owned by `CMakeLists.txt`; the
  full suite is ~70 s. No lint configuration and no CI exist.
- `build/` and build products are git-ignored; the tree is clean.
- The repository is public-facing English documentation; the product is
  Linux/KDE Plasma 6/Wayland for one Logitech G213 Prodigy.

## 5. Selected next whole (COOPERATOR direction): `code-health-and-refactoring`

**Objective (proposed; confirm with the COOPERATOR):** reduce structural
complexity, duplication, and mixed responsibilities in the session-app, core,
and UI-facing code through **behavior-preserving refactoring**, so the later
UI/UX whole works against clean, cohesive, testable components — without
changing behavior, visuals, product claims, or safety boundaries.

**Method:** the initial Planner returns one decision-complete plan with ordered,
behavior-preserving slices; each slice has an exact changed-path allowlist,
focused tests, and the full registered suite as the broad gate. Because AP
budgets one primary independent audit per logical whole, plan the independent
acceptance as **one cumulative fresh acceptance** after the slices (plus at most
one correction re-audit) — or return a justified proposal to split the work
into several wholes with their own selection. The Planner decides slice order
and exact boundaries; it must not reopen accepted decisions.

**Candidate slices with evidence at `235d467...` (the Planner decides
selection, order, boundaries, and allowlists):**

S1 — **Build and test infrastructure (lowest risk; likely first).**
`CMakeLists.txt` (283 lines) repeats 21 near-identical `add_executable` +
`target_link_libraries` + `add_test` triples (from `test_profile_resolver` to
`test_application_launcher`); propose a small `contextdeck_add_test()` helper.
Broker test doubles (`FakeSink`, `FakeSource`, `FakeGrabber` in `src/broker/`)
are compiled into `contextdeck_broker_core` and are used by the in-tree
selftests (`src/broker/Selftest.cpp`, `src/broker/WatchdogSelftest.cpp`) as well
as by tests; decide, with a documented rationale, whether to keep them or move
them to a test-support target linked by both. There is no `.clang-format` /
`.clang-tidy`; adding configuration is optional, and a codebase-wide reformat
commit is prohibited (format only touched files per slice).

S2 — **Split `src/core/Persistence.cpp` (1483 lines).** A 908-line anonymous
namespace (lines ~17–925) holds ~30 parse/serialize helpers (`parseChord`,
`parseAssignment`, `parseKeys`, zone parsers v2/v3, `parseLightingV1/Common`,
`parseMatch`, `parseTitleFallback`, `parseWorkspaceAssignment/Session`,
`parsePreferences`; `assignmentToJson`, `keysToJson`, `lightingToJson`,
`titleFallbackToJson`, `workspaceAssignmentToJson`, `workspaceSessionToJson`).
`ProfileStore::parseDocument` is ~265 lines, `validate` ~108, `toJson` ~60, IO
~90. Proposed: cohesive `src/core/persistence/` units behind the existing public
API, no schema or behavior change; `test_profile_persistence` (1098 lines) plus
the full suite as the gate.

S3 — **Decompose `AppController` (god object).** `src/app/AppController.h` is
277 lines with 48 `Q_PROPERTY`, 52 `Q_INVOKABLE`, and 71 public accessors;
`src/app/AppController.cpp` is 2285 lines with 134 function definitions. Mixed
responsibilities: QML presentation strings/hero/diagnostics; lighting resolution
and OpenRGB push; document/profile editing (~20 setters); workspace session
editing (~20 invokables); workspace preview/apply/revert/launch orchestration;
broker lease (`armPassThrough`/`disarmPassThrough`/`releaseBrokerLease`); power
actions; context/identity handling. 83 unique `app.*` bindings across 9 QML
files (`app.diagnostics` ×34; `workspace*` ≈45; lighting/zone ≈20;
profile/inventory ≈10). Proposed decomposition (Planner decides): e.g.
`LightingController`, `ProfileEditor`, `WorkspaceSessionEditor` +
`WorkspaceApplyController`, `PresentationModel`, `BrokerLeaseController`, with
`AppController` as thin composition or several QML context properties; QML
bindings update mechanically with **no visual change**. `test_workspace_lighting`
(811 lines) drives the controller directly and must keep equivalent behavioral
coverage.

S4 — **Receivers: extract pure codecs, reduce mixed D-Bus concerns.**
`src/context/WorkspaceReceiver.cpp` (834 lines) implements subscription,
owner resolution, coalescing, request ownership, deadlines, and recovery, plus a
pure `decodeSnapshot` (~197 lines, ~577–773) that can become a
`WorkspaceStateCodec` tested without D-Bus. `src/context/ContextReceiver.cpp`
(676 lines) similarly mixes transport lifecycle and parsing; extract pure parts
only where duplication is real and avoid over-abstraction. Keep receiver
semantics identical; `test_workspace_receiver` (1324 lines, private
`dbus-run-session` fake manager) remains the gate.

S5 — **Types and dead API.** `src/core/Types.h` (476 lines) mixes enums,
structs, and inline helpers; an optional split into focused headers is
acceptable if it lowers include coupling. `AppController::remappingState` is a
placeholder constant (`"inactive-until-M2"`) with no QML consumer; remove or
wire it. Sweep for other unused controller members by generating the QML usage
table.

S6 — **Test organization.** Largest tests: `test_workspace_receiver.cpp` 1324,
`test_profile_persistence.cpp` 1098, `test_workspace_mutator.cpp` 1015,
`test_workspace_lighting.cpp` 811, `test_profile_resolver.cpp` 624,
`test_broker_ipc.cpp` 643, `test_broker_watchdog.cpp` 553. `FakeDesktopManager`
is duplicated in two test files; extract shared helpers under a tests-support
location while keeping registered test names stable (`CMakeLists.txt` owns
them).

S7 — **Documentation accuracy (absorbs the carried items).** Re-verify and
correct the ROADMAP M2 "production safety gaps" list (section 4.3.5); fix or
park the two carried documentation lines; update the transient "current whole"
wording; record the closed whole's final state if needed. The access-profile
wording changes only with an explicit COOPERATOR decision.

**Explicit exclusions for this whole:** no UI/UX redesign or visual change (that
is a later whole with another Orchestrator); no behavior change; no new
features; no broker grab/ARM/pass-through semantics change; no live
host/device/desktop/launch/suspend/autostart operation; no `.ap/` change; no
dependency additions unless the Planner proves one is necessary and the
COOPERATOR approves; no specification or claim changes beyond internal structure
and the carried documentation-accuracy fixes; no reopening of M2/M3/M4; no
codebase-wide reformat commit; no renumbering of accepted trace artifacts.

## 6. Hard invariants and safety constraints

- Logitech G213 Prodigy only, USB `046d:c336`; no other keyboards, no generic
  remapper, no Windows/macOS. Target CachyOS/Arch, Plasma 6, KWin, Wayland,
  systemd, C++20/Qt6/KF6.
- Exactly five RGB zones, never per-key RGB. Zone order: Left Area, Middle
  Area, Right Area, Arrow and Homekeys, Numpad.
- Foreground context is event-driven through the KWin scripting API — no
  `xdotool`/`wmctrl`/`xprop`, no title polling. Prefer KWin
  `desktopFileName`/`resourceClass`/`resourceName` identity.
- The session app never opens raw keyboard event nodes; only the input broker
  does, and it starts disabled. Never automatically ARM after restart, resume,
  crash, or reconnect. No live grab without the handout-§29 safety acceptance
  and an independently verified recovery route.
- One persistent OpenRGB SDK connection on loopback (protocol 5); one RGB
  backend at a time.
- Typed action/launch model only: no arbitrary shell strings. Fail-safe default
  is pass-through; unset means inherit.
- Game Mode and Backlight are firmware-only; never substitute PrintScreen or
  Pause.
- Never log ordinary typed keystrokes, raw event lines, key names, scan values,
  typed content, serials, host addresses, host keys, passwords, private paths,
  desktop IDs/names, or window captions in code logs, prompts, reports, or META.
- Suspend goes through logind/systemd policy, never `/sys/power/state`.
  Display-off must not alter KScreen topology. input-remapper is not inspected
  or reconfigured. `kwinrulesrc` is never written. No autostart until the M5
  gates pass.
- Licensing is unresolved (root `LICENSE` is MIT but the COOPERATOR has not
  selected the license; gate G6). Never copy external code before an explicit
  compatible decision.
- Behavior-preserving refactoring must preserve every invariant above; a slice
  that cannot is out of scope and stops `PARTIAL`.

## 7. AP bootstrap is mandatory

Before the first routing decision, read the consumer root `AGENTS.md` and the AP
documents named by its managed block, at minimum:

- `.ap/AP.md`;
- `.ap/AP_ORCHESTRATOR.md`;
- `.ap/PROMPT_CONTRACTS.md`;
- `.ap/AP_WORKER.md`;
- advisory `.ap/INFOSEC.md`.

Apply AP Continuation Bootstrap: restore and reconcile read-only from AP,
current canonical repositories, direct public refs, accepted durable records,
then historical trace; classify verified facts, Worker observations, COOPERATOR
decisions, proposals, inferences, and missing evidence separately; select
exactly one bounded logical whole (or preserve an already selected whole); and
issue one complete prompt with its own fresh/current target, coordinates,
capability route, evidence tier, authority, allowlist, persistence, and stop
conditions.

Verify the AP tuple and run `./.ap/ap doctor` (expected PASS, variant `stable`).
The `.ap` submodule is a pinned detached checkout equal to the containing repo
gitlink; do not attach or update it.

## 8. Mandatory first routing rule for the new whole

For the first newly generated Worker prompt of `code-health-and-refactoring`,
use an **initial Planner** with `fresh-worker-session`, `Native planning mode:
required`, manual COOPERATOR delivery, and a complete English document:
exact candidate/baseline; public-safe path allowlist; mandatory reading; one
measurable planning outcome; no-subagent posture; report filename/destination;
complete META readback; explicit out-of-scope claims; and terminal authority
expiry. The Planner holds repository-grounded planning only; it never
implements.

Trace coordinates: Worker session `01`, exchange `01`; prompt
`01_planning_00.md`; report `01_report_00.md`; destination
`projects/contextdesk/00/06-code-health-and-refactoring/`.

The Planner's terminal report expires planning authority. Implementation
receives a separate complete prompt with `Native planning mode: not-used` and
explicit implementation authority after the plan is accepted.

## 9. Repository and META operating rules

At each bootstrap use fresh container-local clones and canonical HTTPS remotes.
Verify public refs directly with `git ls-remote`; a local `origin/main` is not
proof. If cache/DNS/HTTPS fails, record the bounded symptom, use a fresh clone,
check container-local name resolution, and retry with
`git -c http.version=HTTP/1.1`. Never use a stale clone, mirror, host
credential, or silent alternate commit.

For every new Worker exchange: select the whole and coordinates; write the
complete English prompt to its exact META path before delivery; require the
Worker to persist/verify and read back the prompt; require the Worker to write
the complete English terminal report directly to the paired META path and read
it back; have the COOPERATOR manually deliver the prompt to one Worker;
reconcile the report as a claim; and commit/push the exact prompt/report pair to
META only after the report exists, preserving earlier trace files. The COOPERATOR
owns META add/commit/push unless a later explicit grant moves it. Product
commits and pushes each need their own explicit grant in the Worker prompt.

Do not create `NEXT_WORKER.md`, a session diary, an automated dispatcher, or a
speculative report. A handoff is not a Worker exchange and does not consume a
Worker session ordinal. In each trace, exchange `01` is stored with suffix `_00`.

META local naming: `projects/<project>/<archive-sequence>/<logical-whole-sequence>-<logical-whole-identity>/`;
Worker files `<worker-session>_<phase>_<meta-exchange-index>.md` and
`<worker-session>_report_<meta-exchange-index>.md` with
`meta_exchange_index = Worker exchange ordinal - 1`. Handouts and closures share
their own sequence inside a whole (`00_handout.md` opening; the next is
`01_closure.md` or `01_handout.md`).

## 10. Worker prompt contract for the successor

Every prompt must be a complete English document containing, exactly once at the
opening, the persistent role, logical-whole identity, Worker session and
exchange ordinals, session target, phase, Worker profile, reasoning
recommendation, evidence tier, and Native planning mode. It must state exact
immutable product/AP/META candidate and public verification gates; one
measurable outcome; positive authority and exact path allowlist; explicit
out-of-scope claims; mandatory reading and fail-closed preflight;
host/device/secret/subagent restrictions; private-safe logging/reporting rules;
exact prompt/report META destinations, persistence owner, readback, and archival
owner; and the complete terminal report header, result vocabulary, evidence,
next step, critique, closure line, and authority-expiry sentence.

Planner prompts use Native Plan Mode `required` and never implement.
Implementation prompts use `not-used`, receive the reconciled plan, mutate only
their exact allowlist, publish one candidate, and never self-accept. Acceptance
prompts are fresh and read-only against an immutable candidate. Correctors
receive one smallest concrete finding and are followed by the required scoped or
full-fresh re-acceptance.

For refactoring slices, require behavior-preservation evidence: the full
registered suite from the exact candidate, focused tests for the refactored
unit, and an explicit statement that no observable behavior, string, signal,
property, or visual changed. A slice that changes a semantic validator, a
structural field, or runtime behavior is not behavior-preserving and must
follow the normal acceptance route instead.

## 11. No-silent-claims rule

Never claim any of the following without the separately required evidence:

- M4 live desktop mutation, launch, placement/maximize, title fallback, or M4
  closure;
- M3 physical five-zone visibility, device-default restoration, or M3 closure;
- M2 or whole-G4 closure; independent G3 closure;
- production readiness, deployment, installation, rollback, autostart,
  hibernate, hybrid sleep, or system integration;
- general input-remapper coexistence;
- M5 integration, the deck layer, per-key RGB, or measured control-to-zone
  placement;
- any fresh rerun of the historical Session 27 deployment;
- that refactoring changed nothing observable unless the full suite and the
  focused evidence actually establish it.

Keep implementation-PASS, acceptance-PASS, production acceptance, and
logical-whole closure as separate facts. Worker reports always use
`Logical-whole closure: not-closed`; only the Orchestrator can close after all
AP, COOPERATOR, evidence, residual-risk, and active-mutation conditions are
satisfied.

## 12. Open COOPERATOR decisions for the successor to obtain early

1. Confirm the whole identity and scope `code-health-and-refactoring`, and which
   candidate slices (S1–S7) are in the first plan.
2. Decide the `AGENTS.md`/`ROADMAP.md` access-profile wording (full local
   Orchestrator vs the current ChatOrchestrator declaration).
3. Confirm that physical testing stays deferred (M4 live IRL, M3 physical).
4. Decide whether G6 licensing should be resolved before release-oriented work.
5. Confirm whether the two carried documentation lines and the ROADMAP M2
   "production safety gaps" list are absorbed into this whole or parked.

## 13. Immediate successor checklist

1. Verify actual client, model, reasoning, permission, filesystem, network,
   command, Git, and write capabilities without probing credentials.
2. Read AP, root `AGENTS.md`, current ContextDesk source/docs, the closed whole
   trace `05-...`, and this handout.
3. Fresh-clone and verify product `235d467...` (or a later verified descendant),
   AP `0cf2cff...`, and META `f283f24...` (or later) against direct public refs;
   run `./.ap/ap doctor`.
4. Author the opening `00_notes.md` for the new whole with the reconciliation
   record, selected delivery, and the first exchange reference.
5. Present the restored state in Slovak with one evidence-backed recommendation,
   and obtain the section 12 COOPERATOR decisions.
6. Issue the initial Planner prompt (section 8) as Worker session `01` /
   exchange `01`, write it into
   `projects/contextdesk/00/06-code-health-and-refactoring/`, and leave manual
   delivery to the COOPERATOR.
7. Continue one bounded whole at a time, with all prompts and reports durably
   archived.

This handoff ends here. It grants no automatic Worker dispatch, physical host
operation, live desktop mutation, or logical-whole closure. All subsequent
authority must be issued by the fresh full Orchestrator under the current AP and
explicit COOPERATOR governance.

## 14. Status sentence

Use this wording until a later whole has its own accepted evidence:

> `m4-state-and-ledger-reconciliation` is closed by ORCHESTRATOR (acceptance-PASS
> on `235d467...`; publication-PASS; ledger dispositions recorded). M4, M3, and
> M2 logical wholes remain not closed with their standing deferrals; M1 remains
> historical IRL acceptance. The next whole is `code-health-and-refactoring`,
> beginning with a fresh Planner selected with the COOPERATOR.
