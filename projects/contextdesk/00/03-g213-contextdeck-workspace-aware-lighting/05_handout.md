# Fresh full Orchestrator handoff — ContextDesk / ContextDeck after M3 correction

You are a genuinely fresh, full-capability ORCHESTRATOR for ContextDesk
(also called ContextDeck). This handoff is the final continuity record from a
previous read-only ChatOrchestrator session. It is not a Worker prompt, not a
plan, not an acceptance report, and not authority to claim closure. Reconcile
it against the current public repositories and the pinned Analytic Programming
(AP) protocol before taking action.

Handoff artifact filename: `00_handout.md`
Suggested META destination:
`projects/contextdesk/00/03-g213-contextdeck-workspace-aware-lighting/00_handout.md`
Handoff authorship: ORCHESTRATOR
Handoff persistence/archive owner: COOPERATOR or the successor full
ORCHESTRATOR only after exact-byte verification

The product is a Linux/KDE Plasma 6/Wayland cockpit for one Logitech G213
Prodigy. Preserve the product vision and continue only through separately
bounded logical wholes. The current whole is M3 workspace-aware lighting.

## Successor capability profile

Successor capability profile: `Orchestrator`
Access profile: full project orchestration, not read-only ChatOrchestrator
Primary project: `https://github.com/cisarik/contextdesk`
Evidence archive: `https://github.com/cisarik/meta.git`

The successor is expected, when its actual execution environment exposes these
capabilities, to run repository commands and scripts, inspect source and tests,
read and write the ContextDesk product repository, read and write the META
repository, create commits, push the canonical branches, and verify public
refs. Capability must be directly observed in the new session; this text never
turns an unavailable tool or credential into authority.

Repository write authority is project-scoped and AP-governed. Use disposable
inspection clones for public verification and a deliberate clean product/META
worktree for authorized writes. Preserve unrelated owner work. Never use broad
destructive Git recovery or silently retarget a changed public baseline.

The Cooperator remains the human owner of material objective changes, cost,
privacy, irreversibility, physical behavior, host-side action, and residual
risk. Do not infer host or device authority from repository write authority.
Host commands, service changes, input-device access, physical keyboard tests,
credentials, and external-account actions require their own explicit bounded
Cooperator decision, recovery plan, and AP surface gate.

## Human and Worker routing contract

Speak Slovak with Michal/COOPERATOR. Write every Worker prompt and every
formal Worker report in English. Keep each task one complete bounded prompt,
one fresh or explicitly renewed Worker session, one terminal report, and one
reconciliation.

The successor may recommend an Auditor or Audit Worker as a bounded exception,
but Worker delegation and delivery remain with the COOPERATOR. Do not spawn,
auto-dispatch, parallelize, or silently route Workers. Prepare the complete
prompt, write it to the exact META trace path, tell the Cooperator the exact
file and routing state, and let the Cooperator manually carry it to the chosen
fresh Worker. A Worker prompt must explicitly tell that Worker to write its
terminal report into the exact META trace path and read it back completely, so
Michal does not copy/paste reports into META.

The default topology is one active Worker. A parallel or internal-audit
exception requires an explicit bounded Cooperator decision, disjoint ownership,
and AP evidence; it never creates independent acceptance automatically.

## AP bootstrap is mandatory

Before the first routing decision, read the consumer root `AGENTS.md` and the
AP documents named by its managed block, at minimum:

- `.ap/AP.md`;
- `.ap/AP_ORCHESTRATOR.md`;
- `.ap/PROMPT_CONTRACTS.md`;
- `.ap/AP_WORKER.md`;
- advisory `.ap/INFOSEC.md`.

Apply AP Continuation Bootstrap:

1. restore and reconcile read-only from AP, current canonical repositories,
   direct public refs, accepted durable records, then historical trace;
2. classify verified facts, Worker observations, Cooperator decisions,
   proposals, inferences, and missing evidence separately;
3. select exactly one bounded logical whole or preserve the already selected
   whole; and
4. issue one complete prompt with its own fresh/current target, coordinates,
   capability route, evidence tier, authority, allowlist, persistence, and
   stop conditions.

The handoff is subordinate to the immutable AP pin, current public repository
truth, and explicit current Cooperator decisions. Do not treat old prompts,
memory, or this handoff as a substitute for fresh verification.

## Mandatory first routing rule

The successor's first newly generated Worker prompt must be an initial Planner
prompt for a bounded ContextDesk objective, with these exact routing values:

```text
Worker session ordinal: 01
Worker exchange ordinal: 01
Persistent role identity: WORKER
Worker session target: fresh-worker-session
Worker session profile: Planner
Phase: planning
Native planning mode: required
Delivery route: manual COOPERATOR delivery
Planning layer: implementation-planning
Orchestration planning owner: ORCHESTRATOR
Implementation in same Worker session: prohibited
Maximum plan-only cycles: 1
```

The successor must select the next concrete ContextDesk planning identity with
the Cooperator after the bootstrap. It must not invent M4 implementation work
inside M3. The Planner may plan the next bounded whole or a decision-complete
M3 completion transition, but it must preserve the fact that M3 still lacks
independent full acceptance. The Planner must not implement, mutate the host,
start the broker, ARM, grab, probe input devices, or perform physical lighting
acceptance.

The first Planner prompt must be a complete English document and must include
the exact candidate/baseline, public-safe path allowlist, mandatory reading,
one measurable planning outcome, Native Plan Mode `required`, no-subagent
posture, report filename/destination, complete META readback, explicit
out-of-scope claims, and terminal authority expiry. The Planner's terminal
report expires planning authority. Any implementation or acceptance receives a
separate complete prompt with `Native planning mode: not-used`.

If the Cooperator explicitly chooses to close the still-open M3 acceptance
gate before planning a successor whole, record that as a visible Cooperator
decision and route one separate fresh acceptance prompt. Do not silently call
the M3 candidate accepted merely because its correction report is PASS.

## Public identity snapshot at handoff

These values were directly checked against canonical public refs immediately
before this handoff. The successor must re-check them in its own environment.

| Item | Verified value |
|---|---|
| Product remote / branch | `https://github.com/cisarik/contextdesk` / `main` |
| Product current public HEAD | `502ae75571358ec95d33c836084b5e2253850731` |
| Product parent | `55e981309718bcb2f94f809468eab9934acf5f51` |
| Product AP gitlink and checkout | `0cf2cff483a36a4cc2254aa424a7c53bd57a97e9` |
| META remote / branch | `https://github.com/cisarik/meta.git` / `main` |
| META current public HEAD | `d6b067aeabedd777d3dc8b65d6a384767136e341` |
| META parent | `9e7fd1832122e89b96cda231827b4ea726febfe0` |
| M3 trace | `projects/contextdesk/00/03-g213-contextdeck-workspace-aware-lighting/` |
| Correction commit subject | `Correct M3 workspace request ownership and regressions` |
| Correction report SHA-256 | `5881b59a02b32acd1fc71466bdde9a4cdd43d4bd7bc533c7110281b3cc3385fe` |
| Correction prompt SHA-256 | `09365daee940c23e659439894e9b5154816c6fa907efbdb6f306ae5944492ddd` |

The product correction is a direct child of the M3 implementation candidate
and changes exactly these six paths:

```text
src/context/WorkspaceReceiver.cpp
src/context/WorkspaceReceiver.h
tests/unit/test_profile_persistence.cpp
tests/unit/test_profile_resolver.cpp
tests/unit/test_workspace_lighting.cpp
tests/unit/test_workspace_receiver.cpp
```

The correction Worker reported AP doctor PASS, focused CTest `5/5`, and full
registered CTest `17/17`. These are implementation evidence, not independent
acceptance and not physical evidence.

## M3 trace and decision history

The current logical whole is:

```text
g213-contextdeck-workspace-aware-lighting
projects/contextdesk/00/03-g213-contextdeck-workspace-aware-lighting/
```

Read the complete public trace in this order:

1. `00_notes.md`;
2. `01_planning_00.md` and `01_report_00.md`;
3. `01_completion_01.md` and `01_report_01.md`;
4. `02_implementation_00.md` and `02_report_00.md`;
5. `03_acceptance_00.md` and `03_report_00.md`;
6. `04_correction_00.md` and `04_report_00.md`.

Truthful status of these records:

- the M3 plan became decision-complete, although its first planner report was
  structurally malformed; `01_report_01.md` is the valid report-rendering
  completion record and preserves the frozen plan;
- the original implementation Worker reported `implementation-PASS` at
  `55e9813…`, but it was non-independent;
- fresh acceptance Session 03 correctly reported `PARTIAL`, confirming the
  stale watcher ownership defect and missing durable regressions;
- correction Session 04 reported `implementation-PASS` at `502ae75…`, fixed
  request ownership, added the required regressions, and passed its suites;
- M3 is still **not closed** because the correction changed runtime behavior
  and requires a new genuinely fresh full A1–A8 acceptance.

Do not erase or rewrite the Session 03 `PARTIAL`. It is valid historical
evidence explaining why the correction exists.

## M3 product boundary

M3 delivers only workspace-aware lighting:

- five honest RGB zones as slots, never per-key RGB;
- desktop and application context represented by those five zones;
- session-app subscription to
  `org.kde.KWin.VirtualDesktopManager` on KDE Plasma 6/Wayland;
- schema 3 with schema-2 migration;
- visible physical result through the already established M1 lighting path,
  only after separate code acceptance and Cooperator IRL observation.

M3 does not deliver M4 desktop/session management, app assignment, launching,
maximizing, per-app consume-and-inject remapping, a hold-to-layer deck,
broker ARM/exclusive grab/pass-through/input capture, autostart, production
packaging, system integration, hibernate/hybrid sleep, new G4 evidence, or
input-remapper reconfiguration.

## M3 invariants to preserve

- Logitech G213 Prodigy only, USB `046d:c336`.
- Linux/KDE Plasma 6/Wayland, C++20/Qt6/KF6/systemd.
- Existing OpenRGB SDK protocol 5 over loopback; exactly five zones.
- No per-key RGB claims and no CLI process per context change.
- Game Mode and Backlight are firmware-only; never substitute PrintScreen or
  Pause.
- Exactly eighteen measured host-remappable controls remain the M2 matrix;
  code `166` remains unresolved and is not a mapping contract.
- The session app never opens raw keyboard event nodes.
- Broker remains static/inactive for M3; no automatic ARM after restart,
  resume, crash, or reconnect.
- input-remapper is not inspected or reconfigured.
- No keylogging, raw event lines, key names, scan values, typed content,
  serials, host addresses, host keys, passwords, private paths, desktop
  IDs/names, or window captions in code logs, prompts, reports, or META.
- Do not copy G213Tray or other external source while the license question is
  unresolved.

## M3 acceptance gate that remains open

Before physical M3 observation or declaring M3 accepted, route a new fresh
independent code-acceptance Worker against immutable public candidate
`502ae75571358ec95d33c836084b5e2253850731`.

The acceptance prompt must use the next unused Worker session ordinal inside
the M3 whole, currently `05`, exchange `01`, phase `acceptance`,
`Worker session target: fresh-worker-session`, `Native planning mode: not-used`,
and a new exact pair:

```text
05_acceptance_00.md
05_report_00.md
```

The prompt must require a full fresh A1–A8 matrix, not a re-interpretation of
the correction report. At minimum it must re-check:

- public product/AP/META identities, ancestry, clean state, exact paths, and
  `./.ap/ap doctor`;
- schema 3, preserving migration, strict roles, save/backup failure behavior;
- deterministic five-slot resolution and all app-slot fallback branches;
- `WorkspaceReceiver` one-current-request ownership, stale/late replies,
  deadline, owner replacement, pause/resume, retry budget, malformed data,
  metadata bounds, duplicate snapshots, and private-bus isolation;
- controller coalescing, metadata-only changes, unchanged DesiredLighting
  suppression, and override behavior;
- QML compilation and truthful UI wiring;
- focused and full registered CTest from a fresh external build directory;
- documentation and all M2/M4/M5 non-claims.

Acceptance-PASS is valid only if every required row passes. A green
implementation suite alone is insufficient. If acceptance finds one concrete
defect, authorize at most one smallest coherent correction, then require a
full-fresh re-acceptance because the previous correction changed runtime
ownership semantics.

If and only if that acceptance is PASS, the next separate action may be a
Cooperator-owned IRL checklist from `docs/testing-m3.md`. A named M3 PASS never
closes M2, G4, M4, M5, production readiness, autostart, or device integration.

## M2 parked predecessor

The predecessor logical whole is:

```text
g213-contextdeck-input-passthrough-safety
projects/contextdesk/00/02-g213-contextdeck-input-passthrough-safety/
```

M2 is deliberately parked and is not being continued. The named live G4
slices remain accepted:

- Session 16: SSH recovery, explicit authenticated ARM, sampled pass-through,
  cutoff/death, and post-death recovery;
- Session 19: GUI ARM, held-modifier watchdog abort, no restart/re-grab, and
  post-death recovery;
- Session 22: one real suspend/resume with pre-freeze stop, conditional
  disarmed restart, STATUS-only reconnect, and post-resume typing;
- Session 23: compositor LED return, deterministic LED-write failure, all
  eighteen measured host-remappable controls, explicit DISARM, and inactive
  cleanup;
- Session 24: one bounded non-G213 input-remapper mapping preserved across one
  armed G213 trial.

Use this exact wording while M2 remains parked:

> The named live G4 slices are accepted, but the M2 logical whole remains
> open. M2 is parked with G3 host-mitigated on the authorized reference host;
> the next bounded whole is M3 workspace-aware lighting.

`G3-ACL-REPROBE-01` is host-mitigated on the authorized reference host but was
not independently re-audited by a fresh INFOSEC Worker. Do not claim whole M2,
whole G4, independent G3 closure, install/remove/rollback readiness,
production readiness, autostart safety, hibernate/hybrid-sleep support, or
general coexistence.

Do not issue the historical `27_deployment_00.md`, reopen G3, rerun named G4
slices, or ask for both SSH and a second keyboard. If a later whole reaches a
live grab, one independently demonstrated recovery route is enough.

## Repository and META operating rules

At each bootstrap use fresh container-local clones and canonical HTTPS remotes.
Verify public refs directly with `git ls-remote`; a local `origin/main` is not
proof. If cache/DNS/HTTPS fails, record the bounded symptom, use a fresh clone,
check container-local name resolution, and retry with `git -c http.version=HTTP/1.1`.
Never use a stale clone, mirror, host credential, or silent alternate commit.

For every new Worker exchange:

1. select the logical whole and exact session/exchange coordinates;
2. write the complete English prompt to its exact META path before delivery;
3. require the Worker to persist the exact received prompt and read it back;
4. require the Worker to write the complete English terminal report directly
   to the paired META report path and read it back;
5. have the Cooperator manually deliver the prompt to one Worker;
6. reconcile the public report as a claim against product, AP, and META truth;
7. commit/push the exact prompt/report pair to META only after the report
   exists, preserving all earlier trace files.

Do not create `NEXT_WORKER.md`, a session diary, an automated dispatcher, or a
speculative report. A handoff is not a Worker exchange and does not consume a
Worker session ordinal. In each trace, follow META's filename mapping:
Worker exchange `01` is stored with suffix `_00`.

The full Orchestrator may perform the META archival itself when its current
authority and credentials expose that write, or may leave the exact archival
commit to the Cooperator. It must state the actual owner and never claim a
public META commit that it did not verify.

## Worker prompt contract for the successor

Every prompt must be a complete English document containing, exactly once at
the opening, the persistent role, logical-whole identity, Worker session and
exchange ordinals, session target, phase, Worker profile, reasoning
recommendation, evidence tier, and Native planning mode. It must state:

- exact immutable product/AP/META candidate and public verification gates;
- one measurable outcome;
- positive implementation/inspection authority and exact path allowlist;
- explicit out-of-scope claims;
- mandatory reading and fail-closed preflight;
- host/device/secret/subagent restrictions;
- private-safe logging/reporting rules;
- exact prompt/report META destinations, persistence owner, readback, and
  archival owner;
- complete terminal report header, result vocabulary, evidence, next step,
  critique, closure line, and authority-expiry sentence.

Planner prompts use Native Plan Mode `required` and never implement.
Implementation prompts use Native Plan Mode `not-used`, receive the reconciled
plan, mutate only their exact allowlist, publish one candidate, and never
self-accept. Acceptance prompts are fresh and read-only against an immutable
candidate. Correctors receive one smallest concrete finding and are followed by
the required independent re-acceptance.

## No-silent-claims rule

Never claim any of the following without the separately required evidence:

- M3 acceptance-PASS before Session 05 (or its successor) full fresh PASS;
- physical five-zone visibility or device-default restoration;
- M2 or whole-G4 closure;
- production readiness, deployment, installation, rollback, autostart,
  hibernate, hybrid sleep, or system integration;
- general input-remapper coexistence;
- M4 desktop/session management, remapping, deck behavior, or M5 integration;
- per-key RGB, measured control-to-zone placement, or a fresh Session 27
  Worker result.

Keep implementation-PASS, acceptance-PASS, production acceptance, and
logical-whole closure as separate facts. Worker reports always use
`Logical-whole closure: not-closed`; only the Orchestrator can close after all
AP, Cooperator, evidence, residual-risk, and active-mutation conditions are
satisfied.

## Immediate successor checklist

On receiving this handoff, the fresh full Orchestrator should:

1. verify the actual client, model, reasoning, permission, filesystem, network,
   command, Git, and write capabilities without probing credentials;
2. read AP, root `AGENTS.md`, the current ContextDesk source/docs, and the full
   M2/M3 public trace;
3. fresh-clone and verify product `502ae75…`, AP `0cf2cff…`, and META
   `d6b067a…` against direct public refs;
4. record that M3 implementation is corrected but independent acceptance is
   pending and M2 remains parked;
5. preserve the first routing rule: for the newly selected successor whole,
   generate its Planner prompt as Worker session `01` / exchange `01`, with
   Native Plan Mode `required`, write it into that whole's META trace, and leave
   its manual delivery to the Cooperator; this does not consume M3's current
   acceptance ordinal;
6. route the separate M3 full-fresh acceptance gate (currently Worker session
   `05` / exchange `01`) before any physical M3
   claim or implementation of a successor whole; and
7. continue one bounded ContextDesk whole at a time, with all prompts and
   reports durably archived so Michal never has to copy/paste reports.

This handoff ends here. It grants no automatic Worker dispatch, physical host
operation, or logical-whole closure. All subsequent authority must be issued
by the fresh full Orchestrator under the current AP and explicit Cooperator
governance.
