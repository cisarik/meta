Logical whole identity: g213-contextdeck-workspace-aware-lighting
Worker session ordinal: 01
Worker exchange ordinal: 01
Persistent role identity: WORKER
Worker session target: fresh-worker-session
Worker session profile: Planner
Phase: planning
Task identity: CONTEXTDECK-M3-WORKSPACE-LIGHTING-PLAN
Native planning mode: required
Delivery route: manual COOPERATOR delivery to a genuinely fresh Worker session
Reasoning recommendation: High
Reasoning basis: the bounded plan must reconcile a durable schema migration,
asynchronous KDE D-Bus state, fail-safe lighting resolution, and an
IRL-visible five-zone result without crossing into M4 or M2.
Recommended context capacity: approximately 250k tokens; advisory only
Evidence tier: E3
Evidence-tier basis: schema-2-to-schema-3 durable configuration compatibility,
session-state-driven physical lighting, and a later separate fresh acceptance
are material even though this planning exchange itself is read-only.
Internal delegation: prohibited

# ContextDeck M3 — plan one visible workspace-aware five-zone lighting slice

You are a genuinely fresh WORKER assigned only to repository-grounded
implementation planning. Use the client's actual Native Plan Mode. You did not
participate in M2 implementation or acceptance and inherit no authority from
any previous session. Do not dispatch subagents.

This is a planning grant only. It authorizes no product implementation, no
host operation, no deployment, no acceptance execution, and no Git
publication. Stop at the terminal planning report.

## Planning record and Plan-to-Execution gate

Planning cycle: initial
Prior planning report: none
Targeted revision basis: none
Changed decision boundary: none
Preserved unaffected decisions: M1 five-zone/OpenRGB and application-context
architecture; parked M2 boundaries; future M4/remap/deck/M5 separation
Automatic targeted revisions used: 0

Planning layer: implementation-planning
Orchestration planning owner: ORCHESTRATOR
Worker planning scope: one aggressive but bounded M3 vertical slice that
derives five physical-zone colors from current virtual-desktop and application
context, migrates schema 2 to schema 3, and reaches the existing OpenRGB
protocol-5 lighting path
Plan disposition: approval-gated
Implementation in same Worker session: prohibited
Planning stop event: terminal planning report submitted
Execution authority event: a separate complete ORCHESTRATOR implementation
prompt whose native planning-mode value is `not-used`
Post-plan implementation session: fresh-worker-session
Maximum plan-only cycles: 1

The ORCHESTRATOR will reconcile the plan before any implementation prompt.
Native Plan approval, “continue”, retained context, or the existence of this
prompt never grants execution authority.

## Verified public candidate

Canonical product repository: `https://github.com/cisarik/contextdesk`
Expected branch: `main`
Exact product candidate:
`ca6052e816d4884ddeac9d7499a42c2aca089e7e`
Required parent:
`ab10491c49d0b6574b6953a02935a4664c39d7c2`
Required AP gitlink and checkout:
`0cf2cff483a36a4cc2254aa424a7c53bd57a97e9`

Canonical META repository: `https://github.com/cisarik/meta.git`
Verified public META baseline:
`8e5fc13acb92ef65e763cff59994d2029c865d2b`
M2 park trace:
`projects/contextdesk/00/02-g213-contextdeck-input-passthrough-safety/`
M3 trace destination:
`projects/contextdesk/00/03-g213-contextdeck-workspace-aware-lighting/`

Before planning, verify without changing repository state:

1. Product root, canonical remote, active `main`, clean worktree, exact HEAD,
   exact parent, no Git operation or lock, AP gitlink/checkout equality, and
   `./.ap/ap doctor` PASS.
2. Direct `git ls-remote` for product `main` equals the exact candidate. Do
   not fetch, pull, switch, reset, clean, stash, rebase, merge, or retarget.
3. Direct `git ls-remote` for META `main` is the exact baseline above or a
   verified later descendant. If later, inspect its changed paths and accept it
   only if it does not contradict this prompt.
4. META contains the public M2 `00_handout_03.md`, exact Session 27 pair,
   named Session 16/19/22/23/24 evidence, and Session 26 pair. Treat Session
   27 as owner-path deployment evidence, never fresh Worker acceptance.
5. The M3 trace parent and any prepared files are real directories/files and
   not symlinks. Apart from the exact M3 opening-note/prompt/report preparation
   authorized below, preserve all owner work and stop on unexplained state.

If the product candidate, parent, AP pin, public ref, park record, trace
identity, or repository state differs materially, stop before planning and
return `BLOCKED`. Do not silently use a nearby commit or an old clone.

## Authoritative continuity boundary

Use this exact statement:

“The named live G4 slices are accepted, but the M2 logical whole remains open.
M2 is parked with G3 host-mitigated on the authorized reference host; the next
bounded whole is M3 workspace-aware lighting.”

Do not continue M2. Do not issue, revive, or simulate
`27_deployment_00.md`. Preserve these named accepted boundaries without
rerunning them:

- Session 16: independently demonstrated SSH recovery, explicit authenticated
  ARM, sampled pass-through, invocation-bound cutoff/death, and post-death
  recovery.
- Session 19: GUI ARM, held-modifier watchdog abort, no restart/re-grab, and
  post-death recovery.
- Session 22: one real suspend/resume with pre-freeze stop, conditional
  disarmed restart, STATUS-only reconnect, and post-resume typing.
- Session 23: compositor LED return, deterministic LED-write failure, all
  eighteen measured host-remappable controls, explicit DISARM, and inactive
  cleanup.
- Session 24: one bounded existing non-G213 input-remapper mapping preserved
  before/during/after one armed G213 trial.

`G3-ACL-REPROBE-01` is host-mitigated on the authorized reference host but was
not independently re-audited. That residual gap is accepted for the M2 park;
it is not M3 work.

## Product boundary and invariants

M3 delivers only workspace-aware lighting:

- exactly five honest Logitech G213 RGB zones as slots, never per-key RGB;
- current virtual-desktop and application context represented by those slots;
- a session-app subscription to `org.kde.KWin.VirtualDesktopManager` on KDE
  Plasma 6/Wayland;
- schema 3 with deterministic, preserving migration from schema 2;
- a later IRL-visible result on the physical keyboard through the established
  M1 OpenRGB SDK protocol-5 loopback path.

Preserve:

- Logitech G213 Prodigy only, USB `046d:c336`;
- Linux/KDE Plasma 6/Wayland, C++20/Qt6/KF6/systemd;
- five-zone OpenRGB SDK protocol 5 over loopback with one persistent
  connection and no CLI process per context change;
- non-destructive `untouched` startup and truthful desired/connection/physical
  state separation;
- event-driven application identity through the existing KWin bridge;
- the session application never opens raw keyboard event nodes;
- broker static/inactive, no automatic ARM or re-ARM;
- input-remapper unchanged;
- exactly eighteen measured host-remappable controls; unresolved code `166`
  remains no contract;
- Game Mode and Backlight remain firmware-only and outside host remapping;
- no external source copying while the product license question remains open.

The unfilled control-to-zone map is not permission to invent physical
control placement. Determine whether it is actually required for this M3
workspace-slot slice; do not block ordinary five-zone slot lighting on a
control-to-zone map that only governs control accents.

## Explicitly out of scope

Do not plan as M3 implementation work:

- M4 virtual-desktop creation/removal/renaming, application assignment,
  launching, placement, maximizing, title fallback, or session restoration;
- per-app consume-and-inject remapping;
- a hold-to-layer deck interaction;
- M5 autostart, production packaging, installation lifecycle, or general
  system integration;
- broker IPC changes, LEASE, ARM, DISARM, exclusive grab, pass-through, input
  capture, udev, ACLs, or live input probing;
- hibernate, hybrid sleep, another G4 slice, or input-remapper reconfiguration;
- a broad architecture rewrite, plugin system, cloud, telemetry, generic
  keyboard support, or per-key claims;
- license resolution or copied implementation from G213Tray.

Future dependency classification is allowed only as a short non-authorizing
horizon section.

## Current source facts to reconcile

Verify these facts from the exact candidate rather than trusting this summary:

- `src/core/Types.h` fixes `kSchemaVersion` at 2. `Lighting` has a mode,
  optional base color, optional five `ZoneValue` entries, restore mode, and
  speed. `ZoneValue` currently contains only a color.
- `src/core/Persistence.cpp` serializes schema-2 `lighting.zones` as `null` or
  exactly five color strings, rejects unknown semantic fields and future
  schema versions, migrates schema 1 in memory, and uses atomic save plus a
  bounded backup.
- `src/core/Resolver.cpp` currently resolves temporary override, then matched
  application lighting, then global lighting.
- `src/app/SessionApplication.*` owns the current context receiver, OpenRGB
  client, controller, UI, and broker client. The new desktop subscription must
  remain a session-app concern and must not enter the broker.
- `src/app/AppController.*` recomputes effective lighting on application
  identity changes and sends `DesiredLighting` to the existing OpenRGB client.
- The KWin bridge reports application identity/inventory; M3 must not broaden
  it merely to duplicate `VirtualDesktopManager` state.
- `src/rgb/OpenRgbClient.*` and protocol tests already carry a five-color
  Direct-mode path. “Sent” is not physical readback.
- The five physical zones and Direct/gradient path were accepted IRL in M1.
  The separate `g213-control-to-zone` Results table remains unfilled and its
  control-accent writes remain inert.
- Current README/ROADMAP/AGENTS M2 status contains known documentation drift;
  the M2 park handoff and named reports outrank that drift.

## Single measurable planning outcome

Produce one decision-complete, source-grounded plan for a single later
implementation Worker to build an aggressive M3 MVP that visibly changes all
five physical zones from real desktop/application context while preserving
existing schema-2 users and M1 safety behavior.

The plan must be implementation-ready, not a multi-quarter schema study. It
must resolve all of the following:

1. **Minimal architecture and event flow.** Name the exact new or changed
   types and ownership. Define initial D-Bus snapshot plus subscription to
   `org.kde.KWin.VirtualDesktopManager`, mapping of the current desktop ID to
   bounded desktop metadata, deduplication, reconnect/service-loss behavior,
   and how desktop/app changes trigger one recomputation without polling.
2. **Fail-safe workspace state.** Define limits for desktop count/data,
   malformed or stale state, service loss, signal ordering, desktop removal,
   and current-ID-not-found. Unknown workspace state must have one explicit,
   non-destructive lighting fallback and must never trigger input behavior.
3. **Exact schema-3 contract.** Provide representative JSON for global and
   per-application settings; enumerate allowed fields, enums, bounds, and
   unknown-field behavior. Keep exactly five zone slots. Resolve the minimum
   M3 role set needed for a visible desktop + app result, avoiding speculative
   future roles unless clearly reserved without runtime semantics.
4. **Schema-2-to-schema-3 migration.** Specify exact in-memory mapping for
   every schema-2 lighting form (`untouched`, Direct with base color, Direct
   with five colors, device modes, restore mode, speed, app overrides), byte
   preservation before explicit save, save/backup behavior, failure fallback,
   and continued refusal of future schemas. Existing user-visible lighting
   must not silently change merely because the file was read.
5. **Deterministic five-zone resolver.** Define precedence among session
   overrides, workspace layout, matched app color/preset, global preset,
   desktop indicators, static/off slots, and unavailable context. State how
   the chosen workspace-aware configuration produces exactly five Direct-mode
   colors, how active/inactive desktop brightness or color is bounded, and how
   more desktops than available indicator slots degrades honestly.
6. **Small UI slice.** Name the minimum QML/controller controls and truthful
   labels needed to configure/preview five slot roles and observe the current
   desktop/app contribution. Do not redesign the whole UI.
7. **Tests and validation.** Name exact focused tests for schema migration and
   round trip, unknown/future schema rejection, slot validation, deterministic
   resolution, D-Bus snapshot/signal/reconnect logic through a testable seam,
   deduplication, over-capacity fallback, `untouched` preservation, and exact
   five-color OpenRGB output. State whether a new causal regression test is
   required for each behavior and why. Use the registered CTest suite as the
   broad gate only because a schema/resolver change can affect existing M1 and
   M2 tests.
8. **Later IRL acceptance.** Give a short, numbered, safe COOPERATOR checklist
   for switching between at least two real virtual desktops and two app
   contexts, observing all five zones, checking bridge/D-Bus loss fallback,
   and restoring device default. No raw input, grab, broker start, udev,
   suspend, autostart, or OpenRGB CLI-per-change route.
9. **One implementation slice.** Give an ordered implementation sequence,
   exact candidate changed-path allowlist selected from the ceiling below,
   validation commands, one-commit publication boundary, rollback/recovery for
   configuration behavior, and separate fresh acceptance boundary.
10. **Claim matrix.** State what implementation-PASS could establish, what a
    later fresh code acceptance could establish, what only COOPERATOR IRL eyes
    can establish, and the explicit M2/M4/remap/deck/M5/non-claims.

If one material design choice cannot be resolved from repository evidence,
return `PARTIAL` with exactly that choice and a recommended default. Do not
expand into another planning cycle by default.

## Ceiling for any later implementation changed-path allowlist

This planning prompt grants no edits. The plan must return a minimal exact
subset of this ceiling for a later separately authorized implementation:

- `CMakeLists.txt`
- `src/core/Types.h`
- `src/core/Persistence.cpp`
- `src/core/Resolver.h`
- `src/core/Resolver.cpp`
- `src/context/WorkspaceReceiver.h` (new only if justified)
- `src/context/WorkspaceReceiver.cpp` (new only if justified)
- `src/app/SessionApplication.h`
- `src/app/SessionApplication.cpp`
- `src/app/AppController.h`
- `src/app/AppController.cpp`
- `ui/OverviewPage.qml`
- `ui/ColorsPage.qml`
- `ui/LightingPresetEditor.qml`
- `ui/DiagnosticsPage.qml`
- `tests/unit/test_profile_persistence.cpp`
- `tests/unit/test_profile_resolver.cpp`
- `tests/unit/test_workspace_receiver.cpp` (new only if justified)
- `tests/unit/test_workspace_lighting.cpp` (new only if justified)
- `docs/specification.md`
- `docs/architecture.md`
- `docs/testing-m3.md` (new only if justified)
- `README.md`
- `ROADMAP.md`

Do not include or propose changes to `.ap/`, `src/broker/`, broker IPC,
`packaging/`, M2 testing/ADR files, the KWin bridge, license files, unrelated
tests/UI, generated files, dependencies, lockfiles, or host configuration.
If the plan proves one additional path essential, report `PARTIAL` and name it;
do not silently widen the ceiling.

## Read-only inspection allowlist

Read the pinned AP Worker minimum spine and, at minimum:

- `.ap/AP.md` — Semantic Authority, RF-03/RF-06/RF-12/RF-18/RF-19,
  planning/Plan-to-Execution, Worker responsibilities, stopping conditions;
- `.ap/AP_WORKER.md`;
- `.ap/PROMPT_CONTRACTS.md` — Worker report header, Planning Record,
  Worker Session Target, exchange/trace contract, validation ladder, delivery
  record, Session-and-Mode, Plan-to-Execution;
- `AGENTS.md`;
- `README.md`;
- `ROADMAP.md`, especially the M1 and M3 sections;
- `docs/architecture.md`;
- `docs/specification.md`;
- `docs/operations.md` only for the existing M1 lighting boundary;
- `docs/testing.md` for current M1 IRL lighting behavior;
- `docs/testing-m2.md` only to preserve the no-M2 boundary;
- `docs/hardware/g213-control-matrix.md`;
- `docs/hardware/g213-zone-map.md`;
- all current source/test/QML files named in the source-facts and later-path
  ceiling sections;
- public M2 `00_handout_03.md`, Session 27 report, and the named report headers
  needed to verify continuity.

Read-only inspection of directly included headers/sources needed to understand
those files is allowed. Do not inspect device nodes, private configuration,
logs, home paths, other repositories, credentials, or unrelated host state.

## Authority and side-effect boundary

Product source mutation: prohibited
Product Git mutation: prohibited
Product commit/push: prohibited
AP mutation/update: prohibited
Host mutation: prohibited
Host service operation: prohibited
Broker start/stop/ARM/grab/probe: prohibited
Input-remapper operation: prohibited
OpenRGB/KWin installation or runtime operation: prohibited
Physical-device probing: prohibited
Dependency installation/update: prohibited
Secrets/credentials/private data: prohibited
Network authority: direct public `git ls-remote` identity checks against the
two canonical HTTPS remotes only; no alternate mirror and no external research
Side effects: read-only inspection plus the exact META trace-file preparation
below only

No raw input, raw event lines, key names, scan values, typed content, serials,
host addresses, host keys, passwords, private paths, window captions, or
unredacted tool logs may appear in the prompt/report/trace. Use only semantic
state and public repository-relative paths.

No recovery route is required because this exchange must not start, open,
probe, or grab a device. If any step would cross that boundary, stop. For a
future live-grab whole, one independently demonstrated route is enough: SSH
from another device or a second physical keyboard, not both.

## Validation ladder

Validation ladder: selected
Inspection and provenance: required
Existing focused tests: inspect current persistence, resolver, OpenRGB protocol,
session-app/controller, and M1 lighting test coverage; do not execute tests in
this planning exchange unless they are already available without creating build
state
Affected tests: propose exact schema-3/workspace tests in the terminal plan
New causal regression: required in the later implementation for schema
migration, workspace-state resolution, and five-slot composition because those
behaviors do not exist in the candidate
Broad or full suite: required-because schema/core resolver changes can regress
the existing registered M1/M2 CTest surface
Runtime or testbed: not-used in this planning exchange
Independent acceptance: required-separate-fresh-worker after implementation

The planning result is non-implementation evidence. It does not accept code or
physical behavior.

## Trace persistence contract

External trace disposition: configured
Trace discovery: META README and the exact M3 destination below
Trace project key: contextdesk
Trace logical-whole projection identity: 03-g213-contextdeck-workspace-aware-lighting
Trace authority: historical-evidence-only
Trace archival owner: COOPERATOR
Trace visibility: public
Trace companion outcome: report
Trace self-granted status: none

Cooperator delivery / trace destination: configured
Downloadable prompt filename: 01_planning_00.md
Destination path: projects/contextdesk/00/03-g213-contextdeck-workspace-aware-lighting/
Report filename: 01_report_00.md
Prompt authorship: ORCHESTRATOR
Prompt persistence owner: WORKER
Report persistence owner: WORKER
Git publication owner: COOPERATOR
Archival: wait-for-report

The COOPERATOR establishes the exact M3 trace directory and its
Orchestrator-authored `00_notes.md` before delivery. The WORKER may prepare only
these two exchange files in META:

- `projects/contextdesk/00/03-g213-contextdeck-workspace-aware-lighting/01_planning_00.md`
- `projects/contextdesk/00/03-g213-contextdeck-workspace-aware-lighting/01_report_00.md`

Before substantive planning, if the prompt file is absent, persist the exact
received prompt bytes at the first path; if it exists, require exact byte
identity. Read it back completely. Stop on a non-identical collision. Do not
alter `00_notes.md`, create a handout, or write any other META path.

After planning, write the complete terminal report first to the report path,
read it back completely, and verify the header, coordinates, content, and
filename. Do not overwrite a non-identical existing report. Do not stage,
commit, push, pull, merge, rebase, or otherwise mutate META Git history or
refs. The COOPERATOR archives the exact prompt/report pair together in one
first-add commit only after the report exists.

## Terminal report contract

The report must begin exactly:

`### Report for ORCHESTRATOR_CHAT`

It must echo the opening persistent-role and three coordinate fields exactly
once, with their values unchanged.

Include:

- status: `PASS`, `PARTIAL`, or `BLOCKED`;
- phase-qualified result: `not-applicable` (planning is not implementation);
- start and end product commit (expected unchanged exact candidate);
- verified product/AP/META identities and clean-state result;
- source-grounded current-state findings;
- the complete decision-ready plan covering all ten measurable-outcome items;
- the exact minimal later implementation changed-path allowlist;
- the E3 implementation/acceptance evidence envelope and validation rationale;
- explicit statement that the plan made no product, AP, host, service,
  device, dependency, or Git publication mutation;
- trace prompt/report persistence and complete-readback result;
- deviations, risks, unresolved decisions, and missing evidence;
- exactly one smallest next step: ORCHESTRATOR reconciliation and, only if
  accepted, a separate implementation Worker prompt;
- exactly one `Report justification: new-evidence`;
- `Resolved Execution Issues / Near-Misses: none` or a complete entry;
- `Pre-Existing Failure Classification: none` or a complete entry;
- compact critique:

  `Orchestration critique:`

  `MEASURED: none | <verified finding; evidence; effect; smallest correction>`

  `LEAD: none | <unverified possibility; cheapest useful check>`

- exactly one `Logical-whole closure: not-closed` line;
- the exact authority-expiry sentence below.

Explicit non-claims in the report:

- no implementation-PASS, acceptance-PASS, publication-PASS, deployment-PASS,
  production readiness, M2/G4 closure, G3 independent re-audit, autostart,
  hibernate/hybrid sleep, general input-remapper coexistence, M4 session
  management, remapping, deck-layer behavior, M5 integration, per-key RGB, or
  physical hardware acceptance;
- no claim that Session 27 was a fresh Worker result;
- no claim that an unfilled control-to-zone map is measured.

Authority for this Worker expires at this terminal report.

## Stop conditions

Stop and report `BLOCKED` before substantive planning if the session is not
genuinely fresh, Native Plan Mode is not actually enabled, coordinates or
authority are contradictory, product/AP/META identity fails, the worktree has
unexplained changes, the M2 park record or Session 27 pair is missing, the M3
trace path collides or is unsafe, required repository evidence cannot be read,
or completing the plan would require a prohibited mutation, private data,
host/device access, an additional path outside the ceiling, or subagents.

Stop with `PARTIAL` if the repository can be analyzed but one material design
choice or required evidence item remains unresolved. Do not implement, probe
the host, or silently widen scope to force PASS.

Stop with `PASS` only when the complete decision-ready plan and exact later
implementation boundary are persisted and read back. Then submit the terminal
report and do no further work under this grant.
